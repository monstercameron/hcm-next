# Generated by tools/gen/schemaflux (TOOL-004). DO NOT EDIT BY HAND.
#
# Re-run: go test -run TestSchemaFluxOfflineFixture ./tools/gen/schemaflux/...
schema_version: 1
todo: TOOL-004
decision: PROTOC_FALLBACK
generator: tools/gen/schemaflux (native deterministic Go compiler; no protoc
  invocation needed because these are structured YAML definitions, not .proto
  messages — see doc.go)
schemaflux:
  package: github.com/monstercameron/schemaflux
  version: v1.2.0
  qualification_result: DISQUALIFIED
  reason: >
    Every public entry point (Extracting, Generating, Transforming, Choosing,
    Summarizing, and roughly ninety more builders in schemaflux.go) is a typed
    LLM operation: it calls a configured model provider (OpenAI by default)
    through internal/llm, which builds a plain &http.Client{} with no offline
    path once a real provider is selected. There is no function whose contract
    is "compile a structured definition file into a Go registry, markdown
    catalog or JSON fixture without a model call." The library's own
    deliberate offline stand-in (SCHEMAFLUX_PROVIDER=local /
    Client.WithMockProvider) answers every operation with shape-correct but,
    by internal/llm/mockshape.go's own words, "obviously synthetic" filler
    unrelated to the real input; internal/api/client/client.go separately
    documents that falling back to it silently (no API key, no explicit
    request) would be indistinguishable from a working deployment until
    someone reads the output, which is why it must be asked for explicitly.
    Either way it rules SchemaFlux out for real generation.
  evidence:
    - "TestSchemaFluxOfflineFixture/SchemaFluxRequiresNetworkForRealGeneration proves a non-mock generation attempt dials out through http.DefaultTransport (blocked by a test transport; the attempt still counts as a dial, observed making 4 attempts with backoff before failing)."
    - "TestSchemaFluxOfflineFixture/SchemaFluxOfflineStandInIsUnusablePlaceholderText proves the mock/local provider path answers with canned, shape-correct filler (e.g. {'result': 'transformed data', 'success': true}) that never reflects the real input, not structured content derived from it."
    - "schemafluxtest's cassette Player replays a previously recorded model answer; it cannot originate one, so it is a unit-test double for code that calls SchemaFlux, not a definition compiler."
  fallback:
    selected: true
    kind: PROTOC_STYLE_DETERMINISTIC_GO_COMPILER
    description: >
      Definitions stay declared as structured source (the fourteen YAML files
      under schema/schemaflux/business_intents/v1, matched against the
      Protobuf hcmnext.intents.v1.IntentDefinition message shape) and are
      compiled by a small offline, model-free Go program in this package
      (loader.go, resolve.go, generate.go) — the same "generation is
      deterministic tooling, not an AI call" posture the constitution asks
      protoc + protoc-gen-go to provide for Protobuf. The registry ships as a
      compiled-in Go table either way (capability-registry-and-lifecycle.md
      BOOTSTRAP profile); the hand-authored table in
      internal/intent/definitions already satisfies that requirement today
      (TOOL-002/TOOL-003) and this package cross-checks against it rather than
      replacing it.
fixture:
  definitions_compiled: 14
  capability_manifest_entries_compiled: 10
  network_calls_during_fallback_generation: 0
  runs_compared: 2
  byte_identical: true
  cross_check_mismatches_against_internal_intent_definitions: 0
  content_digest: sha256:f9c5ecad46383e2c16778ff30bbc22e59364ce1f2b32b2c09238ad4816362cac
  content_digest_scope: >
    sha256 over the length-prefixed concatenation of the generated registry Go
    source, markdown catalog and JSON fixture bytes, produced by
    tools/gen/schemaflux from the pinned inputs
    (schema/schemaflux/business_intents/v1/*.yaml plus
    tools/gen/schemaflux/testdata/capability_manifest.yaml). Recomputed fresh
    by TestSchemaFluxOfflineFixture on every run; not expected to survive an
    unrelated cosmetic reformat of this file by a formatter.
