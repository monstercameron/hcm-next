# Shared Workflow Conventions

Use this folder for workflow configuration helpers, shared manifest conventions, reusable schema fragments, and cross-workflow notes that do not belong to a single workflow.

Runtime code that is imported by applications belongs in `src/platform/workflow-runtime`; this folder is for workflow-owned definitions and design assets.
