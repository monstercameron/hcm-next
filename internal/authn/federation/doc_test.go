package federation_test

import "testing"

// TestPackageDocCompiles is a placeholder proving the package (and its
// doc.go package comment) compiles, matching
// internal/platform/configregistry's own doc_test.go convention. The
// substantive behavior doc.go describes is exercised by registry_test.go.
func TestPackageDocCompiles(t *testing.T) {
	t.Parallel()
}
