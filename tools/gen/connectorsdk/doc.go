// Package connectorsdk renders the small, deterministic portion of a typed
// connector SDK from a schema manifest. It deliberately emits source only;
// callers choose when and where generated files are written.
package connectorsdk
