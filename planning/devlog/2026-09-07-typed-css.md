# Devlog — 2026-09-07: the typed-CSS migration

Running log of the migration that removed every string-CSS stylesheet from
the repo (GWC typed constructors throughout), on top of HEAD `b62541d`
(WEB-040 launcher). Written by the orchestrating session; every result below
was re-verified by the orchestrator before it was recorded (`go test
-count=1` on the named packages, windows port of Go via
`C:\Program Files\Go\bin\go.exe`, branch `plan-revision-2026-09-02`).
Entries are in the order the work happened.

## 1. Starting point

- The user demanded typed CSS with no string CSS anywhere in the repo:
  `internal/humanwork/productui/styles.go` held 67 string consts plus
  emitters in `appearance.go`/`theme.go`/`focus.go`, and outer sites in
  `internal/humanwork/workspace`, `tools/uxqual/ssrshell`,
  `tools/uxqual/tokens`, and `tools/uxqual/render/{journey,ssr,gwc}`.
- Proven pattern from the earlier 8-sheet pilot: per-package harness
  (`buildTypedSheet` Reset-declare-Harvest under mutex, `declareGlobal`,
  `mediaRule`, `hoverRule`) + `Raw` only for var() fallbacks, logical props,
  system colors, color-mix, and at-rule headers GWC cannot construct.

## 2. Wave 1 — productui platform sheet (six parallel workers, one const each batch of ~11)

- Workers wrote `typed_mig_A..F.go` (new files only, verified each with
  per-const semantic cssdiff against `git show HEAD:` originals).
- Orchestrator integration fixes that mattered:
  - Workers duplicated `focus.go` bridge text into two files; moved it once
    into `focus.go` as `declareJourneyFocusBridge`/`declareFocusStyles` and
    rewired both callers. Deleted two dead composer wrappers
    (`ThemeCoverageStylesheet`, `themeCoverageBaseStylesStylesheet`) that
    would have been a second source of truth.
  - Workers omitted `@starting-style` and `@supports` (no GWC constructor);
    restored both via a documented `atRule` helper with typed inners and
    exact original positions (`@supports(overflow:clip)` must stay after the
    base `.people-directory` rule for the cascade).
  - Caught a real shimmer-linkage break: `loadingProxyStyles` referenced
    plain `hcm-shimmer` while the frames moved to a hashed name. Fixed with
    `Keyframes()+Animation()` longhands (same body ⇒ same hash ⇒ deduped).
  - Caught and fixed my own focus-selector corruption (`\"` inside backticks
    emits literal backslashes): 20 replacements, selectors now plain-quoted.
- Proof: dumped full `Stylesheet()` old (HEAD worktree) vs new; ordered
  semantic diff adjudicated hunk by hunk. Remaining deltas are all canonical
  serializer behavior: trailing `;`, sorted declarations, `@media ` spacing,
  `1 / -1` grid spacing, `background-color`, longhand expansion, adjacent
  group splits, hashed keyframes (bodies identical, linkage verified).
- `TestTypedKeyframesNamesResolve` added: every `animation-name` and every
  `hcm-*` shorthand reference must resolve to an emitted `@keyframes` block.
- ~90 test oracles re-anchored to canonical emission, each with raw-output
  proof (never assumed). Package suite green.

## 3. Wave 2 — outer sites (three workers: productui emitters, workspace, uxqual)

- `Theme.CSS()` and `customerThemeStylesheet()` converted with
  **byte-identical** output (pinned digest `8ac7d3…` still passes unchanged;
  customer sheet semantically clean per cssdiff).
- Workspace login sheet: 0-hunk ordered diff (identical). Product shell is
  pure composition (`journey.Stylesheet() + productui.Stylesheet()`), so its
  proof inherits.
- Tokens `WorkspaceCSS()`: 130/130 rules, zero semantic hunks; mode-contract
  SHA re-pinned (`d639…` → `d9f2…`) as the golden-review mechanism intends.
- ssrshell goldens: stylesheet-bytes-only change, digest re-pinned.
  Workspace `ux002_document.golden`: post-`</style>` HTML byte-identical;
  only the style block changed.
- Caught a real journey defect: 15 `Raw("animation", "<name>-<hash> …")`
  shorthands hardcoded content hashes (silent breakage on any frames edit).
  14 converted to `Keyframes()+Animation()` longhands; the one irreducible
  dual-animation shorthand stays Raw with an explanatory comment, guarded by
  new `TestJourneyKeyframesResolve` (checks both shorthand names).
- Preserved latent quirk deliberately: eight `@media(print)` blocks never
  match (unknown media feature, not the print type). They stay dead via
  `@media (print)`; converting them to live `@media print` would silently
  change print rendering with no visual gate available. Logged, not fixed.

## 4. Verification

- Green: `internal/humanwork/...` (full tree), `tools/uxqual/...` (full
  tree), `go vet` on both trees, `gofmt` clean on touched dirs.
- `planning/test_coverage_root.md`: +19 rows for the new typed files (all in
  tested packages). The log predates WEB-040 and its generator script is not
  in the repo, so rows were appended in-format and header counts bumped by
  the same delta; a full regeneration is still owed whenever the generator
  is restored.
- CSP posture unchanged: all style hashes derive from the emitting funcs;
  only pinned _values_ (mode SHA, ssrshell digest) were re-pinned after
  proof, never to make a failing test pass.

## 6. WEB-044 — meaningful breadcrumb resolution

- New `breadcrumb.go`: `ResolveBreadcrumbs` walks the canonical
  PageDefinitions ParentNav chain (registry stays the single hierarchy
  authority), filters ancestors through the same
  `navigationDestinationAuthorized` projection the search and scope links
  use, names the selected worker on a profile via `exactPerson`, returns nil
  for unknown pages. `Breadcrumbs` renders a localized nav landmark (new
  `shell.breadcrumbs` key in en-US/de-DE/ar) with an ordered list,
  software-routed ancestor links, an `aria-current="page"` current marker,
  and aria-hidden `/` separators; single generic crumbs render nothing so
  the bar never duplicates the H1. Mounted at the top of `pageHeader`.
- New typed sheet `BreadcrumbStylesheet` (flex-wrap trail, accent links,
  semibold current, muted separators; Raw only for the logical
  `margin-block`, `list-style` which has no typed form, and the
  `text-decoration` hover which matches the preflight precedent). It is
  concatenated BEFORE ContextSwitcher/darkMode so the dark-mode sheet stays
  the final cascade layer — the first attempt appended it last and broke
  the two HasSuffix cascade-boundary tests.
- Two older tests assumed exactly one `aria-current="page"` per document
  (Studio nav leaf, person People-parent). Breadcrumbs legitimately add a
  second marker, so both assertions were re-scoped to a new
  `primaryNavAriaCurrentCount` helper with explicit breadcrumb-marker
  assertions added; no contract was weakened.
- RED-first matrix `TestTodo_WEB_044` (+Golden/Browser/Conformance) green;
  full productui package green; `go vet` and `gofmt` clean; tools/uxqual Go
  suites 26/26 green. Playwright browser gate 18/20: the 2
  accessible-name failures reproduce identically on clean-HEAD fixtures
  (verified via a native-path HEAD worktree with fixture swap), so they are
  pre-existing and unrelated.
- Partial: the sanctioned gate covers desktop-1280 and 320px light only;
  no dedicated 390px or dark-mode breadcrumb render was driven (the CSS is
  flex-wrap with logical properties and no fixed widths, and the 320px
  reflow spec passed). No string CSS was added.

## 7. WEB-045 — canonical page-identity header

- New `header_identity.go` (named to stay out of the `page_*` route-adapter
  markup rule): `ResolvePageIdentity` + `PageIdentityHeader`. The old inline
  `pageHeader` in `shell.go` is deleted; `pageFrame` calls the canonical
  component. Resolution mirrors `ApplyLocale` (registry TitleKey/SubtitleKey,
  Home principal greeting, Home fallback for unknown pages) but reads the
  registry directly instead of mutable view fields.
- Settings is a universal support page under role visibility, so the
  scope-denial test uses a permissions projection that withholds it (same
  pattern as the WEB-040 launcher denial test).
- WEB-037/WEB-039 goldens re-pinned only after a strip-probe proved the
  single `data-hcm-page="home"` attribute is the entire byte delta (digest
  recomputed with the attribute removed equals the old pin, count=1).
- Matrix green; package green; vet clean; `gofmt` reformatted one blank
  line in `shell.go`. uxqual fixtures regenerated; browser gate 18/20 with
  the same 2 pre-existing accessible-name failures. The live dev cell does
  not render productui (separate workspace tree), so no live visual for
  this header; SSR markup inspected by eye before pinning.

## 8. WEB-046 — contextual utility drawer

- New `utility_drawer.go`: `UtilityDrawerItem/Section/Props`,
  `utilityDrawerSections` (Related from ParentNav parent + siblings,
  Actions from `filteredPersonWorkflows` with `LaunchHref(person.ID)`),
  `UtilityDrawer` component (trigger with dialog semantics, hidden dialog
  with close button, fragment when empty), `utilityDrawer(view)` shell
  wrapper mounted in `header-navigation-tools` after the launcher.
- Sibling order follows registry iteration (deterministic, covered by a
  DeepEqual determinism assertion). Person actions reuse the exact
  create-grant gate from `personWorkflowLauncherProps`.
- New typed sheet `UtilityDrawerStylesheet` (trigger/dialog/section/item
  styles mirroring the launcher tokens; logical inset properties for RTL),
  concatenated before ContextSwitcher so dark mode stays final. Five new
  `utility_drawer.*` catalog keys in en-US/de-DE/ar.
- `html.Div(props, close, sections...)` does not compile (spread after a
  fixed arg in this GWC signature), so children are composed into a slice
  first. `hasAttr` helper already existed in `accessibility_test.go` —
  reused, duplicate removed.
- Matrix green first try except the golden pin; full package green with no
  golden fallout; vet clean; `gofmt` realigned the catalog block.
  Fixtures regenerated; browser gate 18/20 with the same 2 pre-existing
  failures. SSR markup inspected by eye before pinning (`0f4db16a…`).

## 9. WEB-047 — responsive mobile shell

- Trigger labels for the launcher and the drawer now render inside
  `action-launcher-label` / `utility-drawer-label` spans; the new typed
  `MobileShellStylesheet` hides exactly those spans at `max-width:430px`
  (canonical emission `@media (max-width:430px){…{display:none;}}` — the
  first RED assertion missed the space and trailing semicolon and was
  corrected to the serializer's canonical form, not the other way round).
  `MobileShellStylesheet` is concatenated before ContextSwitcher so dark
  mode stays the final cascade layer.
- Fallout re-pinned only after strip-proofs recomputed all four digests
  with the label-span wrap removed (WEB-037 `63db306e…`, WEB-039
  `5f2f905e…`, WEB-040 `ec01684d…`, WEB-046 `50ec45c5…`); the span wrap is
  the entire delta in each. Note the WEB-040 pin lives in sibling
  uncommitted work (`web040_action_launcher_test.go`) — re-pinned as
  required fallout, flagged here.
- Matrix green; package green; vet/fmt clean; fixtures regenerated;
  browser gate 18/20 with the same 2 pre-existing failures. 320px overflow
  stays covered by the existing a11y reflow spec (green).

## 10. WEB-048 — shell keyboard and landmark navigation

- New `landmarks.go`: `ShellLandmark`, `ResolveShellLandmarks` (document
  order: complementary, main nav, support nav when projected, breadcrumb
  nav when the trail renders, main with the identity title), and
  `drawerEscapeCloses` — one predicate owning the Escape contract for all
  three dialog surfaces. The drawer dialog now handles Escape like search
  and launcher (handler on the dialog div, same state flip).
- `Breadcrumbs` visibility logic extracted to `showBreadcrumbTrail` so the
  inventory and the renderer share it; behavior verified identical by the
  untouched WEB-044 suite.
- New `tools/uxqual/browser/web048_keyboard_landmarks.spec.mjs` (6/6
  green): no positive tabindex, landmark roles in the a11y tree, 390px
  reflow. First draft asserted skip-link tab stops against the workspace
  fixtures, which contain no skip link (different tree — the link lives in
  productui, proven in Go instead); rescoped honestly rather than forcing
  the fixture to change.
- Support nav resolves only behind a projection (plain testView has none)
  — the matrix covers both shapes. Matrix green; package green; vet/fmt
  clean. Full browser gate 24 passed + same 2 pre-existing failures.

## 11. WEB-049 — tenant-federation entry experience

- New `federation_entry.go`: `FederationEntry` (plain data — tenant,
  issuer, protocol, assurance, href), `FederationEntryProps`,
  `FederationEntryList` (labelled gate section, H1 keeping main's
  labelledby valid, tenant groups sorted deterministically, issuer links
  with protocol metadata, `unavailablePanel` empty state). New
  `View.FederationEntries` projection field, documented as
  registry-derived. Shell gates on empty tenant: gate replaces content and
  suppresses the tenant-scoped page header; sidebar/nav render their
  natural empty states.
- Deliberately no `internal/authn/*` import in presentation: the layering
  rule (domain truth in owning packages) plus the todo's own no-second-
  authority clause mean the frontend receives entries and renders them.
  Protocol vocabulary passes through unvalidated — the server owns it.
- New typed `FederationEntryStylesheet` before ContextSwitcher; four new
  `federation_entry.*` keys in en-US/de-DE/ar. Matrix green except the pin;
  package green with no fallout; vet clean; `gofmt` realigned the catalog.
  Fixtures regenerated; browser gate 24 passed + same 2 pre-existing
  failures. SSR markup inspected by eye before pinning (`b5f9cf65…`).

## 12. WEB-050 — accessible sign-in recovery

- `RecoveryOption` data + `View.RecoveryOptions` projection,
  `validRecoveryHref` (workspace-relative, https, mailto; everything else
  dropped), `federationRecoverySection` (labelled section, zero bytes when
  no safe option), wired into both gate branches. Recovery links are plain
  same-tab anchors — no software routing for off-shell destinations, no
  target/rel surface at all.
- Protocol vocabulary stays server-owned; the frontend validates schemes,
  never issuers. One new `federation_entry.recovery_title` key in three
  locales; two new typed rules. WEB-049 golden unchanged (empty section is
  zero bytes). Matrix green except the pin; package green; vet clean;
  `gofmt` realigned the catalog. Fixtures regenerated; browser gate 24
  passed + same 2 pre-existing failures. Section markup inspected by eye
  before pinning (`8e43b2bc…`).

## 13. WEB-051 — accessible-authentication conformance

- Proof-only todo: `web051_auth_conformance_test.go` sweeps the gate in 3
  states (full, entries-only, empty) × 3 locales through 8 conformance
  properties, plus gate-document shell honesty and the shared scheme
  policy. The sweep passed on current code with zero findings — no
  production change, no re-pins, no fallout. Matrix digest `6380d315…`.
  Package green; vet/fmt clean; fixtures regenerated; browser gate 24
  passed + same 2 pre-existing failures.

## 14. WEB-052 — session-expiry warning

- New `session_warning.go`: `SessionWarningProps` (server detail text +
  re-auth href), `SessionWarning` (labelled section, detail, actions row,
  page-load dismiss state), `sessionWarning(view)` shell wrapper mounted
  between topbar and shell-grid, nil projection rendering a fragment.
  New nilable `View.SessionWarning` field. No expiry computation anywhere
  in presentation by construction — there is nothing to threshold.
- Three new `session_expiry.*` keys in three locales; new typed
  `SessionWarningStylesheet` before ContextSwitcher. Matrix green except
  the pin; package green with no fallout; vet clean; `gofmt` touched
  `i18n.go` and `session_warning.go` (alignment). Fixtures regenerated;
  browser gate 24 passed + same 2 pre-existing failures. Banner markup
  inspected by eye before pinning (`93253689…`).

## 15. WEB-053 — reauthentication work restoration

- `reauthResumeHref(view, base)` in `session_warning.go`, applied in the
  `sessionWarning(view)` wrapper so the pure component and its golden stay
  untouched. Resume comes from `currentPageHref` (the shell's own
  primitive); no second record of user location.
- One real regression caught by the suite: WEB-052 asserted the exact
  re-auth href, which now carries `?resume=…` by design. Evolved that
  assertion to parse the href and check the destination path instead of
  weakening or deleting it.
- Matrix green; package green; vet/fmt clean; fixtures regenerated;
  browser gate 24 passed + same 2 pre-existing failures. No new copy, no
  new CSS, no re-pins.

## 16. WEB-054 — risk-bound step-up authentication

- New `step_up.go`: `StepUpChallengeProps` (server action label, reason,
  challenge href), `StepUpChallenge` (labelled section, action + reason,
  challenge link gated by the shared destination policy, dismiss),
  `stepUpChallenge(view)` mounted after `sessionWarning`. New nilable
  `View.StepUpChallenge`. On-accent text uses the `on-brand` token per
  codebase convention (first draft used a literal white).
- Three new `step_up.*` keys in three locales; new typed `StepUpStylesheet`
  before ContextSwitcher. Matrix green except the pin; package green with
  no fallout; vet clean; `gofmt` touched `i18n.go` and `step_up.go`.
  Fixtures regenerated; browser gate 24 passed + same 2 pre-existing
  failures. Prompt markup inspected by eye before pinning (`24c5c9f1…`).

## 17. WEB-055 — delegation-context selector

- New `delegation_selector.go`: `delegationSelectorOptions` (valid
  delegated non-current grants via the switcher's own normalization),
  `DelegationSelector` (details/summary + PopoverSurface, shared
  `contextOptionButton` renderer, status role), `delegationSelectorSlot`
  mounted after the switcher. Reuse is structural, not copied: selection
  buttons travel `SwitchAuthorityContext` with the same error contract.
- One new `delegation_selector.label` key in three locales; new typed
  `DelegationSelectorStylesheet` before ContextSwitcher. Matrix green
  except the pin; package green with no fallout (WEB-038 untouched and
  green); vet clean; `gofmt` realigned the catalog. Fixtures regenerated
  (byte-identical workspace docs).
- Environment note: Windows interop died mid-session, so verification
  moved to a native Linux Go 1.26.3 toolchain (`~/sdk/go`); the suite
  passes identically. No Chromium can launch anywhere (Windows side dead,
  Linux side missing system libs, no sudo), so the browser gate is
  deferred — recorded here, not skipped silently. Selector markup
  inspected by eye before pinning (`fec579a1…`).

## 18. WEB-056 — persistent acting-authority banner

- New `authority_banner.go`: `ActingAuthorityBanner` (labelled section
  with the switcher's own `currentContextDetail`, nil on invalid
  projection), `actingAuthorityBanner` slot mounted after the step-up
  prompt. Deliberate reading of "persistent": renders for every valid
  projection, own authority included — the strip exposes the authority
  acted under, not just delegations. No new view field, no second source.
- One new `acting_authority.title` key in three locales; new typed
  `AuthorityBannerStylesheet` after StepUp. Matrix green except the pin;
  package green with no fallout (NewView leaves the projection empty, so
  existing shell goldens are untouched); vet/fmt clean. Golden pinned from
  eye-inspected markup (`de98e9a1…`).
- Fixture note: regen dropped a stale inline `<style>` serialization —
  the committed renders predated the tokens typed-CSS migration
  (WorkspaceCSS itself is intact at 14157 bytes; only the emission is now
  canonical). Refreshed fixtures kept; `tools/uxqual/...` suite green, no
  Go test pins those files. Browser gate deferred, same as WEB-055.

## 19. WEB-057 — break-glass activation experience

- New `break_glass.go`: `BreakGlassActivation` (labelled section —
  incident row, reason, labelled capabilities list, window row, activate
  link behind `validRecoveryHref`, page-load dismiss), nil on blank
  incident; `breakGlassActivation` slot from a new nilable
  `View.BreakGlassActivation`, mounted after the authority banner. Empty
  reason/capability/window projections omit their rows. The prompt mints
  no grant — it names the server's activation offer and destination.
- Six new `break_glass.*` keys in three locales; new typed
  `BreakGlassStylesheet` after AuthorityBanner. Matrix green except the
  pin; package green with no fallout (nil projection is a fragment);
  vet/fmt clean. Golden pinned from eye-inspected markup (`3504c15c…`).
- Browser gate deferred, same as WEB-055/056.

## 20. WEB-058 — safe view-as policy simulation

- New `policy_simulation.go`: `PolicySimulation` (simulation notice,
  subject/outcome rows, reason, labelled rules list, versions and
  evidence rows, exit link behind `validRecoveryHref`), nil on blank
  subject; `policySimulation` slot from a new nilable
  `View.PolicySimulation`, mounted after break-glass. No dismiss control
  on purpose: a local dismiss would lie about a simulation still in
  force. Blank reason/rule/version/evidence projections omit their rows.
- Ten new `view_as.*` keys in three locales; new typed
  `PolicySimulationStylesheet` after BreakGlass. Matrix green except the
  pin; package green with no fallout (nil projection is a fragment);
  vet/fmt clean. Golden pinned from eye-inspected markup (`21058a5d…`).
- Browser gate deferred, same as WEB-055–057.

## 21. WEB-059 — logout and revocation convergence

- New `signed_out.go`: `SignedOut` (detail, labelled revoked-grants
  list, sign-in link behind `validRecoveryHref`, never dismissible),
  `signedOutState` predicate checked first by all five strip slots plus
  the switcher and delegation slots, and a shell branch making the panel
  the content (no heading) when projected. New nilable
  `View.SignedOut`; presentation revokes nothing itself.
- Three new `signed_out.*` keys in three locales; new typed
  `SignedOutStylesheet`. Matrix green except the pin; package green —
  the guards touch six existing files with zero fallout (nil state is a
  no-op); vet/fmt clean. Golden pinned from eye-inspected markup
  (`21b09c50…`).
- Test lesson (second occurrence): raw-string absence assertions
  false-positive on the inlined stylesheet — the negative checks now
  walk body class attributes and element ids. Browser gate deferred,
  same as WEB-055–058.

## 22. WEB-060 — authentication telemetry privacy

- Scope decision (asked, answered): no telemetry emitter, secret, or
  password field exists anywhere in productui, and composed URLs come
  from safe view fields — so this is a policy-enforcement sweep with one
  real extension, not a scrubber. Chose proof-plus-policy over inventing
  a DLP heuristic (which would be a second source).
- `validRecoveryHref` now denies credential query params via
  `credentialQueryKeys` + `recoveryHrefCarriesCredentials` (exact
  case-insensitive key match; `mytoken` still passes; unparsable keeps
  its scheme verdict; mailto exempt). One policy, all auth surfaces
  inherit it — step-up, break-glass, simulation exit, sign-in.
- `TestTodo_WEB_060` sweeps session/entry/signed-out/credential-hrefs
  configurations × 3 locales with an independent-superset oracle (test
  denylist ⊃ prod denylist, so a prod miss fails instead of passing
  itself). RED demonstrated on `?token=` rendering pre-fix; green
  post-fix with tainted links dropped and prompts kept. Matrix digest
  pinned (`348ebb8b…`).
- Honest correction in flight: the first RED fired on the global-search
  GET form's hidden inputs — audited, benign UI state (locale/nav/
  menu_q/favorites), so the invariant now governs hidden-input NAMES
  (allowlist of the four presentation owns) instead of banning hidden
  inputs. No production change outside the policy; package green, no
  fallout; vet/fmt clean (gofmt realigned the new test's table).
- Browser gate deferred, same as WEB-055–059.

## 23. WEB-061 — authorized presentation projection

- New `authorized_presentation.go`: `PresentationEffect` display
  vocabulary plus `ProjectAuthorizedValue` / `ProjectAuthorizedRecord`
  pure projectors consuming server verdicts as data — deliberately no
  `internal/trust` import (the architecture keeps evaluation out of
  presentation; the WHY clause's "no second source" is satisfied by
  construction). Unknown effects, missing verdicts, and nil field maps
  all fail closed to withholding; inputs never mutated; deterministic.
- One new `provenance.value.withheld` key in three locales (no withheld
  copy existed). Matrix covers 9 value cases × 3 locales plus record
  cases; digest pinned (`068f8696…`). Browser test proves evidence
  identifiers (tenant, delegator, incident, rules, versions, evidence
  ref) render byte-identically across locales. No markup, no CSS, no
  shell changes — package green with zero fallout; vet/fmt clean.
- This is the foundation WEB-062 (discoverability) builds on; page
  wiring belongs there, not here.
- Browser gate deferred, same as WEB-055–060.

## 24. WEB-062 — resource discoverability decisions

- Gap found: `globalSearchItems` advertised every person and work record
  gated only on page-level navigation — a non-disclosable subject was
  fully discoverable via search. Fixed at the three record loops with
  `DiscoveryAdmitted` (missing verdict or undisclosable excluded once
  verdicts exist; silent server passes through untouched) and
  `DiscoveryLabel` (name/title/person parts projected; denied names
  never leak through search or the promote-person template).
- New `View.RecordVerdicts` map; no new copy, no CSS, no markup change.
  Matrix covers admission/label units plus item-level IDs and a
  silent-server byte-equality check; digest pinned (`2e49b791…`).
  Browser test drives the real `SearchGlobalItems` pipeline (Jordan
  unfindable, Avery found) plus armed renders × 3 locales.
- Boundary (documented, not deferred silently): item DESCRIPTIONS
  (role/team/summary) still render server-composed text for admitted
  records — field-level description scrubbing belongs to WEB-063's
  record pages. All pre-existing search goldens green (passthrough).
- Browser gate deferred, same as WEB-055–061.

## 25. WEB-063 — record-level page authorization

- Wired the projector into the profile seam: `personPage` and
  `myselfPage` admit only disclosable records (reusing
  `DiscoveryAdmitted` — discovery and pages converge on one rule),
  `personProfileProps` projects all 27 facts plus hero name/role/source
  through per-field verdicts, and the breadcrumb keeps the generic page
  label for unadmitted records. New exported `PersonRecordFields`
  vocabulary shared by production and the equivalence test (a new fact
  key breaks the test until listed — self-enforcing).
- The matrix caught two real issues: (1) the breadcrumb named the
  worker on unadmitted profiles — fixed at the source; (2) a genuine
  nondeterminism — `PageSizeControl` ranged over a map for hidden
  inputs, now sorted. (3) Two false alarms handled honestly: "12%" was
  stylesheet text (leak assertions now skip style/script subtrees) and
  full-allow≠silent was the correct fail-closed on field-less verdicts
  per the pinned 061 contract.
- Boundary: workflow launcher and history on disclosable profiles keep
  their own grant surfaces; avatar initials/photo untouched (not
  verdict fields). Digest pinned (`95f42db5…`).
- Browser gate deferred, same as WEB-055–062.

## 26. WEB-064 — current and proposed organization scope

- Gap found: the role-visibility editor mutated a draft of each role's
  policy but never resolved what any mode MEANS — a DENYLIST never
  showed what it admits. Each editor now resolves both scopes through
  one pure `ResolveOrganizationScope` (ALL/admit-all, ALLOWLIST/
  admit-listed, DENYLIST/admit-unlisted, OWN_UNIT viewer-relative →
  nil; unknown modes, blank names, and stale units resolve to nothing;
  case-insensitive match, available-casing output, sorted, de-duplicated,
  inputs never mutated) and displays current (policy) plus proposed
  (draft) blocks with mode label and unit chips, or an honest empty
  state. Display-only resolution — authorization stays server-side.
- Three new `organization_visibility.*_scope` keys in three locales;
  typed CSS for the scope blocks (stacked single-column under 620px).
  Matrix covers 14 resolution cases plus per-role SSR assertions;
  digest pinned (`cd8e3dbd…`).
- Browser gate deferred, same as WEB-055–063.

## 27. WEB-065 — data-domain presentation scope

- Gap found: the role-visibility editor resolved organization-unit scope
  but never data-domain scope — every domain read as admitted regardless
  of policy. Each editor now resolves admitted domains through one pure
  `ResolveDataDomainScope` (granted ∩ available over the closed spec
  taxonomy People/Position/Organization/Compensation; unknown and blank
  names resolve to nothing; case-insensitive match, available-casing
  output, sorted, de-duplicated, inputs never mutated) and renders
  admitted-domain chips or an honest empty state. Non-admitted domains
  never reach the markup. Display-only resolution — authorization stays
  server-side.
- New `OrganizationVisibilityPolicy.DataDomains` presentation admit-list
  plus `AvailableDomains` page prop; two new `data_domain_scope.*` keys
  in three locales; typed CSS for the domain blocks (chips stretch to a
  stacked column under 620px). Matrix covers 11 resolution cases plus
  per-role SSR, security-withholding, and 3-locale assertions; digest
  pinned (`55ea6ea2…`, independently verified with a Python oracle).
- Browser gate deferred, same as WEB-055–064.

## 28. WEB-066 — field-disposition renderings

- Gap found: the profile rendered the four WEB-061 effects but defined
  no rendering for the six spec dispositions (`SHOW`, `MASK`, `REDACT`,
  `HIDE`, `SUMMARY_ONLY`, `DERIVED_ONLY`) — a masked salary would have
  dumped raw, a hidden fact would still occupy a row. New `ProjectField`
  honors `AuthorizedField.Disposition`/`StandIn` (both server-composed;
  presentation invents no masking or summary text): SHOW renders,
  MASK/SUMMARY_ONLY/DERIVED_ONLY render the stand-in with raw values
  never reaching the markup, REDACT reads as redacted, HIDE omits the
  row with no text or reason, unknowns withhold. Blank dispositions
  delegate to the untouched legacy path.
- The shared `personProfileProps` adapter (person + self-service) builds
  facts through the projector, preserving fact order exactly — WEB-063's
  pinned golden and silent≡allow conformance pass untouched. No new
  locale keys (existing withheld/redacted/not-reported copy) and no new
  CSS (existing fact rows); matrix covers 14 projection cases plus
  governed-profile SSR, 3-locale withholding, and determinism; digest
  pinned (`8d109a6e…`, independently verified with a Python oracle).
- Browser gate deferred, same as WEB-055–065.

## 29. WEB-067 — population-query presentation limits

- Gap found: discovery surfaces honored record verdicts, but the
  workforce directory rendered every projected worker regardless —
  denied rows, denied facet options, and denied counts all leaked.
  New `admittedPeople` limits the directory population once the server
  speaks (undisclosable and verdict-less records leave; silent servers
  keep the current set; order preserved; inputs never mutated; empty
  resolves to nil per the WEB-064 contract). `filteredPeople` filters
  the admitted population, and the people page derives rows, team /
  location facets, and result counts from it.
- Governed all-disclosable verdicts render byte-identically to a silent
  server (no second authority source). No new locale keys, no new CSS;
  matrix covers 5 population variants plus SSR absence, 3-locale
  withholding, and determinism; digest pinned (`2f1ac4a4…`,
  independently verified with a Python oracle).
- Browser gate deferred, same as WEB-055–066.

## 30. WEB-068 — semantic action-availability states

- Gap found: actions were binary and decorative — the capability card
  rendered a live action link even when its state read "Unavailable",
  and the visibility save disabled with no reason and no recovery.
  New `ResolveActionAvailability` encodes the spec sentence (granted
  → available; denied but existence-safe → unavailable; denied and
  unsafe → hidden). Unavailable cards render the reason with no live
  action link (marked `data-action-state="unavailable"`); hidden
  capabilities leave the admin page; the grant-less save stays
  disabled with its reason and the Manage-roles recovery link. Blank
  states keep the legacy live rendering byte-identically.
- Three new reason keys in three locales; no new CSS (existing muted
  and button styles). Matrix covers the 4-state resolution table plus
  per-card SSR, hidden-drop, save recovery scoping, 3-locale copy,
  and determinism; digest pinned (`6ea2df1c…`, independently verified
  with a Python oracle).
- Browser gate deferred, same as WEB-055–067.

## 31. WEB-069 — export and artifact visibility

- Gap found: search honored record verdicts, but the work surfaces
  presenting decision artifacts — queue rows with proposal facts,
  history summaries and digests, insights metrics, home activity —
  rendered every projected instance regardless. New `admittedWork`
  gates the queue, the history universe, and the derived counts;
  verdict-less and non-disclosable instances leave once the server
  speaks about work.
- Load-bearing correction: the first cut gated on any non-empty map
  and broke WEB-063's pinned golden — profile renders carry
  person-record verdicts only. Silence is now population-scoped: a
  map with no work-instance verdict keeps journeys ungated, since
  dropping journeys for a verdict about a person would invent
  authority. Explicit denials still hide on every surface including
  person-embedded history. No dedicated export affordance exists in
  the UI; the admitted-population rule governs every extractable
  listing an export draws from.
- No new locale keys, no new CSS; matrix covers 6 work variants plus
  queue/history SSR, 4-surface silent-equivalence in 3 locales, and
  determinism; digest pinned (`34a25cbb…`, independently verified
  with a Python oracle).
- Browser gate deferred, same as WEB-055–068.

## 32. WEB-070 — real-time events by current authority

- Gap found: the attention notification center counted every open work
  item regardless of record verdicts — a denied journey kept its share
  of the count while appearing in no listing. The count now draws from
  the admitted population (`OpenWorkItems(admittedWork(view))`):
  denied open instances leave, denied terminal instances never
  counted anyway, silent servers keep the count, and page-visibility
  keeps its honest restricted state.
- Matrix covers 4 authority variants against exact locale-built
  trigger labels plus shell SSR, 3-locale withholding, and
  determinism; digest pinned (`9fb63cfe…`, independently verified
  with a Python oracle — the first oracle pass caught my own row
  error on the terminal-only variant, corrected before pinning). No
  new copy, no new CSS.
- Browser gate deferred, same as WEB-055–069.

## 33. WEB-071 — invalidate presentation caches on authority drift

- Gap found: two client caches never invalidated. The roles
  permission row seeded its grant toggles from props once, so a
  drifted projection (version-bumped or changed grants, another
  role) kept displaying — and saving — the stale draft. The
  break-glass prompt keyed dismissal to nothing, so dismissing one
  incident hid the next emergency too. New `reconcileRolePermissionDraft`
  (mirroring the directory's reconcile pattern) keeps local edits
  across re-renders and resets exactly on snapshot drift; dismissal
  is now keyed by incident reference via `breakGlassSuppressed`.
- Single server renders are byte-identical to the legacy seeding
  (initial key always matches). Session/step-up dismissals stay
  documented page-load state — no stable authority identity to key
  them by. No new copy, no new CSS; matrix covers 5 draft cases
  plus 4 dismissal cases, SSR grant rendering, 3-locale copy, and
  determinism; digest pinned (`6769a70a…`, independently verified
  with a Python oracle).
- Browser gate deferred, same as WEB-055–070. Live WASM
  cross-render reset behavior needs the browser gate; the pure
  reconcile matrix is the available proof.

## 34. WEB-072 — cross-surface authorization noninterference

- Gap found: the mirror of WEB-069's namespace bug. Work verdicts
  were population-scoped, but any non-empty map — including one
  addressing only journeys — emptied the workforce directory,
  inventing authority over workers. New `peopleVerdictsPresent`
  gates the directory only on person-addressed verdicts. The matrix
  proves all four combinations: silent interferes with nothing,
  person verdicts gate people only, work verdicts gate journeys
  only, mixed maps gate each population independently.
- Test-authoring correction: the security fixture granted no verdict
  for the admitted journey and blamed the implementation for the
  correct verdict-less drop; the fixture now grants it explicitly.
  Discovery search keeps its pinned map-global contract — the
  scoping rule governs page populations. No new copy, no new CSS;
  digest pinned (`91f69d1e…`, independently verified with a Python
  oracle).
- Browser gate deferred, same as WEB-055–071.

## 35. WEB-073 — immutable PageDefinition revisions

- Gap found: the lifecycle draft → validate → preview → publish
  chain had no immutable version anywhere — a published page could
  pin no consumers, roll back nothing, or prove nothing. New
  `PageRevisionLog` records render-free snapshots addressed by
  sha256 over canonical JSON bytes binding each version: identical
  re-records are idempotent, conflicting content at a version is
  refused, versions must be positive, latest tracks the maximum,
  and snapshots deep-copy on record and retrieval.
- `MarshalRevision`/`ParseRevision` persist bytes with tamper
  failing closed (test fix: trailing whitespace is not tampering —
  JSON skips it — so the test flips a content byte instead). The
  registry-wide browser pass records every definition and
  round-trips persisted text bytes; durable truth stays with the
  owning service. No new copy, no new CSS; digest pinned
  (`db66afe2…`, independently verified with a hand-built Python
  oracle that also proves Go's encoding byte-equality).
- Browser gate deferred, same as WEB-055–072.

## 36. WEB-074 — page draft lifecycle

- Gap found: immutable revisions existed with no way to reach them —
  no working copy, no publish step, no guard against forking lineage.
  New `PageDraft` binds a base revision (or nothing from scratch)
  with a freely editable snapshot; `PublishDraft` applies ordered
  rules (positive versions; no retargeting; identical replays
  idempotent; conflicting content refused; stale bases refuse so a
  draft can only land on a still-latest base; from-scratch refuses
  versioned pages). Discard drops the struct; the log never sees
  un-published edits.
- Design correction mid-test: the first stale-base sketch (base
  digest changed) could never trigger against an immutable log, so
  the rule became optimistic concurrency on latest — and the
  idempotent-replay check had to precede the base check or
  republishes would false-refuse. No new copy, no new CSS; matrix
  plus registry-wide draft/publish and determinism; digest pinned
  (`84a560e1…`, independently verified with a Python oracle).
- Browser gate deferred, same as WEB-055–073.

## 37. WEB-075 — floorplan compatibility validation

- Gap found: drafts carried compositions nothing validated — an
  unknown floorplan, a future version, or an unregistered primitive
  would sail through validate into publish. The spec's 10
  floorplans and 11 semantic primitives now register as a
  presentation contract (`RegisteredFloorplans`,
  `RegisteredPrimitives`, both freshly copied), and
  `ValidateFloorplanCompatibility` verdicts one composition with
  stable accumulated reasons. No per-floorplan primitive subsets
  exist in the spec, so none were invented — any registered
  primitive validates anywhere.
- Composition lives draft-scoped (`PageDraft.Composition`); the
  pinned snapshot shape and WEB-073/074 digests are untouched, and
  the full suite confirms it. Regions, widgets, and action bindings
  extend `PageComposition` in the following todos. No new copy, no
  new CSS; digest pinned (`73a56360…`, independently verified with
  a Python oracle).
- Browser gate deferred, same as WEB-055–074.

## 38. WEB-076 — semantic-region composition validation

- Gap found: drafts named regions nothing checked — unknown regions,
  the platform-owned shell claimed as content, out-of-anatomy order,
  duplicated primaries, and compositions with no primary all
  validated. The 7 composable anatomy regions now register in
  resolution order, and `ValidateRegionComposition` verdicts one
  composition (registered regions, non-decreasing rank, one primary
  once composition starts, empty still valid for fresh drafts) with
  stable accumulated reasons.
- Expectation fix: a reversed composition containing primary gets
  only the order reason, not a missing-primary one. Drafts validate
  regions alongside floorplans with zero coupling between the
  layers. No new copy, no new CSS; digest pinned (`f6227695…`,
  independently verified with a Python oracle).
- Browser gate deferred, same as WEB-055–075.

## 39. WEB-077 — typed widget bindings validation

- Gap found: compositions named widget bindings nothing checked —
  unknown types, future versions, unknown authority classes, missing
  classification or source, masked bindings with no display form,
  and sensitive context on external embeds all validated. The
  initial registry holds one widget per trust tier, and
  `ValidateWidgetBinding` verdicts one server-composed binding with
  stable accumulated reasons.
- Deliberately not invented: no schema language (bindings validate
  the type vocabulary, not value shapes), no classification ordering
  (ceilings pin as opaque metadata until the classification step),
  no masked+editable contradiction rule (not spec-stated).
  Sensitivity is server-marked, mirroring verdicts and stand-ins.
  No new copy, no new CSS; digest pinned (`7a0f2967…`,
  independently verified with a Python oracle).
- Browser gate deferred, same as WEB-055–076.

## 40. WEB-078 — semantic action bindings validation

- Gap found: compositions named action bindings nothing checked —
  missing capabilities, synthesized endpoints, tokenless bindings,
  unversioned actions, missing idempotency keys, and untyped input
  all validated. `ValidateActionBinding` verdicts one binding with
  stable accumulated reasons; endpoint-shaped capabilities refuse
  because the client never synthesizes endpoints.
- Deliberately no static capability allowlist: bindable-capability
  availability is runtime gateway authorization proved by the
  token, and presentation allowlisting it would duplicate that
  authorization as a second source. Drafts validate through
  `PageComposition.Actions`. No new copy, no new CSS; digest
  pinned (`850f2e8d…`, independently verified with a Python
  oracle).
- Browser gate deferred, same as WEB-055–077.

- Gap found: compositions named no classification ceiling — a
  confidential binding composed onto a public-ceiling page without
  anything comparing them (widget registration pinned opaque
  limits). `ValidateDraftCeiling` verdicts one draft against its
  declared `PageComposition.ClassificationCeiling` with stable
  accumulated reasons, failing closed on undeclared ceilings and
  unranked labels.
- Deliberately pinned publication ladder public < internal <
  confidential < restricted from the registry's labels plus the
  platform catalog's restricted tier; the Classification service
  taxonomy stays authoritative and the server re-checks at refresh
  time. The ceiling gates composed binding data only — widget
  capability limits and action inputs stay with their owning
  authorities. No new copy, no new CSS; digest pinned
  (`0c08b5f2…`, matrix probe verified line-by-line).
- Browser gate deferred, same as WEB-055–078.

- Gap found: layered page configuration resolved by caller
  convention — no precedence across platform, tenant,
  enterprise-group, and company layers, and no accumulation of
  the ceiling restriction. `ResolvePageConfiguration` merges
  scoped layers into one composition with lineage: winning scope
  and policy version per field, overridden declarations, and
  denied ceiling weakenings.
- Deliberately catalog-derived: most-specific-valid-scope wins
  for configuration (company override down to platform default);
  the ceiling only tightens, so a narrower scope cannot weaken a
  broader denial. Unknown scopes, duplicate scopes, and unranked
  ceilings fail closed. No new copy, no new CSS; digest pinned
  (`1a923dc4…`, scenario probe verified line-by-line).
- Browser gate deferred, same as WEB-055–079.

- Gap found: compositions bound to older widget versions had no
  way forward — validation refuses newer versions, so the only
  path was hand-editing versions with no evidence.
  `MigrateCompositionWidgets` advances behind bindings to the
  registry's pinned versions with per-binding lineage, leaving
  current bindings untouched and every non-version field
  identical.
- Deliberately same-widget moves only: unknown widgets and
  versions outside the registry fail closed with the registry's
  own wording, and cross-widget replacement awaits pinned
  replacement behavior the registry does not declare yet. No new
  copy, no new CSS; digest pinned (`b3061771…`, binding probe
  verified line-by-line).
- Browser gate deferred, same as WEB-055–080.

- Gap found: published revisions were all-or-nothing — nothing
  bound a revision to receiving scopes or go-live dates, so
  staged publication was caller convention. `PageRollout` pins
  one revision (page, version, digest) to target scopes with
  effective dates, with a structural verdict, log-verified
  targeting, and a mechanical live-at query.
- Deliberately forward-only: rollout advances scopes to newer
  revisions and never moves one backward (that is rollback,
  WEB-083); scopes are declared organization-applicability
  strings the organization domain owns, and durable rollout
  state stays with the studio platform. No new copy, no new
  CSS; digest pinned (`77857608…`, target probe verified
  line-by-line).
- Browser gate deferred, same as WEB-055–081.

- Gap found: rollout moved scopes forward only — an incident
  had no verified path back to an older revision, and a dead
  page kept serving its last rollout. `VerifyRollbackTarget`
  verifies a structurally valid rollback to a strictly older
  log-pinned revision over live scopes only, and
  `PageRetirement` carries a dated end-of-life with a recorded
  reason that darkens serving through `RolloutServableAt`.
- Deliberately strict endpoints: cross-page, forward-or-equal,
  out-of-scope, unvalidated, and unpublished (live or
  destination) targets all refuse; the reason is retained
  evidence and never optional; a foreign page's retirement never
  darkens. No new copy, no new CSS; digest pinned (`d95f7474…`,
  rollback probe verified line-by-line).
- Browser gate deferred, same as WEB-055–082.

- Gap found: contract versions moved with no blast-radius
  report — a widget bump or floorplan change left the studio
  guessing which compositions bound the old version.
  `ReportDependencyImpact` inventories one change across page
  compositions, listing every page with its status so the
  studio sees status, not just hits.
- Deliberately presentation-owned surface only: registered
  widgets and catalog floorplans; capabilities and
  classifications change in owning domains, and malformed
  changes (unknown kinds, blank contracts, negative versions)
  fail closed. No new copy, no new CSS; digest pinned
  (`3cbd34c0…`, impact probe verified line-by-line).
- Browser gate deferred, same as WEB-055–083.

- Gap found: revisions, compositions, rollouts, retirements,
  and impact reports lived as separate pieces with no single
  studio view. `BuildPageInventory` unions them into one sorted
  inventory: latest pinned revision, live latest-version
  rollout scopes, active retirement, and impacted contracts per
  page.
- Deliberately composed, not re-owned: validation stays in the
  validate chain, rollouts and retirements read mechanically,
  superseded-version rollouts never count as live, and
  malformed contract changes fail the whole build closed. No
  new copy, no new CSS; digest pinned (`e3a7a1c5…`, inventory
  probe verified line-by-line).
- Browser gate deferred, same as WEB-055–084.

- Gap found: the authoring flow opened with purpose/audience
  but the composition carried neither — a new page could not
  declare why it exists or who it serves. `PageComposition`
  carries purpose and audience from the opening step, and
  `ValidatePagePurpose` verdicts the declaration.
- Deliberately free-text: no length rule is stated, and
  audiences stay declared strings rather than a second
  authorization vocabulary. Single-concern: every other field
  validates in its own step. No new copy, no new CSS; digest
  pinned (`f59aa73e…`, declaration probe verified
  line-by-line).
- Browser gate deferred, same as WEB-055–085.

- Gap found: the authoring flow had no governed floorplan
  choice — nothing resolved an author's pick against the
  catalog, pinned the version, or refused unregistered picks.
  `ChooseFloorplan` resolves catalog, pick, and optional
  version into a pinned choice, reusing the compatibility
  wording so choice and validation speak identically.
- Deliberately catalog-governed only: purpose and audience do
  not narrow the catalog (no such rule is stated); unset
  versions pin current, explicit older versions pin as asked,
  and future or negative versions refuse. No new copy, no new
  CSS; digest pinned (`4a334fbd…`, pick probe verified
  line-by-line).
- Browser gate deferred, same as WEB-055–086.

- Gap found: the outline-first composition had no governed
  edits — primitives and regions changed only by hand-editing
  slices with no vocabulary check and no atomicity.
  `ApplyOutlineEdits` adds and removes primitives and regions
  against the registered sets, applied atomically in order.
- Deliberately convergent: adding present and removing absent
  succeed silently so batches replay idempotently; moves stay
  out for the reorder step, and ordering validates downstream.
  No new copy, no new CSS; digest pinned (`aad41cb9…`, outline
  probe verified line-by-line).
- Browser gate deferred, same as WEB-055–087.

- Gap found: outline regions could not move — drag operations
  had no keyboard-accessible counterpart, so keyboard authors
  could not reorder at all. `MoveOutlineRegion` moves one
  region one step earlier or later, swapping with its
  neighbor, reusing the outline verdict.
- Deliberately axis-neutral: earlier/later holds for vertical,
  horizontal, and RTL layouts; boundary moves converge
  silently like disabled controls; absent regions and unknown
  directions refuse; first occurrences travel on duplicates.
  No new copy, no new CSS; digest pinned (`638461aa…`,
  reorder probe verified line-by-line).
- Browser gate deferred, same as WEB-055–088.

- Gap found: ceilings gated binding data but the studio had
  no per-page widget answer — authors picked
  confidential-capable widgets onto public pages and learned
  it from binding refusals. `PermittedWidgetCatalog` lists
  every registered widget with its status under the page
  ceiling: a widget clears exactly when its limit sits at or
  below the ceiling.
- Deliberately the capability side only: binding data still
  gates per binding; undeclared ceilings clear nothing and
  unranked labels refuse, all fail-closed. No new copy, no new
  CSS; digest pinned (`170eee8c…`, catalog probe verified
  line-by-line).
- Browser gate deferred, same as WEB-055–089.

- Gap found: binding validity lived in separate verdicts
  with no unified author view — no one list showed every
  composed binding with its validity and authorization
  posture. `InspectBindings` reports per-binding findings in
  composition order: validity with verbatim validator reasons
  plus declared-attribute notes.
- Deliberately report-only: authorization stays server-side
  with tokens proving availability; token presence is noted
  but token material never reaches the report, and invalid
  bindings still report their posture. No new copy, no new
  CSS; digest pinned (`15b3b244…`, inspector probe verified
  line-by-line).
- Browser gate deferred, same as WEB-055–090.

- Gap found: binding values changed only by hand-editing
  with no tier check and no markup check, so raw HTML
  reached content bindings. `EditBindingContent` sets one
  content-tier binding's values with tier, markup, and
  masked-display checks.
- Deliberately conservative tag-open sniff (`<` plus letter,
  slash, bang, or query refuses; bare brackets pass) with
  deeper policy left to the content-safety gates; governed
  and external tiers are not author-editable. No new copy, no
  new CSS; digest pinned (`7dd905ae…`, content probe verified
  line-by-line).
- Browser gate deferred, same as WEB-055–091.

- Gap found: compositions reached publication with no
  governed preview — locale, color-mode, and viewport
  coverage was author memory. `PreviewPlan` binds page,
  fixture, and registered dimension axes;
  `ExpandPreviewPlan` expands the cartesian case matrix with
  stable IDs.
- Deliberately axes-registered: locales from the product
  catalog, concrete color presets, author-chosen positive
  widths; fixtures stay declared studio strings and render
  truth stays with the renderer. No new copy, no new CSS;
  digest pinned (`fda98053…`, preview probe verified
  line-by-line).
- Browser gate deferred, same as WEB-055–092.

- Gap found: every validate step verdict alone — authors
  ran six validators and merged reasons by hand, with early
  failures hiding the rest. `ValidateComposition` runs the
  whole draft chain (purpose, floorplan, regions, widgets,
  actions, ceiling) reporting every step with an overall
  publication gate.
- Deliberately content-model only: rendering the panel stays
  with the studio surface; reasons stay verbatim from the
  step validators. No new copy, no new CSS; digest pinned
  (`afe1a8de…`, panel probe verified line-by-line).
- Browser gate deferred, same as WEB-055–093.

- Gap found: review had no semantic comparison — authors
  eyeballed two compositions with no field-level account of
  what moved. `DiffCompositions` reports changed scalars,
  added/removed/moved members, and paired binding changes in
  field order.
- Deliberately meaning-aware: ceiling moves name tightening
  or loosening only when both sides rank; occurrences pair
  by count so duplicates match positionally; token and
  idempotency rotations stay silent while granted/withdrawn
  tokens report. No new copy, no new CSS; digest pinned
  (`b9b395c1…`, diff probe verified line-by-line).
- Browser gate deferred, same as WEB-055–094.

- Gap found: the mechanical publish recorded any draft
  content with no validation, preview, or review — the
  asset pipeline's "versioned, previewed, reviewed, and
  published" unenforced. `PublishGoverned` wraps the
  mechanical publish with the gates in pipeline order:
  validation, preview plan with matching evidence for the
  draft page, named approval, then record.
- Deliberately first-gate-reports: later gates never run
  after a refusal; evidence equality is pinned but
  composition freshness stays a studio responsibility.
  Mechanical conflicts and idempotent retries still surface
  from the wrapped publish. No new copy, no new CSS; digest
  pinned (`5a7bf3b3…`, publication probe verified
  line-by-line).
- Browser gate deferred, same as WEB-055–095.

- Gap found: Home rendered hardcoded sections with
  scattered Allows checks — the phase's sections had no
  governed slots to attach to. `ResolveHomeFloorplan`
  resolves the registered section order per projection:
  attention gates on the work surface, the rest place with
  authorized contents resolved in their section todos.
- Deliberately placement-split-from-content: the floorplan
  owns order and slots, never who sees which record;
  section IDs stay stable for the phase. No new copy, no
  new CSS; digest pinned (`1be2a425…`, floorplan probe
  verified line-by-line).
- Browser gate deferred, same as WEB-055–096.

- Gap found: needs-action work scattered with no single
  governed list for the attention slot. `AttentionList`
  filters one admitted stream to non-terminal items in
  admission order for the attention section's content.
- Deliberately order-preserving: presentation assigns no
  priority without a stated rule, and terminal items belong
  to continuity and history. No new copy, no new CSS;
  digest pinned (`0d78cda7…`, attention probe verified
  line-by-line).
- Browser gate deferred, same as WEB-055–097.

- Gap found: completed work had no governed list for the
  recent-work slot. `RecentWork` filters one admitted stream
  to terminal items in admission order for continuity.
- Deliberately the attention mirror: attention plus recent
  work partition the admitted stream exactly, nothing lost
  or shared. No new copy, no new CSS; digest pinned
  (`7bc97e45…`, recent probe verified line-by-line).
- Browser gate deferred, same as WEB-055–098.

- Gap found: the summaries slot had no governed per-person
  content. `SummarizePersonal` derives one entry per person
  present – active and completed counts in order of first
  appearance – adding no truth beyond the stream.
- Deliberately derived-only: totals reconcile item-for-item
  with the attention and recent-work lists. No new copy
  (counts only), no new CSS; digest pinned (`80b8ce89…`,
  summary probe verified line-by-line).
- Browser gate deferred, same as WEB-055–099.

- Gap found: the quick-actions slot had no governed choice
  resolution. `ResolveQuickActions` resolves viewer-chosen
  IDs against available launcher items – chosen order kept,
  repeats collapsed, unknown IDs dropped fail-closed.
- Deliberately passthrough: items arrive untouched and the
  catalog owns the truth. No new copy, no new CSS; digest
  pinned (`7ec0bf89…`, resolution probe verified
  line-by-line).
- Browser gate deferred, same as WEB-055–100.

- Gap found: the announcements slot had no governed region
  split. `GovernAnnouncements` divides one stream into polite
  and assertive regions in admission order – first assertive
  claim wins, later claims join polite.
- Deliberately membership-only: items pass through untouched
  and the shell keeps its single-assertive policy. No new
  copy, no new CSS; digest pinned (`9b1b07dc…`, region probe
  verified line-by-line).
- Browser gate deferred, same as WEB-055–101.

## Non-web backlog wave

Retention concurrency refinement (2026-09-08): root found that snapshot-selected expired job links could be refreshed while cleanup waited for a row lock. Sol added update-time cutoff and candidate-expiry checks for run and partition rows. Independent `go test -count=1 -run TestTodo_OBS_013_Fault ./internal/data/jobs/` passes, including the two-connection lock-synchronized refresh regression. Cleanup remains globally bounded across runs, partitions and checkpoints, preserving business identifiers and CAS versions. Production retention invocation and signal trace retention are still unresolved; this does not close OBS-013. Independent composition scan/golden tests in `internal/application` pass after the rewards compile repair.

FX persistence review (2026-09-08): independent `go test -count=1 -cover ./internal/data/fxstore/` passes at 74.4%. Corrections now preserve effective time while advancing knowledge time, reject generic-save successor bypasses, and persist exact quote fields plus nanosecond residues for canonical replay. Reviewed PostgreSQL coverage includes independent-connection successor contention and tenant isolation. FX-003 remains open: identifying impacted consumers is not the required cross-domain recomputation and golden parity.

Decomposition gate refinement (2026-09-08): direct validation accepted an omitted version and retained a legacy floating-point cost fallback. Sol removed that path: all decisions now require v2 and integer `monthly_minor` with currency; strict JSON rejects `monthly_usd`. The CLI now verifies pinned source bytes before checking the live inventory. Independent `go test -count=1 -cover ./tools/quality/decomposition/` passes at 77.8%, including omitted-version and float-money file regressions. Commit gates remain pending.

Legal activation audit (2026-09-08): repository call-site inspection finds `EvaluateReceipt`, `AppendEvaluationReceipt` and `AppendEvaluationBinding` invoked only by tests. The composed intent service consumes durable verification at terminal outcome binding, but that does not establish a production receipt producer. LEGAL-014 remains open; the next integration check must connect signed persistence to the actual obligation transition and identify the trusted producer without inventing signing authority or discharge evidence. Independent `go test -count=1 ./internal/intent/model/` also passes after the reviewed instance-owned regex-cache repair.

Legal persistence verification (2026-09-08): independent `go test -count=1 -cover ./internal/data/legalevidencestore/` passed at 74.8%. Review separated authenticated semantic tenant references from storage UUIDs, corrected concurrent idempotent receipt retries after PostgreSQL JSONB normalization, and bound persisted obligations to the exact signed receipt. The suite includes real two-connection retries and conflicting bindings. This verifies the store package, not completion of LEGAL-014; end-to-end legal evidence production and consumption still require integration verification. No atomic non-web commit was made in this check while the frontend session was actively sharing the Git index.

Jobs retention verification (2026-09-08): independent `go test -count=1 -cover ./internal/data/jobs/` passes at 73.1% after direct malformed SQL tuple probes and bounded trace-link deletion tests. Checkpoint links now occupy an operational sidecar; rollback, tenant isolation, unexpired links and permanent checkpoint/business identifiers are preserved. OBS-013 remains open because its other durable continuation paths and production retention invocation are not yet fully integrated.

Legal receipt follow-up (2026-09-08): root found that the reconstructed receipt's BodyDigest hashed AppliedObligation metadata rather than the typed legal rule required by section 6.5. Sol changed it to the exact release/jurisdiction/kind/ID rule's canonical body and added a parameter-only mutation regression. The reviewed LER1 encoding now pins golden `88b634b5384e0e230cdb4e95757c86fde8158b50ece43befd97f7a196bf3f762`; this is a deliberate corrected encoding, not evidence that the lost encoder was recovered. Independent `go test -count=1 -cover ./internal/governance/legal/` passes at 82.6%. LEGAL-014 remains open for durable verifier and production composition review.

Integration checkpoint (2026-09-08): the CRM-002 commit attempt was interrupted before landing when another session added frontend files to the shared index. No hooks were bypassed and no frontend files were changed by this lane. The next retry requires an exclusive staging window. Independent conflict-store tests pass at 70.4%; migration 00269 now has all three storage-disposition rows and the storage-manifest golden check passes. CONFLICT-003 remains open while review checks that fenced footprints actually cover the committed events and production construction supplies the adapter. Independent decomposition tests pass at 76.2%, including the frozen-baseline bypass fix. Tax snapshot, calculation-policy and payroll tests pass at 72.4%, 66.6% and 63.9% respectively (the latter two use existing exact coverage exceptions); final refinement exercises the calculation receipt and frozen-population rejection, not just a conformance helper. Legal receipt persistence and configured-key verification are still being implemented; its unresolved canonical golden mismatch has not been repinned to hide the failure.

Atomic delivery update (2026-09-08): CRM-002's five staged Go files were included by the other session in commit `e65dd67` (`feat(ui): WEB-065 data-domain presentation scope`). Root verified their presence in HEAD and a clean CRM working tree. This is not the requested standalone TODO commit; the shared commit is not rewritten. Independent CRM package tests previously passed at 76.1% and vet was clean. No recruiting deployment or independent observation of that other session's commit-hook results is claimed. Further staging requires coordination between sessions.

Wired `check:decomposition` into `test:all` (therefore the existing pre-commit hook) and root-module CI using the explicit boundary index. The scanner reports PASS on this checkout; the first Windows invocation then hit executable cleanup denial and a repeat reports PASS. Service-resource discovery review is still pending, so ARCH-GO-019 stays open. New Sol reviews now own the actual legal outcome-binding and incident application-bridge handoffs, including trusted provenance and database evidence checks.

Accepted `MERIT-002`, the twenty-first accepted non-web todo in this wave. Root's final synchronized PostgreSQL regression passes in 25.548s after full domain/store verification at 70.1%/76.2%. The test forces preflight to miss an uncommitted competing row before releasing the winner, exercising post-insert payload validation rather than depending on scheduler timing. Separately, the decomposition baseline now names only the ten existing module/command boundaries and the current scanner passes it; mandatory hook wiring awaits the service-resource discovery refinement.

Planning registry and storage-manifest golden tests both pass after the latest acceptance edits. Fresh bounded Luna lanes now own three unfinished integration steps: semantic parent/child/interval conflict fencing (existing migration 00269), complete jobs metadata load/retry round trips (00272), and verified import-simulation to real ProposalRevision construction. Their file ownership excludes frontend and the concurrently edited legal outcome files. No additional completion is claimed.

Root independently verifies merit domain/store at 70.1%/76.2% and vet clean; the full PostgreSQL run passes in 47.522s. Acceptance still awaits deterministic contention synchronization proving the post-insert conflict branch rather than only a preflight conflict. Tax/payroll integration has a fresh Sol review for bypasses and population binding. Incident intake audit found semantic and durable APIs were test-only; a scoped application bridge lane now connects them without touching the other session's serve.go or automatically acknowledging incidents.

Accepted `MOBILITY-002`, bringing the wave to twenty accepted non-web todos. Root independently verified the full package and vet at 71.6% after reviewing exact host selection, detached parent slices, strict extensions and preservation of other host assignments. Vendor observations remain non-authoritative and return closure is exact-effect scoped. Signal telemetry migration 00271 has moved to a fresh Sol review for schema bounds, replay/fan-out propagation and metadata-independent business behavior; OBS-013 remains open.

Root reproduced the OBS-007 identity collision with a failing regression, then replaced delimiter concatenation with versioned byte-length-prefixed tuple hashing for incident identity and tenant correlation. Full incidentstate tests pass at 81.8% and vet is clean; a fresh Sol review owns golden/compatibility verification. A fresh Luna implementation lane owns the authorized tax/payroll binding APIs so the integration proceeds beyond domain-only reports. Mobility review also fixed a real multi-host parent-slice mutation discovered by the requested targeted-host test; independent verification is next.

Accepted `FX-002`, bringing this wave's accepted non-web count to nineteen. Root independently verified full FX/kernel tests (71.7%/71.2%) and vet after Sol's rational oracle reproduced a maximum-precision double-rounding defect in the old finite-precision division path. Exact quotient/remainder rounding now handles all declared modes once; directed multiplication/rescaling also retains sub-unit values correctly. This is conversion conformance, not a live rate feed or authorized financial write. Legal package verification separately passes at 82.3%, but LEGAL-014 remains open until real obligation transitions bind the trusted receipt.

Latest incident-store changes independently pass `go test -count=1 -cover ./internal/data/opsmeta/` (34.891s, 71.6%). Root found a remaining alert fingerprint collision from delimiter-concatenated rule/version/fingerprint keys and requested canonical tuple encoding. The corrected conflict migration no longer contains a shadow ledger-head table and now declares the footprint mutation trigger. A new Luna lane starts RECORDS-COPY-001 in existing records/privacy stores; mobility test repairs proceed to Sol review.

Rejected the latest CONFLICT-003 test handoff: its migration/fixture reintroduced a shadow `conflict_stream_head` despite production validation reading authoritative `stream_head`. Luna must remove that table and seed the actual ledger head; merely launching the PostgreSQL suite is not accepted evidence. DATAOPS-005 review is also checking validation/mapping provenance and missing-versus-empty baseline values before a signed preview can be trusted. Neither todo is complete.

Root independently ran `go test -count=1 -run '^TestTodo_OBS_013_CompleteApproval' ./internal/workflow/execute/`: package PASS (38.275s), followed by the documented Windows executable-cleanup denial. Review still requires matching persisted trace/span identifiers to the actual exported span, counting starts as well as ends, and proving failure rollback; nonempty identifiers alone are insufficient. Mobility and incentive test-repair lanes now address weak property/golden/concurrency assertions without changing their reviewed production implementations.

Registered migration 00270's durable merit child-intent emission table in storage disposition as tenant-scoped, permanent, append-only aggregate data; the storage manifest golden test passes. Incident review found JSON number precision loss in scope decoding and a PostgreSQL timestamp precision replay edge, now assigned for repair with regression tests. Migration 00269's immutable footprint table also needs its required mutation-denial trigger; Luna owns that fix. These are integration corrections, not additional accepted todos.

Integration audit found another ARCH-GO-019 gap: identifying a deployment only by its filename misses a second resource added inside an existing Compose or Kubernetes manifest. Sol is refining discovery before the mandatory gate is wired into CI; no grandfathered baseline is accepted yet. CONFLICT-003 remains open: its store has only 4.0% coverage and lacks real PostgreSQL contention/rollback evidence. Luna now owns that missing store matrix while Sol independently checks plan-to-fence binding in the committer, with non-overlapping file ownership.

Independent follow-up verification: `go test -count=1 -cover ./internal/data/opsmeta/` passes (26.430s, 71.6%), including the row-locked acknowledgement contention regression. `go test -count=1 -cover ./internal/domains/fx/ ./internal/kernel/values/` passes at 71.7% and 70.8%. FX review remains open for maximum-precision nearest-rounding boundaries beyond the new 2,304-case rational oracle; passing bounded examples is not treated as proof of all representable arithmetic. TAXPROFILE-003 is handed to Sol for snapshot provenance and actual consumer integration review. No additional todo is ticked by this checkpoint.

Independent verification of the older matrix repairs passes: asset tests/vet at 77.0% and career tests/vet at 47.8% under the existing exact exception. These replace pre-existing alias wrappers with real golden, concurrency and mutation assertions; no new feature-completion count is claimed. TAXPROFILE-003 is now assigned to Luna after TAXPROFILE-002 acceptance, with explicit requirements for frozen revision/presence-state evidence and distinct Tax/Payroll consumers rather than renamed calls to one helper.

`TAXPROFILE-002` brings accepted non-web work to eighteen todos. The repaired matrix now pins exact golden bytes and proves generated effective boundaries and deterministic resolution, while real PostgreSQL tests cover successor fencing, legacy migration and nanosecond preservation. Root independently verified domain tests/vet (71.3%) and the same-microsecond bounds regression; Sol's full store run passed at 73.8%. Root corrected the resolver comment to describe its latest-known ordering accurately. Payroll execution and tax-rate authority are not part of this acceptance.

MERIT-002's final audit found that recommendation effect revisions were durable but child emission deduplication existed only in MemoryCompensationIntentStore. Migration 00270 is reserved for durable child-intent emission records with transactional creation and replay-safe recovery; claim-and-return alone is not sufficient because a crash could lose dispatch. OBS-007 replay refinement now compares canonical caller scope and propagates read/reload errors; independent full PostgreSQL verification is running before acceptance.

Root independently verified the decimal changes with `go test -count=1 -cover ./internal/kernel/values/` (PASS, 70.4%) and `go vet ./internal/kernel/values/` (PASS). Narrow downstream incentive and merit package suites also pass. The dependency-role policy explicitly permits replacement behind the kernel decimal API; no new dependency or exported backend type was introduced. FX oracle review remains active. Older CAREER-001 matrix aliases are now assigned for the same bounded test repair applied to ASSET-001; this does not expand into frontend ownership.

`SERVICE-003` brings accepted non-web work to seventeen todos after independent verification of the repaired matrix (78.4%, vet PASS). Correction drafts preserve prior explanation and include old removed scope and rule-only changes without affecting unrelated workers. Separately, the FX independent rational oracle exposed a kernel Decimal.Mul directed-rounding defect for positive subunit values; a narrow decimal implementation/test fix is authorized, and FX remains open until the kernel and domain regressions pass.

`LEGAL-013` brings accepted non-web work to sixteen todos. Independent full package tests/vet pass at 82.2%. The resolver now pins actual registered overlays, and default missing-locality behavior preserves signed evidence plus a receipt note; strict refusal is an explicit policy choice as the specification requires. Five state golden vectors, duplicate-release equivocation tests, effective boundaries and legacy canonical compatibility pass. Synthetic citation fixtures remain unreviewed; this acceptance proves preemption mechanics, not legal approval or production pack publication.

CONFLICT-003 review rejected the initial in-memory implementation as insufficient: the todo explicitly forbids process-local locks as business conflict control. Migration 00269 is reserved for durable write-intent fencing, with a PostgreSQL adapter and caller-owned transaction integration alongside ledger/outbox writes. Review also found omitted proposal identity, shared footprint slices and caller-supplied baseline evidence; the implementation lane is addressing these rather than marking the mutex tests complete. No durable conflict completion is claimed yet.

Test-matrix acceptance remains separate from package coverage: root found SERVICE-003 property/golden/conformance aliases to the primary test and sent them back for independent assertions, including exact golden bytes. FX-002's one-example property test is also being replaced with bounded generated vectors checked against an independent arithmetic oracle. Both todos remain open despite passing package suites; the latest independent FX suite/vet passes at 71.4% after fixing direct-product double rounding.

OBS-007 contention was independently verified with `go test -count=1 -run '^TestTodo_OBS_007_Integration_Contention$' ./internal/data/opsmeta/` (PASS, 26.268s): separate PostgreSQL connections exercise durable alert limits, tenant separation and exact-key deduplication. Root also identified mandatory decomposition-scan blind spots in lexical YAML detection and nested-module commands; review is addressing these before any gate or baseline is registered. These results do not claim external notification delivery or completion of the decomposition gate.

`AGENT-002` brings accepted non-web work to fifteen todos. Independent tests/vet pass at 95.6%, with 42,824 fuzz executions. Review fixed forged admissions/results and upstream inference laundering; evidence distinguishes this semantic boundary from exhaustive prompt-injection detection. Legal integration remains open: root compared the missing-locality behavior to the specification and found unconditional refusal too restrictive. The required behavior records unregistered locality evidence and permits tenant-configured fail-closed policy; refinement is correcting that mismatch before acceptance.

Independent populated merit upgrade verification passed: `go test -count=1 -run '^TestMeritPopulatedLegacyDecisionOutcomeMigration$' ./internal/data/meritstore/` (55.056s, followed by the documented Windows cleanup warning). Existing rows survive migration 268 without gaining fabricated decision authority. The latest gateway package test also passes at 95.6% after admission sealing and upstream inference validation/citation propagation. New independent Luna lanes are implementing CONFLICT-003 and DATAOPS-005 in their existing owned roots; Sol review remains required. Service correction review now includes invalidation of removed old scope and rule-only changes, with final integration acceptance still pending.

`READINESS-004` brings the accepted non-web total to fourteen. Root review strengthened the concurrent test so identical errors cannot masquerade as deterministic success; every goroutine must now match a separately successful compilation digest. Full readiness tests and vet pass at 76.8%. The todo covers bounded draft compilation with explicit gates, not execution or persistence. Its evidence and changelog entry are updated; no commit was requested or made.

The FX migration refinement was independently verified with `go test -count=1 -run '^TestTodo_FX_002_ProfileSubmicrosecondIntervalOrdering$' ./internal/data/fxstore/` (PASS, real PostgreSQL, 19.921s). The original timestamp-only constraint rejected valid intervals within one microsecond; migration 00265 now compares timestamp and remainder together, and the regression also rejects reversed bounds. Merit migration 00268 remains under populated-upgrade review. A second AGENT-002 integration pass found that freely constructed admission receipts could bypass admission before output validation; this is being sealed at the gateway boundary rather than treating content hashes as authorization. No additional todos are marked complete on partial integration evidence.

Further integration review on 2026-09-08 independently reran `go test -count=1 -cover ./internal/engines/readiness/`: PASS at 76.8%, followed by the documented Windows executable-cleanup warning. READINESS-004 drafts now bind explicit human-review and governance-approval requirements into semantic identity; this does not grant execution authority. AGENT-002 remains open: review found that a genuine tool-result seal could bless arbitrary caller-supplied fact text, and requested payload-bound validation with adversarial mutation tests. OBS-013 also remains open: persisted causal metadata must be populated by the execution producer and carried through scheduler claims and timer delivery. Those non-web integration paths are assigned for refinement; the separate frontend session's files remain untouched. No commit or whole-repository green claim is made.

Continuation on 2026-09-08: `CAREER-002` brings this session's accepted non-web total to thirteen. The final action-path review found that a public assessment constructor could bypass eligibility; application creation now revalidates its exact assessment/worker/preference/role/consent basis through the trusted port. Package tests and vet pass at 45.5% under the existing exact exception. Merit remains open while authenticated decision rehydration and mixed approved/rejected PostgreSQL persistence are integrated; the private decision seal is not treated as durable state.

`SAFETY-002` and `SUCCESSION-002` bring the accepted total to twelve. Safety's final pass refused resubmission after acceptance or an unobserved timeout; succession now verifies the exact readiness and correction-source tuple and hides withheld candidate counts. Scoped package tests and vet pass with the existing exact coverage exceptions (58.5% and 52.6%). These conformance results do not claim deployed authority adapters, medical/legal conclusions or live payment execution.

`CBA-002` brings the accepted total to ten non-web todos. Integration found ambiguous zero bounds, a mutable registry and provenance that attributed two independently sourced bounds to one clause. Refinement now retains both sources and refuses stale/unpinned seniority inputs. Exact package tests and vet pass; coverage is 52.9% under the existing exact exception through 2026-12-31. This is deterministic composition of supplied pins, not a legal certification or a live policy-source adapter.

OBS-013 outbox delivery is now wired through the real worker build path, with optional configured exporters and deployment/cell resource identity. Exporter-backed tests verify finite linked spans end before Ack/Fail and that absent/invalid diagnostics do not alter delivery. The message-kind attribute uses a closed worker vocabulary and is permitted only on spans. Full worker tests passed at 72.3% in Sol review; integration reran the focused OBS-013 suite successfully (with the documented Windows cleanup warning). OBS-013 remains open while job/workflow continuation coverage is assessed.

`CONTACT-002` and `SERVICE-002` bring the accepted non-web total to nine. Contact tests now expose true minimum-priority ambiguity independently of map iteration and bind reissue retries to the exact prior/replacement/time receipt; coverage is 54.4% under the pre-existing exact exception through 2026-12-31. Service tests separate total credit from current continuity, cover trailing breaks and unknown peers, and pin v1 legacy model versus v2 evaluation canonical bytes; coverage is 77.4%. Exact scoped tests and vet pass. These domain results do not claim live PostgreSQL contact reissue or eligibility orchestration.

`ASSET-002` brings this session's accepted non-web total to seven. Integration review caught caller-mutable offboarding assets and same-tenant worker attribution swaps; both are fixed with adversarial tests. Package tests pass at 75.0% and vet passes. Three further dependency-ready Luna lanes now cover agent trust/taint, evidence-based decomposition decisions, and cited preemption mechanics, with Sol review required before acceptance. Frontend and shared runtime ownership remain unchanged.

Six non-web todos are now recorded complete, including `PROOF-002` and `EQUITY-002`. Their second integration pass caught pre-effective authorization and historical forfeiture errors missed by the initial reviews; both now have boundary regressions. Exact package tests pass at 88.3% and 73.7% respectively, with scoped vet passing. The second wave added ten Luna domain lanes, each followed by Sol review. Other results remain subject to integration acceptance rather than automatic checkbox updates.

The tax-election migration now passes a populated legacy-database upgrade with append-only triggers active; it leaves historical rows untouched and records declared decimal scale only for new writes. The development manifest refresh refused an existing migration checksum mismatch, so that signed fixture has not been silently rewritten. Storage manifests were regenerated using their existing generator test. Worker telemetry still requires production composition wiring; optional unused helpers do not close OBS-013.

`ATTEND-002` passed after additional review cycles: directionless legacy observations remain compatible, typed IN/OUT observations cannot stand in for a missing side, exception ordering and input digests are permutation-stable, and missing-side markers do not invent full-shift absence. Package tests pass at 87.3%; vet completed with the documented Windows executable-cleanup warning. This closes the pure attendance evaluation contract, without claiming a live attendance workflow.

`BEN-002` subsequently passed integration review: exact overlap rejection preserves existing error matching, local-date/calendar boundaries return explicit resolution states, and meaningful memory-store tests bring package coverage to 78.3%. Its primary/property/mutation tests and vet pass. Domain evaluation has no side-effect ports; a future enrollment workflow remains separate work.

The non-web session started 19 Luna implementation lanes and assigned Sol review/refinement to every result. The frontend-owned roots remain assigned to the separate session. The baseline generated todo registry was stale relative to Markdown; an isolated registry snapshot in `.artifacts/lanes/` confirmed 481 open non-web entries before this work.

`CRM-002` and `READINESS-003` passed integration review and are recorded complete at their domain-support depth. Exact package tests and vet passed; CRM coverage is 76.1%, readiness 75.8%. Recruiting invalid consent now stops outreach without granting destructive retention actions. Review also corrected membership error sentinels, removed unused aliases, required immutable readiness pins, replaced a mutable reason registry, and replaced nominal golden/repeated-fixture tests with fixed expected digests and distinct outcomes.

The remaining wave is partial. Reviews found caller-forgeable publication flags, currency rounding and residual-unit errors, missing durable tax-election fences, and absent runtime consumers for telemetry and replanning. Those todos remain open until their required integration passes. Several customer, jurisdiction, and commercial selection todos require real decisions/evidence. No such evidence was fabricated. No non-web commits have been made yet; full repository gates and UI behavior are not claimed by these pure domain results.

## 5. Risks and follow-ups

- GWC gaps worth upstreaming: `@supports`/`@starting-style` constructors,
  stable (unhashed) keyframes names, multi-animation composition, and a raw
  `@media(print)`-exact emitter for dead-form preservation.
- The `@media(print)` dead blocks should get an explicit visual print check
  (or a deliberate fix to `@media print`) in a session with a browser gate.
- AGENTS.md loop compliance: unit + package suites green; e2e/responsive
  visual, a11y/i18n, and changelog/devlog steps are recorded here and in
  `CHANGELOG.md`. No browser was driven in this session (text-equivalence
  proof substituted); the next visual pass should spot-check launcher,
  focus rings, shimmer/route-progress, and print output.
