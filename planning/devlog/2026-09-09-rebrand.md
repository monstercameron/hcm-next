# Devlog 2026-09-09 — rebrand to Human Capital Management Suite

## What

Renamed all `hcm-next` branding refs to the new product name following the
GitHub repo rename to `monstercameron/human-capital-management-suite`
(old URL redirects; verified via `gh repo view` on both names):

- Go module `github.com/monstercameron/hcm-next` ->
  `github.com/monstercameron/human-capital-management-suite` (go.mod + all
  imports), `.proto` `go_package` options, `protoc-gen-go` regeneration.
- Sibling module `hcm-next-executor` ->
  `human-capital-management-suite-executor` (own go.mod, build + tests green).
- npm scope `@hcm-next/*` -> `@human-capital-management-suite/*`
  (package.json files, TS imports, tsconfig paths, lockfile relinked via
  `npm install`; typecheck + all vitest suites green).
- Display strings (`HCM Next` -> `Human Capital Management Suite`) in Go
  comments/UI text/help, TS brand pack (`brand.ts` id/name/logo/alt), JSON
  schema titles, READMEs, CI scripts, live planning specs/design docs.
- Regenerated every affected golden through its own sanctioned path (never
  hand-patched generator output): `buf generate` (pb.go), schemaflux
  `modelgen`, `tools/gen/modelgen`, `buf build` baseline
  (`HCMNEXT_COMPAT_REGEN_BASELINE=1`), connectorsdk/deferredschema/intg001/
  conformance/archdoc/productslice goldens, SBOM + signed provenance
  (`provgen` targeting the pinned windows/arm64), phaseone manifest
  (`HCMNEXT_UPDATE_PHASEONE_GOLDEN=1`), pinned digests updated only after
  proving each diff was exactly the rename.

## Incidents

- `SIGILL` in `tools/gen` traced to a poisoned `GOCACHE` inside the repo
  (`.artifacts/gocache` on the 9P `/mnt/c` mount served bit-flipped build
  archives; disassembly proved identical inputs, different codegen). Moved
  all Go caches to `/tmp`. `.artifacts/` is gitignored; no repo impact.
- `gen/TOOLS.lock` was found downgraded to buf 1.57.2 (policy pins 1.72.0,
  enforced by LIB-019). Restored the lock and regenerated all buf artifacts
  with buf 1.72.0; protoc-gen-go output is byte-identical across the two.
- Phase-one manifest refresh also absorbed pre-existing drift (golden was
  already stale at HEAD: 141/386 vs fresh 155/377 allowlist/deferred). The
  refreshed golden is byte-identical to a HEAD-tree refresh modulo the path
  prefix (verified via worktree diff).

## Deliberately NOT renamed (compat surface)

Crypto domain-separation strings, persisted provenance/version markers
(`SourceSystem`, manifest/asset/browser-state versions, backup envelope),
protocol values (API audiences, issuers, SPIFFE authority, cert CNs),
client storage keys (`hcm-next:*`, `hcm-next.*`), DB identifiers
(`hcm_next`, `hcm_next_cost_cents`), `HCMNEXT_*`/`HCM_NEXT_*` env vars,
`HCM_NEXT_*` adoption states, binary/proto-package/dir names
(`cmd/hcmnext`, `hcmnext/` proto package, `hcmnext` SBOM subject), Go/TS
identifiers, dated history (devlogs, CHANGELOG entries, dated audits,
research corpus, trackers). `definitions/planning/gates/p1a-manifest.yaml`
is also untouched: it is a SIGNED document and any content edit (including
this rename) invalidates its signature — re-signing needs the key holder.
Full list in the commit message / report.

## Verification (this session)

- `go build ./...` clean; `tools/gen/...` (minus two slow arch suites: archdoc
  green 452s, architecturedoc green at baseline), compatibility, contracts,
  schemaflux+drift, modelgen, connectorsdk, deferredschema, crosscut,
  phaseonegate, archdoc, productslice, conformance, connectivity, sbom,
  provenance, driftgate, release, vulnimpact, riskbinding, workspace,
  journey render/wasm, artifacts, inventory, oidc, libqualification and the
  display-touched suites all green (hermetic `GOPROXY=off`, clean cache).
- Executor module `go build` + `go test ./...` green.
- `tsc --noEmit` all workspaces green; vitest root (159) + all workspace
  suites green; eslint/prettier/gofmt gates green.
- Known-red, unrelated to the rebrand: `sbomgen` cmd tests fail on the
  license-completeness gate (5 tool-chain components without declared
  licenses; fails with and without network; dependency set untouched by
  this change). Depadmission `TestRunAgainstRealRepository` needs full
  module-graph network (`GOPROXY=off` fails it; passes with network).
  Gateevidence `TestP1AManifestMigrationChecksumsMatchEmbeddedMigrationsFS`
  fails identically at HEAD (manifest declares 112 migrations, embedded FS
  has 131) — pre-existing, unrelated. Browser playwright suite not run
  (needs live server + DB).

## 2026-09-10 leave substrate (DB-023, LEAVE-016)

DB-023 materializes the Medical Leave and Return-to-Work state in a new
owned package `internal/data/leavestore` over migration 00281 (twelve
tenant-scoped tables, RLS forced, append-only triggers, idempotency keys,
overlap/employment/compartment checks). LEAVE-016 adds the pure-domain
process anchor in `internal/domains/leave/anchor.go`: a mutex-guarded
`AnchorStore` binds the LEAVE-001 process and persists the CHANGE_REQUEST
intent, request revision 1 and LeaveRequested provenance with digest
idempotency. The anchor stays out of `internal/data` on purpose — no domain
package imports a data store, so INTEGRATION runs through the store's own
seams and DB-023's pgtest suite covers the SQL.

Defects found while turning RED to GREEN: `SELECT count(*) ... FOR UPDATE`
is illegal in PostgreSQL (moved the lock into a subquery); `FOR UPDATE`
under RLS needs the UPDATE grant, so `leave_availability_revision` carries
`SELECT, INSERT, UPDATE` like the sibling lock-read tables with the trigger
still forbidding mutation; negative probes sharing one transaction poisoned
it, so each refusal now runs in its own rolled-back transaction; the
employment probe first failed on the request FK instead of the employment
check until it referenced the seeded request; test queries used three wrong
column names (`absence_id`, `ending`, `ref`) and one wrong table
(`leave_availability_revision`, not `worker_availability`); the anchor bound
`string(uint8-family)` (a rune) into its digest instead of the family name,
missed the `LeaveRequested` event name in the record, and aliased stored
slices into returned records until a clone step was added. RED was proven
for both todos: without the overlap lock/grant the seed fails, without the
anchor replay branch duplicates fork and eight racers create eight anchors.

Decisions: checksums and signatures for the 00281 manifest entry come from
`migrations.Files()` and the manifest's own dev-key signer via throwaway
helpers under /tmp (never hand-copied); the evidence report and archdoc
snapshot were regenerated with their sanctioned generators
(Compile/Render, `archdoc -out`).

Verified: `go test -count=1 ./internal/data/leavestore/` PASS (6/6 incl.
golden, coverage 78.7%), `go test -count=1 ./internal/domains/leave/` PASS
(coverage 87.9%), `go vet` and `gofmt` clean on both,
`./tools/planning/gateevidence/` PASS (manifest checksum, signature,
report freshness), driftgate PASS, `./internal/data/health/` PASS,
tableinventory and placementbindings PASS. The race-detector run is
deferred to CI per AGENTS.md (no compiler toolchain on this machine); the
RACE matrix tests run eight goroutines each. Left partial: nothing in this
lane. Known-red and untouched (fails at HEAD, other owners):
`tools/policy/rlsparity` on the committed 00280 admission tables and the
safety store tables — no `leave_*` finding involves this change.

## 2026-09-10 leave domain kernels (LEAVE-002–LEAVE-011, LEAVE-014, LEAVE-017)

Follow-up group commit lands the twelve ticked leave kernel lanes as one
domain-area commit: snapshot, eligibility, plan, simulation, transaction,
commit, determination, reconcile-effects, replan, successor and the
evidence-review loop. Review read all ten source files: closed
vocabularies, frozen digests, employment/overlap guards and idempotent
registries throughout, no findings worth editing another lane's files.
`go test -count=1 ./internal/domains/leave/` PASS, coverage 87.9%
against the 70% floor; `go vet` and `gofmt` clean. No migration, manifest
or disposition fallout (pure-domain package, no new tables).

## 2026-09-10 legal entitlement kernels (LEGAL-003, LEGAL-004, LEGAL-006)

Group commit lands three ticked legal lanes: entitlement composition,
rule-change impact assessment and the obligation lifecycle. Review read
all three source files: closed authority/interaction/status vocabularies,
frozen digests, statutory-protection precedence and sealed waivers that
never delete the original — no findings. `go test -count=1
./internal/governance/legal/` PASS, coverage 83.4% against the 70%
floor; `go vet` and `gofmt` clean. Pure-domain addition, no migration
or manifest fallout.

## 2026-09-10 balance kernels (BAL-012, BAL-013)

Group commit lands two ticked balance lanes: the atomic posting
transaction and the availability dependency index. Review read both
source files: per-account heads, idempotent receipts, stale-head and
failpoint rollback, explicit old/new amounts in every finding — no
findings. `go test -count=1 ./internal/domains/balance/` PASS, coverage
81.6% against the 70% floor; `go vet` and `gofmt` clean. Pure-domain
addition, no migration or manifest fallout.

## 2026-09-10 delivery kernels (MSG-007, MSG-008, MSG-009, MSG-010, MSG-013)

Group commit lands five ticked delivery lanes: honest attempt/recipient
states, typed workflow signals, bounded reconciliation, redaction
scanning and legal-notice assessment. Review read all five source
files: dual lifecycles that never stand in for each other, exactly-once
signals, bounded retries with visible obligations, send gates on every
surface, version-pinned assessments — no findings. `go test -count=1
./internal/operations/messagingdelivery/` PASS, coverage 89.3% against
the 70% floor; `go vet` and `gofmt` clean. Pure-domain addition, no
migration or manifest fallout. Also repairs the previous commit's
CHANGELOG placement: the balance bullet now sits under its date header.

## 2026-09-10 work-item kernels (WORK-007, WORK-008, WORK-009)

Group commit lands three ticked work-item lanes: fair queue ordering,
compartment authorization and restricted review. Review read all three
source files: legal-authority precedence with reserved capacity,
manager-fact minimization with recorded decisions, typed findings
behind grants with sealed digests — no findings. `go test -count=1
./internal/humanwork/workitem/` PASS, coverage 75.9% against the 70%
floor; `go vet` and `gofmt` clean. Pure-domain addition, no migration
or manifest fallout.

## 2026-09-10 contact kernels (CONF-017, CONF-018)

Group commit lands two ticked contact lanes: typed endpoint updates
and ordered emergency-contact sets. Review read both source files:
tenant isolation with stale-revision refusal, single-use verification
challenges, priority uniqueness, labels that grant no authority,
minimum disclosure — no findings. `go test -count=1
./internal/domains/contact/` PASS, coverage 72.3% against the 70%
floor; `go vet` and `gofmt` clean. Pure-domain addition, no migration
or manifest fallout.

## 2026-09-10 outbox consumer group (EVENT-002)

Group commit lands the ticked outbox consumer-group lane: idempotent
dispatch, per-partition checkpoints, fenced duplicates, per-record
poison isolation with expiry requeue. Review found one real defect in
the lane output: `MemoryCheckpointStore.Poisoned` documented stable
order but returned Go map order — fixed with a partition/identity sort
and re-verified. `go test -count=1 ./internal/data/outbox/` PASS at
83.1% against the 70% floor (one load-flaky lease-timing test,
`TestBreak_RunHaltsWhenLeaseExpiresMidHandler`, fails only under full
parallel load and passes in isolation; unrelated type, no shared code
with this change); `go vet` and `gofmt` clean. No migration or manifest
fallout.

## 2026-09-10 org kernels (CONF-007, CONF-025)

Group commit lands two ticked org lanes: the manager-change lifecycle
and the ChangeManager intent compiler. Review read both source files:
closed story vocabulary with ordered lifecycle enforcement, cycle and
self-management refusal, authority-versioned approvals, typed
block/replan negatives, observation never asserted as authority — no
findings (one latent note: map-order first-match in edge resolution
only matters with overlapping edges, which Validate refuses, so left
untouched). `go test -count=1 ./internal/domains/org/` PASS, coverage
76.6% against the 70% floor; `go vet` and `gofmt` clean. Pure-domain
addition, no migration or manifest fallout.

## 2026-09-10 intent kernels (nine lanes)

Group commit lands nine ticked intent lanes: child emission
(INTENT-016), event policy conversion (INTENT-018), bulk batches
(INTENT-019), governed surfaces (INTENT-021), composition (INTENT-026),
preflight (INTENT-027), trigger discovery (WF-DISC-011), approval reuse
(REPLAN-002) and successor proposals (REPLAN-004). Review read all
nine source files: closed vocabularies, deterministic digests,
attenuated authority, typed negatives — no findings. `go test
-count=1` PASS for internal/intent (87.6%), approval (83.3%),
eventpolicy (96.7%) and surface (94.9%); `go vet` and `gofmt` clean.
Blocked, left untracked: schedule dispatch/intent (SCHED-003,
INTENT-017) — the engines/schedule package sits at 66.7% against the
floor on pre-existing occurrence.go debt, so staging it would redden
covergate; same standing exclusion as compensation/read.go.

## 2026-09-10 runtime kernels (WF-RUN-006, WF-RUN-007)

Group commit lands two ticked runtime lanes: the immediate-caller
retry policy and poison-node quarantine. Review read both source
files: capped backoff with injected jitter, one shared budget token
per scheduled retry, DO_NOT_RETRY above everything, quarantine that
retains everything and routes only to BLOCKED/REPAIR_REQUIRED/
QUARANTINED — no findings. `go test -count=1
./internal/workflow/runtime/` PASS, coverage 81.3% against the 70%
floor; `go vet` and `gofmt` clean. Note: retry.go imports the
untracked admission provisioner lane; that lane commits separately and
the worktree (which the hook tests) builds throughout.

## 2026-09-10 admission provisioner (ADMISSION-002)

Group commit lands the ticked admission lane: the durable retry-budget
provisioner the runtime retry policy consumes. Review read the source:
durable counters under one mutex, replay-safe attempt identities,
bounded single refunds, re-provisioning that never fabricates
allowance — no findings. `go test -count=1
./internal/operations/admission/` PASS, coverage 94.5% against the 70%
floor; `go vet` and `gofmt` clean. This completes the runtime retry
story (WF-RUN-006 now resolves its import inside history).

## 2026-09-10 legal-name kernel (CONF-019)

Group commit lands the ticked legal-name lane: structured multilingual
names, scanned evidence cites, jurisdiction obligations and one atomic
revision with separately authorized downstream effects. Review read the
source: no western-name assumption, single-script parts, confusable
bypass guard, deterministic sufficiency checklist with no score —
no findings. `go test -count=1 ./internal/domains/people/` PASS,
coverage 75.3% against the 70% floor; `go vet` and `gofmt` clean.
Pure-domain addition, no migration or manifest fallout.

## 2026-09-10 configuration rollback (CP-009)

Group commit lands the ticked rollback lane: governed activation of
the exact prior bundle at a new epoch. Review read the source:
digest-pinned selection, byte-reproduction proof, epoch advancement,
revocation and pinned-workflow guards, history-unchanged proof —
no findings. `go test -count=1 ./internal/platform/configbundle/`
PASS, coverage 70.9% against the 70% floor (thin margin, all of it in
long-committed files); `go vet` and `gofmt` clean. No migration or
manifest fallout.

## 2026-09-10 availability revision (AVAIL-003)

Group commit lands the ticked revision lane: mutex-guarded per-worker
append log with two-phase prepare/commit, digest-sealed revisions and
exactly-once crash recovery. Review read the source: one real finding —
History sorted raw sequence IDs lexicographically, misordering past
the tenth revision — fixed with zero-padded IDs plus a 12-revision
order regression test (proven to fail before, pass after); map-order
iteration is refusal-only or re-sorted, so output stays deterministic.
`go test -count=1 ./internal/domains/availability/` PASS, coverage
68.3% against the 70% floor (below_floor exception to 2026-12-31);
`go vet` and `gofmt` clean. Pure-domain addition, no migration or
manifest fallout.

## 2026-09-10 execution conflict preflight (CONFLICT-004)

Group commit lands the ticked preflight lane: read-only
ReevaluatePreflight over pinned versus current write intents with
sorted deterministic output, per-overlap explanations and a bound
verdict digest. Review read the source: no findings — map iteration
is re-sorted before output, overlap precedence (blocked beats
reapproval) matches the verdict switch, and the 16-worker race test
pins digest convergence. `go test -count=1
./internal/transaction/conflict/` PASS, coverage 85.3% against the
70% floor; `go vet` and `gofmt` clean. No migration or manifest
fallout.

## 2026-09-10 headcount requisition proof (CONF-021)

Group commit lands the ticked conformance proof: test-only addition
over the HEADCOUNT-001 API, no production change. Review read the
source: no findings — approval/creation conflation refused at every
step, exact decimal conservation (0.10+0.20=0.30 with float64 fenced
as corrupt), wrong-scale thousandths refused, idempotent reservation
consumption with single Held-to-Consumed event, Explain leak-scanned,
lifecycle pinned APPROVED→REQUESTED→CREATED→REQUESTED→OPEN.
`go test -count=1 ./internal/domains/headcount/` PASS, coverage 86.3%
against the 70% floor; `go vet` and `gofmt` clean. No migration or
manifest fallout.

## 2026-09-10 governed workflow cancellation (WF-RUN-010, WF-RUN-011, WF-STEP-009)

Group commit lands the ticked cancellation lanes: DecideCancellation and
PropagateCancellationToChildren over the new steps/subworkflow package
(version-pinned expansion, three-way scope attenuation, depth/fanout
bounds, cycle rejection, detach obligations, verbatim parent completion).
Review read the source: no findings — slice-ordered and sorted output is
deterministic, validity checks precede processing, repair outranks refusal
in the decision switch, and both race tests run 16 goroutines with digest
convergence. `go test -count=1 -cover ./internal/workflow/
./internal/workflow/steps/subworkflow/` PASS on windows/arm64 Go 1.26.3
(88.7%/100.0% against the 70% floor); `go vet` and `gofmt` clean on both
toolchains. Environment note: the full `internal/workflow` package
SIGILL-crashes under WSL Linux Go inside protobuf reflect on a
pre-existing WF-COMP-001 path, so Windows Go is the working test gate;
targeted cancel tests pass on both. No migration or manifest fallout.

## 2026-09-10 compensation change and composition (CONF-020, COMP-005)

Group commit lands the ticked compensation lanes: SimulateChange with
exact-decimal annualization, ReserveChange against a live budget fence
(expiry, context-digest and over-budget refusal), atomic CommitChange
behind disclosure authorization, failed-effect-only RepairChange, and
ComposePackage with omitted components carried at exact revisions,
revision-bumped children, correction chains and deterministic digests.
Review read the source: no findings — authorization precedes effects,
map use is membership-only with sorted outputs, and both race tests run
goroutines with convergence assertions. `go test -count=1
./internal/domains/compensation/` PASS on windows/arm64 Go 1.26.3;
package coverage 47.6% under the standing below_floor exception
(baseline 10.2% on 2026-09-06, expiry 2026-12-31; new change.go and
composition.go at 82–100%); `go vet` and `gofmt` clean. No migration or
manifest fallout.

## 2026-09-10 schedule conversion and dispatch (INTENT-017, SCHED-003)

Group commit lands the ticked schedule lanes: Converter turns frozen
occurrences into exactly one intent (DST-fold twins name distinct intents;
replay, leader overlap and post-crash return the identical intent; unknown
misfires and schedule revisions become typed receipts) and Dispatcher
ingests occurrence/event firings in sequence (redelivery replays the stored
outcome, rewinds refuse, stale firings receipt without work, poison never
records). Review read the source: no findings — instance-level mutexes
hold the only state, conflict comparison strips leader fencing before
DeepEqual, cursor advances only on recorded outcomes, and both race tests
run 16 goroutines with exactly-one-winner assertions. `go test -count=1
./internal/engines/schedule/` PASS on windows/arm64 Go 1.26.3; package
coverage 66.7% under the standing below_floor exception (baseline 61.5%
on 2026-09-06, expiry 2026-12-31); `go vet` and `gofmt` clean. No
migration or manifest fallout.

## 2026-09-10 rule re-evaluation before execution (RULE-004)

Group commit lands the ticked re-evaluation lane:
ReevaluatePromotionApproval replays the approval rules at execution time
over the approved plan — approval binding and input-digest tamper checks
first, then identical inputs confirm, moved inputs require re-approval
even when the tier holds, moved tiers and unknown inputs invalidate, and
republished-but-identical tables confirm under the new citation with the
original version retained. Review read the source: no findings — pure
function over committed rule types, validity before evaluation, verdict
precedence matches the todo contract. `go test -count=1
./internal/engines/rules/` PASS on windows/arm64 Go 1.26.3 (matrix has
no RACE label, matching the todo); package coverage 63.4% under the
standing below_floor exception (baseline 62.7% on 2026-09-06, expiry
2026-12-31); `go vet` and `gofmt` clean. No migration or manifest
fallout.

## 2026-09-10 agent trust boundary (AGENT-003, AGENT-004, AGENT-005)

Group commit lands the ticked agent lanes: OwnerRegistry binds each draft
tool to its reference/field/claim owners with a production IngestDraft
caller (closing the 2026-09-08 partial-evidence gap on AGENT-003),
Evaluator seals immutable version-bound eval runs behind Publisher, the
kill switch revokes leases by scope with deterministic fallback, incidents
bind exact versions and material, and one ActionCompiler emits attributed
receipt-bound drafts for concierge and domain agents. Review read the
source: no findings — validity and admission checks precede processing,
state lives on mutex-guarded values, digests sort their inputs, and the
race test runs 16 concurrent disable/invoke leases with none in flight
after. One dead test helper (`_ = value`) removed as refine.
`go test -count=1 ./internal/agentsecurity/` PASS on windows/arm64 Go
1.26.3 with 91.3% cover against the 70% floor; `FuzzOwnerRegistryIngestDraft`
10s PASS (499,393 execs, no crashes); `go vet` and `gofmt` clean. Missing
evidence lines added to the AGENT-003/004/005 ticks. No migration or
manifest fallout.

## 2026-09-10 timer-resume span links (OBS-013)

Group commit lands the ticked resume-link lane: the drift-checked timer
row's stored causal identity rides out of the transaction for one resume
span (new advanceInputsFunc causal return, run timerID, optional
ResumeSpanStarter so existing instrumentations compile unchanged), with
the OTel implementation linking the parked trace under the new timer_id
contract attribute and falling back to an identical unlinked span on any
unusable identity. Review read the diffs: no findings — the causal value
never governs (drift checks are unchanged and refuse first), the deferred
span ending reads the function-scoped err so commit failures close
FAILURE (pinned by TestResumeSpanMarksCommitFailure), and DeepEqual
receipt equality proves linked/unlinked business identity. `go test
-count=1` PASS on windows/arm64 Go 1.26.3 for all four packages
(execute 73.9%, timer 84.3%, platform/execution 75.3%,
platform/telemetry 82.7%, all against the 70% floor); `go vet` and
`gofmt` clean. The 2026-09-09 OBS-013 evidence already names these
tests; no tick change needed. No migration fallout (timer_id added to
the telemetry contract allowlist plus its compiled mirror).

## 2026-09-10 bulk, triage, edge and branch conformance (CONF-022/023/024, WF-STEP-007/008)

Group commit lands five ticked lanes: bounded bulk acknowledgement
(versioned frozen audience, bounded partitions, per-child bindings,
honest legal satisfaction), agent case triage (quarantine/redaction
first, hostile roles and write attempts refused, cited routing),
the twelve-case adversarial edge matrix (exact outcome dimensions with
authority bindings and zero prohibited effects), bounded PARALLEL
execution and typed JOIN strategies. Review read the sources: no
contract findings — validation precedes execution everywhere, outputs
are slice-ordered or sorted, and every race test runs goroutines with
convergence assertions (16 for parallel, 12 concurrent matrix cases,
8 for audience freeze). One observation for the owning lane, not a
blocker: triage forces specialist routing on protected signals in the
subject line, while protected signals in message bodies are redacted
from the prompt without forcing specialist routing — the tested
contract; tightening it is a product-semantics decision. `go test
-count=1` PASS on windows/arm64 Go 1.26.3 for all four packages
(bulkack 94.5%, edges 94.6%, triage 96.8%, parallel 84.6%, all against
the 70% floor); `go vet` and `gofmt` clean. Evidence lines added to all
five ticks. No migration or manifest fallout.

## 2026-09-10 planning and policy tooling (GOV/ARCH/CLOSE/WF-DISC/ROLLOUT/IAC batch)

Group commit lands the ticked governance lanes: design-closure register
(CLOSE-001), design-ownership records (WF-DISC-009), direct-capability
dispositions (WF-DISC-008), oracle strength/specificity gates (GOV-021,
GOV-028), engine-ownership registry (GOV-029), IaC-stack contracts
(IAC-001), the workflow-design toolchain registry/archetypes/decisions/
design/join/expansion/promotion (WF-DISC-001 through WF-DISC-007 plus
WF-DISC-010 scenario matrices), rollout-plan validation and activation
(ROLLOUT-001), config boundaries (ARCH-GO-024), the control-crosswalk
test repair (GOV-030: E-16 now asserted Implemented from the registry,
forgery probe moved to F-13, mutation makes a real status change),
the regenerated intentcoverage digest (GOV-026). The traceability
dot-directory regression test (GOV-003, `scan_dotdirs_test.go`, verified
green standalone) is staged out of this commit: covergate judges staged
packages by their `go test` lines and the traceability package is red
on the pre-existing evidence-less ticks below, so no file in that
package can land until the orphan backlog is resolved. Review skimmed
the sources: no
contract findings — pure validators with sorted deterministic output;
two hardening observations for the owning lanes, neither a blocker
(triage message-body routing noted previously; engineownership Verify
joins owner packages under the caller root without a `..` guard —
dev-time tool over the repo's own registry, but worth pinning).
`go test -count=1` PASS on windows/arm64 Go 1.26.3 for every touched
package (84–97% cover, all against the 70% floor); `go vet` and `gofmt`
clean. Defect fixed in this pass: my AGENT-003 evidence line used the
`_Golden` suffix shorthand that the extractor expands with an
underscore (`TestOwnerRegistryIngestDraft_Golden`, nonexistent) — now
the brace form with exact names; the same-class pre-existing
ADMISSION-002 triple-suffix names repaired the same way; my CONF-020/
021/022/023/024 slash shorthand converted to brace form so the matrix
names actually register. Traceability re-run confirms zero "does not
exist" findings. Left red (pre-existing, not mine): the traceability
package still lists evidence-less ticks (INTENT-019, REPLAN-002/004,
MSG-009/013, UX-006/007/008, LEGAL-003/004/006, DOC-SIGN-001,
WORK-007/008/009, BAL-012/013, AVAIL-003, LEAVE-003 through LEAVE-011
plus LEAVE-014/017, WF-DISC-011, CLIENT-001/002) — backfilling
those is a separate registry-repair decision, and WF-DISC-011 has no
implementation in the tree to land. No migration fallout.

## 2026-09-10 domain kernels batch (CONF-006, PROMO-008, DATA-015, EVIDENCE-001, OPS-006, DOC-SIGN-001, ARCH-GO-024)

Group commit lands seven ticked lanes: payroll correction and retro
repair (CONF-006), promotion persistence traces (PROMO-008),
transaction lineage (DATA-015), business execution receipts
(EVIDENCE-001), epoch-fenced workload drain (OPS-006), proof-bound
e-signature ceremonies (DOC-SIGN-001, evidenced in this pass) and the
configuration kernel (ARCH-GO-024, claimed by its evidence). Review read
the signing ceremony fully and scanned the rest for kernel purity: no
findings — no wall-clock, network, database or package-level mutable
state; validity precedes effects; digests sort their inputs. One
comment-only refine (nonsense "Oligocene-free ticks" wording). `go test
-count=1` PASS on windows/arm64 Go 1.26.3 for all seven packages
(71.4–95.9% cover, all against the 70% floor with no exceptions
needed); `go vet` and `gofmt` clean. No migration or manifest fallout.

## 2026-09-10 worker/journey/diagnostic hardening (unticked — registry gap)

Group commit lands lane hardening that has no dedicated todo ticks, so
it lands disclosed rather than attributed: `workerids.Preview` (bounded
range-jumping previews with unit-aware ordinals; replaces an exclusion
loop with no upper bound), worker-specific declared baselines in
`transport/journey` and `intent/app` with unknown-worker refusal and
span events, shared `fixtures.JanePromotionBase/Bonus` constants, and
the transport `ErrorType` classification behind an unexported capability
grant (`serve.go` logs only the bounded type, and the new test pins
that a secret payload never reaches the diagnostic). Review read the
diffs: no findings — validity precedes effects, no ambient state, the
grant type is unexported so only this package unlocks internal
diagnostics. `go test -count=1` PASS on windows/arm64 Go 1.26.3
(transport 35.8%, intent/app 26.1%, simulate 72.1%, workerids 86.6%,
application 81.0%, fixtures 56.2% — each at or above its standing
below_floor baseline); `go vet` and `gofmt` clean. Follow-up for the
backlog owner: register ticks for this batch (worker-ID previews,
declared baselines, diagnostic classification) so the traceability
corpus owns them. No migration or manifest fallout.
