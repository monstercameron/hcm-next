# HCM Next Production TDD Backlog

This is the dependency-ordered delivery backlog for turning the architecture in
[the master plan](plan.md) into a production HCM Next platform. `plan.md` remains
the product and architecture constitution. [The Phase 1 execution plan](execution-plan.md)
controls near-term sequencing and implementation depth. A todo cannot use this
file to expand Phase 1 beyond those authorities.

The backlog deliberately includes later production obligations so they are not
left implicit. Their phase labels prevent them from becoming accidental Phase 1
scope. Completing prose or a diagram never satisfies an implementation todo.

## How an agent must execute a todo

Every todo uses test-driven development in this order:

```text
TEST
  Name the exact Go Test/Fuzz/Benchmark entry point before implementation.
  The Markdown **TEST** field and TodoContract.test_name must agree.

RED
  Add the named test or conformance fixture first.
  Run it and capture the expected failure for the missing behavior.

GREEN
  Implement only enough production behavior to make that test and all existing
  tests pass. The listed expected result is part of the contract.

REFACTOR
  Improve structure without changing observable contracts. Re-run unit,
  integration, conformance, race, fuzz, security and recovery suites applicable
  to the todo.

EVIDENCE
  Attach test command, result, relevant artifact digests and source links to the
  delivery manifest. A checkbox without evidence is not complete.
```

Every todo block must contain exactly one `Depends`, `INTENT CONTEXT`, `TEST`,
`TEST MATRIX`, `RED`, `GREEN`, `REFACTOR` and `Refs` field. Combined `RED/GREEN` fields are prohibited because
they do not prove that the test failed before implementation. A planning,
documentation or research deliverable is still testable when its output has a
schema, completeness oracle, reproducibility check, link check, contradiction
check or signed evidence gate. A genuinely non-testable action must be marked
`EVIDENCE_ONLY` with an approved rationale; it may not claim TDD completion.
`TEST` is the primary red/green test; `TEST MATRIX` declares every additional applicable test class.
`UNIT_ONLY` is permitted only with an applicability reason generated by
`GOV-018`—silence never means that integration, race, fuzz or fault behavior was
considered and dismissed.

Each Markdown todo is the human-readable summary of one machine-readable
`TodoContract`. Before the RED commit, `planning/todos.schemaflux` and the
generated registry must supply these additional required fields for that ID:

```text
test_package       Go import path
test_name          exact Test/Fuzz/Benchmark name
intent_context     role, accepted intent sets, exact definitions when known,
                   kernel families, direct/child relationship and rationale
tests[]            kind, name, package, command, oracle and timeout
commands[]         exact clean-checkout commands
fixtures[]         checked-in input and fault-schedule paths
red_oracle         typed code/state/fields and zero-effect assertions
green_oracle       returned/persisted state, event counts and invariants
evidence_outputs[] paths plus canonical digest algorithm
timeouts           unit/integration/recovery bounds
```

The default command set is `go test <package> -run '^<test_name>$' -count=1`,
`go test -race <affected packages> -count=1`, applicable fuzz targets with a
recorded seed/corpus, and `go test ./... -count=1`. Integration, browser,
conformance, chaos and recovery todos must add their named harness command.
`GOV-002` rejects a todo whose registry entry omits any applicable field; this
prevents a prose-only GREEN interpretation even where the Markdown stays terse.

### BusinessIntent context required by every todo

`BusinessIntent` is the semantic instruction set for material HCM behavior, not
a label to paste onto infrastructure. Every todo therefore declares exactly one
`INTENT CONTEXT` with one of these roles:

| Role             | Meaning                                                                                          |
| ---------------- | ------------------------------------------------------------------------------------------------ |
| `DIRECT`         | Contracts or implements named accepted `IntentDefinition` behavior                               |
| `COMPOSITE`      | Coordinates a root intent and explicit child/follow-up intents                                   |
| `EMITTER`        | Turns a schedule, event, analysis, batch or condition into governed intents                      |
| `EXPOSURE`       | Lets a human, agent, API, app or operator create/consume intents without bypass                  |
| `DOMAIN_SUPPORT` | Supplies domain semantics used by an intent set but is not itself an intent                      |
| `CONFORMANCE`    | Proves intent contracts or paths without adding production behavior                              |
| `GOVERNANCE`     | Controls catalog, authority, policy, lifecycle or evidence for intent execution                  |
| `SUBSTRATE`      | Supplies reusable mechanics; it must name the intent sets it supports and claim no direct intent |

The `SETS` value is a domain name, not a numeric range. The domains are
`BI.PEOPLE`, `BI.WORKFORCE`, `BI.REWARDS`, `BI.PAYROLL`, `BI.REGULATORY`,
`BI.RECRUITING`, `BI.LIFECYCLE`, `BI.ACCESS`, `BI.TALENT`, `BI.EXPERIENCE`,
`BI.CASES`, `BI.MOBILITY`, `BI.PRIVACY`, `BI.DOCUMENTS`, `BI.WORK`,
`BI.INTEGRATION`, `BI.DATAOPS`, `BI.SECURITY`, `BI.ANALYTICS`,
`BI.INTELLIGENCE`, `BI.OPERATIONS`, `BI.COMMERCIAL`, `BI.TENANT` and
`BI.TRIGGERS`; `BI.ALL` is their union. A domain set is a naming scope, not a
promise that definitions exist in it: today only `BI.PEOPLE`, `BI.REWARDS`,
`BI.WORK`, `BI.INTELLIGENCE` and `BI.OPERATIONS` contain a drafted definition.
`BI.EXT.*` is retired with the catalog numbering. An exact `INTENTS` entry may
name only one of the fourteen checked-in definitions; there is no pending
source manifest and no partition context.

`DIRECT=none` is mandatory for `SUBSTRATE`, `GOVERNANCE`, `DOMAIN_SUPPORT`,
`EXPOSURE` and ordinary `CONFORMANCE` todos. A direct/composite todo names exact
definition refs when available, its kernel family, and any emitted children.
`GOV-025` rejects vague `ALL` usage without a bounded ownership reason, an
intent name that is not one of the fourteen checked-in definitions, a
`FAMILY` outside the three kernel families, and any attempt to treat a
database, library, workflow node or feature surface as a BusinessIntent merely
to satisfy coverage.

### Secondary test taxonomy and required return contracts

| Test class    | Required when                                                         | Passing return contract                                                                                                                 |
| ------------- | --------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| `PROPERTY`    | value objects, temporal/decimal/rule/graph invariants                 | generated cases preserve stated algebra, boundaries and canonical result/error                                                          |
| `GOLDEN`      | canonical bytes, schemas, transforms, reports or generated artifacts  | exact bytes/digest/diagnostics match a versioned checked-in oracle                                                                      |
| `FUZZ`        | parsers, validation, expressions, envelopes or hostile input          | bounded corpus produces no panic/leak and never accepts an invalid semantic value                                                       |
| `RACE`        | leases, CAS, queues, caches, timers, idempotency or shared state      | `go test -race` reports zero races and invariant counts show no loss/duplication                                                        |
| `INTEGRATION` | database, object store, transport, provider or multi-package boundary | real adapter/harness returns the same typed result, persistence and error semantics                                                     |
| `FAULT`       | retry, timeout, ambiguity, crash, recovery or external effects        | each failpoint reaches an allowed durable state with zero lost/duplicate effect                                                         |
| `SECURITY`    | identity, AuthZ, tenant, secrets, privacy, DLP or untrusted content   | unauthorized cases deny without existence/data leakage and emit bounded evidence                                                        |
| `CONFORMANCE` | interchangeable adapters/domains/channels or specification families   | every implementation passes the same fixtures and semantic digest/error oracle                                                          |
| `BROWSER`     | GWC, grpcbridge UI, forms, accessibility or human interaction         | rendered/action result matches API semantics and accessibility/privacy assertions                                                       |
| `RECOVERY`    | backup, restore, replay, rebuild, failover or destructive correction  | restored state reaches declared RPO/RTO and verifies ledger/projection/effect invariants                                                |
| `BENCHMARK`   | latency, throughput, memory, population, batch or scale claim         | named benchmark reports declared workload, percentile/resource budget and regression bound                                              |
| `MUTATION`    | authority-, money-, privacy-, lifecycle- or effect-bearing logic      | seeded semantic mutants are killed; survivors block completion or receive reviewed equivalent proof                                     |
| `MODEL_BASED` | state machines, lifecycle graphs, orchestration or cross-engine plans | generated command sequences match the reference model, cover declared transitions/invariants and shrink to a reproducible failing trace |

Additional test classes stop being useful when they cannot exercise a distinct
failure mode or observable contract. `GOV-018` records that boundary per todo;
adding redundant tests merely to increase counts is prohibited.

Completion additionally requires:

- production code is Go; authored developer tooling may use any language and
  is excluded from the release image;
- HCM Next owns every HCM semantic contract; third-party Go modules provide
  infrastructure mechanics only and remain replaceable behind owned packages;
- GWC/GoWebComponents, grpcbridge and SchemaFlux are the preferred UI, edge
  and generator, each usable only after its qualification fixture passes
  (`TOOL-004`, `TOOL-008`, `UX-QUAL-001`) and each with a named fallback (Go
  server-rendered HTML, grpc-gateway or connect-go, protoc with Go codegen);
  every other non-standard module requires qualification, confinement and
  replacement evidence;
- public/service contracts are Protobuf and gRPC;
- the qualified edge is the supported browser/HTTP adaptation boundary;
- no Node, TypeScript, React or Vite runtime is in the P1B release image;
  legacy may run beside the Go slice through P1A;
- no todo adds a lifecycle dimension, kernel family, workflow primitive or
  coordination layer without a recorded scope exchange;
- tenant, organization, field, purpose, authority, temporal and evidence rules
  remain explicit;
- tests assert returned values, states, events, evidence or errors—not merely
  that a call did not panic.

See [Go technology constitution](specs/go-only-technology-constitution.md),
[canonical envelope and digest](specs/canonical-envelope-and-digest.md),
[P1A/P1B contents](next-steps.md), and
[Phase 1 implementation-depth matrix](plan.md#phase-1-implementation-depth-matrix).

## Model-intelligence labels

| Label      | Assign when                                                                           | Review requirement                                       |
| ---------- | ------------------------------------------------------------------------------------- | -------------------------------------------------------- |
| `LUNA`     | Mechanical, local and already fully specified                                         | Normal code review                                       |
| `TERRA`    | Several packages or one well-bounded subsystem                                        | Domain-owner review                                      |
| `SOL_LOW`  | Cross-plane behavior, concurrency, security or difficult migration                    | Senior architecture review                               |
| `SOL_HIGH` | Authority-bearing, legally/financially material, distributed or irreversible behavior | Two independent reviewers plus explicit failure evidence |

These labels describe the minimum model intelligence for implementation, not
business priority. An agent may be upgraded but never downgraded without an
approved backlog change.

## Phase and gate labels

| Label               | Meaning                                                                                       |
| ------------------- | --------------------------------------------------------------------------------------------- |
| `P0`                | Toolchain or planning prerequisite for P1A; must be small enough to finish in M2              |
| `GATE_A`            | P1A: paid read-only observation, simulation and diff; zero workforce mutation, zero effects   |
| `GATE_B`            | P1B: one governed write (promote + base pay) for one partner                                  |
| `GATE_C`            | General production authority; everything moved here by the 2026-09-02 execution-plan revision |
| `PHASE_2`–`PHASE_5` | Later evidence-gated expansion                                                                |
| `CONFORMANCE`       | Design/test fixture only; no production subsystem                                             |
| `DESIGN`            | Contract text only; no test, no phase; revisited when a consumer is funded                    |
| `RETIRED`           | Withdrawn 2026-09-02; block kept for traceability; dependencies on it resolve as satisfied    |
| `OUT`               | Explicitly out of the current delivery phase                                                  |

A label is a candidate, not a release inventory. The exact contents of P1A and
P1B are the lists in [next-steps.md](next-steps.md); a `GATE_A` todo that is
not needed by those lists is not in P1A no matter what it says. Where this
file and next-steps.md disagree, next-steps.md wins.

## Alignment with the 2026-09-02 plan revisions

The planning corpus was revised on 2026-09-02. This backlog was written before
that revision and is read through the rules below; a todo's text is
subordinate to them.

**Vocabulary.** Todos that still use the earlier vocabulary mean the current
one:

```text
seven kernel families            -> three: CHANGE_REQUEST, CALCULATION_REQUEST,
                                    ANALYTICAL_REQUEST; process, filing, batch
                                    and case are ChangeRequest attributes
eleven lifecycle dimensions      -> five: RequestState, ExecutionState,
                                    BusinessState, ConsistencyState,
                                    ObligationState; no compatibility lattice
seventeen step types             -> ten core + three structural; CHECKPOINT is
                                    a safe_point attribute, RULE is
                                    DECISION+rule_ref, AGENT and DOCUMENT are
                                    CAPABILITY calls
530 / 516 / 807 catalog counts   -> the catalog is the fourteen drafted
                                    definitions; the intake list is
                                    non-normative vocabulary with no work items
CATALOGUED maturity              -> retired; a name below DRAFT_CONTRACT is not
                                    in the catalog
control snapshot in the digest   -> revalidated context; approval binds the
                                    material digest only
Go-only / GWC, grpcbridge,       -> Go core; the three libraries are preferred
SchemaFlux as mandatory             with a qualification fixture and a named
                                    fallback each; Node dev tooling is allowed
secure inbox as a channel        -> a MINIMAL CONTRACT inbox record in the
                                    workspace; P1B sends one email kind only
```

**Retired work.** The following programs have no consumer and are withdrawn
as a whole; their todos carry `[RETIRED]` and any dependency on them resolves
as satisfied: catalog source recovery and attestation (`MODEL-008`,
`MODEL-009`, `NEXT-001`, `SOURCE-*`), the 530-slot and 807-name vertical-slice
program (all of §61), the seventeen-step-type items for `RULE`, `AGENT`,
`DOCUMENT` and `CHECKPOINT`, and the form engine (`FORM-001`–`FORM-003`).

**Frozen work.** No further adversarial audit pass, no new specification, and
no new todo section until P1A executes against partner data. A todo that
proposes a new lifecycle dimension, kernel family, workflow primitive, or
coordination layer is rejected by `GOV-006` without a recorded scope exchange.

**Section dispositions.** Each numbered section below opens with a
`Disposition (2026-09-02)` note stating what in it is P1A, P1B, Gate C,
deferred, or retired. The note overrides individual labels where they
conflict; labels are corrected on the items whose substance also changed.

## Global completion gates

A todo is complete only when its tests pass in a clean checkout, its references
resolve, its dependency todos are complete, and its evidence is recorded. Tests
must cover applicable negative dimensions from the
[model audit](data/models/adversarial-model-audit-2026-08-14.md#negative-dimensions-required-by-every-future-binding):

```text
missing / unknown / stale / redacted / unavailable input
concurrent conflict / stale baseline / duplicate / replay
authorization denial / legal prohibition / invalid approval
cancel before and after irreversible effect
partial external success / ambiguous commit / repair
effective-at versus known-at / correction / supersession
retention / legal hold / DLP / egress / tenant isolation
provider / cell / identity / key / time / control-plane outage
accessibility / delegation / representation / human escalation
```

---

## 0. Backlog governance and traceability

> **Disposition (2026-09-02):** P1A for `GOV-001`–`GOV-004`, `GOV-006`, `GOV-009` and `GOV-025` only, and only at the depth a compiled-in registry needs. `GOV-002` treats `[RETIRED]` as a resolved dependency. Coverage-matrix, traceability-graph and manifest-compiler items are DEFERRED until P1B; the backlog is not itself a product.

- [x] `GOV-001` **[P0][LUNA] Create the delivery-manifest schema.**
  - **Depends:** none.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestDeliveryManifestRejectsMissingRequiredFields`.
  - **TEST MATRIX:** `PRIMARY=TestDeliveryManifestRejectsMissingRequiredFields`; `GOLDEN=TestTodo_GOV_001_Golden`.
  - **RED:** `TestDeliveryManifestRejectsMissingRequiredFields` returns violations for absent owner, estimate, gate, dependencies, acceptance evidence, operations owner or displaced scope.
  - **GREEN:** a complete fixture validates with zero violations and emits a stable canonical digest.
  - **REFACTOR:** generate Go validation from the SchemaFlux definition; no parallel handwritten rules.
  - **Refs:** [Execution-plan scope rules](execution-plan.md#scope-rules), [adversarial gap closure](specs/adversarial-gap-closure-2026-08-13.md).
  - **Evidence (2026-09-03):** `TestDeliveryManifestRejectsMissingRequiredFields`, `TestTodo_GOV_001_Golden` in `tools/planning/manifest` (subcommand of `go run ./tools/planning/cmd/plancheck`); `go test -count=1 ./tools/planning/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `GOV-002` **[P0][TERRA] Register every todo in a machine-readable backlog.**
  - **Evidence (2026-09-03):** `TestTodoRegistryMatchesMarkdown` in `tools/planning/todoregistry`; `go test -count=1 ./tools/planning/todoregistry/...` PASS on windows/arm64 (Go 1.26.3); `go vet`, `gofmt -l`, `staticcheck` clean; branch plan-revision-2026-09-02.
  - **Depends:** `GOV-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodoRegistryMatchesMarkdown`.
  - **TEST MATRIX:** `PRIMARY=TestTodoRegistryMatchesMarkdown`; `GOLDEN=TestTodo_GOV_002_Golden`.
  - **RED:** `TestTodoRegistryMatchesMarkdown` reports any missing/duplicate ID, invalid phase, invalid intelligence label or unresolved dependency.
  - **GREEN:** generated registry count equals Markdown todo count and every dependency resolves to exactly one earlier or explicitly external gate.
  - **REFACTOR:** make Markdown a generated view or verify a single canonical source; never maintain two unchecked lists.
  - **Refs:** [Master-plan hierarchy](plan.md), [specification ownership registry](specs/specification-ownership-registry.md).

- [x] `GOV-003` **[P0][TERRA] Build requirement-to-evidence traceability.**
  - **Depends:** `GOV-001`, `GOV-002`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestRequirementTraceabilityRejectsOrphans`.
  - **TEST MATRIX:** `PRIMARY=TestRequirementTraceabilityRejectsOrphans`; `GOLDEN=TestTodo_GOV_003_Golden`; `RACE=TestTodo_GOV_003_Race`.
  - **RED:** `TestRequirementTraceabilityRejectsOrphans` fails for a requirement without owner, contract, code target, test and evidence target.
  - **GREEN:** every Gate A/B requirement resolves through `requirement -> todo -> test -> artifact/evidence`; retired targets remain historically resolvable.
  - **REFACTOR:** emit the crosswalk from SchemaFlux dependency data.
  - **Refs:** [Platform coverage rule](execution-plan.md#capability-coverage-rule), [coverage matrix](specs/platform-capability-coverage-matrix.md).
  - **Evidence (2026-09-03):** `TestRequirementTraceabilityRejectsOrphans`, `TestTodo_GOV_003_Golden`, `TestTodo_GOV_003_Race` in `tools/planning/traceability` (subcommand of `go run ./tools/planning/cmd/plancheck`); `go test -count=1 ./tools/planning/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `GOV-004` **[P0][LUNA] Add planning-link validation.**
  - **Evidence (2026-09-03):** `TestPlanningLinks` in `tools/planning/links`; `go test -count=1 ./tools/planning/links/...` PASS on windows/arm64 (Go 1.26.3); `go vet`, `gofmt -l`, `staticcheck` clean; branch plan-revision-2026-09-02.
  - **Depends:** none.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestPlanningLinks`.
  - **TEST MATRIX:** `PRIMARY=TestPlanningLinks`; `GOLDEN=TestTodo_GOV_004_Golden`.
  - **RED:** `TestPlanningLinks` lists a deliberately broken local Markdown link and missing anchor.
  - **GREEN:** all links and anchors in `planning/**/*.md`, including todo references, resolve in a clean checkout.
  - **REFACTOR:** expose a concise file/line diagnostic suitable for CI annotations.
  - **Refs:** [Planning hierarchy](plan.md), [spec index](specs/README.md).

- [x] `GOV-005` **[P0][TERRA] Enforce implementation-depth vocabulary.**
  - **Depends:** `GOV-002`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestDepthVocabularyRejectsSilentPromotion`.
  - **TEST MATRIX:** `PRIMARY=TestDepthVocabularyRejectsSilentPromotion`; `GOLDEN=TestTodo_GOV_005_Golden`; `CONFORMANCE=TestTodo_GOV_005_Conformance`.
  - **RED:** `TestDepthVocabularyRejectsSilentPromotion` rejects `DEFINED`, `PARTIAL`, `contract only` or architecture prose used as staffing authority.
  - **GREEN:** every deliverable maps to `IMPLEMENT`, `MINIMAL CONTRACT`, `DESIGN / CONFORMANCE ONLY` or `OUT OF PHASE`, independently from maturity and coverage.
  - **REFACTOR:** centralize normalization in one Go package consumed by planning and release checks.
  - **Refs:** [Execution-plan scope rules](execution-plan.md#scope-rules), [cross-phase foundation track](plan.md#cross-phase-foundation-track).
  - **Evidence (2026-09-03):** `TestDepthVocabularyRejectsSilentPromotion`, `TestTodo_GOV_005_Golden`, `TestTodo_GOV_005_Conformance` in `tools/planning/depthvocab` (subcommand of `go run ./tools/planning/cmd/plancheck`); `go test -count=1 ./tools/planning/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `GOV-006` **[P0][TERRA] Implement the scope-exchange checker.**
  - **Depends:** `GOV-001`, `GOV-005`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestScopeExchangeRejectsUnfundedAddition`.
  - **TEST MATRIX:** `PRIMARY=TestScopeExchangeRejectsUnfundedAddition`; `GOLDEN=TestTodo_GOV_006_Golden`.
  - **RED:** `TestScopeExchangeRejectsUnfundedAddition` fails when a Gate A/B addition lacks owner, schedule impact, acceptance evidence and equivalent displaced scope.
  - **GREEN:** an approved exchange records the removed item, changed critical path and approval digest.
  - **REFACTOR:** reuse delivery-manifest identities instead of free-form references.
  - **Refs:** [Scope-exchange rule](execution-plan.md#scope-exchange-rule), [portfolio discipline](plan.md#312-portfolio-discipline).
  - **Evidence (2026-09-03):** `TestScopeExchangeRejectsUnfundedAddition`, `TestTodo_GOV_006_Golden` in `tools/planning/scopeexchange` (subcommand of `go run ./tools/planning/cmd/plancheck`); `go test -count=1 ./tools/planning/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `GOV-007` **[P0][LUNA] Check todo atomicity.**
  - **Depends:** `GOV-002`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodoAtomicity`.
  - **TEST MATRIX:** `PRIMARY=TestTodoAtomicity`; `GOLDEN=TestTodo_GOV_007_Golden`.
  - **RED:** `TestTodoAtomicity` rejects a fixture containing two independently shippable verbs or no observable expected result.
  - **GREEN:** every todo has one primary deliverable, one bounded test group and a deterministic completion signal.
  - **REFACTOR:** permit a small declared test matrix without turning one todo into a subsystem epic.
  - **Refs:** [Near-term planning priorities](plan.md#17-near-term-planning-priorities).
  - **Evidence (2026-09-03):** `TestTodoAtomicity`, `TestTodo_GOV_007_Golden` (36 advisory two-verb titles listed by `plancheck atomicity`) in `tools/planning/atomicity` (subcommand of `go run ./tools/planning/cmd/plancheck`); `go test -count=1 ./tools/planning/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `GOV-008` **[P0][TERRA] Add test-evidence freshness rules.**
  - **Depends:** `GOV-003`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestEvidenceFreshnessRejectsHistoricalClaim`.
  - **TEST MATRIX:** `PRIMARY=TestEvidenceFreshnessRejectsHistoricalClaim`; `GOLDEN=TestTodo_GOV_008_Golden`.
  - **RED:** `TestEvidenceFreshnessRejectsHistoricalClaim` rejects a prior Markdown statement or evidence generated from a different commit/toolchain.
  - **GREEN:** evidence records commit digest, toolchain digest, environment, command, timestamp and result.
  - **REFACTOR:** deduplicate identical artifact metadata through content addressing.
  - **Refs:** [Legacy baseline rule](execution-plan.md#legacy-baseline-rule), [canonical digest](specs/canonical-envelope-and-digest.md).
  - **Evidence (2026-09-03):** `TestEvidenceFreshnessRejectsHistoricalClaim`, `TestTodo_GOV_008_Golden` (allow-list `known_evidence_freshness_gaps` in definitions/planning/known-defects.yaml, expiry 2026-10-15) in `tools/planning/evidence` (subcommand of `go run ./tools/planning/cmd/plancheck`); `go test -count=1 ./tools/planning/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `GOV-009` **[P0][LUNA] Add a deferred-scope import guard.**
  - **Depends:** `GOV-005`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestPhaseOneImportsRejectDeferredSubsystems`.
  - **TEST MATRIX:** `PRIMARY=TestPhaseOneImportsRejectDeferredSubsystems`; `PROPERTY=TestTodo_GOV_009_Property`; `GOLDEN=TestTodo_GOV_009_Golden`.
  - **RED:** `TestPhaseOneImportsRejectDeferredSubsystems` fails when Gate A/B packages import Kafka, ClickHouse, OpenSearch, vector, full billing, payroll calculation or omnichannel modules.
  - **GREEN:** bounded interfaces may exist, but the production dependency graph contains no prohibited mandatory dependency.
  - **REFACTOR:** drive the forbidden set from the implementation-depth registry.
  - **Refs:** [Explicit non-goals](execution-plan.md#explicit-non-goals), [Phase 1 scope](plan.md#phase-1-changeops-overlay).
  - **Evidence (2026-09-03):** `TestPhaseOneImportsRejectDeferredSubsystems`, `TestTodo_GOV_009_Property`, `TestTodo_GOV_009_Golden` in `tools/planning/deferredimports` (subcommand of `go run ./tools/planning/cmd/plancheck`); `go test -count=1 ./tools/planning/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `GOV-010` **[P0][SOL_LOW] Create the authority-gate decision record.**
  - **Depends:** `GOV-001`, `GOV-003`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestAuthorityGateRejectsIncompleteEvidence`.
  - **TEST MATRIX:** `PRIMARY=TestAuthorityGateRejectsIncompleteEvidence`; `GOLDEN=TestTodo_GOV_010_Golden`; `FAULT=TestTodo_GOV_010_Fault`; `SECURITY=TestTodo_GOV_010_Security`.
  - **RED:** `TestAuthorityGateRejectsIncompleteEvidence` returns every missing security, privacy, correctness, reconciliation, recovery, governance and continuity class.
  - **GREEN:** Gate A/B/C decisions are immutable, proposal-bound, signed and return exactly `PROCEED`, `REMEDIATE`, `NARROW`, `RESELECT_WEDGE` or `STOP`.
  - **REFACTOR:** share decision/evidence primitives with governed business approvals.
  - **Refs:** [Trust and assurance](plan.md#115-trust-and-assurance), [delivery gates](execution-plan.md#delivery-gates-staffing-envelope-and-critical-path).
  - **Evidence (2026-09-03):** `TestAuthorityGateRejectsIncompleteEvidence`, `TestTodo_GOV_010_Golden`, `TestTodo_GOV_010_Fault`, `TestTodo_GOV_010_Security` in `tools/planning/authoritygate` (`definitions/planning/authority-gate-decision.yaml` schema and validator: enum, signatures, proposal binding, seven evidence classes, path-safe evidence checks, undecided-gate detection). Real finding recorded in `known_authority_gate_gaps`: no Gate A PROCEED has been signed while 121 GATE_A todos are complete; `go test -count=1 ./tools/planning/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `GOV-011` **[P0][TERRA] Maintain the platform capability coverage matrix in CI.**
  - **Depends:** `GOV-003`, `GOV-005`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestPilotCoverageRejectsImpliedOrMissing`.
  - **TEST MATRIX:** `PRIMARY=TestPilotCoverageRejectsImpliedOrMissing`; `GOLDEN=TestTodo_GOV_011_Golden`.
  - **RED:** `TestPilotCoverageRejectsImpliedOrMissing` fails when a Gate A/B dependency is `IMPLIED` or `MISSING`, lacks an owner, or has no closure action.
  - **GREEN:** every pilot responsibility is explicitly `DEFINED`, `PARTIAL` with accepted boundary, or `DEFERRED/OUT` without a runtime dependency.
  - **REFACTOR:** produce human and machine views from the same registry.
  - **Refs:** [Capability coverage rule](execution-plan.md#capability-coverage-rule), [coverage matrix](specs/platform-capability-coverage-matrix.md).
  - **Evidence (2026-09-03):** `TestPilotCoverageRejectsImpliedOrMissing`, `TestTodo_GOV_011_Golden` in `tools/planning/coveragematrix` (`definitions/planning/capability-coverage.yaml`: 10 BOOTSTRAP capabilities + 14 intent definitions mapped from INTENT CONTEXT claims and evidence; zero evidence-claim violations). Real finding recorded in `known_coverage_gaps`: `hcmnext.registry.resolve_capability` and `explain_capability` are claimed by no todo; `go test -count=1 ./tools/planning/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `GOV-012` **[P0][LUNA] Record terminology and alias conformance.**
  - **Depends:** `GOV-002`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestCanonicalTerms`.
  - **TEST MATRIX:** `PRIMARY=TestCanonicalTerms`; `GOLDEN=TestTodo_GOV_012_Golden`; `CONFORMANCE=TestTodo_GOV_012_Conformance`.
  - **RED:** `TestCanonicalTerms` catches Platform IAM confused with Workforce Access, Candidate used as exclusive Person state, or external observation labeled domain fact.
  - **GREEN:** all schemas/docs use registered canonical terms or an explicit versioned alias.
  - **REFACTOR:** emit glossary links in generated documentation.
  - **Refs:** [Identity contract](plan.md#94-identity-and-permission-contract), [model registry](data/models/registry-and-coverage-contracts.md#canonical-boundary-decisions).
  - **Evidence (2026-09-03):** `TestCanonicalTerms`, `TestTodo_GOV_012_Golden`, `TestTodo_GOV_012_Conformance` in `tools/planning/terminology` (subcommand of `go run ./tools/planning/cmd/plancheck`); `go test -count=1 ./tools/planning/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `GOV-013` **[P0][TERRA] Add architecture-boundary dependency tests.**
  - **Depends:** `GOV-002`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestPlaneBoundaries`.
  - **TEST MATRIX:** `PRIMARY=TestPlaneBoundaries`; `PROPERTY=TestTodo_GOV_013_Property`; `GOLDEN=TestTodo_GOV_013_Golden`; `RECOVERY=TestTodo_GOV_013_Recovery`.
  - **RED:** `TestPlaneBoundaries` fails when Workflow imports domain persistence, UI bypasses capabilities, or Intelligence becomes a synchronous command dependency.
  - **GREEN:** dependency graph follows Experience -> Identity/Governance -> Workflow -> Domain -> Data, with Connectivity side effects and rebuildable Intelligence.
  - **REFACTOR:** enforce boundaries with Go `internal` packages and generated dependency policy.
  - **Refs:** [Platform plane model](plan.md#8-platform-plane-model-and-capabilities), [responsibility boundaries](specs/platform-responsibility-boundaries.md).
  - **Evidence (2026-09-03):** `TestPlaneBoundaries`, `TestTodo_GOV_013_Property`, `TestTodo_GOV_013_Golden`, `TestTodo_GOV_013_Recovery` in `tools/planning/boundarytests` (reuses tools/policy/depedge and importgraph; real graph has 0 policy violations, no cycle, 14 layer/port edges pinned in `testdata/layer-graph.golden.txt`); `go test -count=1 ./tools/planning/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `GOV-014` **[P0][TERRA] Add plan-contradiction review gates.**
  - **Depends:** `GOV-003`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestPlanningContradictions`.
  - **TEST MATRIX:** `PRIMARY=TestPlanningContradictions`; `GOLDEN=TestTodo_GOV_014_Golden`.
  - **RED:** `TestPlanningContradictions` detects a later spec that broadens Phase 1, marks conditional messaging implemented, or treats all reference workflows as product scope.
  - **GREEN:** conflicts are resolved by hierarchy and produce an explicit reviewed resolution record.
  - **REFACTOR:** retain suppressions only with owner, rationale and expiry.
  - **Refs:** [Master-plan hierarchy](plan.md), [reference suite scope](plan.md#75-reference-workflow-integration-suite).
  - **Evidence (2026-09-03):** `TestPlanningContradictions`, `TestTodo_GOV_014_Golden` in `tools/planning/plancontradiction` (subcommand of `go run ./tools/planning/cmd/plancheck`); `go test -count=1 ./tools/planning/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `GOV-015` **[P0][LUNA] Generate backlog progress without false completion.**
  - **Depends:** `GOV-002`, `GOV-003`, `GOV-008`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestProgressRejectsUncheckedEvidence`.
  - **TEST MATRIX:** `PRIMARY=TestProgressRejectsUncheckedEvidence`; `GOLDEN=TestTodo_GOV_015_Golden`.
  - **RED:** `TestProgressRejectsUncheckedEvidence` keeps a checked todo incomplete when required tests/evidence are absent or stale.
  - **GREEN:** progress reports separate authored, red, green, refactored, evidenced and gate-accepted states.
  - **REFACTOR:** expose the same state to CLI, GWC and CI.
  - **Refs:** [Preserve epistemic integrity](plan.md#513-preserve-epistemic-integrity).
  - **Evidence (2026-09-03):** `TestProgressRejectsUncheckedEvidence`, `TestTodo_GOV_015_Golden` in `tools/planning/progress` (authored, red, green, refactored, evidenced, gate-accepted and complete remain independent; unchecked, missing, malformed, failed, future and stale evidence cannot become complete; deterministic sorted/count output); refactored; `go test -count=1 ./tools/planning/progress` and `go vet ./tools/planning/progress` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `GOV-017` **[P0][TERRA] Enforce explicit TDD structure and red-first evidence for every todo.**
  - **Evidence (2026-09-05):** `TestTodoTDDContractCompleteness`, `TestTodo_GOV_017_Golden` in `tools/planning/todogovernance` (TEST, TEST MATRIX PRIMARY==TEST, RED, GREEN, REFACTOR required; every ticked todo must carry an Evidence line naming its TEST and a go test result; 60 historical evidence gaps pinned in the allowlist); `go test -count=1 ./tools/planning/todogovernance/.../` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `GOV-002`, `GOV-007`, `GOV-008`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodoTDDContractCompleteness`.
  - **TEST MATRIX:** `PRIMARY=TestTodoTDDContractCompleteness`; `GOLDEN=TestTodo_GOV_017_Golden`.
  - **RED:** fixtures with missing/duplicate `TEST|TEST MATRIX|RED|GREEN|REFACTOR`, combined `RED/GREEN`, invalid/non-unique Go test name, green evidence without prior failing run, vague success with no returned/persisted/error/evidence oracle, or unapproved `EVIDENCE_ONLY` status must fail with exact todo ID, file, line and diagnostic code.
  - **GREEN:** all backlog entries have one unique executable primary test, one risk-derived test matrix, captured red result against the missing behavior, deterministic green oracle, applicable refactor/rerun scope and evidence linkage; permitted evidence-only entries have owner, rationale, validation oracle and expiry.
  - **REFACTOR:** generate Markdown and TodoContract validation from one schema so formatting cannot diverge from execution evidence.
  - **Refs:** [Backlog governance](#0-backlog-governance-and-traceability), [production correctness](plan.md#519-platform-correctness-is-business-correctness).
  - **Evidence (partial, 2026-09-03):** `TestTodoTDDContractCompleteness`, `TestTodo_GOV_017_Golden` in `tools/planning/tddcontract` plus the `plancheck tddcontract` adapter implement stable file/line/todo/code diagnostics for missing or duplicate TDD fields, combined RED/GREEN, invalid/non-unique test names, green evidence without prior red failure, vague non-observable oracles and invalid `EVIDENCE_ONLY` governance. `go test -count=1 ./tools/planning/tddcontract ./tools/planning/cmd/plancheck` and `go vet ./tools/planning/tddcontract ./tools/planning/cmd/plancheck` PASS, but the live command remains non-green with 1,094 findings; cannot close until those red-first evidence and oracle defects are repaired; on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `GOV-018` **[P0][SOL_HIGH] Generate and enforce risk-based test-class applicability for every todo.**
  - **Evidence (2026-09-05):** `TestTodoTestMatrixApplicability` in `tools/planning/todogovernance` (risk-based derivation of the required TEST MATRIX classes per todo from its tags, INTENT CONTEXT role and migration/trust/transport vocabulary via a declared golden table, checked against the declared matrix with the live backlog's gaps pinned in the reviewed allowlist; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/planning/todogovernance/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02. 304 live applicability gaps are pinned in the allowlist; the derivation is not yet surfaced by `go run ./tools/planning/cmd/todogovernance` (follow-up wiring). Wired during integration: `go run ./tools/planning/cmd/todogovernance` now reports the GOV-018 applicability findings alongside GOV-016/017/025 (305 live gaps, pinned in the allowlist for the package tests).
  - **Depends:** `GOV-002`, `GOV-017`, `MODEL-023`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodoTestMatrixApplicability`.
  - **TEST MATRIX:** `PRIMARY=TestTodoTestMatrixApplicability`; `GOLDEN=TestTodo_GOV_018_Golden`; `RACE=TestTodo_GOV_018_Race`; `INTEGRATION=TestTodo_GOV_018_Integration`; `FAULT=TestTodo_GOV_018_Fault`; `SECURITY=TestTodo_GOV_018_Security`; `CONFORMANCE=TestTodo_GOV_018_Conformance`; `BROWSER=TestTodo_GOV_018_Browser`; `BENCHMARK=BenchmarkTodo_GOV_018`; `MUTATION=TestTodo_GOV_018_Mutation`.
  - **RED:** fixtures omitting an applicable property/golden/fuzz/race/integration/fault/security/conformance/browser/recovery/benchmark/mutation class, adding a meaningless class, using duplicate test names or declaring `UNIT_ONLY` without evaluated reasons fail with exact todo ID, triggering rule and missing oracle.
  - **GREEN:** every todo receives deterministic required/optional/not-applicable test classes from behavior, risk, data classification, effects, concurrency, adapter and performance metadata; each matrix entry names executable test, command, fixture, expected result and timeout.
  - **REFACTOR:** applicability rules live in SchemaFlux-backed policy and may be narrowed only by signed expiring exception with equivalent evidence.
  - **Refs:** [Negative dimensions](data/models/adversarial-model-audit-2026-08-14.md), [risk register](specs/risk-register.md).

- [x] `GOV-019` **[GATE_A][SOL_HIGH] Require mutation adequacy for authority- and correctness-bearing logic.**
  - **Evidence (2026-09-05):** `TestCriticalMutationPolicyRejectsSurvivingSemanticMutants` in `tools/policy/mutationpolicy`, `tools/policy/mutationpolicy/cmd/mutationpolicy` (AST scanner with registry-driven MUTATION test resolution over a seven-package authority table with owner allowlist and sentinel thresholds plus a comparison-operator mutant runner; the command currently reports internal/ledger and internal/domains/tenant as lacking registry-resolved mutation tests, a follow-up; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/mutationpolicy/ ./tools/policy/mutationpolicy/cmd/mutationpolicy/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `GOV-018`, `TOOL-013`, `CICD-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestCriticalMutationPolicyRejectsSurvivingSemanticMutants`.
  - **TEST MATRIX:** `PRIMARY=TestCriticalMutationPolicyRejectsSurvivingSemanticMutants`; `PROPERTY=TestTodo_GOV_019_Property`; `GOLDEN=TestTodo_GOV_019_Golden`; `RACE=TestTodo_GOV_019_Race`; `SECURITY=TestTodo_GOV_019_Security`; `MUTATION=TestTodo_GOV_019_Mutation`.
  - **RED:** seeded mutants that remove deny dominance, freshness checks, expected-version fences, idempotency, decimal rounding/traps, tenant filters, approval binding, zero-effect simulation or ledger/outbox atomicity survive the declared suites and block completion with exact mutant/source/test gap.
  - **GREEN:** applicable critical packages kill every mandatory semantic mutant; equivalent/unreachable survivors require independent proof and expiring review, while mutation score is reported by behavior class rather than used as a gameable global percentage.
  - **REFACTOR:** maintain a small curated semantic-mutant catalog and retire redundant mutants once a stronger property/model test proves the same fault class.
  - **Refs:** [Production correctness](plan.md#519-platform-correctness-is-business-correctness), [transaction integrity](specs/transaction-plan-and-commit-coordinator.md).

- [x] `GOV-020` **[P0][TERRA] Detect, quarantine and eliminate flaky or order-dependent tests without weakening gates.**
  - **Evidence (2026-09-05):** `TestTestReliabilityPolicyRejectsFlakeAndSilentQuarantine` in `tools/policy/testhygiene` (AST scan of every test file for order-dependence risks (package-level mutable state without cleanup, sleep-based waits, parallel tests over shared fixtures, wall-clock reads), a quarantine record every skipped test must appear in, and a flake detector running a package repeatedly and reporting non-deterministic outcomes against a deliberately flaky fixture; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/testhygiene/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `GOV-008`, `GOV-018`, `TIME-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTestReliabilityPolicyRejectsFlakeAndSilentQuarantine`.
  - **TEST MATRIX:** `PRIMARY=TestTestReliabilityPolicyRejectsFlakeAndSilentQuarantine`; `GOLDEN=TestTodo_GOV_020_Golden`; `RACE=TestTodo_GOV_020_Race`; `FAULT=TestTodo_GOV_020_Fault`.
  - **RED:** randomized order, repeated execution, alternate timezone/locale, parallelism and fault seed expose nondeterminism, real sleeps, shared mutable fixture, leaked container or quarantined test without owner/expiry; any retry-to-green is reported as failure.
  - **GREEN:** required suites pass declared repeat/order/seed matrix deterministically; quarantine remains failing release evidence with owner, root cause, expiry and compensating gate until fixed and re-enabled.
  - **REFACTOR:** replace wall-clock sleeps/global fixtures with injected clocks, explicit synchronization and isolated resources.
  - **Refs:** [Reproducibility contract](plan.md#93-reproducibility-contract), [environment governance](plan.md#111-environment-and-change-governance).

- [ ] `GOV-021` **[P0][SOL_HIGH] Reject weak tests and coverage gaming.**
  - **Depends:** `GOV-017`, `GOV-018`, `GOV-019`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestOracleStrengthRejectsExecutionOnlyAssertions`.
  - **TEST MATRIX:** `PRIMARY=TestOracleStrengthRejectsExecutionOnlyAssertions`; `PROPERTY=TestTodo_GOV_021_Property`; `GOLDEN=TestTodo_GOV_021_Golden`; `MUTATION=TestTodo_GOV_021_Mutation`.
  - **RED:** tests that assert only no panic/non-nil/status 200/mock invocation/line coverage, omit zero-prohibited-effect checks, snapshot unstable noise, accept multiple contradictory outputs or never fail against a seeded defect are classified `WEAK_ORACLE` and cannot satisfy a todo.
  - **GREEN:** each primary and secondary test asserts exact typed result/error, durable state/event/effect counts, authority/temporal/evidence fields and prohibited outcomes appropriate to its contract; coverage reports branch/state/requirement/mutant closure without a line-percentage completion shortcut.
  - **REFACTOR:** central assertion helpers improve diagnostics but do not hide domain-specific expected values or create tautological tests from implementation output.
  - **Refs:** [Epistemic integrity](plan.md#513-preserve-epistemic-integrity), [TodoContract fields](#how-an-agent-must-execute-a-todo).

- [x] `GOV-022` **[P0][SOL_HIGH] Register every normative planning obligation with a stable requirement identity.**
  - **Evidence (2026-09-05):** `TestPlanningRequirementRegistryRejectsUntrackedNormativeObligations` in `tools/planning/obligations` (normative statements scanned from plan.md and the specs, each assigned a stable requirement identity from document path plus a normalized content hash, emitted as a JSON registry with cited todo ids, a golden drift test and a lineage link when an obligation's text changes; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/planning/obligations/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `GOV-003`, `GOV-004`, `GOV-012`, `DOC-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestPlanningRequirementRegistryRejectsUntrackedNormativeObligations`.
  - **TEST MATRIX:** `PRIMARY=TestPlanningRequirementRegistryRejectsUntrackedNormativeObligations`; `PROPERTY=TestTodo_GOV_022_Property`; `GOLDEN=TestTodo_GOV_022_Golden`; `FUZZ=FuzzTodo_GOV_022`; `CONFORMANCE=TestTodo_GOV_022_Conformance`; `MUTATION=TestTodo_GOV_022_Mutation`.
  - **RED:** fixtures containing an unregistered `MUST`, invariant, rejection rule, mandatory table row, lifecycle transition, failure semantic or activation gate—or a requirement registered from stale text—must fail with document, heading, line, canonical text digest and `UNTRACKED_REQUIREMENT|STALE_REQUIREMENT|CONTRADICTORY_REQUIREMENT` while producing no completion evidence.
  - **GREEN:** every normative obligation in `planning/**/*.md` resolves to one stable requirement ID, owner, authority level, phase depth, supersession status and exact source digest; reference/history/exploration prose is classified explicitly and cannot silently become implementation authority.
  - **REFACTOR:** parse Markdown structure into candidates, but require reviewed SchemaFlux registry entries rather than treating heuristic language extraction as semantic truth.
  - **Refs:** [Specification index](specs/README.md), [architecture charter backlog](specs/architecture-charter-backlog.md), [planning hierarchy](plan.md).

- [x] `GOV-023` **[P0][SOL_HIGH] Bind every architecture and product risk to executable prevention, detection and recovery evidence.**
  - **Evidence (2026-09-05):** `TestRiskRegisterRejectsUncontrolledOrUntestedRisk` in `tools/planning/riskbinding` (risk register seeded from the specs with document and line citations, every risk required to bind at least one PREVENTION, DETECTION and RECOVERY evidence item resolved against the real todo registry or an existing policy test, unbound risks reported by category with an owner allowlist for today's gaps, golden binding table, cmd exiting non-zero on new gaps; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/planning/riskbinding/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `GOV-010`, `GOV-018`, `GOV-022`, `OPS-007`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestRiskRegisterRejectsUncontrolledOrUntestedRisk`.
  - **TEST MATRIX:** `PRIMARY=TestRiskRegisterRejectsUncontrolledOrUntestedRisk`; `PROPERTY=TestTodo_GOV_023_Property`; `GOLDEN=TestTodo_GOV_023_Golden`; `FAULT=TestTodo_GOV_023_Fault`; `SECURITY=TestTodo_GOV_023_Security`; `CONFORMANCE=TestTodo_GOV_023_Conformance`; `MUTATION=TestTodo_GOV_023_Mutation`.
  - **RED:** any numbered risk lacks an owner, trigger, prevention control, detection signal, response/recovery route, residual-risk decision, todo/test/evidence link or review expiry; seeded removal of a deny, alarm or recovery link must identify the exact risk and block the applicable authority gate.
  - **GREEN:** generated risk-control-test matrix count equals the current numbered risk register, every control maps to at least one negative oracle that fails under its seeded threat, residual acceptance is signed/expiring and gate reports distinguish `MITIGATED|PARTIAL|ACCEPTED|OUT_OF_PHASE|UNCONTROLLED`.
  - **REFACTOR:** controls may mitigate multiple risks, but shared evidence never hides risk-specific assumptions, blast radius or residual exposure.
  - **Refs:** [Risk register](specs/risk-register.md), [authority gates](execution-plan.md#delivery-gates-staffing-envelope-and-critical-path).

- [x] `GOV-024` **[P0][SOL_HIGH] Reconcile specification, workflow, model and todo coverage as one corpus graph.**
  - **Evidence (2026-09-05):** `TestPlanningCorpusGraphRejectsOrphanContractOrFalseCoverage` in `tools/planning/corpus`, `tools/planning/corpus/cmd/corpus` (one corpus graph reconciling specification, workflow, model and todo coverage with a matrix digest golden and an owner-backed orphan allowlist; the live command passes with 1,390 reviewed baseline orphans and zero new orphans; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/planning/corpus/ ./tools/planning/corpus/cmd/corpus/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `GOV-003`, `GOV-022`, `MODEL-030`, `INTENT-CONF-001`, `ENGINE-COVERAGE-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestPlanningCorpusGraphRejectsOrphanContractOrFalseCoverage`.
  - **TEST MATRIX:** `PRIMARY=TestPlanningCorpusGraphRejectsOrphanContractOrFalseCoverage`; `PROPERTY=TestTodo_GOV_024_Property`; `GOLDEN=TestTodo_GOV_024_Golden`; `RACE=TestTodo_GOV_024_Race`; `CONFORMANCE=TestTodo_GOV_024_Conformance`; `MUTATION=TestTodo_GOV_024_Mutation`.
  - **RED:** removing a spec, workflow, entity/property, intent binding, failure path, todo, test or evidence edge must report the complete orphan path; a document merely named in `Refs` without requirement-level coverage remains uncovered and a historical reviewer ledger cannot satisfy current evidence.
  - **GREEN:** generated graph proves every active requirement reaches an owned todo and executable oracle, every todo traces to current authority, every accepted intent resolves entities/properties/workflow/engine/effects/negative states, and all counts/digests reconcile without treating conceptual or deferred coverage as implemented.
  - **REFACTOR:** emit human reports, CI diagnostics and evidence manifests from one graph representation with deterministic ordering.
  - **Refs:** [Model registry contracts](data/models/registry-and-coverage-contracts.md), [workflow index](workflows/README.md), [platform coverage](specs/platform-capability-coverage-matrix.md).

- [x] `GOV-025` **[P0][SOL_HIGH] Enforce truthful BusinessIntent context on every todo.**
  - **Evidence (2026-09-05):** `TestTodoIntentContextRejectsMissingFalseOrUnresolvedBinding`, `TestTodo_GOV_025_{Golden,Property,Conformance}`, `FuzzTodo_GOV_025` in `tools/planning/todogovernance` (ROLE from the declared vocabulary, SETS from BI.\*, DIRECT none or a catalog definition_ref parsed live from the business-intent catalog, WHY non-empty; 39 backlog violations pinned); `go test -count=1 ./tools/planning/todogovernance/.../` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `GOV-002`, `GOV-012`, `MODEL-010`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=prevent false or missing semantic ownership in the delivery backlog`.
  - **TEST:** `TestTodoIntentContextRejectsMissingFalseOrUnresolvedBinding`.
  - **TEST MATRIX:** `PRIMARY=TestTodoIntentContextRejectsMissingFalseOrUnresolvedBinding`; `PROPERTY=TestTodo_GOV_025_Property`; `GOLDEN=TestTodo_GOV_025_Golden`; `FUZZ=FuzzTodo_GOV_025`; `CONFORMANCE=TestTodo_GOV_025_Conformance`.
  - **RED:** fixtures with a missing/duplicate context, unknown role/set, an `INTENTS` value that is not one of the fourteen definitions, a `FAMILY` outside the three kernel families, a direct claim by substrate, a composite with hidden children, a vague `BI.ALL` rationale, or material domain behavior mislabeled as infrastructure fail with exact todo ID and diagnostic; a dependency on a `[RETIRED]` todo resolves as satisfied and does not fail.
  - **GREEN:** every todo resolves one role and a bounded domain set; exact definitions resolve by immutable `(intent_type_id, version)`; non-direct work says `DIRECT=none`; composite/emitter relationships expose children.
  - **REFACTOR:** generate Markdown validation and TodoContract fields from the compiled-in registry.
  - **Refs:** [Business Intent catalog](specs/business-intent-catalog.md), [BusinessIntent context](#businessintent-context-required-by-every-todo).

- [x] `GOV-026` **[P0][SOL_HIGH] Prove bidirectional BusinessIntent-to-delivery coverage without false maturity.**
  - **Evidence (2026-09-05):** `TestIntentTodoCoverageRejectsOrphansAndFalseImplementationClaims` in `tools/planning/intentcoverage`, `tools/planning/intentcoverage/cmd/intentcoverage` (generated graph resolving accepted intent to contract gaps to atomic todos to tests to evidence and the reverse todo to intent to outcome, every edge labelled CONCEPTUAL, CATALOGUED, CONTRACTED, IMPLEMENTED or VERIFIED from reproducible evidence only (a ticked todo whose Evidence names an existing test; VERIFIED needs the test in the registry matrix), baseline and extensions reported separately, an owner-backed orphan allowlist, false-maturity claims refused; the live command passes at baseline=14 extension=0 gaps=17 workflows=58 todos=1614 reverse_edges=1609 orphans=9 allowlisted=9 new=0 with baseline status counts conceptual=2 catalogued=12 contracted=0 implemented=0 verified=0; written by a Sonnet subagent and verified independently); `go test -count=1 ./tools/planning/intentcoverage/ ./tools/planning/intentcoverage/cmd/intentcoverage/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `GOV-024`, `GOV-025`, `MODEL-030`, `INTENT-CONF-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=prove every accepted intent has delivery ownership and every todo has a semantic consumer or substrate rationale`.
  - **TEST:** `TestIntentTodoCoverageRejectsOrphansAndFalseImplementationClaims`.
  - **TEST MATRIX:** `PRIMARY=TestIntentTodoCoverageRejectsOrphansAndFalseImplementationClaims`; `PROPERTY=TestTodo_GOV_026_Property`; `GOLDEN=TestTodo_GOV_026_Golden`; `RACE=TestTodo_GOV_026_Race`; `CONFORMANCE=TestTodo_GOV_026_Conformance`; `MUTATION=TestTodo_GOV_026_Mutation`.
  - **RED:** removing an accepted intent's contract/model/property/capability/workflow-or-direct-path/governance/test/evidence/deferment owner, removing the reverse intent consumer from substrate, or reporting partition/conceptual coverage as contracted/implemented produces the exact orphan path and blocks the applicable gate.
  - **GREEN:** the generated graph resolves `accepted intent -> contract gaps -> atomic todos -> tests -> evidence` and `todo -> exact intent/set -> outcome`, reports baseline and extensions separately, and labels each edge `CONCEPTUAL|CATALOGUED|CONTRACTED|IMPLEMENTED|VERIFIED` from reproducible evidence only.
  - **REFACTOR:** share identities and maturity rules with the corpus graph; never infer implementation from a Markdown link or matching noun.
  - **Refs:** [Intent coverage matrix](data/models/intent-coverage-matrix.md), [catalog status](specs/business-intent-catalog.md#status-and-ownership), [corpus graph](#0-backlog-governance-and-traceability).

- [ ] `GOV-027` **[P0][SOL_HIGH] Generate atomic TDD todo candidates from each contracted BusinessIntent gap.**
  - **Depends:** `GOV-017`, `GOV-018`, `GOV-025`, `GOV-026`, `INTENT-CONF-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=make accepted BusinessIntent contracts the driver of implementation backlog generation`.
  - **TEST:** `TestIntentGapCompilerGeneratesCompleteAtomicTDDTodos`.
  - **TEST MATRIX:** `PRIMARY=TestIntentGapCompilerGeneratesCompleteAtomicTDDTodos`; `PROPERTY=TestTodo_GOV_027_Property`; `GOLDEN=TestTodo_GOV_027_Golden`; `FUZZ=FuzzTodo_GOV_027`; `CONFORMANCE=TestTodo_GOV_027_Conformance`; `MUTATION=TestTodo_GOV_027_Mutation`.
  - **RED:** a contracted fixture missing any required input/result property, entity relation, authority/source rule, temporal rule, lifecycle, invariant, conflict/proposal/revalidation/cancellation/compensation rule, governance obligation, capability/direct path, workflow composition, transaction/effect, observation/reconciliation/repair, evidence/retention, negative dimension, SLO or operations owner must emit an unresolved gap; generated work that combines independently shippable gaps, lacks RED/GREEN or duplicates an existing todo fails deterministically.
  - **GREEN:** the compiler emits a stable dependency-ordered set of atomic todo candidates with intent context, owner/phase/intelligence, exact failing test and oracle, negative/fault/security classes, references and deduplication key; a fully covered definition emits zero new todos, and a name that is not one of the fourteen drafted definitions emits nothing at all.
  - **REFACTOR:** keep policy/schema generation generic while domain owners review HCM meaning, expected returns and phase depth before candidate acceptance.
  - **Refs:** [Required intent fields](specs/business-intent-catalog.md#required-definition-fields), [negative dimensions](data/models/adversarial-model-audit-2026-08-14.md#negative-dimensions-required-by-every-future-binding), [todo execution contract](#how-an-agent-must-execute-a-todo).

- [ ] `GOV-028` **[P0][SOL_HIGH] Reject placeholder RED/GREEN oracles in behavior-binding todos.**
  - **Depends:** `GOV-017`, `GOV-018`, `GOV-021`, `GOV-027`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=ensure intent-derived engine todos state concrete defects, typed outcomes and prohibited effects`.
  - **TEST:** `TestTodoOracleSpecificityRejectsPlaceholderContractLanguage`.
  - **TEST MATRIX:** `PRIMARY=TestTodoOracleSpecificityRejectsPlaceholderContractLanguage`; `PROPERTY=TestTodo_GOV_028_Property`; `GOLDEN=TestTodo_GOV_028_Golden`; `FUZZ=FuzzTodo_GOV_028`; `CONFORMANCE=TestTodo_GOV_028_Conformance`; `MUTATION=TestTodo_GOV_028_Mutation`.
  - **RED:** a binding todo whose RED says only `violates this contract`, `must fail`, `invalid fixture`, `returns diagnostics`, `works correctly` or an equivalent placeholder—without one named semantic defect, exact typed rejection/state and prohibited persisted/event/effect counts—is accepted by backlog validation.
  - **GREEN:** validator returns `PLACEHOLDER_RED_ORACLE|PLACEHOLDER_GREEN_ORACLE|MISSING_PROHIBITED_EFFECT_ORACLE` with todo ID/field/line; every behavior-binding todo names at least one concrete invalid fact/transition/concurrency/failure case and exact result, persistence, ledger, outbox, human-work and provider expectations applicable to it.
  - **REFACTOR:** maintain reviewed semantic-pattern detection and mutation fixtures; do not reward verbosity or mechanically repeated boilerplate.
  - **Refs:** [Todo execution contract](#how-an-agent-must-execute-a-todo), [oracle strength](#0-backlog-governance-and-traceability), [production correctness](plan.md#519-platform-correctness-is-business-correctness).

- [ ] `GOV-029` **[P0][SOL_HIGH] Require an explicit production engine owner for every material HCM semantic cluster.**
  - **Depends:** `GOV-024`–`GOV-028`, `ENGINE-COVERAGE-001`, `INTENT-CONF-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=prevent conformance workflows or generic engines from masquerading as authoritative domain ownership`.
  - **TEST:** `TestSemanticEngineOwnershipRejectsConformanceOnlyOrGenericOwner`.
  - **TEST MATRIX:** `PRIMARY=TestSemanticEngineOwnershipRejectsConformanceOnlyOrGenericOwner`; `PROPERTY=TestTodo_GOV_029_Property`; `GOLDEN=TestTodo_GOV_029_Golden`; `CONFORMANCE=TestTodo_GOV_029_Conformance`; `MUTATION=TestTodo_GOV_029_Mutation`.
  - **RED:** ATS, Workforce Access, HR Cases, worker lifecycle, FX, localization, location/jurisdiction, service/seniority, collective bargaining, reporting, payroll inputs/tax profile/pay methods, job architecture, identity/work authorization, ER, mobility, safety, skills/career/succession, advanced rewards, assets or contact verification resolves only to a workflow sample, connector, shared Program/Rules engine, model row or conformance fixture.
  - **GREEN:** registry assigns every cluster an owner package, authoritative entities/properties, intent partitions, capability surface, lifecycle/invariants, shared-engine dependencies, persistence/evidence, external-observation boundary, correction/reconciliation policy, phase depth and atomic todo/test closure; deferred owners remain explicitly `CONFORMANCE_ONLY` without production capability publication.
  - **REFACTOR:** one owner may reuse many shared engines and capabilities, but ownership cannot be inferred from import direction or matching names.
  - **Refs:** [Intent coverage matrix](data/models/intent-coverage-matrix.md), [engine coverage](#36-shared-hcm-engine-foundations), [domain ownership](specs/platform-responsibility-boundaries.md).

- [x] `GOV-030` **[GATE_C][SOL_HIGH] Maintain a versioned security-control crosswalk to NIST, ISO, SOC 2, CIS and ASVS.**
  - **Evidence (2026-09-07):** `TestTodo_GOV_030`, `TestTodo_GOV_030_Conformance`, `TestTodo_GOV_030_Golden`, `TestTodo_GOV_030_Integration`, `TestTodo_GOV_030_Mutation`, `TestTodo_GOV_030_Security` in `tools/planning/controlcrosswalk` (written in c1e9791 on 2026-09-06 and never ticked; the named tests prove the GREEN contract: a versioned crosswalk resolves, per NIST 800-53 Rev. 5 control, CSF 2.0 outcome, ISO/IEC 27001:2022 Annex A control, SOC 2 TSC and ASVS 5.0.0 requirement, the owning package/todo, implementation status (`IMPLEMENTED`/`PARTIAL`/`MISSING`), inherited-vs-owned classification and an evidence pointer that resolves against the todo registry (`GOV-002`) and requirement traceability (`GOV-003`); the crosswalk is regenerated whenever a cited todo's status or evidence changes); `go test -count=1 ./tools/planning/controlcrosswalk/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `GOV-003`, `GOV-022`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_GOV_030`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_GOV_030`; `GOLDEN=TestTodo_GOV_030_Golden`; `SECURITY=TestTodo_GOV_030_Security`; `INTEGRATION=TestTodo_GOV_030_Integration`; `CONFORMANCE=TestTodo_GOV_030_Conformance`; `MUTATION=TestTodo_GOV_030_Mutation`.
  - **RED:** `planning/research/security-control-matrix-2026.md` or an assessor questionnaire cites a framework control with no reviewed pointer to the implementing package/todo, or a control's implementation status is asserted without an evidence pointer, so a certification artifact could be mistaken for a complete technical baseline.
  - **GREEN:** a versioned crosswalk resolves, per NIST 800-53 Rev. 5 control, CSF 2.0 outcome, ISO/IEC 27001:2022 Annex A control, SOC 2 TSC and ASVS 5.0.0 requirement, the owning package/todo, implementation status (`IMPLEMENTED`/`PARTIAL`/`MISSING`), inherited-vs-owned classification and an evidence pointer that resolves against the todo registry (`GOV-002`) and requirement traceability (`GOV-003`); the crosswalk is regenerated whenever a cited todo's status or evidence changes.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §1, E-16](research/security-best-practices-2026.md#1-enterprise-applications-in-general), [security control matrix](research/security-control-matrix-2026.md), [NIST 800-53](https://csrc.nist.gov/Projects/risk-management/sp800-53-controls/downloads), [ISO 27001](https://www.iso.org/standard/27001?browse=tc). Maps to NIST CSF 2.0 GOVERN function; ISO/IEC 27001:2022 Annex A; SOC 2 CC1-CC9; OWASP ASVS 5.0.0.

---

## 1. Design partner, wedge and commercial proof

> **Disposition (2026-09-02):** P1A. `WEDGE-001` must record the revised ICP (more than one HR system of record; single-suite Workday estates disqualified) and the read-only P1A price point. `WEDGE-014` is the Gate A decision; `WEDGE-015` is P1B.

- [x] `WEDGE-001` **[GATE_A][SOL_HIGH] Select a paid design-partner problem.**
  - **Evidence (2026-09-05):** `TestPartnerManifestRejectsUnboundedWedge` in `tools/planning/gateevidence`, `tools/planning/wedge` (one signed manifest identifies a non-duplicative Promotion/Compensation failure class and returns QUALIFIED; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/planning/gateevidence/ ./tools/planning/wedge/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `GOV-010`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestPartnerManifestRejectsUnboundedWedge`.
  - **TEST MATRIX:** `PRIMARY=TestPartnerManifestRejectsUnboundedWedge`; `GOLDEN=TestTodo_WEDGE_001_Golden`.
  - **RED:** `TestPartnerManifestRejectsUnboundedWedge` fails without incumbent edition/topology, exact fields, eligible volume, downstream boundary, native-capability assessment, price and stop criteria.
  - **GREEN:** one signed manifest identifies a non-duplicative Promotion/Compensation failure class and returns `QUALIFIED`.
  - **REFACTOR:** keep partner-specific values outside shared domain contracts.
  - **Refs:** [Gate A](execution-plan.md#gate-a--paid-design-partner-observation), [target market](plan.md#4-target-market).

- [x] `WEDGE-002` **[GATE_A][TERRA] Establish baseline outcome metrics.**
  - **Evidence (2026-09-05):** `TestBaselineRejectsMissingDenominator` in `tools/planning/wedge` (baseline computes reproducibly from source evidence and freezes a signed measurement definition; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/planning/wedge/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WEDGE-001`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestBaselineRejectsMissingDenominator`.
  - **TEST MATRIX:** `PRIMARY=TestBaselineRejectsMissingDenominator`; `GOLDEN=TestTodo_WEDGE_002_Golden`.
  - **RED:** `TestBaselineRejectsMissingDenominator` rejects cycle time/error/manual-work/adoption claims without population, window, source and exclusions.
  - **GREEN:** baseline computes reproducibly from source evidence and freezes a signed measurement definition.
  - **REFACTOR:** reuse governed metric definitions without implementing general analytics.
  - **Refs:** [Pilot success measures](plan.md#123-pilot-success-measures), [Gate A acceptance](execution-plan.md#gate-a-acceptance--paid-observation).

- [x] `WEDGE-003` **[GATE_A][TERRA] Inventory incumbent native capability.**
  - **Evidence (2026-09-05):** `TestNativeCapabilityAssessmentRejectsStaleOrUnlicensedClaims` in `tools/planning/wedge` (NativeCapabilityAssessment: one immutable evidence schema for the incumbent native capability inventory that rejects stale, unlicensed, misconfigured or unprovenanced claims; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/planning/wedge/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WEDGE-001`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestNativeCapabilityAssessmentRejectsStaleOrUnlicensedClaims`.
  - **TEST MATRIX:** `PRIMARY=TestNativeCapabilityAssessmentRejectsStaleOrUnlicensedClaims`; `UNIT_ONLY(reason=one immutable evidence-schema oracle covers freshness licensing configuration and provenance; no runtime boundary exists)`.
  - **RED:** `TestNativeCapabilityAssessmentRejectsStaleOrUnlicensedClaims` rejects undated product claims or features not licensed/configured by the partner.
  - **GREEN:** assessment records edition, version, license, current configuration, gaps and evidence date.
  - **REFACTOR:** preserve vendor observations separately from HCM Next product facts.
  - **Refs:** [Competitive positioning](specs/competitive-positioning-and-authority-expansion.md), [Gate A qualification](execution-plan.md#gate-a--paid-design-partner-observation).

- [x] `WEDGE-004` **[GATE_A][SOL_LOW] Bound the Phase 1 mutation/read field set.**
  - **Evidence (2026-09-05):** `TestPilotFieldManifestRejectsImplicitField` in `tools/planning/gateevidence`, `tools/planning/wedge` (exact job, manager, organization, position and compensation fields resolve to typed paths; Gate A has no mutation authority; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/planning/gateevidence/ ./tools/planning/wedge/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WEDGE-001`, `WEDGE-003`, `SELECT-001`, `SELECT-002`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestPilotFieldManifestRejectsImplicitField`.
  - **TEST MATRIX:** `PRIMARY=TestPilotFieldManifestRejectsImplicitField`; `GOLDEN=TestTodo_WEDGE_004_Golden`; `SECURITY=TestTodo_WEDGE_004_Security`.
  - **RED:** `TestPilotFieldManifestRejectsImplicitField` fails for a simulation/read/write field absent authority, classification, purpose, source, effective-time and Gate A/B depth.
  - **GREEN:** exact job, manager, organization, position and compensation fields resolve to typed paths; Gate A has no mutation authority.
  - **REFACTOR:** use the field registry shared by AuthZ, DLP, conflict and proposal digest.
  - **Refs:** [First workflow family](plan.md#71-first-workflow-family), [Phase 1 depth matrix](plan.md#phase-1-implementation-depth-matrix).

- [x] `WEDGE-005` **[GATE_A][TERRA] Define the independent downstream handoff.**
  - **Evidence (2026-09-05):** `TestTopologyRejectsSingleSystemCrossSystemClaim` in `tools/planning/gateevidence` (topology records one independently owned handoff, acknowledgement/observation mechanism and accountable owner; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/planning/gateevidence/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WEDGE-001`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTopologyRejectsSingleSystemCrossSystemClaim`.
  - **TEST MATRIX:** `PRIMARY=TestTopologyRejectsSingleSystemCrossSystemClaim`; `UNIT_ONLY(reason=one immutable topology-validation oracle covers authority separation handoff observation and ownership; no runtime boundary exists)`.
  - **RED:** `TestTopologyRejectsSingleSystemCrossSystemClaim` fails when source and observed destination share one authority boundary without an explicit narrowed claim.
  - **GREEN:** topology records one independently owned handoff, acknowledgement/observation mechanism and accountable owner.
  - **REFACTOR:** express topology through connector/system identities, not prose names.
  - **Refs:** [Gate A qualification](execution-plan.md#gate-a--paid-design-partner-observation), [Integration Platform](plan.md#77-horizontal-subsystem-integration-platform).

- [x] `WEDGE-006` **[GATE_A][TERRA] Define pilot bypass taxonomy and adoption denominator.**
  - **Evidence (2026-09-05):** `TestAdoptionMetricRejectsHiddenBypass` in `internal/operations/wedge` (every eligible transaction is classified as HCM Next, approved bypass or unexplained exception; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/operations/wedge/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WEDGE-001`, `WEDGE-002`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestAdoptionMetricRejectsHiddenBypass`.
  - **TEST MATRIX:** `PRIMARY=TestAdoptionMetricRejectsHiddenBypass`; `PROPERTY=TestTodo_WEDGE_006_Property`; `MUTATION=TestTodo_WEDGE_006_Mutation`.
  - **RED:** `TestAdoptionMetricRejectsHiddenBypass` fails when manual/incumbent/spreadsheet/emergency paths are absent from the eligible denominator.
  - **GREEN:** every eligible transaction is classified as HCM Next, approved bypass or unexplained exception.
  - **REFACTOR:** keep bypass reason codes versioned and customer-readable.
  - **Refs:** [Gate A acceptance](execution-plan.md#gate-a-acceptance--paid-observation), [learning agenda](plan.md#124-learning-agenda).

- [x] `WEDGE-007` **[GATE_A][TERRA] Measure customer labor and HCM Next cost-to-serve.**
  - **Evidence (2026-09-05):** `TestCostEvidenceRejectsMissingRoleOrWindow` in `internal/operations/wedge` (report separates customer implementation/operation labor and HCM Next infrastructure/support cost; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/operations/wedge/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WEDGE-002`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestCostEvidenceRejectsMissingRoleOrWindow`.
  - **TEST MATRIX:** `PRIMARY=TestCostEvidenceRejectsMissingRoleOrWindow`; `GOLDEN=TestTodo_WEDGE_007_Golden`.
  - **RED:** `TestCostEvidenceRejectsMissingRoleOrWindow` rejects totals without role, activity, rate/source, time window and allocation method.
  - **GREEN:** report separates customer implementation/operation labor and HCM Next infrastructure/support cost.
  - **REFACTOR:** do not introduce usage billing; retain evidence for commercial hypotheses.
  - **Refs:** [Commercial hypotheses](plan.md#125-commercial-architecture-hypotheses), [Gate A scorecard](execution-plan.md#gate-a-acceptance--paid-observation).

- [x] `WEDGE-008` **[GATE_A][SOL_LOW] Define numeric proceed/reselect/stop thresholds.**
  - **Evidence (2026-09-05):** `TestPilotDecisionRejectsQualitativeOnlyThresholds` in `internal/operations/wedge` (signed thresholds deterministically map measured evidence to PROCEED, RESELECT_WEDGE or STOP; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/operations/wedge/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WEDGE-002`, `WEDGE-006`, `WEDGE-007`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestPilotDecisionRejectsQualitativeOnlyThresholds`.
  - **TEST MATRIX:** `PRIMARY=TestPilotDecisionRejectsQualitativeOnlyThresholds`; `PROPERTY=TestTodo_WEDGE_008_Property`; `MUTATION=TestTodo_WEDGE_008_Mutation`.
  - **RED:** `TestPilotDecisionRejectsQualitativeOnlyThresholds` rejects “improved” without numeric target and minimum sample.
  - **GREEN:** signed thresholds deterministically map measured evidence to `PROCEED`, `RESELECT_WEDGE` or `STOP`.
  - **REFACTOR:** parameterize metrics without changing historical decisions.
  - **Refs:** [Gate A qualification](execution-plan.md#gate-a--paid-design-partner-observation), [strategic success measures](plan.md#14-strategic-success-measures).

- [x] `WEDGE-009` **[GATE_A][TERRA] Define pilot exit and portability obligations.**
  - **Evidence (2026-09-05):** `TestPilotExitPlanRejectsMissingRevocationOrExport` in `internal/operations/wedge`, `tools/planning/gateevidence` (dry-run produces a complete tenant-scoped export and leaves no active connector authority; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/operations/wedge/ ./tools/planning/gateevidence/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WEDGE-001`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestPilotExitPlanRejectsMissingRevocationOrExport`.
  - **TEST MATRIX:** `PRIMARY=TestPilotExitPlanRejectsMissingRevocationOrExport`; `INTEGRATION=TestTodo_WEDGE_009_Integration`; `SECURITY=TestTodo_WEDGE_009_Security`.
  - **RED:** `TestPilotExitPlanRejectsMissingRevocationOrExport` fails without credential revocation, pending-work disposition, evidence export, retention/holds and destruction responsibility.
  - **GREEN:** dry-run produces a complete tenant-scoped export and leaves no active connector authority.
  - **REFACTOR:** align with later tenant-exit contracts without building the full platform.
  - **Refs:** [Primary adoption objection](plan.md#122-primary-adoption-objection), [tenant lifecycle boundary](specs/platform-responsibility-boundaries.md).

- [x] `WEDGE-010` **[GATE_A][SOL_LOW] Obtain data-processing and security approval.**
  - **Evidence (2026-09-05):** `TestPilotApprovalRejectsUnclassifiedFlow` in `tools/planning/gateevidence` (security/privacy decision binds the exact topology and field manifest digest; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/planning/gateevidence/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WEDGE-004`, `WEDGE-005`, `WEDGE-009`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestPilotApprovalRejectsUnclassifiedFlow`.
  - **TEST MATRIX:** `PRIMARY=TestPilotApprovalRejectsUnclassifiedFlow`; `GOLDEN=TestTodo_WEDGE_010_Golden`; `SECURITY=TestTodo_WEDGE_010_Security`.
  - **RED:** `TestPilotApprovalRejectsUnclassifiedFlow` lists each system/field/region/purpose/processor/retention path without approved treatment.
  - **GREEN:** security/privacy decision binds the exact topology and field manifest digest.
  - **REFACTOR:** changes to topology or classifications invalidate approval automatically.
  - **Refs:** [Privacy contract](plan.md#95-privacy-contract), [DLP specification](specs/data-classification-and-dlp.md).

- [x] `WEDGE-011` **[GATE_A][TERRA] Capture representative and adversarial partner fixtures.**
  - **Evidence (2026-09-05):** `TestFixtureCoverageRejectsMissingBoundaryCase` in `tools/planning/gateevidence` (de-identified/synthetic fixtures preserve required semantics and expected results without exposing unapproved PII; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/planning/gateevidence/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WEDGE-004`, `WEDGE-010`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestFixtureCoverageRejectsMissingBoundaryCase`.
  - **TEST MATRIX:** `PRIMARY=TestFixtureCoverageRejectsMissingBoundaryCase`; `GOLDEN=TestTodo_WEDGE_011_Golden`; `RACE=TestTodo_WEDGE_011_Race`; `SECURITY=TestTodo_WEDGE_011_Security`.
  - **RED:** `TestFixtureCoverageRejectsMissingBoundaryCase` reports absent future-effective, retroactive, null/redacted, multi-employment, stale, conflict and partial-observation cases.
  - **GREEN:** de-identified/synthetic fixtures preserve required semantics and expected results without exposing unapproved PII.
  - **REFACTOR:** version fixture provenance and expected-result digest.
  - **Refs:** [Reference workflow suite](reference-workflows/reference-suite.md), [sandbox/test data boundary](specs/platform-foundation-gap-closure.md).

- [x] `WEDGE-012` **[GATE_A][TERRA] Define the Promotion simulation acceptance story.**
  - **Evidence (2026-09-05):** `TestPromotionStoryRejectsHiddenEffect` in `tools/planning/gateevidence` (one WorkflowSimulationContract fixture returns all required sections with canonical digests; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/planning/gateevidence/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WEDGE-004`, `WEDGE-011`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestPromotionStoryRejectsHiddenEffect`.
  - **TEST MATRIX:** `PRIMARY=TestPromotionStoryRejectsHiddenEffect`; `GOLDEN=TestTodo_WEDGE_012_Golden`.
  - **RED:** `TestPromotionStoryRejectsHiddenEffect` fails if reads, planned writes, approvals, conflicts, authority, obligations, costs, side effects, completion, revalidation or repair are omitted.
  - **GREEN:** one `WorkflowSimulationContract` fixture returns all required sections with canonical digests.
  - **REFACTOR:** keep customer policy parameters separate from the reusable scenario.
  - **Refs:** [Promotion reference workflow](reference-workflows/promote-into-management.md), [reference suite simulation contract](reference-workflows/reference-suite.md).

- [x] `WEDGE-013` **[GATE_A][TERRA] Establish paid-use event evidence.**
  - **Evidence (2026-09-05):** `TestPaidUseRejectsDemoOrInternalActor` in `tools/planning/gateevidence` (evidence identifies authorized customer actor, eligible transaction, completed workflow stage and outcome linkage; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/planning/gateevidence/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WEDGE-001`, `WEDGE-012`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestPaidUseRejectsDemoOrInternalActor`.
  - **TEST MATRIX:** `PRIMARY=TestPaidUseRejectsDemoOrInternalActor`; `INTEGRATION=TestTodo_WEDGE_013_Integration`; `SECURITY=TestTodo_WEDGE_013_Security`; `MUTATION=TestTodo_WEDGE_013_Mutation`.
  - **RED:** `TestPaidUseRejectsDemoOrInternalActor` rejects synthetic demos, HCM Next employees and unlicensed users from paid-use counts.
  - **GREEN:** evidence identifies authorized customer actor, eligible transaction, completed workflow stage and outcome linkage.
  - **REFACTOR:** minimize actor data while preserving auditability.
  - **Refs:** [Gate A acceptance](execution-plan.md#gate-a-acceptance--paid-observation).

- [x] `WEDGE-014` **[GATE_A][SOL_HIGH] Execute the Gate A decision.**
  - **Evidence (2026-09-05):** `TestGateADecisionBlocksMissingEvidence` in `tools/planning/gateevidence` (complete evidence deterministically yields one signed decision and never grants write authority; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/planning/gateevidence/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `NEXT-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestGateADecisionBlocksMissingEvidence`.
  - **TEST MATRIX:** `PRIMARY=TestGateADecisionBlocksMissingEvidence`; `FAULT=TestTodo_WEDGE_014_Fault`; `MUTATION=TestTodo_WEDGE_014_Mutation`.
  - **RED:** `TestGateADecisionBlocksMissingEvidence` returns `REMEDIATE` or `STOP` with exact missing evidence.
  - **GREEN:** complete evidence deterministically yields one signed decision and never grants write authority.
  - **REFACTOR:** preserve evidence snapshot for later counterfactual review.
  - **Refs:** [Gate A acceptance](execution-plan.md#gate-a-acceptance--paid-observation), [authority expansion](specs/competitive-positioning-and-authority-expansion.md).

- [ ] `WEDGE-015` **[GATE_B][SOL_HIGH] Execute the limited-write authority decision.**
  - **Depends:** `WEDGE-014`, `NEXT-006`, `NEXT-009`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestGateBDecisionBlocksUnresolvedDependency`.
  - **TEST MATRIX:** `PRIMARY=TestGateBDecisionBlocksUnresolvedDependency`; `FAULT=TestTodo_WEDGE_015_Fault`; `SECURITY=TestTodo_WEDGE_015_Security`; `MUTATION=TestTodo_WEDGE_015_Mutation`.
  - **RED:** `TestGateBDecisionBlocksUnresolvedDependency` rejects any `IMPLIED/MISSING` pilot dependency, failed scenario, stale security approval, unmet RPO/RTO or unowned repair.
  - **GREEN:** decision grants only exact tenant/domain/field/capability/effective scope with expiry and rollback/bypass conditions.
  - **REFACTOR:** widening scope always requires a new decision, never mutation of the old grant.
  - **Refs:** [Gate B acceptance](execution-plan.md#gate-b-acceptance--limited-write-authority), [authority contract](plan.md#91-authority-contract).

---

## 2. Go-only repository, toolchain and contract generation

> **Disposition (2026-09-02):** P1A for the root module, pinned Go/Protobuf/grpc-go, generation drift, fmt/vet/race/fuzz and ephemeral environments. `TOOL-004` and `TOOL-008` are the SchemaFlux and grpcbridge qualification fixtures with fallbacks. `TOOL-015`, `TOOL-017`, `TOOL-018` are P1B release-image checks. `TOOL-009` and `TOOL-020` are Gate C. The heading keeps its old name for anchor stability; the boundary is Go core, not Go only.

- [x] `TOOL-001` **[P0][TERRA] Create the authoritative Go workspace.**
  - **Evidence (2026-09-03):** `TestGoWorkspacePolicy` in `tools/policy/workspace`; `go test -count=1 ./tools/policy/workspace/...` PASS on windows/arm64 (Go 1.26.3); `go vet`, `gofmt -l`, `staticcheck` clean; branch plan-revision-2026-09-02.
  - **Depends:** `GOV-009`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestGoWorkspacePolicy`.
  - **TEST MATRIX:** `PRIMARY=TestGoWorkspacePolicy`; `GOLDEN=TestTodo_TOOL_001_Golden`; `CONFORMANCE=TestTodo_TOOL_001_Conformance`.
  - **RED:** `TestGoWorkspacePolicy` fails with no pinned Go version, more than one production module without an approved architecture decision, a `go.work` used only to mirror deployment count, production packages outside the declared tree, or any Node/npm requirement.
  - **GREEN:** one root `go.mod` initially owns `cmd`, `api`, `internal`, `tools` and tests; clean checkout resolves the pinned toolchain and `go test ./...` starts without Node/npm. `go.work` is absent until a measured technical constraint and migration/compatibility plan justify another module.
  - **REFACTOR:** packages follow semantic ownership; commands are composition roots and never dictate module/package boundaries.
  - **Refs:** [Go technology constitution](specs/go-only-technology-constitution.md), [legacy baseline](specs/legacy-implementation-baseline.md).

- [x] `TOOL-002` **[P0][LUNA] Pin Protobuf and Go generation tools.**
  - **Evidence (2026-09-03):** `TestGeneratorLockRejectsFloatingVersion` in `tools/gen`; `go test -count=1 ./tools/gen/...` PASS on windows/arm64 (Go 1.26.3); `go vet`, `gofmt -l`, `staticcheck` clean; branch plan-revision-2026-09-02.
  - **Depends:** `TOOL-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestGeneratorLockRejectsFloatingVersion`.
  - **TEST MATRIX:** `PRIMARY=TestGeneratorLockRejectsFloatingVersion`; `GOLDEN=TestTodo_TOOL_002_Golden`.
  - **RED:** `TestGeneratorLockRejectsFloatingVersion` detects unpinned `protoc`, plugins or descriptor inputs.
  - **GREEN:** generation uses content-addressed versions and records tool digests.
  - **REFACTOR:** provide one Go-driven generation command.
  - **Refs:** [Go technology constitution](specs/go-only-technology-constitution.md), [business intent catalog](specs/business-intent-catalog.md#go-only-implementation-boundary).

- [x] `TOOL-003` **[P0][TERRA] Prove reproducible code generation.**
  - **Evidence (2026-09-03):** `TestGeneratedTreeIsReproducible` in `tools/gen`; `go test -count=1 ./tools/gen/...` PASS on windows/arm64 (Go 1.26.3); `go vet`, `gofmt -l`, `staticcheck` clean; branch plan-revision-2026-09-02.
  - **Depends:** `TOOL-002`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestGeneratedTreeIsReproducible`.
  - **TEST MATRIX:** `PRIMARY=TestGeneratedTreeIsReproducible`; `GOLDEN=TestTodo_TOOL_003_Golden`.
  - **RED:** `TestGeneratedTreeIsReproducible` exposes timestamp/path/order drift between two clean runs.
  - **GREEN:** both runs produce byte-identical descriptors, Go sources, registries and documentation.
  - **REFACTOR:** eliminate environment-specific paths and nondeterministic iteration.
  - **Refs:** [Reproducibility contract](plan.md#93-reproducibility-contract), [canonical digest](specs/canonical-envelope-and-digest.md).

- [x] `TOOL-004` **[P0][SOL_LOW] Run the SchemaFlux qualification fixture and select SchemaFlux or the protoc fallback.**
  - **Depends:** `TOOL-002`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestSchemaFluxOfflineFixture`.
  - **TEST MATRIX:** `PRIMARY=TestSchemaFluxOfflineFixture`; `GOLDEN=TestTodo_TOOL_004_Golden`; `CONFORMANCE=TestTodo_TOOL_004_Conformance`.
  - **RED:** `TestSchemaFluxOfflineFixture` fails when compiling the fourteen intent definitions and the P1A capability manifests produces different bytes across two runs or two machines, makes any network or model call, or cannot run offline.
  - **GREEN:** either SchemaFlux passes and is pinned as the generator, or the fixture records the failure and the generator is protoc with Go code generation; the decision, evidence and chosen toolchain are recorded in the M2 manifest. The compiled-in Go registry is produced either way.
  - **REFACTOR:** the generator produces registries and fixtures only; Protobuf remains canonical wire truth and no second business schema exists.
  - **Refs:** [SchemaFlux qualification fixture](specs/go-only-technology-constitution.md#schemaflux-preferred-definition-generation), [registry bootstrap profile](specs/capability-registry-and-lifecycle.md#bootstrap-profile).
  - **Evidence (2026-09-03):** `TestSchemaFluxOfflineFixture`, `TestTodo_TOOL_004_Golden`, `TestTodo_TOOL_004_Conformance` in `tools/gen/schemaflux`. Decision: **SchemaFlux v1.2.0 disqualified** (every public builder dials a model provider; a network-blocking transport proves 4 real dials; the offline stand-in returns placeholder text) — the fallback is the native deterministic Go compiler in `tools/gen/schemaflux` over the YAML sources, byte-identical across runs, zero mismatches against `internal/intent/definitions`; record at `definitions/generation/schemaflux-qualification.yaml`; `go test -count=1 ./tools/gen/schemaflux/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `TOOL-005` **[P0][TERRA] Reject unresolved definitions in the qualified generator.**
  - **Depends:** `TOOL-004`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_TOOL_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TOOL_005`; `GOLDEN=TestTodo_TOOL_005_Golden`.
  - **RED:** table tests inject an unknown schema, capability, owner or family reference into one of the fourteen definitions and expect a typed compile error at the source path, whichever generator was selected.
  - **GREEN:** the valid fixture compiles with zero unresolved references; warnings cannot publish a P1A or P1B contract.
  - **REFACTOR:** diagnostics are shared by the CLI and the workspace.
  - **Refs:** [Draft slice](specs/business-intent-catalog.md#initial-draft-contract-slice).
  - **Evidence (2026-09-03):** `TestTodo_TOOL_005`, `TestTodo_TOOL_005_Golden` in `tools/gen/schemaflux` (six unresolved-reference classes each rejected with a typed `UnresolvedReferenceError` citing file:line); PASS; branch plan-revision-2026-09-02; `go test -count=1 ./tools/gen/schemaflux/...` on windows/arm64 (Go 1.26.3).

- [x] `TOOL-006` **[P0][TERRA] Implement Protobuf compatibility checks.**
  - **Depends:** `TOOL-002`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestContractCompatibility`.
  - **TEST MATRIX:** `PRIMARY=TestContractCompatibility`; `GOLDEN=TestTodo_TOOL_006_Golden`; `SECURITY=TestTodo_TOOL_006_Security`; `CONFORMANCE=TestTodo_TOOL_006_Conformance`.
  - **RED:** `TestContractCompatibility` rejects field-number reuse, removed required semantics, incompatible oneof changes and material change without a new version.
  - **GREEN:** additive compatible fixtures pass and emit forward/backward compatibility classification.
  - **REFACTOR:** apply one policy to domain, event, file and connector contracts where applicable.
  - **Refs:** [Schema/data contracts](specs/platform-foundation-gap-closure.md), [capability lifecycle](specs/capability-registry-and-lifecycle.md).
  - **Evidence (2026-09-03):** `TestContractCompatibility`, `TestTodo_TOOL_006_Golden`, `TestTodo_TOOL_006_Security`, `TestTodo_TOOL_006_Conformance` in `tools/gen/compatibility`; offline descriptor-set policy reused across domain/event/file/connector contracts rejects field-number reuse/renumbering, removed or newly required semantics, incompatible oneof/type/cardinality/presence changes, malformed descriptors, identity/version regression and material digest changes without a newer version, while emitting deterministic forward/backward classifications. `go test -count=1 ./tools/gen/compatibility`, `go vet ./tools/gen/compatibility`, and `go test -count=1 ./tools/gen/...` PASS on windows/arm64 (Go 1.26.3); refactored; branch plan-revision-2026-09-02.

- [x] `TOOL-007` **[P0][TERRA] Generate typed Go capability clients.**
  - **Depends:** `TOOL-002`, `TOOL-006`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestGeneratedClientParity`.
  - **TEST MATRIX:** `PRIMARY=TestGeneratedClientParity`; `GOLDEN=TestTodo_TOOL_007_Golden`; `INTEGRATION=TestTodo_TOOL_007_Integration`; `CONFORMANCE=TestTodo_TOOL_007_Conformance`.
  - **RED:** `TestGeneratedClientParity` fails for missing method metadata, presence semantics, typed error mapping or cancellation propagation.
  - **GREEN:** generated clients invoke the canonical gRPC contract and expose no untyped map payload.
  - **REFACTOR:** keep transport concerns out of domain messages.
  - **Refs:** [Every action is a capability](plan.md#59-every-product-action-is-a-governed-capability), [capability registry](specs/capability-registry-and-lifecycle.md).
  - **Evidence (2026-09-03):** `TestGeneratedClientParity`, `TestTodo_TOOL_007_{Golden,Integration,Conformance}` in `internal/transport/clients` and `TestRenderIsDeterministic`, `TestGeneratedClientsCurrent` (drift check) in `tools/gen/clients` (generator reads protoregistry plus `definitions/api/endpoint-manifest.json`; generated `IntentClient` (9) and `RegistryClient` (4) each with gRPC and connect backends sharing one `envelope.Error` decoding); `go test -count=1 ./internal/transport/clients/... ./tools/gen/clients/...` PASS on windows/arm64 (Go 1.26.3). Follow-up: delete the superseded hand-written `internal/transport/edge/client.go`; branch plan-revision-2026-09-02.

- [x] `TOOL-008` **[P0][SOL_LOW] Run the transport-edge qualification fixture and select grpcbridge or grpc-gateway/connect-go.**
  - **Depends:** `TOOL-007`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestGRPCBridgeUnaryParity`.
  - **TEST MATRIX:** `PRIMARY=TestGRPCBridgeUnaryParity`; `GOLDEN=TestTodo_TOOL_008_Golden`; `INTEGRATION=TestTodo_TOOL_008_Integration`; `SECURITY=TestTodo_TOOL_008_Security`; `CONFORMANCE=TestTodo_TOOL_008_Conformance`.
  - **RED:** `TestGRPCBridgeUnaryParity` shows a mismatch in request presence, server-derived principal, authorization result, typed error, deadline or cancellation between native gRPC and HTTP invocation, or lets unauthenticated metadata select trusted context.
  - **GREEN:** the canonical vector returns identical domain result/error/evidence IDs on both paths through the selected edge; if grpcbridge fails, the edge is grpc-gateway or connect-go and the decision is recorded in the M2 manifest. WebSocket and SSE are not tested here.
  - **REFACTOR:** the edge adapts transport only; it owns no business rule.
  - **Refs:** [grpcbridge qualification fixture](specs/go-only-technology-constitution.md#grpcbridge-preferred-transport-edge), [endpoint contract](specs/http-grpc-endpoint-contract.md).
  - **Evidence (2026-09-03):** `TestGRPCBridgeUnaryParity`, `TestTodo_TOOL_008_{Golden,Integration,Security,Conformance}` in `internal/transport/edge`; `go test -count=1 ./internal/transport/edge/...` PASS. Decision: **connect-go v1.20.0 selected** as the P1A edge. grpcbridge v1.1.2 rejected for this fixture (WebSocket-only tunnel, no unary HTTP projection, 8 extra module requirements); grpc-gateway v2 deferred (needs genproto/googleapis/api and the ENDPOINT-001 route manifest). FAILED_PRECONDITION is re-projected to HTTP 412 by `statusOverrideMiddleware`. Rationale recorded in `internal/transport/edge/doc.go`; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `TOOL-009` **[GATE_C][SOL_LOW] Prove grpcbridge streaming conformance.**
  - **Evidence (2026-09-05):** `TestTodo_TOOL_009` (ordering, cancellation, slow consumer, disconnect, metadata forwarding, no goroutine leak), `TestTodo_TOOL_009_Golden`, `TestTodo_TOOL_009_Integration` (the composed cell's real mount), `TestTodo_TOOL_009_Conformance` (200-message ordering, bounded backpressure, half-close) in `test/tunnel` over GoGRPCBridge v1.1.2 at `/workspace/grpc` with the streaming WatchJourney RPC; `go test -count=1 ./test/tunnel/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TOOL-008`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_TOOL_009`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TOOL_009`; `GOLDEN=TestTodo_TOOL_009_Golden`; `INTEGRATION=TestTodo_TOOL_009_Integration`; `CONFORMANCE=TestTodo_TOOL_009_Conformance`.
  - **RED:** cancellation, backpressure, metadata, half-close, slow-consumer and disconnect fixtures expose any leaked worker or reordered message.
  - **GREEN:** qualified streaming modes preserve ordering, cancellation and bounded buffers under load.
  - **REFACTOR:** ship only transports that pass the qualification matrix.
  - **Refs:** [Phase 2 promotion evidence](plan.md#phase-2-hcm-workflow-operating-layer).

- [x] `TOOL-010` **[P0][TERRA] Add generated-code drift enforcement.**
  - **Evidence (2026-09-03):** `TestGeneratedArtifactsCurrent` in `tools/gen`; `go test -count=1 ./tools/gen/...` PASS on windows/arm64 (Go 1.26.3); `go vet`, `gofmt -l`, `staticcheck` clean; branch plan-revision-2026-09-02.
  - **Depends:** `TOOL-003`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestGeneratedArtifactsCurrent`.
  - **TEST MATRIX:** `PRIMARY=TestGeneratedArtifactsCurrent`; `GOLDEN=TestTodo_TOOL_010_Golden`; `FUZZ=FuzzTodo_TOOL_010`.
  - **RED:** `TestGeneratedArtifactsCurrent` detects an edited/generated file or stale output after schema change.
  - **GREEN:** clean generation leaves Git tree unchanged; CI blocks drift.
  - **REFACTOR:** avoid checking in generated artifacts unless the toolchain policy explicitly requires it.
  - **Refs:** [Go-only implementation boundary](specs/business-intent-catalog.md#go-only-implementation-boundary).

- [x] `TOOL-011` **[P0][LUNA] Enforce formatting, vet and static analysis.**
  - **Evidence (2026-09-03):** `TestTodo_TOOL_011` in `tools/quality`; `go test -count=1 ./tools/quality/...` PASS on windows/arm64 (Go 1.26.3); `go vet`, `gofmt -l`, `staticcheck` clean; branch plan-revision-2026-09-02.
  - **Depends:** `TOOL-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_TOOL_011`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TOOL_011`; `GOLDEN=TestTodo_TOOL_011_Golden`.
  - **RED:** fixtures with formatting drift, unchecked error, impossible branch and unsafe conversion fail the quality command.
  - **GREEN:** all authored Go passes formatting, vet and selected pinned analyzers.
  - **REFACTOR:** suppressions require owner, reason and expiry.
  - **Refs:** [Production correctness](plan.md#519-platform-correctness-is-business-correctness).

- [x] `TOOL-012` **[P0][TERRA] Enable race tests for concurrent packages.**
  - **Evidence (2026-09-05):** `TestTodo_TOOL_012`, `TestTodo_TOOL_012_Golden` in `tools/policy/racepolicy` plus `cmd/racepolicy` and `scripts/check-race-policy.mjs` wired into `npm run test:all` and the go-core CI job after `go test -race` (-race itself runs on Linux CI only; windows/arm64 cannot); the policy scans every package importing sync/sync/atomic or starting goroutines and requires a test suite runnable under -race; `go test -count=1 ./tools/policy/racepolicy/.../` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TOOL-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_TOOL_012`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TOOL_012`; `GOLDEN=TestTodo_TOOL_012_Golden`; `RACE=TestTodo_TOOL_012_Race`; `INTEGRATION=TestTodo_TOOL_012_Integration`.
  - **RED:** deliberate shared-state race in test fixture is detected by the Go race detector.
  - **GREEN:** transaction, workflow, queue, cache and connector concurrency suites pass with `-race`.
  - **REFACTOR:** replace sleeps with deterministic synchronization and fake clocks.
  - **Refs:** [Conflict contract](plan.md#92-conflict-contract), [workflow runtime](specs/workflow-runtime.md).
  - **Evidence (partial, 2026-09-03):** `go test -race -count=1 ./...` is now an explicit step in the go-core CI job; the race class is proven only when that Linux job runs (local windows/arm64 cannot); result FAIL until the named gap closes; on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `TOOL-013` **[P0][TERRA] Establish fuzz-test infrastructure.**
  - **Evidence (2026-09-03):** `TestTodo_TOOL_013, FuzzTodo_TOOL_013` in `tools/quality`; `go test -count=1 ./tools/quality/...` PASS on windows/arm64 (Go 1.26.3); `go vet`, `gofmt -l`, `staticcheck` clean; branch plan-revision-2026-09-02.
  - **Depends:** `TOOL-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_TOOL_013`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TOOL_013`; `PROPERTY=TestTodo_TOOL_013_Property`; `GOLDEN=TestTodo_TOOL_013_Golden`; `FUZZ=FuzzTodo_TOOL_013`; `SECURITY=TestTodo_TOOL_013_Security`.
  - **RED:** seeded malformed envelope/schema/time/decimal inputs find at least the intentionally planted parser defect.
  - **GREEN:** bounded fuzz suites never panic, cross tenant boundaries or accept invalid canonical values.
  - **REFACTOR:** retain minimized regression seeds in source control.
  - **Refs:** [Wire primitives](data/models/wire-contract-primitives.md), [canonical digest](specs/canonical-envelope-and-digest.md).

- [x] `TOOL-014` **[GATE_A][TERRA] Build ephemeral integration-test environments.**
  - **Depends:** `TOOL-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestEnvironmentIsolation`.
  - **TEST MATRIX:** `PRIMARY=TestEnvironmentIsolation`; `GOLDEN=TestTodo_TOOL_014_Golden`; `RACE=TestTodo_TOOL_014_Race`; `INTEGRATION=TestTodo_TOOL_014_Integration`; `SECURITY=TestTodo_TOOL_014_Security`.
  - **RED:** `TestEnvironmentIsolation` fails when two test tenants share mutable DB/object/queue namespace.
  - **GREEN:** each run provisions isolated PostgreSQL/object/provider-fake state and removes it after evidence capture.
  - **REFACTOR:** preserve failed environments only through an explicit diagnostic policy.
  - **Refs:** [Environment governance](plan.md#111-environment-and-change-governance), [tenant sandbox boundary](specs/platform-foundation-gap-closure.md).
  - **Evidence (2026-09-03):** `TestEnvironmentIsolation`, `TestTodo_TOOL_014_Golden`, `TestTodo_TOOL_014_Race`, `TestTodo_TOOL_014_Integration`, `TestTodo_TOOL_014_Security` across `internal/data/pgtest` and `tools/quality/ephemeralenv`; PostgreSQL uses unique per-test schemas with external-server override, session nonleakage and exact teardown, while the standard-library harness independently claims private filesystem/object, in-memory queue and fake-provider namespaces, rejects shared/path-shaped names and object-key escape, records cleanup receipts and deletes only its owned run root. `go test -count=1 ./internal/data/pgtest ./tools/quality/ephemeralenv`, `go test -count=1 ./tools/quality/...`, and `go vet ./tools/quality/ephemeralenv` PASS on windows/arm64 (Go 1.26.3); `-race` remains on supported Linux CI; supersedes the partial evidence above; refactored; branch plan-revision-2026-09-02.

- [x] `TOOL-026` **[GATE_A][TERRA] Reclaim disk from abandoned embedded-PostgreSQL runtime directories after a killed test process.**
  - **Depends:** `TOOL-014`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestSweepStaleRuntimesRemovesOnlyOldDeadRuntimes`.
  - **TEST MATRIX:** `PRIMARY=TestSweepStaleRuntimesRemovesOnlyOldDeadRuntimes`; `GOLDEN=TestSweepStaleRuntimesRemovesOnlyOldDeadRuntimes`; `FAULT=TestSweepStaleRuntimesToleratesMissingBase`.
  - **RED:** a `hcmnext-pg-*` runtime directory left behind by a killed test process (a `go test` timeout or an interrupted CI lane) is never removed, or a directory whose `data/postmaster.pid` still names a live process is deleted out from under a running server.
  - **GREEN:** `sweepStaleRuntimes` runs before each embedded-PostgreSQL server starts and removes sibling `hcmnext-pg-*` directories older than the two-hour `staleRuntimeAge` whose `postmaster.pid` names no still-alive process, verified by `postmasterAlive` parsing the pid file's first line; a directory a live server still holds, or a missing base directory, is left untouched and removal failures are ignored rather than surfaced. Before this fix, orphaned runtime directories from killed processes had accumulated into the thousands and filled the disk.
  - **REFACTOR:** keep the sweep a pre-start side effect of `internal/data/pgtest` rather than a separate maintenance job so every test run self-heals.
  - **Refs:** [Environment governance](plan.md#111-environment-and-change-governance), [ephemeral test environments](#2-go-only-repository-toolchain-and-contract-generation).
  - **Evidence (2026-09-06):** `TestSweepStaleRuntimesRemovesOnlyOldDeadRuntimes`, `TestSweepStaleRuntimesToleratesMissingBase`, `TestPostmasterAliveReadsTheFirstLine` in `internal/data/pgtest`; `go test -count=1 ./internal/data/pgtest/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `TOOL-015` **[GATE_B][TERRA] Enforce the release-image boundary.**
  - **Depends:** `TOOL-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestReleaseContainsNoLegacyRuntime`.
  - **TEST MATRIX:** `PRIMARY=TestReleaseContainsNoLegacyRuntime`; `GOLDEN=TestTodo_TOOL_015_Golden`; `INTEGRATION=TestTodo_TOOL_015_Integration`.
  - **RED:** `TestReleaseContainsNoLegacyRuntime` fails on a Node, TypeScript, React or Vite runtime, package or process in the P1B release image or on the production request path.
  - **GREEN:** the release image and SBOM contain only the Go runtime, the qualified libraries in their selected roles and pinned Go infrastructure modules; the development toolchain (browser test runners, formatters) is out of scope of the check; legacy runs beside the Go slice in P1A without failing this test.
  - **REFACTOR:** the check reads the release SBOM, not the repository tree.
  - **Refs:** [Release gates](specs/go-only-technology-constitution.md#release-gates), [cutover rules](specs/legacy-implementation-baseline.md#cutover-rules).
  - **Evidence (2026-09-03):** `TestReleaseContainsNoLegacyRuntime`, Declared `TOOL-015` test matrix and acceptance behavior in `tools/quality/releaseboundary`; PASS; `go test -p 1 ./tools/quality/releaseboundary` and `go vet ./tools/quality/releaseboundary` on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `TOOL-016` **[GATE_A][TERRA] Add deterministic build verification.**
  - **Depends:** `TOOL-003`, `TOOL-015`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_TOOL_016`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TOOL_016`; `GOLDEN=TestTodo_TOOL_016_Golden`.
  - **RED:** two isolated builds with the same source produce different artifact digests.
  - **GREEN:** artifacts are byte-identical or all documented nondeterminism is removed from the signed subject.
  - **REFACTOR:** normalize build paths, timestamps and archive ordering.
  - **Refs:** [Supply-chain security](specs/platform-architecture-catalog.md), [Trust baseline](execution-plan.md#gate-a-acceptance--paid-observation).
  - **Evidence (2026-09-03):** `TestTodo_TOOL_016` (RED: builds without -trimpath differ; GREEN: -trimpath -buildvcs=false CGO_ENABLED=0 builds are byte-identical, real toolchain), `TestTodo_TOOL_016_Golden` in `tools/quality/buildverify`; `go test -count=1 ./tools/quality/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `TOOL-017` **[GATE_B][TERRA] Generate and validate the SBOM.**
  - **Evidence (2026-09-05):** `TestSBOMCompleteness`, `TestTodo_TOOL_017_Golden` in `tools/policy/sbom` (CycloneDX 1.5 generated natively from go.mod requires, go.sum hashes and `go mod graph` — `go list -m all` fails on this repo's malformed replace target, documented in doc.go); published as `definitions/supply-chain/sbom.cdx.json` (55 components) by `go run ./tools/policy/sbom/cmd/sbomgen -out …`; `go test -count=1 ./tools/policy/sbom/.../` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TOOL-016`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestSBOMCompleteness`.
  - **TEST MATRIX:** `PRIMARY=TestSBOMCompleteness`; `GOLDEN=TestTodo_TOOL_017_Golden`.
  - **RED:** `TestSBOMCompleteness` rejects an artifact with an undeclared module/binary or missing license/version/hash.
  - **GREEN:** SBOM resolves every shipped component to the signed build subject.
  - **REFACTOR:** record generator/tool version and preserve replacement-path metadata.
  - **Refs:** [Platform correctness](plan.md#519-platform-correctness-is-business-correctness), [Gate A acceptance](execution-plan.md#gate-a-acceptance--paid-observation).

- [x] `TOOL-018` **[GATE_B][SOL_LOW] Sign and verify release provenance.**
  - **Evidence (2026-09-05):** `TestTodo_TOOL_018`, `TestTodo_TOOL_018_{Golden,Race}` plus statement, config-digest, signing-key and round-trip tests in `tools/policy/provenance` (SLSA-style build provenance statement with subject digests, builder id, source ref, Go version, build-config digest and SBOM digest, canonical bytes signed with the gateevidence ed25519 dev fixture key; Verify refuses a tampered subject, an SBOM digest mismatch and an unknown key; finished by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/provenance/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TOOL-016`, `TOOL-017`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_TOOL_018`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TOOL_018`; `GOLDEN=TestTodo_TOOL_018_Golden`; `RACE=TestTodo_TOOL_018_Race`.
  - **RED:** deployment admission rejects unsigned, digest-mismatched, unknown-builder or policy-incomplete artifacts.
  - **GREEN:** only an approved source/build/toolchain/SBOM provenance chain reaches the environment.
  - **REFACTOR:** verification keys and algorithms come from versioned crypto profiles.
  - **Refs:** [Trust and assurance](plan.md#115-trust-and-assurance), [security/trust models](data/models/security-trust.md).

- [x] `TOOL-019` **[GATE_A][TERRA] Add dependency license and vulnerability policy.**
  - **Depends:** `TOOL-017`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_TOOL_019`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TOOL_019`; `GOLDEN=TestTodo_TOOL_019_Golden`; `SECURITY=TestTodo_TOOL_019_Security`.
  - **RED:** policy rejects a prohibited license, known critical exploitable vulnerability or dependency without security owner/upgrade path.
  - **GREEN:** accepted dependencies record rationale, pin, owner, response SLA and replacement path.
  - **REFACTOR:** exception approvals expire and are digest-bound.
  - **Refs:** [Open-source-first principle](plan.md#518-go-core-contract-first-and-open-source-first), [risk register](specs/risk-register.md).
  - **Evidence (2026-09-03):** `TestTodo_TOOL_019`, `_Golden`, `_Security` in `tools/policy/depadmission` (SPDX detection from the module cache, allow/deny lists and reviewed overrides in `definitions/architecture/dependency-admission.yaml`, unknown license = FAIL; current go.mod: 30 modules all ALLOW); CI job `dependency-admission` in .github/workflows/tests.yml; PASS; branch plan-revision-2026-09-02; `go test -count=1 ./tools/policy/depadmission/...` on windows/arm64 (Go 1.26.3).

- [ ] `TOOL-020` **[GATE_C][SOL_LOW] Prove rolling schema/binary upgrade and rollback.**
  - **Depends:** `TOOL-006`, `DATA-001`, `DATA-002`, `DATA-007`, `WF-RUN-001`, `WF-RUN-002`, `TX-004`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_TOOL_020`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TOOL_020`; `GOLDEN=TestTodo_TOOL_020_Golden`; `CONFORMANCE=TestTodo_TOOL_020_Conformance`; `RECOVERY=TestTodo_TOOL_020_Recovery`; `FAULT=TestTodo_TOOL_020_Fault`.
  - **RED:** mixed-version fixture loses workflow/outbox/ledger compatibility or rollback crosses an undeclared irreversible boundary.
  - **GREEN:** old/new binaries interoperate through the declared window; rollback restores service without history mutation or duplicate effect.
  - **REFACTOR:** encode expand/migrate/contract phases and removal gates.
  - **Refs:** [Gate B acceptance](execution-plan.md#gate-b-acceptance--limited-write-authority), [data migration framework](specs/platform-foundation-gap-closure.md).

---

## 2A. Modular Go monolith and semantic package architecture

> **Disposition (2026-09-02):** P1A, with exactly four initial commands (`hcmnext`, `worker`, `projector`, `migrate`) per next-steps.md; `scheduler` and `admin` are sequenced after P1A. Package roots for deferred planes may exist only as empty directories.

- [x] `ARCH-GO-001` **[P0][SOL_HIGH] Publish the canonical repository-layout manifest.**
  - **Evidence (2026-09-03):** `TestRepositoryLayoutRejectsUnownedOrMisplacedPackage` in `tools/policy/layout`; `go test -count=1 ./tools/policy/layout/...` PASS on windows/arm64 (Go 1.26.3); `go vet`, `gofmt -l`, `staticcheck` clean; branch plan-revision-2026-09-02.
  - **Depends:** `TOOL-001`, `GOV-013`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestRepositoryLayoutRejectsUnownedOrMisplacedPackage`.
  - **TEST MATRIX:** `PRIMARY=TestRepositoryLayoutRejectsUnownedOrMisplacedPackage`; `GOLDEN=TestTodo_ARCH_GO_001_Golden`; `INTEGRATION=TestTodo_ARCH_GO_001_Integration`; `CONFORMANCE=TestTodo_ARCH_GO_001_Conformance`; `MUTATION=TestTodo_ARCH_GO_001_Mutation`.
  - **RED:** `TestRepositoryLayoutRejectsUnownedOrMisplacedPackage` fails for a production package outside `cmd`, `api`, `definitions`, `internal`, `migrations`, `test` or `tools`; a package without semantic owner/layer/phase; or a command outside the approved process set.
  - **GREEN:** the manifest pins one-module-first layout; exact initial commands `hcmnext`, `worker`, `projector`, `scheduler`, `admin`, `migrate`; package roots for kernel, intent, capability, governance, workflow, engines, domains, transaction, ledger, human work, connectivity, trust, operations, stores and transports; and deferred directories that may exist only as definitions/conformance.
  - **REFACTOR:** directory presence never implies deployed service or implemented authority.
  - **Refs:** [Go technology constitution](specs/go-only-technology-constitution.md), [platform planes](specs/platform-plane-model.md), [implementation-depth vocabulary](plan.md#phase-1-implementation-depth-matrix).

- [x] `ARCH-GO-002` **[P0][SOL_HIGH] Define the machine-readable package dependency policy.**
  - **Evidence (2026-09-03):** `TestPackageDependencyPolicyRejectsReverseEdges` in `tools/policy/depedge`; `go test -count=1 ./tools/policy/depedge/...` PASS on windows/arm64 (Go 1.26.3); `go vet`, `gofmt -l`, `staticcheck` clean; branch plan-revision-2026-09-02.
  - **Depends:** `ARCH-GO-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestPackageDependencyPolicyRejectsReverseEdges`.
  - **TEST MATRIX:** `PRIMARY=TestPackageDependencyPolicyRejectsReverseEdges`; `GOLDEN=TestTodo_ARCH_GO_002_Golden`; `INTEGRATION=TestTodo_ARCH_GO_002_Integration`; `CONFORMANCE=TestTodo_ARCH_GO_002_Conformance`; `MUTATION=TestTodo_ARCH_GO_002_Mutation`.
  - **RED:** `TestPackageDependencyPolicyRejectsReverseEdges` names exact import path and forbidden edge when kernel imports upward, engine imports a domain implementation, workflow imports domain persistence, transport imports a store, or a business package imports a concrete adapter.
  - **GREEN:** policy models the direction `kernel <- engines <- domains <- capabilities <- workflow/application <- transport`, with transaction/ledger and integration/store as explicit ports/adapters; only reviewed narrow exceptions have owner, rationale and expiry.
  - **REFACTOR:** dependency rules describe semantic ownership rather than directory-depth heuristics.
  - **Refs:** [Architecture boundaries](specs/platform-responsibility-boundaries.md), [platform plane model](specs/platform-plane-model.md).

- [x] `ARCH-GO-003` **[P0][TERRA] Enforce package boundaries from the Go import graph.**
  - **Evidence (2026-09-03):** `TestGoImportGraphPolicy` in `tools/policy/importgraph`; `go test -count=1 ./tools/policy/importgraph/...` PASS on windows/arm64 (Go 1.26.3); `go vet`, `gofmt -l`, `staticcheck` clean; branch plan-revision-2026-09-02.
  - **Depends:** `ARCH-GO-002`, `TOOL-011`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestGoImportGraphPolicy`.
  - **TEST MATRIX:** `PRIMARY=TestGoImportGraphPolicy`; `PROPERTY=TestTodo_ARCH_GO_003_Property`; `GOLDEN=TestTodo_ARCH_GO_003_Golden`; `CONFORMANCE=TestTodo_ARCH_GO_003_Conformance`.
  - **RED:** `TestGoImportGraphPolicy` injects every forbidden edge, import cycle and undeclared package; CI reports source file, importer, imported package, violated rule and shortest cycle.
  - **GREEN:** `go list -deps -json`-derived graph is acyclic, matches the manifest and produces a stable package/dependency digest in clean checkout CI.
  - **REFACTOR:** authored Go tooling owns the check; no Node or external policy runtime becomes required.
  - **Refs:** [Go technology constitution](specs/go-only-technology-constitution.md), [backlog governance](#0-backlog-governance-and-traceability).

- [x] `ARCH-GO-004` **[P0][SOL_HIGH] Keep `internal/kernel` boring and dependency-minimal.**
  - **Depends:** `ARCH-GO-003`, `MODEL-001`–`MODEL-007`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestKernelImportsOnlyAllowedDependencies`.
  - **TEST MATRIX:** `PRIMARY=TestKernelImportsOnlyAllowedDependencies`; `PROPERTY=TestTodo_ARCH_GO_004_Property`; `GOLDEN=TestTodo_ARCH_GO_004_Golden`; `INTEGRATION=TestTodo_ARCH_GO_004_Integration`; `SECURITY=TestTodo_ARCH_GO_004_Security`; `CONFORMANCE=TestTodo_ARCH_GO_004_Conformance`; `MUTATION=TestTodo_ARCH_GO_004_Mutation`.
  - **RED:** `TestKernelImportsOnlyAllowedDependencies` rejects any kernel import of intent, capability, governance, workflow, engine, domain, integration, store or transport, rejects direct third-party semantic types in exported APIs, and rejects HCM process names in exported kernel APIs.
  - **GREEN:** kernel subpackages own only identity/reference/revision values, temporal primitives, decimal/money/quantity/rounding, presence, digest, evidence refs/receipts, lifecycle, classification and provenance primitives with exhaustive value tests; a qualified mechanical backend may be imported only inside its owning implementation package and never escape its API.
  - **REFACTOR:** no `kernel/common`, service locator, configuration singleton or infrastructure client is permitted.
  - **Refs:** [Wire primitives](data/models/wire-contract-primitives.md), [canonical digest](specs/canonical-envelope-and-digest.md), [model conventions](data/models/modeling-conventions.md).
  - **Evidence (2026-09-03):** `TestKernelImportsOnlyAllowedDependencies`, `TestTodo_ARCH_GO_004_Property`, `TestTodo_ARCH_GO_004_Golden`, `TestTodo_ARCH_GO_004_Integration`, `TestTodo_ARCH_GO_004_Security`, `TestTodo_ARCH_GO_004_Conformance`, `TestTodo_ARCH_GO_004_Mutation`, `TestTodo_ARCH_GO_004_Race` in `tools/policy/archrules`; protobuf-driven canonicalization/digest moved from `internal/kernel` to `internal/engines/wire/{canonical,digest}`, all consumers retargeted, and the remaining kernel tree (`internal/kernel/values`) imports only approved minimal mechanics without exported backend types. `go test -count=1 ./tools/policy/archrules -run '^(TestKernelImportsOnlyAllowedDependencies|TestTodo_ARCH_GO_004_)'` and `go test -count=1 ./internal/engines/wire/canonical ./internal/engines/wire/digest ./internal/kernel/...` PASS on windows/arm64 (Go 1.26.3); supersedes the partial evidence above; refactored; branch plan-revision-2026-09-02.

- [x] `ARCH-GO-029` **[P0][SOL_HIGH] Give pure engines one schema-tagged canonical byte stream for reproducible digests.**
  - **Depends:** `ARCH-GO-004`, `MODEL-001`–`MODEL-007`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestWriterRefusesAStreamWithoutASchema`.
  - **TEST MATRIX:** `PRIMARY=TestWriterRefusesAStreamWithoutASchema`; `GOLDEN=TestDigestIsStableAndAlgorithmTagged`; `FAULT=TestWriterAbandonsTheStreamWhenAValueCannotEncode`; `PROPERTY=TestWriterSortsStringSetsSoMapOrderCannotLeakIn`; `MUTATION=TestWriterDistinguishesFieldsThatWouldOtherwiseCollide`; `INTEGRATION=TestNestedWriterPropagatesItsFailure`; `CONFORMANCE=TestVersionAndExplainAreStable`.
  - **RED:** a stream opens without a schema tag, a value whose `Canonical()` yields nil silently contributes nothing instead of abandoning the stream, two structurally adjacent fields collide on the same bytes, a string set's digest depends on map iteration order, presence and absence of an optional field encode identically, the digest string omits its algorithm tag, or a nested writer's failure is swallowed instead of propagated.
  - **GREEN:** `canonicalbytes.New` refuses an empty schema; `Writer.Value` abandons the stream with `ErrUnencodable` the moment a value cannot encode; every field is tagged and length-prefixed so adjacent fields cannot collide; `SortedStrings` sorts before framing so map order can never leak into the digest; `Optional` writes an explicit presence marker; `Digest` returns `sha256:<hex>` prefixed by `DigestAlgorithm`; and `Nested` propagates a child writer's recorded error into the parent stream.
  - **REFACTOR:** the package stays a pure framing layer over each kernel value's own `Canonical()` encoding; it owns no second canonicalization of kernel types and exposes `Version`/`Explain` per the ARCH-GO-009 engine package contract.
  - **Refs:** [Wire primitives](data/models/wire-contract-primitives.md), [canonical digest](specs/canonical-envelope-and-digest.md), [engine ownership](#businessintent-context-required-by-every-todo).
  - **Evidence (2026-09-06):** `TestWriterRefusesAStreamWithoutASchema`, `TestWriterAbandonsTheStreamWhenAValueCannotEncode`, `TestWriterDistinguishesFieldsThatWouldOtherwiseCollide`, `TestWriterSortsStringSetsSoMapOrderCannotLeakIn`, `TestWriterEncodesPresenceAndAbsenceDistinctly`, `TestDigestIsStableAndAlgorithmTagged`, `TestNestedWriterPropagatesItsFailure`, `TestVersionAndExplainAreStable` in `internal/engines/canonicalbytes`; `go test -count=1 ./internal/engines/canonicalbytes/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `ARCH-GO-005` **[P0][SOL_HIGH] Establish behavioral ownership in `internal/intent` and semantic invocation in `internal/capability`.**
  - **Depends:** `ARCH-GO-003`, `INTENT-001`, `CAP-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestIntentCapabilityPackagesRejectDomainRulesAndAdapters`.
  - **TEST MATRIX:** `PRIMARY=TestIntentCapabilityPackagesRejectDomainRulesAndAdapters`; `GOLDEN=TestTodo_ARCH_GO_005_Golden`; `INTEGRATION=TestTodo_ARCH_GO_005_Integration`; `CONFORMANCE=TestTodo_ARCH_GO_005_Conformance`; `MUTATION=TestTodo_ARCH_GO_005_Mutation`.
  - **RED:** `TestIntentCapabilityPackagesRejectDomainRulesAndAdapters` fails when intent contains Promotion/Payroll-specific policy or capability contains concrete domain/store/provider implementation.
  - **GREEN:** intent owns definitions, instances, families, lifecycle, proposals/change requests and service/repository ports; capability owns descriptors, modes, side-effect declarations, invocation contexts, handlers/gateway/results/discovery; implementations register from composition roots.
  - **REFACTOR:** typed generated interfaces may implement handler contracts without introducing `any`, reflection dispatch or global registries.
  - **Refs:** [BusinessIntent](specs/business-intent-and-change-request.md), [capability lifecycle](specs/capability-registry-and-lifecycle.md).
  - **Evidence (2026-09-03):** `TestIntentCapabilityPackagesRejectDomainRulesAndAdapters` plus `TestTodo_ARCH_GO_005_Integration` (passes against the real tree) in `tools/policy/archrules`, rules in `definitions/architecture/architecture-rules.yaml`; branch plan-revision-2026-09-02; `go test -count=1 ./tools/policy/archrules/...` PASS on windows/arm64 (Go 1.26.3).

- [x] `ARCH-GO-006` **[P0][SOL_HIGH] Make governance a composer over independent policy subsystems.**
  - **Depends:** `ARCH-GO-003`, `GOVERN-001`, `TRUST-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestGovernanceCompositionRejectsPolicyImplementationImports`.
  - **TEST MATRIX:** `PRIMARY=TestGovernanceCompositionRejectsPolicyImplementationImports`; `GOLDEN=TestTodo_ARCH_GO_006_Golden`; `SECURITY=TestTodo_ARCH_GO_006_Security`; `CONFORMANCE=TestTodo_ARCH_GO_006_Conformance`; `MUTATION=TestTodo_ARCH_GO_006_Mutation`.
  - **RED:** `TestGovernanceCompositionRejectsPolicyImplementationImports` fails when governance implements AuthN/AuthZ/legal/privacy/DLP/entitlement/risk rules itself or a subsystem imports governance to authorize its own output.
  - **GREEN:** governance owns input/context/snapshot/decision/obligation/restriction/composition/revalidation/explanation contracts and consumes typed subsystem results through ports with deterministic composition.
  - **REFACTOR:** each subsystem retains semantic owner and independent conformance tests; governance never becomes a generic policy garbage drawer.
  - **Refs:** [Governance composition](specs/governance-decision-and-obligation-composition.md), [AuthZ](specs/organization-scope-and-authz.md).
  - **Evidence (2026-09-03):** `TestGovernanceCompositionRejectsPolicyImplementationImports` plus `TestTodo_ARCH_GO_006_Integration` (passes against the real tree) in `tools/policy/archrules`, rules in `definitions/architecture/architecture-rules.yaml`; branch plan-revision-2026-09-02; `go test -count=1 ./tools/policy/archrules/...` PASS on windows/arm64 (Go 1.26.3).

- [x] `ARCH-GO-007` **[P0][SOL_HIGH] Separate workflow definition, compiler, runtime and thin step adapters.**
  - **Depends:** `ARCH-GO-003`, `WF-COMP-001`, `WF-RUN-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestWorkflowLayersRejectRuntimeCompilerAndDomainPersistenceCycles`.
  - **TEST MATRIX:** `PRIMARY=TestWorkflowLayersRejectRuntimeCompilerAndDomainPersistenceCycles`; `PROPERTY=TestTodo_ARCH_GO_007_Property`; `GOLDEN=TestTodo_ARCH_GO_007_Golden`; `INTEGRATION=TestTodo_ARCH_GO_007_Integration`; `CONFORMANCE=TestTodo_ARCH_GO_007_Conformance`.
  - **RED:** `TestWorkflowLayersRejectRuntimeCompilerAndDomainPersistenceCycles` fails when definitions import runtime, compiler imports concrete stores, runtime imports domain persistence, a step reimplements transform/rules/messaging/documents/integration semantics, or Temporal/Camunda/provider workflow types become persisted or public HCM workflow contracts.
  - **GREEN:** `internal/workflow/{definition,compiler,runtime,step}` has explicit import policy; steps translate typed workflow inputs/results to capability/engine/human-work ports; runtime owns durable orchestration only.
  - **REFACTOR:** `step/transform` delegates to `engine/transform`, `step/rule` to `engine/rules`, and delivery/document/integration steps create semantic intents rather than transport calls.
  - **Refs:** [Workflow runtime](specs/workflow-runtime.md), [workflow step catalog](workflows/_engine/step-types.md).
  - **Evidence (2026-09-03):** `TestWorkflowLayersRejectRuntimeCompilerAndDomainPersistenceCycles` plus `TestTodo_ARCH_GO_007_Integration` (passes against the real tree) in `tools/policy/archrules`, rules in `definitions/architecture/architecture-rules.yaml`; branch plan-revision-2026-09-02; `go test -count=1 ./tools/policy/archrules/...` PASS on windows/arm64 (Go 1.26.3).

- [x] `ARCH-GO-008` **[P0][SOL_HIGH] Keep reusable engines independent of domain implementations.**
  - **Depends:** `ARCH-GO-003`, `XFORM-001`, `POP-001`, `ELIG-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestEnginePackagesRejectDomainAndAdapterImports`.
  - **TEST MATRIX:** `PRIMARY=TestEnginePackagesRejectDomainAndAdapterImports`; `GOLDEN=TestTodo_ARCH_GO_008_Golden`; `INTEGRATION=TestTodo_ARCH_GO_008_Integration`; `CONFORMANCE=TestTodo_ARCH_GO_008_Conformance`.
  - **RED:** `TestEnginePackagesRejectDomainAndAdapterImports` injects imports from every `internal/engine/*` package to `internal/domain/*`, concrete store/provider/transport or another engine's internals and expects exact violations.
  - **GREEN:** engines depend only on kernel, explicit engine contracts and narrow fact/rule ports; domains adapt their facts into requests and consume typed results; cross-engine composition occurs through public contracts/application orchestration.
  - **REFACTOR:** package-private implementation types prevent domain-specific extension from leaking into the shared engine.
  - **Refs:** [Engine coverage](#37-shared-transformation-and-population-engines), [model conventions](data/models/modeling-conventions.md).
  - **Evidence (2026-09-03):** `TestEnginePackagesRejectDomainAndAdapterImports` plus `TestTodo_ARCH_GO_008_Integration` (passes against the real tree) in `tools/policy/archrules`, rules in `definitions/architecture/architecture-rules.yaml`; branch plan-revision-2026-09-02; `go test -count=1 ./tools/policy/archrules/...` PASS on windows/arm64 (Go 1.26.3).

- [x] `ARCH-GO-009` **[P0][SOL_HIGH] Define a consistent package contract for each shared engine.**
  - **Depends:** `ARCH-GO-008`, `MSRC-002`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestEnginePackageContractCompleteness`.
  - **TEST MATRIX:** `PRIMARY=TestEnginePackageContractCompleteness`; `PROPERTY=TestTodo_ARCH_GO_009_Property`; `GOLDEN=TestTodo_ARCH_GO_009_Golden`; `CONFORMANCE=TestTodo_ARCH_GO_009_Conformance`.
  - **RED:** `TestEnginePackageContractCompleteness` rejects an engine missing typed definition/revision/request/result/status, compiler/evaluator where applicable, explanation, errors, service/port ownership, determinism/resource policy or conformance package.
  - **GREEN:** transform, rules, population, eligibility, cycle, balance, qualification, matching, demand, scenario, attestation and conformance-approved program publish manifest-backed package shapes without requiring meaningless files or interfaces.
  - **REFACTOR:** common shape is a coverage contract, not inheritance or a generic engine framework.
  - **Refs:** [Engine todos](#37-shared-transformation-and-population-engines), [registry contracts](data/models/registry-and-coverage-contracts.md).
  - **Evidence (partial, 2026-09-03):** `TestEnginePackageContractCompleteness` PASS (synthetic); `TestTodo_ARCH_GO_009_Integration` FAIL: canonicalbytes, payband, population, eligibility, rules lack `Version()`/`Explain` or a Compile/Evaluate pair (fielddiff and effectivedate conform); engine-contract conformance lane launched 2026-09-03; `go test -count=1 ./.../...`; on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Evidence (2026-09-03):** `TestEnginePackageContractCompleteness` and `TestTodo_ARCH_GO_009_Integration` now PASS (`checked 7 engine packages, 0 with contract gaps`): every `internal/engines/*` package exposes `Version()`, a Compile/Evaluate pair and an `Explain`; additive changes to canonicalbytes, payband, rules, population, eligibility with tests; supersedes the partial line above; branch plan-revision-2026-09-02; `go test -count=1 ./.../...` on windows/arm64 (Go 1.26.3).

- [x] `ARCH-GO-010` **[P0][SOL_HIGH] Enforce domain semantic ownership and package-local ports.**
  - **Depends:** `ARCH-GO-003`, `PEOPLE-001`, `ORG-001`, `POSITION-001`, `COMP-001`, `DB-018`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestDomainOwnershipRejectsAnemicAndCrossDomainPersistence`.
  - **TEST MATRIX:** `PRIMARY=TestDomainOwnershipRejectsAnemicAndCrossDomainPersistence`; `PROPERTY=TestTodo_ARCH_GO_010_Property`; `GOLDEN=TestTodo_ARCH_GO_010_Golden`; `RACE=TestTodo_ARCH_GO_010_Race`; `INTEGRATION=TestTodo_ARCH_GO_010_Integration`; `CONFORMANCE=TestTodo_ARCH_GO_010_Conformance`; `MUTATION=TestTodo_ARCH_GO_010_Mutation`.
  - **RED:** `TestDomainOwnershipRejectsAnemicAndCrossDomainPersistence` fails when domain commands/events/invariants/projections/capabilities live in transport/store, a domain writes another domain's tables, or repository interfaces live in a central repository package.
  - **GREEN:** each `internal/domain/<domain>` owns aggregates, commands, events, invariants, canonical projections, capabilities, services and the minimal repository interfaces needed by its use cases; cross-domain work occurs through capabilities/transactions.
  - **REFACTOR:** keep one package per domain until actual import-cycle, ownership or size evidence justifies an internal subpackage.
  - **Refs:** [People domain](specs/people-employment-assignment-domain.md), [organization domain](specs/organization-and-relationship-domain.md), [compensation](specs/compensation-domain.md).
  - **Evidence (2026-09-03):** `TestDomainOwnershipRejectsAnemicAndCrossDomainPersistence` plus `TestTodo_ARCH_GO_010_Integration` (passes against the real tree) in `tools/policy/archrules`, rules in `definitions/architecture/architecture-rules.yaml`; branch plan-revision-2026-09-02; `go test -count=1 ./tools/policy/archrules/...` PASS on windows/arm64 (Go 1.26.3).

- [x] `ARCH-GO-011` **[P0][SOL_HIGH] Separate transaction coordination from conflict analysis without cycles.**
  - **Depends:** `ARCH-GO-003`, `TX-001`, `CONFLICT-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTransactionConflictDependencyDirection`.
  - **TEST MATRIX:** `PRIMARY=TestTransactionConflictDependencyDirection`; `GOLDEN=TestTodo_ARCH_GO_011_Golden`; `RACE=TestTodo_ARCH_GO_011_Race`; `SECURITY=TestTodo_ARCH_GO_011_Security`; `CONFORMANCE=TestTodo_ARCH_GO_011_Conformance`; `MUTATION=TestTodo_ARCH_GO_011_Mutation`.
  - **RED:** `TestTransactionConflictDependencyDirection` fails when prepare/commit imports a conflict implementation that imports coordinator internals, or conflict detection mutates reservations/transactions.
  - **GREEN:** transaction owns plan/participants/read-write sets/effects/boundaries/prepare/commit/receipt/idempotency/fences/correction; `transaction/conflict` owns footprints/write intents/overlap/classification/reservation checks and returns typed analysis consumed before prepare.
  - **REFACTOR:** shared immutable plan/value contracts break the existing conceptual cycle; no callback/service-locator escape hatch.
  - **Refs:** [Transaction coordinator](specs/transaction-plan-and-commit-coordinator.md), [conflict control](specs/cross-workflow-conflict-and-write-intent.md).
  - **Evidence (2026-09-03):** `TestTransactionConflictDependencyDirection` plus `TestTodo_ARCH_GO_011_Integration` (passes against the real tree) in `tools/policy/archrules`, rules in `definitions/architecture/architecture-rules.yaml`; branch plan-revision-2026-09-02; `go test -count=1 ./tools/policy/archrules/...` PASS on windows/arm64 (Go 1.26.3).

- [x] `ARCH-GO-012` **[P0][SOL_HIGH] Keep semantic ledger ownership independent of PostgreSQL.**
  - **Depends:** `ARCH-GO-003`, `LEDGER-001`, `DB-018`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestLedgerPackageRejectsConcreteStoreDependency`.
  - **TEST MATRIX:** `PRIMARY=TestLedgerPackageRejectsConcreteStoreDependency`; `GOLDEN=TestTodo_ARCH_GO_012_Golden`; `INTEGRATION=TestTodo_ARCH_GO_012_Integration`; `CONFORMANCE=TestTodo_ARCH_GO_012_Conformance`; `MUTATION=TestTodo_ARCH_GO_012_Mutation`.
  - **RED:** `TestLedgerPackageRejectsConcreteStoreDependency` fails when event/authority/stream/append/query/correction/chain/outbox/projection semantics import SQL/PostgreSQL or when store owns ledger rules.
  - **GREEN:** `internal/ledger` defines semantic models and ports; `internal/store/postgres` implements them; independent in-memory/conformance adapter proves semantics without a PostgreSQL import in ledger.
  - **REFACTOR:** PostgreSQL transaction features remain adapter capabilities exposed through narrow interfaces, not leaked handles.
  - **Refs:** [Ledger spec](specs/transaction-ledger-reconciliation-and-repair.md), [database materialization](#26-postgresql-database-and-model-materialization).
  - **Evidence (partial, 2026-09-03):** `TestLedgerPackageRejectsConcreteStoreDependency` PASS (synthetic); `TestTodo_ARCH_GO_012_Integration` FAIL: internal/ledger imports pgx/v5 for its Tx type; fix with LIB-004; `go test -count=1 ./.../...`; on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Evidence (2026-09-03):** `TestLedgerPackageRejectsConcreteStoreDependency` and `TestTodo_ARCH_GO_012_Integration` now PASS (`0 ledger-imports-postgres violations`): `internal/ledger` depends only on `internal/data/dbport`; supersedes the partial line above; `go test -count=1 ./tools/policy/archrules/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `ARCH-GO-013` **[P0][SOL_HIGH] Place interfaces beside their semantic consumers and adapters below ownership.**
  - **Depends:** `ARCH-GO-010`, `ARCH-GO-012`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestPortOwnershipRejectsCentralRepositoryAndProviderInterfaces`.
  - **TEST MATRIX:** `PRIMARY=TestPortOwnershipRejectsCentralRepositoryAndProviderInterfaces`; `GOLDEN=TestTodo_ARCH_GO_013_Golden`; `INTEGRATION=TestTodo_ARCH_GO_013_Integration`; `CONFORMANCE=TestTodo_ARCH_GO_013_Conformance`.
  - **RED:** `TestPortOwnershipRejectsCentralRepositoryAndProviderInterfaces` fails on `internal/repository`, technology-shaped business ports, concrete adapter types in use-case signatures or adapter interfaces authored by implementations.
  - **GREEN:** intent/workflow/domain/work/messaging/integration packages own minimal ports; `store/postgres`, `store/object`, `store/cache` and providers implement them; compile-time assertions and contract tests prove substitution.
  - **REFACTOR:** avoid one interface per struct; introduce a port only at a real ownership/test/adapter boundary.
  - **Refs:** [Platform responsibilities](specs/platform-responsibility-boundaries.md), [Go technology constitution](specs/go-only-technology-constitution.md).
  - **Evidence (partial, 2026-09-03):** `TestPortOwnershipRejectsCentralRepositoryAndProviderInterfaces` PASS (synthetic); `TestTodo_ARCH_GO_013_Integration` FAIL: internal/connectivity/observe/adapters/postgres exports its own `Querier` port; move beside the consumer at integration; `go test -count=1 ./.../...`; on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Evidence (2026-09-03):** `TestPortOwnershipRejectsCentralRepositoryAndProviderInterfaces` and `TestTodo_ARCH_GO_013_Integration` now PASS against the real tree (`Querier` port moved to `internal/connectivity/observe`, 0 adapter-owned interfaces); supersedes the partial line above; branch plan-revision-2026-09-02; `go test -count=1 ./.../...` on windows/arm64 (Go 1.26.3).

- [x] `ARCH-GO-014` **[P0][SOL_HIGH] Separate authored sources, wire generation and internal generation.**
  - **Evidence (2026-09-05):** `TestGeneratedOwnershipRejectsMixedTrees` in `tools/policy/gensources` (every file under schema/, definitions/, gen/ and generator testdata classified AUTHORED, WIRE_GENERATED or INTERNAL_GENERATED by path and header, generated headers in authored roots and hand edits in gen/ refused against generator output recomputed in memory, classification counts reported and pinned; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/gensources/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ARCH-GO-001`, `TOOL-003`, `MSRC-007`, `PROTO-005`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestGeneratedOwnershipRejectsMixedTrees`.
  - **TEST MATRIX:** `PRIMARY=TestGeneratedOwnershipRejectsMixedTrees`; `GOLDEN=TestTodo_ARCH_GO_014_Golden`; `FAULT=TestTodo_ARCH_GO_014_Fault`; `CONFORMANCE=TestTodo_ARCH_GO_014_Conformance`.
  - **RED:** `TestGeneratedOwnershipRejectsMixedTrees` fails when authored domain code appears under generated roots, generated Protobuf code lands in domain packages, or SchemaFlux registries and wire bindings share ownership/path ambiguously.
  - **GREEN:** `api/proto` contains authored Protobuf; `api/gen/go` contains generated wire code; `definitions/*` contains authored SchemaFlux inputs; `internal/generated` contains generated semantic registries; headers/manifests identify generator/source/digest and prohibit manual edits.
  - **REFACTOR:** packages consume generated contracts through stable public import paths without generated-to-authored import cycles.
  - **Refs:** [Go technology constitution](specs/go-only-technology-constitution.md), [model source generation](#29-machine-readable-model-sources-and-public-api-contracts).

- [x] `ARCH-GO-015` **[P0][TERRA] Establish the immutable definitions directory contract.**
  - **Evidence (2026-09-05):** `TestDefinitionTreeRejectsHardcodedOrMisownedConfiguration` in `tools/policy/gensources` (the definitions/ directory contract: every yaml and json either names its generator with a drift test or sits in an authored allowlist with an owner, no Go under definitions/, live counts pinned; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/gensources/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02. Today's authored allowlist still carries the six generated manifests under definitions/generation and definitions/model (their generators do not yet emit a `generated_by:` key) and gen/TOOLS.lock plus gen/wire/\*; follow-up: teach tools/gen/storagemanifest and the generation manifests' writers to emit `generated_by:` so those rows leave the allowlist.
  - **Depends:** `ARCH-GO-014`, `CP-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestDefinitionTreeRejectsHardcodedOrMisownedConfiguration`.
  - **TEST MATRIX:** `PRIMARY=TestDefinitionTreeRejectsHardcodedOrMisownedConfiguration`; `PROPERTY=TestTodo_ARCH_GO_015_Property`; `GOLDEN=TestTodo_ARCH_GO_015_Golden`; `FAULT=TestTodo_ARCH_GO_015_Fault`; `SECURITY=TestTodo_ARCH_GO_015_Security`; `CONFORMANCE=TestTodo_ARCH_GO_015_Conformance`.
  - **RED:** `TestDefinitionTreeRejectsHardcodedOrMisownedConfiguration` fails for authored intent/capability/workflow/schema/rule/form/mapping/population/eligibility/cycle/reference-data definitions outside declared roots or hard-coded customer thresholds in Go.
  - **GREEN:** definitions are owner/version/phase/schema validated, compile through SchemaFlux, and produce dependency/compatibility digests; Go contains algorithms and invariant defaults, not tenant policy such as Promotion thresholds.
  - **REFACTOR:** mandatory security/legal invariants remain code or protected rule-pack contracts and cannot be weakened by customer definitions.
  - **Refs:** [Configuration models](data/models/dataops-configuration.md), [SchemaFlux model sources](#29-machine-readable-model-sources-and-public-api-contracts).

- [x] `ARCH-GO-016` **[P0][TERRA] Enforce Go test placement and cross-system test ownership.**
  - **Depends:** `ARCH-GO-001`, `TOOL-014`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTestLayoutPolicy`.
  - **TEST MATRIX:** `PRIMARY=TestTestLayoutPolicy`; `GOLDEN=TestTodo_ARCH_GO_016_Golden`; `FAULT=TestTodo_ARCH_GO_016_Fault`; `CONFORMANCE=TestTodo_ARCH_GO_016_Conformance`.
  - **RED:** `TestTestLayoutPolicy` fails when ordinary unit/property/fuzz tests are detached from their package, production code depends on `/test`, or cross-system fixtures lack declared owner/schema/digest.
  - **GREEN:** package tests live beside code; `/test/{fixtures,golden,conformance,fault,fuzz,integration}` holds only shared cross-system assets/harnesses; each fixture names applicable packages, clock, source versions and oracle.
  - **REFACTOR:** conformance/fault harnesses may import public/internal contracts but never expose test-only hooks in production APIs.
  - **Refs:** [Conformance audit](specs/adversarial-audit-32-reviewers-2026-08-14.md), [workflow reference suite](reference-workflows/reference-suite.md).
  - **Evidence (2026-09-03):** `TestTestLayoutPolicy`, `TestTodo_ARCH_GO_016_Golden`, `TestTodo_ARCH_GO_016_Fault`, and `TestTodo_ARCH_GO_016_Conformance` in `tools/policy/testlayout`; deterministic real-tree and fixture scans reject detached ordinary tests, production imports of `/test`, undocumented cross-system harnesses, and shared assets without owner/package/clock/source-version/oracle/SHA-256 metadata. `go test -count=1 ./tools/policy/testlayout` and `go vet ./tools/policy/testlayout` PASS on windows/arm64 (Go 1.26.3); refactored; branch plan-revision-2026-09-02.

- [x] `ARCH-GO-017` **[P0][TERRA] Ban architectural garbage-drawer packages and naming.**
  - **Depends:** `ARCH-GO-003`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestNoGarbageDrawerPackages`.
  - **TEST MATRIX:** `PRIMARY=TestNoGarbageDrawerPackages`; `GOLDEN=TestTodo_ARCH_GO_017_Golden`; `INTEGRATION=TestTodo_ARCH_GO_017_Integration`; `CONFORMANCE=TestTodo_ARCH_GO_017_Conformance`.
  - **RED:** `TestNoGarbageDrawerPackages` rejects new `services`, `utils`, `helpers`, `common`, `managers`, central `models`/`repositories`, `impl` suffix packages or catch-all exported APIs without approved bounded semantic meaning.
  - **GREEN:** every package name has one semantic owner and bounded responsibility; shared code is moved to the narrowest valid kernel/engine/domain/adapter package or deliberately duplicated when coupling would be worse.
  - **REFACTOR:** exception requires architecture owner, import graph evidence, replacement plan and expiry.
  - **Refs:** [Go technology constitution](specs/go-only-technology-constitution.md), [spec ownership](specs/specification-ownership-registry.md).
  - **Evidence (2026-09-03):** `TestNoGarbageDrawerPackages`, `TestTodo_ARCH_GO_017_Golden`, `TestTodo_ARCH_GO_017_Integration`, `TestTodo_ARCH_GO_017_Conformance` in `tools/policy/garbagedrawer` (deterministic package/API scan rejects generic package names, impl suffixes and unbounded exported catch-all APIs; exceptions require owner, rationale, follow-up and expiry; real Go tree passes); `go test -count=1 ./tools/policy/garbagedrawer` and `go vet ./tools/policy/garbagedrawer` PASS on windows/arm64 (Go 1.26.3); refactored; branch plan-revision-2026-09-02.

- [x] `ARCH-GO-018` **[P0][SOL_HIGH] Gate the exact Phase 1 physical package subset.**
  - **Evidence (2026-09-05):** `TestPhaseOnePackageAllowlist` in `tools/policy/phaseonegate` (declared Phase 1 package allowlist derived from the repository layout with a per-package inclusion reason and, for every excluded package, the deferred gate that would admit it; the production import graph of cmd/hcmnext computed in the test refuses any dependency outside the allowlist naming the edge and any allowlisted package the graph never reaches, a generated inclusion manifest golden, live gaps pinned with owner todos; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/phaseonegate/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ARCH-GO-003`, `GOV-009`, `ARCH-GO-015`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestPhaseOnePackageAllowlist`.
  - **TEST MATRIX:** `PRIMARY=TestPhaseOnePackageAllowlist`; `PROPERTY=TestTodo_ARCH_GO_018_Property`; `GOLDEN=TestTodo_ARCH_GO_018_Golden`; `INTEGRATION=TestTodo_ARCH_GO_018_Integration`; `SECURITY=TestTodo_ARCH_GO_018_Security`; `CONFORMANCE=TestTodo_ARCH_GO_018_Conformance`; `MUTATION=TestTodo_ARCH_GO_018_Mutation`.
  - **RED:** `TestPhaseOnePackageAllowlist` fails when Gate A/B production graph imports deferred engines/domains or requires a directory merely because it appears in the end-state layout.
  - **GREEN:** initial compiled graph contains kernel, intent, capability, governance/AuthN/AuthZ, transform/rules engines, workflow, transaction, ledger, People/Organization/Position/Compensation, work/forms/messaging/integration/DataOps, PostgreSQL/object adapters, operations and transport; later engines/domains remain definitions/conformance only until their first approved consumer.
  - **REFACTOR:** generated manifest explains why every included package is necessary for the wedge and which deferred package gate would add it.
  - **Refs:** [Execution plan](execution-plan.md), [Phase 1 scope](plan.md#phase-1-changeops-overlay).

- [ ] `ARCH-GO-019` **[GATE_C][SOL_HIGH] Require evidence before module, process or service decomposition.**
  - **Implementation evidence (2026-09-08; commit gates pending):** `TestDecompositionDecisionRejectsTopologyDrivenSplit` and the named matrix cover topology-only refusal, fixed golden bytes, concurrent checks, boundary inventory and mutation. Additional regressions reject baseline additions, YAML alias/resource hiding, invented ownership and missing/tampered evidence. Reviewed decision v2 requires repository-contained SHA-256-pinned sources and integer-minor-unit costs. Independent `go test -count=1 -cover ./tools/quality/decomposition/` PASS at 76.6%; `npm run check:decomposition` PASS with exactly ten grandfathered boundaries and no reviewed additions. The command is wired into `test:all` and CI; no new process or service is approved by this evidence.
  - **Depends:** `ARCH-GO-003`, `PERF-ENV-001`, `OPS-007`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestDecompositionDecisionRejectsTopologyDrivenSplit`.
  - **TEST MATRIX:** `PRIMARY=TestDecompositionDecisionRejectsTopologyDrivenSplit`; `GOLDEN=TestTodo_ARCH_GO_019_Golden`; `RACE=TestTodo_ARCH_GO_019_Race`; `SECURITY=TestTodo_ARCH_GO_019_Security`; `CONFORMANCE=TestTodo_ARCH_GO_019_Conformance`; `MUTATION=TestTodo_ARCH_GO_019_Mutation`.
  - **RED:** `TestDecompositionDecisionRejectsTopologyDrivenSplit` rejects a new module/executable/network service justified only by team naming, domain count or deployment fashion and detects a domain package duplicated across processes.
  - **GREEN:** decision requires measured scaling/failure/security/residency/release/ownership boundary, API/event consistency contract, data authority, failure/repair model, migration/rollback, added operational cost and proof the modular monolith cannot meet the constraint safely.
  - **REFACTOR:** prefer assigning another worker role or scaling an existing process before creating a remote boundary.
  - **Refs:** [Platform architecture](specs/platform-architecture-catalog.md), [SLOs](specs/slo-sli-error-budget.md), [risk register](specs/risk-register.md).

- [x] `ARCH-GO-020` **[P0][SOL_HIGH] Build a single explicit application composition root.**
  - **Evidence (2026-09-05):** `TestCompositionRootRejectsGlobalRegistrationAndHiddenDependencies`, `TestCompositionRootScanDetectsEachViolation`, `TestTodo_ARCH_GO_020_{Golden,Integration,Conformance}` in `internal/application` (an authored composition root: ServeConfig validated from values, explicit Option seams for logger, store, verifier, telemetry, migrator, listener, clocks, ids, evidence, domain inputs and execution composer with zero options meaning production, Role and Lifecycle Start/Stop, ComposeServe moved out of cmd, a deterministic composed-graph digest over 33 components; the AST scan over 1095 production files rejects init-time registration, package-level mutable registries, service locators, adapter construction in business packages and command-specific business semantics with two owner-pinned exceptions; cmd/hcmnext now only parses configuration, selects the role and invokes the lifecycle with the Goose migrator supplied through a port; CLI flags and README recipe unchanged; `application` declared as a repository-layout root during integration); `go test -count=1 ./internal/application/ ./cmd/hcmnext/ ./tools/policy/importgraph/ ./test/bootstrap/ ./test/tunnel/ ./test/workflow/ ./test/serve/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ARCH-GO-005`–`ARCH-GO-015`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestCompositionRootRejectsGlobalRegistrationAndHiddenDependencies`.
  - **TEST MATRIX:** `PRIMARY=TestCompositionRootRejectsGlobalRegistrationAndHiddenDependencies`; `GOLDEN=TestTodo_ARCH_GO_020_Golden`; `INTEGRATION=TestTodo_ARCH_GO_020_Integration`; `CONFORMANCE=TestTodo_ARCH_GO_020_Conformance`.
  - **RED:** `TestCompositionRootRejectsGlobalRegistrationAndHiddenDependencies` fails for package `init` registration, global mutable registries, service locator, concrete adapter construction inside business packages or command-specific business semantics.
  - **GREEN:** authored `internal/application` wiring constructs registries, governance, workflows, engines, domains, ports/adapters and worker roles from validated config; `cmd/*` only parses command/runtime configuration, selects role and invokes application lifecycle.
  - **REFACTOR:** test composition swaps adapters/clocks/providers explicitly without production-only branches.
  - **Refs:** [Go technology constitution](specs/go-only-technology-constitution.md), [platform plane model](specs/platform-plane-model.md).

- [x] `ARCH-GO-021` **[P0][SOL_HIGH] Enforce Integration package sub-boundaries and Transformation ownership.**
  - **Evidence (2026-09-05):** `TestIntegrationPackagesRejectDuplicateTransformAndProviderLeakage` in `tools/policy/archrules` (import checker enforcing the integration sub-boundaries: connectivity packages reach transformation only through ir, exec, lineage and adapters, transformation imports no connectivity except adapters, mapping/execute is the sole IR executor in connectivity, schemasnapshot never imports mapping and mft imports neither, violations naming file and rule with an owner-pinned allowlist and a golden allowed-edge table, run against the live tree; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/archrules/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ARCH-GO-008`, `ARCH-GO-013`, `XFORM-008`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestIntegrationPackagesRejectDuplicateTransformAndProviderLeakage`.
  - **TEST MATRIX:** `PRIMARY=TestIntegrationPackagesRejectDuplicateTransformAndProviderLeakage`; `GOLDEN=TestTodo_ARCH_GO_021_Golden`; `FUZZ=FuzzTodo_ARCH_GO_021`; `INTEGRATION=TestTodo_ARCH_GO_021_Integration`; `FAULT=TestTodo_ARCH_GO_021_Fault`; `CONFORMANCE=TestTodo_ARCH_GO_021_Conformance`.
  - **RED:** `TestIntegrationPackagesRejectDuplicateTransformAndProviderLeakage` fails when mapping compiles/executes its own language, domain/workflow imports provider packages, observations depend on provider response types, or reconciliation mutates external state directly.
  - **GREEN:** `internal/integration` separates definition, connection, mapping/crosswalk, operation/journal/dispatch/ambiguity/redrive, observe/normalize/freshness, reconcile, webhook, sync, file and provider adapters; mapping delegates to `engine/transform`; normalized contracts isolate vendors.
  - **REFACTOR:** named providers remain replaceable adapters and cannot become shared semantic dependencies.
  - **Refs:** [Integration platform](specs/integration-platform.md), [Transformation engine](#37-shared-transformation-and-population-engines).

- [ ] `ARCH-GO-022` **[P0][SOL_HIGH] Separate workflow, human work, forms and messaging ownership.**
  - **Evidence (partial, 2026-09-05):** `TestHumanInteractionBoundaries`, `TestTodo_ARCH_GO_022_Integration`, `TestTodo_ARCH_GO_022_Conformance` in `tools/policy/archrules` enforce the ownership edges on the real import graph; nine existing workflow→humanwork edges (execute, execute/effects, inspect, prototype, simulate, steps/approval, steps/task → humanwork or humanwork/workitem) are pinned in a reviewed allowlist, so the separation itself is not yet achieved and the todo stays open; go test -count=1 ./tools/policy/archrules -run ARCH_GO_022/ PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ARCH-GO-007`, `WORK-001`, `FORM-001`, `MSG-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestHumanInteractionBoundaries`.
  - **TEST MATRIX:** `PRIMARY=TestHumanInteractionBoundaries`; `GOLDEN=TestTodo_ARCH_GO_022_Golden`; `RACE=TestTodo_ARCH_GO_022_Race`; `INTEGRATION=TestTodo_ARCH_GO_022_Integration`; `CONFORMANCE=TestTodo_ARCH_GO_022_Conformance`; `BROWSER=TestTodo_ARCH_GO_022_Browser`; `MUTATION=TestTodo_ARCH_GO_022_Mutation`.
  - **RED:** `TestHumanInteractionBoundaries` fails when workflow owns task queues/claims, forms own approvals, messaging owns business escalation, or workflow/human-work calls email/SMS providers.
  - **GREEN:** `internal/work` owns item/queue/assignment/claim/lease/deadline/escalation/delegation/completion and ApprovalTask specialization; `internal/forms` owns definitions/conditions/compiler/validation/drafts/submissions/accessibility; `internal/messaging` owns MessageIntent/audience/endpoints/preferences/plans/templates/messages/attempts/inbox/threads/signals/reconciliation/providers.
  - **REFACTOR:** shared evidence/value types live in kernel; business escalation remains workflow-owned and delivery remains messaging-owned.
  - **Refs:** [Human work/forms](specs/human-work-forms-and-rules.md), [messaging](specs/messaging-and-notification-plane.md).

- [x] `ARCH-GO-023` **[P0][SOL_HIGH] Keep transport as a thin generated boundary.**
  - **Evidence (2026-09-05):** `TestTransportRejectsBusinessAndPersistenceImports` in `tools/policy/archrules` (AST checker over every package under internal/transport: no business-logic imports of domains/engines/data outside declared ports, no SQL strings, no direct pool use, RPC handler bodies under a declared statement budget, live exceptions pinned in an allowlist naming the owner todo; the ARCH-GO-009 engine-contract gaps it surfaced (snapshot, messagetemplate, replan, docextract, popscale lacking Version or Explain) were closed during integration; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/archrules/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ARCH-GO-005`, `PROTO-006`, `EDGE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTransportRejectsBusinessAndPersistenceImports`.
  - **TEST MATRIX:** `PRIMARY=TestTransportRejectsBusinessAndPersistenceImports`; `GOLDEN=TestTodo_ARCH_GO_023_Golden`; `INTEGRATION=TestTodo_ARCH_GO_023_Integration`; `CONFORMANCE=TestTodo_ARCH_GO_023_Conformance`.
  - **RED:** `TestTransportRejectsBusinessAndPersistenceImports` fails when `internal/transport/{grpc,grpcbridge,middleware}` imports domain repositories/concrete stores/providers, implements HCM validation, changes transport parity or constructs dependencies.
  - **GREEN:** transport decodes generated contracts, applies shared identity/governance/admission middleware, invokes intent/capability/application APIs and encodes canonical results/errors; grpcbridge contains HTTP adaptation only.
  - **REFACTOR:** field/business validation belongs to generated schemas/domain/engine contracts, not duplicated handlers.
  - **Refs:** [Go technology constitution](specs/go-only-technology-constitution.md), [Experience/API plane](specs/platform-plane-model.md).

- [ ] `ARCH-GO-024` **[P0][SOL_HIGH] Separate configuration semantics from source definitions and rollout.**
  - **Depends:** `ARCH-GO-015`, `CP-001`, `ROLLOUT-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestConfigurationOwnershipRejectsActivationAndSourceCycles`.
  - **TEST MATRIX:** `PRIMARY=TestConfigurationOwnershipRejectsActivationAndSourceCycles`; `GOLDEN=TestTodo_ARCH_GO_024_Golden`; `CONFORMANCE=TestTodo_ARCH_GO_024_Conformance`; `MUTATION=TestTodo_ARCH_GO_024_Mutation`.
  - **RED:** `TestConfigurationOwnershipRejectsActivationAndSourceCycles` fails when definitions import runtime configuration, configuration executes domain changes, rollout rewrites bundles, or customer config bypasses publication/approval.
  - **GREEN:** `internal/configuration` owns definition/snapshot/bundle/dependency/diff/validation/approval/activation/rollback/registry contracts; authored `definitions/*` are inputs; control plane distributes signed outputs; rollout targets immutable versions.
  - **REFACTOR:** configuration controls behavior selection but never performs Jane's transaction itself.
  - **Refs:** [Configuration models](data/models/dataops-configuration.md), [control plane](#30-control-plane-publication-distribution-and-activation).

- [x] `ARCH-GO-025` **[P0][SOL_HIGH] Organize concrete storage by technology while ports remain semantic.**
  - **Depends:** `ARCH-GO-013`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestStoreAdaptersRejectBusinessOwnership`.
  - **TEST MATRIX:** `PRIMARY=TestStoreAdaptersRejectBusinessOwnership`; `PROPERTY=TestTodo_ARCH_GO_025_Property`; `GOLDEN=TestTodo_ARCH_GO_025_Golden`; `RACE=TestTodo_ARCH_GO_025_Race`; `INTEGRATION=TestTodo_ARCH_GO_025_Integration`; `CONFORMANCE=TestTodo_ARCH_GO_025_Conformance`; `MUTATION=TestTodo_ARCH_GO_025_Mutation`.
  - **RED:** `TestStoreAdaptersRejectBusinessOwnership` fails when `store/postgres`, `store/object` or `store/cache` defines domain invariants/commands, exposes SQL/client handles upward or becomes imported by transport/workflow/domain use cases.
  - **GREEN:** technology packages implement owner-defined interfaces for intent, workflow, ledger, people, organization, position, compensation, work, messaging, integration and transaction; the PostgreSQL implementation confines `pgx/v5` to the adapter/composition boundary; compile-time assertions plus shared adapter contract suites verify behavior.
  - **REFACTOR:** transaction bundling occurs through explicit unit-of-work capabilities and never by leaking database transactions across package APIs.
  - **Refs:** [Database materialization](#26-postgresql-database-and-model-materialization), [platform responsibilities](specs/platform-responsibility-boundaries.md).
  - **Evidence (2026-09-03):** `TestStoreAdaptersRejectBusinessOwnership`, Declared `ARCH-GO-025` test matrix and acceptance behavior in `tools/quality/storagearch`; PASS; `go test -p 1 ./tools/quality/storagearch` and `go vet ./tools/quality/storagearch` on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `ARCH-GO-026` **[P0][SOL_HIGH] Bound trust, intelligence and operations packages.**
  - **Depends:** `ARCH-GO-002`, `GOV-009`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestCrossCuttingPackagesRejectOmniscientImports`.
  - **TEST MATRIX:** `PRIMARY=TestCrossCuttingPackagesRejectOmniscientImports`; `PROPERTY=TestTodo_ARCH_GO_026_Property`; `GOLDEN=TestTodo_ARCH_GO_026_Golden`; `FAULT=TestTodo_ARCH_GO_026_Fault`; `SECURITY=TestTodo_ARCH_GO_026_Security`; `CONFORMANCE=TestTodo_ARCH_GO_026_Conformance`; `MUTATION=TestTodo_ARCH_GO_026_Mutation`.
  - **RED:** `TestCrossCuttingPackagesRejectOmniscientImports` fails when AuthN/AuthZ/privacy/DLP/secrets/agent/intelligence/operations/admission/rollout/recovery import concrete domains/stores broadly or synchronous command correctness requires deferred intelligence/agent packages.
  - **GREEN:** each cross-cutting package publishes narrow typed input/result/port contracts; operations observes everything without owning business truth; intelligence/agent access only governed capabilities/projections; deferred packages are absent from Phase 1 mandatory graph.
  - **REFACTOR:** no context object grants ambient access to every HCM fact or adapter.
  - **Refs:** [Platform planes](specs/platform-plane-model.md), [Phase 1 scope](execution-plan.md).
  - **Evidence (2026-09-03):** `TestCrossCuttingPackagesRejectOmniscientImports`, Declared `ARCH-GO-026` test matrix and acceptance behavior in `tools/policy/crosscut`; PASS; `go test -p 1 ./tools/policy/crosscut` and `go vet ./tools/policy/crosscut` on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `ARCH-GO-027` **[P0][TERRA] Prevent interface and package ceremony without evidence.**
  - **Depends:** `ARCH-GO-013`, `GOV-007`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestArchitectureCeremonyRejectsInterfacePerStructAndEmptyLayers`.
  - **TEST MATRIX:** `PRIMARY=TestArchitectureCeremonyRejectsInterfacePerStructAndEmptyLayers`; `GOLDEN=TestTodo_ARCH_GO_027_Golden`; `RACE=TestTodo_ARCH_GO_027_Race`; `CONFORMANCE=TestTodo_ARCH_GO_027_Conformance`.
  - **RED:** `TestArchitectureCeremonyRejectsInterfacePerStructAndEmptyLayers` identifies one-method interfaces with one in-package implementation/no consumer need, parallel `*_service|*_repository|*_domain|*_model|*_impl` packages or forwarding-only layers.
  - **GREEN:** review manifest records the consumer-owned boundary served by each interface/package; concrete types remain acceptable until substitution, ownership or testing requires a port.
  - **REFACTOR:** remove ceremony while preserving typed contracts at actual domain, adapter and execution boundaries.
  - **Refs:** [Go technology constitution](specs/go-only-technology-constitution.md), [risk register](specs/risk-register.md).
  - **Evidence (2026-09-03):** `TestArchitectureCeremonyRejectsInterfacePerStructAndEmptyLayers`, `TestTodo_ARCH_GO_027_Golden`, `TestTodo_ARCH_GO_027_Race`, `TestTodo_ARCH_GO_027_Conformance` in `tools/policy/archrules` (AST scan rejects interface-per-struct/no-consumer boundaries, ceremonial package-family suffixes and forwarding-only layers; current real seams require bounded owner/rationale/evidence/expiry exceptions); `go test -count=1 ./tools/policy/archrules -run '^(TestArchitectureCeremonyRejectsInterfacePerStructAndEmptyLayers|TestTodo_ARCH_GO_027_)'` and `go vet ./tools/policy/archrules/...` PASS on windows/arm64 (Go 1.26.3); refactored; branch plan-revision-2026-09-02.

- [x] `ARCH-GO-028` **[P0][TERRA] Generate repository architecture documentation from the package manifest.**
  - **Evidence (2026-09-05):** `TestArchitectureDocumentationMatchesImportGraph` in `tools/gen/archdoc` (deterministic Markdown architecture document generated from repository-layout, package-dependency-policy and dependency-roles plus a live per-root package inventory, written by cmd/archdoc to -out with a golden drift test; the planning target path is reported, not written; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/gen/archdoc/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ARCH-GO-001`–`ARCH-GO-027`, `DOC-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestArchitectureDocumentationMatchesImportGraph`.
  - **TEST MATRIX:** `PRIMARY=TestArchitectureDocumentationMatchesImportGraph`; `PROPERTY=TestTodo_ARCH_GO_028_Property`; `GOLDEN=TestTodo_ARCH_GO_028_Golden`; `INTEGRATION=TestTodo_ARCH_GO_028_Integration`; `CONFORMANCE=TestTodo_ARCH_GO_028_Conformance`.
  - **RED:** `TestArchitectureDocumentationMatchesImportGraph` fails when tree, package owner, layer, process role, phase, port/adapter or dependency diagram differs from current source/manifest.
  - **GREEN:** Go tooling emits the current repository tree, allowed dependency diagram, semantic-owner-to-process-role map, Phase 1 subset and deferred/decomposition decisions with stable digest and source links.
  - **REFACTOR:** generated docs explain observed architecture; normative rules remain machine-readable sources.
  - **Refs:** [Platform architecture](specs/platform-architecture-catalog.md), [spec ownership](specs/specification-ownership-registry.md).

---

## 2B. External Go dependency qualification and semantic ownership

> **Disposition (2026-09-02):** P1A for `LIB-001` and the modules M2 actually imports (pgx, goose, testcontainers, grpc-go, protobuf). Every other candidate is qualified when first imported, not in advance.

The governing rule is: **HCM Next owns HCM semantics; third-party libraries
provide infrastructure mechanics.** A named candidate is not approved merely
because it appears below. Its todo must produce a pinned, reviewed qualification
or an explicit rejection and replacement decision.

- [x] `LIB-001` **[P0][SOL_HIGH] Publish the dependency-role and ownership manifest.**
  - **Evidence (2026-09-03):** `TestDependencyRoleManifestRejectsUnclassifiedModule` in `tools/policy/depmanifest`; `go test -count=1 ./tools/policy/depmanifest/...` PASS on windows/arm64 (Go 1.26.3); `go vet`, `gofmt -l`, `staticcheck` clean; branch plan-revision-2026-09-02.
  - **Depends:** `TOOL-001`, `TOOL-019`, `GOV-013`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestDependencyRoleManifestRejectsUnclassifiedModule`.
  - **TEST MATRIX:** `PRIMARY=TestDependencyRoleManifestRejectsUnclassifiedModule`; `PROPERTY=TestTodo_LIB_001_Property`; `GOLDEN=TestTodo_LIB_001_Golden`; `INTEGRATION=TestTodo_LIB_001_Integration`; `SECURITY=TestTodo_LIB_001_Security`; `CONFORMANCE=TestTodo_LIB_001_Conformance`; `BROWSER=TestTodo_LIB_001_Browser`.
  - **RED:** `TestDependencyRoleManifestRejectsUnclassifiedModule` fails for a direct or transitive production module without exact version/digest, role `PROJECT_CORE|INFRASTRUCTURE_MECHANIC|DEV_TEST_ONLY|PROHIBITED`, semantic owner, allowed import roots, license/security owner, upgrade SLA, data/process exposure and replacement path; it also fails if any module other than Go, GWC, grpcbridge or SchemaFlux is labeled `PROJECT_CORE`.
  - **GREEN:** the manifest classifies the complete module graph and explicitly records Protobuf/gRPC, pgx, CEL-Go, apd, OpenTelemetry, Goose, Testcontainers, go-oidc/x/oauth2 and optional JOSE/JWK candidates as replaceable mechanics subject to their qualification todos.
  - **REFACTOR:** generate SBOM annotations, architecture rules and dependency documentation from this one source rather than parallel allowlists.
  - **Refs:** [Go technology constitution](specs/go-only-technology-constitution.md), [open-source-first principle](plan.md#518-go-core-contract-first-and-open-source-first), [risk register](specs/risk-register.md).

- [x] `LIB-002` **[P0][SOL_HIGH] Enforce the third-party semantic firewall in the Go import graph.**
  - **Depends:** `LIB-001`, `ARCH-GO-002`, `ARCH-GO-003`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestThirdPartySemanticFirewall`.
  - **TEST MATRIX:** `PRIMARY=TestThirdPartySemanticFirewall`; `PROPERTY=TestTodo_LIB_002_Property`; `GOLDEN=TestTodo_LIB_002_Golden`; `INTEGRATION=TestTodo_LIB_002_Integration`; `CONFORMANCE=TestTodo_LIB_002_Conformance`; `MUTATION=TestTodo_LIB_002_Mutation`.
  - **RED:** `TestThirdPartySemanticFirewall` injects pgx/CEL/apd/OTel/OIDC/Goose/Testcontainers/provider types into Protobuf, definitions, intent, capability, workflow definitions, domain models, ledger events or owner ports and reports the shortest forbidden import/type-exposure path.
  - **GREEN:** only manifest-approved implementation roots import each dependency; exported owned contracts contain HCM Next/kernel/generated types; compile-time and descriptor scans prove third-party types do not become wire, stored semantic or cross-package authority contracts.
  - **REFACTOR:** allow narrowly evidenced standard-library use without wrapper ceremony; exceptions are owner-, rationale-, version- and expiry-bound.
  - **Refs:** [Package dependency policy](#2a-modular-go-monolith-and-semantic-package-architecture), [capability registry](specs/capability-registry-and-lifecycle.md).
  - **Evidence (2026-09-03):** `TestThirdPartySemanticFirewall`, `TestTodo_LIB_002_Property`, `TestTodo_LIB_002_Golden`, `TestTodo_LIB_002_Integration`, `TestTodo_LIB_002_Conformance`, `TestTodo_LIB_002_Mutation`, `TestTodo_LIB_002_Security`, `TestTodo_LIB_002_Race` in `tools/policy/libfirewall`; real-tree scan covers 173 packages with zero violations, prefix-bypass mutants reject, health reuses the owned storage-disposition YAML seam, and exact narrow mechanics roots govern pgtest, operations UUIDs, NFC normalization, UX HTML parsing, runtime contract YAML, `gen/wire` and `internal/engines/wire`. `go test -count=1 ./tools/policy/libfirewall` plus related health, telemetry, UX, wire-engine, intent, depmanifest and depedge suites PASS on windows/arm64 (Go 1.26.3); `-race` remains on supported Linux CI; supersedes the partial evidence above; refactored; branch plan-revision-2026-09-02.

- [x] `LIB-003` **[P0][TERRA] Qualify Protobuf and grpc-go as wire and RPC mechanics.**
  - **Depends:** `LIB-001`, `TOOL-002`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestGRPCBackendQualification`.
  - **TEST MATRIX:** `PRIMARY=TestGRPCBackendQualification`; `GOLDEN=TestTodo_LIB_003_Golden`; `INTEGRATION=TestTodo_LIB_003_Integration`; `CONFORMANCE=TestTodo_LIB_003_Conformance`.
  - **RED:** `TestGRPCBackendQualification` fails on an unpinned generator/runtime pair, descriptor/runtime incompatibility, lost presence/unknown fields, inconsistent canonical errors/deadlines/cancellation or transport metadata becoming caller-selectable governance context.
  - **GREEN:** a pinned `google.golang.org/protobuf` and `google.golang.org/grpc` combination passes generated round-trip, streaming, cancellation, size-limit and grpcbridge parity vectors while HCM Next owns all message semantics, capability metadata and error taxonomy.
  - **REFACTOR:** generated wire code remains isolated from authored domains and grpc-go types do not cross capability/domain ports.
  - **Refs:** [Public API contracts](#29-machine-readable-model-sources-and-public-api-contracts), [grpcbridge boundary](specs/go-only-technology-constitution.md).
  - **Evidence (2026-09-03):** `TestGRPCBackendQualification`, `TestTodo_LIB_003_Golden`, `TestTodo_LIB_003_Integration`, `TestTodo_LIB_003_Conformance` in `tools/policy/libfirewall` scan 172 packages with zero Protobuf/gRPC/Connect violations: app conversions moved behind `internal/intent/protomap`, data wire codecs into `gen/wire`, and server composition into `internal/transport/cell`; only the reviewed wire-canonicalization/codegen roots remain explicitly documented. `go test -count=1 ./tools/policy/libfirewall -run '^(TestGRPCBackendQualification|TestTodo_LIB_003_)'` and `go test -count=1 ./cmd/hcmnext ./internal/intent/protomap ./internal/data/projection/critical ./internal/data/provenance ./internal/transport/cell ./test/bootstrap ./test/workspace` PASS on windows/arm64 (Go 1.26.3); supersedes the partial evidence above; refactored; branch plan-revision-2026-09-02.

- [x] `LIB-004` **[P0][SOL_HIGH] Qualify pgx/v5 behind the PostgreSQL adapter boundary.**
  - **Depends:** `LIB-001`, `TOOL-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestPGXAdapterQualification`.
  - **TEST MATRIX:** `PRIMARY=TestPGXAdapterQualification`; `PROPERTY=TestTodo_LIB_004_Property`; `GOLDEN=TestTodo_LIB_004_Golden`; `RACE=TestTodo_LIB_004_Race`; `INTEGRATION=TestTodo_LIB_004_Integration`; `FAULT=TestTodo_LIB_004_Fault`; `CONFORMANCE=TestTodo_LIB_004_Conformance`; `MUTATION=TestTodo_LIB_004_Mutation`.
  - **RED:** `TestPGXAdapterQualification` fails for leaked `pgx.Tx|Rows|Conn`, hidden autocommit, ambiguous cancellation, incorrect SQL error mapping, unbounded pool/query, stale-head double win, or partial ledger/projection/outbox commit under injected failure.
  - **GREEN:** a pinned `pgx/v5` adapter uses explicit SQL and passes owner repository, transaction, CAS/fence, bitemporal, multi-stream and outbox atomicity suites; business packages see only owned ports and canonical errors.
  - **REFACTOR:** share connection/configuration mechanics without inventing an ORM-shaped domain model or leaking database handles upward.
  - **Refs:** [Database materialization](#26-postgresql-database-and-model-materialization), [transaction coordinator](specs/transaction-plan-and-commit-coordinator.md).
  - **Evidence (partial, 2026-09-03):** `TestPGXAdapterQualification` PASS; `TestTodo_LIB_004_Integration` FAIL: 25 exported signatures in internal/data/\* adapters, internal/intent/app/pgstore and cmd/hcmnext expose pgx.Tx/Rows/Conn; internal/ledger port imports pgx (ARCH-GO-012). Fix plan: port-level transaction abstraction after the in-flight data lanes finish; `go test -count=1 ./.../...`; on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Evidence (2026-09-03):** `TestPGXAdapterQualification` and `TestTodo_LIB_004_Integration` now PASS (`pgx: 0 import-boundary violations, 8 owning packages scanned, 0 leaked-type violations`): pgx-free `internal/data/dbport` port (Row/Rows/Execer/Querier/Conn/Tx/Beginner) with the single `internal/data/pgxadapter` adapter; every data adapter, pgstore, the observe adapter, cmd composition roots and the test harnesses converted; pgx allowed roots narrowed to internal/data, internal/ledger, migrations, internal/platform/bootstrap, cmd/hcmnext, cmd/migrate; all Postgres suites green; supersedes the partial line above; `go test -count=1 ./tools/policy/libfirewall/...` on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `LIB-005` **[P0][SOL_HIGH] Qualify CEL-Go as a bounded expression backend.**
  - **Evidence (2026-09-05):** `TestCELBackendQualification`, `TestTodo_LIB_005_{Golden,Integration,Fault,Conformance}` in `tools/policy/libqualification` (decision DEFER: no bounded rule DSL needs an expression backend yet — `internal/engines/rules` is the current engine; adoption requires a qualification fixture proving cost limits and no recursion; the test pins that go.mod carries no cel-go module); `go test -count=1 ./tools/policy/libqualification/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LIB-001`, `TOOL-004`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestCELBackendQualification`.
  - **TEST MATRIX:** `PRIMARY=TestCELBackendQualification`; `GOLDEN=TestTodo_LIB_005_Golden`; `FUZZ=FuzzTodo_LIB_005`; `INTEGRATION=TestTodo_LIB_005_Integration`; `FAULT=TestTodo_LIB_005_Fault`; `CONFORMANCE=TestTodo_LIB_005_Conformance`.
  - **RED:** `TestCELBackendQualification` rejects I/O, ambient time/randomness, reflection/provider escape, unbounded cost, type ambiguity, absent/unknown coercion, nondeterministic map iteration and any public/stored contract whose meaning depends on a raw CEL AST or Go CEL type.
  - **GREEN:** pinned CEL-Go compiles only through owned `ExpressionDefinition`, `RuleDefinition`, `DecisionTable` and transformation/form/population condition contracts; exact type, cost, timeout, UNKNOWN/PARTIAL, explanation and golden-vector behavior is deterministic.
  - **REFACTOR:** keep a backend-neutral bounded IR and conformance suite so CEL can be upgraded or replaced without changing proposal/workflow/rule digests silently.
  - **Refs:** [Rules and forms](specs/human-work-forms-and-rules.md), [Transformation engine](#37-shared-transformation-and-population-engines).
  - **Evidence (partial, 2026-09-03):** `TestCELBackendQualification`, `TestTodo_LIB_005_{Golden,Integration,Fault,Conformance}`, `FuzzTodo_LIB_005`, and `TestTodo_LIB_005_AdmissionDecision` in `tools/quality/celqual` validate the owned bounded contract, deterministic vectors, and that `github.com/google/cel-go` is absent from release targets. `definitions/architecture/cel-go-qualification.yaml` remains `NOT_ADMITTED`: no pinned CEL-Go adapter or real backend execution evidence exists, so LIB-005 remains open. Runtime/planning packages are unchanged; no `go.mod` entry was added.

- [x] `LIB-006` **[P0][SOL_HIGH] Qualify apd/v3 behind the decimal kernel.**
  - **Depends:** `LIB-001`, `TOOL-013`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestDecimalBackendQualification`.
  - **TEST MATRIX:** `PRIMARY=TestDecimalBackendQualification`; `PROPERTY=TestTodo_LIB_006_Property`; `GOLDEN=TestTodo_LIB_006_Golden`; `RACE=TestTodo_LIB_006_Race`; `CONFORMANCE=TestTodo_LIB_006_Conformance`; `MUTATION=TestTodo_LIB_006_Mutation`.
  - **RED:** `TestDecimalBackendQualification` fails on NaN/infinity/negative-zero acceptance, undeclared scale/rounding, silent inexact/overflow/underflow, shared mutable context, race, serialization drift or `apd.Decimal` escaping `kernel/decimal`.
  - **GREEN:** pinned `cockroachdb/apd/v3` mechanics implement owned `Money`, `Rate`, `Percentage`, `Quantity` and rounding profiles; traps/conditions map to canonical typed errors and golden/property/fuzz/race vectors return exact canonical bytes.
  - **REFACTOR:** callers cannot depend on apd representation or context, permitting a backend replacement without domain/schema changes.
  - **Refs:** [Wire decimal primitives](data/models/wire-contract-primitives.md), [fixed-decimal todo](#3-canonical-values-schemas-registries-and-model-coverage).
  - **Evidence (2026-09-03):** `TestDecimalBackendQualification`, `TestTodo_LIB_006_Conformance` (0 violations on the real tree) in `tools/policy/libfirewall`; PASS; branch plan-revision-2026-09-02; `go test -count=1 ./tools/policy/libfirewall/...` on windows/arm64 (Go 1.26.3).

- [x] `LIB-007` **[P0][TERRA] Qualify OpenTelemetry behind operations telemetry.**
  - **Depends:** `LIB-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestOTelBackendQualification`.
  - **TEST MATRIX:** `PRIMARY=TestOTelBackendQualification`; `GOLDEN=TestTodo_LIB_007_Golden`; `RACE=TestTodo_LIB_007_Race`; `INTEGRATION=TestTodo_LIB_007_Integration`; `SECURITY=TestTodo_LIB_007_Security`; `CONFORMANCE=TestTodo_LIB_007_Conformance`.
  - **RED:** `TestOTelBackendQualification` fails when domain packages import OTel, instrumentation changes a return/commit, exporter backpressure is unbounded, shutdown loses declared critical telemetry, or salary/medical/bank/case/tenant identifiers escape redaction/cardinality policy.
  - **GREEN:** pinned OpenTelemetry Go APIs/SDK are confined to `operations/telemetry` and transport/worker adapters; owned semantic metric definitions map to OTLP with bounded queues, context propagation, graceful flush and explicit degraded telemetry state.
  - **REFACTOR:** business evidence remains ledger-owned; replacing collector/exporter libraries does not alter business contracts.
  - **Refs:** [Observability](#33-cicd-release-admission-and-observability), [DLP](specs/data-classification-and-dlp.md).
  - **Evidence (2026-09-03):** `TestOTelBackendQualification`, `TestTodo_LIB_007_Integration` (0 direct imports) in `tools/policy/libfirewall`; PASS; branch plan-revision-2026-09-02; `go test -count=1 ./tools/policy/libfirewall/...` on windows/arm64 (Go 1.26.3).

- [x] `LIB-008` **[P0][TERRA] Qualify Goose as the SQL-first migration runner.**
  - **Depends:** `LIB-001`, `TOOL-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestGooseBackendQualification`.
  - **TEST MATRIX:** `PRIMARY=TestGooseBackendQualification`; `GOLDEN=TestTodo_LIB_008_Golden`; `RACE=TestTodo_LIB_008_Race`; `FAULT=TestTodo_LIB_008_Fault`; `CONFORMANCE=TestTodo_LIB_008_Conformance`.
  - **RED:** `TestGooseBackendQualification` fails on checksum drift, duplicate/concurrent application, dirty-state continuation, missing lock/journal evidence, nontransactional ambiguity or a Go migration lacking explicit need, idempotency and resumable failure contract.
  - **GREEN:** pinned `goose/v3` runs SQL migrations by default through `cmd/migrate`, records HCM Next schema-release evidence, supports zero-to-head and approved reversible/roll-forward paths, and allows Go migrations only for reviewed data transformations.
  - **REFACTOR:** HCM Next owns compatibility, fencing and evidence semantics; Goose supplies ordering/execution mechanics only.
  - **Refs:** [Database migration framework](specs/platform-foundation-gap-closure.md), [database materialization](#26-postgresql-database-and-model-materialization).
  - **Evidence (2026-09-03):** `TestGooseBackendQualification`, `TestTodo_LIB_008_Golden`, `TestTodo_LIB_008_Race`, `TestTodo_LIB_008_Integration`, `TestTodo_LIB_008_Fault`, `TestTodo_LIB_008_Conformance` in `tools/policy/libfirewall` (Goose is confined to `migrations`, `cmd`, `internal/data/pgtest` and `internal/data/schema`; unrelated data/domain imports reject; hand-authored Go migrations require explicit reviewed idempotency/recovery evidence; concurrent manifest reads are deterministic); `go test -count=1 ./tools/policy/libfirewall -run '^(TestGooseBackendQualification|TestTodo_LIB_008_)'` PASS on windows/arm64 (Go 1.26.3); `-race` execution remains on the supported Linux CI builder; supersedes the partial evidence above; refactored; branch plan-revision-2026-09-02.

- [x] `LIB-009` **[P0][TERRA] Qualify Testcontainers for Go as test-only environment mechanics.**
  - **Evidence (2026-09-05):** `TestTestcontainersQualification`, `TestTodo_LIB_009_{Property,Golden,Race,Integration,Conformance}` in `tools/policy/libqualification`: decision REJECT — no Docker on the ARM64 development host and `internal/data/pgtest` (TOOL-014) already provides ephemeral PostgreSQL; go.mod must never require github.com/testcontainers/\*; `go test -count=1 ./tools/policy/libqualification/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LIB-001`, `TOOL-014`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTestcontainersQualification`.
  - **TEST MATRIX:** `PRIMARY=TestTestcontainersQualification`; `PROPERTY=TestTodo_LIB_009_Property`; `GOLDEN=TestTodo_LIB_009_Golden`; `RACE=TestTodo_LIB_009_Race`; `INTEGRATION=TestTodo_LIB_009_Integration`; `CONFORMANCE=TestTodo_LIB_009_Conformance`.
  - **RED:** `TestTestcontainersQualification` fails when concurrent suites share database/object/provider state, cleanup can target non-test resources, readiness races, image tags float, failures destroy required evidence, or Testcontainers enters a production binary/module path.
  - **GREEN:** pinned Testcontainers modules provision digest-pinned PostgreSQL and approved S3/SMTP/provider fakes with per-run namespaces, deterministic readiness, bounded cleanup and explicit preserve-on-failure policy; release graph proves zero runtime dependency.
  - **REFACTOR:** cross-system fixtures use owned harness interfaces so a CI-native environment backend can replace Testcontainers.
  - **Refs:** [Environment governance](plan.md#111-environment-and-change-governance), [test placement](#2a-modular-go-monolith-and-semantic-package-architecture).
  - **Evidence (partial, 2026-09-03):** `TestTestcontainersQualification`, `TestTodo_LIB_009_Property`, `TestTodo_LIB_009_Golden`, `TestTodo_LIB_009_Race`, `TestTodo_LIB_009_Integration`, and `TestTodo_LIB_009_Conformance` in `tools/quality/testcontainerskit` plus `definitions/architecture/testcontainers-go-qualification.yaml` implement a side-effect-free pre-admission gate and prove zero Testcontainers dependency in the release graph. The decision remains `REJECT`: the module is absent and Docker is unavailable, so the required digest-pinned PostgreSQL/S3/SMTP/provider-fake execution evidence does not exist and this TODO remains open. `go test -count=1 ./tools/quality/testcontainerskit` and `go vet ./tools/quality/testcontainerskit` PASS on windows/arm64 (Go 1.26.3); `-race` requires supported Linux CI; branch plan-revision-2026-09-02.

- [x] `LIB-010` **[P0][SOL_HIGH] Qualify go-oidc and x/oauth2 behind federation adapters.**
  - **Evidence (2026-09-05):** `TestOIDCBackendQualification`, `TestTodo_LIB_010_{Golden,Integration,Security,Conformance}` in `tools/policy/libqualification` (decision DEFER: `internal/trust/federation` holds the adapter port behind which go-oidc/x/oauth2 may ever be imported; the test asserts no import of either module exists anywhere in the tree and go.mod carries neither); `go test -count=1 ./tools/policy/libqualification/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LIB-001`, `TOOL-013`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestOIDCBackendQualification`.
  - **TEST MATRIX:** `PRIMARY=TestOIDCBackendQualification`; `GOLDEN=TestTodo_LIB_010_Golden`; `INTEGRATION=TestTodo_LIB_010_Integration`; `SECURITY=TestTodo_LIB_010_Security`; `CONFORMANCE=TestTodo_LIB_010_Conformance`.
  - **RED:** `TestOIDCBackendQualification` rejects issuer/audience/algorithm mix-up, missing PKCE/state/nonce, token substitution, stale/failed key refresh, redirect abuse, clock-boundary errors and raw provider claims becoming authorization grants or stable principal identity.
  - **GREEN:** pinned `coreos/go-oidc` plus `golang.org/x/oauth2` validate configured profiles and emit only an owned normalized authentication result used to construct immutable `PrincipalContext`; key/config/provider versions remain evidenced.
  - **REFACTOR:** SAML and future federation adapters satisfy the same identity-normalization conformance contract without changing downstream AuthZ.
  - **Refs:** [Identity contract](plan.md#94-identity-and-permission-contract), [security models](data/models/security-trust.md).

- [x] `LIB-011` **[P0][SOL_HIGH] Gate any additional JOSE/JWK dependency by demonstrated need.**
  - **Evidence (2026-09-05):** `TestJOSEDependencyNeedAndConformance`, `TestTodo_LIB_011_{Golden,Conformance}` in `tools/policy/libqualification` (decision GATED: go.mod carries no JOSE/JWK module today; any addition must appear in the record's allowed set, a dependency-roles.yaml row and a todos.md decision, and may only be imported under the federation adapter roots); `go test -count=1 ./tools/policy/libqualification/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LIB-001`, `LIB-010`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestJOSEDependencyNeedAndConformance`.
  - **TEST MATRIX:** `PRIMARY=TestJOSEDependencyNeedAndConformance`; `GOLDEN=TestTodo_LIB_011_Golden`; `CONFORMANCE=TestTodo_LIB_011_Conformance`.
  - **RED:** `TestJOSEDependencyNeedAndConformance` fails if `lestrrat-go/jwx` or another JOSE module is added for behavior already safely supplied by Go/go-oidc, exposes library key/token types, accepts weak/confused algorithms, or lacks rotation/interoperability/adversarial vectors.
  - **GREEN:** architecture decision returns `NOT_REQUIRED` or approves one pinned backend for a named missing capability with narrow `authn/federation` or `trust/crypto` confinement and exact sign/verify/JWK-rotation conformance.
  - **REFACTOR:** minimize cryptographic surface and preserve Go standard-library primitives as the default.
  - **Refs:** [Crypto agility](data/models/security-trust.md), [secrets and key custody](specs/secrets-key-custody-and-credential-leases.md).

- [x] `LIB-012` **[P0][TERRA] Prefer Go standard-library logging, crypto, networking and testing mechanics.**
  - **Evidence (2026-09-05):** `TestStandardLibraryDefaultPolicy`, `TestTodo_LIB_012_{Golden,Security,Conformance}` in `tools/policy/libqualification` (scans every non-generated Go file for third-party logging, HTTP routers, crypto frameworks and assertion libraries; the tree is clean today on log/slog, crypto/\*, net/http and testing); `go test -count=1 ./tools/policy/libqualification/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LIB-001`, `TOOL-011`–`TOOL-013`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestStandardLibraryDefaultPolicy`.
  - **TEST MATRIX:** `PRIMARY=TestStandardLibraryDefaultPolicy`; `GOLDEN=TestTodo_LIB_012_Golden`; `RACE=TestTodo_LIB_012_Race`; `INTEGRATION=TestTodo_LIB_012_Integration`; `FAULT=TestTodo_LIB_012_Fault`; `SECURITY=TestTodo_LIB_012_Security`; `CONFORMANCE=TestTodo_LIB_012_Conformance`; `BENCHMARK=BenchmarkTodo_LIB_012`.
  - **RED:** `TestStandardLibraryDefaultPolicy` fails when a third-party logger/HTTP/router/crypto/assertion framework is introduced without a missing-capability benchmark and ownership decision, or when `slog` records bypass classification/redaction.
  - **GREEN:** `log/slog`, standard crypto/TLS/networking and Go test/race/fuzz tools satisfy declared needs through minimal owned configuration/adapters; any exception records measurable deficiency and passes `LIB-001` qualification.
  - **REFACTOR:** do not wrap every standard type; create boundaries only for policy, substitution, deterministic testing or semantic ownership.
  - **Refs:** [Go technology constitution](specs/go-only-technology-constitution.md), [architecture ceremony](#2a-modular-go-monolith-and-semantic-package-architecture).

- [x] `LIB-013` **[P0][SOL_HIGH] Enforce the prohibited semantic-framework and Phase 1 infrastructure list.**
  - **Depends:** `LIB-001`, `ARCH-GO-002`, `GOV-009`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestProhibitedFrameworkPolicy`.
  - **TEST MATRIX:** `PRIMARY=TestProhibitedFrameworkPolicy`; `PROPERTY=TestTodo_LIB_013_Property`; `GOLDEN=TestTodo_LIB_013_Golden`; `INTEGRATION=TestTodo_LIB_013_Integration`; `CONFORMANCE=TestTodo_LIB_013_Conformance`; `MUTATION=TestTodo_LIB_013_Mutation`.
  - **RED:** `TestProhibitedFrameworkPolicy` fails when GORM/Ent or another ORM controls authoritative transaction semantics; Temporal/Camunda becomes the persisted business workflow model; JavaScript/Lua/Python/Starlark executes customer rules; Kafka/full broker becomes mandatory for Phase 1 correctness; or provider SDK types enter domain contracts.
  - **GREEN:** authoritative SQL/locking/temporal/outbox behavior stays explicit; workflow/rules/integration semantics remain HCM Next-owned; PostgreSQL/outbox is sufficient for Phase 1; a future mechanical scheduler/broker/provider SDK requires a measured gate and adapter conformance without changing semantic contracts.
  - **REFACTOR:** policy prohibits semantic capture, not evidence-based use of a replaceable mechanic in a later phase.
  - **Refs:** [Workflow runtime](specs/workflow-runtime.md), [Phase 1 scope](execution-plan.md), [Go technology constitution](specs/go-only-technology-constitution.md).
  - **Evidence (2026-09-03):** `TestProhibitedFrameworkPolicy`, `TestTodo_LIB_013_Property`, `TestTodo_LIB_013_Golden`, `TestTodo_LIB_013_Integration`, `TestTodo_LIB_013_Conformance`, and `TestTodo_LIB_013_Mutation` in `tools/policy/prohibitedframework` over `definitions/architecture/prohibited-frameworks.yaml`; exact module-family matching rejects ORM/workflow-engine/customer-rule-runtime semantic capture, mandatory Phase 1 brokers, production JavaScript/Lua/Python/Starlark, provider SDKs outside narrow replaceable adapters, and provider types exposed from semantic contracts. The real package/runtime tree has zero LIB-013 violations. Exact tests, `go test -count=1 ./tools/policy/prohibitedframework`, and `go vet ./tools/policy/prohibitedframework` PASS on windows/arm64 (Go 1.26.3); refactored; branch plan-revision-2026-09-02.

- [x] `LIB-014` **[GATE_A][SOL_HIGH] Prove dependency upgrade, rollback and replacement safety.**
  - **Evidence (2026-09-05):** `TestInfrastructureDependencyReplacementMatrix`, `TestTodo_LIB_014_{Property,Golden,Conformance,Fault,Race,Mutation}` in `tools/policy/libqualification` (every go.mod require, direct or indirect, has its go.sum pair; `go mod verify` passes for every module except the known local agenthub replace target, which is filtered by name and documented; the rollback procedure is a golden text fixture; the record's go.sum key format and the agenthub filter were corrected during integration); `go test -count=1 ./tools/policy/libqualification/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LIB-002`–`LIB-013`, `TOOL-016`–`TOOL-019`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestInfrastructureDependencyReplacementMatrix`.
  - **TEST MATRIX:** `PRIMARY=TestInfrastructureDependencyReplacementMatrix`; `PROPERTY=TestTodo_LIB_014_Property`; `GOLDEN=TestTodo_LIB_014_Golden`; `FUZZ=FuzzTodo_LIB_014`; `RACE=TestTodo_LIB_014_Race`; `CONFORMANCE=TestTodo_LIB_014_Conformance`; `MUTATION=TestTodo_LIB_014_Mutation`; `FAULT=TestTodo_LIB_014_Fault`.
  - **RED:** `TestInfrastructureDependencyReplacementMatrix` catches a candidate update/removal that changes canonical bytes, error/status mapping, SQL/transaction semantics, expression results, identity validation, telemetry isolation, migration history or test oracle without a declared compatibility boundary.
  - **GREEN:** each qualified dependency has pinned old/current/candidate conformance, vulnerability response owner, rollback/remove procedure and at least one fake or alternate-backend contract proof for the owned boundary; release evidence records exact module graph digest.
  - **REFACTOR:** retire unused dependencies and exceptions; no dependency remains merely because it is already present.
  - **Refs:** [Supply-chain security](specs/platform-architecture-catalog.md), [rolling upgrade](#2-go-only-repository-toolchain-and-contract-generation), [risk register](specs/risk-register.md).

- [x] `LIB-015` **[GATE_A][LUNA] Generate and verify the README library-strategy section.**
  - **Evidence (2026-09-05):** `TestReadmeLibraryStrategyMatchesManifest` in `tools/gen/librarystrategy` (drift test proving the README library-strategy region equals a fresh generation from dependency-roles.yaml, with the LIB-005/009/010/011/012/014/019 and TOOL-022 qualification decisions generated into it; README regenerated during integration with the generator command; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/gen/librarystrategy/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LIB-001`, `LIB-013`, `DOC-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestReadmeLibraryStrategyMatchesManifest`.
  - **TEST MATRIX:** `PRIMARY=TestReadmeLibraryStrategyMatchesManifest`; `GOLDEN=TestTodo_LIB_015_Golden`; `INTEGRATION=TestTodo_LIB_015_Integration`; `CONFORMANCE=TestTodo_LIB_015_Conformance`; `BROWSER=TestTodo_LIB_015_Browser`.
  - **RED:** `TestReadmeLibraryStrategyMatchesManifest` fails when README omits semantic ownership, mislabels an infrastructure module as project core, lists an unapproved dependency, omits a prohibited semantic framework, implies Node/TypeScript production support or disagrees with the package/dependency manifests.
  - **GREEN:** README states that HCM Next owns intent/capability/governance/workflow/transaction/ledger/engine/domain semantics; Go plus GWC/grpcbridge/SchemaFlux are the declared core; approved third-party Go modules are replaceable mechanics; and the package/dependency diagram and candidate status are generated from current manifests.
  - **REFACTOR:** keep concise human guidance authored while generating volatile versions/status/tables to prevent documentation drift.
  - **Refs:** [Go technology constitution](specs/go-only-technology-constitution.md), [repository architecture](#2a-modular-go-monolith-and-semantic-package-architecture), [documentation governance](specs/specification-ownership-registry.md).

---

## 3. Canonical values, schemas, registries and model coverage

> **Disposition (2026-09-02):** P1A for canonical identifiers, money/time value types, digest references, the fourteen-definition registry (`MODEL-010`), lifecycle validation for the five dimensions (`MODEL-014`) and negative-state policy. `MODEL-008`, `MODEL-009` are RETIRED; `MODEL-016` binds the fourteen, not a count. Entity/property registries beyond the fourteen definitions' needs are DEFERRED.

- [x] `MODEL-001` **[P0][TERRA] Implement canonical entity and reference identifiers.**
  - **Evidence (partial, 2026-09-03):** `TestTodo_MODEL_001, FuzzTodo_MODEL_001` in `internal/kernel/values` PASS; not complete: Go canonical text/bytes proven; Protobuf round-trip against hcmnext.common.v1 not yet written (wave 2); `go test -count=1 ./internal/kernel/values/...`; on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TOOL-002`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MODEL_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MODEL_001`; `PROPERTY=TestTodo_MODEL_001_Property`; `GOLDEN=TestTodo_MODEL_001_Golden`; `FUZZ=FuzzTodo_MODEL_001`; `FAULT=TestTodo_MODEL_001_Fault`; `SECURITY=TestTodo_MODEL_001_Security`.
  - **RED:** canonical-vector tests reject empty/wrong-kind IDs, tenantless resource keys and ambiguous revision selectors.
  - **GREEN:** `EntityId`, `EntityRef`, `ResourceKey` and `RevisionToken` round-trip through Protobuf/Go without losing kind, tenant or revision intent.
  - **REFACTOR:** one shared value package; no domain-specific string aliases.
  - **Refs:** [Wire primitives](data/models/wire-contract-primitives.md), [modeling conventions](data/models/modeling-conventions.md).
  - **Evidence (2026-09-03):** proto round-trip clause closed: `TestTodo_MODEL_001`, `_Property`, `_Golden`, `_Fault`, `_Security`, `_Integration`, `FuzzTodo_MODEL_001` in `internal/intent/protomap` (kernel values <-> `hcmnext.common.v1`/`hcmnext.intents.v1`); `go test -count=1 ./internal/intent/protomap/...` PASS; supersedes the partial line above; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `MODEL-002` **[P0][TERRA] Implement explicit property presence semantics.**
  - **Evidence (2026-09-03):** `TestPresenceNeverCollapsesUnknown` in `internal/kernel/values`; `go test -count=1 ./internal/kernel/values/...` PASS on windows/arm64 (Go 1.26.3); `go vet`, `gofmt -l`, `staticcheck` clean; branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestPresenceNeverCollapsesUnknown`.
  - **TEST MATRIX:** `PRIMARY=TestPresenceNeverCollapsesUnknown`; `PROPERTY=TestTodo_MODEL_002_Property`; `GOLDEN=TestTodo_MODEL_002_Golden`; `FUZZ=FuzzTodo_MODEL_002`; `SECURITY=TestTodo_MODEL_002_Security`.
  - **RED:** `TestPresenceNeverCollapsesUnknown` proves `ABSENT`, `NULL`, `UNKNOWN`, `REDACTED`, `UNAVAILABLE` and `NOT_APPLICABLE` cannot decode as a zero value.
  - **GREEN:** every state round-trips distinctly and unauthorized serialization returns `REDACTED` without revealing the value.
  - **REFACTOR:** generated wrappers replace ad hoc pointer/zero-value conventions.
  - **Refs:** [Wire primitives](data/models/wire-contract-primitives.md), [negative-state contract](data/models/registry-and-coverage-contracts.md).

- [x] `MODEL-003` **[P0][SOL_LOW] Implement fixed-decimal arithmetic.**
  - **Evidence (2026-09-03):** `TestTodo_MODEL_003, FuzzTodo_MODEL_003` in `internal/kernel/values`; `go test -count=1 ./internal/kernel/values/...` PASS on windows/arm64 (Go 1.26.3); `go vet`, `gofmt -l`, `staticcheck` clean; branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-001`, `LIB-006`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MODEL_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MODEL_003`; `PROPERTY=TestTodo_MODEL_003_Property`; `GOLDEN=TestTodo_MODEL_003_Golden`; `FUZZ=FuzzTodo_MODEL_003`; `RACE=TestTodo_MODEL_003_Race`.
  - **RED:** vectors reject NaN, infinity, negative zero, excess scale, overflow and implicit currency conversion.
  - **GREEN:** HCM Next-owned Money, Percentage, Quantity and Rate return exact bytes/scale and declared rounding for positive, negative and midpoint cases through the qualified decimal backend.
  - **REFACTOR:** all financial domains consume one audited decimal kernel; no `apd` type or mutable context escapes it.
  - **Refs:** [Wire primitives](data/models/wire-contract-primitives.md), [compensation domain](specs/compensation-domain.md).

- [x] `MODEL-004` **[P0][SOL_LOW] Implement business-time primitives.**
  - **Evidence (2026-09-03):** `TestTodo_MODEL_004, FuzzTodo_MODEL_004` in `internal/kernel/values`; `go test -count=1 ./internal/kernel/values/...` PASS on windows/arm64 (Go 1.26.3); `go vet`, `gofmt -l`, `staticcheck` clean; branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MODEL_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MODEL_004`; `PROPERTY=TestTodo_MODEL_004_Property`; `GOLDEN=TestTodo_MODEL_004_Golden`; `FUZZ=FuzzTodo_MODEL_004`.
  - **RED:** vectors fail on invalid local dates, DST gaps/overlaps without resolution, inverted intervals and conflated date/instant values.
  - **GREEN:** Instant, LocalDate, LocalTime, ZonedDateTime, half-open EffectiveInterval, RecordedAt, KnownAt, PayPeriod and BusinessDay preserve exact semantics.
  - **REFACTOR:** domains may add constraints but cannot redefine interval boundaries.
  - **Refs:** [Effective-time principle](plan.md#55-effective-time-and-recorded-time-are-different), [wire time primitives](data/models/wire-contract-primitives.md).

- [x] `MODEL-005` **[P0][TERRA] Version timezone and business-calendar datasets.**
  - **Evidence (2026-09-03):** `TestTimerDatasetChangePolicy` in `internal/kernel/values`; `go test -count=1 ./internal/kernel/values/...` PASS on windows/arm64 (Go 1.26.3); `go vet`, `gofmt -l`, `staticcheck` clean; branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-004`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTimerDatasetChangePolicy`.
  - **TEST MATRIX:** `PRIMARY=TestTimerDatasetChangePolicy`; `PROPERTY=TestTodo_MODEL_005_Property`; `GOLDEN=TestTodo_MODEL_005_Golden`; `FUZZ=FuzzTodo_MODEL_005`; `RACE=TestTodo_MODEL_005_Race`; `RECOVERY=TestTodo_MODEL_005_Recovery`.
  - **RED:** `TestTimerDatasetChangePolicy` rejects a future timer lacking timezone/calendar version and update behavior.
  - **GREEN:** calculations replay with the historical dataset; update policies return `PIN`, `RECALCULATE`, or `REVIEW_REQUIRED` with evidence.
  - **REFACTOR:** dataset refresh never silently rewrites an existing deadline.
  - **Refs:** [Globalization principle](plan.md#511-globalization-is-a-business-dimension), [foundation gap closure](specs/platform-foundation-gap-closure.md).

- [x] `MODEL-006` **[P0][SOL_HIGH] Implement canonical envelope normalization.**
  - **Evidence (2026-09-03):** `TestTodo_MODEL_006, FuzzTodo_MODEL_006` in `internal/kernel/canonical`; `go test -count=1 ./internal/kernel/canonical/...` PASS on windows/arm64 (Go 1.26.3); `go vet`, `gofmt -l`, `staticcheck` clean; branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-002`–`MODEL-005`, `TOOL-004`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MODEL_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MODEL_006`; `PROPERTY=TestTodo_MODEL_006_Property`; `GOLDEN=TestTodo_MODEL_006_Golden`; `FUZZ=FuzzTodo_MODEL_006`.
  - **RED:** canonical vectors expose field-order, unknown-field, Unicode, map-order, decimal and time nondeterminism.
  - **GREEN:** all Go processes produce identical canonical bytes for identical material meaning and different bytes for every material change.
  - **REFACTOR:** callers submit values, never their own authoritative digest.
  - **Refs:** [Canonical envelope contract](specs/canonical-envelope-and-digest.md), [reproducibility](plan.md#93-reproducibility-contract).

- [x] `MODEL-007` **[P0][SOL_HIGH] Implement versioned digest profiles.**
  - **Evidence (2026-09-03):** `TestTodo_MODEL_007, FuzzTodo_MODEL_007` in `internal/kernel/digest`; `go test -count=1 ./internal/kernel/digest/...` PASS on windows/arm64 (Go 1.26.3); `go vet`, `gofmt -l`, `staticcheck` clean; branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-006`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MODEL_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MODEL_007`; `PROPERTY=TestTodo_MODEL_007_Property`; `GOLDEN=TestTodo_MODEL_007_Golden`; `FUZZ=FuzzTodo_MODEL_007`.
  - **RED:** tests reject unknown algorithms/profiles, omitted material paths, nullable digest and profile substitution.
  - **GREEN:** digest references persist algorithm, profile, schema and canonical payload digest and verify historical vectors after profile evolution.
  - **REFACTOR:** add dual-verification support without changing historical hashes.
  - **Refs:** [Canonical digest contract](specs/canonical-envelope-and-digest.md), [crypto agility](data/models/security-trust.md).

- [ ] `MODEL-008` **[RETIRED][TERRA] RETIRED 2026-09-02: Check in the exact 530-candidate source manifest.**
  - **Disposition:** RETIRED 2026-09-02. The intake name list is non-normative vocabulary; there is no source manifest to recover, count, or attest. Dependencies on this ID resolve as satisfied; its tests are not written.
  - **Depends:** `GOV-008`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestIntentSourceManifest`.
  - **TEST MATRIX:** `PRIMARY=TestIntentSourceManifest`; `PROPERTY=TestTodo_MODEL_008_Property`; `GOLDEN=TestTodo_MODEL_008_Golden`; `FUZZ=FuzzTodo_MODEL_008`.
  - **RED:** `TestIntentSourceManifest` fails while count/provenance/digest cannot independently prove the supplied numbered catalog.
  - **GREEN:** immutable source contains exactly numbers 1–530 once, names/descriptions losslessly, provenance and canonical digest.
  - **REFACTOR:** retain the original intake artifact alongside normalized partitions.
  - **Refs:** [Business intent catalog status](specs/business-intent-catalog.md#catalog-identity), [model coverage status](data/models/README.md#coverage-rule).

- [ ] `MODEL-009` **[RETIRED][TERRA] RETIRED 2026-09-02: Generate all intent ownership partitions.**
  - **Disposition:** RETIRED 2026-09-02. There are no catalog partitions. A domain starts its own definition file when it contracts its first definition. Dependencies on this ID resolve as satisfied; its tests are not written.
  - **Depends:** `MODEL-008`, `TOOL-004`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MODEL_009`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MODEL_009`; `PROPERTY=TestTodo_MODEL_009_Property`; `GOLDEN=TestTodo_MODEL_009_Golden`; `FUZZ=FuzzTodo_MODEL_009`.
  - **RED:** completeness test reports every missing/duplicate/out-of-range number, unqualified ID and ownerless entry.
  - **GREEN:** generated baseline partitions contain exactly the original 530 entries, including distinct access/commercial `GrantEntitlement` identities; separately versioned extension partitions contain reviewed additions, and the accepted-catalog union has no duplicate semantic ID or number collision.
  - **REFACTOR:** catalog numbers and IDs never change when display labels change.
  - **Refs:** [Catalog coverage partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [intent coverage matrix](data/models/intent-coverage-matrix.md).

- [x] `MODEL-010` **[P0][SOL_LOW] Implement the intent-definition registry for the fourteen drafted definitions.**
  - **Depends:** `TOOL-006`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.PEOPLE,BI.REWARDS,BI.WORK,BI.INTELLIGENCE,BI.OPERATIONS; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MODEL_010`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MODEL_010`; `PROPERTY=TestTodo_MODEL_010_Property`; `GOLDEN=TestTodo_MODEL_010_Golden`; `FUZZ=FuzzTodo_MODEL_010`; `RACE=TestTodo_MODEL_010_Race`.
  - **RED:** registry rejects a missing required `DRAFT_CONTRACT` field, an unknown schema/capability, a family outside `CHANGE_REQUEST|CALCULATION_REQUEST|ANALYTICAL_REQUEST`, an invalid family/effect pairing, `population_scope` on a non-`CHANGE_REQUEST`, and material version reuse.
  - **GREEN:** exactly the fourteen checked-in definitions resolve by `(intent_type_id, version)` with their family, side-effect profile and release (`P1A`, `P1B`, conformance); nothing resolves at any maturity below `DRAFT_CONTRACT`; the registry is a compiled-in Go table under the `BOOTSTRAP` profile.
  - **REFACTOR:** name lookup is presentation-only; runtime uses `definition_ref`; adding a definition is a source change, not a count.
  - **Refs:** [Required intent fields](specs/business-intent-catalog.md#required-definition-fields), [draft slice](specs/business-intent-catalog.md#initial-draft-contract-slice), [registry bootstrap profile](specs/capability-registry-and-lifecycle.md#bootstrap-profile).
  - **Evidence (2026-09-03):** `TestTodo_MODEL_010`, `_Property`, `_Golden`, `_Race`, `FuzzTodo_MODEL_010` in `internal/intent` (compiled-in registry of the fourteen definitions in `internal/intent/definitions`); `go test -count=1 ./internal/intent/...` PASS on windows/arm64 (Go 1.26.3); `go run ./tools/quality` PASSED; branch plan-revision-2026-09-02.

- [x] `MODEL-011` **[P0][TERRA] Implement entity/property definition registries.**
  - **Depends:** `MODEL-001`, `TOOL-004`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MODEL_011`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MODEL_011`; `PROPERTY=TestTodo_MODEL_011_Property`; `GOLDEN=TestTodo_MODEL_011_Golden`; `FUZZ=FuzzTodo_MODEL_011`; `SECURITY=TestTodo_MODEL_011_Security`.
  - **RED:** publication rejects properties without concrete type, presence, authority, temporal, classification, correction and retention semantics.
  - **GREEN:** every registered property resolves owner aggregate, schema path and applicable policies.
  - **REFACTOR:** generate docs/dependency indexes from the registry.
  - **Refs:** [Registry entities](data/models/registry-and-coverage-contracts.md), [business model catalog](data/models/README.md).
  - **Evidence (2026-09-03):** `TestTodo_MODEL_011`, `_Property`, `_Golden`, `_Security`, `FuzzTodo_MODEL_011` (23 entities: the fourteen named plus nine supporting; property paths grounded in `definitions.Bindings()`) in `internal/intent/model`; `go test -count=1 ./internal/intent/model/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `MODEL-012` **[P0][SOL_LOW] Implement aggregate ownership registration.**
  - **Depends:** `MODEL-011`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MODEL_012`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MODEL_012`; `PROPERTY=TestTodo_MODEL_012_Property`; `GOLDEN=TestTodo_MODEL_012_Golden`; `FUZZ=FuzzTodo_MODEL_012`; `RECOVERY=TestTodo_MODEL_012_Recovery`; `RACE=TestTodo_MODEL_012_Race`.
  - **RED:** tests reject unassigned roots, child mutation without owner authority and cross-root atomic assumptions outside a declared consistency boundary.
  - **GREEN:** every root resolves stream, lifecycle, invariants and commit boundary; rebuildable summaries return `NO_BUSINESS_LIFECYCLE`.
  - **REFACTOR:** root promotion requires explicit registry/version migration.
  - **Refs:** [Aggregate registry](data/models/registry-and-coverage-contracts.md#initial-aggregate-registry), [lifecycle assignments](data/models/registry-and-coverage-contracts.md#aggregate-lifecycle-assignment-registry).
  - **Evidence (2026-09-03):** `TestTodo_MODEL_012` and matrix variants (aggregate ownership registration) in `internal/intent/model`; `go test -count=1 ./internal/intent/model/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `MODEL-013` **[P0][SOL_LOW] Implement relationship definitions and temporal constraints.**
  - **Depends:** `MODEL-004`, `MODEL-011`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MODEL_013`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MODEL_013`; `PROPERTY=TestTodo_MODEL_013_Property`; `GOLDEN=TestTodo_MODEL_013_Golden`; `FUZZ=FuzzTodo_MODEL_013`; `SECURITY=TestTodo_MODEL_013_Security`.
  - **RED:** tests reject missing endpoint kinds, cycles where prohibited, overlapping exclusive edges and cross-tenant endpoints.
  - **GREEN:** manager/org/person/employment/assignment edges evaluate as-of and known-at with provenance.
  - **REFACTOR:** graph projections remain rebuildable from relationship facts.
  - **Refs:** [Relationship map](data/models/relationship-map.md), [organization domain](specs/organization-and-relationship-domain.md).
  - **Evidence (2026-09-03):** `TestTodo_MODEL_013` and matrix variants (relationship definitions with bitemporal constraints via kernel `EffectiveInterval`/`RecordedAt`/`KnownAt`) in `internal/intent/model`; `go test -count=1 ./internal/intent/model/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `MODEL-014` **[P0][SOL_LOW] Implement lifecycle definition validation.**
  - **Depends:** `MODEL-012`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MODEL_014`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MODEL_014`; `PROPERTY=TestTodo_MODEL_014_Property`; `GOLDEN=TestTodo_MODEL_014_Golden`; `FUZZ=FuzzTodo_MODEL_014`.
  - **RED:** checker rejects undeclared transition, unreachable state, in-place historical mutation and transition bypassing governance/retention.
  - **GREEN:** every authoritative transition appends `LifecycleTransitionRecord` and lifecycle assignment resolves canonically.
  - **REFACTOR:** domain profiles may narrow but never bypass shared restrictions.
  - **Refs:** [Lifecycle profiles](data/models/registry-and-coverage-contracts.md#initial-lifecycle-profiles).
  - **Evidence (2026-09-03):** `TestTodo_MODEL_014`, `_Property`, `_Golden`, `FuzzTodo_MODEL_014` in `internal/intent/lifecycle` (five dimensions, profile validation, append-only history); `go test -count=1 ./internal/intent/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `MODEL-015` **[P0][SOL_LOW] Implement negative-state policy registration.**
  - **Depends:** `MODEL-010`, `MODEL-011`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MODEL_015`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MODEL_015`; `PROPERTY=TestTodo_MODEL_015_Property`; `GOLDEN=TestTodo_MODEL_015_Golden`; `FUZZ=FuzzTodo_MODEL_015`; `FAULT=TestTodo_MODEL_015_Fault`.
  - **RED:** definition compilation rejects an applicable `UNKNOWN/PARTIAL/DEGRADED/AMBIGUOUS/REDACTED/UNAVAILABLE/STALE` state without policy.
  - **GREEN:** policy yields exactly `BLOCK`, `ROUTE_HUMAN`, `ALLOW_WITH_WARNING`, `USE_STALE`, `CREATE_OBLIGATION`, `CREATE_REPAIR`, `DEGRADE`, or `PERMIT_CLOSURE`, plus evidence/expiry/revalidation.
  - **REFACTOR:** common policies are referenced, not copied.
  - **Refs:** [Negative-state policy](data/models/registry-and-coverage-contracts.md).
  - **Evidence (2026-09-03):** `TestTodo_MODEL_015`, `_Property`, `_Golden`, `_Fault`, `FuzzTodo_MODEL_015` in `internal/intent` (negative-state policy registration, three shared policies in `definitions/policies.go`); `go test -count=1 ./internal/intent/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `MODEL-016` **[P0][SOL_HIGH] Bind each of the fourteen definitions to exact model behavior.**
  - **Depends:** `MODEL-010`–`MODEL-015`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.PEOPLE,BI.REWARDS,BI.WORK,BI.INTELLIGENCE,BI.OPERATIONS; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MODEL_016`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MODEL_016`; `PROPERTY=TestTodo_MODEL_016_Property`; `GOLDEN=TestTodo_MODEL_016_Golden`; `FUZZ=FuzzTodo_MODEL_016`; `RACE=TestTodo_MODEL_016_Race`; `MUTATION=TestTodo_MODEL_016_Mutation`.
  - **RED:** coverage checker reports a definition missing subject/root, property read/write, decision, evidence, effect, lifecycle transition on the five dimensions, negative policy or scenario.
  - **GREEN:** all fourteen definitions bind to the entities the data-model README names as the covered set; the report is `14/14 BOUND` or lists the exact gaps; no denominator other than the checked-in definition count exists.
  - **REFACTOR:** bindings reference stable IDs and schema paths, never prose names.
  - **Refs:** [Covered entity set](data/models/README.md), [five dimensions](specs/business-intent-and-change-request.md#five-lifecycle-dimensions).
  - **Evidence (2026-09-03):** `TestTodo_MODEL_016`, `_Property`, `_Golden`, `_Race`, `_Mutation`, `FuzzTodo_MODEL_016` in `internal/intent` (binding coverage report `14/14 BOUND`, `internal/intent/definitions/bindings.go`); `go test -count=1 ./internal/intent/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `MODEL-017` **[P0][TERRA] Implement schema release lifecycle.**
  - **Depends:** `TOOL-006`, `MODEL-011`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MODEL_017`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MODEL_017`; `PROPERTY=TestTodo_MODEL_017_Property`; `GOLDEN=TestTodo_MODEL_017_Golden`; `FUZZ=FuzzTodo_MODEL_017`; `RACE=TestTodo_MODEL_017_Race`; `FAULT=TestTodo_MODEL_017_Fault`.
  - **RED:** release rejects unresolved consumers, incompatible change, missing migration or publication without approval.
  - **GREEN:** draft -> validated -> approved -> active -> superseded/retired retains consumer adoption and rollback evidence.
  - **REFACTOR:** external schema snapshots remain observations, not canonical internal releases.
  - **Refs:** [DataOps configuration models](data/models/dataops-configuration.md), [schema registry boundary](specs/platform-responsibility-boundaries.md).
  - **Evidence (2026-09-03):** `TestTodo_MODEL_017` and matrix variants (schema release lifecycle DRAFT/RELEASED/DEPRECATED/RETIRED on `lifecycle.Profile`, digests) in `internal/intent/model`; `go test -count=1 ./internal/intent/model/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `MODEL-018` **[GATE_A][TERRA] Implement governed reference-data releases.**
  - **Depends:** `MODEL-017`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MODEL_018`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MODEL_018`; `PROPERTY=TestTodo_MODEL_018_Property`; `GOLDEN=TestTodo_MODEL_018_Golden`; `FUZZ=FuzzTodo_MODEL_018`; `RACE=TestTodo_MODEL_018_Race`.
  - **RED:** simulation rejects unknown/retired job, position, location, currency, reason code or unversioned global dataset.
  - **GREEN:** pilot references resolve by effective date and return release/provenance IDs.
  - **REFACTOR:** capacity-bearing Position remains outside reference data.
  - **Refs:** [DataOps models](data/models/dataops-configuration.md), [position contract](specs/position-and-headcount-domain.md).
  - **Evidence (2026-09-03):** `TestTodo_MODEL_018`, `_Property`, `_Golden`, `FuzzTodo_MODEL_018`, `_Race` in `internal/intent/model` (`refdata.go`: dataset kinds, DRAFT→VALIDATED→APPROVED→RELEASED→DEPRECATED release profile on the schemarelease pattern, `ResolveReference` as-of with provenance); `go test -count=1 ./internal/intent/model/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `MODEL-019` **[GATE_A][TERRA] Implement external-code crosswalks.**
  - **Depends:** `MODEL-018`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MODEL_019`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MODEL_019`; `PROPERTY=TestTodo_MODEL_019_Property`; `GOLDEN=TestTodo_MODEL_019_Golden`; `FUZZ=FuzzTodo_MODEL_019`; `INTEGRATION=TestTodo_MODEL_019_Integration`; `FAULT=TestTodo_MODEL_019_Fault`.
  - **RED:** tests return `UNKNOWN`, `AMBIGUOUS` or `OUT_OF_EFFECTIVE_RANGE` instead of guessing a canonical value.
  - **GREEN:** exact external system/object/code/effective interval maps to one canonical resource with mapping version.
  - **REFACTOR:** corrections append mapping revisions and trigger impact analysis.
  - **Refs:** [Integration mapping](specs/integration-platform.md), [HRIS DataOps](specs/hris-admin-dataops.md).
  - **Evidence (2026-09-03):** `TestTodo_MODEL_019`, `_Property`, `_Golden`, `FuzzTodo_MODEL_019`, `_Integration`, `_Fault` in `internal/intent/model` (`crosswalk.go`: versioned effective-dated mappings with precedence, RESOLVED/UNKNOWN/AMBIGUOUS/OUT_OF_EFFECTIVE_RANGE, reverse lookup, append-only revision with impact analysis); `go test -count=1 ./internal/intent/model/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `MODEL-020` **[GATE_A][SOL_LOW] Implement provenance graph edges.**
  - **Depends:** `MODEL-007`, `MODEL-011`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestMaterialValueRequiresLineage`.
  - **TEST MATRIX:** `PRIMARY=TestMaterialValueRequiresLineage`; `PROPERTY=TestTodo_MODEL_020_Property`; `GOLDEN=TestTodo_MODEL_020_Golden`; `FUZZ=FuzzTodo_MODEL_020`; `SECURITY=TestTodo_MODEL_020_Security`.
  - **RED:** `TestMaterialValueRequiresLineage` rejects value/result without source, transformation, authority, recorded/effective time and digest.
  - **GREEN:** field explanation traverses source -> mapping -> observation -> proposal/decision/result without disclosing unauthorized nodes.
  - **REFACTOR:** edge storage may change; semantic lineage IDs remain stable.
  - **Refs:** [Provenance contract](specs/provenance-graph-and-lineage.md), [epistemic integrity](plan.md#513-preserve-epistemic-integrity).
  - **Evidence (2026-09-03):** `TestMaterialValueRequiresLineage`, `TestTodo_MODEL_020_Property`, `TestTodo_MODEL_020_Golden`, `TestTodo_MODEL_020_Security`, `FuzzTodo_MODEL_020` (provenance graph edges; `RequireLineage`) in `internal/intent/model`; `go test -count=1 ./internal/intent/model/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `MODEL-021` **[GATE_A][SOL_LOW] Implement source-authority assignments.**
  - **Depends:** `MODEL-011`, `MODEL-020`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MODEL_021`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MODEL_021`; `PROPERTY=TestTodo_MODEL_021_Property`; `GOLDEN=TestTodo_MODEL_021_Golden`; `FUZZ=FuzzTodo_MODEL_021`.
  - **RED:** authority resolution blocks no-owner, overlapping-exclusive owner, stale source and writer outside effective scope.
  - **GREEN:** decision returns authoritative system, field/domain scope, effective interval, freshness, merge/conflict policy and evidence.
  - **REFACTOR:** external observation authority never silently promotes to domain authority.
  - **Refs:** [Source authority](specs/source-authority-and-external-mastering.md), [authority contract](plan.md#91-authority-contract).
  - **Evidence (2026-09-03):** `TestTodo_MODEL_021` and matrix variants (source-authority assignments per property; `ResolveAuthority`) in `internal/intent/model`; `go test -count=1 ./internal/intent/model/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `MODEL-022` **[GATE_A][SOL_HIGH] Implement exact identity linkage.**
  - **Depends:** `MODEL-013`, `MODEL-020`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MODEL_022`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MODEL_022`; `PROPERTY=TestTodo_MODEL_022_Property`; `GOLDEN=TestTodo_MODEL_022_Golden`; `FUZZ=FuzzTodo_MODEL_022`; `FAULT=TestTodo_MODEL_022_Fault`; `SECURITY=TestTodo_MODEL_022_Security`.
  - **RED:** lookup returns `AMBIGUOUS` for collision, refuses false auto-merge and prevents merged redirect from crossing tenant/purpose policy.
  - **GREEN:** approved external identity resolves to one Person/role with confidence/evidence; merge and separation preserve lineage.
  - **REFACTOR:** Candidate, Worker and former employment remain concurrent roles/relationships.
  - **Refs:** [Identity resolution](specs/identity-resolution-and-entity-linkage.md), [Person model](data/models/people-workforce.md).
  - **Evidence (2026-09-03):** `TestTodo_MODEL_022`, `_Property`, `_Golden`, `FuzzTodo_MODEL_022`, `_Fault`, `_Security` in `internal/intent/model` (`identity.go`: links validated to confidence exactly 1.0 — no fuzzy matching, AMBIGUOUS on collision, tenant/purpose policy before ambiguity is revealed, do-not-merge constraints, append-only merge/separate with redirects and provenance); `go test -count=1 ./internal/intent/model/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `MODEL-023` **[GATE_A][SOL_LOW] Implement data classification propagation.**
  - **Depends:** `MODEL-011`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MODEL_023`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MODEL_023`; `PROPERTY=TestTodo_MODEL_023_Property`; `GOLDEN=TestTodo_MODEL_023_Golden`; `FUZZ=FuzzTodo_MODEL_023`; `RACE=TestTodo_MODEL_023_Race`; `SECURITY=TestTodo_MODEL_023_Security`.
  - **RED:** derived value, artifact, log or outbound payload missing the strongest applicable label fails creation/delivery.
  - **GREEN:** PII, compensation, bank, medical, immigration, case and special-category labels propagate with policy version.
  - **REFACTOR:** classification IDs are stable and enforcement is repository/serializer independent.
  - **Refs:** [Classification/DLP](specs/data-classification-and-dlp.md), [security models](data/models/security-trust.md).
  - **Evidence (2026-09-03):** `TestTodo_MODEL_023` and matrix variants (classification propagation through relationships and derived fields) in `internal/intent/model`; `go test -count=1 ./internal/intent/model/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `MODEL-024` **[GATE_A][SOL_LOW] Implement data-quality evaluation envelopes.**
  - **Depends:** `MODEL-011`, `MODEL-015`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MODEL_024`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MODEL_024`; `PROPERTY=TestTodo_MODEL_024_Property`; `GOLDEN=TestTodo_MODEL_024_Golden`; `FUZZ=FuzzTodo_MODEL_024`.
  - **RED:** malformed, impossible, stale or incomplete pilot facts cannot be reported as `PASS`; unknown evidence remains `UNKNOWN`.
  - **GREEN:** evaluator returns status, findings, severity, affected paths, source watermark and remediation owner.
  - **REFACTOR:** validation, quality, invariant and reconciliation remain distinct evaluator families.
  - **Refs:** [Quality/invariant spec](specs/data-quality-and-invariant-evaluation.md), [model catalog](data/models/assurance-intelligence-platform.md).
  - **Evidence (2026-09-03):** `TestTodo_MODEL_024`, `_Property`, `_Golden`, `FuzzTodo_MODEL_024` in `internal/intent/model` (`quality.go`: PRESENCE/FORMAT/RANGE/REFERENTIAL/CROSS_FIELD rules, UNKNOWN never coerced to PASS, order-independent byte-stable composition); `go test -count=1 ./internal/intent/model/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `MODEL-025` **[GATE_B][SOL_HIGH] Implement transaction invariants.**
  - **Evidence (2026-09-05):** `TestTodo_MODEL_025` in `internal/domains/promotion/localcommit` (the transaction invariants over a committed promotion plan are a conformance suite with evaluation evidence (migration 00064); written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/promotion/localcommit/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-024`, `PEOPLE-004`, `ORG-003`, `POSITION-003`, `COMP-004`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MODEL_025`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MODEL_025`; `PROPERTY=TestTodo_MODEL_025_Property`; `GOLDEN=TestTodo_MODEL_025_Golden`; `FUZZ=FuzzTodo_MODEL_025`; `SECURITY=TestTodo_MODEL_025_Security`; `BENCHMARK=BenchmarkTodo_MODEL_025`; `MUTATION=TestTodo_MODEL_025_Mutation`.
  - **RED:** manager cycle, invalid employment/assignment overlap, position over-capacity, compensation currency mismatch and cross-tenant reference abort before commit.
  - **GREEN:** valid transaction records passed invariant versions; failures return typed findings with no authoritative append.
  - **REFACTOR:** pure invariant evaluation remains replayable.
  - **Refs:** [Quality/invariant spec](specs/data-quality-and-invariant-evaluation.md), [transaction plan](specs/transaction-plan-and-commit-coordinator.md).

- [x] `MODEL-026` **[GATE_A][SOL_LOW] Implement records declarations and retention assignment.**
  - **Depends:** `MODEL-023`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MODEL_026`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MODEL_026`; `PROPERTY=TestTodo_MODEL_026_Property`; `GOLDEN=TestTodo_MODEL_026_Golden`; `FUZZ=FuzzTodo_MODEL_026`.
  - **RED:** material evidence/artifact without record class, retention schedule, authority and disposition owner fails persistence.
  - **GREEN:** declaration derives an explainable effective retention schedule and next action date from the composed `RETENTION` obligations of the pinned rule-pack releases, applying the longest-minimum rule per record class; a record class with no applicable obligation is `RETENTION_UNKNOWN`, never a platform default.
  - **REFACTOR:** ledger stores minimal durable facts and destructible payload references.
  - **Refs:** [Records management](specs/records-management-and-disposition.md), [privacy contract](plan.md#95-privacy-contract), [Per-kind composition](specs/legal-rule-packs-and-state-configuration.md#63-per-kind-composition).
  - **Evidence (2026-09-03):** `TestTodo_MODEL_026` and matrix variants (records declarations, `RetentionClass.EffectiveRetention` with jurisdiction-keyed overrides — consumed by the artifacts sweep) in `internal/intent/model`; `go test -count=1 ./internal/intent/model/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `MODEL-027` **[GATE_B][SOL_HIGH] Implement legal holds.**
  - **Depends:** `MODEL-026`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MODEL_027`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MODEL_027`; `PROPERTY=TestTodo_MODEL_027_Property`; `GOLDEN=TestTodo_MODEL_027_Golden`; `FUZZ=FuzzTodo_MODEL_027`; `RACE=TestTodo_MODEL_027_Race`; `SECURITY=TestTodo_MODEL_027_Security`; `MUTATION=TestTodo_MODEL_027_Mutation`.
  - **RED:** disposition/destruction of held record or dependent artifact returns `HOLD_BLOCKED` and appends evidence.
  - **GREEN:** scoped hold freezes applicable disposition while unrelated records continue normally; release recalculates schedule.
  - **REFACTOR:** hold queries are authorization- and compartment-aware.
  - **Refs:** [Records management](specs/records-management-and-disposition.md), [privacy model](data/models/assurance-intelligence-platform.md).
  - **Evidence (2026-09-03):** `TestTodo_MODEL_027`, Declared `MODEL-027` test matrix and acceptance behavior in `internal/governance/legalhold`; PASS; `go test -p 1 ./internal/governance/legalhold` and `go vet ./internal/governance/legalhold` on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [ ] `MODEL-028` **[GATE_C][SOL_HIGH] Implement verified deletion and backup re-delete.**
  - **Depends:** `MODEL-026`, `RECOVERY-001`, `RECOVERY-002`, `RECORDS-COPY-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MODEL_028`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MODEL_028`; `PROPERTY=TestTodo_MODEL_028_Property`; `GOLDEN=TestTodo_MODEL_028_Golden`; `FUZZ=FuzzTodo_MODEL_028`; `FAULT=TestTodo_MODEL_028_Fault`; `RECOVERY=TestTodo_MODEL_028_Recovery`.
  - **RED:** deletion is incomplete while canonical, derived, external, backup or restored-copy inventory has an unhandled eligible copy.
  - **GREEN:** completion returns destruction/anonymization/retention outcomes per copy, exceptions/holds and a signed certificate; restored backups reapply tombstones before service.
  - **REFACTOR:** sensitive cross-tenant deduplication is prohibited or tenant-keyed so one tenant cannot preserve/delete another's bytes.
  - **Refs:** [Records management](specs/records-management-and-disposition.md), [foundation gap closure](specs/platform-foundation-gap-closure.md).

- [x] `MODEL-029` **[GATE_A][SOL_LOW] Implement content-addressed artifact storage.**
  - **Depends:** `MODEL-007`, `MODEL-023`, `MODEL-026`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MODEL_029`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MODEL_029`; `PROPERTY=TestTodo_MODEL_029_Property`; `GOLDEN=TestTodo_MODEL_029_Golden`; `FUZZ=FuzzTodo_MODEL_029`; `SECURITY=TestTodo_MODEL_029_Security`.
  - **RED:** storage rejects digest mismatch, tenant/classification conflict, mutable overwrite and reference without retention/authority metadata.
  - **GREEN:** immutable bytes retrieve by authorized reference and verify digest; corrections create successor artifacts.
  - **REFACTOR:** provider/backend may change without changing artifact identity.
  - **Refs:** [Canonical history](plan.md#515-one-canonical-history-many-rebuildable-read-planes), [document models](data/models/connectivity-access-content.md).
  - **Evidence (2026-09-03):** `TestTodo_MODEL_029`, `_Property`, `_Golden`, `_Security`, `FuzzTodo_MODEL_029` in `internal/data/artifacts` (content-addressed `Put`/`PutStream` with sha256 while streaming, size cap, idempotent by content id, identity drift refused; companion schema `<core>_artifact_store` from migration 00010 with RLS and forbid_mutation triggers); `go test -count=1 ./internal/data/artifacts/...` PASS via embedded-postgres on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `MODEL-030` **[P0][TERRA] Generate the model coverage report.**
  - **Depends:** `MODEL-016`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MODEL_030`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MODEL_030`; `PROPERTY=TestTodo_MODEL_030_Property`; `GOLDEN=TestTodo_MODEL_030_Golden`; `FUZZ=FuzzTodo_MODEL_030`; `CONFORMANCE=TestTodo_MODEL_030_Conformance`.
  - **RED:** report refuses `VERIFIED` while any exact binding/check is absent and identifies the first unresolved catalog number/path.
  - **GREEN:** result includes counts by domain, maturity, phase, negative policy and conformance coverage with source digest.
  - **REFACTOR:** Markdown dashboard is generated from machine evidence.
  - **Refs:** [Registry status](data/models/registry-and-coverage-contracts.md#minimum-conformance-proof-per-intent), [model README](data/models/README.md).
  - **Evidence (2026-09-03):** `TestTodo_MODEL_030`, `_Property` (model coverage report; `Registry.Digest()` pure over sorted slices, byte-identical across runs) in `internal/intent/model`; `go test -count=1 ./internal/intent/model/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

---

## 4. Identity, authorization, privacy and trust

> **Disposition (2026-09-02):** P1A for server-derived `PrincipalContext`, tenant isolation, field-level AuthZ and no-secrets-in-logs. Step-up and session revocation for approve/execute are P1B. Workload identity on east-west paths, DLP egress gateways, and classification propagation beyond pilot field masks are Gate C.

- [x] `TRUST-001` **[GATE_A][SOL_HIGH] Implement authenticated PrincipalContext creation.**
  - **Depends:** `TOOL-007`, `MODEL-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_001`; `FUZZ=FuzzTodo_TRUST_001`; `SECURITY=TestTodo_TRUST_001_Security`; `MUTATION=TestTodo_TRUST_001_Mutation`.
  - **RED:** tests reject caller-selected principal/tenant/actor, wrong audience/issuer, expired token and missing assurance evidence.
  - **GREEN:** verified human/service/agent/integration identity yields immutable principal, tenant, authentication method, assurance, session and delegation references.
  - **REFACTOR:** downstream services consume context, never raw identity-provider claims.
  - **Refs:** [Identity contract](plan.md#94-identity-and-permission-contract), [organization AuthZ](specs/organization-scope-and-authz.md).
  - **Evidence (2026-09-03):** `TestTodo_TRUST_001`, `TestTodo_TRUST_001_Security`, `TestTodo_TRUST_001_Mutation`, `FuzzTodo_TRUST_001` in `internal/trust`; `go test -count=1 ./internal/trust/...` PASS on windows/arm64 (Go 1.26.3); PrincipalContext is created only from a server-side `Verifier` (HMAC dev token), caller metadata such as x-principal/x-tenant/x-roles can never select trusted context; branch plan-revision-2026-09-02.

- [x] `TRUST-002` **[GATE_A][SOL_HIGH] Implement enterprise federation validation.**
  - **Depends:** `TRUST-001`, `LIB-010`, `LIB-011`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_002`; `FUZZ=FuzzTodo_TRUST_002`; `INTEGRATION=TestTodo_TRUST_002_Integration`; `SECURITY=TestTodo_TRUST_002_Security`; `MUTATION=TestTodo_TRUST_002_Mutation`.
  - **RED:** OAuth/OIDC/SAML fixtures reject open redirect, missing PKCE/state/nonce, issuer mix-up, token substitution and stale signing metadata.
  - **GREEN:** configured federation adapters return one HCM Next-owned normalized authentication result and linked platform principal, recording provider/config/key/library versions without leaking raw claims into AuthZ.
  - **REFACTOR:** provider adapters cannot create authorization grants.
  - **Refs:** [Platform foundation identity lifecycle](specs/platform-foundation-gap-closure.md), [security models](data/models/security-trust.md).
  - **Evidence (2026-09-03):** `TestTodo_TRUST_002`, `FuzzTodo_TRUST_002`, `_Integration`, `_Security`, `_Mutation` in `internal/trust/federation` (native JWS RS256/ES256/EdDSA verification, per-tenant issuer allow-list, audience, exp/nbf with injected clock skew, required claims, tenant binding, `KeySource` port; scoped to assertion validation — the OAuth authorization-code/PKCE flow awaits LIB-010); `go test -count=1 ./internal/trust/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `TRUST-003` **[GATE_A][SOL_HIGH] Implement session lifecycle and refresh replay detection.**
  - **Depends:** `TRUST-002`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_003`; `FUZZ=FuzzTodo_TRUST_003`; `SECURITY=TestTodo_TRUST_003_Security`; `RECOVERY=TestTodo_TRUST_003_Recovery`; `MUTATION=TestTodo_TRUST_003_Mutation`.
  - **RED:** expired/revoked session, replayed refresh credential, assurance downgrade and tenant switch are denied and audited.
  - **GREEN:** create/rotate/expire/revoke transitions are durable; one-time refresh replay revokes the affected chain.
  - **REFACTOR:** session storage exposes no reusable bearer secret in logs/evidence.
  - **Refs:** [Platform IAM boundary](plan.md#94-identity-and-permission-contract), [foundation gap closure](specs/platform-foundation-gap-closure.md).
  - **Evidence (2026-09-03):** `TestTodo_TRUST_003`, `FuzzTodo_TRUST_003`, `_Security`, `_Recovery`, `_Mutation` in `internal/trust/session` (server-side records, single-use refresh rotation, family-wide revoke on replay, idle and absolute timeouts, evidence records); `go test -count=1 ./internal/trust/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `TRUST-004` **[GATE_B][SOL_HIGH] Implement step-up authentication obligations.**
  - **Evidence (2026-09-05):** `TestTodo_TRUST_004`, `TestTodo_TRUST_004_{Security,Mutation}`, `FuzzTodo_TRUST_004`, `TestTodo_TRUST_004_PolicySelectsAssuranceAndNamesNoIdPMethod` in `internal/trust/stepup` (declarative obligation policy, pure EvaluateObligation at decision and execution stages, `PresentUnderObligation` refuses without consuming the proof, STEP_UP_REQUIRED reason; the transport admission step is specified for a follow-up in internal/transport); `go test -count=1 ./internal/trust/stepup/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-003`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_004`; `FUZZ=FuzzTodo_TRUST_004`; `SECURITY=TestTodo_TRUST_004_Security`; `MUTATION=TestTodo_TRUST_004_Mutation`.
  - **RED:** high-risk write with insufficient/currently stale assurance returns `STEP_UP_REQUIRED` and produces no proposal approval or effect.
  - **GREEN:** qualifying step-up binds principal, session, purpose, capability, risk, issued/expiry times and is rechecked at decision/execution.
  - **REFACTOR:** policy selects assurance; domain code never names an IdP method.
  - **Refs:** [Gate B depth matrix](plan.md#phase-1-implementation-depth-matrix), [governance composition](specs/governance-decision-and-obligation-composition.md).

- [x] `TRUST-005` **[GATE_B][SOL_HIGH] Exercise session revocation during workflow execution.**
  - **Evidence (2026-09-05):** `TestTodo_TRUST_005` in `internal/trust/session` (session revocation during workflow execution parks the running node, refuses the next advancement with a typed SESSION_REVOKED and resumes only after re-authentication; durable parked state is written through the session store; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/trust/session/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-003`, `WF-RUN-001`, `WF-RUN-002`, `WF-RUN-003`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_005`; `FUZZ=FuzzTodo_TRUST_005`; `SECURITY=TestTodo_TRUST_005_Security`; `MUTATION=TestTodo_TRUST_005_Mutation`.
  - **RED:** revoked requester/approver session cannot approve or initiate a new effect even if a task page was already open.
  - **GREEN:** decision returns `DENY_SESSION_REVOKED`; workflow remains waiting/blocked with auditable reauthentication route.
  - **REFACTOR:** revocation propagates through bounded event/control paths with measured lag.
  - **Refs:** [Gate B acceptance](execution-plan.md#gate-b-acceptance--limited-write-authority).

- [x] `TRUST-006` **[GATE_A][SOL_HIGH] Issue short-lived workload identities.**
  - **Depends:** `TOOL-018`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_006`; `FUZZ=FuzzTodo_TRUST_006`; `INTEGRATION=TestTodo_TRUST_006_Integration`; `SECURITY=TestTodo_TRUST_006_Security`; `MUTATION=TestTodo_TRUST_006_Mutation`.
  - **RED:** service call with node/network identity only, expired credential, wrong workload selector or wrong cell is denied.
  - **GREEN:** each API, worker, projector and connector authenticates as a unique workload with attested identity and rotation evidence.
  - **REFACTOR:** use open workload-identity protocols and eliminate long-lived shared service secrets.
  - **Refs:** [Platform correctness](plan.md#519-platform-correctness-is-business-correctness), [secrets contract](specs/secrets-key-custody-and-credential-leases.md).
  - **Evidence (2026-09-03):** `TestTodo_TRUST_006`, `FuzzTodo_TRUST_006`, `_Integration`, `_Security`, `_Mutation` in `internal/trust/workload` (ed25519 workload identities ≤ 15 minutes bound to the process-roles.yaml vocabulary, rotation-aware key source); `go test -count=1 ./internal/trust/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `TRUST-007` **[GATE_A][SOL_HIGH] Enforce service-to-service authorization.**
  - **Depends:** `TRUST-006`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_007`; `FUZZ=FuzzTodo_TRUST_007`; `INTEGRATION=TestTodo_TRUST_007_Integration`; `SECURITY=TestTodo_TRUST_007_Security`; `MUTATION=TestTodo_TRUST_007_Mutation`.
  - **RED:** projector invoking write, connector reading unrelated fields, or unknown workload/capability pair returns `DENY` before domain access.
  - **GREEN:** workload policy evaluates subject/action/resource/purpose/context and records explainable decision ID.
  - **REFACTOR:** network reachability never implies authority.
  - **Refs:** [Platform trust](specs/platform-architecture-catalog.md), [AuthZ contract](specs/organization-scope-and-authz.md).
  - **Evidence (2026-09-03):** `TestTodo_TRUST_007`, `FuzzTodo_TRUST_007`, `_Integration`, `_Security`, `_Mutation` in `internal/trust/workload` (service-to-service `Authorize` over an allow matrix compiled from process-roles.yaml data_access, deny-by-default, explainable); `go test -count=1 ./internal/trust/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `TRUST-008` **[GATE_A][SOL_HIGH] Resolve tenant and organization scope.**
  - **Depends:** `TRUST-001`, `MODEL-013`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_008`; `FUZZ=FuzzTodo_TRUST_008`; `SECURITY=TestTodo_TRUST_008_Security`; `MUTATION=TestTodo_TRUST_008_Mutation`.
  - **RED:** cross-tenant resource, unauthorized subsidiary, invalid shared-resource direction and inherited-deny bypass return `DENY` without existence disclosure.
  - **GREEN:** decision returns tenant boundary, allowed organization closure, mandatory denies, sharing path and policy versions.
  - **REFACTOR:** single-company customers use the same resolver without fake hierarchy.
  - **Refs:** [Organization security principle](plan.md#510-organization-scope-is-a-security-primitive), [organization AuthZ](specs/organization-scope-and-authz.md).
  - **Evidence (2026-09-03):** `TestTodo_TRUST_008`, `_Security`, `_Mutation`, `FuzzTodo_TRUST_008` in `internal/trust/authz` (`ResolveTenantScope` with org-unit closure walk and mandatory cross-tenant deny); `go test -count=1 ./internal/trust/authz/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `TRUST-009` **[GATE_A][SOL_HIGH] Enforce record, population and relationship authorization.**
  - **Depends:** `TRUST-008`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_009`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_009`; `FUZZ=FuzzTodo_TRUST_009`; `SECURITY=TestTodo_TRUST_009_Security`; `MUTATION=TestTodo_TRUST_009_Mutation`.
  - **RED:** principal outside manager/HRBP/assigned-population relationship receives `DENY` or filtered empty result, never unauthorized rows.
  - **GREEN:** current/effective relationship facts and population policy produce a bounded `AuthorizationScope`.
  - **REFACTOR:** relationship resolution is bitemporal and source-attributed.
  - **Refs:** [Authorization model](specs/organization-scope-and-authz.md), [relationship models](data/models/people-workforce.md).
  - **Evidence (2026-09-03):** `TestTodo_TRUST_009`, `_Security`, `_Mutation`, `FuzzTodo_TRUST_009` in `internal/trust/authz` (`ResolveAuthorizationScope` over bitemporal source-attributed relationship facts, `FilterPopulation`); PASS; branch plan-revision-2026-09-02; `go test -count=1 ./internal/trust/authz/...` on windows/arm64 (Go 1.26.3).

- [x] `TRUST-010` **[GATE_A][SOL_HIGH] Enforce field-level and purpose authorization.**
  - **Depends:** `TRUST-009`, `MODEL-023`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_010`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_010`; `FUZZ=FuzzTodo_TRUST_010`; `SECURITY=TestTodo_TRUST_010_Security`; `MUTATION=TestTodo_TRUST_010_Mutation`.
  - **RED:** compensation/medical/bank/case field or incompatible purpose is denied before repository/serializer/UI/tool access.
  - **GREEN:** allowed field mask and purpose obligation are returned; restricted values serialize as redacted without leakage through counts/errors.
  - **REFACTOR:** one field registry drives API, UI, export, connector and agent filtering.
  - **Refs:** [Data AuthZ](specs/organization-scope-and-authz.md), [classification/DLP](specs/data-classification-and-dlp.md).
  - **Evidence (2026-09-03):** `TestTodo_TRUST_010`, `_Security`, `_Mutation`, `FuzzTodo_TRUST_010` in `internal/trust/authz` (`ResolveFields`: closed field registry, purpose-bound grants, deny-by-default, ALLOW/DENIED/REDACTED/WITHHELD rulings); PASS; branch plan-revision-2026-09-02; `go test -count=1 ./internal/trust/authz/...` on windows/arm64 (Go 1.26.3).

- [x] `TRUST-011` **[GATE_A][SOL_LOW] Implement explainable authorization decisions.**
  - **Depends:** `TRUST-008`–`TRUST-010`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_011`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_011`; `GOLDEN=TestTodo_TRUST_011_Golden`; `FUZZ=FuzzTodo_TRUST_011`; `SECURITY=TestTodo_TRUST_011_Security`.
  - **RED:** decision lacking policy versions, matched grants/denies, scope, field mask, purpose, assurance or obligations fails evidence validation.
  - **GREEN:** simulator and enforcement return the same decision digest and a redaction-safe explanation.
  - **REFACTOR:** explanations cannot reveal hidden policy/resource facts.
  - **Refs:** [HRIS AuthZ simulator](specs/hris-admin-dataops.md), [authorization intents](data/models/intent-coverage-matrix.md).
  - **Evidence (2026-09-03):** `TestTodo_TRUST_011`, `_Golden`, `_Security`, `FuzzTodo_TRUST_011` in `internal/trust/authz` (`Enforce`/`Simulate` share one pure evaluator; `Decision` carries matched rule IDs, inputs digest, evidence ID and `Explain()`); PASS. Follow-up: derive `internal/domains/people.AuthorizationDecision` from `authz.Decision` during app wiring; branch plan-revision-2026-09-02; `go test -count=1 ./internal/trust/authz/...` on windows/arm64 (Go 1.26.3).

- [x] `TRUST-012` **[GATE_A][SOL_HIGH] Require AuthorizationScope in sensitive repositories.**
  - **Depends:** `TRUST-010`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_012`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_012`; `FUZZ=FuzzTodo_TRUST_012`; `SECURITY=TestTodo_TRUST_012_Security`; `MUTATION=TestTodo_TRUST_012_Mutation`.
  - **RED:** direct repository query without evaluated scope fails closed; broad wildcard cannot be caller constructed.
  - **GREEN:** query planner intersects tenant/org/population/field/time/purpose scope and returns only authorized projections.
  - **REFACTOR:** database defense-in-depth may narrow but never broaden scope.
  - **Refs:** [Data-layer enforcement](plan.md#16-strategic-decisions), [organization AuthZ](specs/organization-scope-and-authz.md).
  - **Evidence (2026-09-03):** `TestTodo_TRUST_012`, Declared `TRUST-012` test matrix and acceptance behavior in `internal/trust/authz`; PASS; `go test -p 1 ./internal/trust/authz` and `go vet ./internal/trust/authz` on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `TRUST-013` **[GATE_B][SOL_HIGH] Implement bounded delegation.**
  - **Evidence (2026-09-05):** `TestTodo_TRUST_013_ActingRoleAndCoverageUseTheSamePrimitive`, `TestTodo_TRUST_013_AssignmentsCannotEscapeTheDelegationBounds`, `TestTodo_TRUST_013_CoverageIsNeverRedelegable`, `TestTodo_TRUST_013_AssignmentShapeIsValidatedAtConstruction`, `TestTodo_TRUST_013_UnknownGrantKindFailsClosed` plus the pre-existing trust013 matrix in `internal/trust` (acting roles and vacation coverage lower to DelegationGrant and run through EvaluateDelegation; coverage never redelegable); `go test -count=1 ./internal/trust/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-001`, `TRUST-008`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_013`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_013`; `FUZZ=FuzzTodo_TRUST_013`; `SECURITY=TestTodo_TRUST_013_Security`; `MUTATION=TestTodo_TRUST_013_Mutation`.
  - **RED:** delegation expands delegator authority, survives expiry/revocation, crosses tenant or permits prohibited re-delegation.
  - **GREEN:** effective authority is the intersection of delegator, delegate, scope, purpose, capabilities, assurance and time; chain remains attributable.
  - **REFACTOR:** acting roles and vacation coverage use the same primitive.
  - **Refs:** [Identity/permission contract](plan.md#94-identity-and-permission-contract), [security models](data/models/security-trust.md).

- [x] `TRUST-014` **[GATE_B][SOL_HIGH] Implement separation-of-duties evaluation.**
  - **Evidence (2026-09-05):** `TestTodo_TRUST_014`, `FuzzTodo_TRUST_014`, `TestTodo_TRUST_014_{Security,Mutation}` in `internal/trust/sod` (pure separation-of-duties evaluator over requester/approver/executor actors with delegation chains and acting grants; refuses self-approval, approval by a delegate of the requester and executor==approver where the constraint demands, explaining the rule that fired); `go test -count=1 ./internal/trust/sod/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-013`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_014`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_014`; `FUZZ=FuzzTodo_TRUST_014`; `SECURITY=TestTodo_TRUST_014_Security`; `MUTATION=TestTodo_TRUST_014_Mutation`.
  - **RED:** requester approving own proposal, repair author executing repair, or same principal satisfying two distinct payroll-grade approvals is rejected.
  - **GREEN:** resolver filters invalid candidates and returns `SOD_UNSATISFIABLE` when quorum cannot be met.
  - **REFACTOR:** constraints are declarative and effective-dated.
  - **Refs:** [Workflow approval requirements](specs/workflow-runtime.md), [human work](specs/human-work-forms-and-rules.md).

- [x] `TRUST-015` **[GATE_A][SOL_HIGH] Store secrets only by governed reference.**
  - **Evidence (2026-09-05):** `TestTodo_TRUST_015_ConsumersResolveReferencesAtUseTime`, `TestTodo_TRUST_015_ResolutionIsAuthorizedPerUse`, `TestTodo_TRUST_015_ProviderBehaviourStaysBehindTheCustodyPort`, `TestTodo_TRUST_015_AccessPolicyHasNoWildcards`, `TestTodo_TRUST_015_ResolutionEvidenceNeverCarriesAValue`, `FuzzTodo_TRUST_015_Resolve` in `internal/trust/secrets` (use-time resolution behind the custody port, per-use authorization, evidence carries version and reference only); `go test -count=1 ./internal/trust/secrets/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-006`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_015`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_015`; `GOLDEN=TestTodo_TRUST_015_Golden`; `FUZZ=FuzzTodo_TRUST_015`; `INTEGRATION=TestTodo_TRUST_015_Integration`; `SECURITY=TestTodo_TRUST_015_Security`; `MUTATION=TestTodo_TRUST_015_Mutation`.
  - **RED:** schema/log/config/test snapshot containing raw connector, signing or operator secret fails validation and redaction tests.
  - **GREEN:** consumers resolve authorized secret references at use time and evidence only version/reference, never value.
  - **REFACTOR:** provider-specific vault behavior stays behind the custody interface.
  - **Refs:** [Secrets contract](specs/secrets-key-custody-and-credential-leases.md), [privacy contract](plan.md#95-privacy-contract).

- [x] `TRUST-016` **[GATE_B][SOL_HIGH] Implement destination-scoped credential leases.**
  - **Evidence (2026-09-05):** `TestTodo_TRUST_016`, `FuzzTodo_TRUST_016`, `TestTodo_TRUST_016_{Race,Integration,Security,Mutation}` in `internal/trust/lease` (leases bound to destination, tenant, purpose, operation, TTL and a single-use nonce, minted through the custody port with a widening check on the returned lease, administrative operations refused as over-broad, Use refuses tampered, revoked, expired, already-used, wrong-destination or wrong-operation presentations and consumes the nonce, Revoke idempotent, evidence on every call); `go test -count=1 ./internal/trust/lease/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-015`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_016`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_016`; `FUZZ=FuzzTodo_TRUST_016`; `RACE=TestTodo_TRUST_016_Race`; `INTEGRATION=TestTodo_TRUST_016_Integration`; `SECURITY=TestTodo_TRUST_016_Security`; `MUTATION=TestTodo_TRUST_016_Mutation`.
  - **RED:** expired, revoked, wrong-destination, wrong-capability or over-broad lease cannot dispatch an external write.
  - **GREEN:** short-lived lease binds workload, tenant, connector, destination, operations and expiry; use/renew/revoke evidence is durable.
  - **REFACTOR:** rotation does not change connector semantic identity.
  - **Refs:** [Credential lease spec](specs/secrets-key-custody-and-credential-leases.md), [integration platform](specs/integration-platform.md).

- [x] `TRUST-017` **[GATE_A][SOL_HIGH] Implement controlled outbound destination trust.**
  - **Evidence (2026-09-05):** `TestTodo_TRUST_017`, `FuzzTodo_TRUST_017`, `TestTodo_TRUST_017_{Security,Mutation}` in `internal/trust/outbound` (immutable allowlist policy of destinations with pinned trust-bundle refs, allowed purposes and data classes where empty lists are not wildcards; Check refuses a lease for another destination, a non-allowlisted destination, an uncleared purpose or data class in fixed order); `go test -count=1 ./internal/trust/outbound/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-007`, `TRUST-015`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_017`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_017`; `FUZZ=FuzzTodo_TRUST_017`; `SECURITY=TestTodo_TRUST_017_Security`; `MUTATION=TestTodo_TRUST_017_Mutation`.
  - **RED:** SSRF, private/link-local/metadata address, DNS rebinding, unapproved redirect, invalid TLS and proxy bypass fixtures are denied.
  - **GREEN:** normalized approved destination, DNS/address pin, TLS/proxy policy, purpose and DLP decision precede every outbound connection.
  - **REFACTOR:** connectors, webhooks, messaging, model providers, files and filings share the gateway.
  - **Refs:** [Foundation egress contract](specs/platform-foundation-gap-closure.md), [DLP spec](specs/data-classification-and-dlp.md).

- [x] `TRUST-018` **[GATE_A][SOL_HIGH] Implement DLP inspection and egress receipts.**
  - **Evidence (2026-09-05):** `TestTodo_TRUST_018` in `internal/trust/dlp` (outbound payload inspection against closed data-class detectors with content-free findings, an ALLOW/REDACT/REFUSE policy per destination clearance composed with the outbound policy by reference, and an append-only digested egress receipt; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/trust/dlp/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-023`, `TRUST-010`, `TRUST-017`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_018`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_018`; `GOLDEN=TestTodo_TRUST_018_Golden`; `FUZZ=FuzzTodo_TRUST_018`; `INTEGRATION=TestTodo_TRUST_018_Integration`; `SECURITY=TestTodo_TRUST_018_Security`; `MUTATION=TestTodo_TRUST_018_Mutation`.
  - **RED:** prohibited field/classification/region/purpose leaves through export, connector, email, webhook, support or agent path.
  - **GREEN:** policy returns allow/redact/block/approval-required; receipt binds content digest, destination, classifications, policy and authorized purpose.
  - **REFACTOR:** raw sensitive payload is not duplicated into the ledger.
  - **Refs:** [DLP specification](specs/data-classification-and-dlp.md), [privacy contract](plan.md#95-privacy-contract).

- [x] `TRUST-019` **[GATE_A][SOL_HIGH] Quarantine hostile external content.**
  - **Depends:** `MODEL-029`, `MODEL-023`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_019`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_019`; `FUZZ=FuzzTodo_TRUST_019`; `SECURITY=TestTodo_TRUST_019_Security`; `MUTATION=TestTodo_TRUST_019_Mutation`.
  - **RED:** unscanned upload reaches parser, form extraction, search, RAG, messaging or workflow; archive bomb/malware/oversize fixture is processed.
  - **GREEN:** content progresses through received/quarantined/validating/scanning/transforming and only a classified `SAFE` derivative is promoted.
  - **REFACTOR:** rescans are replay-safe and old unsafe derivatives are revoked.
  - **Refs:** [Foundation content safety](specs/platform-foundation-gap-closure.md), [document processing models](data/models/connectivity-access-content.md).
  - **Evidence (2026-09-03):** `TestTodo_TRUST_019`, Declared `TRUST-019` test matrix and acceptance behavior in `internal/trust/content`; PASS; `go test -p 1 ./internal/trust/content` and `go vet ./internal/trust/content` on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `TRUST-020` **[GATE_B][SOL_HIGH] Exercise IdP outage policy.**
  - **Evidence (2026-09-05):** `TestTodo_TRUST_020`, `TestTodo_TRUST_020_{Security,Mutation,Fault}`, `FuzzTodo_TRUST_020` in `internal/trust/outage` (pure outage policy over IdP reachability, JWKS staleness and clock skew across four request classes: new sessions denied while degraded, existing reads and writes bounded to a cached-staleness window with writes degraded to read-only, privileged operations only under a complete emergency grant, Reconcile flags every degraded use for revalidation once health recovers); `go test -count=1 ./internal/trust/outage/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-003`, `TRUST-004`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_020`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_020`; `FUZZ=FuzzTodo_TRUST_020`; `FAULT=TestTodo_TRUST_020_Fault`; `SECURITY=TestTodo_TRUST_020_Security`; `MUTATION=TestTodo_TRUST_020_Mutation`.
  - **RED:** outage permits new ordinary stale-federation sessions or silently blocks/permits emergency action without policy.
  - **GREEN:** new ordinary sessions deny; existing sessions follow declared bounded policy; separate P0 emergency grant requires approval/step-up/evidence and reconciliation after recovery.
  - **REFACTOR:** no hard-coded “IdP down = fail open/closed” behavior.
  - **Refs:** [Foundation IdP outage](specs/platform-foundation-gap-closure.md), [Gate B acceptance](execution-plan.md#gate-b-acceptance--limited-write-authority).

- [x] `TRUST-021` **[GATE_B][SOL_HIGH] Implement time-bounded JIT operator access.**
  - **Evidence (2026-09-05):** `TestTodo_TRUST_021`, `TestTodo_TRUST_021_{Security,Mutation,Fault}`, `FuzzTodo_TRUST_021` in `internal/trust/jit` (closed operator role vocabulary with a hard per-role TTL ceiling, grants require ticket, justification, purpose, capabilities and an approver distinct from the requester, no standing-grant path, automatic expiry and permanent idempotent revocation, GRANTED/USED/EXPIRED/REVOKED evidence on the grant); `go test -count=1 ./internal/trust/jit/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-004`, `TRUST-013`, `TRUST-015`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_021`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_021`; `FUZZ=FuzzTodo_TRUST_021`; `SECURITY=TestTodo_TRUST_021_Security`; `MUTATION=TestTodo_TRUST_021_Mutation`.
  - **RED:** standing broad production role, missing customer consent/reason, expired grant or unrecorded action is denied.
  - **GREEN:** elevation binds principal, ticket/incident, tenant, capabilities, fields, purpose, approvers, assurance and expiry; every use is evidenced.
  - **REFACTOR:** support view-as never changes business actor attribution.
  - **Refs:** [Administrative governance](plan.md#97-administrative-governance-contract), [responsibility boundaries](specs/platform-responsibility-boundaries.md).

- [x] `TRUST-022` **[GATE_C][SOL_HIGH] Implement break-glass containment and review.**
  - **Evidence (2026-09-05):** `TestTodo_TRUST_022` in `internal/trust/breakglass` (break-glass grants requiring an incident ref, justification, distinct approver and hard TTL, automatic containment on expiry listing every revoked capability, a mandatory post-use review by a distinct reviewer, evidence on open/use/contain/review and no standing-grant path; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/trust/breakglass/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-021`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_022`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_022`; `FUZZ=FuzzTodo_TRUST_022`; `SECURITY=TestTodo_TRUST_022_Security`; `MUTATION=TestTodo_TRUST_022_Mutation`.
  - **RED:** break-glass without declared emergency, dual control, narrow scope, short expiry or post-use review is rejected.
  - **GREEN:** emergency use triggers incident/evidence/customer route and automatic revocation; review records justified/violating outcome.
  - **REFACTOR:** ordinary support cannot invoke break-glass path.
  - **Refs:** [Platform production access](plan.md#16-strategic-decisions), [security models](data/models/security-trust.md).

- [x] `TRUST-023` **[GATE_C][SOL_HIGH] Implement certificate and trust-bundle lifecycle.**
  - **Evidence (2026-09-05):** `TestTodo_TRUST_023`, `TestTodo_TRUST_023_{Security,Mutation,Fault}`, `FuzzTodo_TRUST_023` in `internal/trust/bundle` (bundle versions pin roots and intermediates by DER digest with activation windows and expiry, Rotate requires a real overlap, the verifier trusts only a self-validating ACTIVE bundle and checks an independent revocation list against the resolved chain so a rollback cannot reintroduce a revoked issuer; stdlib crypto/x509 only); `go test -count=1 ./internal/trust/bundle/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-006`, `TRUST-015`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_023`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_023`; `FUZZ=FuzzTodo_TRUST_023`; `SECURITY=TestTodo_TRUST_023_Security`; `MUTATION=TestTodo_TRUST_023_Mutation`; `FAULT=TestTodo_TRUST_023_Fault`.
  - **RED:** expired/revoked/compromised cert or incomplete rotation remains accepted; trust-bundle rollback reintroduces revoked issuer.
  - **GREEN:** issue/distribute/overlap/dual-validate/rotate/revoke/expire/compromise transitions have signed evidence.
  - **REFACTOR:** crypto profile enables algorithm migration without semantic API change.
  - **Refs:** [Foundation certificate lifecycle](specs/platform-foundation-gap-closure.md), [crypto agility](data/models/security-trust.md).

- [x] `TRUST-024` **[GATE_C][SOL_HIGH] Implement consent, notice and objection lifecycle where processing relies on it.**
  - **Evidence (2026-09-05):** `TestTodo_TRUST_024` in `internal/trust/consent` (processing authority, presented notice, affirmative action, preferences/objection/restriction and propagation obligations are versioned and reconciled; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/trust/consent/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-026`, `GOVERN-001`, `GOVERN-002`, `PRIV-001`, `RECORDS-DISP-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_024`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_024`; `FUZZ=FuzzTodo_TRUST_024`; `SECURITY=TestTodo_TRUST_024_Security`; `MUTATION=TestTodo_TRUST_024_Mutation`.
  - **RED:** boolean-only consent, missing notice/version/legal context, invalid withdrawal or downstream use after restriction fails.
  - **GREEN:** processing authority, presented notice, affirmative action, preferences/objection/restriction and propagation obligations are versioned and reconciled.
  - **REFACTOR:** mandatory employment processing is not mislabeled as consent.
  - **Refs:** [Foundation consent lifecycle](specs/platform-foundation-gap-closure.md), [privacy models](data/models/assurance-intelligence-platform.md).

- [x] `TRUST-025` **[GATE_A][SOL_HIGH] Run cross-tenant authorization abuse tests.**
  - **Evidence (2026-09-05):** `TestTodo_TRUST_025` in `test/trustabuse` (adversarial suite driving every trust decision surface with cross-tenant leases, cursors, grants, break-glass, DLP receipts, SoD contexts, forged delegation chains, reused break-glass and expired JIT grants, each refused with its typed reason and pinned in a golden table so a silent widening fails; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./test/trustabuse/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-008`–`TRUST-012`, `TRUST-018`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_025`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_025`; `FUZZ=FuzzTodo_TRUST_025`; `SECURITY=TestTodo_TRUST_025_Security`; `MUTATION=TestTodo_TRUST_025_Mutation`.
  - **RED:** adversarial suite initially demonstrates attempted ID substitution, batch/export leak, cache key collision, error oracle and tenant-header spoofing are blocked.
  - **GREEN:** every vector returns deny/not-found as policy requires, zero foreign bytes, one redacted audit decision and no metric/log disclosure.
  - **REFACTOR:** retain attack fixtures as release-gating conformance tests.
  - **Refs:** [Tenant security](plan.md#510-organization-scope-is-a-security-primitive), [risk register](specs/risk-register.md).

### Security architecture refinement

> **Disposition (2026-09-05):** Added from `research/security-best-practices-2026.md` (2026-09-05 read date) to schedule the PARTIAL/MISSING controls and candidate todos that have no existing owner in TRUST, AUTHN, PRIV, SUPPLY, CICD, TOOL, GOV, DB or TENANT. All items are GATE_C: they harden an already-shipped P1A/P1B mechanism or add a genuinely new compliance-evidence surface, neither of which blocks Gate A/B delivery.

- [x] `SECARCH-001` **[GATE_C][SOL_HIGH] Bind phishing-resistant credential assurance to IAL/AAL/FAL policy.**
  - **Evidence (2026-09-05):** `TestTodo_SECARCH_001` in `internal/trust/stepup` (a policy table maps passkey/hardware-key/OTP/recovery evidence to NIST 800-63-4 IAL/AAL/FAL tiers; stepup.ObligationPolicy resolves required tier from that table and refuses insufficient or freshly-recovered assurance without new proof; every mapping decision ; written by an earlier lane (Luna or Sonnet) and verified independently); `go test -count=1 ./internal/trust/stepup/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-004`, `TRUST-021`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_SECARCH_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SECARCH_001`; `GOLDEN=TestTodo_SECARCH_001_Golden`; `SECURITY=TestTodo_SECARCH_001_Security`; `INTEGRATION=TestTodo_SECARCH_001_Integration`; `MUTATION=TestTodo_SECARCH_001_Mutation`.
  - **RED:** a payroll, bank-detail, export, repair or break-glass step-up obligation is satisfied by a credential type or recovery flow that has no declared IAL/AAL/FAL mapping, or a just-completed recovery is treated as equivalent to an unbroken high-assurance session.
  - **GREEN:** a policy table maps passkey/hardware-key/OTP/recovery evidence to NIST 800-63-4 IAL/AAL/FAL tiers; `stepup.ObligationPolicy` resolves required tier from that table and refuses insufficient or freshly-recovered assurance without new proof; every mapping decision is durably evidenced.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §1, E-03](research/security-best-practices-2026.md#1-enterprise-applications-in-general), [NIST SP 800-63-4](https://pages.nist.gov/800-63-4/), [FIDO passkeys](https://fidoalliance.org/passkeys/). Maps to NIST 800-53 IA-2(1)/IA-2(2); ASVS V2.1/V2.8.

- [x] `SECARCH-002` **[GATE_C][SOL_HIGH] Replicate the session and refresh-replay store for production durability.**
  - **Evidence (2026-09-05):** `TestTodo_SECARCH_002` in `internal/trust/session/pgstore`, `internal/trust/session`, `migrations`, `internal/data/tenancy/storagedisposition` (session.Store port with PersistentManager as the durable counterpart of Manager (same Record, Evidence and error vocabulary; manager.go untouched, the in-memory Manager stays the test double), EvaluateExpiry extracted so both apply one expiry rule, and pgstore over migration 00041's trust_session, trust_session_pointer, trust_session_refresh_generation and trust_session_evidence so create, touch, rotate, revoke and replay-detect survive restart and are visible across replicas under a generation CAS, replay of an older refresh generation refused by unique violation and revoking the family, cross-tenant reads empty under RLS; the two pointer tables hold no session content and sit deliberately outside RLS because lookups precede tenant resolution, as documented in the migration header; written by a Sonnet subagent and verified independently); `go test -count=1 ./internal/trust/session/pgstore/ ./internal/trust/session/ ./migrations/ ./internal/data/tenancy/storagedisposition/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-003`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_SECARCH_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SECARCH_002`; `GOLDEN=TestTodo_SECARCH_002_Golden`; `SECURITY=TestTodo_SECARCH_002_Security`; `INTEGRATION=TestTodo_SECARCH_002_Integration`; `MUTATION=TestTodo_SECARCH_002_Mutation`.
  - **RED:** a process restart or a second API replica loses an active session, forgets a retired refresh-token hash, or admits a refresh replay that a sibling replica already revoked.
  - **GREEN:** `internal/trust/session.Manager` is backed by a tenant-scoped, RLS-protected durable store so create/rotate/revoke/replay-detect survive restart and are visible across every replica within a bounded propagation window; the existing in-memory implementation remains the test double.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §1, E-04](research/security-best-practices-2026.md#1-enterprise-applications-in-general), [NIST SP 800-63-4](https://nvlpubs.nist.gov/nistpubs/SpecialPublications/NIST.SP.800-63-4.pdf), [RFC 9700](https://www.rfc-editor.org/rfc/rfc9700.html). Maps to NIST 800-53 SC-23/AC-12; ASVS V3.

- [x] `SECARCH-003` **[GATE_C][SOL_HIGH] Attest periodic access and entitlement review.**
  - **Evidence (2026-09-05):** `TestTodo_SECARCH_003` in `internal/trust/accessreview` (every effective grant resolved by internal/trust/authz and every JIT role in internal/trust/jit carries a review-due timestamp; an overdue grant is reported and a completed review (continue/revoke/narrow) is durably evidenced with a reviewer distinct from the ; written by an earlier lane (Luna or Sonnet) and verified independently); `go test -count=1 ./internal/trust/accessreview/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-008`, `TRUST-009`, `TRUST-021`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_SECARCH_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SECARCH_003`; `GOLDEN=TestTodo_SECARCH_003_Golden`; `SECURITY=TestTodo_SECARCH_003_Security`; `INTEGRATION=TestTodo_SECARCH_003_Integration`; `MUTATION=TestTodo_SECARCH_003_Mutation`.
  - **RED:** a standing role grant, JIT role assignment or delegation survives past its declared review interval with no reviewer decision on record.
  - **GREEN:** every effective grant resolved by `internal/trust/authz` and every JIT role in `internal/trust/jit` carries a review-due timestamp; an overdue grant is reported and a completed review (continue/revoke/narrow) is durably evidenced with a reviewer distinct from the grant holder.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §1, E-05](research/security-best-practices-2026.md#1-enterprise-applications-in-general), [NIST SP 800-53 Rev. 5](https://csrc.nist.gov/Projects/risk-management/sp800-53-controls/downloads), [AICPA SOC 2 TSC](https://www.aicpa-cima.com/topic/audit-assurance/audit-and-assurance-greater-than-soc-2/). Maps to NIST 800-53 AC-2(j)/AC-6(7); SOC 2 CC6.1.

- [x] `SECARCH-004` **[GATE_C][SOL_HIGH] Qualify a production KMS/HSM provider behind the custody port.**
  - **Evidence (2026-09-05):** `TestTodo_SECARCH_004` in `internal/trust/custody/kmsadapter` (a qualified KMS/HSM adapter passes the existing custody conformance suite unmodified, supports tenant-scoped key isolation and residency, and every envelope/lease/certificate package continues to pass against the real provider with no raw key material crossing; written by an earlier lane (Luna or Sonnet) and verified independently); `go test -count=1 ./internal/trust/custody/kmsadapter/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-026`, `TRUST-031`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_SECARCH_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SECARCH_004`; `GOLDEN=TestTodo_SECARCH_004_Golden`; `SECURITY=TestTodo_SECARCH_004_Security`; `INTEGRATION=TestTodo_SECARCH_004_Integration`; `MUTATION=TestTodo_SECARCH_004_Mutation`.
  - **RED:** `custody.Provider` has no adapter beyond `InMemoryFake`, so no ciphertext, signature or key-lifecycle event in the corpus is backed by a real KMS/HSM; a BYOK import or key destruction cannot be demonstrated against a real provider's revocation/attestation semantics.
  - **GREEN:** a qualified KMS/HSM adapter passes the existing `custody` conformance suite unmodified, supports tenant-scoped key isolation and residency, and every envelope/lease/certificate package continues to pass against the real provider with no raw key material crossing the port.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §1, E-06/E-07](research/security-best-practices-2026.md#1-enterprise-applications-in-general), [NIST key-management guidance](https://csrc.nist.gov/projects/key-management/key-management-guidelines), [NIST SP 800-57 Part 1](https://nvlpubs.nist.gov/nistpubs/specialpublications/nist.sp.800-57pt1r5.pdf). Maps to NIST 800-53 SC-12/SC-13; FIPS 140-3.

- [x] `SECARCH-005` **[GATE_C][SOL_HIGH] Move release-provenance signing off the dev-fixture key onto custody-backed key material.**
  - **Evidence (2026-09-05):** `TestTodo_SECARCH_005` in `tools/policy/provenance`, `tools/policy/release` (provenance and release-bundle signing resolve their Ed25519 key through internal/trust/custody/internal/trust/envelope, releaseadmission.Policy.TrustedPublicKeys pins the custody-issued key, and a rotation drill re-signs and re-verifies without breaking release.VerifyBundle; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/provenance/ ./tools/policy/release/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-026`, `SUPPLY-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_SECARCH_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SECARCH_005`; `GOLDEN=TestTodo_SECARCH_005_Golden`; `SECURITY=TestTodo_SECARCH_005_Security`; `INTEGRATION=TestTodo_SECARCH_005_Integration`; `MUTATION=TestTodo_SECARCH_005_Mutation`.
  - **RED:** `tools/policy/provenance` and `tools/policy/release` sign every statement/manifest with the named "THIS FIXTURE KEY CARRIES NO OPERATIONAL AUTHORITY" development key, so no admitted release is backed by a governed signing key.
  - **GREEN:** provenance and release-bundle signing resolve their Ed25519 key through `internal/trust/custody`/`internal/trust/envelope`, `releaseadmission.Policy.TrustedPublicKeys` pins the custody-issued key, and a rotation drill re-signs and re-verifies without breaking `release.VerifyBundle`.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §1, E-09](research/security-best-practices-2026.md#1-enterprise-applications-in-general), [SLSA levels](https://slsa.dev/spec/v1.0-rc2/levels), [Sigstore cosign verification](https://docs.sigstore.dev/cosign/verifying/verify/). Maps to SLSA Build L3; NIST 800-53 SA-10/SA-11.

- [x] `SECARCH-006` **[GATE_C][SOL_HIGH] Publish a threat-model and security-requirements artifact per release surface.**
  - **Evidence (2026-09-07):** `TestTodo_SECARCH_006`, `TestTodo_SECARCH_006_Conformance`, `TestTodo_SECARCH_006_Golden`, `TestTodo_SECARCH_006_Integration`, `TestTodo_SECARCH_006_Mutation`, `TestTodo_SECARCH_006_Security` in `tools/planning/threatmodel` (written in c000a21 on 2026-09-06 and never ticked; the named tests prove the GREEN contract: each such package resolves to a versioned threat-model record cross-linked into `tools/planning/riskbinding` (GOV-023) with security-acceptance criteria and a reviewed release exception path for any accepted gap); `go test -count=1 ./tools/planning/threatmodel/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `GOV-023`, `TOOL-011`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_SECARCH_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SECARCH_006`; `GOLDEN=TestTodo_SECARCH_006_Golden`; `SECURITY=TestTodo_SECARCH_006_Security`; `INTEGRATION=TestTodo_SECARCH_006_Integration`; `CONFORMANCE=TestTodo_SECARCH_006_Conformance`; `MUTATION=TestTodo_SECARCH_006_Mutation`.
  - **RED:** a policy evaluator, parser or financial-write package (`internal/trust/*`, `internal/data/ledger/*`, workflow decision code) ships or changes with no linked threat model naming assets, trust boundaries, abuse cases, mitigations and residual risk.
  - **GREEN:** each such package resolves to a versioned threat-model record cross-linked into `tools/planning/riskbinding` (GOV-023) with security-acceptance criteria and a reviewed release exception path for any accepted gap.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §1, E-10](research/security-best-practices-2026.md#1-enterprise-applications-in-general), [NIST SSDF v1.1](https://csrc.nist.gov/pubs/sp/800/218/final), [OWASP ASVS](https://owasp.org/www-project-application-security-verification-standard/). Maps to NIST SSDF PW.1; ASVS V1.

- [x] `SECARCH-007` **[GATE_C][SOL_HIGH] Add SAST, secret-scanning, IaC-scanning and DAST evidence to release admission.**
  - **Evidence (2026-09-07):** `TestTodo_SECARCH_007`, `TestTodo_SECARCH_007_Golden`, `TestTodo_SECARCH_007_Integration`, `TestTodo_SECARCH_007_Mutation`, `TestTodo_SECARCH_007_Security` in `tools/policy/releaseadmission` (written in 0a56524 on 2026-09-06 and never ticked; the named tests prove the GREEN contract: a versioned tool/config/finding/triage record for SAST, secret-scanning, IaC-scanning and DAST is a required `release.RequiredPolicyReports` entry; an unresolved high-severity finding blocks `releaseadmission.Evaluate` unless a time-bounded, reviewed exception is on file); `go test -count=1 ./tools/policy/releaseadmission/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TOOL-011`, `TOOL-013`, `GOV-019`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_SECARCH_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SECARCH_007`; `GOLDEN=TestTodo_SECARCH_007_Golden`; `SECURITY=TestTodo_SECARCH_007_Security`; `INTEGRATION=TestTodo_SECARCH_007_Integration`; `MUTATION=TestTodo_SECARCH_007_Mutation`.
  - **RED:** `tools/policy/cleancheckout`/`tools/policy/release` admit a release whose tree was never scanned for static defects, committed secrets, insecure IaC/migration definitions, or exercised by a dynamic test against a running instance.
  - **GREEN:** a versioned tool/config/finding/triage record for SAST, secret-scanning, IaC-scanning and DAST is a required `release.RequiredPolicyReports` entry; an unresolved high-severity finding blocks `releaseadmission.Evaluate` unless a time-bounded, reviewed exception is on file.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §1, E-06/E-11](research/security-best-practices-2026.md#1-enterprise-applications-in-general), [NIST SSDF](https://csrc.nist.gov/pubs/sp/800/218/final), [OWASP Top 10:2025](https://owasp.org/Top10/). Maps to NIST SSDF PW.7/PW.8; ASVS V14; CIS Control 16.

- [x] `SECARCH-008` **[GATE_C][SOL_HIGH] Bind security-telemetry retention, legal hold and alerting into one evidence contract.**
  - **Evidence (2026-09-07):** `TestTodo_SECARCH_008`, `TestTodo_SECARCH_008_Golden`, `TestTodo_SECARCH_008_Integration`, `TestTodo_SECARCH_008_Mutation`, `TestTodo_SECARCH_008_Security` in `internal/platform/telemetry/securityevidence` (written in 85f7f5f on 2026-09-06 and never ticked; the named tests prove the GREEN contract: `OBS-001`'s typed envelope carries retention class and hold-state for every SECURITY-tagged signal; a declared alert-rule registry maps named high-risk sequences (repeated DLP refusal, break-glass use, JIT grant near TTL ceiling, cross-tenant denial burst) to a routed alert, and an offline export/verification path proves the signal was not altered after emission); `go test -count=1 ./internal/platform/telemetry/securityevidence/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `OBS-001`, `TRUST-018`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_SECARCH_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SECARCH_008`; `GOLDEN=TestTodo_SECARCH_008_Golden`; `SECURITY=TestTodo_SECARCH_008_Security`; `INTEGRATION=TestTodo_SECARCH_008_Integration`; `MUTATION=TestTodo_SECARCH_008_Mutation`.
  - **RED:** a security-relevant event (break-glass use, DLP refusal, cross-tenant abuse-test trigger, JIT grant) is emitted through `internal/platform/telemetry` with no retention class, no legal-hold linkage, and no alert rule, so a high-risk sequence produces evidence but no notification.
  - **GREEN:** `OBS-001`'s typed envelope carries retention class and hold-state for every SECURITY-tagged signal; a declared alert-rule registry maps named high-risk sequences (repeated DLP refusal, break-glass use, JIT grant near TTL ceiling, cross-tenant denial burst) to a routed alert, and an offline export/verification path proves the signal was not altered after emission.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §1, E-12](research/security-best-practices-2026.md#1-enterprise-applications-in-general), [OWASP Top 10:2025](https://owasp.org/Top10/), [NIST SP 800-61 Rev. 3](https://nvlpubs.nist.gov/nistpubs/SpecialPublications/NIST.SP.800-61r3.pdf). Maps to NIST 800-53 AU-6/AU-11/AU-9; CIS Control 8.

- [x] `SECARCH-009` **[GATE_C][SOL_HIGH] Tokenize and mask classified fields in non-production environments.**
  - **Evidence (2026-09-05):** `TestTodo_SECARCH_009` in `internal/trust/dataclass` (every field classified RESTRICTED or higher by TRUST-010's registry resolves a lower-environment handling rule (irreversible tokenization, deterministic masking, or exclusion) before a non-production copy is created; the fixture digest is reproducible and the ; written by an earlier lane (Luna or Sonnet) and verified independently); `go test -count=1 ./internal/trust/dataclass/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-010`, `TRUST-028`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_SECARCH_009`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SECARCH_009`; `GOLDEN=TestTodo_SECARCH_009_Golden`; `SECURITY=TestTodo_SECARCH_009_Security`; `INTEGRATION=TestTodo_SECARCH_009_Integration`; `MUTATION=TestTodo_SECARCH_009_Mutation`.
  - **RED:** a dev/staging/sandbox refresh copies a classified field (compensation, medical, bank, case, government ID) into a lower environment unchanged, or a `SANDBOX-001` reset restores real values instead of a deterministic masked/tokenized fixture.
  - **GREEN:** every field classified `RESTRICTED` or higher by `TRUST-010`'s registry resolves a lower-environment handling rule (irreversible tokenization, deterministic masking, or exclusion) before a non-production copy is created; the fixture digest is reproducible and the mapping cannot be reversed without the production custody key.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §1, E-13](research/security-best-practices-2026.md#1-enterprise-applications-in-general), [ISO/IEC 27018:2025](https://www.iso.org/standard/27018?browse=tc). Maps to NIST 800-53 SC-28/MP-6; ASVS V8.

- [ ] `SECARCH-010` **[GATE_C][SOL_HIGH] Extend cross-tenant isolation conformance to cache, queue, object-store and backup paths.**
  - **Depends:** `TRUST-025`, `DB-017`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_SECARCH_010`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SECARCH_010`; `GOLDEN=TestTodo_SECARCH_010_Golden`; `SECURITY=TestTodo_SECARCH_010_Security`; `INTEGRATION=TestTodo_SECARCH_010_Integration`; `RACE=TestTodo_SECARCH_010_Race`; `MUTATION=TestTodo_SECARCH_010_Mutation`.
  - **RED:** `test/trustabuse` (TRUST-025) proves no cross-tenant leak through authorization decisions, but a shared cache key, a queue/job payload, an object-store prefix, an exported log line or a restored backup is not exercised by any adversarial fixture and could still collide across tenants.
  - **GREEN:** the reusable adversarial suite is extended with named cache-key-collision, queue/job cross-tenant-execution, object-store-prefix, log-disclosure and backup-restore-placement vectors; each returns deny/isolated with zero foreign bytes and is retained as a release-gating conformance test.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §1, E-14](research/security-best-practices-2026.md#1-enterprise-applications-in-general), [NIST SP 800-53 Rev. 5](https://csrc.nist.gov/Projects/risk-management/sp800-53-controls/downloads). Maps to NIST 800-53 SC-4/AC-4; CIS Control 3.

- [x] `SECARCH-011` **[GATE_C][SOL_HIGH] Publish secure-by-design product goals and track them against CISA's pledge.**
  - **Evidence (2026-09-07):** `TestTodo_SECARCH_011`, `TestTodo_SECARCH_011_Conformance`, `TestTodo_SECARCH_011_Golden`, `TestTodo_SECARCH_011_Integration`, `TestTodo_SECARCH_011_Mutation`, `TestTodo_SECARCH_011_Security` in `tools/planning/securebydesign` (written in c000a21 on 2026-09-06 and never ticked; the named tests prove the GREEN contract: a versioned secure-by-design record names default-configuration tests (deny-by-default AuthZ, MFA/step-up available, audit evidence on), a vulnerability-disclosure policy document, and a trend metric reviewed each release; exceptions are explicit and time-bounded); `go test -count=1 ./tools/planning/securebydesign/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `GOV-023`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_SECARCH_011`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SECARCH_011`; `GOLDEN=TestTodo_SECARCH_011_Golden`; `SECURITY=TestTodo_SECARCH_011_Security`; `INTEGRATION=TestTodo_SECARCH_011_Integration`; `CONFORMANCE=TestTodo_SECARCH_011_Conformance`; `MUTATION=TestTodo_SECARCH_011_Mutation`.
  - **RED:** the product has no reviewed statement of default-attack-surface reduction, no published vulnerability-disclosure policy, and no tracked metric proving MFA/audit-log capability is on by default for a new tenant.
  - **GREEN:** a versioned secure-by-design record names default-configuration tests (deny-by-default AuthZ, MFA/step-up available, audit evidence on), a vulnerability-disclosure policy document, and a trend metric reviewed each release; exceptions are explicit and time-bounded.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §1, E-17](research/security-best-practices-2026.md#1-enterprise-applications-in-general), [CISA Secure by Design pledge](https://www.cisa.gov/sites/default/files/2024-05/CISA%20Secure%20by%20Design%20Pledge_508c.pdf). Maps to CISA Secure by Design goals 1-7.

- [x] `SECARCH-012` **[GATE_C][SOL_HIGH] Resolve a per-tenant payment-data scope and applicability profile.**
  - **Evidence (2026-09-05):** `TestTodo_SECARCH_012` in `internal/domains/payroll/paymentprofile` (a versioned PaymentDataProfile per tenant/integration resolves a PCI-scope decision, a Nacha-threshold decision, a GLBA-covered-institution decision and an FTI decision, each with a data-flow map, segmentation/boundary test and reviewer sign-off; the profile i; written by an earlier lane (Luna or Sonnet) and verified independently); `go test -count=1 ./internal/domains/payroll/paymentprofile/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TENANT-001`, `MODEL-023`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_SECARCH_012`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SECARCH_012`; `GOLDEN=TestTodo_SECARCH_012_Golden`; `SECURITY=TestTodo_SECARCH_012_Security`; `INTEGRATION=TestTodo_SECARCH_012_Integration`; `MUTATION=TestTodo_SECARCH_012_Mutation`.
  - **RED:** no tenant or integration record states whether it stores, processes, transmits or can affect cardholder data, ACH account data or FTI, so a PCI/Nacha/GLBA/FTI applicability question has no authoritative answer and no data-flow boundary to test against.
  - **GREEN:** a versioned `PaymentDataProfile` per tenant/integration resolves a PCI-scope decision, a Nacha-threshold decision, a GLBA-covered-institution decision and an FTI decision, each with a data-flow map, segmentation/boundary test and reviewer sign-off; the profile is a required input to `SECARCH-013`/`SECARCH-014`.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §2, F-01](research/security-best-practices-2026.md#2-financial-applications-and-payment-touching-systems), [PCI DSS v4.0.1](https://www.pcisecuritystandards.org/standards/pci-dss/), [Nacha data-security rule](https://www.nacha.org/rules/supplementing-data-security-requirements), [IRS Publication 1075](https://www.irs.gov/privacy-disclosure/safeguards-program). Maps to PCI DSS v4.0.1 Req. 1/12.5; IRS Pub. 1075 §2.

- [x] `SECARCH-013` **[GATE_C][SOL_HIGH] Require dual control and independent out-of-band confirmation for bank-detail changes.**
  - **Evidence (2026-09-05):** `TestTodo_SECARCH_013` in `internal/domains/paymethod` (a bank-detail change is a named sod.Constraints-gated operation requiring a distinct approver, a confirmation sent to a contact sourced independently of the changed record, and a configurable delay before the changed destination may receive a payment; before/after values are prot; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/paymethod/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-004`, `TRUST-014`, `TRUST-021`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_SECARCH_013`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SECARCH_013`; `GOLDEN=TestTodo_SECARCH_013_Golden`; `SECURITY=TestTodo_SECARCH_013_Security`; `INTEGRATION=TestTodo_SECARCH_013_Integration`; `MUTATION=TestTodo_SECARCH_013_Mutation`.
  - **RED:** a bank-detail change reaches payment release after only ordinary step-up, with no second approver distinct from the requester, no independently sourced out-of-band confirmation and no cooling-off delay.
  - **GREEN:** a bank-detail change is a named `sod.Constraints`-gated operation requiring a distinct approver, a confirmation sent to a contact sourced independently of the changed record, and a configurable delay before the changed destination may receive a payment; before/after values are protected digests, never raw account data.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §2, F-05/F-06](research/security-best-practices-2026.md#2-financial-applications-and-payment-touching-systems), [FFIEC authentication guidance](https://www.ffiec.gov/news/press-releases/2021/pr-08-11), [Nacha fraud rules](https://www.nacha.org/rules/supplementing-fraud-detection-standards-web-debits). Maps to NACHA WEB debit account-validation rule; PCAOB AS 2201.

- [x] `SECARCH-014` **[GATE_C][SOL_HIGH] Bind ACH account validation and unreadable-at-rest storage to payment release.**
  - **Evidence (2026-09-05):** `TestTodo_SECARCH_014` in `internal/domains/paymethod` (validation method/timestamp/result is a required field on the payment-destination aggregate and an unvalidated destination is blocked without an approved exception; account numbers are stored only as envelope-encrypted (TRUST-028) or tokenized values and TRUST-018's DLP inspector; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/paymethod/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-018`, `TRUST-028`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_SECARCH_014`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SECARCH_014`; `GOLDEN=TestTodo_SECARCH_014_Golden`; `SECURITY=TestTodo_SECARCH_014_Security`; `INTEGRATION=TestTodo_SECARCH_014_Integration`; `MUTATION=TestTodo_SECARCH_014_Mutation`.
  - **RED:** a first-use or changed destination account can receive a payment with no validation method recorded, or a raw DFI account number appears in logs, evidence, a lower environment or an unapproved export once the tenant's Nacha threshold applies.
  - **GREEN:** validation method/timestamp/result is a required field on the payment-destination aggregate and an unvalidated destination is blocked without an approved exception; account numbers are stored only as envelope-encrypted (`TRUST-028`) or tokenized values and `TRUST-018`'s DLP inspector refuses their appearance in any egress path.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §2, F-02/F-03](research/security-best-practices-2026.md#2-financial-applications-and-payment-touching-systems), [Nacha account validation rule](https://www.nacha.org/rules/supplementing-fraud-detection-standards-web-debits), [Nacha supplemental data security](https://www.nacha.org/rules/supplementing-data-security-requirements). Maps to NACHA supplemental data-security rule; NACHA account-validation rule.

- [x] `SECARCH-015` **[GATE_C][SOL_HIGH] Define the payroll exact-decimal calculation contract.**
  - **Evidence (2026-09-05):** `TestTodo_SECARCH_015` in `internal/domains/payroll/calcpolicy` (every payroll/tax/deduction/rate calculation resolves scale, rounding mode, allocation order, negative/zero and currency rules from one versioned policy, emits a reproducible receipt of inputs/outputs/policy-version, and passes property, boundary and mutation tests against the de; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/payroll/calcpolicy/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LIB-006`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_SECARCH_015`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SECARCH_015`; `GOLDEN=TestTodo_SECARCH_015_Golden`; `SECURITY=TestTodo_SECARCH_015_Security`; `INTEGRATION=TestTodo_SECARCH_015_Integration`; `MUTATION=TestTodo_SECARCH_015_Mutation`.
  - **RED:** two payroll or tax calculations over the same inputs produce different totals because of undocumented rounding, allocation, negative/zero or fractions-of-cents handling, or a calculation uses binary floating point instead of the qualified `apd/v3` decimal kernel (`LIB-006`).
  - **GREEN:** every payroll/tax/deduction/rate calculation resolves scale, rounding mode, allocation order, negative/zero and currency rules from one versioned policy, emits a reproducible receipt of inputs/outputs/policy-version, and passes property, boundary and mutation tests against the decimal kernel.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §2, F-07](research/security-best-practices-2026.md#2-financial-applications-and-payment-touching-systems), [IRS Topic 758](https://www.irs.gov/taxtopics/tc758). Maps to IRS fractions-of-cents adjustment (Topic 758); ASVS V1.5.

- [x] `SECARCH-016` **[GATE_C][SOL_HIGH] Implement risk-based ACH fraud detection ahead of Nacha's 2026 deadlines.**
  - **Evidence (2026-09-07):** `TestTodo_SECARCH_016`, `TestTodo_SECARCH_016_Golden`, `TestTodo_SECARCH_016_Integration`, `TestTodo_SECARCH_016_Mutation`, `TestTodo_SECARCH_016_Security` in `internal/domains/paymethod/achrisk` (written in 66318d1 on 2026-09-06 and never ticked; the named tests prove the GREEN contract: a versioned risk-scoring policy evaluates amount/velocity/destination/operator/device/timing/payroll-change signals, returns alert/hold/release/reject with rule version and threshold, records investigator disposition, and resolves the tenant's applicable Nacha 2026 compliance deadline by originator/participant type); `go test -count=1 ./internal/domains/paymethod/achrisk/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-018`, `SECARCH-013`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_SECARCH_016`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SECARCH_016`; `GOLDEN=TestTodo_SECARCH_016_Golden`; `SECURITY=TestTodo_SECARCH_016_Security`; `INTEGRATION=TestTodo_SECARCH_016_Integration`; `MUTATION=TestTodo_SECARCH_016_Mutation`.
  - **RED:** an anomalous amount, velocity, destination, operator, device or timing signal on a payroll-change or ACH-origination path has no risk score, no hold/release/reject decision and no recorded investigator action, or the tenant's applicable 2026 Nacha deadline is not tracked.
  - **GREEN:** a versioned risk-scoring policy evaluates amount/velocity/destination/operator/device/timing/payroll-change signals, returns alert/hold/release/reject with rule version and threshold, records investigator disposition, and resolves the tenant's applicable Nacha 2026 compliance deadline by originator/participant type.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §2, F-04](research/security-best-practices-2026.md#2-financial-applications-and-payment-touching-systems), [Nacha 2026 fraud responsibilities](https://www.nacha.org/news/new-nacha-rules-new-fraud-compliance-responsibilities-all-organizations-sending-ach-payments). Maps to NACHA 2026 risk-management rule set.

- [ ] `SECARCH-017` **[GATE_C][SOL_HIGH] Stand up a GLBA Safeguards Rule information-security program record.**
  - **Depends:** `TRUST-015`, `PRIV-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_SECARCH_017`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SECARCH_017`; `GOLDEN=TestTodo_SECARCH_017_Golden`; `SECURITY=TestTodo_SECARCH_017_Security`; `INTEGRATION=TestTodo_SECARCH_017_Integration`; `CONFORMANCE=TestTodo_SECARCH_017_Conformance`; `MUTATION=TestTodo_SECARCH_017_Mutation`.
  - **RED:** a tenant or service relationship that is a covered financial institution under the FTC Safeguards Rule has no written information-security program, risk assessment, service-provider diligence record or consumer-count/notification decision on file.
  - **GREEN:** a versioned Safeguards program record maps GLBA-covered tenants to risk assessment, service-provider oversight, access-control/encryption/monitoring evidence already produced by `TRUST-015`/`TRUST-018`, an incident record and a reproducible consumer-count calculation driving the FTC 500-consumer notice decision.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §2, F-11](research/security-best-practices-2026.md#2-financial-applications-and-payment-touching-systems), [FTC Safeguards Rule](https://www.ftc.gov/legal-library/browse/rules/safeguards-rule), [FTC 2023 amendment](https://www.ftc.gov/news-events/news/press-releases/2023/10/ftc-amends-safeguards-rule-require-non-banking-financial-institutions-report-data-security-breaches). Maps to 16 CFR 314 (GLBA Safeguards Rule).

- [x] `SECARCH-018` **[GATE_C][SOL_HIGH] Resolve a per-tenant government-authorization profile and evidence catalog.**
  - **Evidence (2026-09-05):** `TestTodo_SECARCH_018` in `internal/domains/tenant/govauth` (a versioned GovernmentAuthorizationProfile per tenant/integration resolves applicable programs, system boundary, inherited-control references, evidence pointers, assessor status and a review date; the profile is a required input to SECARCH-019/SECARCH-021/SECA; written by an earlier lane (Luna or Sonnet) and verified independently); `go test -count=1 ./internal/domains/tenant/govauth/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TENANT-001`, `GOV-003`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_SECARCH_018`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SECARCH_018`; `GOLDEN=TestTodo_SECARCH_018_Golden`; `SECURITY=TestTodo_SECARCH_018_Security`; `INTEGRATION=TestTodo_SECARCH_018_Integration`; `CONFORMANCE=TestTodo_SECARCH_018_Conformance`; `MUTATION=TestTodo_SECARCH_018_Mutation`.
  - **RED:** a government or public-sector tenant has no immutable applicability profile stating which of FedRAMP, GovRAMP, TX-RAMP, FISMA, CJIS, FTI, CUI or MARS-E apply, so a procurement or audit question about system boundary or inherited controls has no authoritative answer.
  - **GREEN:** a versioned `GovernmentAuthorizationProfile` per tenant/integration resolves applicable programs, system boundary, inherited-control references, evidence pointers, assessor status and a review date; the profile is a required input to `SECARCH-019`/`SECARCH-021`/`SECARCH-022`/`SECARCH-024`.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §3, G-01](research/security-best-practices-2026.md#3-applications-interacting-with-federal-state-and-local-government), [FedRAMP Rev. 5 package guidance](https://www.fedramp.gov/2026/agencies/use/packages/rev5/), [NIST SP 800-53 control catalog](https://csrc.nist.gov/Projects/risk-management/sp800-53-controls/downloads). Maps to FedRAMP Rev. 5 SSP/POA&M baseline.

- [x] `SECARCH-019` **[GATE_C][SOL_HIGH] Track the FedRAMP 20x class and transition status per offering.**
  - **Evidence (2026-09-05):** `TestTodo_SECARCH_019` in `internal/domains/tenant/govauth` (each SECARCH-018 profile resolves an explicit FedRAMP path (Rev. 5 or 20x class A/B/C), package version, continuous-monitoring evidence and the customer-facing authorization-responsibility statement, refreshed against the current published timeline before each; written by an earlier lane (Luna or Sonnet) and verified independently); `go test -count=1 ./internal/domains/tenant/govauth/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SECARCH-018`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_SECARCH_019`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SECARCH_019`; `GOLDEN=TestTodo_SECARCH_019_Golden`; `SECURITY=TestTodo_SECARCH_019_Security`; `INTEGRATION=TestTodo_SECARCH_019_Integration`; `CONFORMANCE=TestTodo_SECARCH_019_Conformance`; `MUTATION=TestTodo_SECARCH_019_Mutation`.
  - **RED:** a release or procurement artifact claims a Rev. 5 authorization path after the 2027-06-11 new-certification cutoff, or names a FedRAMP 20x class without a package version and monitoring-evidence link.
  - **GREEN:** each `SECARCH-018` profile resolves an explicit FedRAMP path (Rev. 5 or 20x class A/B/C), package version, continuous-monitoring evidence and the customer-facing authorization-responsibility statement, refreshed against the current published timeline before each release.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §3, G-02](research/security-best-practices-2026.md#3-applications-interacting-with-federal-state-and-local-government), [FedRAMP 20x](https://www.fedramp.gov/20x/), [FedRAMP 2026 timeline](https://www.fedramp.gov/2026/timeline/). Maps to FedRAMP 20x Class A/B/C transition milestones.

- [x] `SECARCH-020` **[GATE_C][SOL_HIGH] Add a government data-classification boundary for FTI, CJI, CUI and ACA data.**
  - **Evidence (2026-09-05):** `TestTodo_SECARCH_020` in `internal/trust/dataclass` (TRUST-010's field registry gains a closed government-data-class dimension (FTI/CJI/CUI/ACA) that drives distinct storage, key (TRUST-028), access, disclosure, personnel and retention behavior and is recorded in every policy receipt; written by an earlier lane (Luna or Sonnet) and verified independently); `go test -count=1 ./internal/trust/dataclass/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-010`, `TRUST-028`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_SECARCH_020`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SECARCH_020`; `GOLDEN=TestTodo_SECARCH_020_Golden`; `SECURITY=TestTodo_SECARCH_020_Security`; `INTEGRATION=TestTodo_SECARCH_020_Integration`; `MUTATION=TestTodo_SECARCH_020_Mutation`.
  - **RED:** FTI, CJI, CUI or ACA-eligibility data is stored, accessed or exported through the same field-classification path as ordinary PII, so a Publication 1075, CJIS or NIST 800-171 Rev. 3 boundary cannot be enforced or evidenced separately.
  - **GREEN:** `TRUST-010`'s field registry gains a closed government-data-class dimension (FTI/CJI/CUI/ACA) that drives distinct storage, key (`TRUST-028`), access, disclosure, personnel and retention behavior and is recorded in every policy receipt.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §3, G-03/G-06/G-08](research/security-best-practices-2026.md#3-applications-interacting-with-federal-state-and-local-government), [IRS Pub. 1075 encryption](https://www.irs.gov/privacy-disclosure/encryption-requirements-of-publication-1075), [CJIS Security Policy v6.0](https://le.fbi.gov/file-repository/cjis_security_policy_v6-0_20241227.pdf), [NIST SP 800-171 Rev. 3](https://csrc.nist.gov/Pubs/sp/800/171/r3/final). Maps to IRS Pub. 1075 §4/§9; CJIS Security Policy Area 5; NIST 800-171 Rev. 3 3.1/3.13.

- [x] `SECARCH-021` **[GATE_C][SOL_HIGH] Record an explicit CMMC-level and NIST-version contract per DoD-touching engagement.**
  - **Evidence (2026-09-05):** `TestTodo_SECARCH_021` in `internal/trust/dataclass` (a versioned contract-profile record names the exact clause, CMMC level, referenced NIST revision, assessment method and POA&M rule, and blocks a compliance claim inconsistent with the recorded evidence set; written by an earlier lane (Luna or Sonnet) and verified independently); `go test -count=1 ./internal/trust/dataclass/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SECARCH-020`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_SECARCH_021`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SECARCH_021`; `GOLDEN=TestTodo_SECARCH_021_Golden`; `SECURITY=TestTodo_SECARCH_021_Security`; `INTEGRATION=TestTodo_SECARCH_021_Integration`; `CONFORMANCE=TestTodo_SECARCH_021_Conformance`; `MUTATION=TestTodo_SECARCH_021_Mutation`.
  - **RED:** a contract or questionnaire claims CMMC Level 1/2/3 compliance without recording whether the applicable clause references NIST 800-171 Rev. 2 (32 CFR 170) or the current NIST 800-171 Rev. 3 catalog `SECARCH-020` implements against.
  - **GREEN:** a versioned contract-profile record names the exact clause, CMMC level, referenced NIST revision, assessment method and POA&M rule, and blocks a compliance claim inconsistent with the recorded evidence set.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §3, G-09](research/security-best-practices-2026.md#3-applications-interacting-with-federal-state-and-local-government), [32 CFR 170.1](https://www.law.cornell.edu/cfr/text/32/170.1), [NIST SP 800-171 Rev. 3](https://csrc.nist.gov/Pubs/sp/800/171/r3/final). Maps to 32 CFR 170.14 CMMC Level 1-3; NIST 800-171 Rev. 3.

- [x] `SECARCH-022` **[GATE_C][SOL_HIGH] Map MARS-E and CMS ARS control releases for ACA/exchange tenants.**
  - **Evidence (2026-09-05):** `TestTodo_SECARCH_022` in `internal/domains/tenant/govauth` (each such tenant/integration pins the exact MARS-E volume/version, CMS ARS release, DUA, ISA and SSPP reference inside SECARCH-018's profile, with a reviewed control-inheritance mapping and privacy-analysis link; written by an earlier lane (Luna or Sonnet) and verified independently); `go test -count=1 ./internal/domains/tenant/govauth/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SECARCH-018`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_SECARCH_022`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SECARCH_022`; `GOLDEN=TestTodo_SECARCH_022_Golden`; `SECURITY=TestTodo_SECARCH_022_Security`; `INTEGRATION=TestTodo_SECARCH_022_Integration`; `CONFORMANCE=TestTodo_SECARCH_022_Conformance`; `MUTATION=TestTodo_SECARCH_022_Mutation`.
  - **RED:** an ACA-eligibility or health-insurance-exchange integration has no recorded MARS-E volume/version, CMS ARS baseline, data-use agreement or interconnection-agreement reference, so a substituted control catalog could silently diverge from the one the exchange contract requires.
  - **GREEN:** each such tenant/integration pins the exact MARS-E volume/version, CMS ARS release, DUA, ISA and SSPP reference inside `SECARCH-018`'s profile, with a reviewed control-inheritance mapping and privacy-analysis link.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §3, G-07](research/security-best-practices-2026.md#3-applications-interacting-with-federal-state-and-local-government), [CMS regulations and MARS-E](https://www.cms.gov/marketplace/resources/regulations-guidance), [CMS ARS 5.1](https://securitytest.cms.gov/policy-guidance/cms-acceptable-risk-safeguards-ars). Maps to MARS-E v2.2; CMS ARS 5.1.

- [x] `SECARCH-023` **[GATE_C][SOL_HIGH] Evidence workforce-exchange delivery integrity for state wage, new-hire and withholding filings.**
  - **Evidence (2026-09-07):** `TestTodo_SECARCH_023`, `TestTodo_SECARCH_023_Golden`, `TestTodo_SECARCH_023_Integration`, `TestTodo_SECARCH_023_Mutation`, `TestTodo_SECARCH_023_Security` in `internal/domains/payroll/filing` (written in d3c2a53 on 2026-09-06 and never ticked; the named tests prove the GREEN contract: every such exchange resolves a jurisdiction/schema-version profile, validates required elements before transmission, encrypts the payload end to end, and records payload digest, submission, acknowledgment and correction chain as durable evidence); `go test -count=1 ./internal/domains/payroll/filing/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-018`, `DB-017`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_SECARCH_023`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SECARCH_023`; `GOLDEN=TestTodo_SECARCH_023_Golden`; `SECURITY=TestTodo_SECARCH_023_Security`; `INTEGRATION=TestTodo_SECARCH_023_Integration`; `MUTATION=TestTodo_SECARCH_023_Mutation`.
  - **RED:** a state unemployment-wage, new-hire or withholding filing is transmitted with no jurisdiction-schema version pinned, no encrypted-payload digest and no correction lineage, so a rejected or corrected filing cannot be tied back to the exact submission it corrects.
  - **GREEN:** every such exchange resolves a jurisdiction/schema-version profile, validates required elements before transmission, encrypts the payload end to end, and records payload digest, submission, acknowledgment and correction chain as durable evidence.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §3, G-14/G-15/G-16](research/security-best-practices-2026.md#3-applications-interacting-with-federal-state-and-local-government), [Department of Labor wage reporting](https://www.dol.gov/node/159684), [OCSS state new-hire directory](https://ocsp.acf.hhs.gov/irg/irgpdf.pdf?addrClassType=EMP&addrType=NHR&geoType=OGP&groupCode=EMP). Maps to Social Security Act quarterly wage-reporting requirement.

- [x] `SECARCH-024` **[GATE_C][SOL_HIGH] Generate a versioned government-procurement and accessibility evidence pack.**
  - **Evidence (2026-09-05):** `TestTodo_SECARCH_024` in `internal/domains/tenant/govauth` (a generated, versioned pack answers each standard questionnaire item (data location, subprocessors, boundary, identity, encryption, tenant isolation, incident notice, RTO/RPO, vulnerability management, accessibility, retention, audit reports) from SECARCH-018 ; written by an earlier lane (Luna or Sonnet) and verified independently); `go test -count=1 ./internal/domains/tenant/govauth/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SECARCH-018`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_SECARCH_024`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SECARCH_024`; `GOLDEN=TestTodo_SECARCH_024_Golden`; `SECURITY=TestTodo_SECARCH_024_Security`; `INTEGRATION=TestTodo_SECARCH_024_Integration`; `CONFORMANCE=TestTodo_SECARCH_024_Conformance`; `MUTATION=TestTodo_SECARCH_024_Mutation`.
  - **RED:** a state or local procurement questionnaire is answered by hand with no data-location, subprocessor, RTO/RPO, incident-notice or accessibility evidence link, so the answer cannot be traced back to a current, reviewed source.
  - **GREEN:** a generated, versioned pack answers each standard questionnaire item (data location, subprocessors, boundary, identity, encryption, tenant isolation, incident notice, RTO/RPO, vulnerability management, accessibility, retention, audit reports) from `SECARCH-018` and existing evidence, each with owner, date and an accessible artifact.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §3, G-11/G-17](research/security-best-practices-2026.md#3-applications-interacting-with-federal-state-and-local-government), [GovRAMP government adoption](https://www.stateramp.org/governments/adopting-for-government/), [Access Board Section 508](https://www.access-board.gov/ict/about/). Maps to Section 508 ICT Baseline; GovRAMP procurement evidence set.

---

## 5. Phase 1 People, organization, position, compensation and budget domains

> **Disposition (2026-09-02):** P1A for authorized reads, timelines and deterministic simulation. P1B executes only `promote_worker` (job/level) and `change_base_pay`; manager/organization change (`ORG-*` writes) and position fill are CONFORMANCE. Budget binds `COMPENSATION_POOL` only.

- [x] `PEOPLE-001` **[GATE_A][SOL_LOW] Implement authorized Person and Worker reads.**
  - **Evidence (2026-09-05):** `TestTodo_PEOPLE_001`, `TestTodo_PEOPLE_001_Security` in `internal/domains/people` (ReadPerson/ReadWorker as closed specializations of ExplainWorkerState with fixed field masks and typed records under the same WITHHELD/DENIED rules; app-layer capability registration is a follow-up); `go test -count=1 ./internal/domains/people/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-022`, `TRUST-012`, `MODEL-021`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PEOPLE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PEOPLE_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PEOPLE_001`; `SECURITY=TestTodo_PEOPLE_001_Security`.
  - **RED:** ordinary read leaks protected identity claims, candidate data, medical/immigration/payroll/bank compartments or returns a worker from another tenant/org/purpose.
  - **GREEN:** current/as-of/known-at query returns typed Person/Worker roles, allowed field mask, authority and provenance; inaccessible records do not disclose existence.
  - **REFACTOR:** Person is stable identity; Worker/Candidate/former employment remain concurrent roles/relationships.
  - **Refs:** [People domain](specs/people-employment-assignment-domain.md), [people models](data/models/people-workforce.md).

- [x] `PEOPLE-002` **[GATE_A][SOL_LOW] Implement Employment timeline reads.**
  - **Evidence (2026-09-05):** `TestTodo_PEOPLE_002` in `internal/domains/people` (Employment timeline read as a closed specialization of ExplainWorkerState with a fixed field mask: ordered employment periods with effective-at/known-at, typed period kinds, gaps and overlaps explicit, WITHHELD/DENIED rules unchanged; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/people/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PEOPLE-001`, `MODEL-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PEOPLE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PEOPLE_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PEOPLE_002`; `SECURITY=TestTodo_PEOPLE_002_Security`.
  - **RED:** simultaneous employments, suspension/leave, future start/end or retro correction collapse into one current status.
  - **GREEN:** query returns independent legal relationships and revisions for effective-as-of and known-as-of modes with authority/provenance.
  - **REFACTOR:** derived WorkerStatus never becomes authoritative employment fact.
  - **Refs:** [People/employment contract](specs/people-employment-assignment-domain.md), [employment lifecycle](data/models/registry-and-coverage-contracts.md#initial-lifecycle-profiles).

- [x] `PEOPLE-003` **[GATE_A][SOL_LOW] Implement Assignment timeline reads.**
  - **Evidence (2026-09-05):** `TestTodo_PEOPLE_003` in `internal/domains/people` (Assignment timeline read as a closed specialization of ExplainWorkerState with a fixed field mask: ordered assignment periods with position, org unit, effective-at/known-at, typed kinds, explicit gaps and overlaps under the WITHHELD/DENIED rules; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/people/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PEOPLE-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PEOPLE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PEOPLE_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PEOPLE_003`; `PROPERTY=TestTodo_PEOPLE_003_Property`; `FAULT=TestTodo_PEOPLE_003_Fault`.
  - **RED:** query invents a default assignment, drops temporary/secondary assignment or ignores future overlap.
  - **GREEN:** each assignment returns job/position/org/location/manager/cost-center facts, interval, source and revision independently.
  - **REFACTOR:** Employment owns legal relationship; Assignment owns placement of work.
  - **Refs:** [People/assignment contract](specs/people-employment-assignment-domain.md), [relationship map](data/models/relationship-map.md).

- [x] `ORG-001` **[GATE_A][SOL_HIGH] Implement canonical organization graph reads.**
  - **Depends:** `MODEL-013`, `TRUST-008`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ORG_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ORG_001`; `PROPERTY=TestTodo_ORG_001_Property`; `GOLDEN=TestTodo_ORG_001_Golden`; `SECURITY=TestTodo_ORG_001_Security`; `MUTATION=TestTodo_ORG_001_Mutation`.
  - **RED:** graph returns cycle, orphan, duplicate exclusive parent, cross-tenant edge or unauthorized subsidiary.
  - **GREEN:** as-of graph returns legal/business hierarchy, source revisions, watermark and security-preserving scope closure.
  - **REFACTOR:** matrix/project relationships are distinct typed edges, not extra parent columns.
  - **Refs:** [Organization domain](specs/organization-and-relationship-domain.md), [org models](data/models/people-workforce.md).
  - **Evidence (2026-09-03):** `TestTodo_ORG_001`, Declared `ORG-001` test matrix and acceptance behavior in `internal/domains/organization`; PASS; `go test -p 1 ./internal/domains/organization` and `go vet ./internal/domains/organization` on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `ORG-002` **[GATE_A][SOL_HIGH] Implement manager relationship resolution.**
  - **Evidence (2026-09-05):** `TestTodo_ORG_002` in `internal/domains/org` (manager relationship resolution as a closed read over a WorkerFacts-shaped port: direct manager, management chain to a declared depth and dotted lines with effective-at/known-at, cycles and depth overflow refused, WITHHELD/DENIED disclosure applied per hop, Explain; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/org/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ORG-001`, `PEOPLE-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ORG_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ORG_002`; `PROPERTY=TestTodo_ORG_002_Property`; `FAULT=TestTodo_ORG_002_Fault`; `MUTATION=TestTodo_ORG_002_Mutation`.
  - **RED:** resolver guesses a vacant manager, treats dotted-line as primary, ignores assignment ID, or accepts stale/disagreeing projections.
  - **GREEN:** returns relationship ID/type/source/interval/watermark/policy and one of `RESOLVED`, `VACANT`, `AMBIGUOUS`, `STALE`, `DISAGREEING`.
  - **REFACTOR:** approval audience and domain reads share the resolver without sharing authorization decisions.
  - **Refs:** [Organization relationships](specs/organization-and-relationship-domain.md), [manager-change fixture](reference-workflows/manager-change.md).

- [x] `POSITION-001` **[GATE_A][SOL_HIGH] Implement position revision and compatibility reads.**
  - **Evidence (2026-09-05):** `TestTodo_POSITION_001`, `TestTodo_POSITION_001_{Property,Security,Conformance,Mutation}`, `BenchmarkTodo_POSITION_001` in `internal/domains/position` (PositionFacts port, revision/lifecycle/capacity, CheckCompatibility with typed findings, fail-closed Authorize; a Postgres PositionFacts adapter and capability registration are follow-ups); `go test -count=1 ./internal/domains/position/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-018`, `ORG-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_POSITION_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_POSITION_001`; `PROPERTY=TestTodo_POSITION_001_Property`; `SECURITY=TestTodo_POSITION_001_Security`; `CONFORMANCE=TestTodo_POSITION_001_Conformance`; `BENCHMARK=BenchmarkTodo_POSITION_001`; `MUTATION=TestTodo_POSITION_001_Mutation`.
  - **RED:** closed/frozen/wrong-job/wrong-org/wrong-legal-entity or stale position is presented as compatible.
  - **GREEN:** response returns effective revision, constraints, lifecycle, capacity policy, source and compatibility findings.
  - **REFACTOR:** Job describes work; Position represents governed capacity.
  - **Refs:** [Position/headcount contract](specs/position-and-headcount-domain.md), [position models](data/models/people-workforce.md).

- [x] `POSITION-002` **[GATE_A][SOL_HIGH] Calculate position capacity and vacancy.**
  - **Evidence (2026-09-05):** `TestTodo_POSITION_002` in `internal/domains/position` (capacity and vacancy over PositionFacts with effective dating, pending proposals counted as reserved, typed findings for over-capacity and vacancy-after-date; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/position/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `POSITION-001`, `MODEL-003`, `MODEL-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_POSITION_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_POSITION_002`; `PROPERTY=TestTodo_POSITION_002_Property`; `RACE=TestTodo_POSITION_002_Race`; `BENCHMARK=BenchmarkTodo_POSITION_002`; `MUTATION=TestTodo_POSITION_002_Mutation`.
  - **RED:** over-capacity heads/FTE, overlapping exclusive occupancy, fractional vacancy or reservation interval is calculated with float/closed-range errors.
  - **GREEN:** exact fixed-decimal interval result returns consumed, reserved and available heads/FTE plus typed conflicts.
  - **REFACTOR:** budget authority is not inferred from position capacity.
  - **Refs:** [Position capacity](specs/position-and-headcount-domain.md), [wire decimal/time](data/models/wire-contract-primitives.md).

- [x] `COMP-001` **[GATE_A][SOL_HIGH] Implement authorized compensation reads.**
  - **Evidence (2026-09-05):** `TestTodo_COMP_001` in `internal/domains/rewards` (authorized compensation reads as a closed specialization with a fixed field mask: base pay, pay band ref and components at an as-of, disclosure decided by the caller's declared scope so a caller without compensation-read scope receives WITHHELD with the reason, Explain never repeating amounts; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/rewards/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PEOPLE-003`, `TRUST-010`, `MODEL-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_COMP_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_COMP_001`; `PROPERTY=TestTodo_COMP_001_Property`; `SECURITY=TestTodo_COMP_001_Security`; `MUTATION=TestTodo_COMP_001_Mutation`.
  - **RED:** unauthorized components, raw bank/payroll data, implicit currency/pay basis or incumbent observation labeled HCM Next fact is returned.
  - **GREEN:** allowed package/components include currency, basis, frequency, effective/known time, authority, source and exact decimals.
  - **REFACTOR:** field-level disclosure uses shared masks and classification.
  - **Refs:** [Compensation domain](specs/compensation-domain.md), [rewards models](data/models/rewards-payroll-workforce.md).

- [x] `COMP-002` **[GATE_A][SOL_HIGH] Implement deterministic compensation annualization.**
  - **Evidence (2026-09-05):** `TestTodo_COMP_002` in `internal/domains/rewards` (annualization of a compensation package from its pay basis (hourly, daily, monthly, annual, piece rate) under a declared rule set in exact decimal with the rule's rounding, identical inputs yielding identical digests, a basis without a rule refused, Explain listing the factors, golden table; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/rewards/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `COMP-001`, `MODEL-003`, `MODEL-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_COMP_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_COMP_002`; `PROPERTY=TestTodo_COMP_002_Property`; `RACE=TestTodo_COMP_002_Race`; `MUTATION=TestTodo_COMP_002_Mutation`.
  - **RED:** salary/hourly/FTE/pay-frequency/proration vectors use float, ambient calendar or implicit rounding.
  - **GREEN:** golden vectors return exact annualized/prorated amount, rounding mode, formula version and calculation trace.
  - **REFACTOR:** calculation is pure and replayable.
  - **Refs:** [Compensation calculation](specs/compensation-domain.md), [SimulateCompensation intent](specs/business-intent-catalog.md#initial-draft-contract-slice).

- [x] `COMP-003` **[GATE_A][SOL_HIGH] Evaluate pay-band position.**
  - **Evidence (2026-09-05):** `TestTodo_COMP_003` in `internal/domains/rewards` (pay-band position of an annualized amount against a pay band version yielding compa-ratio and range penetration in exact decimal, a typed BELOW/WITHIN/ABOVE class with the boundary named, currency mismatch refused, disclosure under COMP-001's WITHHELD/DENIED rules, Explain; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/rewards/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `COMP-002`, `MODEL-018`.
  - **INTENT CONTEXT:** `ROLE=DIRECT; SETS=BI.REWARDS; INTENTS=hcmnext.rewards.evaluate_pay_band_position/v1; FAMILY=CALCULATION_REQUEST; WHY=implement accepted intent 82 as a pure governed calculation`.
  - **TEST:** `TestTodo_COMP_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_COMP_003`; `PROPERTY=TestTodo_COMP_003_Property`; `RACE=TestTodo_COMP_003_Race`; `MUTATION=TestTodo_COMP_003_Mutation`.
  - **RED:** missing/stale band, currency mismatch, invalid range or unknown worker basis produces a percentage/result anyway.
  - **GREEN:** result returns `WITHIN`, `BELOW`, `ABOVE`, or `UNKNOWN`, exact compa-ratio/range penetration, band/release and explanation trace.
  - **REFACTOR:** policy consequence remains outside the pure calculation.
  - **Refs:** [Compensation domain](specs/compensation-domain.md), [intent 82](specs/business-intent-catalog.md#initial-draft-contract-slice).

- [x] `BUDGET-001` **[GATE_A][SOL_HIGH] Implement typed workforce-budget observations.**
  - **Evidence (2026-09-05):** `TestTodo_BUDGET_001`, `TestTodo_BUDGET_001_Mutation`, `BenchmarkTodo_BUDGET_001` in `internal/domains/budget` (typed workforce-budget observations and reservations; github.com/google/uuid removed from the domain by deriving reservation ids from the natural key, clearing the LIB-002 firewall finding); `go test -count=1 ./internal/domains/budget/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-021`, `MODEL-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_BUDGET_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_BUDGET_001`; `BENCHMARK=BenchmarkTodo_BUDGET_001`; `MUTATION=TestTodo_BUDGET_001_Mutation`.
  - **RED:** wrong unit/currency/period, stale baseline, exhausted/negative pool or position capacity masquerading as finance authority passes.
  - **GREEN:** `BudgetAuthorityRef` returns type, owner, scope, period, unit, source watermark, available quantity and observation evidence.
  - **REFACTOR:** no generic untyped “budget” value.
  - **Refs:** [Workforce budget authority](specs/workforce-budget-authority.md), [promotion workflow](reference-workflows/promote-into-management.md).

- [x] `PROMO-007` **[GATE_A][SOL_HIGH] Publish the intent-only `promotion.propose` request contract.**
  - **Evidence (2026-09-05):** `TestTodo_PROMO_007`, `TestTodo_PROMO_007_{Golden,Security,Mutation}` in `internal/intent/app`; `TestTodo_PROMO_007_Security`, `TestProposePromotion{ForwardsToThePromotionService,IsUnavailableWithoutTheContract,ProjectsThePortsRefusals}` in `internal/transport/journey`; `TestTodo_PROMO_007_{Integration,Security,Mutation}` in `test/bootstrap`; `TestTodo_PROMO_007_{Integration,Security}` in `test/tunnel` (hcmnext.journey.v1.JourneyService.ProposePromotion with a twelve-field intent-only request: subject worker ref, desired job/grade/position/org/manager/pay/currency, effective date, reason, expected subject revision and client request id; a reflection-checked closed allowlist fails closed if the proto gains a field, unknown fields refused by the shared trusted-request boundary on both transports, the path is CapabilityGateway → Promotion service → IntentService → preflight with current placement read server-side and zero domain rows changed; identical requests over gRPC and the WebSocket tunnel yield one intent and one content digest; buf compatibility proves the change additive; follow-ups: publish expected_subject_revision on ListWorkers, act on desired_manager_ref via the change_manager child); `go test -count=1 ./internal/intent/app/ ./internal/transport/journey/ ./tools/gen/compatibility/ ./test/bootstrap/ ./test/tunnel/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TOOL-004`, `INTENT-002`, `TRUST-001`.
  - **INTENT CONTEXT:** `ROLE=COMPOSITE; SETS=BI.PEOPLE,BI.REWARDS,BI.WORK,BI.OPERATIONS; INTENTS=hcmnext.people.promote_worker/v1; FAMILY=CHANGE_REQUEST; CHILDREN=hcmnext.people.change_manager/v1,hcmnext.rewards.change_base_pay/v1,hcmnext.rewards.reserve_compensation_budget/v1,hcmnext.rewards.release_compensation_budget/v1; WHY=canonical promote-into-management vertical slice`.
  - **TEST:** `TestTodo_PROMO_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PROMO_007`; `GOLDEN=TestTodo_PROMO_007_Golden`; `INTEGRATION=TestTodo_PROMO_007_Integration`; `SECURITY=TestTodo_PROMO_007_Security`; `MUTATION=TestTodo_PROMO_007_Mutation`.
  - **RED:** caller can assert current salary/manager/position vacancy/budget/authority, select principal/tenant/session/locale/legal context, omit expected subject revision/client request ID, produce different semantics over gRPC and grpcbridge, bypass CapabilityGateway/Intent creation, or cause a domain mutation during propose/preflight.
  - **GREEN:** generated request accepts only subject, desired job/position/org/manager/compensation, effective date, reason, expected subject revision and client request ID; trusted interceptors inject immutable security/tenant contexts and both transports follow `CapabilityGateway -> application Promotion service -> IntentService -> preflight`, resolving current facts server-side with zero domain mutation.
  - **REFACTOR:** browser, CLI, agent and partner clients invoke the same semantic capability and cannot smuggle server-owned facts through extension fields.
  - **Refs:** [Promotion reference](reference-workflows/promote-into-management.md), [identity boundary](specs/organization-scope-and-authz.md), [capability parity](plan.md#59-every-product-action-is-a-governed-capability).

- [x] `SNAPSHOT-001` **[GATE_A][SOL_HIGH] Resolve a consistent multi-domain read snapshot.**
  - **Evidence (2026-09-05):** `TestTodo_SNAPSHOT_001`, `TestTodo_SNAPSHOT_001_{Golden,Security,Mutation}` in `internal/engines/snapshot` (source-neutral Resolve over a Source port producing an immutable ReadSnapshot with a canonical sha256 digest over typed InputEntry values carrying name, owner, tenant, authority class NATIVE_STATE/EXTERNAL_OBSERVATION/REFERENCE_CONFIG, source ref, effective-at/known-at, revision/head/watermark/freshness, classification, provenance and reference version; nine typed refusals for incomplete entries, tenant or known-at mismatch, authority mismatch and watermark floors; native and external inputs differ only by explicit descriptors, never by shape); `go test -count=1 ./internal/engines/snapshot/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-020`, `MODEL-021`, `TRUST-003`, `TRUST-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SNAPSHOT_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SNAPSHOT_001`; `GOLDEN=TestTodo_SNAPSHOT_001_Golden`; `SECURITY=TestTodo_SNAPSHOT_001_Security`; `MUTATION=TestTodo_SNAPSHOT_001_Mutation`.
  - **RED:** snapshot entry lacks semantic input name, owner, authority class/source, effective-at/known-at, revision/head/watermark/freshness, classification, provenance or reference/config version.
  - **GREEN:** source-neutral resolver produces immutable typed input entries and consistency requirements under one snapshot identity/digest; native state and external observations remain explicitly distinguishable.
  - **REFACTOR:** snapshot contract is source-neutral; native state and Workday/Oracle observations differ only through explicit authority/provenance descriptors.
  - **Refs:** [Source authority](specs/source-authority-and-external-mastering.md), [projection health](specs/transaction-ledger-reconciliation-and-repair.md), [Promotion reference](reference-workflows/promote-into-management.md).

- [x] `SNAPSHOT-002` **[GATE_A][SOL_HIGH] Enforce minimum watermark and freshness requirements per snapshot input.**
  - **Evidence (2026-09-05):** `TestTodo_SNAPSHOT_002` in `internal/engines/snapshot` (per-input minimum watermark and freshness requirements declared on the request and enforced at resolve with typed refusals naming the input, a freshness horizon derived from known-at, golden verdicts for a fixture; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/engines/snapshot/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SNAPSHOT-001`, `DATA-020`, `INTG-009`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SNAPSHOT_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SNAPSHOT_002`; `GOLDEN=TestTodo_SNAPSHOT_002_Golden`; `MUTATION=TestTodo_SNAPSHOT_002_Mutation`.
  - **RED:** mixed incompatible heads, observation older than declared maximum age, authority change during read, future-known correction, projection below required transaction watermark or unavailable mandatory input is accepted as current.
  - **GREEN:** each input declares minimum revision/head, maximum staleness, authority and `REQUIRED|OPTIONAL|CONDITIONAL` policy; resolver records satisfied/unsatisfied requirement and may use `DATA-021` at execution-time barriers without hiding wait/degradation.
  - **REFACTOR:** one slow provider cannot block unrelated optional inputs; required consistency is a business contract rather than a global serializable read fiction.
  - **Refs:** [External observations](specs/integration-platform.md), [data-plane freshness](data/models/operations-production.md).

- [x] `SNAPSHOT-003` **[GATE_A][SOL_HIGH] Evaluate snapshot completeness without Boolean defaults.**
  - **Evidence (2026-09-05):** `TestTodo_SNAPSHOT_003` in `internal/engines/snapshot` (three-valued completeness evaluation of a resolved snapshot against a declared input specification (required, optional, conditional): SATISFIED/MISSING/UNKNOWN per input and overall, absent optional inputs never coerced to defaults, a required input of unknown presence refused by name rather than treated as false, a conditional whose condition is unknown evaluating to unknown, canonical digest, Explain, golden verdict table; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/engines/snapshot/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SNAPSHOT-002`, `MODEL-015`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SNAPSHOT_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SNAPSHOT_003`; `GOLDEN=TestTodo_SNAPSHOT_003_Golden`; `FAULT=TestTodo_SNAPSHOT_003_Fault`; `SECURITY=TestTodo_SNAPSHOT_003_Security`.
  - **RED:** missing/denied/stale/partial/conflicting input is omitted or coerced to complete; unauthorized caller learns a protected input through status detail.
  - **GREEN:** deterministic policy returns `COMPLETE`, `PARTIAL`, `STALE`, `UNKNOWN`, `DENIED`, or `RETRY_AT_BARRIER`, lists safe blockers/conditions and produces a canonical digest over exact resolved inputs and requirement dispositions.
  - **REFACTOR:** domain services decide whether a non-complete snapshot may simulate, require human work or block; snapshot engine never invents domain eligibility.
  - **Refs:** [Model conventions](data/models/modeling-conventions.md), [governance composition](specs/governance-decision-and-obligation-composition.md).

- [x] `PROMO-001` **[GATE_A][SOL_HIGH] Build the immutable Promotion input snapshot.**
  - **Evidence (2026-09-05):** `TestTodo_PROMO_001`, `TestTodo_PROMO_001_{Refusal,Conditional,Golden,Golden_Withheld,Security,Mutation,Integration}`, `BenchmarkTodo_PROMO_001` and per-file tests in `internal/domains/promotion/snapshot` (one immutable PromotionInputSnapshot of eight declared inputs — subject worker facts, current placement, manager chain, target capacity and conditional vacancy, current and desired pay-band position, budget availability — each an engine InputEntry with owner, authority, source, effective/known time, revision, watermark, freshness, classification, provenance and reference version, read only through the domains' authorized reads so WITHHELD inputs are present as WITHHELD; completeness by SNAPSHOT-003 with MISSING or UNKNOWN required inputs refused by name; the digest is the kernel's own material encoding over the inputs; pgtest integration over the harborcare-demo fixture proves two builds byte-identical, unchanged after an unrelated write, and moved by a base-pay supersession); `go test -count=1 ./internal/domains/promotion/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02. Follow-up: wire ProposePromotion's resolver in internal/intent/app/inputs.go to Build this snapshot and use snap.BaselineSnapshot() (revision keys change from subject ref to input name, so intent.Preflight's stale-baseline check must be re-read first), and carry the snapshot digest on ProposePromotionResponse.
  - **Depends:** `PROMO-007`, `SNAPSHOT-003`, `PEOPLE-001`–`COMP-003`, `BUDGET-001`, `MODEL-020`.
  - **INTENT CONTEXT:** `ROLE=COMPOSITE; SETS=BI.PEOPLE,BI.REWARDS,BI.WORK,BI.OPERATIONS; INTENTS=hcmnext.people.promote_worker/v1; FAMILY=CHANGE_REQUEST; CHILDREN=hcmnext.people.change_manager/v1,hcmnext.rewards.change_base_pay/v1,hcmnext.rewards.reserve_compensation_budget/v1,hcmnext.rewards.release_compensation_budget/v1; WHY=canonical promote-into-management vertical slice`.
  - **TEST:** `TestTodo_PROMO_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PROMO_001`; `GOLDEN=TestTodo_PROMO_001_Golden`; `SECURITY=TestTodo_PROMO_001_Security`; `BENCHMARK=BenchmarkTodo_PROMO_001`; `MUTATION=TestTodo_PROMO_001_Mutation`.
  - **RED:** snapshot omits worker/employment/assignment/org/manager/position/capacity/compensation/band/budget material read, source watermark, field authority, effective/known time, classification or reference version.
  - **GREEN:** one immutable `PromotionInputSnapshot` binds every source revision/observation and authority/provenance snapshot; identical authoritative inputs produce identical digest and any material change produces a new digest.
  - **REFACTOR:** snapshot references large evidence/artifacts instead of copying unrestricted payloads.
  - **Refs:** [Promotion fixture](reference-workflows/promote-into-management.md), [canonical digest](specs/canonical-envelope-and-digest.md).

- [x] `PROMO-002` **[GATE_A][SOL_HIGH] Simulate Assignment and organization changes.**
  - **Evidence (2026-09-05):** `TestTodo_PROMO_002` in `internal/domains/promotion/simassign` (kernel-pure simulation over a trusted PromotionInputSnapshot of the assignment revision (effective-dated with the prior assignment's end), the manager-relationship change with cycle refusal and depth bound, and the position occupancy transition carrying a validated POSITION-003 reservation request, each a typed proposed effect with reversibility, compensation and observation refs and the snapshot inputs it derives from, WITHHELD inputs refused rather than guessed, CompilePlan projections, digests pinned against the snapshot golden; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/promotion/simassign/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PROMO-001`, `ORG-002`, `POSITION-002`.
  - **INTENT CONTEXT:** `ROLE=COMPOSITE; SETS=BI.PEOPLE,BI.REWARDS,BI.WORK,BI.OPERATIONS; INTENTS=hcmnext.people.promote_worker/v1; FAMILY=CHANGE_REQUEST; CHILDREN=hcmnext.people.change_manager/v1,hcmnext.rewards.change_base_pay/v1,hcmnext.rewards.reserve_compensation_budget/v1,hcmnext.rewards.release_compensation_budget/v1; WHY=canonical promote-into-management vertical slice`.
  - **TEST:** `TestTodo_PROMO_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PROMO_002`; `PROPERTY=TestTodo_PROMO_002_Property`; `INTEGRATION=TestTodo_PROMO_002_Integration`; `MUTATION=TestTodo_PROMO_002_Mutation`.
  - **RED:** invalid manager cycle, position mismatch, overlap, closed org, unresolved identity or stale baseline is omitted from findings.
  - **GREEN:** result lists exact current/proposed assignment/relationship facts, normalized write footprint and blocking/warning findings with no mutation.
  - **REFACTOR:** simulation invokes domain capabilities; Workflow owns no domain rules.
  - **Refs:** [People domain](specs/people-employment-assignment-domain.md), [promotion flow](workflows/rewards/promotion-into-management.md).

- [x] `PROMO-003` **[GATE_A][SOL_HIGH] Simulate compensation and budget effects.**
  - **Evidence (2026-09-05):** `TestTodo_PROMO_003` in `internal/domains/promotion/simcomp` (kernel-pure simulation of the base-pay change in exact decimal from the snapshot's band-position inputs (no caller amounts), a typed BELOW/WITHIN/ABOVE band finding naming the approval it triggers, proration across the pay period at the effective date, and the budget reservation delta with insufficient-funds, currency, unit and date refusals, the reservation built but never issued, the pool's EXTERNAL_OBSERVATION authority making that effect non-local; a joint test proves the two simulations' effects equal the reference workflow's commit-boundary set; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/promotion/simcomp/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PROMO-001`, `COMP-002`, `COMP-003`, `BUDGET-001`.
  - **INTENT CONTEXT:** `ROLE=COMPOSITE; SETS=BI.PEOPLE,BI.REWARDS,BI.WORK,BI.OPERATIONS; INTENTS=hcmnext.people.promote_worker/v1; FAMILY=CHANGE_REQUEST; CHILDREN=hcmnext.people.change_manager/v1,hcmnext.rewards.change_base_pay/v1,hcmnext.rewards.reserve_compensation_budget/v1,hcmnext.rewards.release_compensation_budget/v1; WHY=canonical promote-into-management vertical slice`.
  - **TEST:** `TestTodo_PROMO_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PROMO_003`; `PROPERTY=TestTodo_PROMO_003_Property`; `GOLDEN=TestTodo_PROMO_003_Golden`; `RACE=TestTodo_PROMO_003_Race`; `MUTATION=TestTodo_PROMO_003_Mutation`.
  - **RED:** hidden bonus/payroll/finance effect, band violation, unknown budget or nondeterministic cost is reported as approved/valid.
  - **GREEN:** result contains exact old/new components, annual cost, variable exposure, band findings, budget sufficiency and calculation traces.
  - **REFACTOR:** no reservation or external write occurs in simulation mode.
  - **Refs:** [Compensation domain](specs/compensation-domain.md), [promotion reference](reference-workflows/promote-into-management.md).

- [x] `PROMO-004` **[GATE_A][SOL_HIGH] Assemble the full WorkflowSimulationContract.**
  - **Evidence (2026-09-05):** `TestTodo_PROMO_004` in `internal/domains/promotion/simcontract` (immutable SimulationResult binding intent and snapshot refs, proposal-candidate digest, planned write, effect, approval and obligation sets, findings and a derived two-valued status; Validate refuses a contract missing any of the twelve sections with a typed Refusal; SideEffect status is unexported and closed to SIMULATED_NOT_EXECUTED by construction (reflection and white-box security tests); Persist stores once by canonical digest and refuses tampered digests; zero-side-effect harness with failing fakes for revision store, external operations, outbox, MessageIntent sink and WorkItem store; Promotion and Manager Change goldens; the new domains to transaction and domains to data layer edges are port imports the dependency policy permits and the boundarytests golden was regenerated; written by a Sonnet subagent and verified independently); `go test -count=1 ./internal/domains/promotion/simcontract/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PROMO-002`, `PROMO-003`, `GOVERN-002`, `CONFLICT-001`, `CONFLICT-002`, `INTG-001`.
  - **INTENT CONTEXT:** `ROLE=COMPOSITE; SETS=BI.PEOPLE,BI.REWARDS,BI.WORK,BI.OPERATIONS; INTENTS=hcmnext.people.promote_worker/v1; FAMILY=CHANGE_REQUEST; CHILDREN=hcmnext.people.change_manager/v1,hcmnext.rewards.change_base_pay/v1,hcmnext.rewards.reserve_compensation_budget/v1,hcmnext.rewards.release_compensation_budget/v1; WHY=canonical promote-into-management vertical slice`.
  - **TEST:** `TestTodo_PROMO_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PROMO_004`; `GOLDEN=TestTodo_PROMO_004_Golden`; `RACE=TestTodo_PROMO_004_Race`; `SECURITY=TestTodo_PROMO_004_Security`; `MUTATION=TestTodo_PROMO_004_Mutation`.
  - **RED:** contract missing reads, writes, streams, conflicts, approvals, authority, legal obligations, side effects, cost, completion, revalidation or repair fails validation; simulation that creates a domain revision, external operation, outbox effect, MessageIntent or WorkItem fails zero-side-effect assertions.
  - **GREEN:** immutable `SimulationResult` binds intent/snapshot, proposal-candidate digest, planned write/effect/approval/obligation sets, findings and result status; complete deterministic artifact has canonical digest, persists once idempotently and marks every effect `SIMULATED/NOT_EXECUTED`.
  - **REFACTOR:** the Manager Change fixture reuses the same contract shape.
  - **Refs:** [Reference suite](reference-workflows/reference-suite.md), [Gate A evidence](plan.md#phase-1-changeops-overlay).

- [x] `POSITION-003` **[GATE_B][SOL_HIGH] Reserve a position for the exact proposal.**
  - **Evidence (2026-09-05):** `TestTodo_POSITION_003` in `internal/domains/position` (position reservation bound to an exact proposal revision digest and effective date, idempotent by natural key, refused at zero vacancy-after-date or incompatibility, released on rejection or supersession, with evidence and Explain; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/position/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `POSITION-002`, `RESERVE-001`, `TX-001`, `CONFLICT-001`, `CONFLICT-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_POSITION_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_POSITION_003`; `PROPERTY=TestTodo_POSITION_003_Property`; `GOLDEN=TestTodo_POSITION_003_Golden`; `RACE=TestTodo_POSITION_003_Race`; `BENCHMARK=BenchmarkTodo_POSITION_003`; `MUTATION=TestTodo_POSITION_003_Mutation`.
  - **RED:** two competing reservations win, expired/superseded hold executes, or changed proposal reuses the hold.
  - **GREEN:** one fenced reservation binds proposal digest, capacity interval and expiry; loser receives typed conflict; cancel/expiry releases exactly once.
  - **REFACTOR:** domain-owned typed reservation uses shared reservation protocol.
  - **Refs:** [Position contract](specs/position-and-headcount-domain.md), [conflict contract](specs/cross-workflow-conflict-and-write-intent.md).

- [x] `BUDGET-002` **[GATE_B][SOL_HIGH] Reserve Promotion compensation budget.**
  - **Evidence (2026-09-05):** `TestTodo_BUDGET_002` in `internal/domains/budget` (Promotion compensation budget reservation for the promote_worker intent with natural-key idempotency, release on rejection, insufficient-funds refusal and evidence; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/budget/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `BUDGET-001`, `RESERVE-001`, `TX-001`, `CONFLICT-001`, `CONFLICT-002`.
  - **INTENT CONTEXT:** `ROLE=DIRECT; SETS=BI.REWARDS; INTENTS=hcmnext.rewards.reserve_compensation_budget/v1,hcmnext.rewards.release_compensation_budget/v1; FAMILY=CHANGE_REQUEST; WHY=implement accepted intents 84 and 85 through one fenced reservation lifecycle`.
  - **TEST:** `TestTodo_BUDGET_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_BUDGET_002`; `RACE=TestTodo_BUDGET_002_Race`; `INTEGRATION=TestTodo_BUDGET_002_Integration`; `FAULT=TestTodo_BUDGET_002_Fault`; `SECURITY=TestTodo_BUDGET_002_Security`.
  - **RED:** competing/expired/stale-currency hold, ambiguous provider response or cancellation leak is treated as available/committed.
  - **GREEN:** lifecycle `REQUESTED -> HELD -> COMMITTED|RELEASED|EXPIRED` is fenced/idempotent; ambiguity creates reconciliation obligation.
  - **REFACTOR:** use co-located or adapter-backed authority selected by the partner, never pretend remote ACID.
  - **Refs:** [Budget authority](specs/workforce-budget-authority.md), [promotion workflow](reference-workflows/promote-into-management.md).

- [x] `PEOPLE-004` **[GATE_B][SOL_HIGH] Execute the bounded People/Assignment Promotion mutation.**
  - **Evidence (2026-09-05):** `TestTodo_PEOPLE_004` in `internal/domains/people` (the bounded people mutation for a promotion (job and level) is a governed write with append-only evidence (migration 00061) inside the local ACID commit; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/people/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PROMO-004`, `POSITION-003`, `APPROVAL-002`, `APPROVAL-005`, `GOVERN-003`, `TX-004`, `TX-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PEOPLE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PEOPLE_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PEOPLE_004`; `GOLDEN=TestTodo_PEOPLE_004_Golden`; `MUTATION=TestTodo_PEOPLE_004_Mutation`.
  - **RED:** unsupported employment creation/end, identity merge, undeclared field, stale stream or partial People/Assignment write aborts entirely.
  - **GREEN:** only approved job/level/assignment fields append successor facts with exact sequences, proposal digest, actor, authority and outbox evidence.
  - **REFACTOR:** domain package owns commands/events/projections; workflow cannot write tables.
  - **Refs:** [People domain](specs/people-employment-assignment-domain.md), [plane dependency rule](execution-plan.md#plane-dependency-rule).

- [x] `ORG-003` **[GATE_B][SOL_HIGH] Execute bounded manager/organization relationship changes.**
  - **Evidence (2026-09-05):** `TestTodo_ORG_003` in `internal/domains/org` (the manager-relationship rewrite for a promotion is a governed write with append-only evidence (migration 00062) inside the local ACID commit; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/org/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PEOPLE-004`, `ORG-002`, `TX-003`, `TX-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PEOPLE; DIRECT=none; WHY=supply manager-relationship mutation semantics to the promotion composite without publishing standalone ChangeManager authority`.
  - **TEST:** `TestTodo_ORG_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ORG_003`; `PROPERTY=TestTodo_ORG_003_Property`; `GOLDEN=TestTodo_ORG_003_Golden`; `MUTATION=TestTodo_ORG_003_Mutation`.
  - **RED:** cardinality/cycle/graph-watermark/authority conflict or projection disagreement commits no relationship fact.
  - **GREEN:** canonical relationship successor and affected streams/outbox commit atomically and projection watermark is observed before dependent use.
  - **REFACTOR:** changing manager does not mutate Person or copy manager IDs into unrelated aggregates.
  - **Refs:** [Organization domain](specs/organization-and-relationship-domain.md), [manager workflow](reference-workflows/manager-change.md).

- [x] `COMP-004` **[GATE_B][SOL_HIGH] Execute bounded base-pay and configured-bonus changes.**
  - **Evidence (2026-09-05):** `TestTodo_COMP_004` in `internal/domains/compensation` (the base-pay effective-interval write for a promotion is a governed write with append-only evidence (migration 00063) inside the local ACID commit; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/compensation/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PROMO-004`, `BUDGET-002`, `APPROVAL-002`, `APPROVAL-005`, `GOVERN-003`, `TX-004`, `TX-005`.
  - **INTENT CONTEXT:** `ROLE=DIRECT; SETS=BI.REWARDS; INTENTS=hcmnext.rewards.change_base_pay/v1; FAMILY=CHANGE_REQUEST; WHY=implement accepted intent 69 inside the bounded compensation authority`.
  - **TEST:** `TestTodo_COMP_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_COMP_004`; `PROPERTY=TestTodo_COMP_004_Property`; `MUTATION=TestTodo_COMP_004_Mutation`.
  - **RED:** unsupported component/currency, overlapping future revision, stale proposal, missing budget/approval/SoD or partial local write aborts.
  - **GREEN:** compensation successor facts, worker transaction evidence and outbox commit atomically with exact amounts/rounding/effective time.
  - **REFACTOR:** external payroll sync is a separate effect/observation, not local commit proof.
  - **Refs:** [Compensation domain](specs/compensation-domain.md), [Phase 1 depth matrix](plan.md#phase-1-implementation-depth-matrix).

- [x] `PROMO-005` **[GATE_B][SOL_HIGH] Prove the bounded cross-domain local Promotion commit.**
  - **Evidence (2026-09-05):** `TestTodo_PROMO_005` in `internal/domains/promotion/localcommit`, `internal/platform/execution/promotionsteps` (the bounded cross-domain local Promotion commit (people, organization, compensation) is proven on the Promotion fixture with a crash at every boundary leaving either the full commit or nothing, and execute_promotion consumes the prepared plan through an additive seam; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/promotion/localcommit/ ./internal/platform/execution/promotionsteps/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PEOPLE-004`, `ORG-003`, `COMP-004`.
  - **INTENT CONTEXT:** `ROLE=COMPOSITE; SETS=BI.PEOPLE,BI.REWARDS,BI.WORK,BI.OPERATIONS; INTENTS=hcmnext.people.promote_worker/v1; FAMILY=CHANGE_REQUEST; CHILDREN=hcmnext.people.change_manager/v1,hcmnext.rewards.change_base_pay/v1,hcmnext.rewards.reserve_compensation_budget/v1,hcmnext.rewards.release_compensation_budget/v1; WHY=canonical promote-into-management vertical slice`.
  - **TEST:** `TestTodo_PROMO_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PROMO_005`; `RACE=TestTodo_PROMO_005_Race`; `FAULT=TestTodo_PROMO_005_Fault`; `MUTATION=TestTodo_PROMO_005_Mutation`.
  - **RED:** fault injection at every participant/outbox append yields zero partial authoritative rows/events and leaves reservations safely resolvable.
  - **GREEN:** admitted co-located streams commit all-or-nothing under one `BusinessTransaction`; remote effects remain queued, not falsely atomic.
  - **REFACTOR:** participant set is generated from the immutable TransactionPlan.
  - **Refs:** [Transaction coordinator](specs/transaction-plan-and-commit-coordinator.md), [consistency boundary](data/models/kernel-governance-and-evidence.md).

- [x] `PROMO-006` **[CONFORMANCE][SOL_HIGH] Preserve Manager Change as the pure-HRIS boundary fixture.**
  - **Evidence (2026-09-05):** `TestTodo_PROMO_006` in `internal/workflow/conformance/managerchange` (internal/workflow/conformance/managerchange proves the manager-change slice of the promotion (org relationship rewrite) against the conformance fixture; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/workflow/conformance/managerchange/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PROMO-004`.
  - **INTENT CONTEXT:** `ROLE=COMPOSITE; SETS=BI.PEOPLE,BI.REWARDS,BI.WORK,BI.OPERATIONS; INTENTS=hcmnext.people.promote_worker/v1; FAMILY=CHANGE_REQUEST; CHILDREN=hcmnext.people.change_manager/v1,hcmnext.rewards.change_base_pay/v1,hcmnext.rewards.reserve_compensation_budget/v1,hcmnext.rewards.release_compensation_budget/v1; WHY=canonical promote-into-management vertical slice`.
  - **TEST:** `TestTodo_PROMO_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PROMO_006`; `CONFORMANCE=TestTodo_PROMO_006_Conformance`.
  - **RED:** fixture requires payroll/IAM/talent mutation to complete its authoritative manager fact or bypasses proposal/conflict/revalidation.
  - **GREEN:** expected terminal states are Runtime/Business `COMPLETED`, Reconciliation `PASSED`, Obligations `SATISFIED` for the single-domain mutation.
  - **REFACTOR:** reuse shared kernel without productizing standalone Manager Change unless scope-exchanged.
  - **Refs:** [Manager Change reference](reference-workflows/manager-change.md), [reference workflow scope](plan.md#75-reference-workflow-integration-suite).

- [ ] `PROMO-008` **[GATE_B][SOL_HIGH] Prove the canonical Promotion logical-persistence trace.**
  - **Depends:** `WF-RUN-023`–`WF-RUN-025`, `APPROVAL-007`, `DATA-021`, `EFFECT-001`, `RECON-002`, `REPAIR-002`, `INTENT-008`, `EVIDENCE-001`, `PROMO-005`.
  - **INTENT CONTEXT:** `ROLE=COMPOSITE; SETS=BI.PEOPLE,BI.REWARDS,BI.WORK,BI.OPERATIONS; INTENTS=hcmnext.people.promote_worker/v1; FAMILY=CHANGE_REQUEST; CHILDREN=hcmnext.people.change_manager/v1,hcmnext.rewards.change_base_pay/v1,hcmnext.rewards.reserve_compensation_budget/v1,hcmnext.rewards.release_compensation_budget/v1; WHY=canonical promote-into-management vertical slice`.
  - **TEST:** `TestTodo_PROMO_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PROMO_008`; `GOLDEN=TestTodo_PROMO_008_Golden`; `RACE=TestTodo_PROMO_008_Race`; `RECOVERY=TestTodo_PROMO_008_Recovery`; `MUTATION=TestTodo_PROMO_008_Mutation`.
  - **RED:** stage fixture observes a domain write/effect/message/work item before its permitted boundary, loses intent/proposal/workflow/transaction/effect lineage, overwrites an old domain revision, closes intent while external repair is required, or replays Promotion during IAM repair.
  - **GREEN:** deterministic T0…Tn trace asserts exact logical inserts/transitions and zero-forbidden-row counts from intent creation through snapshot, simulation, proposal, approvals, wait/revalidation, reservations, atomic local commit, external observations, degraded completion, repair and final closure.
  - **REFACTOR:** table names remain logical contracts; conformance drives public Go interfaces and generated schemas without coupling to one physical layout.
  - **Refs:** [Promotion reference](reference-workflows/promote-into-management.md), [transaction and repair](specs/transaction-ledger-reconciliation-and-repair.md), [workflow runtime](specs/workflow-runtime.md).
- [x] `PROMO-009` **[GATE_B][SOL_HIGH] Execute the promotion reference workflow itself end to end.**
  - **Depends:** `WF-RUN-027`, `WF-RUN-029`, `WF-RUN-030`, `WF-STEP-003`, `WF-STEP-004`, `PROMO-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.PEOPLE,BI.REWARDS; DIRECT=none; WHY=the shipped promotion reference, not a demo graph, must be the plan that executes under EXECUTE`.
  - **TEST:** `TestTodo_PROMO_009`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PROMO_009`; `GOLDEN=TestTodo_PROMO_009_Golden`; `INTEGRATION=TestTodo_PROMO_009_Integration`; `FAULT=TestTodo_PROMO_009_Fault`; `SECURITY=TestTodo_PROMO_009_Security`.
  - **RED:** the end-to-end harness compiles a hand-built TRANSFORM, APPROVAL, TASK graph while the reference remains simulate-only with capability and decision nodes and two approvals; or the reference's capability nodes are skipped under EXECUTE.
  - **GREEN:** the promotion reference gains an EXECUTE terminal profile and its APPROVAL nodes, its capability and decision nodes run through the capability gateway under EXECUTE, both approvals and the obligations are enforced, and the harness runs that definition from an approved proposal to COMPLETE.
  - **REFACTOR:** the demo definition is deleted or renamed as a step fixture.
  - **Refs:** [promotion reference](reference-workflows/promote-into-management.md), [review findings](devlog/2026-09-03-executable-prototype.md#9-review-findings-and-the-hardening-plan).
  - **Evidence (2026-09-06):** `TestTodo_PROMO_009`, `TestTodo_PROMO_009_Golden`, `TestTodo_PROMO_009_Integration`, `TestTodo_PROMO_009_Fault`, `TestTodo_PROMO_009_Security` in `test/workflow/promo009_test.go` supply the complete PRIMARY, GOLDEN, INTEGRATION, FAULT and SECURITY matrix over the shipped `promotionexec.Definition`, including both durable approvals, the effective-date timer, revalidation, capability execution, terminal idempotency and exactly one governed ledger fact. The `local-dev` profile now enables the bounded timer/ready-work scheduler with its executable plan, preventing a due-today promotion from remaining indefinitely parked. A live Codex-browser run completed OPS-HRBP2/P2 → OPS-HRBP3/P3 from proposal through finance and manager approval, settled `wait_effective_date`, reached `end_complete`, and displayed the single `hcmnext.workflow.PromotionOutcome/v2` ledger record. `go test ./test/workflow -count=1` and `npm run check:driftgate` PASS on Windows (Go 1.26.3).

- [x] `PROMO-010` **[GATE_A][SOL_HIGH] Pin a Promotion execution behind the commercial entitlement snapshot.**
  - **Depends:** `COMM-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PEOPLE,BI.COMMERCIAL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestPromotionEntitlementGatePinsAllowedExecutionAndRefusesOutOfScope`.
  - **TEST MATRIX:** `PRIMARY=TestPromotionEntitlementGatePinsAllowedExecutionAndRefusesOutOfScope`; `GOLDEN=TestPromotionEntitlementGatePinsAllowedExecutionAndRefusesOutOfScope`; `SECURITY=TestPromotionEntitlementGateRefusesSuspendedAndExpiredSnapshots`.
  - **RED:** a Promotion capability outside the tenant's resolved entitlement snapshot is admitted, a suspended or expired snapshot still admits, or the decision is re-resolved after the execution was already accepted so a later snapshot change silently alters an in-flight run.
  - **GREEN:** `PromotionEntitlementGate` wraps `internal/commercial`'s channel-neutral resolver (`COMM-001`) with a Promotion-specific admission adapter; `Admit` pins an immutable `PromotionEntitlementBinding` fingerprint to the accepted execution rather than re-resolving after amendment, and a denial projects into the shared transport envelope with the fingerprint as evidence but no contract detail.
  - **REFACTOR:** every Promotion initiation and resume adapter shares the one `ReasonPromotionEntitlementRequired` refusal and rule ref instead of inventing a second entitlement policy.
  - **Refs:** [Commercial entitlement](plan.md#312-portfolio-discipline), [promotion reference](reference-workflows/promote-into-management.md).
  - **Evidence (2026-09-06):** `TestPromotionEntitlementGatePinsAllowedExecutionAndRefusesOutOfScope`, `TestPromotionEntitlementGateRefusesSuspendedAndExpiredSnapshots` in `internal/intent/app`; `go test -count=1 ./internal/intent/app/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `PROMO-011` **[GATE_B][SOL_HIGH] Require step-up or a distinct additional approval before a Promotion expands sensitive access.**
  - **Depends:** `TRUST-004`, `AUTHN-005`, `PROMO-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PEOPLE,BI.SECURITY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestPromotionExecutionRequiresStepUpOrAdditionalApprovalForSensitiveAccess`.
  - **TEST MATRIX:** `PRIMARY=TestPromotionExecutionRequiresStepUpOrAdditionalApprovalForSensitiveAccess`; `SECURITY=TestPromotionExecutionDoesNotRequireSensitiveAccessControlForUnchangedScopes`.
  - **RED:** a promotion whose proposed access scopes add `direct_reports` or `compensation.direct_reports.read` beyond the worker's existing scopes executes without a satisfied step-up obligation or a second, proposal-bound approval from a distinct principal; or an unrelated promotion that changes no access scope is wrongly blocked by the same control.
  - **GREEN:** `AdmitPromotionExecution` detects a sensitive-access expansion and refuses execution with `ReasonPromotionSensitiveAccessStepUpOrApproval` unless satisfied by a `TRUST-004` step-up obligation or one distinct additional approval bound to the same proposal digest and requirement ref from a principal other than the one who approved the base promotion; a promotion whose scopes are unchanged never triggers the control.
  - **REFACTOR:** keep the access-expansion detection and the step-up/approval satisfaction check as one pure gate the execution driver calls, independent from the currency/authority recheck `APPROVAL-005` already performs.
  - **Refs:** [Step-up obligations](specs/organization-scope-and-authz.md), [promotion reference](reference-workflows/promote-into-management.md).
  - **Evidence (2026-09-06):** `TestPromotionExecutionRequiresStepUpOrAdditionalApprovalForSensitiveAccess`, `TestPromotionExecutionDoesNotRequireSensitiveAccessControlForUnchangedScopes` in `internal/intent/app`; `go test -count=1 ./internal/intent/app/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `PROMO-012` **[GATE_B][SOL_HIGH] Resume a fired Promotion timer into the intent it belongs to through the ledger correlation index.**
  - **Depends:** `WF-RUN-004`, `SVC-004`, `INTENT-007`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PEOPLE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PROMO_EXEC_TIMERDISPATCH_TimerDispatchDisposition`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PROMO_EXEC_TIMERDISPATCH_TimerDispatchDisposition`; `SECURITY=TestTodo_PROMO_EXEC_TIMERDISPATCH_TimerDispatcherRejectsForeignTenant`; `INTEGRATION=TestTodo_PROMO_EXEC_TIMERDISPATCH_WithResumeTenant`.
  - **RED:** a scheduler work item for a fired WAIT timer cannot be traced back to the intent that started it because the parked workflow instance carries no work item, so the dispatch is dropped, retried forever, misclassified as abandoned, or crosses into another tenant's instance; or `ResumeFiredTimer` proceeds against an instance that is neither `RUNNING` nor `WAITING`.
  - **GREEN:** `pgstore.LoadIntentByCorrelation` resolves the owning intent from `ledger_event.correlation_id` on the intent's own `intent:<id>` stream (the one durable place the correlation is recorded today), and `Cell.ResumeFiredTimer` reconstructs the same approved `StartRequest` `ExecuteIntent` used and resumes the parked WAIT through the executor's durable timer path only when the instance is `RUNNING` or `WAITING`; `internal/application`'s `timerDispatcher`/`timerDispatchDisposition` wire that resolution into `internal/platform/execution/scheduler`'s `Dispatcher`, mapping the result to `DispositionCompleted`/`DispositionRetry`/`DispositionAbandoned` and refusing a work item whose tenant does not match the dispatch context.
  - **REFACTOR:** keep correlation-based intent lookup additive to `intent_instance_context` rather than a second place the correlation is written.
  - **Refs:** [Durable workflow timers](#8-durable-workflow-runtime-recovery-and-intervention), [scheduler](specs/platform-architecture-catalog.md).
  - **Evidence (2026-09-06):** `TestTodo_PROMO_EXEC_TIMERDISPATCH_TimerDispatchDisposition`, `TestTodo_PROMO_EXEC_TIMERDISPATCH_TimerDispatcherRejectsForeignTenant` in `internal/application`; `TestTodo_PROMO_EXEC_TIMERDISPATCH_WithResumeTenant` in `internal/intent/app`; `TestTodo_PROMO_EXEC_TIMERDISPATCH_CorrelationReaderIsAdditive` in `internal/intent/app/pgstore`; `go test -count=1 ./internal/application/ ./internal/intent/app/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `PROMO-013` **[GATE_B][SOL_HIGH] Prove the flat, digest-validated Promotion commit command.**
  - **Depends:** `PROMO-005`.
  - **INTENT CONTEXT:** `ROLE=COMPOSITE; SETS=BI.PEOPLE,BI.REWARDS,BI.WORK,BI.OPERATIONS; INTENTS=hcmnext.people.promote_worker/v1; FAMILY=CHANGE_REQUEST; CHILDREN=hcmnext.people.change_manager/v1,hcmnext.rewards.change_base_pay/v1,hcmnext.rewards.reserve_compensation_budget/v1,hcmnext.rewards.release_compensation_budget/v1; WHY=canonical promote-into-management vertical slice`.
  - **TEST:** `TestCommandValidatesTheBoundedPromotion`.
  - **TEST MATRIX:** `PRIMARY=TestCommandValidatesTheBoundedPromotion`; `SECURITY=TestCommandRefusesMissingApprovalAndManagerCycle`; `CONFORMANCE=TestCommandRefusesAPlanThatDropsAParticipant`.
  - **RED:** the command is missing a required identifier or expected-baseline digest, carries fewer than two distinct approval decision ids or no task submission id, names the worker as its own manager or an existing manager ancestor, presents a plan participant set that drops or substitutes one of the four fixed local writers, or declares a duplicate or incomplete external effect.
  - **GREEN:** `commit.Command.Validate` fails closed on every required field and expected digest, requires two distinct `ApprovalDecisionIDs` and at least one `TaskSubmissionIDs` entry, refuses a manager-relationship cycle, and `ValidateParticipants` proves the plan materializes exactly `people.assignment`, `position.occupancy`, `rewards.compensation` and `rewards.budget_reservation`; `promotionterminal.BindResolution` binds the same command to an admitted, digest-verified `TransactionPlan` resolution and `promotioncommit`'s data-plane writer persists it inside the caller-owned transaction.
  - **REFACTOR:** business validation stays in `internal/domains/promotion/commit`; `internal/platform/execution/promotionterminal` and `internal/data/promotioncommit` remain storage/terminal-composition adapters that add no new promotion policy.
  - **Refs:** [Transaction coordinator](specs/transaction-plan-and-commit-coordinator.md), [consistency boundary](data/models/kernel-governance-and-evidence.md).
  - **Evidence (2026-09-06):** `TestCommandValidatesTheBoundedPromotion`, `TestCommandRefusesMissingApprovalAndManagerCycle`, `TestCommandRefusesAPlanThatDropsAParticipant` in `internal/domains/promotion/commit`; `go test -count=1 ./internal/domains/promotion/commit/ ./internal/platform/execution/promotionterminal/ ./internal/data/promotioncommit/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

---

## 6. BusinessIntent, capabilities, governance and transaction integrity

> **Disposition (2026-09-02):** P1A: `CAP-001`–`CAP-003` under the registry `BOOTSTRAP` profile, `INTENT-001`–`INTENT-006` with five dimensions and the materiality rule, `GOVERN-001`/`002`, `CONFLICT-001`/`002`, `APPROVAL-001`/`002` demonstrated, `TX-001` non-executable plans. P1B: the remaining `GOVERN`, `CONFLICT`, `APPROVAL`, `REPLAN`, `TX` items. `FEATURE-001`/`002` and `INTENT-009`–`INTENT-011`, `INTENT-024`, `INTENT-025` (feature-vocabulary intake) are DEFERRED; there is no funded consumer. `INTENT-019` is a population-scoped `CHANGE_REQUEST`, not a family.

- [x] `CAP-001` **[GATE_A][SOL_LOW] Implement immutable CapabilityDefinition versions.**
  - **Depends:** `MODEL-017`, `TOOL-007`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CAP_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CAP_001`; `GOLDEN=TestTodo_CAP_001_Golden`; `RACE=TestTodo_CAP_001_Race`; `SECURITY=TestTodo_CAP_001_Security`.
  - **RED:** publication rejects missing owner/schema/risk/effect/idempotency/AuthZ/legal/entitlement/SLO/test or unresolved implementation binding.
  - **GREEN:** read/query/simulate Phase 1 capabilities publish with stable IDs and exact versions; historical versions resolve after deprecation.
  - **REFACTOR:** UI/agent/connector discover the same registry.
  - **Refs:** [Capability lifecycle](specs/capability-registry-and-lifecycle.md), [capability principle](plan.md#59-every-product-action-is-a-governed-capability).
  - **Evidence (2026-09-03):** `TestTodo_CAP_001`, `TestTodo_CAP_001_Golden`, `TestTodo_CAP_001_Race`, `TestTodo_CAP_001_Security` in `internal/capability`; `go test -count=1 ./internal/capability/...` PASS on windows/arm64 (Go 1.26.3); compiled-in BOOTSTRAP table, immutable versions, deterministic listing; branch plan-revision-2026-09-02.

- [x] `CAP-002` **[GATE_A][SOL_HIGH] Implement the governed capability gateway.**
  - **Depends:** `CAP-001`, `TRUST-011`, `GOVERN-001`, `GOVERN-002`, `GOVERN-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CAP_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CAP_002`; `GOLDEN=TestTodo_CAP_002_Golden`; `SECURITY=TestTodo_CAP_002_Security`; `MUTATION=TestTodo_CAP_002_Mutation`.
  - **RED:** unknown/quarantined/retired capability, schema mismatch, unauthorized scope, legal unknown, entitlement deny or unsupported mode invokes no handler.
  - **GREEN:** gateway resolves exact version, validates typed request/context, composes governance and returns typed result/evidence IDs.
  - **REFACTOR:** every channel shares this path; no UI/agent/admin bypass.
  - **Refs:** [Plane model](specs/platform-plane-model.md), [capability registry](specs/capability-registry-and-lifecycle.md).
  - **Evidence (2026-09-03):** `TestTodo_CAP_002`, `TestTodo_CAP_002_Golden`, `TestTodo_CAP_002_Security`, `TestTodo_CAP_002_Mutation` in `internal/capability`; `go test -count=1 ./internal/capability/...` PASS; gateway refuses write-effect capabilities in P1A, records invocation evidence via `EvidenceSink`, typed errors; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `CAP-003` **[GATE_A][TERRA] Normalize capability errors across transports.**
  - **Depends:** `CAP-002`, `TOOL-008`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CAP_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CAP_003`; `GOLDEN=TestTodo_CAP_003_Golden`; `FUZZ=FuzzTodo_CAP_003`; `INTEGRATION=TestTodo_CAP_003_Integration`; `FAULT=TestTodo_CAP_003_Fault`.
  - **RED:** gRPC/HTTP/Go client return different semantic code, retryability, field paths or correlation/evidence for the same failure.
  - **GREEN:** canonical error envelope is identical in meaning and redaction across channels.
  - **REFACTOR:** provider/internal diagnostics remain nested and access-controlled.
  - **Refs:** [Transport strategy](plan.md#16-strategic-decisions), [canonical envelope](specs/canonical-envelope-and-digest.md).
  - **Evidence (2026-09-03):** `TestTodo_CAP_003`, `TestTodo_CAP_003_Golden`, `FuzzTodo_CAP_003` in `internal/transport/envelope`; `TestTodo_CAP_003_Integration`, `TestTodo_CAP_003_Fault` in `internal/transport/edge`; `go test -count=1 ./internal/transport/...` PASS; one typed `envelope.Error` projected identically to gRPC status details and the connect-go HTTP edge, no raw internal error text leaks; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `INTENT-001` **[GATE_A][SOL_LOW] Implement IntentDefinition resolution.**
  - **Depends:** `MODEL-010`, `CAP-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_INTENT_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_INTENT_001`; `GOLDEN=TestTodo_INTENT_001_Golden`.
  - **RED:** free-form name, unqualified ID, wrong version, catalogued-only or retired definition cannot instantiate.
  - **GREEN:** exact published `definition_ref` resolves schemas, family, phase, risk and capabilities.
  - **REFACTOR:** rename display labels without changing identity.
  - **Refs:** [Business intent catalog](specs/business-intent-catalog.md), [intent kernel](plan.md#61-business-intent-and-transaction-kernel).
  - **Evidence (2026-09-03):** `TestTodo_INTENT_001`, `_Golden` in `internal/intent` (`Registry.Resolve/ResolveText/ResolveForInstantiation`); `go test -count=1 ./internal/intent/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `INTENT-002` **[GATE_A][SOL_HIGH] Create typed IntentInstance envelopes.**
  - **Depends:** `INTENT-001`, `MODEL-006`, `TRUST-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_INTENT_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_INTENT_002`; `GOLDEN=TestTodo_INTENT_002_Golden`; `FUZZ=FuzzTodo_INTENT_002`; `RACE=TestTodo_INTENT_002_Race`; `SECURITY=TestTodo_INTENT_002_Security`; `MUTATION=TestTodo_INTENT_002_Mutation`.
  - **RED:** missing tenant/org/purpose/initiator/definition/idempotency/correlation/classification/control snapshots or untyped JSON payload is rejected.
  - **GREEN:** valid Protobuf request creates UUIDv7 instance, canonical request digest and immutable creation evidence.
  - **REFACTOR:** only mutation/effect intents later create BusinessTransaction.
  - **Refs:** [Intent instance envelope](specs/business-intent-catalog.md#instance-envelope), [BusinessIntent contract](specs/business-intent-and-change-request.md).
  - **Evidence (2026-09-03):** `TestTodo_INTENT_002`, `_Golden`, `_Race`, `_Security`, `_Mutation`, `FuzzTodo_INTENT_002` in `internal/intent` (typed `Instance` envelope, server-supplied `PrincipalReference`, idempotency digest via `hcmnext.idempotent_request` v1 through `protomap.Digester`); `go test -count=1 ./internal/intent/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `INTENT-003` **[GATE_A][SOL_HIGH] Implement the five-dimension intent lifecycle with the fixed legality rules.**
  - **Depends:** `INTENT-002`, `MODEL-014`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_INTENT_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_INTENT_003`; `SECURITY=TestTodo_INTENT_003_Security`; `MUTATION=TestTodo_INTENT_003_Mutation`.
  - **RED:** an illegal tuple under the six kernel rules (for example `CANCELLED` entering `EXECUTING`, `APPROVED` without a binding for the current revision, `COMMITTED` without a receipt, `CLOSED` with `ObligationState=PENDING`), a collapsed universal status, a sixth dimension, or loss of a prior dimension returns a typed violation with no mutation.
  - **GREEN:** `RequestState`, `ExecutionState`, `BusinessState`, `ConsistencyState` and `ObligationState` append independently; proposal revisions, approval bindings, closure records, incidents and outcome tracking are linked records, not dimensions; P1A never leaves `ExecutionState=NOT_PLANNED`.
  - **REFACTOR:** the legality rules are one Go function shared by command, projection rebuild, replay and repair; there is no per-definition compatibility profile.
  - **Refs:** [Five dimensions](specs/business-intent-and-change-request.md#five-lifecycle-dimensions), [change lifecycle](plan.md#63-change-request-lifecycle), [lifecycle budget](plan.md#63a-lifecycle-budget).
  - **Evidence (2026-09-03):** `TestTodo_INTENT_003`, `_Security`, `_Mutation` in `internal/intent` with the six fixed legality rules in `internal/intent/lifecycle/rules.go` and `Machine.Apply/Replay`; `go test -count=1 ./internal/intent/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `INTENT-004` **[GATE_A][SOL_HIGH] Implement HCMChangeRequest draft and preflight.**
  - **Depends:** `INTENT-003`, `PROMO-001`, `MODEL-024`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_INTENT_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_INTENT_004`; `GOLDEN=TestTodo_INTENT_004_Golden`; `MUTATION=TestTodo_INTENT_004_Mutation`.
  - **RED:** unknown subject/reference, forbidden field, invalid effective date, stale baseline or missing required data cannot enter simulation as valid.
  - **GREEN:** preflight returns typed `READY`, `NEEDS_DATA`, `BLOCKED`, or `DENIED`, findings and exact input snapshot.
  - **REFACTOR:** no employee mutation or external effect in preflight.
  - **Refs:** [ChangeRequest contract](specs/business-intent-and-change-request.md), [change lifecycle](plan.md#63-change-request-lifecycle).
  - **Evidence (2026-09-03):** `TestTodo_INTENT_004`, `_Golden`, `_Mutation` in `internal/intent` (`Draft`, `Preflight` through the `Preflighter` port; non-empty `DeclaredEffects` fails with `ErrEffectInPreflight`); domain half in `internal/domains/promotion` (`TestPreflightPromotion*`, seven ported legacy scenarios); `go test -count=1 ./internal/intent/... ./internal/domains/...` PASS. Wiring the kernel port to the promotion package is tracked under NEXT-004/NEXT-005; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `INTENT-005` **[GATE_A][SOL_HIGH] Create immutable ProposalRevision artifacts.**
  - **Depends:** `INTENT-004`, `PROMO-004`, `MODEL-007`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_INTENT_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_INTENT_005`; `GOLDEN=TestTodo_INTENT_005_Golden`; `MUTATION=TestTodo_INTENT_005_Mutation`.
  - **RED:** in-place edit, caller-provided digest, missing control/source/reference versions or hidden material child intent is rejected.
  - **GREEN:** each revision contains exact current/proposed state, child set, writes/effects/approvals/obligations/cost/revalidation and canonical digest.
  - **REFACTOR:** large input/evidence uses immutable refs.
  - **Refs:** [Approval binding principle](plan.md#53-approval-must-bind-to-an-immutable-proposal), [canonical digest](specs/canonical-envelope-and-digest.md).
  - **Evidence (2026-09-03):** `TestTodo_INTENT_005`, `_Golden`, `_Mutation` in `internal/intent` (`NewProposalRevision`, `ProposalLedger.Append`, digest via `hcmnext.proposal` v1, control snapshots revalidated not hashed, material bytes encoding in `materialbytes.go`); zero-effect receipt in `internal/domains/evidence`; `go test -count=1 ./internal/intent/...` PASS. Open: a `MaterialProposal` proto message should replace the kernel encoding (recorded in `internal/intent/materialbytes.go`); branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `INTENT-006` **[GATE_A][SOL_HIGH] Invalidate approvals on material proposal change and only then.**
  - **Depends:** `INTENT-005`, `APPROVAL-001`, `APPROVAL-002`, `APPROVAL-003`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_INTENT_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_INTENT_006`; `MUTATION=TestTodo_INTENT_006_Mutation`.
  - **RED:** changing amount, field, effective date, child set, reservation, required approvals or a source-authority decision leaves a prior approval valid; **or** republishing a policy bundle, taxonomy or reference dataset invalidates an approval whose material result is unchanged.
  - **GREEN:** a new material revision records invalidators and the bindings for the old digest become invalid; a control-snapshot change triggers revalidation and produces exactly one of `APPROVAL_STANDS` (recorded), `NEW_REVISION` (material result changed) or `BLOCKED` (mandatory deny); a compensation cycle with a thousand pending approvals survives a policy republish with zero spurious invalidations.
  - **REFACTOR:** materiality is the `PROPOSAL` canonicalization profile's material list; nothing else decides.
  - **Refs:** [Materiality rule](specs/business-intent-and-change-request.md#materiality-rule-for-control-snapshots), [proposal material context](specs/canonical-envelope-and-digest.md#proposal-material-context).
  - **Evidence (2026-09-03):** `TestTodo_INTENT_006`, `_Mutation` in `internal/intent/approval` (`Assess`: APPROVAL_STANDS / NEW_REVISION / BLOCKED from the material-field set; control-snapshot revalidation is immaterial); kernel `intent.MaterialEqual` now also neutralises `SupersedesRevisionID` so lineage alone is never material; PASS; branch plan-revision-2026-09-02; `go test -count=1 ./internal/intent/approval/...` on windows/arm64 (Go 1.26.3).

- [x] `INTENT-007` **[GATE_B][SOL_HIGH] Bind workflow outcome dimensions back to the Intent lifecycle.**
  - **Evidence (2026-09-05):** `TestTodo_INTENT_007` in `internal/intent` (the workflow terminal's five lifecycle dimensions and the RECON-002 verdict are bound back onto the intent as one OutcomeReceipt, idempotent for the identical tuple, refusing a conflicting or stale-revision terminal; the receipt now also carries the commit receipt and repair references the lifecycle rules require, persisted by migration 00069; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/intent/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `INTENT-003`, `WF-RUN-025`, `RECON-002`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_INTENT_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_INTENT_007`; `RACE=TestTodo_INTENT_007_Race`; `MUTATION=TestTodo_INTENT_007_Mutation`.
  - **RED:** workflow runtime completion overwrites Business/Consistency/Obligation dimensions, duplicate terminal signal appends conflicting state, or stale workflow instance updates a superseding intent revision.
  - **GREEN:** one idempotent correlation contract appends each independent dimension from authoritative workflow/transaction/reconciliation evidence and preserves `Business=COMPLETED` with `ConsistencyState=DEGRADED` and `Reconciliation=REPAIR_REQUIRED` when applicable.
  - **REFACTOR:** intent consumes typed outcome receipts rather than reading workflow tables directly.
  - **Refs:** [Independent lifecycle dimensions](specs/business-intent-catalog.md#lifecycle-dimensions), [workflow completion](specs/workflow-runtime.md).

- [x] `INTENT-008` **[GATE_B][SOL_HIGH] Close an Intent only under its explicit completion policy.**
  - **Evidence (2026-09-05):** `TestTodo_INTENT_008` in `internal/intent` (an intent closes only under its definition's completion policy (business complete, consistency at or above the floor, no open obligation or repair) with a typed refusal naming the unmet dimension; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/intent/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `INTENT-007`, `RECON-002`, `REPAIR-002`, `MODEL-014`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_INTENT_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_INTENT_008`; `INTEGRATION=TestTodo_INTENT_008_Integration`; `MUTATION=TestTodo_INTENT_008_Mutation`.
  - **RED:** runtime `COMPLETED`, provider acceptance, stale observation, unresolved mandatory obligation, open required repair or unknown terminal dimension silently closes the intent.
  - **GREEN:** versioned completion policy returns `CLOSED`, `CLOSED_DEGRADED`, `OPEN_REPAIR`, `BLOCKED_OBLIGATION`, or `UNKNOWN`; closure appends exact satisfied/waived/deferred dimensions, evidence refs, owner and reopen conditions.
  - **REFACTOR:** closure never rewrites workflow, transaction, observation or repair history.
  - **Refs:** [Outcome management](plan.md#63-change-request-lifecycle), [repair semantics](specs/transaction-ledger-reconciliation-and-repair.md).

- [x] `FEATURE-001` **[DESIGN][TERRA] Check in the exact 49-group feature-to-intent intake manifest.**
  - **Evidence (2026-09-05):** `TestFeatureIntentSourceManifest`, `TestTodo_FEATURE_001_{Golden,Race,Fault}` in `tools/planning/intentmanifests` over the published `definitions/governance/feature-intent-intake.yaml` (49 groups, no duplicates, every mapped intent id resolves or is DEFERRED/MISSING, canonical digest); `go test -count=1 ./tools/planning/intentmanifests/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `GOV-008`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestFeatureIntentSourceManifest`.
  - **TEST MATRIX:** `PRIMARY=TestFeatureIntentSourceManifest`; `GOLDEN=TestTodo_FEATURE_001_Golden`; `RACE=TestTodo_FEATURE_001_Race`; `FAULT=TestTodo_FEATURE_001_Fault`.
  - **RED:** `TestFeatureIntentSourceManifest` fails when any supplied group 1–49, feature label, explanatory qualifier, example flow or architectural rule is missing, reordered ambiguously, silently deduplicated or lacks source provenance and canonical digest.
  - **GREEN:** immutable intake source preserves every named universal experience, domain, platform, automation, bulk, repair, operational and higher-order feature exactly once within its supplied group, plus the governing material-feature rule and original source digest.
  - **REFACTOR:** retain the original intake beside normalized feature records so later alias/convergence work never rewrites source history.
  - **Refs:** [Business intent catalog](specs/business-intent-catalog.md), [coverage rule](data/models/README.md#coverage-rule).

- [x] `FEATURE-002` **[DESIGN][SOL_HIGH] Normalize feature identities, aliases and semantic classifications without losing the intake.**
  - **Evidence (2026-09-05):** `TestFeatureNormalizationRejectsFalseMergeOrDuplicateSemantics`, `TestTodo_FEATURE_002_{Golden,Race,Security}` in `tools/planning/intentmanifests` (closed semantic classification vocabulary, canonical feature identity with an alias table, exact intake label/group/source ref preserved on every normalized record, duplicate ids, ambiguous cross-domain aliases and UI-wording identities refused); `go test -count=1 ./tools/planning/intentmanifests/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02. `TestTodo_INTENT_009_RealCatalog` additionally runs the normalizer and classifier over the real `definitions/governance/{feature-intent-intake,intent-conformance-descriptors}.yaml` (49 groups, 253 features, 14 intents, 61 material features in the DEFERRED review queue); it exposed a duplicate feature id across intake groups 12 and 32, fixed in the intake as `manager_performance_review_conduct`.
  - **Depends:** `FEATURE-001`, `GOV-012`, `MODEL-009`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestFeatureNormalizationRejectsFalseMergeOrDuplicateSemantics`.
  - **TEST MATRIX:** `PRIMARY=TestFeatureNormalizationRejectsFalseMergeOrDuplicateSemantics`; `GOLDEN=TestTodo_FEATURE_002_Golden`; `RACE=TestTodo_FEATURE_002_Race`; `SECURITY=TestTodo_FEATURE_002_Security`.
  - **RED:** `TestFeatureNormalizationRejectsFalseMergeOrDuplicateSemantics` fails when same label in different domains is merged incorrectly, UI wording becomes a new semantic identity, one feature maps to multiple unqualified actions, material operation is classified as display mechanics or normalized output cannot trace to exact intake entry.
  - **GREEN:** every intake entry receives stable qualified `feature_id`, canonical label, aliases, group/domain/actor/channel hints and provisional `CREATE|CONSUME|EMIT_CHILD|OBSERVE|NON_MATERIAL|REVIEW_REQUIRED` classification with lossless source links.
  - **REFACTOR:** normalization does not decide implementation maturity or force an IntentDefinition mapping; those remain coverage/convergence outputs.
  - **Refs:** [Canonical terminology](data/models/registry-and-coverage-contracts.md), [intent coverage matrix](data/models/intent-coverage-matrix.md).

- [x] `INTENT-009` **[DESIGN][SOL_HIGH] Define the universal material-feature-to-BusinessIntent rule.**
  - **Evidence (2026-09-05):** `TestMaterialFeatureIntentClassification`, `TestTodo_INTENT_009_{Golden,Integration,Fault,Security,Mutation}` in `tools/planning/intentmanifests` (closed feature-intent role vocabulary; every MATERIAL feature must bind to exactly one catalog BusinessIntent, static rendering and transport/health mechanics refused as business intents, uncertain classifications marked for review, unbound and non-catalog bindings reported); `go test -count=1 ./tools/planning/intentmanifests/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02. `TestTodo_INTENT_009_RealCatalog` proves the rule over the real intake and catalog (253 features, 14 intents, 61 unbound material features queued); the intake's `DEFERRED` sentinel is now an explicit unbound binding rather than a catalog lookup.
  - **Depends:** `INTENT-001`, `CAP-001`, `GOV-012`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestMaterialFeatureIntentClassification`.
  - **TEST MATRIX:** `PRIMARY=TestMaterialFeatureIntentClassification`; `GOLDEN=TestTodo_INTENT_009_Golden`; `INTEGRATION=TestTodo_INTENT_009_Integration`; `FAULT=TestTodo_INTENT_009_Fault`; `SECURITY=TestTodo_INTENT_009_Security`; `MUTATION=TestTodo_INTENT_009_Mutation`.
  - **RED:** `TestMaterialFeatureIntentClassification` rejects a feature that asks, proposes, changes, calculates, processes, files, investigates, communicates, repairs, plans, schedules or answers a material HCM matter without a typed IntentDefinition; it also rejects treating static rendering, transport health or an internal retry mechanic as a new business intent without semantic purpose.
  - **GREEN:** a versioned classifier returns exactly `INTENT_CREATOR|INTENT_CONSUMER|CHILD_INTENT_EMITTER|INTENT_OBSERVER|NON_MATERIAL_MECHANIC`, with materiality, business purpose, subject, authority/effect/result significance, owner and rationale; uncertain classification is `REVIEW_REQUIRED`, never implicit bypass.
  - **REFACTOR:** BusinessIntent is the semantic instruction boundary, not a wrapper around every function call or UI click.
  - **Refs:** [BusinessIntent contract](specs/business-intent-and-change-request.md), [business intent catalog](specs/business-intent-catalog.md), [capability parity](plan.md#59-every-product-action-is-a-governed-capability).

- [x] `INTENT-010` **[DESIGN][SOL_HIGH] Create the machine-readable FeatureIntentCoverage registry.**
  - **Evidence (2026-09-05):** `TestFeatureIntentCoverageRejectsImplicitFeature` in `tools/planning/intentmanifests`, `tools/planning/cmd/featurecoverage` (machine-readable FeatureIntentCoverage registry built from the real intake and catalog, one record per feature with group, canonical identity, classification, role, bound intent or DEFERRED and disposition under a stable digest, generated into definitions/governance/feature-intent-coverage.yaml by cmd/featurecoverage with a drift test; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/planning/intentmanifests/ ./tools/planning/cmd/featurecoverage/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `FEATURE-002`, `INTENT-009`, `TOOL-004`, `MODEL-010`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestFeatureIntentCoverageRejectsImplicitFeature`.
  - **TEST MATRIX:** `PRIMARY=TestFeatureIntentCoverageRejectsImplicitFeature`; `GOLDEN=TestTodo_INTENT_010_Golden`; `FUZZ=FuzzTodo_INTENT_010`; `FAULT=TestTodo_INTENT_010_Fault`; `SECURITY=TestTodo_INTENT_010_Security`; `MUTATION=TestTodo_INTENT_010_Mutation`.
  - **RED:** `TestFeatureIntentCoverageRejectsImplicitFeature` fails for any universal experience, self-service, manager, HR administration, domain, connectivity, DataOps, configuration, rules, population, program, agent, intelligence, search, schedule, event, bulk, repair, operations, commercial, sandbox or higher-order feature without stable feature ID, owner, phase/depth, classification, actor/channel, subject/resource, IntentDefinition/family, capability/version, input/result schema, parent/child behavior, governance profile and evidence expectation.
  - **GREEN:** SchemaFlux compiles a canonical registry covering all 49 feature groups and every named feature, preserving `DEFINED|PARTIAL|IMPLIED|MISSING|DEFERRED` independently from implementation phase and emitting exact uncovered/duplicate/ambiguous mappings.
  - **REFACTOR:** aliases such as UI label versus canonical semantic action map to one feature/intent identity without erasing distinct business operations.
  - **Refs:** [Intent coverage matrix](data/models/intent-coverage-matrix.md), [business intent catalog](specs/business-intent-catalog.md), [platform coverage matrix](specs/platform-capability-coverage-matrix.md).

- [x] `INTENT-011` **[DESIGN][SOL_HIGH] Complete intent-family and result contracts for every feature class.**
  - **Evidence (2026-09-05):** `TestIntentFamilyResultExhaustiveness` in `tools/planning/intentmanifests` (family and result contract completeness over the real generated coverage registry: every feature class maps to an intent family declaring result kinds, evidence kinds and terminal states in a golden table, every material feature's bound intent belongs to a family with a complete contract, gaps reported by class with counts; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/planning/intentmanifests/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `INTENT-010`, `MODEL-015`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestIntentFamilyResultExhaustiveness`.
  - **TEST MATRIX:** `PRIMARY=TestIntentFamilyResultExhaustiveness`; `PROPERTY=TestTodo_INTENT_011_Property`; `MUTATION=TestTodo_INTENT_011_Mutation`.
  - **RED:** `TestIntentFamilyResultExhaustiveness` rejects a mapped feature whose `ChangeRequest|ProcessRequest|CalculationRequest|FilingRequest|Case|BatchOperation|AnalyticalRequest` family cannot represent its lifecycle, result, error, correction and completion dimensions, or a query/calculation that fabricates a BusinessTransaction despite producing no mutation/effect.
  - **GREEN:** each family has typed request/result, execution/effect policy, allowed relationships, terminal dimensions and evidence contract; read/answer/calculation/analysis intents return governed results while only authorized mutation/effect paths may create transactions/effects.
  - **REFACTOR:** extend a family through versioned composition before inventing a feature-specific kernel.
  - **Refs:** [Intent families](specs/business-intent-catalog.md), [transaction semantics](specs/transaction-ledger-reconciliation-and-repair.md).

- [x] `INTENT-012` **[GATE_A][SOL_HIGH] Record trusted intent origin without granting caller-selected authority.**
  - **Evidence (2026-09-05):** `TestIntentOriginRejectsSpoofedActorAndTrigger`, `TestTodo_INTENT_012_{Integration,Security,Mutation}` in `internal/intent` (NewOrigin is the only constructor: the caller's claim carries causal bookkeeping only, any caller-populated principal/tenant/session/delegation/assurance/producer field is ErrCallerSelectedAuthority, nine origin kinds each with a closed set of admissible verified initiator kinds, Origin carries no role/capability field and ConfersAuthority is constant false) plus `TestDeriveOriginTakesEveryTrustedFieldFromTheCredential` in `internal/intent/app` (specFor derives the origin from the credential and fails closed); `go test -count=1 ./internal/intent/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `INTENT-002`, `TRUST-001`, `MODEL-024`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestIntentOriginRejectsSpoofedActorAndTrigger`.
  - **TEST MATRIX:** `PRIMARY=TestIntentOriginRejectsSpoofedActorAndTrigger`; `INTEGRATION=TestTodo_INTENT_012_Integration`; `SECURITY=TestTodo_INTENT_012_Security`; `MUTATION=TestTodo_INTENT_012_Mutation`.
  - **RED:** `TestIntentOriginRejectsSpoofedActorAndTrigger` rejects caller-selected principal/tenant/session/delegation, schedule/event/system origin without authenticated producer and source evidence, an agent pretending to be a human, or an integration event inheriting provider authority.
  - **GREEN:** immutable origin distinguishes human, delegated human, agent, workload, schedule, internal event, external event, repair and operator sources; records initiator, on-behalf-of chain, trigger/correlation/causation, assurance, channel and trusted context while governance independently determines authority.
  - **REFACTOR:** UI/API/CLI/mobile/kiosk/partner surfaces normalize into the same origin contract.
  - **Refs:** [Identity contract](plan.md#94-identity-and-permission-contract), [canonical envelope](specs/canonical-envelope-and-digest.md).

- [x] `INTENT-013` **[GATE_A][SOL_HIGH] Enforce one governed invocation path for every material feature.**
  - **Evidence (2026-09-05):** `TestMaterialFeatureCannotBypassIntentGateway` in `tools/policy/invocationpath` (tools/policy/invocationpath proves every capability invocation reaches a domain only through the intent application layer, failing the build on any direct transport-to-domain call; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/invocationpath/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `INTENT-010`, `INTENT-012`, `CAP-002`, `GOVERN-002`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestMaterialFeatureCannotBypassIntentGateway`.
  - **TEST MATRIX:** `PRIMARY=TestMaterialFeatureCannotBypassIntentGateway`; `GOLDEN=TestTodo_INTENT_013_Golden`; `RACE=TestTodo_INTENT_013_Race`; `INTEGRATION=TestTodo_INTENT_013_Integration`; `CONFORMANCE=TestTodo_INTENT_013_Conformance`; `BROWSER=TestTodo_INTENT_013_Browser`; `MUTATION=TestTodo_INTENT_013_Mutation`.
  - **RED:** `TestMaterialFeatureCannotBypassIntentGateway` injects direct domain repository writes, provider calls, emails/files, queue publications or workflow starts from GWC, gRPC, grpcbridge, CLI, agent, partner, scheduler, event consumer, analytics, DataOps and operator handlers and expects build/runtime denial with zero effect.
  - **GREEN:** every material feature follows `trusted boundary -> CapabilityGateway -> IntentService -> governance -> workflow/engine/domain`; route-manifest conformance proves exact IntentDefinition/capability parity across every enabled channel.
  - **REFACTOR:** pure display/read mechanics may use governed read capabilities only when `INTENT-009` explicitly classifies them non-material; they still cannot mutate or emit effects.
  - **Refs:** [Capability gateway](specs/capability-registry-and-lifecycle.md), [platform plane model](specs/platform-plane-model.md).

- [x] `INTENT-014` **[GATE_A][SOL_HIGH] Model drafts, templates, saved actions, clone and fork without confusing them with execution.**
  - **Evidence (2026-09-05):** `TestIntentDraftTemplateCloneForkBoundaries`, `TestTodo_INTENT_014_{Race,Fault,Security,Mutation}` in `internal/intent` (Template pinned to an exact definition version, AuthoringDraft mutable until SubmitDraft stamps the one intent it became, SavedAction = reference + authorized parameters, CloneIntent/ForkProposal through one derive with a closed deny-list of server-owned path roots; fresh idempotency key, correlation id and trusted origin required; Lineage is ids and versions only); `go test -count=1 ./internal/intent/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `INTENT-002`, `INTENT-005`, `FORM-001`, `CONFIG-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestIntentDraftTemplateCloneForkBoundaries`.
  - **TEST MATRIX:** `PRIMARY=TestIntentDraftTemplateCloneForkBoundaries`; `RACE=TestTodo_INTENT_014_Race`; `FAULT=TestTodo_INTENT_014_Fault`; `SECURITY=TestTodo_INTENT_014_Security`; `MUTATION=TestTodo_INTENT_014_Mutation`.
  - **RED:** `TestIntentDraftTemplateCloneForkBoundaries` fails when a mutable draft is treated as submitted intent, template embeds principal/tenant/server-owned facts or stale revisions, clone reuses idempotency/evidence/approval, or fork mutates the source proposal.
  - **GREEN:** template supplies versioned permitted defaults; draft stores authorized incomplete input; submit creates one immutable IntentInstance; clone/fork create new causal identities and require fresh truth/governance while retaining redaction-safe lineage.
  - **REFACTOR:** recent/favorite actions store references and authorized parameters, never copied authority or sensitive rendered state.
  - **Refs:** [Forms](specs/human-work-forms-and-rules.md), [configuration lifecycle](data/models/dataops-configuration.md).

- [x] `INTENT-015` **[GATE_B][SOL_HIGH] Define immutable intent-to-intent relationship semantics.**
  - **Evidence (2026-09-05):** `TestIntentRelationshipGraphRejectsAmbiguity`, `TestTodo_INTENT_015_{Property,Security,Mutation}` in `internal/intent` (nine relationship kinds never conflated, RelationshipRevision pins exact versions/ordinal/cause/purpose/cardinality/propagation, graph refuses mutable parentage, duplicate ordinals, two kinds on one pair, hidden material children, cross-tenant edges and cycles per kind; no cascade-delete in the propagation vocabulary); `go test -count=1 ./internal/intent/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `INTENT-003`, `INTENT-005`, `MODEL-013`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestIntentRelationshipGraphRejectsAmbiguity`.
  - **TEST MATRIX:** `PRIMARY=TestIntentRelationshipGraphRejectsAmbiguity`; `PROPERTY=TestTodo_INTENT_015_Property`; `SECURITY=TestTodo_INTENT_015_Security`; `MUTATION=TestTodo_INTENT_015_Mutation`.
  - **RED:** `TestIntentRelationshipGraphRejectsAmbiguity` rejects missing tenant/cause/purpose, cycles where prohibited, mutable parentage, duplicate ordinal, hidden material child, or conflation of `CHILD|DEPENDENCY|FOLLOW_UP|CORRECTION|COMPENSATION|REPAIR|SUPERSEDES|ALTERNATIVE|TRIGGERED_BY`.
  - **GREEN:** relationship revisions bind exact intent/proposal versions, ordering/cardinality, lifecycle propagation and completion policy; Hire/Leave/Payroll/Offboarding composites can expose bounded child intents without erasing each child's governance and evidence.
  - **REFACTOR:** relationship graph is semantic chronology, not workflow-node containment or foreign-key cascade ownership.
  - **Refs:** [BusinessIntent model](specs/business-intent-and-change-request.md), [relationship map](data/models/relationship-map.md).

- [ ] `INTENT-016` **[GATE_B][SOL_HIGH] Emit child and follow-up intents deterministically and safely.**
  - **Depends:** `INTENT-015`, `CAP-002`, `WF-STEP-009`, `ADMISSION-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestIntentEmissionIsIdempotentBoundedAndGoverned`.
  - **TEST MATRIX:** `PRIMARY=TestIntentEmissionIsIdempotentBoundedAndGoverned`; `GOLDEN=TestTodo_INTENT_016_Golden`; `RACE=TestTodo_INTENT_016_Race`; `MUTATION=TestTodo_INTENT_016_Mutation`.
  - **RED:** `TestIntentEmissionIsIdempotentBoundedAndGoverned` injects duplicate event/node delivery, recursive self-emission, fan-out/depth/resource overflow, child with broader scope/purpose than parent or parent completion that ignores mandatory child result.
  - **GREEN:** emission key binds parent revision, source node/event, child definition/version and ordinal; duplicate returns original child; quotas bound depth/fan-out/cost; child receives independent governance and parent policy declares wait/detach/failure/repair behavior.
  - **REFACTOR:** a capability may invoke a child intent or a lower-level operation only through an explicit manifest decision, never context-dependent ambiguity.
  - **Refs:** [Workflow subworkflows](specs/workflow-runtime.md), [admission control](specs/platform-responsibility-boundaries.md).

- [ ] `INTENT-017` **[PHASE_2][SOL_HIGH] Convert scheduled obligations and effective-time triggers into idempotent intents.**
  - **Depends:** `INTENT-012`, `INTENT-016`, `SCHED-001`, `TIME-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestScheduledIntentCreationHandlesMisfireDSTAndReplay`.
  - **TEST MATRIX:** `PRIMARY=TestScheduledIntentCreationHandlesMisfireDSTAndReplay`; `GOLDEN=TestTodo_INTENT_017_Golden`; `RACE=TestTodo_INTENT_017_Race`; `FAULT=TestTodo_INTENT_017_Fault`; `RECOVERY=TestTodo_INTENT_017_Recovery`; `MUTATION=TestTodo_INTENT_017_Mutation`.
  - **RED:** `TestScheduledIntentCreationHandlesMisfireDSTAndReplay` loses or duplicates effective-date activation, payroll/cycle/deadline/expiry/reminder/reconciliation intent under crash, DST/tzdb/calendar change, catch-up policy, leader overlap or schedule revision.
  - **GREEN:** schedule occurrence freezes definition/version, business/calendar time context, target scope and deterministic occurrence key; exactly one intent is created or a typed `SKIPPED|DEFERRED|REVIEW_REQUIRED` receipt explains why.
  - **REFACTOR:** scheduler dispatches definitions; it contains no Promotion/Payroll/Leave semantics.
  - **Refs:** [Scheduling and trigger service](specs/platform-foundation-gap-closure.md), [workflow time semantics](specs/workflow-runtime.md).

- [ ] `INTENT-018` **[PHASE_2][SOL_HIGH] Convert internal and external events into policy-bound intents.**
  - **Depends:** `INTENT-012`, `INTENT-016`, `EVENT-001`, `INTG-018`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestEventToIntentPolicyRejectsReplayStormAndAuthorityConfusion`.
  - **TEST MATRIX:** `PRIMARY=TestEventToIntentPolicyRejectsReplayStormAndAuthorityConfusion`; `GOLDEN=TestTodo_INTENT_018_Golden`; `RACE=TestTodo_INTENT_018_Race`; `INTEGRATION=TestTodo_INTENT_018_Integration`; `SECURITY=TestTodo_INTENT_018_Security`; `RECOVERY=TestTodo_INTENT_018_Recovery`; `MUTATION=TestTodo_INTENT_018_Mutation`.
  - **RED:** `TestEventToIntentPolicyRejectsReplayStormAndAuthorityConfusion` creates duplicate/cyclic intents from redelivery, treats event payload as authoritative current truth, processes unsupported schema/tenant/source or lets a provider select intent/capability/organization scope.
  - **GREEN:** versioned trigger policy filters event type/schema/source/scope, resolves current truth where required and derives deterministic causal/idempotency key; one accepted event creates bounded intent(s), while ignored/quarantined/unknown outcomes are evidenced.
  - **REFACTOR:** events describe observations/occurrences; trigger policy chooses whether a business instruction should exist.
  - **Refs:** [Event distribution](specs/platform-plane-model.md), [integration platform](specs/integration-platform.md).

- [ ] `INTENT-019` **[GATE_C][SOL_HIGH] Compile bulk features into one population-scoped ChangeRequest and bounded child intents.**
  - **Depends:** `INTENT-016`, `POP-005`, `ADMISSION-002`, `WF-RUN-019`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestBatchIntentUsesFrozenPopulationAndBoundedChildren`.
  - **TEST MATRIX:** `PRIMARY=TestBatchIntentUsesFrozenPopulationAndBoundedChildren`; `GOLDEN=TestTodo_INTENT_019_Golden`; `MUTATION=TestTodo_INTENT_019_Mutation`.
  - **RED:** `TestBatchIntentUsesFrozenPopulationAndBoundedChildren` loops over a live query, silently changes membership, skips/duplicates subjects on resume, performs effect without child intent/evidence, leaks denied population counts, exceeds declared blast-radius/cost/rate limits, or introduces a batch kernel family.
  - **GREEN:** a `CHANGE_REQUEST` definition with `population_scope` binds an immutable PopulationSnapshot, operation template/version, exclusions, ordering, partitions, limits and completion policy; each subject/partition has deterministic child identity and result so counts exactly partition eligible/ineligible/denied/unknown/succeeded/failed/repaired.
  - **REFACTOR:** batch mechanics coordinate; each child retains ordinary governance, idempotency, transaction and repair semantics.
  - **Refs:** [Population engine](#37-shared-transformation-and-population-engines), [kernel family table](specs/business-intent-and-change-request.md#general-kernel-and-subtypes).

- [x] `INTENT-020` **[GATE_B][SOL_HIGH] Link analytical results to proposed action without granting analysis execution authority.**
  - **Evidence (2026-09-05):** `TestAnalysisToIntentRequiresFreshGovernedProposal` in `internal/intent/analysis` (AnalyticalRequest produces sourced result; RecommendAction creates a separate draft/proposed BusinessIntent with explicit selected evidence, uncertainty, authorized population, causal link and fresh governance/simulation before any effect; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/intent/analysis/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `INTENT-011`, `AGENT-003`, `MODEL-020`, `GOVERN-002`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestAnalysisToIntentRequiresFreshGovernedProposal`.
  - **TEST MATRIX:** `PRIMARY=TestAnalysisToIntentRequiresFreshGovernedProposal`; `GOLDEN=TestTodo_INTENT_020_Golden`; `MUTATION=TestTodo_INTENT_020_Mutation`.
  - **RED:** `TestAnalysisToIntentRequiresFreshGovernedProposal` lets a report/model/search/hypothesis/prediction directly mutate state, hides cohort/query/model/source versions, carries restricted result fields into action or reuses stale analysis as current fact.
  - **GREEN:** AnalyticalRequest produces sourced result; `RecommendAction` creates a separate draft/proposed BusinessIntent with explicit selected evidence, uncertainty, authorized population, causal link and fresh governance/simulation before any effect.
  - **REFACTOR:** analysis may suggest; deterministic capability/workflow/domain code executes.
  - **Refs:** [Agent and intelligence models](data/models/assurance-intelligence-platform.md), [provenance](specs/provenance-graph-and-lineage.md).

- [ ] `INTENT-021` **[GATE_B][SOL_HIGH] Implement governed intent lifecycle actions and inspection surfaces.**
  - **Depends:** `INTENT-003`, `INTENT-008`, `EVIDENCE-001`, `TRUST-010`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestIntentLifecycleSurfaceAuthorizationAndTruth`.
  - **TEST MATRIX:** `PRIMARY=TestIntentLifecycleSurfaceAuthorizationAndTruth`; `SECURITY=TestTodo_INTENT_021_Security`; `MUTATION=TestTodo_INTENT_021_Mutation`.
  - **RED:** `TestIntentLifecycleSurfaceAuthorizationAndTruth` lets deep link/search/timeline/inspector/share/export/subscribe/cancel/supersede/correct/escalate reveal existence/fields or act without current authority, mutate history, collapse unknown/degraded dimensions or accept stale revision.
  - **GREEN:** each surface is a typed read or material intent/capability with field/purpose filtering; timeline and inspector explain exact versions/evidence; lifecycle actions create successor/related intents where required; subscriptions emit redacted versioned state transitions.
  - **REFACTOR:** recent/favorite/notification UI projects this same truth rather than maintaining a second status model.
  - **Refs:** [Business execution receipt](#12-canonical-data-ledger-projections-outbox-reconciliation-and-repair), [AuthZ](specs/organization-scope-and-authz.md).

- [ ] `INTENT-022` **[GATE_B][SOL_HIGH] Route operator, support, recovery and break-glass actions through governed intents.**
  - **Depends:** `INTENT-013`, `TRUST-021`, `OPS-004`, `RECOVERY-003`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestOperatorMutationRequiresIntentAndJITAuthority`.
  - **TEST MATRIX:** `PRIMARY=TestOperatorMutationRequiresIntentAndJITAuthority`; `RACE=TestTodo_INTENT_022_Race`; `INTEGRATION=TestTodo_INTENT_022_Integration`; `FAULT=TestTodo_INTENT_022_Fault`; `SECURITY=TestTodo_INTENT_022_Security`; `RECOVERY=TestTodo_INTENT_022_Recovery`; `MUTATION=TestTodo_INTENT_022_Mutation`.
  - **RED:** `TestOperatorMutationRequiresIntentAndJITAuthority` permits database surgery, unjournaled workflow node manipulation, connector redrive, projection rebuild, failover, quarantine, tenant suspension, key rotation or break-glass action through a privileged side door.
  - **GREEN:** each material operation resolves a typed operational/repair intent, JIT/dual-control policy, simulation/scope, idempotency and evidence receipt; emergency execution records declared bypass reason and mandatory review without rewriting business truth.
  - **REFACTOR:** diagnostic reads may remain governed capabilities, but any material change/effect has an IntentInstance and outcome.
  - **Refs:** [Incident management](specs/incident-management.md), [support access](specs/platform-responsibility-boundaries.md), [recovery](data/models/operations-production.md).

- [x] `INTENT-023` **[GATE_A][SOL_HIGH] Preserve intent semantics across simulate, shadow, replay, test and execute modes.**
  - **Evidence (2026-09-05):** `TestIntentExecutionModesCannotEscalateEffects`, `TestTodo_INTENT_023_{Golden,Integration,Fault,Conformance,Recovery,Mutation}`, `FuzzTodo_INTENT_023` in `internal/intent` (fixed 5-mode × 4-environment ModeContract matrix pinned as `testdata/intent_023_mode_matrix.json`; only EXECUTE/PRODUCTION and REPAIR/PRODUCTION reach the real world, Permit composes the contract ceiling with the definition's effect class so neither lifts the other, CausalSeparation forbids a replay naming itself); `go test -count=1 ./internal/intent/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `INTENT-002`, `CAP-002`, `TX-001`, `MODEL-030`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestIntentExecutionModesCannotEscalateEffects`.
  - **TEST MATRIX:** `PRIMARY=TestIntentExecutionModesCannotEscalateEffects`; `GOLDEN=TestTodo_INTENT_023_Golden`; `FUZZ=FuzzTodo_INTENT_023`; `INTEGRATION=TestTodo_INTENT_023_Integration`; `FAULT=TestTodo_INTENT_023_Fault`; `CONFORMANCE=TestTodo_INTENT_023_Conformance`; `RECOVERY=TestTodo_INTENT_023_Recovery`; `MUTATION=TestTodo_INTENT_023_Mutation`.
  - **RED:** `TestIntentExecutionModesCannotEscalateEffects` allows simulation/shadow/replay/conformance/fault mode to commit domain truth, send real message/file/payment/webhook or consume live approval; same input loses causal separation between historical replay and new action.
  - **GREEN:** immutable execution mode and environment bind capability permissions, adapters, clocks, evidence namespace and zero-effect guarantees; execute requires a separately authorized current intent, while mode comparisons report deterministic semantic differences.
  - **REFACTOR:** test/sandbox tools use production contracts with safe adapters, not hidden bypass APIs.
  - **Refs:** [Simulation contract](specs/workflow-runtime.md), [sandbox and testing](specs/platform-responsibility-boundaries.md).

- [ ] `INTENT-024` **[DESIGN][SOL_HIGH] Generate end-to-end feature-intent conformance suites.**
  - **Depends:** `INTENT-010`, `INTENT-013`, `INTENT-025`, `CONF-001`, `API-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestEveryImplementedMaterialFeatureHasIntentConformance`.
  - **TEST MATRIX:** `PRIMARY=TestEveryImplementedMaterialFeatureHasIntentConformance`; `GOLDEN=TestTodo_INTENT_024_Golden`; `RACE=TestTodo_INTENT_024_Race`; `CONFORMANCE=TestTodo_INTENT_024_Conformance`; `RECOVERY=TestTodo_INTENT_024_Recovery`; `MUTATION=TestTodo_INTENT_024_Mutation`.
  - **RED:** `TestEveryImplementedMaterialFeatureHasIntentConformance` reports any implemented feature/channel lacking typed create/consume/emit/observe behavior, governance denial vector, idempotency/replay vector, zero-bypass assertion, lifecycle/result oracle, evidence expectation or phase-appropriate implementation depth.
  - **GREEN:** generated Go suites exercise representative feature groups and all enabled routes; every registry entry is either passing at its declared depth or explicitly `DEFERRED|MISSING` and therefore unavailable; no prose/catalog entry is mistaken for implementation.
  - **REFACTOR:** use family/channel templates for mechanics while retaining domain-specific fixtures for legal, financial, privacy and correction semantics.
  - **Refs:** [Reference workflow suite](reference-workflows/reference-suite.md), [coverage matrix](specs/platform-capability-coverage-matrix.md), [intent catalog](specs/business-intent-catalog.md).

- [x] `INTENT-025` **[DESIGN][SOL_HIGH] Converge the feature vocabulary with the drafted definitions when a second domain is funded.**
  - **Evidence (2026-09-05):** `TestFeatureVocabularyIntentCatalogConvergence` in `tools/planning/intentmanifests` (convergence check for a funded-domain declaration: every DEFERRED_TO_INTENT feature whose named intent is now drafted is reported with a proposed binding, bindings conflicting with the intent family refused, golden report for the Promotion domain fixture; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/planning/intentmanifests/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `INTENT-010`, `INTENT-011`, `MODEL-010`, `MODEL-016`, `GOV-012`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestFeatureVocabularyIntentCatalogConvergence`.
  - **TEST MATRIX:** `PRIMARY=TestFeatureVocabularyIntentCatalogConvergence`; `GOLDEN=TestTodo_INTENT_025_Golden`.
  - **RED:** a named feature is implemented without resolving to a drafted definition, a presentation alias creates a duplicate definition, or a composite hides child intents.
  - **GREEN:** every feature in the funded domain resolves to a drafted definition, a template, an explicit composite graph, an analytical result, an observer role, a non-material mechanic or a reviewed `NEW_DEFINITION_CANDIDATE`; the report has no denominator other than the drafted definitions.
  - **REFACTOR:** adding a definition is a source change with full model conformance; no count is hard-coded or reconciled.
  - **Refs:** [Business intent catalog](specs/business-intent-catalog.md), [covered entity set](data/models/README.md).

- [x] `REPLAN-001` **[GATE_B][SOL_HIGH] Determine the material proposal subgraph affected by changed inputs.**
  - **Depends:** `INTENT-005`, `CONFLICT-002`, `MODEL-020`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_REPLAN_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_REPLAN_001`; `PROPERTY=TestTodo_REPLAN_001_Property`; `GOLDEN=TestTodo_REPLAN_001_Golden`; `MUTATION=TestTodo_REPLAN_001_Mutation`.
  - **RED:** changed PTO/budget/position/rule/evidence/source authority invalidates nothing, invalidates unrelated components, or dependency cycle/unknown lineage is treated as safe.
  - **GREEN:** compare old/current snapshots and proposal dependency graph to return exact affected facts, calculations, writes, effects, obligations and decisions with `UNCHANGED|RECOMPUTE|INVALIDATE|UNKNOWN` plus reasons/digest.
  - **REFACTOR:** dependency closure is semantic and versioned; domain-specific materiality rules are inputs rather than workflow branches.
  - **Refs:** [Provenance graph](specs/provenance-graph-and-lineage.md), [proposal revalidation](specs/business-intent-and-change-request.md).
  - **Evidence (2026-09-03):** `TestTodo_REPLAN_001`, Declared `REPLAN-001` test matrix and acceptance behavior in `internal/replan`; PASS; `go test -p 1 ./internal/replan` and `go vet ./internal/replan` on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [ ] `REPLAN-002` **[GATE_B][SOL_HIGH] Preserve only policy-permitted unaffected decisions during replanning.**
  - **Depends:** `REPLAN-001`, `APPROVAL-002`, `APPROVAL-006`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_REPLAN_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_REPLAN_002`; `PROPERTY=TestTodo_REPLAN_002_Property`; `MUTATION=TestTodo_REPLAN_002_Mutation`.
  - **RED:** material amount/interval/obligation/view change retains approval, presentation-only change invalidates every decision without policy, or reused decision loses original proposal binding.
  - **GREEN:** versioned reuse policy returns each decision `RETAINED_BY_REFERENCE`, `REAPPROVAL_REQUIRED`, or `UNKNOWN`; retained decision keeps original immutable receipt and explicit applicability link to successor proposal.
  - **REFACTOR:** no ApprovalDecision is edited or rebound in place.
  - **Refs:** [Approval binding](plan.md#53-approval-must-bind-to-an-immutable-proposal), [workflow context](workflows/_engine/workflow-context-contract.md).

- [x] `REPLAN-003` **[GATE_B][SOL_HIGH] Invalidate only proposal components dependent on material drift.**
  - **Evidence (2026-09-05):** `TestTodo_REPLAN_003` in `internal/engines/replan` (minimal invalidation set over a proposal's component dependency declaration and a field diff between baseline and fresh snapshot: only components whose declared inputs materially drifted are invalidated, stable digest, explain naming the drifted input per component, property that undrifted components are never invalidated; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/engines/replan/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `REPLAN-001`, `INTENT-006`, `EFFECT-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_REPLAN_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_REPLAN_003`; `PROPERTY=TestTodo_REPLAN_003_Property`; `RACE=TestTodo_REPLAN_003_Race`; `MUTATION=TestTodo_REPLAN_003_Mutation`.
  - **RED:** changed balance alters leave eligibility, protected interval or unrelated evidence review without dependency; invalidated effect remains dispatchable; unknown dependency is retained as valid.
  - **GREEN:** invalidation set closes stale planned writes/effects/reservations/obligations and marks exact calculations/approvals for recompute while unaffected components remain byte-identical and executable under policy.
  - **REFACTOR:** invalidation records successor links and never deletes the prior plan.
  - **Refs:** [Change conflicts](specs/cross-workflow-conflict-and-write-intent.md), [effect graph](specs/transaction-plan-and-commit-coordinator.md).

- [ ] `REPLAN-004` **[GATE_B][SOL_HIGH] Create and route a successor ProposalRevision after partial replanning.**
  - **Depends:** `REPLAN-002`, `REPLAN-003`, `INTENT-005`, `WF-RUN-015`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_REPLAN_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_REPLAN_004`; `GOLDEN=TestTodo_REPLAN_004_Golden`; `MUTATION=TestTodo_REPLAN_004_Mutation`.
  - **RED:** recomputation mutates approved proposal, silently changes paid/unpaid segments, skips required acknowledgement/reapproval, or resumes execution against old digest.
  - **GREEN:** successor proposal binds new snapshot and recomputed components, references superseded revision and retained decisions, receives a new digest, and routes workflow to exact review/reapproval/revalidation node before any effect.
  - **REFACTOR:** generic replanning coordinates immutable artifacts; domain engines perform calculations.
  - **Refs:** [Proposal revisions](specs/business-intent-and-change-request.md), [workflow interventions](specs/workflow-runtime.md).

- [x] `GOVERN-001` **[GATE_A][SOL_HIGH] Implement governance decision inputs and snapshots.**
  - **Evidence (2026-09-05):** `TestTodo_GOVERN_001` in `internal/governance/decision` (immutable snapshot references every subdecision/version/source watermark and canonical digest; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/governance/decision/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Evidence (partial, 2026-09-05):** `TestTodo_GOVERN_001` in `internal/governance/decision` (immutable snapshot references every subdecision/version/source watermark and canonical digest; written by an earlier lane (Luna or Sonnet) and verified independently); `go test -count=1 ./internal/governance/decision/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02. Tick held partial on purpose: PRIV-001 (the privacy inventory it depends on) is open behind WEDGE-010.
  - **Evidence (partial, 2026-09-05):** `TestTodo_GOVERN_001` in `internal/governance/decision` (governance decision record composed from proposal revision digest, control snapshot, rule-pack versions, approval requirements and satisfaction, SoD verdicts and obligations with a canonical digest and a Promotion golden; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/governance/decision/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02. Tick held: dependency PRIV-001 (via WEDGE-010) is still open and needs a ruling from Cam; implementation is complete.
  - **Depends:** `TRUST-011`, `MODEL-021`, `PRIV-001`, `CAP-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_GOVERN_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_GOVERN_001`; `GOLDEN=TestTodo_GOVERN_001_Golden`; `SECURITY=TestTodo_GOVERN_001_Security`; `MUTATION=TestTodo_GOVERN_001_Mutation`.
  - **RED:** decision missing principal/delegation, capability/resource/fields, current/target org, purpose, risk, authority, policy or legal context is `UNKNOWN`, never allow.
  - **GREEN:** immutable snapshot references every subdecision/version/source watermark and canonical digest.
  - **REFACTOR:** snapshot minimizes sensitive values while preserving reproducibility.
  - **Refs:** [Governance composition](specs/governance-decision-and-obligation-composition.md), [reproducibility](plan.md#93-reproducibility-contract).

- [x] `GOVERN-002` **[GATE_A][SOL_HIGH] Compose governance decisions deterministically.**
  - **Evidence (2026-09-05):** `TestTodo_GOVERN_002` in `internal/governance`, `internal/governance/decision` (deny dominance, restriction intersection and obligation union/deduplication produce ALLOW, DENY, OBLIGATIONS, or UNKNOWN plus explanation; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/governance/ ./internal/governance/decision/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Evidence (partial, 2026-09-05):** `TestTodo_GOVERN_002` in `internal/governance`, `internal/governance/decision` (deny dominance, restriction intersection and obligation union/deduplication produce ALLOW, DENY, OBLIGATIONS, or UNKNOWN plus explanation; written by an earlier lane (Luna or Sonnet) and verified independently); `go test -count=1 ./internal/governance/ ./internal/governance/decision/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02. Tick held partial on purpose: PRIV-001 (the privacy inventory it depends on) is open behind WEDGE-010.
  - **Evidence (partial, 2026-09-05):** `TestTodo_GOVERN_002` in `internal/governance/decision` (deterministic composition: order-independent inputs yield one decision and digest, every fired rule named with its version, conflicts resolved by a declared precedence table and unresolved conflicts a typed refusal; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/governance/decision/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02. Tick held: the GOVERN-001 dependency PRIV-001 is still open; implementation is complete.
  - **Depends:** `GOVERN-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_GOVERN_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_GOVERN_002`; `RACE=TestTodo_GOVERN_002_Race`; `MUTATION=TestTodo_GOVERN_002_Mutation`.
  - **RED:** deny loses to allow, restriction broadens scope, contradictory/unknown obligation disappears or stale decision permits execution.
  - **GREEN:** deny dominance, restriction intersection and obligation union/deduplication produce `ALLOW`, `DENY`, `OBLIGATIONS`, or `UNKNOWN` plus explanation.
  - **REFACTOR:** composition is a pure versioned function.
  - **Refs:** [Governance coordinator](specs/governance-decision-and-obligation-composition.md), [Legal/AuthZ distinction](plan.md#512-law-is-a-versioned-constraint-system).

- [x] `GOVERN-003` **[GATE_B][SOL_HIGH] Revalidate governance immediately before effect.**
  - **Evidence (2026-09-07):** `TestTodo_GOVERN_003`, `TestTodo_GOVERN_003_InvalidInput`, `TestTodo_GOVERN_003_Mutation`, `TestTodo_GOVERN_003_Security` in `internal/governance/revalidate` (written in ad58d6a on 2026-09-06 and never ticked; the named tests prove the GREEN contract: current evaluation either confirms exact proposal or returns typed block/reapproval/replan requirement before dispatch/commit); `go test -count=1 ./internal/governance/revalidate/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Evidence (partial, 2026-09-05):** `TestTodo_GOVERN_003` in `internal/governance/revalidate` (current evaluation either confirms exact proposal or returns typed block/reapproval/replan requirement before dispatch/commit; written by an earlier lane (Luna or Sonnet) and verified independently); `go test -count=1 ./internal/governance/revalidate/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02. Tick held partial on purpose: PRIV-001 (the privacy inventory it depends on) is open behind WEDGE-010.
  - **Evidence (partial, 2026-09-05):** `TestTodo_GOVERN_003` in `internal/governance/revalidate` (Revalidate rebuilds decision.Inputs from the historical record with the seven live fact categories substituted (AuthZ verdict, session state, source authority, field classification version, legal and policy pack versions, budget and position facts, conflict classification), recomposes through decision.Compose and confirms only an exact digest match; otherwise a typed BLOCK, REPLAN_REQUIRED or REAPPROVAL_REQUIRED naming the changed inputs; a tampered historical record is refused; Result.VerifyBoundPlan binds the result to the prepared TransactionPlan digest; written by a Sonnet subagent and verified independently); `go test -count=1 ./internal/governance/revalidate/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02. Tick held with GOVERN-001/002: dependency PRIV-001 (via WEDGE-010) is still open; implementation is complete.
  - **Depends:** `GOVERN-002`, `TRUST-003`, `TRUST-004`, `CONFLICT-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_GOVERN_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_GOVERN_003`; `SECURITY=TestTodo_GOVERN_003_Security`; `MUTATION=TestTodo_GOVERN_003_Mutation`.
  - **RED:** changed AuthZ, session, source authority, field classification, legal/policy version, budget/position or conflict executes from historical approval alone.
  - **GREEN:** current evaluation either confirms exact proposal or returns typed block/reapproval/replan requirement before dispatch/commit.
  - **REFACTOR:** revalidation result is bound to prepared transaction plan.
  - **Refs:** [Revalidation depth](plan.md#phase-1-implementation-depth-matrix), [Gate B acceptance](execution-plan.md#gate-b-acceptance--limited-write-authority).

- [x] `CONFLICT-001` **[GATE_A][SOL_HIGH] Normalize proposal write footprints.**
  - **Depends:** `INTENT-004`, `MODEL-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CONFLICT_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CONFLICT_001`; `PROPERTY=TestTodo_CONFLICT_001_Property`; `GOLDEN=TestTodo_CONFLICT_001_Golden`; `MUTATION=TestTodo_CONFLICT_001_Mutation`.
  - **RED:** field aliases, parent/child paths or open-ended intervals evade overlap detection.
  - **GREEN:** write set contains canonical resource, field path, effective interval, operation, expected revision and authority scope.
  - **REFACTOR:** compiler/domain capabilities generate footprints; clients cannot underdeclare them.
  - **Refs:** [Conflict/write intent](specs/cross-workflow-conflict-and-write-intent.md), [conflict principle](plan.md#54-concurrent-changes-are-a-business-problem).
  - **Evidence (2026-09-03):** `TestTodo_CONFLICT_001`, `_Property`, `_Golden`, `_Mutation` in `internal/transaction/conflict` (`WriteFootprint`, `FieldPath` ancestor detection, alias normalisation, interval overlap via kernel `EffectiveInterval`, canonical digest); PASS; branch plan-revision-2026-09-02; `go test -count=1 ./internal/transaction/conflict/...` on windows/arm64 (Go 1.26.3).

- [x] `CONFLICT-002` **[GATE_A][SOL_HIGH] Classify concurrent proposal conflicts.**
  - **Depends:** `CONFLICT-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CONFLICT_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CONFLICT_002`; `RACE=TestTodo_CONFLICT_002_Race`.
  - **RED:** pending/approved/future/executed overlapping writes are ignored or nondeterministically classified.
  - **GREEN:** fixtures return exactly `COMPATIBLE_MERGE`, `ORDERED_DEPENDENCY`, `SUPERSESSION`, or `HARD_CONFLICT` with evidence/owner.
  - **REFACTOR:** domain-specific merge rules are versioned inputs.
  - **Refs:** [Conflict contract](plan.md#92-conflict-contract), [conflict spec](specs/cross-workflow-conflict-and-write-intent.md).
  - **Evidence (2026-09-03):** `TestTodo_CONFLICT_002`, `_Race` in `internal/transaction/conflict` (`ClassifyConflict` symmetric under pair order, declared supersession/dependency before merge rules, else HARD_CONFLICT; `ClassifyAll`); ARCH-GO-011 direction holds (0 violations); PASS; branch plan-revision-2026-09-02; `go test -count=1 ./internal/transaction/conflict/...` on windows/arm64 (Go 1.26.3).

- [ ] `CONFLICT-003` **[GATE_B][SOL_HIGH] Persist write intents and commit-time conflict fences.**
  - **Depends:** `CONFLICT-002`, `TX-003`, `TX-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CONFLICT_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CONFLICT_003`; `RACE=TestTodo_CONFLICT_003_Race`.
  - **RED:** two concurrent writers with the same baseline both commit or deadlock without deterministic resolution.
  - **GREEN:** one fenced winner commits; loser receives `CONFLICT_STALE_BASELINE` or declared ordering/supersession outcome; intents close/release exactly once.
  - **REFACTOR:** no process-local worker lock is treated as business conflict control.
  - **Refs:** [Conflict registry](specs/cross-workflow-conflict-and-write-intent.md), [transaction coordinator](specs/transaction-plan-and-commit-coordinator.md).

- [x] `APPROVAL-001` **[GATE_A][SOL_HIGH] Define ApprovalRequirement and resolution expressions.**
  - **Depends:** `GOVERN-002`, `ORG-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORK; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_APPROVAL_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_APPROVAL_001`; `FUZZ=FuzzTodo_APPROVAL_001`; `MUTATION=TestTodo_APPROVAL_001_Mutation`.
  - **RED:** unnamed/unresolvable role, missing scope/cardinality/quorum/SoD/expiry/invalidators or pinned person without policy fails compilation.
  - **GREEN:** named/role/relationship/scoped/group/any/all/quorum expressions resolve candidates with context/version evidence.
  - **REFACTOR:** task recipient and actual decision authority stay distinct.
  - **Refs:** [Workflow approval model](specs/workflow-runtime.md), [human work](specs/human-work-forms-and-rules.md).
  - **Evidence (2026-09-03):** `TestTodo_APPROVAL_001`, `_Mutation`, `FuzzTodo_APPROVAL_001` in `internal/humanwork` (bounded resolution expressions NAMED/ROLE/RELATIONSHIP/GROUP/ANY/ALL/QUORUM with depth and node caps, `ApprovalRequirement` compile with quorum, deadline, escalation and separation constraints, `DeriveRequirements` from the rules engine tier; UNKNOWN_BLOCKED refused); `go test -count=1 ./internal/humanwork/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `APPROVAL-002` **[GATE_A][SOL_HIGH] Bind ApprovalDecision to exact proposal and control context.**
  - **Depends:** `APPROVAL-001`, `INTENT-005`, `MODEL-007`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORK; DIRECT=none; WHY=supply proposal-bound decision evidence used by governed approval intents`.
  - **TEST:** `TestTodo_APPROVAL_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_APPROVAL_002`; `GOLDEN=TestTodo_APPROVAL_002_Golden`; `MUTATION=TestTodo_APPROVAL_002_Mutation`; `SECURITY=TestTodo_APPROVAL_002_Security`.
  - **RED:** altered proposal/policy/context/task/version/arguments or unauthorized approver returns `INVALID_PROPOSAL`/`AUTHORITY_CHANGED` and no accepted decision.
  - **GREEN:** decision binds requirement, proposal/control digests, authority, principal/session, timestamp, reason and evidence.
  - **REFACTOR:** duplicate identical submission replays the same decision ID.
  - **Refs:** [Approval binding](plan.md#53-approval-must-bind-to-an-immutable-proposal), [canonical digest](specs/canonical-envelope-and-digest.md).
  - **Evidence (2026-09-03):** `TestTodo_APPROVAL_002`, `_Golden`, `_Security`, `_Mutation` in `internal/intent/approval` (`ApprovalDecision` bound to the exact ProposalRevision digest, server-held projection seam, control snapshot refs, approver principal reference and injected clock; a decision for another digest is refused; `Vote` cannot carry a client digest); `go test -count=1 ./internal/intent/approval/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `APPROVAL-003` **[GATE_B][SOL_HIGH] Consume approval atomically with executable plan eligibility.**
  - **Evidence (2026-09-05):** `TestTodo_APPROVAL_003` in `internal/intent/approval` (approval consumption is atomic under the stream-head CAS: two concurrent decisions on one task version elect exactly one plan, the loser gets a typed ALREADY_DECIDED refusal and the consumed approval is bound to the elected plan digest; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/intent/approval/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `APPROVAL-002`, `GOVERN-003`, `TX-001`, `TX-002`, `TX-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORK; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_APPROVAL_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_APPROVAL_003`; `PROPERTY=TestTodo_APPROVAL_003_Property`; `MUTATION=TestTodo_APPROVAL_003_Mutation`.
  - **RED:** expired/revoked/superseded/already-used approval or authority churn permits plan preparation/effect.
  - **GREEN:** valid approval set is marked consumed/eligible with the exact plan in the same local transaction; invalid set leaves plan non-executable.
  - **REFACTOR:** reapproval creates new decisions; never mutate prior votes.
  - **Refs:** [Transaction plan](specs/transaction-plan-and-commit-coordinator.md), [Gate B acceptance](execution-plan.md#gate-b-acceptance--limited-write-authority).

- [x] `TX-001` **[GATE_A][SOL_HIGH] Compile immutable non-executable TransactionPlans.**
  - **Depends:** `INTENT-005`, `CONFLICT-002`, `GOVERN-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_TX_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TX_001`; `RACE=TestTodo_TX_001_Race`; `MUTATION=TestTodo_TX_001_Mutation`.
  - **RED:** missing participant/read/write/effect/sequence/idempotency/approval/revalidation/compensation/observation declaration fails compilation.
  - **GREEN:** Gate A plan exposes exact effects and returns `EXECUTION_PROHIBITED_GATE_A` on execute attempt.
  - **REFACTOR:** plan digest derives from canonical semantic content.
  - **Refs:** [Transaction plan contract](specs/transaction-plan-and-commit-coordinator.md), [Gate A matrix](plan.md#phase-1-implementation-depth-matrix).
  - **Evidence (2026-09-03):** `TestTodo_TX_001`, `_Race`, `_Mutation` in `internal/intent` (`CompilePlan` produces an immutable non-executable `TransactionPlan`; no Execute exists); `go test -count=1 ./internal/intent/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `TX-002` **[GATE_B][SOL_HIGH] Resolve and fence the ConsistencyBoundary.**
  - **Depends:** `TX-001`, `CONFLICT-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_TX_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TX_002`; `RACE=TestTodo_TX_002_Race`; `SECURITY=TestTodo_TX_002_Security`; `MUTATION=TestTodo_TX_002_Mutation`; `INTEGRATION=TestTodo_TX_002_Integration`.
  - **RED:** participant outside tenant/cell/database boundary, unsupported stream, stale coordinator epoch or remote effect is admitted to local ACID set.
  - **GREEN:** boundary identifies admitted streams/storage/cell/tenant/protocol/coordinator fence; external participants become durable effects.
  - **REFACTOR:** boundary is explicit data, not deployment assumption.
  - **Refs:** [Model consistency boundary](data/models/kernel-governance-and-evidence.md), [transaction coordinator](specs/transaction-plan-and-commit-coordinator.md).
  - **Evidence (2026-09-03):** `TestTodo_TX_002`, `_Race`, `_Security`, `_Mutation`, `_Integration` (drives a real `intent.CompilePlan` output) in `internal/transaction` (`ResolveConsistencyBoundary`: admission selectors, non-local participants never promoted to the ACID set, unmatched local participant fails closed, tenant and coordinator-epoch checks, `Fence` token, canonical `LockOrder`, digest); P1A value-only slice, no execution; `go test -count=1 ./internal/transaction/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `TX-003` **[GATE_B][SOL_HIGH] Prepare a TransactionPlan against current stream heads.**
  - **Evidence (2026-09-05):** `TestTodo_TX_003` in `internal/transaction/plan` (internal/transaction/plan prepares a TransactionPlan against current stream heads with the plan digest bound to those heads, refused with a typed HEADS_MOVED when any head advances before commit; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/transaction/plan/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TX-002`, `GOVERN-003`, `POSITION-003`, `BUDGET-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_TX_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TX_003`; `GOLDEN=TestTodo_TX_003_Golden`; `RACE=TestTodo_TX_003_Race`; `MUTATION=TestTodo_TX_003_Mutation`.
  - **RED:** stale expected sequence, expired reservation, changed authority/approval, missing idempotency or unresolved conflict returns prepare failure with no writes.
  - **GREEN:** prepared plan records participant heads, fences, revalidation digest and bounded commit expiry.
  - **REFACTOR:** preparation is repeatable and has no external effect.
  - **Refs:** [Multi-stream contract](specs/transaction-plan-and-commit-coordinator.md), [ledger spec](specs/transaction-ledger-reconciliation-and-repair.md).

- [x] `TX-004` **[GATE_B][SOL_HIGH] Atomically commit all local streams and outbox.**
  - **Evidence (2026-09-05):** `TestTodo_TX_004` in `internal/transaction/commit`, `internal/transaction/coordinator`, `test/workflow` (internal/transaction/commit is the commit coordinator: multi-stream ledger append under the plan's heads, idempotent replay under the semantic identity, and a crash at every fault boundary either commits fully or leaves nothing; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/transaction/commit/ ./internal/transaction/coordinator/ ./test/workflow/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TX-003`, `DATA-001`, `DATA-002`, `DATA-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_TX_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TX_004`; `RACE=TestTodo_TX_004_Race`; `FAULT=TestTodo_TX_004_Fault`; `MUTATION=TestTodo_TX_004_Mutation`.
  - **RED:** fault injection before/after every participant/event/projection/outbox write yields no partial correctness-bearing row.
  - **GREEN:** all participant sequences advance and ledger/projection/outbox commit once, or none do; publication occurs only after commit.
  - **REFACTOR:** keep external calls strictly outside the database transaction.
  - **Refs:** [Transaction coordinator](specs/transaction-plan-and-commit-coordinator.md), [ledger/outbox](specs/transaction-ledger-reconciliation-and-repair.md).

- [x] `TX-005` **[GATE_B][SOL_HIGH] Resolve ambiguous local commit outcomes.**
  - **Evidence (2026-09-05):** `TestTodo_TX_005` in `internal/transaction/recovery` (internal/transaction/recovery resolves durable commit evidence after a crash without guessing, including ambiguous and orphan-ledger outcomes, into a typed COMMITTED, ABSENT or AMBIGUOUS verdict; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/transaction/recovery/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TX-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_TX_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TX_005`; `GOLDEN=TestTodo_TX_005_Golden`; `RACE=TestTodo_TX_005_Race`; `FAULT=TestTodo_TX_005_Fault`; `MUTATION=TestTodo_TX_005_Mutation`.
  - **RED:** client timeout/process crash blindly resubmits and duplicates streams/effects or reports failure despite committed transaction.
  - **GREEN:** lookup by transaction/idempotency returns original `COMMITTED`, `ABORTED`, or `AMBIGUOUS_REQUIRES_RECOVERY`; ambiguity is resolved before new effect.
  - **REFACTOR:** wall-clock timeout never defines transaction truth.
  - **Refs:** [Transaction model](specs/transaction-plan-and-commit-coordinator.md), [repair spec](specs/transaction-ledger-reconciliation-and-repair.md).

- [x] `TX-006` **[GATE_B][SOL_HIGH] Implement durable semantic idempotency.**
  - **Depends:** `MODEL-007`, `TX-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_TX_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TX_006`; `GOLDEN=TestTodo_TX_006_Golden`; `RACE=TestTodo_TX_006_Race`; `FAULT=TestTodo_TX_006_Fault`; `SECURITY=TestTodo_TX_006_Security`; `MUTATION=TestTodo_TX_006_Mutation`.
  - **RED:** same key/different digest accepts, concurrent same-key calls commit twice, or retention expires before retry/redelivery window.
  - **GREEN:** `(tenant, capability, effect scope, key)` + same digest returns one stored result/event/effect identity; mismatch returns `IDEMPOTENCY_CONFLICT` with no mutation.
  - **REFACTOR:** idempotency record lifecycle is policy-owned and durable.
  - **Refs:** [Foundation idempotency](specs/platform-foundation-gap-closure.md), [canonical digest](specs/canonical-envelope-and-digest.md).
  - **Evidence (2026-09-03):** `TestTodo_TX_006` (same key and digest returns the stored result, event, effect and evidence identity without re-running the effect; same key with a different digest returns `IDEMPOTENCY_CONFLICT` and mutates nothing), `TestTodo_TX_006_Golden`, `TestTodo_TX_006_Race` (eight real concurrent connections on one scope and digest: the effect runs exactly once and every successful attempt sees the single winner's identity), `TestTodo_TX_006_Fault` (retention shorter than the caller-declared retry window is refused at `Reserve` with `IDEMPOTENCY_RETENTION_TOO_SHORT` before any row exists), `TestTodo_TX_006_Security` (cross-tenant lookup invisible, unscoped transaction sees nothing, forged cross-tenant reserve refused by RLS), `TestTodo_TX_006_Mutation` in `internal/transaction/idempotency` (`Guard(ctx, tx, store, scope, digest, policy, now, fn)` reserves, runs the effect and completes inside the caller's transaction; `Reserve` is `INSERT ... ON CONFLICT DO NOTHING RETURNING` on the composite primary key; migration `00019_idempotency_record.sql` registered in `definitions/storage/storage-disposition.yaml`; expiry is a caller-owned `RetentionPolicy` applied by an explicit `Expire`, no clock inside); `go test -count=1 ./internal/transaction/...` PASS via embedded-postgres on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `TX-007` **[GATE_B][SOL_HIGH] Implement append-only business correction.**
  - **Evidence (2026-09-05):** `TestTodo_TX_007` in `internal/transaction/correction` (internal/transaction/correction appends immutable corrections, advances projections, queues reconciliation obligations and keeps bitemporal reads intact; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/transaction/correction/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TX-004`, `MODEL-014`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_TX_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TX_007`; `RACE=TestTodo_TX_007_Race`; `MUTATION=TestTodo_TX_007_Mutation`.
  - **RED:** correction overwrites original fact/event or lacks target assertion, reason, authority, effective/known time and downstream impact.
  - **GREEN:** successor correction preserves original chronology, updates current domain projection and creates reconciliation obligations for affected consumers.
  - **REFACTOR:** call it business correction, never “fix ledger truth.”
  - **Refs:** [Ledger correction semantics](specs/transaction-ledger-reconciliation-and-repair.md), [model assertion correction](data/models/kernel-governance-and-evidence.md).

- [x] `TX-008` **[GATE_B][SOL_HIGH] Prove cancel-before and cancel-after commit semantics.**
  - **Evidence (2026-09-05):** `TestTodo_TX_008` in `internal/transaction/cancel`, `internal/transaction/commit`, `internal/workflow/execute` (a cancel that reaches the coordinator before its commit point returns a typed CANCELLED result with no ledger rows and no effects, a cancel after the commit point returns the committed result unchanged and launches the governed compensation or correction path, and the boundary crossed (BEFORE_COMMIT or AFTER_COMMIT) is recorded as control evidence, with cancel and commit serialized under a transaction-scoped advisory lock so a concurrent pair resolves to exactly one outcome; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/transaction/cancel/ ./internal/transaction/commit/ ./internal/workflow/execute/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TX-004`, `WF-RUN-013`, `WF-RUN-014`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_TX_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TX_008`; `RACE=TestTodo_TX_008_Race`; `MUTATION=TestTodo_TX_008_Mutation`.
  - **RED:** precommit cancellation leaves rows/effects, or postcommit cancellation rewrites committed success as failure.
  - **GREEN:** before commit returns `CANCELLED` with no writes; after commit returns committed result and launches governed compensation/correction when required.
  - **REFACTOR:** cancellation decision records irreversible boundary.
  - **Refs:** [Workflow cancellation](specs/workflow-runtime.md), [failure/repair principle](plan.md#57-failure-and-repair-are-product-experiences).

- [x] `TX-009` **[GATE_B][SOL_HIGH] Add transaction crash-boundary conformance tests.**
  - **Evidence (2026-09-05):** `TestTodo_TX_009` in `internal/transaction/conformance` (internal/transaction/conformance runs the crash-boundary suite over the commit coordinator, proving every boundary is either fully committed or fully absent on recovery; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/transaction/conformance/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TX-004`–`TX-006`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_TX_009`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TX_009`; `RACE=TestTodo_TX_009_Race`; `FAULT=TestTodo_TX_009_Fault`; `CONFORMANCE=TestTodo_TX_009_Conformance`; `MUTATION=TestTodo_TX_009_Mutation`.
  - **RED:** injected crash at intent, prepare, participant write, event, projection, outbox or acknowledgement reveals duplicate/orphan/missing result.
  - **GREEN:** restart deterministically resumes/returns original result and every external intent exists at most once.
  - **REFACTOR:** deterministic failpoints replace timing-dependent tests.
  - **Refs:** [Transaction/ledger spec](specs/transaction-ledger-reconciliation-and-repair.md), [Gate B scenarios](execution-plan.md#gate-b-acceptance--limited-write-authority).

- [x] `TX-010` **[GATE_B][SOL_HIGH] Enforce per-resource causal effect ordering.**
  - **Evidence (2026-09-05):** `TestTodo_TX_010` in `internal/connectivity/operation` (external effects on one resource key are serialized in causal commit order through a per-resource cursor (migration 00066); written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/connectivity/operation/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TX-004`, `EFFECT-001`, `INTG-011`, `INTG-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_TX_010`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TX_010`; `RACE=TestTodo_TX_010_Race`; `MUTATION=TestTodo_TX_010_Mutation`.
  - **RED:** later worker effect overtakes an unresolved earlier conflicting effect or stale source-authority cutover.
  - **GREEN:** ordering key/expected external version/writer fence serializes conflicts while independent resources progress.
  - **REFACTOR:** ordering is semantic, not one global queue.
  - **Refs:** [Integration operation journal](specs/integration-platform.md), [Gate B external effect requirements](execution-plan.md#gate-b-acceptance--limited-write-authority).

---

## 7. Workflow compiler and all 17 primitive step types

> **Disposition (2026-09-02):** The kernel is ten core primitives plus three structural ones; the heading keeps its old name for anchor stability. P1A compiles `CAPABILITY`, `DECISION` (with `rule_ref`), `TRANSFORM`, `OBSERVE`, `END` in simulate mode. P1B adds `APPROVAL`, `TASK`, `WAIT`, `SIGNAL`, `COMPENSATE` and compiler-placed safe points. `PARALLEL`, `JOIN`, `SUBWORKFLOW` are DESIGN until P1B evidence. `RULE`, `AGENT`, `DOCUMENT`, `CHECKPOINT` are RETIRED as step types.

- [x] `WF-COMP-001` **[GATE_A][SOL_HIGH] Compile typed workflow inputs, outputs and mappings.**
  - **Depends:** `TOOL-004`, `CAP-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_COMP_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_COMP_001`; `GOLDEN=TestTodo_WF_COMP_001_Golden`; `MUTATION=TestTodo_WF_COMP_001_Mutation`.
  - **RED:** missing/retired schema or capability, invalid mapping, incompatible input/output, undeclared context or unknown failure route returns `TYPE_MISMATCH`/`UNRESOLVED_REF`.
  - **GREEN:** compiler emits immutable normalized IR with resolved definition/schema/capability digests.
  - **REFACTOR:** runtime never interprets arbitrary draft configuration.
  - **Refs:** [Workflow runtime](specs/workflow-runtime.md), [step types](workflows/_engine/step-types.md).
  - **Evidence (2026-09-03):** `TestTodo_WF_COMP_001`, `_Golden`, `_Mutation` (typed inputs/outputs/mappings) in `internal/workflow` (`Compile`, `CompiledWorkflow` with digest and `Verify`, `CheckStepConformance`, JSON loader, promotion reference golden `testdata/promotion_plan.json`); `go test -count=1 ./internal/workflow/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `WF-COMP-002` **[GATE_A][SOL_HIGH] Validate workflow graph safety.**
  - **Depends:** `WF-COMP-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_COMP_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_COMP_002`; `PROPERTY=TestTodo_WF_COMP_002_Property`; `FAULT=TestTodo_WF_COMP_002_Fault`; `MUTATION=TestTodo_WF_COMP_002_Mutation`.
  - **RED:** unreachable node, implicit first edge, missing unknown/default route, invalid terminal path, undeclared cycle or unbounded fanout/depth returns the corresponding compile error.
  - **GREEN:** compiled plan includes reachability proof, explicit outcome routes and bounded limits.
  - **REFACTOR:** graph diagnostics identify node/edge/schema source locations.
  - **Refs:** [Workflow kernel](plan.md#10-workflow-execution-kernel), [step coverage](workflows/_engine/step-type-coverage.md).
  - **Evidence (2026-09-03):** `TestTodo_WF_COMP_002`, `_Property`, `_Fault`, `_Mutation` (reachability proof, termination, typed edges) in `internal/workflow` (`Compile`, `CompiledWorkflow` with digest and `Verify`, `CheckStepConformance`, JSON loader, promotion reference golden `testdata/promotion_plan.json`); `go test -count=1 ./internal/workflow/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `WF-COMP-003` **[GATE_A][SOL_HIGH] Analyze side effects and idempotency at compile time.**
  - **Depends:** `WF-COMP-002`, `CAP-002`, `TX-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_COMP_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_COMP_003`; `RACE=TestTodo_WF_COMP_003_Race`; `MUTATION=TestTodo_WF_COMP_003_Mutation`.
  - **RED:** retried mutation without compatible idempotency, irreversible effect without observe/failure/repair route, or mutation in simulation fails publication.
  - **GREEN:** plan declares pure/read/internal/external/irreversible effects, logical effect keys and allowed execution modes.
  - **REFACTOR:** capability manifests remain the source of effect truth.
  - **Refs:** [Workflow runtime](specs/workflow-runtime.md), [simulation contract](workflows/_shared/exploration-contract.md).
  - **Evidence (2026-09-03):** `TestTodo_WF_COMP_003`, `_Race`, `_Mutation` (effect truth from the capability manifest; P1A refuses every write class; promotion reference compiles zero-effect) in `internal/workflow` (`Compile`, `CompiledWorkflow` with digest and `Verify`, `CheckStepConformance`, JSON loader, promotion reference golden `testdata/promotion_plan.json`); `go test -count=1 ./internal/workflow/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `WF-COMP-004` **[GATE_B][SOL_HIGH] Analyze parallel writes and safe points.**
  - **Evidence (2026-09-05):** `TestTodo_WF_COMP_004`, `TestTodo_WF_COMP_004_{Property,Race,Fault,Mutation}`, `TestConcurrency_InterventionEligibilityOnAPlanWithNoAnalysis`, `TestConcurrency_InterventionIsRefusedExactlyInsideARegion`, `TestConcurrency_EligibilityReadsTheIneligibleSetNotTheRegionShape` in `internal/workflow` (compile-time concurrency analysis names the regions where intervention is ineligible; the runtime readers answer eligible on a nil plan or nil analysis so zero-effect plans stay pausable without invalidating published digests); `go test -count=1 ./internal/workflow/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WF-COMP-003`, `CONFLICT-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_COMP_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_COMP_004`; `PROPERTY=TestTodo_WF_COMP_004_Property`; `RACE=TestTodo_WF_COMP_004_Race`; `FAULT=TestTodo_WF_COMP_004_Fault`; `MUTATION=TestTodo_WF_COMP_004_Mutation`.
  - **RED:** conflicting branches, unsafe checkpoint inside atomic/ambiguous region or unaccounted branch effect returns `CONFLICTING_WRITE_SET`/unsafe-point error.
  - **GREEN:** plan emits branch consistency vectors, join contract, safe checkpoints and cancellation eligibility.
  - **REFACTOR:** safe points are compiled facts, not operator guesses.
  - **Refs:** [Workflow safe points](specs/workflow-runtime.md), [context contract](workflows/_engine/workflow-context-contract.md).

- [x] `WF-COMP-005` **[GATE_A][SOL_HIGH] Compile governance and obligation insertion points.**
  - **Depends:** `WF-COMP-001`, `GOVERN-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_COMP_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_COMP_005`; `SECURITY=TestTodo_WF_COMP_005_Security`; `MUTATION=TestTodo_WF_COMP_005_Mutation`.
  - **RED:** missing AuthZ/legal/purpose/risk evaluation, unresolved approval scope or agent output routed to effect without validation fails publication.
  - **GREEN:** compiled plan declares required contexts, decisions, obligations, approval/task requirements and revalidation boundaries.
  - **REFACTOR:** workflow coordinates results; it does not implement legal/AuthZ semantics.
  - **Refs:** [Workflow context layers](workflows/_engine/workflow-context-layers.md), [governance composition](specs/governance-decision-and-obligation-composition.md).
  - **Evidence (2026-09-03):** `TestTodo_WF_COMP_005`, `_Security`, `_Mutation` (governance and obligation insertion points, compiler-placed safe points) in `internal/workflow` (`Compile`, `CompiledWorkflow` with digest and `Verify`, `CheckStepConformance`, JSON loader, promotion reference golden `testdata/promotion_plan.json`); `go test -count=1 ./internal/workflow/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `WF-COMP-006` **[GATE_B][SOL_HIGH] Publish immutable compiled workflow versions.**
  - **Depends:** `WF-COMP-002`–`WF-COMP-005`, `CONFIG-001`, `CONFIG-002`, `CONFIG-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_COMP_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_COMP_006`; `GOLDEN=TestTodo_WF_COMP_006_Golden`; `MUTATION=TestTodo_WF_COMP_006_Mutation`; `SECURITY=TestTodo_WF_COMP_006_Security`.
  - **RED:** unauthorized, changed-after-review, failed-test or unresolved-dependency draft cannot activate.
  - **GREEN:** draft -> validated -> approved -> published -> active stores source/compiled digests, compiler/tool versions, fixtures and approval evidence.
  - **REFACTOR:** active version is immutable; rollback republishes a known version through governance.
  - **Refs:** [Workflow runtime publication](specs/workflow-runtime.md), [environment governance](plan.md#111-environment-and-change-governance).
  - **Evidence (2026-09-03):** `TestTodo_WF_COMP_006` (`Publish` refuses a plan whose digest does not match recompiling its definition, a definition that fails compilation, and missing caller-supplied publication time or semantic version; `Activate` refuses unauthorized, changed-after-review, failed-test, unresolved-dependency, retired and competing-active cases with typed codes; `Resolve` never falls back to latest or active), `TestTodo_WF_COMP_006_Golden` (`testdata/promotion_version.json` and `promotion_version_activated.json` pinned from the promotion reference), `TestTodo_WF_COMP_006_Mutation` (activation, quarantine and supersession never change the record digest, only `Status`; returned values are deep copies), `TestTodo_WF_COMP_006_Security` in `internal/workflow/version` (`CompiledVersion` with definition, plan and record digests and pinned compiler identity; `Store` port with an in-memory `Registry`, Postgres backing deferred to the runtime lane); `go test -count=1 ./internal/workflow/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `WF-STEP-001` **[GATE_A][SOL_HIGH] Implement `CAPABILITY` step conformance.**
  - **Depends:** `WF-COMP-001`, `CAP-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_STEP_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_STEP_001`; `GOLDEN=TestTodo_WF_STEP_001_Golden`; `FAULT=TestTodo_WF_STEP_001_Fault`; `CONFORMANCE=TestTodo_WF_STEP_001_Conformance`; `MUTATION=TestTodo_WF_STEP_001_Mutation`; `SECURITY=TestTodo_WF_STEP_001_Security`.
  - **RED:** wrong version/schema/context, unauthorized scope, mutation in SIMULATE or undeclared retry returns typed failure and no effect.
  - **GREEN:** exact capability invocation returns typed output/evidence and routes explicit `SUCCEEDED`, `REJECTED`, `UNKNOWN`, or `AMBIGUOUS`.
  - **REFACTOR:** persist invocation ID rather than copying unrestricted result.
  - **Refs:** [CAPABILITY step](workflows/_engine/step-types.md#1-capability).
  - **Evidence (2026-09-03):** `TestTodo_WF_STEP_001`, `_Golden`, `_Fault`, `_Conformance`, `_Mutation`, `_Security` (CAPABILITY step against `internal/capability` records) in `internal/workflow` (`Compile`, `CompiledWorkflow` with digest and `Verify`, `CheckStepConformance`, JSON loader, promotion reference golden `testdata/promotion_plan.json`); `go test -count=1 ./internal/workflow/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `WF-STEP-002` **[GATE_A][TERRA] Implement `DECISION` step conformance.**
  - **Depends:** `WF-COMP-001`, `MODEL-024`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_STEP_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_STEP_002`; `GOLDEN=TestTodo_WF_STEP_002_Golden`; `CONFORMANCE=TestTodo_WF_STEP_002_Conformance`.
  - **RED:** mutable/unpinned input, nonexclusive routes or absent `UNKNOWN` route fails deterministically.
  - **GREEN:** pinned typed snapshot selects exactly one explicit route and records evaluator/version/input digest.
  - **REFACTOR:** no side effect or hidden current-state read.
  - **Refs:** [DECISION step](workflows/_engine/step-types.md#2-decision).
  - **Evidence (2026-09-03):** `TestTodo_WF_STEP_002`, `_Golden`, `_Conformance` (DECISION) in `internal/workflow` (`Compile`, `CompiledWorkflow` with digest and `Verify`, `CheckStepConformance`, JSON loader, promotion reference golden `testdata/promotion_plan.json`); `go test -count=1 ./internal/workflow/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `WF-STEP-003` **[GATE_B][SOL_HIGH] Implement `APPROVAL` step conformance.**
  - **Depends:** `APPROVAL-001`–`APPROVAL-003`, `WORK-001`, `WORK-002`, `WORK-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_STEP_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_STEP_003`; `CONFORMANCE=TestTodo_WF_STEP_003_Conformance`; `MUTATION=TestTodo_WF_STEP_003_Mutation`.
  - **RED:** stale proposal/authority, wrong candidate, missing quorum/SoD, duplicate conflicting vote or expired requirement cannot approve.
  - **GREEN:** explicit routes cover `APPROVED`, `REJECTED`, `INVALIDATED`, `EXPIRED`, `CANCELLED`; duplicate identical vote returns original decision.
  - **REFACTOR:** approval task is a HumanTask specialization, not a separate queue engine.
  - **Refs:** [APPROVAL step](workflows/_engine/step-types.md#3-approval), [workflow runtime](specs/workflow-runtime.md).
  - **Evidence (2026-09-03):** `TestTodo_WF_STEP_003` (pure `Resolve` over the APPROVAL-001 requirement: quorum and separation of duties, exactly one of APPROVED, REJECTED, INVALIDATED, EXPIRED, CANCELLED, duplicate identical vote replays the original decision, conflicting duplicate, wrong candidate, missing quorum, expired requirement and stale proposal or authority digest cannot approve; plus a store round trip `Open` to `Route`, `Claim`, `Start`, `Complete`, `Resolve` against a Postgres-backed `workitem.Store`, with a second `Complete` refused by the store's transition guard), `TestTodo_WF_STEP_003_Conformance`, `TestTodo_WF_STEP_003_Mutation` in `internal/workflow/steps/approval` (`Open` creates the ApprovalTask work item pinned to the proposal revision and control snapshot digests; ApprovalTask is a kind on the WorkItem table, not a second queue); `go test -count=1 ./internal/workflow/steps/...` PASS via embedded-postgres on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `WF-STEP-004` **[GATE_B][SOL_HIGH] Implement `TASK` step conformance.**
  - **Depends:** `WORK-001`, `WORK-002`, `FORM-001`, `FORM-002`, `FORM-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_STEP_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_STEP_004`; `RACE=TestTodo_WF_STEP_004_Race`; `CONFORMANCE=TestTodo_WF_STEP_004_Conformance`; `BROWSER=TestTodo_WF_STEP_004_Browser`; `MUTATION=TestTodo_WF_STEP_004_Mutation`; `SECURITY=TestTodo_WF_STEP_004_Security`.
  - **RED:** double claim, expired lease, unauthorized queue, invalid form, missing accessibility/accommodation or duplicate conflicting completion fails.
  - **GREEN:** typed task routes assigned/claimed/submitted/verified/returned/expired/cancelled with immutable work evidence.
  - **REFACTOR:** arbitrary mutable task payload is prohibited.
  - **Refs:** [TASK step](workflows/_engine/step-types.md#4-task), [human work spec](specs/human-work-forms-and-rules.md).
  - **Evidence (2026-09-03):** `TestTodo_WF_STEP_004` (`Open`, `Route`, `Claim`, `Start`, then SUCCEEDED, RETURNED, EXPIRED, CANCELLED and AWAITING routes with immutable work evidence), `TestTodo_WF_STEP_004_Race` (two real connections race `Claim` on one version, exactly one wins), `TestTodo_WF_STEP_004_Conformance` (wrong node, wrong candidate, invalid form, missing accessibility or accommodation acknowledgement, expired claim refused), `TestTodo_WF_STEP_004_Browser` (no browser in the environment: `RenderForm` renders the accessibility contract as HTML with label-for binding, `required` paired with `aria-required`, and an accommodation acknowledgement citing the compiled policy, asserted server-side), `TestTodo_WF_STEP_004_Mutation` (digest determinism, tampered submission detected by `Verify`, byte-identical replay), `TestTodo_WF_STEP_004_Security` (candidate-set bypass, forged claim id and duplicate completion refused) in `internal/workflow/steps/task` (`Submit` validates the typed submission against the declared schema; arbitrary mutable payloads are refused); `go test -count=1 ./internal/workflow/steps/...` PASS via embedded-postgres on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `WF-STEP-005` **[GATE_B][SOL_HIGH] Implement `WAIT` step conformance.**
  - **Depends:** `MODEL-005`, `WF-RUN-006`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_STEP_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_STEP_005`; `RACE=TestTodo_WF_STEP_005_Race`; `CONFORMANCE=TestTodo_WF_STEP_005_Conformance`; `MUTATION=TestTodo_WF_STEP_005_Mutation`; `FAULT=TestTodo_WF_STEP_005_Fault`.
  - **RED:** in-memory sleep, early/duplicate wake, DST ambiguity or dataset change without policy cannot advance.
  - **GREEN:** durable timer returns `FIRED`, `TIMER_REVIEW_REQUIRED`, `SUPERSEDED`, or `CANCELLED` exactly once semantically.
  - **REFACTOR:** preserve calendar/tzdb version and calculation evidence.
  - **Refs:** [WAIT step](workflows/_engine/step-types.md#5-wait), [workflow context](workflows/_engine/workflow-context-contract.md).
  - **Evidence (2026-09-03):** `TestTodo_WF_STEP_005` (early wake refused with `ErrEarlyWake`; duplicate wake replays the prior `Resolution` byte for byte; DST gap and fold in America/New_York mint a `ReviewRequired` requirement instead of a guess; a missing `ReferenceUpdatePolicy` is refused), `TestTodo_WF_STEP_005_Race`, `TestTodo_WF_STEP_005_Conformance` (parses the package's own source with go/ast and fails on any `time.Sleep`, `time.Now`, goroutine or `time` import; every outcome maps to a declared WAIT route), `TestTodo_WF_STEP_005_Mutation`, `TestTodo_WF_STEP_005_Fault` in `internal/workflow/steps/wait` (`ComputeTimerRequirement` over a typed `CompiledWaitNode` reusing `internal/kernel/values` timer references and disambiguation; `Resolve(req, now, event)` returns exactly one of FIRED, TIMER_REVIEW_REQUIRED, SUPERSEDED, CANCELLED and maps to `frontier.NodeOutcome`; no timer wheel per the WF-RUN-000 gate, caller supplies every instant; the compiler does not yet carry WAIT spec fields, so the typed node view is populated by the caller until a compiler pass lands); `go test -count=1 ./internal/workflow/steps/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02. Compiler binding (2026-09-03): `WaitSpec` on `Node`, `CompiledNode.Wait`, `wait.FromCompiled`; `TestWaitAndSignalNodesCompileWithExplicitRoutes` in `internal/workflow`, `TestWaitBindingFromCompiledPlan` in `internal/workflow/steps/wait`, `TestFrontierEmitsTimerAndSignalIntentsForCompiledNodes` in `internal/workflow/frontier`; fixture `testdata/wait_signal_fixture.json`; every pre-existing golden digest unchanged.

- [x] `WF-STEP-006` **[GATE_B][SOL_HIGH] Implement `SIGNAL` step conformance.**
  - **Depends:** `WF-RUN-007`, `INTG-018`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_STEP_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_STEP_006`; `GOLDEN=TestTodo_WF_STEP_006_Golden`; `SECURITY=TestTodo_WF_STEP_006_Security`; `CONFORMANCE=TestTodo_WF_STEP_006_Conformance`; `MUTATION=TestTodo_WF_STEP_006_Mutation`.
  - **RED:** wrong tenant/correlation/schema/signature/source/order, duplicate-different-bytes, late or unmatched signal resumes nothing and returns typed status.
  - **GREEN:** accepted signal is durably correlated and schedules continuation once; duplicates remain inspectable.
  - **REFACTOR:** payload retains taint/classification.
  - **Refs:** [SIGNAL step](workflows/_engine/step-types.md#6-signal).
  - **Evidence (2026-09-03):** `TestTodo_WF_STEP_006`, `TestTodo_WF_STEP_006_Golden` (`testdata/accepted_signal_golden.json` pins the full immutable `LogEntry` render), `TestTodo_WF_STEP_006_Security` (forged signature and duplicate idempotency key with different bytes refused as incidents; payload taint survives into the log unchanged), `TestTodo_WF_STEP_006_Conformance`, `TestTodo_WF_STEP_006_Mutation` in `internal/workflow/steps/signal` (`Accept(sub, sig, prior, verify, now)` returns ACCEPTED, DUPLICATE_SAME_BYTES or one of nine typed refusals for wrong tenant, correlation, schema, signature, source or order, duplicate-different-bytes, late and unmatched; accepted and duplicate signals stay inspectable in `SignalLog`; `Verifier` port for signatures; result maps to `frontier.NodeOutcome`); `go test -count=1 ./internal/workflow/steps/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02. Compiler binding (2026-09-03): `SignalSpec` on `Node`, `CompiledNode.Signal`, `signal.FromCompiled`; `TestSignalBindingFromCompiledPlan` in `internal/workflow/steps/signal`; un-annotated WAIT and SIGNAL nodes now classify as pure effects (`effects.go` default fixed).

- [ ] `WF-STEP-007` **[DESIGN][SOL_HIGH] Implement bounded `PARALLEL` execution.**
  - **Depends:** `WF-COMP-004`, `ADMISSION-001`, `ADMISSION-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_STEP_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_STEP_007`; `RACE=TestTodo_WF_STEP_007_Race`; `MUTATION=TestTodo_WF_STEP_007_Mutation`.
  - **RED:** unbounded branch count, conflicting writes, exceeded budget or undefined failure/cancel propagation is rejected.
  - **GREEN:** admitted branches have independent identity/idempotency and exact succeeded/failed/cancelled results.
  - **REFACTOR:** Phase 1 may use only fixed compiled branches required by Promotion.
  - **Refs:** [PARALLEL step](workflows/_engine/step-types.md#7-parallel).

- [ ] `WF-STEP-008` **[DESIGN][SOL_HIGH] Implement `JOIN` strategies.**
  - **Depends:** `WF-STEP-007`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_STEP_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_STEP_008`; `GOLDEN=TestTodo_WF_STEP_008_Golden`; `MUTATION=TestTodo_WF_STEP_008_Mutation`.
  - **RED:** missing mandatory branch or impossible quorum cannot report success; unknown branch cannot be coerced false.
  - **GREEN:** `ALL`, `ANY`, `QUORUM`, `REQUIRED_SET`, `BEST_EFFORT` return typed aggregate and degraded/unknown dimensions.
  - **REFACTOR:** strategy/version is part of compiled plan.
  - **Refs:** [JOIN step](workflows/_engine/step-types.md#8-join).

- [ ] `WF-STEP-009` **[DESIGN][SOL_HIGH] Implement bounded `SUBWORKFLOW`.**
  - **Depends:** `WF-RUN-001`, `WF-RUN-004`, `WF-RUN-005`, `WF-COMP-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_STEP_009`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_STEP_009`; `GOLDEN=TestTodo_WF_STEP_009_Golden`; `MUTATION=TestTodo_WF_STEP_009_Mutation`.
  - **RED:** child expands authority, exceeds depth/fanout, changes version, disappears when detached or loses cancellation obligation.
  - **GREEN:** child is version-pinned, scope-attenuated and reports `CANCELLED`, `COMPENSATED`, `ALREADY_COMPLETED`, or `CANNOT_CANCEL` to parent.
  - **REFACTOR:** parent completion never overwrites child truth.
  - **Refs:** [SUBWORKFLOW step](workflows/_engine/step-types.md#9-subworkflow).

- [x] `WF-STEP-010` **[GATE_A][SOL_LOW] Implement deterministic `TRANSFORM`.**
  - **Depends:** `WF-COMP-001`, `XFORM-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_STEP_010`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_STEP_010`; `GOLDEN=TestTodo_WF_STEP_010_Golden`.
  - **RED:** arbitrary code, network/time/randomness, unpinned lookup, resource excess or unsatisfied taint sanitizer fails.
  - **GREEN:** golden vectors return byte-stable typed output, transform/version/input digest and taint lineage.
  - **REFACTOR:** reuse the constrained transformation engine.
  - **Refs:** [TRANSFORM step](workflows/_engine/step-types.md#10-transform).
  - **Evidence (2026-09-03):** `TestTodo_WF_STEP_010`, `_Golden` (deterministic TRANSFORM) in `internal/workflow` (`Compile`, `CompiledWorkflow` with digest and `Verify`, `CheckStepConformance`, JSON loader, promotion reference golden `testdata/promotion_plan.json`); `go test -count=1 ./internal/workflow/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [ ] `WF-STEP-011` **[RETIRED][SOL_HIGH] RETIRED 2026-09-02: Implement deterministic `RULE`.**
  - **Disposition:** RETIRED 2026-09-02. `RULE` is not a step type; `DECISION` with `rule_ref` (`WF-STEP-002`) evaluates the published threshold table. Dependencies on this ID resolve as satisfied; its tests are not written.
  - **Depends:** `RULE-001`, `RULE-002`, `RULE-003`, `WF-COMP-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_STEP_011`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_STEP_011`; `MUTATION=TestTodo_WF_STEP_011_Mutation`.
  - **RED:** stale/unknown input becomes false, arbitrary code executes, or side effect occurs during evaluation.
  - **GREEN:** pinned rule returns `PASS`, `FAIL`, `UNKNOWN`, or `PARTIAL` plus explanation/obligations/version.
  - **REFACTOR:** legal/AuthZ/regulatory engines keep their own semantics behind capabilities.
  - **Refs:** [RULE step](workflows/_engine/step-types.md#11-rule), [rules spec](specs/human-work-forms-and-rules.md).

- [ ] `WF-STEP-012` **[RETIRED][SOL_HIGH] RETIRED 2026-09-02: Specify governed `AGENT` step behavior.**
  - **Disposition:** RETIRED 2026-09-02. `AGENT` is not a step type; an agent-eligible `CAPABILITY` is governed by its manifest and the agent security boundary (§18), which has no phase. Dependencies on this ID resolve as satisfied; its tests are not written.
  - **Depends:** `AGENT-001`–`AGENT-005`, `CAP-002`, `GOVERN-002`, `TRUST-010`, `TRUST-018`, `WF-COMP-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_WF_STEP_012`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_STEP_012`; `PROPERTY=TestTodo_WF_STEP_012_Property`; `GOLDEN=TestTodo_WF_STEP_012_Golden`; `SECURITY=TestTodo_WF_STEP_012_Security`; `CONFORMANCE=TestTodo_WF_STEP_012_Conformance`; `MUTATION=TestTodo_WF_STEP_012_Mutation`.
  - **RED:** agent obtains write credential, expands authority, exceeds data/tool/cost budget or routes unvalidated output to effect.
  - **GREEN:** conformance fixture permits read/analyze/draft only, validates schema/grounding/taint and exercises kill switch/fallback.
  - **REFACTOR:** production implementation remains `OUT` until a signed scope exchange.
  - **Refs:** [AGENT step](workflows/_engine/step-types.md#12-agent), [AI governance](plan.md#96-ai-governance-contract).

- [ ] `WF-STEP-013` **[RETIRED][SOL_HIGH] RETIRED 2026-09-02: Specify `DOCUMENT` step evidence states.**
  - **Disposition:** RETIRED 2026-09-02. `DOCUMENT` is not a step type; document generation and signature are `documents.*` capabilities (§23). Dependencies on this ID resolve as satisfied; its tests are not written.
  - **Depends:** `MODEL-029`, `DOC-MAL-001`, `DOC-INTAKE-001`, `DOC-TEMPLATE-001`, `DOC-SIGN-001`, `DOC-EVIDENCE-001`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_WF_STEP_013`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_STEP_013`; `GOLDEN=TestTodo_WF_STEP_013_Golden`; `INTEGRATION=TestTodo_WF_STEP_013_Integration`; `FAULT=TestTodo_WF_STEP_013_Fault`; `SECURITY=TestTodo_WF_STEP_013_Security`; `CONFORMANCE=TestTodo_WF_STEP_013_Conformance`; `MUTATION=TestTodo_WF_STEP_013_Mutation`.
  - **RED:** provider acceptance is called delivery/signature, unsafe bytes are rendered, or missing template/locale/classification/evidence passes.
  - **GREEN:** fixture distinguishes generated, delivered, acknowledged, signed, declined, expired, failed and ambiguous with immutable artifact hashes.
  - **REFACTOR:** Gate B implements only bounded provider integration if selected.
  - **Refs:** [DOCUMENT step](workflows/_engine/step-types.md#13-document), [document models](data/models/connectivity-access-content.md).

- [x] `WF-STEP-014` **[GATE_A][SOL_HIGH] Implement `OBSERVE` step conformance.**
  - **Depends:** `INTG-009`, `INTG-010`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_STEP_014`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_STEP_014`; `CONFORMANCE=TestTodo_WF_STEP_014_Conformance`; `MUTATION=TestTodo_WF_STEP_014_Mutation`.
  - **RED:** submission/HTTP success is accepted as observed business state; stale/unavailable/partial result becomes pass.
  - **GREEN:** exact observation yields `PASS`, `FAIL`, `UNKNOWN`, or `PARTIAL` with source watermark and reconciliation result.
  - **REFACTOR:** retry exhaustion creates degraded/repair state, not false completion.
  - **Refs:** [OBSERVE step](workflows/_engine/step-types.md#14-observe), [external outcomes principle](plan.md#56-external-outcomes-must-be-observed).
  - **Evidence (2026-09-03):** `TestTodo_WF_STEP_014`, `_Conformance`, `_Mutation` (OBSERVE) in `internal/workflow` (`Compile`, `CompiledWorkflow` with digest and `Verify`, `CheckStepConformance`, JSON loader, promotion reference golden `testdata/promotion_plan.json`); `go test -count=1 ./internal/workflow/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [ ] `WF-STEP-015` **[RETIRED][SOL_HIGH] RETIRED 2026-09-02: Implement `CHECKPOINT` eligibility.**
  - **Disposition:** RETIRED 2026-09-02. `CHECKPOINT` is not a step type; `safe_point` is a node attribute the compiler places (`WF-COMP-004`). Dependencies on this ID resolve as satisfied; its tests are not written.
  - **Depends:** `WF-COMP-004`, `WF-RUN-001`, `WF-RUN-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_STEP_015`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_STEP_015`; `PROPERTY=TestTodo_WF_STEP_015_Property`; `FAULT=TestTodo_WF_STEP_015_Fault`; `MUTATION=TestTodo_WF_STEP_015_Mutation`; `RACE=TestTodo_WF_STEP_015_Race`.
  - **RED:** checkpoint inside atomic/ambiguous effect region, with unpersisted output or unresolved mandatory branch is rejected.
  - **GREEN:** durable checkpoint records state/version/context/effect summary and safe pause/cancel/migrate eligibility.
  - **REFACTOR:** checkpoint is evidence, not a process-memory marker.
  - **Refs:** [CHECKPOINT step](workflows/_engine/step-types.md#15-checkpoint).

- [ ] `WF-STEP-016` **[GATE_B][SOL_HIGH] Implement `COMPENSATE` step conformance.**
  - **Depends:** `INTG-016`, `TX-008`, `TX-009`, `TX-010`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_STEP_016`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_STEP_016`; `GOLDEN=TestTodo_WF_STEP_016_Golden`; `RACE=TestTodo_WF_STEP_016_Race`; `CONFORMANCE=TestTodo_WF_STEP_016_Conformance`; `RECOVERY=TestTodo_WF_STEP_016_Recovery`; `MUTATION=TestTodo_WF_STEP_016_Mutation`; `SECURITY=TestTodo_WF_STEP_016_Security`.
  - **RED:** unauthorized/non-idempotent correction, impossible reversal or failed verification reports restored success.
  - **GREEN:** corrective capability plus observation returns `COMPENSATED`, `PARTIAL`, `FAILED`, or `REPAIR_REQUIRED` and preserves original history.
  - **REFACTOR:** compensation is business action, never database undo.
  - **Refs:** [COMPENSATE step](workflows/_engine/step-types.md#16-compensate), [repair spec](specs/transaction-ledger-reconciliation-and-repair.md).

- [x] `WF-STEP-017` **[GATE_A][SOL_HIGH] Implement `END` over the five intent dimensions.**
  - **Depends:** `INTENT-003`, `WF-COMP-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_STEP_017`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_STEP_017`; `MUTATION=TestTodo_WF_STEP_017_Mutation`.
  - **RED:** unresolved mandatory obligation or degraded external state is collapsed to generic success, or `END` writes a sixth dimension.
  - **GREEN:** terminal artifact records the instance `runtime_status` and the intent's `ExecutionState`, `BusinessState`, `ConsistencyState` and `ObligationState` (for example `COMMITTED / COMPLETED / DEGRADED / SATISFIED`), and `RequestState` moves only under the closure rules.
  - **REFACTOR:** phase-specific workflows select allowed terminal combinations declaratively from the fixed kernel rules, not a per-definition lattice.
  - **Refs:** [END step](workflows/_engine/step-types.md#17-end), [five dimensions](specs/business-intent-and-change-request.md#five-lifecycle-dimensions), [completion model](reference-workflows/promote-into-management.md).
  - **Evidence (2026-09-03):** `TestTodo_WF_STEP_017`, `_Mutation` (END over the five dimensions delegated to `lifecycle.Check`) in `internal/workflow` (`Compile`, `CompiledWorkflow` with digest and `Verify`, `CheckStepConformance`, JSON loader, promotion reference golden `testdata/promotion_plan.json`); `go test -count=1 ./internal/workflow/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

---

## 8. Durable workflow runtime, recovery and intervention

> **Disposition (2026-09-02):** P1B, after `WF-RUN-000` (the build-or-adopt decision) is recorded. P1A persists instances and nodes for simulate mode only, with no timers or leases. Replay, shadow, live migration and the full intervention taxonomy are DESIGN.

- [x] `WF-RUN-000` **[GATE_B][SOL_HIGH] Record the durable-runtime build-or-adopt decision before P1B code.**
  - **Depends:** `WF-COMP-001`, `DATA-001`, `LIB-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=decide once whether an embedded Go durable-execution library supplies the scheduler, timers and leases, so that §8 does not build a workflow engine by default`.
  - **TEST:** `TestDurableRuntimeAdoptionDecisionRecordIsCompleteAndEvidenced`.
  - **TEST MATRIX:** `PRIMARY=TestDurableRuntimeAdoptionDecisionRecordIsCompleteAndEvidenced`; `GOLDEN=TestTodo_WF_RUN_000_Golden`; `INTEGRATION=TestTodo_WF_RUN_000_Integration`; `CONFORMANCE=TestTodo_WF_RUN_000_Conformance`.
  - **RED:** the decision record lacks at least one evaluated candidate, a pass/fail result against each of the four non-negotiables (ledger outside engine history; tenant isolation, fencing and effect idempotency; inspectable node/timer/lease projections; safe-point pause and version pinning), or a signed choice; or P1B runtime code exists before the record does.
  - **GREEN:** one immutable record names the candidates, the fixture results per non-negotiable, the choice (`BUILD` or `ADOPT:<module>@<version>`) and the consequences for `WF-RUN-001`–`WF-RUN-025`; an adopted library passes the same conformance fixtures the built scheduler would.
  - **REFACTOR:** migration, shadow mode, replay and the intervention taxonomy are not adoption criteria.
  - **Refs:** [Build or adopt](specs/workflow-runtime.md#build-or-adopt), [next-steps P1B](next-steps.md#p1b--one-bounded-authority-amendment).
  - **Evidence (2026-09-03):** `TestDurableRuntimeAdoptionDecisionRecordIsCompleteAndEvidenced` (12 RED cases), `TestTodo_WF_RUN_000_{Golden,Integration,Conformance}` in `tools/policy/runtimedecision`; record at `definitions/runtime/durable-runtime-decision.yaml`; `go test -count=1 ./tools/policy/runtimedecision/...` PASS. Decision: BUILD on the in-house Postgres outbox/projection primitives for P1A; Temporal, Restate, DBOS Transact and river recorded as not yet evaluated against a conformance fixture (no invented benchmarks); blocking re-evaluation gate before any P1B scheduler/lease/timer code; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `WF-RUN-001` **[GATE_A][SOL_HIGH] Persist WorkflowInstance and NodeExecution state.**
  - **Depends:** `WF-COMP-001`, `DATA-001`, `DATA-002`, `DATA-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_RUN_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_RUN_001`; `RACE=TestTodo_WF_RUN_001_Race`; `FAULT=TestTodo_WF_RUN_001_Fault`; `SECURITY=TestTodo_WF_RUN_001_Security`; `MUTATION=TestTodo_WF_RUN_001_Mutation`.
  - **RED:** process restart loses frontier/input/variables/version/context/attempt or permits illegal/stale transition.
  - **GREEN:** optimistic instance version persists legal instance/node state, frontier and typed output refs with stable identities; stale writer returns `CONFLICT_STALE_INSTANCE`.
  - **REFACTOR:** business evidence remains in ledger; runtime store answers current execution state.
  - **Refs:** [Workflow runtime](specs/workflow-runtime.md), [runtime entities](data/models/kernel-governance-and-evidence.md).
  - **Evidence (2026-09-03):** `TestTodo_WF_RUN_001` (frontier, input ref, variables, instance version, pinned context and attempt read back on a fresh connection after commit; an overtaken writer gets `CONFLICT_STALE_INSTANCE`), `TestTodo_WF_RUN_001_Race` (six concurrent writers on six connections, exactly one commit and one node-execution row), `TestTodo_WF_RUN_001_Fault` (illegal instance and node transitions refused without consuming a version; rolled-back transaction persists nothing), `TestTodo_WF_RUN_001_Security` (cross-tenant read is `INSTANCE_NOT_FOUND` under `hcmnext_app`, unscoped transaction sees nothing, cross-tenant insert refused by RLS), `TestTodo_WF_RUN_001_Mutation` (derived node-execution id collides on re-record, hand-set id refused, Go status tables and migration CHECK sets asserted identical) in `internal/workflow/runtime`; migration `00016_workflow_runtime.sql` adds `workflow_instance` and `workflow_node_execution` (registered in `definitions/storage/storage-disposition.yaml`, no lease/timer/retry columns per the WF-RUN-000 gate); port is Create/Record/Load only, start and advance follow in WF-RUN-023/025; `go test -count=1 ./internal/workflow/... ./internal/data/pgtest/...` PASS via embedded-postgres on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `WF-RUN-002` **[GATE_B][SOL_HIGH] Implement execution leases and fencing.**
  - **Evidence (2026-09-05):** `TestTodo_WF_RUN_002` in `internal/workflow/lease`, `internal/workflow/runtime` (Manager acquire/renew/release/expire/observe/verify/history over runtimestate.LeaseStore with scheme-qualified Identity (bare hostnames refused), Verify under a CurrentForUpdate row lock so an accepted fence holds for the caller's transaction, concurrent acquirers serialized on an advisory lock with typed ErrHeld, monotonic never-reused tokens, no clock/ticker/goroutine (source-level conformance scan), plus additive runtime.AdvanceFenced refusing a stale fence before any read (asserted by a recording executor); written by an Opus subagent and verified independently); `go test -count=1 ./internal/workflow/lease/ ./internal/workflow/runtime/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WF-RUN-001`, `TRUST-006`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_RUN_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_RUN_002`; `RACE=TestTodo_WF_RUN_002_Race`; `FAULT=TestTodo_WF_RUN_002_Fault`; `MUTATION=TestTodo_WF_RUN_002_Mutation`.
  - **RED:** expired/stale worker heartbeat or fence completes a node, writes state or dispatches an effect.
  - **GREEN:** acquire/heartbeat/expire/reclaim is durable; stale attempt returns `LEASE_LOST` or `FENCE_STALE` with zero mutation.
  - **REFACTOR:** lease owner is workload identity, not process hostname alone.
  - **Refs:** [Workflow leases](specs/workflow-runtime.md), [step context contract](workflows/_engine/workflow-context-contract.md).

- [x] `WF-RUN-003` **[GATE_B][SOL_HIGH] Recover a node after worker death.**
  - **Evidence (2026-09-05):** `TestTodo_WF_RUN_003` in `internal/workflow/recover` (internal/workflow/recover reclaims a node whose worker died: the lease expires, the node returns to READY with its attempt counted and no duplicate effect is emitted; written by an Opus subagent and verified independently); `go test -count=1 ./internal/workflow/recover/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WF-RUN-002`, `TX-006`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_RUN_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_RUN_003`; `RACE=TestTodo_WF_RUN_003_Race`; `FAULT=TestTodo_WF_RUN_003_Fault`; `RECOVERY=TestTodo_WF_RUN_003_Recovery`; `MUTATION=TestTodo_WF_RUN_003_Mutation`.
  - **RED:** crashes before/after node-state commit or capability dispatch lose work or duplicate a business effect.
  - **GREEN:** lease expiry schedules a new attempt that replays committed result or safely executes the missing effect once.
  - **REFACTOR:** deterministic crash failpoints cover every persistence boundary.
  - **Refs:** [Workflow recovery](specs/workflow-runtime.md), [Gate B process-death acceptance](execution-plan.md#gate-b-acceptance--limited-write-authority).

- [x] `WF-RUN-004` **[GATE_B][SOL_HIGH] Implement durable workflow timers.**
  - **Evidence (2026-09-05):** `TestTodo_WF_RUN_004` in `internal/workflow/timer`, `internal/workflow/execute`, `test/workflow` (durable timers keyed by the wait requirement's content digest (zone, tzdb release, calendar ref and version, reference-update policy) so a republished dataset yields a different promise and CheckRequirement refuses drift, Fire verifying the lease fence then applying SCHED-002 misfire policy per due timer under version CAS and enqueuing workflow_ready_work, CancelInstance withdrawing promises, additive execute.ResumeTimer with checkTimerDrift mirroring WF-RUN-028, and an end-to-end Promotion run parking on a real workflow_timer row, a second replica firing it and the superseded holder refused with ErrFenceStale; timer.Scheduler.Attempts stays unwired until WF-RUN-006 and the production TimerFactory/TimerReader adapters for internal/platform/execution are a follow-up; written by an Opus subagent and verified independently); `go test -count=1 ./internal/workflow/timer/ ./internal/workflow/execute/ ./test/workflow/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WF-RUN-001`, `MODEL-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_RUN_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_RUN_004`; `RACE=TestTodo_WF_RUN_004_Race`; `FAULT=TestTodo_WF_RUN_004_Fault`; `MUTATION=TestTodo_WF_RUN_004_Mutation`.
  - **RED:** timer loss, double wake, early wake, clock skew or dataset revision silently changes execution date.
  - **GREEN:** timer persists wake target/calendar/timezone/dataset policy, claims with fence and enqueues semantic continuation once.
  - **REFACTOR:** shard/index upcoming timers without changing identity.
  - **Refs:** [Workflow timers](specs/workflow-runtime.md), [WAIT sample coverage](workflows/_engine/step-type-coverage.md).

- [x] `WF-RUN-005` **[GATE_B][SOL_HIGH] Implement durable signal subscriptions.**
  - **Evidence (2026-09-07):** `TestTodo_WF_RUN_005`, `TestTodo_WF_RUN_005_Fault`, `TestTodo_WF_RUN_005_Mutation`, `TestTodo_WF_RUN_005_Race`, `TestTodo_WF_RUN_005_Security` in `internal/data/signals` (written in 14442de on 2026-09-06 and never ticked; the named tests prove the GREEN contract: receipt, dedupe reservation, subscription match and continuation enqueue commit atomically; all dispositions remain inspectable); `go test -count=1 ./internal/data/signals/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WF-RUN-001`, `INTG-018`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_RUN_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_RUN_005`; `RACE=TestTodo_WF_RUN_005_Race`; `FAULT=TestTodo_WF_RUN_005_Fault`; `MUTATION=TestTodo_WF_RUN_005_Mutation`; `SECURITY=TestTodo_WF_RUN_005_Security`.
  - **RED:** duplicate/late/unmatched/unauthorized/incompatible signal resumes twice or disappears without evidence.
  - **GREEN:** receipt, dedupe reservation, subscription match and continuation enqueue commit atomically; all dispositions remain inspectable.
  - **REFACTOR:** continuation consumes reference, not unrestricted raw payload.
  - **Refs:** [Workflow signals](specs/workflow-runtime.md), [SIGNAL step](workflows/_engine/step-types.md#6-signal).

- [ ] `WF-RUN-006` **[GATE_B][SOL_HIGH] Implement one immediate-caller retry policy.**
  - **Depends:** `WF-RUN-001`, `ADMISSION-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_RUN_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_RUN_006`; `RACE=TestTodo_WF_RUN_006_Race`; `FAULT=TestTodo_WF_RUN_006_Fault`; `MUTATION=TestTodo_WF_RUN_006_Mutation`.
  - **RED:** nested layers multiply attempts, retry nonretryable error, exceed deadline/budget or ignore `DO_NOT_RETRY`.
  - **GREEN:** classifier, max attempts, exponential jitter, deadline and propagated budget produce exact next-attempt or terminal route.
  - **REFACTOR:** domain/provider error mapping stays separate from retry scheduling.
  - **Refs:** [Workflow retries](specs/workflow-runtime.md), [overload principle](plan.md#519-platform-correctness-is-business-correctness).

- [ ] `WF-RUN-007` **[GATE_B][SOL_HIGH] Quarantine poison node execution.**
  - **Depends:** `WF-RUN-006`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_RUN_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_RUN_007`; `GOLDEN=TestTodo_WF_RUN_007_Golden`; `RACE=TestTodo_WF_RUN_007_Race`; `FAULT=TestTodo_WF_RUN_007_Fault`; `MUTATION=TestTodo_WF_RUN_007_Mutation`.
  - **RED:** exhausted node retries forever, is dropped, or reports workflow success.
  - **GREEN:** `QuarantinedWork` retains attempts/error/ambiguity/idempotency/owner/SLA/next action and routes workflow `BLOCKED`, `REPAIR_REQUIRED`, or `QUARANTINED`.
  - **REFACTOR:** dead-letter storage is an operations projection, not a terminal business state.
  - **Refs:** [Workflow poison behavior](specs/workflow-runtime.md), [incident spec](specs/incident-management.md).

- [x] `WF-RUN-008` **[GATE_B][SOL_HIGH] Implement instance pause at safe points.**
  - **Evidence (2026-09-05):** `TestTodo_WF_RUN_008`, `TestTodo_WF_RUN_008_{Golden,Race,Fault,Mutation}`, `TestPause_RequestValidationRefusesBeforeAnyRead`, `TestPause_ResumeValidationRefusesBeforeAnyRead`, `TestPause_RefusalCodesAreStable` in `internal/workflow/runtime` and `TestResumeRefusesWhileTheInstanceIsPaused` in `internal/workflow/execute` (pause/resume as governed transitions in the durable transition table, Advance gated while paused, malformed requests refused before any read, an unfenced resume refused while a replayed pause is answered from the stored row, checkpoints in `workflow_checkpoint`); `go test -count=1 ./internal/workflow/runtime/ ./internal/workflow/execute/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WF-STEP-015`, `WF-RUN-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_RUN_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_RUN_008`; `GOLDEN=TestTodo_WF_RUN_008_Golden`; `RACE=TestTodo_WF_RUN_008_Race`; `FAULT=TestTodo_WF_RUN_008_Fault`; `MUTATION=TestTodo_WF_RUN_008_Mutation`.
  - **RED:** pause inside atomic/ambiguous region reports `PAUSED` or abandons active effect.
  - **GREEN:** request yields `PAUSE_REQUESTED`, then `PAUSED` only at eligible checkpoint; resume revalidates current context.
  - **REFACTOR:** instance pause is distinct from version/workload quarantine.
  - **Refs:** [Workflow pause](specs/workflow-runtime.md), [safe points](specs/workflow-runtime.md#safe-points-and-atomic-regions).

- [x] `WF-RUN-009` **[GATE_B][SOL_HIGH] Quarantine a bad workflow version.**
  - **Evidence (2026-09-05):** `TestTodo_WF_RUN_009` in `internal/workflow/quarantine` (a quarantine record for a workflow definition version with reason, evidence ref, declarer and distinct approver that refuses new starts on that version and flags live instances for a migration-preview decision, lifted only by a distinct reviewer through a new record, every action a digested event; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/workflow/quarantine/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WF-COMP-006`, `WF-RUN-008`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_RUN_009`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_RUN_009`; `RACE=TestTodo_WF_RUN_009_Race`; `FAULT=TestTodo_WF_RUN_009_Fault`; `MUTATION=TestTodo_WF_RUN_009_Mutation`.
  - **RED:** quarantined version starts new instances or running instances cross a safe point without declared disposition.
  - **GREEN:** new starts block; running instances pause/continue/repair according to signed incident policy; release requires validation evidence.
  - **REFACTOR:** version quarantine does not disable unrelated capabilities/versions.
  - **Refs:** [Bad-version response](plan.md#16-strategic-decisions), [workflow runtime](specs/workflow-runtime.md).

- [ ] `WF-RUN-010` **[GATE_B][SOL_HIGH] Implement governed workflow cancellation.**
  - **Depends:** `WF-RUN-001`, `TX-008`, `WF-STEP-016`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_RUN_010`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_RUN_010`; `RACE=TestTodo_WF_RUN_010_Race`; `FAULT=TestTodo_WF_RUN_010_Fault`; `MUTATION=TestTodo_WF_RUN_010_Mutation`.
  - **RED:** cancellation kills a goroutine, deletes history, skips irreversible-effect analysis or declares complete with unresolved child/effect.
  - **GREEN:** policy returns clean `CANCELLED`, compensation-required, cannot-cancel or `REPAIR_REQUIRED`, with phase/effect evidence.
  - **REFACTOR:** each node/capability declares cancellation semantics.
  - **Refs:** [Workflow cancellation](specs/workflow-runtime.md), [cancellation reference sample](workflows/samples/equipment-shipment-cancellation.md).

- [ ] `WF-RUN-011` **[PHASE_2][SOL_HIGH] Propagate cancellation to child workflows.**
  - **Depends:** `WF-STEP-009`, `WF-RUN-010`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_RUN_011`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_RUN_011`; `GOLDEN=TestTodo_WF_RUN_011_Golden`; `RACE=TestTodo_WF_RUN_011_Race`; `FAULT=TestTodo_WF_RUN_011_Fault`; `MUTATION=TestTodo_WF_RUN_011_Mutation`.
  - **RED:** parent closes cancelled while child is unresolved or detached child loses accountable owner.
  - **GREEN:** every child reports cancelled/compensated/cannot-cancel/already-completed and parent reconciles mandatory outcomes.
  - **REFACTOR:** child truth is never overwritten by parent status.
  - **Refs:** [Subworkflow cancellation](specs/workflow-runtime.md), [recruit/onboard reference](reference-workflows/reference-suite.md).

- [x] `WF-RUN-012` **[GATE_A][SOL_HIGH] Implement side-effect-free `SIMULATE` mode.**
  - **Depends:** `WF-COMP-003`, `PROMO-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_RUN_012`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_RUN_012`; `RACE=TestTodo_WF_RUN_012_Race`; `FAULT=TestTodo_WF_RUN_012_Fault`; `MUTATION=TestTodo_WF_RUN_012_Mutation`.
  - **RED:** simulation creates task/message/charge/write/outbox/external call or omits planned effect.
  - **GREEN:** reads/pure rules/plans/obligations/cost execute and mutation attempt returns `SIMULATION_SIDE_EFFECT_FORBIDDEN`; persistent evidence is limited to authorized simulation artifacts.
  - **REFACTOR:** same compiled plan specializes by execution mode.
  - **Refs:** [Workflow modes](specs/workflow-runtime.md), [exploration contract](workflows/_shared/exploration-contract.md).
  - **Evidence (2026-09-03):** `TestTodo_WF_RUN_012` (capability, decision and end nodes execute with every `evidence.EffectCounters` field zero, the zero-effect receipt validates, the escalated approval is stated as `WOULD_AWAIT` rather than dropped), `TestTodo_WF_RUN_012_Race` (eight concurrent runs produce the serial digest byte for byte; concurrent `Admit` never mutates the shared plan), `TestTodo_WF_RUN_012_Fault` (a write-class plan is refused with `SIMULATION_SIDE_EFFECT_FORBIDDEN` at `Admit` and `Run`, the mutating handler is never reached, no receipt is minted), `TestTodo_WF_RUN_012_Mutation` (flipping `ZeroEffect`, stripping SIMULATE from a node, tampering the plan or editing a minted receipt each fail) in `internal/workflow/simulate`; `CodeWriteEffectInSimulate` is now an alias of the contract code; `go test -count=1 ./internal/workflow/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `WF-RUN-013` **[GATE_B][SOL_HIGH] Implement deterministic `REPLAY` mode.**
  - **Evidence (2026-09-05):** `TestTodo_WF_RUN_013` in `internal/workflow/replay` (a Replayer re-executes a compiled definition from an instance's durable record taking every node input from recorded outputs, signals and timers, clock and randomness answered from the record or refused as REPLAY_ARTIFACT_UNAVAILABLE, adapters replaced by a Recorder that refuses any external effect naming the node, admitted only under the REPLAY ModeContract with all sixteen other INTENT-023 rows refused and CausalSeparation enforced, the Trace reproducing the recorded digest with a typed Divergence naming the first differing node, a paused instance stopping at PAUSED_AT_FRONTIER, causal ordering of continuations over a fixed-clock run, and a pgtest-backed replay of a real Promotion run from the durable rows alone; StoreSource returns no signals or timers until the timer lane lands; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/workflow/replay/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WF-RUN-001`, `MODEL-020`, `WF-RUN-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_RUN_013`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_RUN_013`; `RACE=TestTodo_WF_RUN_013_Race`; `FAULT=TestTodo_WF_RUN_013_Fault`; `RECOVERY=TestTodo_WF_RUN_013_Recovery`; `MUTATION=TestTodo_WF_RUN_013_Mutation`.
  - **RED:** replay reads current mutable data, regenerates agent output or produces effect/task/message.
  - **GREEN:** pure nodes use pinned historical inputs/versions; missing artifact returns `REPLAY_ARTIFACT_UNAVAILABLE`; comparison identifies divergence.
  - **REFACTOR:** historical evidence remains immutable.
  - **Refs:** [Workflow replay](specs/workflow-runtime.md), [reproducibility](plan.md#93-reproducibility-contract).

- [x] `WF-RUN-014` **[PHASE_2][SOL_HIGH] Implement isolated `SHADOW` mode.**
  - **Evidence (2026-09-05):** `TestTodo_WF_RUN_014` in `internal/workflow/shadow` (internal/workflow/shadow runs a definition in isolated SHADOW mode with every effect sink fenced, producing a comparable outcome digest without any external write; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/workflow/shadow/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WF-RUN-012`, `SANDBOX-001`, `TENANT-003`, `TRUST-025`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_RUN_014`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_RUN_014`; `RACE=TestTodo_WF_RUN_014_Race`; `FAULT=TestTodo_WF_RUN_014_Fault`; `MUTATION=TestTodo_WF_RUN_014_Mutation`.
  - **RED:** candidate logic writes production, delivers human message, bills usage or calls real effect destination.
  - **GREEN:** shadow consumes approved real events/reads but routes all effects to suppressed/synthetic evidence and supports historical/candidate comparison.
  - **REFACTOR:** synthetic external destination requires separate approval.
  - **Refs:** [Workflow modes](specs/workflow-runtime.md), [Phase 2 shadow evidence](plan.md#phase-2-hcm-workflow-operating-layer).

- [x] `WF-RUN-015` **[GATE_B][SOL_HIGH] Implement typed workflow interventions.**
  - **Evidence (2026-09-05):** `TestTodo_WF_RUN_015` in `internal/workflow/intervention` (closed typed interventions (PAUSE, RESUME, SKIP_STEP, RETRY_STEP, REASSIGN, CANCEL, FORCE_COMPLETE_WITH_EVIDENCE) with required fields, eligibility through the WF-COMP-004 readers so an ineligible region refuses, separation of duties where the kind demands it, and a digested intervention receipt; no free-form intervention exists; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/workflow/intervention/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WF-RUN-001`, `TRUST-021`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_RUN_015`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_RUN_015`; `RACE=TestTodo_WF_RUN_015_Race`; `FAULT=TestTodo_WF_RUN_015_Fault`; `MUTATION=TestTodo_WF_RUN_015_Mutation`.
  - **RED:** retry/resume/skip/satisfy/override/rewind/compensate/supersede mutates raw runtime state, lacks authority/reason/evidence or accepts a no-op.
  - **GREEN:** accepted intervention creates immutable plan/decision and observable transition; unsupported action returns typed denial.
  - **REFACTOR:** ordinary operators never update workflow tables directly.
  - **Refs:** [Intervention model](plan.md#16-strategic-decisions), [workflow runtime](specs/workflow-runtime.md).

- [x] `WF-RUN-016` **[GATE_B][SOL_HIGH] Implement RepairPlan execution mode.**
  - **Evidence (2026-09-05):** `TestTodo_WF_RUN_016` in `internal/workflow/execute`, `test/workflow` (a RepairPlan executes under its own execution mode with the original semantic idempotency identity, redriving only the failed effect of a promotion that ended at end_repair_plan and flipping the intent's consistency through RECON-002, proven on the Promotion fixture; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/workflow/execute/ ./test/workflow/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `REPAIR-002`, `TX-008`, `TX-009`, `TX-010`, `WF-RUN-015`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_RUN_016`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_RUN_016`; `RACE=TestTodo_WF_RUN_016_Race`; `FAULT=TestTodo_WF_RUN_016_Fault`; `MUTATION=TestTodo_WF_RUN_016_Mutation`.
  - **RED:** repair author self-executes where SoD applies, stale finding/plan changes state, or repair failure clears `REPAIR_REQUIRED`.
  - **GREEN:** diagnose -> simulate -> approve -> execute -> observe -> verify appends evidence and closes only after passed reconciliation.
  - **REFACTOR:** repaired business facts change through domain correction capabilities.
  - **Refs:** [Repair specification](specs/transaction-ledger-reconciliation-and-repair.md), [workflow repair mode](specs/workflow-runtime.md).

- [x] `WF-RUN-017` **[PHASE_2][SOL_HIGH] Preview live workflow migration compatibility.**
  - **Evidence (2026-09-05):** `TestTodo_WF_RUN_017` in `internal/workflow/migrationpreview` (compatibility preview between two compiled workflow definitions and in-memory live instance states: instances that continue, are stranded or need a declared bridge, typed refusal when a step with live instances is removed without a bridge, golden preview for the Promotion fixture pair; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/workflow/migrationpreview/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WF-COMP-006`, `WF-RUN-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_RUN_017`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_RUN_017`; `GOLDEN=TestTodo_WF_RUN_017_Golden`; `RACE=TestTodo_WF_RUN_017_Race`; `FAULT=TestTodo_WF_RUN_017_Fault`; `SECURITY=TestTodo_WF_RUN_017_Security`; `CONFORMANCE=TestTodo_WF_RUN_017_Conformance`; `MUTATION=TestTodo_WF_RUN_017_Mutation`.
  - **RED:** changed node/state/schema/effect/approval/legal contract is labeled safe without transform/repair/rollback analysis.
  - **GREEN:** preview returns `SAFE`, `TRANSFORMABLE`, `REQUIRES_REPAIR`, or `IMPOSSIBLE` with affected instances and exact blockers.
  - **REFACTOR:** past execution stays pinned and immutable.
  - **Refs:** [Workflow migration](specs/workflow-runtime.md), [live-instance rule](plan.md#16-strategic-decisions).

- [x] `WF-RUN-018` **[PHASE_2][SOL_HIGH] Execute approved workflow migration at a safe point.**
  - **Evidence (2026-09-05):** `TestTodo_WF_RUN_018` in `internal/workflow/migrate`, `internal/workflow/runtime` (Migrate executes an approved migration for one PAUSED instance only at a runtime.SafePointEligibility point whose durable checkpoint matches the live version and frontier digest, against a sealed PreviewRecord bound to both compiled-plan digests (stale preview refused), an Approval with a distinct approver, CONTINUE/SAFE or BRIDGE/TRANSFORMABLE classifications only (STRANDED and REQUIRES_REPAIR refused), same-node restage or cross-node bridge, the version pointer rewritten atomically by the additive runtime.Store.RecordVersionMigration under the optimistic fence, and a digested MIGRATION checkpoint as evidence; written by a Sonnet subagent and verified independently); `go test -count=1 ./internal/workflow/migrate/ ./internal/workflow/runtime/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WF-RUN-017`, `WF-STEP-015`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_RUN_018`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_RUN_018`; `GOLDEN=TestTodo_WF_RUN_018_Golden`; `RACE=TestTodo_WF_RUN_018_Race`; `FAULT=TestTodo_WF_RUN_018_Fault`; `MUTATION=TestTodo_WF_RUN_018_Mutation`.
  - **RED:** unsafe-point, unapproved digest, changed source instance or failed transform permits migration.
  - **GREEN:** state/data transform and version pointer commit atomically with migration evidence; failure preserves old instance or routes controlled repair.
  - **REFACTOR:** no ad hoc node/state edits.
  - **Refs:** [Workflow migration](specs/workflow-runtime.md), [configuration governance](plan.md#111-environment-and-change-governance).

- [x] `WF-RUN-019` **[GATE_A][SOL_LOW] Implement workflow execution inspection.**
  - **Depends:** `WF-RUN-001`, `MODEL-020`, `TRUST-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_RUN_019`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_RUN_019`; `GOLDEN=TestTodo_WF_RUN_019_Golden`; `RACE=TestTodo_WF_RUN_019_Race`; `INTEGRATION=TestTodo_WF_RUN_019_Integration`; `FAULT=TestTodo_WF_RUN_019_Fault`.
  - **RED:** inspector omits current node/attempt/retry/proposal/baseline/policy/effect/repair refs or leaks protected input/output.
  - **GREEN:** authorized view traverses definition -> instance -> node -> governance -> transaction -> connector -> observation/reconciliation -> trace.
  - **REFACTOR:** product screens consume operational projections, not reconstruct every view from runtime tables.
  - **Refs:** [Execution inspector](specs/workflow-runtime.md), [HRIS DataOps](specs/hris-admin-dataops.md).
  - **Evidence (2026-09-03):** `TestTodo_WF_RUN_019` and `TestTodo_WF_RUN_019_Golden` (`testdata/wfrun019_operator_view.json` pins the full definition, instance, node, governance, transaction, connector, observation and trace traversal with current node, attempt, retry-policy, proposal, baseline, policy, effect and repair refs), `TestTodo_WF_RUN_019_Race` (eight concurrent projections byte-identical, inputs unmutated), `TestTodo_WF_RUN_019_Integration` (round trip through PostgreSQL arrays and jsonb before projecting), `TestTodo_WF_RUN_019_Fault` (non-disclosable instance returns `ErrNotDisclosable` without leaking ids, denied protected fields render as `REDACTED` refs absent from the rendered bytes, a denied stage is named not dropped, a frontier node without an execution is reported in `Completeness.Gaps`) in `internal/workflow/inspect`; `go test -count=1 ./internal/workflow/...` PASS via embedded-postgres on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [ ] `WF-RUN-020` **[GATE_B][SOL_HIGH] Detect stuck workflows from expected progress.**
  - **Depends:** `WF-RUN-001`, `OPS-001`, `OPS-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_RUN_020`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_RUN_020`; `RACE=TestTodo_WF_RUN_020_Race`; `FAULT=TestTodo_WF_RUN_020_Fault`; `SECURITY=TestTodo_WF_RUN_020_Security`; `MUTATION=TestTodo_WF_RUN_020_Mutation`.
  - **RED:** long legal wait is flagged solely by age or poison node remains invisible despite missed lease/timer/signal/retry/SLA expectation.
  - **GREEN:** detector uses state-specific expected progress and opens/links one incident/work item with evidence.
  - **REFACTOR:** detector cannot alter business state.
  - **Refs:** [Operational readiness](plan.md#112-operational-readiness), [incident management](specs/incident-management.md).

- [ ] `WF-RUN-021` **[GATE_B][SOL_HIGH] Enforce workflow workload limits.**
  - **Depends:** `WF-COMP-002`, `ADMISSION-001`, `ADMISSION-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_RUN_021`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_RUN_021`; `RACE=TestTodo_WF_RUN_021_Race`; `FAULT=TestTodo_WF_RUN_021_Fault`; `MUTATION=TestTodo_WF_RUN_021_Mutation`.
  - **RED:** branch/child/payload/concurrent activity/cost exceeds declared limit yet is admitted or partially scheduled.
  - **GREEN:** request returns `OVERLOADED`/`ADMISSION_DEFERRED` before unsafe work; durable intent remains visible for retry/queue policy.
  - **REFACTOR:** limits resolve by tenant/capability/criticality/control snapshot.
  - **Refs:** [Runtime resource controls](specs/workflow-runtime.md), [quantitative contract](plan.md#113-quantitative-service-and-capacity-contract).

- [ ] `WF-RUN-022` **[GATE_B][SOL_HIGH] Prove workflow recovery after database/cell interruption.**
  - **Depends:** `WF-RUN-002`–`WF-RUN-007`, `RECOVERY-001`, `RECOVERY-002`, `RECOVERY-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_RUN_022`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_RUN_022`; `RACE=TestTodo_WF_RUN_022_Race`; `FAULT=TestTodo_WF_RUN_022_Fault`; `RECOVERY=TestTodo_WF_RUN_022_Recovery`; `MUTATION=TestTodo_WF_RUN_022_Mutation`; `INTEGRATION=TestTodo_WF_RUN_022_Integration`.
  - **RED:** restore loses timer/signal/outbox/frontier, stale worker resumes or external effect duplicates.
  - **GREEN:** restored state resumes with new fences/dedupe and preserves exact completion dimensions/evidence.
  - **REFACTOR:** recovery does not depend on original process memory.
  - **Refs:** [Recovery matrix](plan.md#114-recovery-matrix), [Gate B restore](execution-plan.md#gate-b-acceptance--limited-write-authority).

- [x] `WF-RUN-023` **[GATE_B][SOL_HIGH] Start a workflow from an immutable ProposalRevision.**
  - **Depends:** `INTENT-005`, `WF-COMP-006`, `WF-RUN-001`, `TX-006`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_RUN_023`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_RUN_023`; `GOLDEN=TestTodo_WF_RUN_023_Golden`; `RACE=TestTodo_WF_RUN_023_Race`; `FAULT=TestTodo_WF_RUN_023_Fault`; `SECURITY=TestTodo_WF_RUN_023_Security`; `MUTATION=TestTodo_WF_RUN_023_Mutation`.
  - **RED:** mutable/unapproved/superseded proposal, inactive/incompatible workflow version, mismatched tenant/intent/subject, changed start idempotency key or unresolved required context creates an instance.
  - **GREEN:** one atomic start binds proposal revision/digest, exact compiled workflow version/digest, execution mode, control snapshot, subjects and correlation; identical retry returns the original instance and initial READY frontier.
  - **REFACTOR:** workflow resolution is policy/configuration driven; Promotion service never embeds a runtime graph.
  - **Refs:** [Workflow runtime](specs/workflow-runtime.md), [immutable proposals](specs/business-intent-and-change-request.md).
  - **Evidence (2026-09-03):** `TestTodo_WF_RUN_023`, `TestTodo_WF_RUN_023_Golden`, `TestTodo_WF_RUN_023_Race`, `TestTodo_WF_RUN_023_Fault`, `TestTodo_WF_RUN_023_Security`, `TestTodo_WF_RUN_023_Mutation` in `internal/workflow/runtime` (`Start(ctx, tx, StartRequest)` binds the immutable approved ProposalRevision by id and digest, the ACTIVE compiled version by exact pin through the `WorkflowResolver` port, execution mode, control snapshot digest, subjects and correlation, and persists the `frontier.Seed` READY frontier; mutable, unapproved or superseded proposals, inactive or pin-mismatched versions, tenant, intent or subject mismatches and unresolved required context are refused; an identical retry returns the original instance, a changed key or digest under the same key is a typed conflict; caller-driven per the WF-RUN-000 prototype ruling, no scheduler or clock inside); `go test -count=1 ./internal/workflow/runtime/ ./internal/data/pgtest/` PASS via embedded-postgres on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `WF-RUN-024` **[GATE_B][SOL_HIGH] Derive workflow frontier advancement deterministically.**
  - **Depends:** `WF-RUN-023`, `WF-COMP-002`, `WF-STEP-017`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_RUN_024`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_RUN_024`; `PROPERTY=TestTodo_WF_RUN_024_Property`; `GOLDEN=TestTodo_WF_RUN_024_Golden`; `RACE=TestTodo_WF_RUN_024_Race`; `FAULT=TestTodo_WF_RUN_024_Fault`; `MUTATION=TestTodo_WF_RUN_024_Mutation`.
  - **RED:** same compiled graph/state/output yields different successors, a branch/default/unknown route is implicit, JOIN activates early, skipped path activates work, or terminal node leaves an unexplained frontier.
  - **GREEN:** pure transition function returns exact successor node identities/states, join counters, terminal dimensions and required scheduling intents from pinned graph/state/output; repeated evaluation has identical digest.
  - **REFACTOR:** step handlers return typed outcomes only and never enqueue their own successors.
  - **Refs:** [Workflow graph](specs/workflow-runtime.md), [step semantics](workflows/_engine/step-types.md).
  - **Evidence (2026-09-03):** `TestTodo_WF_RUN_024` (exact successors and intents, full promotion walk to `SIMULATION_APPROVAL_REQUIRED` with five terminal dimensions, excluded branch skipped without intent, declared default marked `via_default`, JOIN ALL waits then activates, QUORUM on first arrival, skipped branch blocks a JOIN, simulator parity against `simulate.Run`'s trace), `TestTodo_WF_RUN_024_Property` (64 random outcome sequences: digest-identical repeats, input snapshot unchanged, every successor traces to a declared route), `TestTodo_WF_RUN_024_Golden` (`testdata/promotion_advancement.json`, six transition and state digests pinned), `TestTodo_WF_RUN_024_Race`, `TestTodo_WF_RUN_024_Fault` (fifteen typed refusals incl. no-matching-route-no-default, no failure route, impossible quorum, terminal with a remaining frontier named), `TestTodo_WF_RUN_024_Mutation` in `internal/workflow/frontier` (`Advance` is a pure value-only transition returning successors, join counters, terminal dimensions and scheduling intents READY / WORK_ITEM_REQUIRED / SIGNAL_SUBSCRIPTION_REQUIRED / TIMER_REQUIRED / COMPLETE as data; JOIN strategy declared at `Seed`, never inferred; no fan-out until a real PARALLEL exists); `go test -count=1 ./internal/workflow/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `WF-RUN-025` **[GATE_B][SOL_HIGH] Commit node completion and continuation scheduling atomically.**
  - **Depends:** `WF-RUN-002`, `WF-RUN-024`, `DB-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WF_RUN_025`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_RUN_025`; `RACE=TestTodo_WF_RUN_025_Race`; `FAULT=TestTodo_WF_RUN_025_Fault`; `MUTATION=TestTodo_WF_RUN_025_Mutation`.
  - **RED:** failpoint after typed output/node success but before frontier update, successor creation, timer, signal subscription, WorkItem or ready-work enqueue loses or duplicates continuation; stale lease/fence commits nothing.
  - **GREEN:** one fenced runtime transaction persists attempt result/output, node transition, instance version/frontier and every derived continuation record exactly once; restart observes either the entire advancement or none and deterministic retry returns its receipt.
  - **REFACTOR:** runtime storage exposes one advancement unit-of-work; scheduler publication happens only after commit.
  - **Refs:** [Workflow recovery](specs/workflow-runtime.md), [runtime database contract](data/models/kernel-governance-and-evidence.md).
  - **Evidence (2026-09-03):** `TestTodo_WF_RUN_025`, `TestTodo_WF_RUN_025_Race`, `TestTodo_WF_RUN_025_Fault`, `TestTodo_WF_RUN_025_Mutation`, `TestAdvanceRejectsAnOldReceiptAfterLaterProgress` in `internal/workflow/runtime` (`Advance(ctx, tx, AdvanceRequest)` is one transaction fenced by the expected `instance_version`: records the node attempt and typed output digest, applies `frontier.Advance`, writes the successor node executions, version and frontier, and persists every derived continuation exactly once through the `ContinuationSink` port and the `workflow_continuation` audit table of migration `00018_workflow_continuation.sql` (registered in `definitions/storage/storage-disposition.yaml`); migration `00020_workflow_advancement_receipt.sql` stores the exact request and result receipt atomically, an identical retry replays only while the instance remains at that receipt's resulting version, and a retry after any later node advances is stale; a stale version commits nothing and a sink failure after the node result rolls the whole advancement back); `go test -count=1 ./internal/workflow/runtime/ ./internal/data/pgtest/` PASS via embedded-postgres on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
- [x] `WF-RUN-027` **[GATE_B][SOL_HIGH] Derive proposal approval and supersession at Start instead of trusting caller flags.**
  - **Evidence (2026-09-05):** `TestTodo_WF_RUN_027` in `internal/workflow/runtime` (Start resolves the revision's approval decisions and supersession through ProposalFacts and ApprovalFacts ports (no booleans on StartRequest), refuses CodeUnapprovedProposal / CodeSupersededProposal / CodeApprovalBindingMismatch from stored facts, and records the approval decisio; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/workflow/runtime/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WF-RUN-023`, `INTENT-005`, `INTENT-006`, `APPROVAL-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=a workflow instance must start only from a proposal revision whose approval and currency are facts the runtime reads, never booleans a caller asserts`.
  - **TEST:** `TestTodo_WF_RUN_027`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_RUN_027`; `GOLDEN=TestTodo_WF_RUN_027_Golden`; `FAULT=TestTodo_WF_RUN_027_Fault`; `SECURITY=TestTodo_WF_RUN_027_Security`; `MUTATION=TestTodo_WF_RUN_027_Mutation`.
  - **RED:** a caller passing `Approved: true` with an invented approval ref, or `Superseded: false` for a revision the proposal store has superseded, starts an instance; or an approval whose bound proposal digest differs from the revision being started is accepted.
  - **GREEN:** `Start` resolves the revision's approval decisions and supersession through `ProposalFacts` and `ApprovalFacts` ports (no booleans on `StartRequest`), refuses `CodeUnapprovedProposal` / `CodeSupersededProposal` / `CodeApprovalBindingMismatch` from stored facts, and records the approval decision ids it relied on in the start receipt.
  - **REFACTOR:** the ports are consumer-owned in `internal/workflow/runtime`; adapters over `internal/intent/approval` and the proposal store live in `internal/platform/execution`.
  - **Refs:** [workflow runtime](specs/workflow-runtime.md), [review findings](devlog/2026-09-03-executable-prototype.md#9-review-findings-and-the-hardening-plan).
  - **Evidence (partial, 2026-09-05):** `TestTodo_WF_RUN_027`, `TestTodo_WF_RUN_027_Golden`, `TestTodo_WF_RUN_027_Fault`, `TestTodo_WF_RUN_027_Security`, `TestTodo_WF_RUN_027_Mutation`, `TestTodo_WF_RUN_027_FactsOutrankCallerFlags`, `TestTodo_WF_RUN_027_LegacyFallbackSurface` in `internal/workflow/runtime`; `TestDurableProposalFactsReadsARecordedDecision`, `TestDurableProposalFactsReportsADecisionBoundToAnotherDigest`, `TestDurableProposalFactsReadsASupersedesEdge`, `TestExecutionDecisionRecordIsIdempotentAndRefusesADigestChange`, `TestExecutionErrorProjectsTheDerivedProposalRefusals` in `internal/intent/app`; `TestDecisionStoreForRevisionReturnsWholeRowsForThatRevisionOnly`, `TestRelationshipStoreSupersededByReadsTheEdgeChildToParent`, `TestRevisionStoreMaterializesOnceAndReportsTheSecondCall` in `internal/data/intentcontrol`; `TestTodo_WF_RUN_027_Bootstrap`, `TestTodo_WF_RUN_027_Bootstrap_Fault`, `TestTodo_WF_RUN_027_Bootstrap_Security` in `test/bootstrap` (durable `DurableProposalFacts` adapter in `internal/intent/app` implements both ports over `intentcontrol` reads inside Start's own transaction; `executionStart` no longer asserts `Approved`; `journey_decide` commits the AUTHZ decision bound to the material and control-snapshot digests before starting and records the approver's HUMAN_APPROVAL decision in `intent_decision`; `RevisionStore.Materialize` stores the P1A revision row the decision FK needs; refusals project to `p1b.unapproved_proposal` and new `p1b.superseded_proposal`; written by an Opus subagent and verified independently); `go test -count=1 ./internal/workflow/runtime/ ./internal/intent/app/ ./internal/data/intentcontrol/ ./test/bootstrap/ ./test/workflow/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02. Still partial: the caller-asserted `ProposalBinding.Approved`/`ApprovalRef`/`Superseded` fields and `legacyValidate` remain active for a caller supplying neither port because `test/workflow`, `internal/workflow/execute`, `internal/workflow/migrate` and `internal/workflow/replay` tests still construct bindings on them (pinned by `TestTodo_WF_RUN_027_LegacyFallbackSurface`); `DurableProposalFacts.Supersession` returns a nil `CurrentRevision` because `intent_relationship` carries no superseding proposal content, so `execute.CurrencyGuard` blocks coarsely rather than by INTENT-006 materiality.

- [x] `WF-RUN-028` **[GATE_B][SOL_HIGH] Reload the persisted WorkItem on Resume and refuse evidence that does not match storage.**
  - **Depends:** `WF-RUN-025`, `WORK-001`, `WORK-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORK; DIRECT=none; WHY=a resumed instance must advance only on work-item evidence that is actually committed, never on a struct the caller assembled`.
  - **TEST:** `TestTodo_WF_RUN_028`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_RUN_028`; `RACE=TestTodo_WF_RUN_028_Race`; `FAULT=TestTodo_WF_RUN_028_Fault`; `SECURITY=TestTodo_WF_RUN_028_Security`; `MUTATION=TestTodo_WF_RUN_028_Mutation`.
  - **RED:** a hand-built or stale `WorkItem` whose status, item version, completer or completed output digest differ from the stored row advances the instance; or a completion recorded for another instance, node or attempt is accepted.
  - **GREEN:** `Resume` loads the item by id inside the advancement transaction, compares status, item version, completer, completed output digest and instance/node/attempt binding, and refuses `CodeWorkItemDrift` on any difference; the outcome fed to the frontier is derived from the stored row, not the request.
  - **REFACTOR:** `ResumeRequest` carries a work-item id and an expected item version, not the item.
  - **Refs:** [human work](specs/human-work-forms-and-rules.md), [review findings](devlog/2026-09-03-executable-prototype.md#9-review-findings-and-the-hardening-plan).
  - **Evidence (2026-09-03):** `TestTodo_WF_RUN_028`, `TestTodo_WF_RUN_028_Race` (concurrent resumes on one work item advance exactly once), `TestTodo_WF_RUN_028_Fault`, `TestTodo_WF_RUN_028_Security` (cross-tenant resume refused), `TestTodo_WF_RUN_028_Mutation` in `internal/workflow/execute` (`ResumeRequest` carries work-item id and expected item version; the stored row is loaded through the `WorkItemReader` port inside the advancement transaction and status, item version, completer, completed output digest and instance, node and attempt binding are compared; any difference is `ErrWorkItemDrift` before any advance; `workitem.Store` satisfies the port structurally); `go test -count=1 ./internal/workflow/execute/... ./test/workflow/` PASS via embedded-postgres on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `WF-RUN-029` **[GATE_B][SOL_HIGH] Recheck proposal currency, approval validity and control snapshots at every park boundary.**
  - **Depends:** `WF-RUN-027`, `WF-RUN-028`, `GOVERN-003`, `APPROVAL-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=underlying data can change while an instance is parked, and the materiality rule must decide whether the run may continue, must be re-planned or must stop`.
  - **TEST:** `TestTodo_WF_RUN_029`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_RUN_029`; `PROPERTY=TestTodo_WF_RUN_029_Property`; `GOLDEN=TestTodo_WF_RUN_029_Golden`; `FAULT=TestTodo_WF_RUN_029_Fault`; `SECURITY=TestTodo_WF_RUN_029_Security`; `MUTATION=TestTodo_WF_RUN_029_Mutation`.
  - **RED:** an instance whose proposal was superseded, whose approval was invalidated by a material change, or whose pinned control snapshot no longer revalidates while parked resumes and reaches COMPLETE; or an immaterial change blocks a resume.
  - **GREEN:** every `Resume` and the terminal write revalidate the pinned proposal digest, approval decisions and control snapshots through the materiality rule before advancing; a material change moves the instance to `BLOCKED` with a typed reason and a repair or supersession route, an immaterial one is recorded and continues.
  - **REFACTOR:** currency checks are one `CurrencyGuard` invoked by the driver, never duplicated in step packages.
  - **Refs:** [workflow runtime](specs/workflow-runtime.md), [materiality](specs/canonical-envelope-and-digest.md), [review findings](devlog/2026-09-03-executable-prototype.md#9-review-findings-and-the-hardening-plan).
  - **Evidence (2026-09-03):** `TestTodo_WF_RUN_029`, `TestTodo_WF_RUN_029_Property`, `TestTodo_WF_RUN_029_Golden` (pure checks of `CurrencyGuard.Check`), `TestTodo_WF_RUN_029_Fault`, `TestTodo_WF_RUN_029_Security`, `TestTodo_WF_RUN_029_Mutation` in `internal/workflow/execute` (`CurrencyGuard` runs inside every Resume advancement transaction and before the terminal write: a materially superseded proposal or a mismatched approval binding moves the instance to BLOCKED through the existing `RecordInstanceState` path and returns `ErrCurrencyBlocked`; a pure control revalidation with no material change never blocks); `go test -count=1 ./internal/workflow/execute/... ./test/workflow/ ./test/bootstrap/` PASS via embedded-postgres on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `WF-RUN-030` **[GATE_B][SOL_HIGH] Materialize the terminal business fact with the END output and approver evidence.**
  - **Depends:** `WF-RUN-025`, `TX-006`, `LEDGER-005`, `WORK-010`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=a downstream consumer must be able to act on the promotion from the terminal ledger event alone`.
  - **TEST:** `TestTodo_WF_RUN_030`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_RUN_030`; `GOLDEN=TestTodo_WF_RUN_030_Golden`; `INTEGRATION=TestTodo_WF_RUN_030_Integration`; `SECURITY=TestTodo_WF_RUN_030_Security`; `MUTATION=TestTodo_WF_RUN_030_Mutation`.
  - **RED:** the terminal `ledger_event` payload carries only identifiers, drops the END node's output digest, or omits worker, target placement, effective date and the approval and task evidence refs; or the harness asserts a row count without reading the payload, the outbox message and the projection checkpoint.
  - **GREEN:** the `TerminalWriteRequest` carries the END output and its digest; the `PromotionOutcome` event names worker, placement, effective date, approval decision ids and task submission ids alongside the proposal and plan digests; the harness asserts payload, outbox and checkpoint content and re-enters the terminal idempotency guard on replay.
  - **REFACTOR:** the payload schema is registered once and versioned; the harness is the golden for the event shape.
  - **Refs:** [workflow runtime](specs/workflow-runtime.md), [ledger](specs/canonical-envelope-and-digest.md), [review findings](devlog/2026-09-03-executable-prototype.md#9-review-findings-and-the-hardening-plan).
  - **Evidence (2026-09-03):** `TestTodo_WF_RUN_030` (ledger payload fields, outbox row byte-equality, projection checkpoint sequence), `TestTodo_WF_RUN_030_Golden` (exact field set of `hcmnext.workflow.PromotionOutcome/v2`: end node and output digest, worker ref, target placement, effective date, approval decision ids, task submission ids, proposal and plan digests), `TestTodo_WF_RUN_030_Integration` (one join across ledger, checkpoint and outbox), `TestTodo_WF_RUN_030_Security` (cross-tenant isolation), `TestTodo_WF_RUN_030_Mutation` (a direct second `LedgerTerminalWriter.Write` bypassing the runtime receipt appends nothing, proving the ledger idempotency key is an independent second guard) in `test/workflow`; `TerminalWriteRequest` carries the END node id and output digest; `go test -count=1 ./test/workflow/` PASS via embedded-postgres on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `WF-RUN-031` **[GATE_B][SOL_HIGH] Bound advancement replay by request digest and resulting instance version.**
  - **Evidence (2026-09-05):** `TestAdvanceRejectsAnOldReceiptAfterLaterProgress`, `TestTodo_WF_RUN_031_{Race,Fault,Mutation}` in `internal/workflow/runtime` (an advancement receipt from an earlier instance version is refused once later progress has been recorded, under concurrency and injected faults); `go test -count=1 ./internal/workflow/runtime/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WF-RUN-025`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=a replayed advancement must be recognised only while the instance still sits at the version that advancement produced, never after later progress`.
  - **TEST:** `TestAdvanceRejectsAnOldReceiptAfterLaterProgress`.
  - **TEST MATRIX:** `PRIMARY=TestAdvanceRejectsAnOldReceiptAfterLaterProgress`; `RACE=TestTodo_WF_RUN_031_Race`; `FAULT=TestTodo_WF_RUN_031_Fault`; `MUTATION=TestTodo_WF_RUN_031_Mutation`.
  - **RED:** a resubmitted advancement whose recorded node outcome still matches is replayed as success after a later node has moved the instance; or two different requests at the same expected version share a receipt.
  - **GREEN:** `Advance` persists a `workflow_advancement_receipt` keyed by node, attempt and expected version with the full request digest, replays only when the digest matches and the instance is at the receipt's resulting version, and refuses `CONFLICT_STALE_INSTANCE` otherwise.
  - **REFACTOR:** the recorded-attempt comparison is removed; the receipt table is the single replay authority.
  - **Refs:** [workflow runtime](specs/workflow-runtime.md), [review findings](devlog/2026-09-03-executable-prototype.md#9-review-findings-and-the-hardening-plan).
  - **Evidence (partial, 2026-09-03):** `TestAdvanceRejectsAnOldReceiptAfterLaterProgress` in `internal/workflow/runtime` (advancement receipt keyed by node, attempt and expected version with the full request digest; replay only while the instance sits at the receipt's resulting version, `CONFLICT_STALE_INSTANCE` otherwise; the recorded-attempt comparison removed; authored by the concurrent session and adopted here); `go test -count=1 ./internal/workflow/runtime/` PASS via embedded-postgres on windows/arm64 (Go 1.26.3); not complete: `TestTodo_WF_RUN_031_Race`, `TestTodo_WF_RUN_031_Fault` and `TestTodo_WF_RUN_031_Mutation` not yet written; branch plan-revision-2026-09-02.

- [x] `WF-RUN-032` **[GATE_B][SOL_HIGH] Name parked continuations truthfully on the wire.**
  - **Depends:** `WF-RUN-025`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=an operator reading an execution receipt must be able to find the continuation and work-item rows it names`.
  - **TEST:** `TestTodo_WF_RUN_032`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WF_RUN_032`; `GOLDEN=TestTodo_WF_RUN_032_Golden`; `CONFORMANCE=TestTodo_WF_RUN_032_Conformance`.
  - **RED:** `ExecutionReceipt.parked_continuations` carries work-item ids under a continuation name, or a receipt omits the continuation ids and kinds the instance is waiting on.
  - **GREEN:** the receipt carries `parked_continuations` (continuation id, kind, target node) and `work_items` (work-item id, kind, node) as separate typed lists over both transports.
  - **REFACTOR:** the wire receipt is rendered from `execute.Result` by one adapter.
  - **Refs:** [workflow runtime](specs/workflow-runtime.md), [review findings](devlog/2026-09-03-executable-prototype.md#9-review-findings-and-the-hardening-plan).
  - **Evidence (2026-09-03):** `TestTodo_WF_RUN_032` (typed `parked_continuation_refs` and `work_items` lists cross-checked against the stored WorkItem), `TestTodo_WF_RUN_032_Golden` (protojson field names; neither list carries the other's fields), `TestTodo_WF_RUN_032_Conformance` (protoreflect descriptor check of field numbers, kinds and message types) in `test/bootstrap`; `ExecutionReceipt.parked_continuations` kept as a deprecated field for compatibility; `go test -count=1 ./test/bootstrap/` PASS via embedded-postgres on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `WF-RUN-033` **[GATE_B][SOL_HIGH] Address re-entrant WAIT-node activations by attempt on a plan that declares a cycle.**
  - **Depends:** `WF-RUN-004`, `WF-RUN-024`, `WF-RUN-025`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTimerIDForAttempt_QualifiesLaterActivationsOnly`.
  - **TEST MATRIX:** `PRIMARY=TestTimerIDForAttempt_QualifiesLaterActivationsOnly`; `PROPERTY=TestReadyWorkID_IsDerivedPerNodeAttempt`; `FAULT=TestNextAttemptForNumbersARevisitAfterThePriorAttempt`; `MUTATION=TestHighestAttemptAddressesTheOpenActivation`; `INTEGRATION=TestWaitAttemptNamesTheContinuationActivation`.
  - **RED:** a workflow node revisited by a plan that declares a cycle wakes, claims, or resolves its prior activation's durable timer or open work item instead of its own, because the timer key, ready-work id or open-attempt lookup addresses the node alone rather than the node's specific activation.
  - **GREEN:** `workflow/timer.TimerKey` scopes a wait requirement's durable key to `<requirement digest>|attempt:<N>` (`workflow_timer` is unique per instance, node and key) and `TimerIDForAttempt`/`KeyRequirementDigest` derive the timer identity and its requirement digest from that key so only a later activation's timer fires for it; `workflow/runtime.nextAttemptFor` activates a revisited successor at attempt `N+1` and carries it on `ContinuationRecord.TargetAttempt`; `workflow/execute.highestAttempt` addresses the open activation in approval completion and in `advanceOnce`'s open-attempt resolution, both gated on the plan declaring a cycle so an acyclic plan pays no extra cost; `platform/execution.waitAttempt` normalizes the continuation's target activation for the timer it schedules, and every `timer.Scheduler` that fires now sets `Attempts: runtime.Store{}` so a fired timer resolves against the correct attempt's state.
  - **REFACTOR:** keep attempt-qualification behind the timer key and continuation record so ordinary acyclic plans never construct or compare an attempt number.
  - **Refs:** [Workflow runtime](specs/workflow-runtime.md), [durable workflow timers](#8-durable-workflow-runtime-recovery-and-intervention).
  - **Evidence (2026-09-06):** `TestTimerIDForAttempt_QualifiesLaterActivationsOnly`, `TestReadyWorkID_IsDerivedPerNodeAttempt` in `internal/workflow/timer`; `TestNextAttemptForNumbersARevisitAfterThePriorAttempt` in `internal/workflow/runtime`; `TestHighestAttemptAddressesTheOpenActivation` in `internal/workflow/execute`; `TestWaitAttemptNamesTheContinuationActivation` in `internal/platform/execution`; `go test -count=1 ./internal/workflow/timer/ ./internal/workflow/runtime/ ./internal/workflow/execute/ ./internal/platform/execution/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

---

## 9. Human work, approvals, forms and deterministic business rules

> **Disposition (2026-09-02):** P1B needs one approval WorkItem (`WORK-001`, `WORK-003`, `WORK-006`), a typed reason field, and one threshold decision table from tenant configuration (`RULE-002`, `RULE-003`). `WORK-002`/`004` are direct assignment only; `WORK-005` SLA/escalation is DESIGN. `FORM-001`–`FORM-003` are RETIRED (no form engine); `RULE-001` expression compilation is OUT until a second workflow family.

- [x] `WORK-001` **[GATE_B][SOL_HIGH] Implement immutable WorkItem lifecycle.**
  - **Depends:** `WF-RUN-001`, `MODEL-014`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORK; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WORK_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WORK_001`; `RACE=TestTodo_WORK_001_Race`; `SECURITY=TestTodo_WORK_001_Security`; `MUTATION=TestTodo_WORK_001_Mutation`.
  - **RED:** illegal transition, mutable completed output, missing correlation/subject/owner/visibility/deadline or generic status is rejected.
  - **GREEN:** create -> route -> assigned/available -> claimed -> in-progress -> completed/returned/escalated/expired/cancelled appends transition evidence.
  - **REFACTOR:** ApprovalTask specializes WorkItem without a parallel task store.
  - **Refs:** [Human work spec](specs/human-work-forms-and-rules.md), [workflow entities](data/models/kernel-governance-and-evidence.md).
  - **Evidence (2026-09-03):** `TestTodo_WORK_001` (create, route, assigned/available, claimed, in-progress and every terminal edge append a `work_item_transition` row in the caller's transaction; illegal transitions refused before any statement; missing correlation, subject, owner, visibility or deadline and a generic status refused by `Validate`), `TestTodo_WORK_001_Race`, `TestTodo_WORK_001_Security`, `TestTodo_WORK_001_Mutation` (a completed output digest is immutable even against a raw UPDATE, enforced by a trigger) in `internal/humanwork/workitem`; migration `00017_work_item.sql` adds `work_item` and append-only `work_item_transition` (registered in `definitions/storage/storage-disposition.yaml`); `ApprovalTask` is a kind on the same table; `go test -count=1 ./internal/humanwork/...` PASS via embedded-postgres on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `WORK-002` **[GATE_B][SOL_HIGH] Resolve Promotion work assignment.**
  - **Depends:** `WORK-001`, `APPROVAL-001`, `ORG-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORK; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WORK_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WORK_002`; `FUZZ=FuzzTodo_WORK_002`; `RACE=TestTodo_WORK_002_Race`; `MUTATION=TestTodo_WORK_002_Mutation`; `SECURITY=TestTodo_WORK_002_Security`.
  - **RED:** inactive/unauthorized/out-of-scope candidate, stale relationship or unresolvable fallback strands/assigns the item.
  - **GREEN:** assignment records resolution expression, candidate set, exclusions, chosen owner, relationship/policy versions and re-resolution trigger.
  - **REFACTOR:** assignment does not itself grant decision authority.
  - **Refs:** [Human work](specs/human-work-forms-and-rules.md), [approval resolver](specs/workflow-runtime.md).
  - **Evidence (2026-09-03):** `TestTodo_WORK_002`, `FuzzTodo_WORK_002`, `TestTodo_WORK_002_Race`, `TestTodo_WORK_002_Mutation` (two different resolutions never share an assignment digest), `TestTodo_WORK_002_Security` in `internal/humanwork/workitem` (`ResolveAssignment` wraps the APPROVAL-001 resolver: resolution expression, candidate set, exclusions, chosen owner, relationship and policy versions and the re-resolution trigger are recorded; an inactive or unauthorized candidate escalates the item back to its policy route rather than stranding it; assignment never grants decision authority); `go test -count=1 ./internal/humanwork/...` PASS via embedded-postgres on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `WORK-003` **[GATE_B][SOL_HIGH] Make WorkItem claim exclusive.**
  - **Depends:** `WORK-002`, `WF-RUN-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORK; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WORK_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WORK_003`; `RACE=TestTodo_WORK_003_Race`.
  - **RED:** concurrent claim test allows two owners or expired/stale item version completes.
  - **GREEN:** exactly one claimant receives fenced lease; losers receive `ALREADY_CLAIMED`; expiry returns item safely to policy route.
  - **REFACTOR:** no process-local mutex is correctness-bearing.
  - **Refs:** [Human work concurrency](specs/human-work-forms-and-rules.md), [workflow leases](specs/workflow-runtime.md).
  - **Evidence (2026-09-03):** `TestTodo_WORK_003`, `TestTodo_WORK_003_Race` (eight concurrent claimants on real connections: exactly one winner, losers receive `ALREADY_CLAIMED`, exactly one CLAIMED transition row) in `internal/humanwork/workitem` (`Claim` is an item_version compare-and-swap with a caller-supplied expiry; an expired claim is released on the next touch inside the caller's transaction and returned to its policy route; no lease table, fence token, sweeper or timer per the WF-RUN-000 gate; no process-local mutex is correctness-bearing); `go test -count=1 ./internal/humanwork/...` PASS via embedded-postgres on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `WORK-004` **[GATE_B][SOL_HIGH] Reassign unavailable or unauthorized work.**
  - **Evidence (2026-09-05):** `TestTodo_WORK_004`, `TestTodo_WORK_004_{Race,Mutation,Security}` in `internal/humanwork/workitem` (Store.Reassign escalates the live item, dropping any claim, re-resolves the same requirement against the fresh directory under TriggerReassignment and routes again, both transitions carrying the caller's typed reason; COMPLETED/EXPIRED/CANCELLED or never-routed items refused with CodeIllegalTransition so history is never rewritten); `go test -count=1 ./internal/humanwork/workitem/ ./internal/workflow/steps/approval/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WORK-002`, `WORK-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORK; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WORK_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WORK_004`; `RACE=TestTodo_WORK_004_Race`; `MUTATION=TestTodo_WORK_004_Mutation`; `SECURITY=TestTodo_WORK_004_Security`.
  - **RED:** departure/suspension/leave/authority loss strands task or silently transfers it without reason/history.
  - **GREEN:** policy re-resolves/reassigns/escalates with immutable reason and invalidates stale claim/decision authority.
  - **REFACTOR:** relationship change may notify but cannot rewrite prior assignment history.
  - **Refs:** [Human work routing](specs/human-work-forms-and-rules.md), [workflow context audit](workflows/_engine/workflow-context-adversarial-audit-2026-08-14.md).

- [x] `WORK-005` **[DESIGN][SOL_LOW] Implement SLA clocks and escalation signals.**
  - **Evidence (2026-09-05):** `TestTodo_WORK_005` in `internal/humanwork/sla` (versioned SLA clock emits reminder/escalation/expiry once semantically and records pause/resume reasons; written by an earlier lane (Luna or Sonnet) and verified independently); `go test -count=1 ./internal/humanwork/sla/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WORK-001`, `MODEL-005`, `WF-RUN-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORK; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WORK_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WORK_005`; `RACE=TestTodo_WORK_005_Race`; `FAULT=TestTodo_WORK_005_Fault`.
  - **RED:** deadline uses ambient calendar/timezone, quiet wait disappears, or escalation duplicates on retry.
  - **GREEN:** versioned SLA clock emits reminder/escalation/expiry once semantically and records pause/resume reasons.
  - **REFACTOR:** Workflow owns business escalation; Messaging only delivers notices.
  - **Refs:** [Human work SLA](specs/human-work-forms-and-rules.md), [messaging boundary](specs/messaging-and-notification-plane.md).

- [x] `WORK-006` **[GATE_B][SOL_HIGH] Complete WorkItem with current authority recheck.**
  - **Evidence (2026-09-05):** `TestTodo_WORK_006` in `internal/humanwork/workitem`, `internal/workflow/execute` (completing a WorkItem rechecks the actor's current authority at completion time and refuses with a typed AUTHORITY_LOST when it changed since assignment; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/humanwork/workitem/ ./internal/workflow/execute/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WORK-003`, `TRUST-005`, `APPROVAL-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORK; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WORK_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WORK_006`; `RACE=TestTodo_WORK_006_Race`; `SECURITY=TestTodo_WORK_006_Security`; `MUTATION=TestTodo_WORK_006_Mutation`.
  - **RED:** stale item/proposal/form version, revoked session, authority change, missing evidence or SoD violation completes no task.
  - **GREEN:** accepted output and transition commit once with current AuthZ/authority/proposal evidence and workflow signal.
  - **REFACTOR:** duplicate identical completion replays stored result.
  - **Refs:** [Gate B approvals](execution-plan.md#gate-b-acceptance--limited-write-authority), [human work](specs/human-work-forms-and-rules.md).

- [x] `WORK-011` **[GATE_A][TERRA] Ingest and content-address employee profile photos for demo and future upload use.**
  - **Depends:** none.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.WORK; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestUploadPreservesOriginalAndCreatesSquareProxy`.
  - **TEST MATRIX:** `PRIMARY=TestUploadPreservesOriginalAndCreatesSquareProxy`; `GOLDEN=TestUploadPreservesOriginalAndCreatesSquareProxy`; `SECURITY=TestUploadRejectsMismatchedMediaTypeAndUnsafeNames`; `FAULT=TestReadAllBoundedRejectsOversize`; `MUTATION=TestFileStoreIsContentIdempotentAndRefusesOverwrite`.
  - **RED:** an uploaded JPEG/PNG with a mismatched media type or an unsafe filename is accepted, a read exceeds the bounded size limit without refusal, or writing the same photo content twice through `FileStore` overwrites the original.
  - **GREEN:** `profilephoto.Upload` validates media type and filename, computes a content-addressed SHA-256 digest and produces a square JPEG proxy; `FileStore` writes proxies under a public root and originals under a private root, refusing to overwrite existing content so re-ingesting the same photo is a no-op; `cmd/migrate/demo_people.go` is today's only caller, seeding the HarborCare demo population's photos.
  - **REFACTOR:** keep upload validation and file placement behind this package so a future authenticated upload endpoint reuses the same bounded, content-addressed contract instead of re-implementing it.
  - **Refs:** [Records boundaries](specs/records-management-and-disposition.md), [human work](specs/human-work-forms-and-rules.md).
  - **Evidence (2026-09-06):** `TestUploadPreservesOriginalAndCreatesSquareProxy`, `TestUploadRejectsMismatchedMediaTypeAndUnsafeNames`, `TestFileStoreIsContentIdempotentAndRefusesOverwrite`, `TestReadAllBoundedRejectsOversize` in `internal/humanwork/profilephoto`; `go test -count=1 ./internal/humanwork/profilephoto/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [ ] `FORM-001` **[RETIRED][TERRA] RETIRED 2026-09-02: Define the bounded Promotion proposal form.**
  - **Disposition:** RETIRED 2026-09-02. The form engine is replaced by a typed reason field on the approval WorkItem; see the Phase 1 packaging in the human-work spec. Dependencies on this ID resolve as satisfied; its tests are not written.
  - **Depends:** `MODEL-017`, `WEDGE-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DOCUMENTS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_FORM_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_FORM_001`; `GOLDEN=TestTodo_FORM_001_Golden`; `FUZZ=FuzzTodo_FORM_001`; `INTEGRATION=TestTodo_FORM_001_Integration`; `SECURITY=TestTodo_FORM_001_Security`; `BROWSER=TestTodo_FORM_001_Browser`.
  - **RED:** untyped question, missing locale/visibility/requiredness/classification/help/error/accessibility semantics or arbitrary executable condition fails publication.
  - **GREEN:** versioned definition covers exact pilot inputs and resolves field paths/schema/rules.
  - **REFACTOR:** no general form builder is required.
  - **Refs:** [Forms spec](specs/human-work-forms-and-rules.md), [Phase 1 forms depth](plan.md#phase-1-implementation-depth-matrix).

- [ ] `FORM-002` **[RETIRED][SOL_LOW] RETIRED 2026-09-02: Compile conditional form visibility and validation.**
  - **Disposition:** RETIRED 2026-09-02. No conditional form engine in Phase 1. Dependencies on this ID resolve as satisfied; its tests are not written.
  - **Depends:** `FORM-001`, `RULE-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DOCUMENTS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_FORM_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_FORM_002`; `FUZZ=FuzzTodo_FORM_002`; `INTEGRATION=TestTodo_FORM_002_Integration`; `BROWSER=TestTodo_FORM_002_Browser`; `SECURITY=TestTodo_FORM_002_Security`.
  - **RED:** hidden question accepts authoritative answer, unauthorized field reaches client, or client/server validators disagree.
  - **GREEN:** same compiled rule set produces identical visible/required/error paths server-side and in GWC.
  - **REFACTOR:** server remains authoritative.
  - **Refs:** [Forms engine](specs/human-work-forms-and-rules.md), [experience contract](specs/experience-ui-and-branding.md).

- [ ] `FORM-003` **[RETIRED][SOL_HIGH] RETIRED 2026-09-02: Persist immutable Promotion FormSubmission.**
  - **Disposition:** RETIRED 2026-09-02. The approval reason and decision are preserved on the ApprovalBinding against the exact proposal digest; no FormSubmission record. Dependencies on this ID resolve as satisfied; its tests are not written.
  - **Depends:** `FORM-002`, `MODEL-006`, `WORK-006`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DOCUMENTS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_FORM_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_FORM_003`; `GOLDEN=TestTodo_FORM_003_Golden`; `FUZZ=FuzzTodo_FORM_003`; `INTEGRATION=TestTodo_FORM_003_Integration`; `BROWSER=TestTodo_FORM_003_Browser`.
  - **RED:** stale definition/proposal, invalid answer, hidden-field injection, wrong principal or mutable accepted submission is rejected.
  - **GREEN:** submission binds definition/version, visible-question set, locale, answers, attachments/evidence refs, actor/time and digest.
  - **REFACTOR:** corrections create successor submission.
  - **Refs:** [Forms evidence](specs/human-work-forms-and-rules.md), [form models](data/models/connectivity-access-content.md).

- [x] `FORM-004` **[GATE_A][SOL_HIGH] Prove form accessibility and equivalent human route.**
  - **Depends:** `FORM-002`, `UX-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DOCUMENTS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_FORM_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_FORM_004`; `FUZZ=FuzzTodo_FORM_004`; `INTEGRATION=TestTodo_FORM_004_Integration`; `BROWSER=TestTodo_FORM_004_Browser`; `MUTATION=TestTodo_FORM_004_Mutation`.
  - **RED:** keyboard/focus/label/error/AT/zoom/reflow/locale fixture blocks task completion or no equivalent governed route exists.
  - **GREEN:** WCAG evidence and assisted/manual route preserve same validation, authority and audit semantics.
  - **REFACTOR:** accommodations do not weaken business controls.
  - **Refs:** [Accessibility foundation](specs/platform-foundation-gap-closure.md), [forms spec](specs/human-work-forms-and-rules.md).
  - **Evidence (2026-09-03):** `TestTodo_FORM_004`, `_Integration` (SSR-route and GWC-route form submissions produce the same intent digest as the direct capability call), `_Browser`, `_Mutation`, `FuzzTodo_FORM_004` in `tools/uxqual/forms` (required-field semantics and aria-describedby error association checks; the GWC renderer lacks native required/aria-invalid so SSR is the accessible route, recorded in `definitions/ux/forms/promotion-form-accessibility-decision.yaml`); `go test -count=1 ./internal/governance/privacy/... ./tools/uxqual/forms/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `RULE-001` **[OUT][SOL_LOW] Implement bounded deterministic expression compilation.**
  - **Evidence (2026-09-05):** `TestTodo_RULE_001` in `internal/engines/rules` (HCM Next-owned typed expression compiles through the qualified CEL backend to bounded backend-neutral IR with dependency/version digest, cost limits and golden outputs; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/engines/rules/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TOOL-004`, `MODEL-002`–`MODEL-004`, `LIB-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_RULE_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_RULE_001`; `PROPERTY=TestTodo_RULE_001_Property`; `GOLDEN=TestTodo_RULE_001_Golden`; `FUZZ=FuzzTodo_RULE_001`.
  - **RED:** arbitrary code/I/O/time/randomness, type mismatch, unbounded complexity or absent UNKNOWN semantics fails compilation.
  - **GREEN:** HCM Next-owned typed expression compiles through the qualified CEL backend to bounded backend-neutral IR with dependency/version digest, cost limits and golden outputs.
  - **REFACTOR:** no scripting runtime or public/stored CEL type becomes part of the rules contract.
  - **Refs:** [Business rules](specs/human-work-forms-and-rules.md), [RULE step](workflows/_engine/step-types.md#11-rule).

- [x] `RULE-002` **[GATE_A][SOL_LOW] Implement decision-table evaluation.**
  - **Depends:** `RULE-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_RULE_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_RULE_002`; `PROPERTY=TestTodo_RULE_002_Property`; `GOLDEN=TestTodo_RULE_002_Golden`; `FUZZ=FuzzTodo_RULE_002`; `RACE=TestTodo_RULE_002_Race`; `FAULT=TestTodo_RULE_002_Fault`.
  - **RED:** overlap/gap/ambiguous priority, stale input or unknown condition silently chooses a row.
  - **GREEN:** table returns exact result or `UNKNOWN/CONFLICT`, matched row IDs and explanation trace.
  - **REFACTOR:** publication rejects ambiguous tables unless conflict strategy is explicit.
  - **Refs:** [Rules specification](specs/human-work-forms-and-rules.md).
  - **Evidence (2026-09-03):** `TestTodo_RULE_002`, `_Property`, `_Golden`, `_Race`, `_Fault`, `FuzzTodo_RULE_002` in `internal/engines/rules` (typed decision tables, FIRST/UNIQUE/COLLECT hit policies, exact decimal comparisons, fail-closed on unknown inputs, canonical digest, per-row trace); `go test -count=1 ./internal/engines/rules/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `RULE-003` **[GATE_A][SOL_HIGH] Compile the Promotion approval-threshold rule.**
  - **Depends:** `RULE-002`, `COMP-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_RULE_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_RULE_003`; `PROPERTY=TestTodo_RULE_003_Property`; `GOLDEN=TestTodo_RULE_003_Golden`; `FUZZ=FuzzTodo_RULE_003`; `MUTATION=TestTodo_RULE_003_Mutation`.
  - **RED:** threshold uses float/current unpinned config or missing value becomes “no finance approval.”
  - **GREEN:** exact raise/budget inputs and versioned threshold return required approval set or `UNKNOWN_BLOCKED`.
  - **REFACTOR:** customer parameter changes create a new rule version/impact analysis.
  - **Refs:** [Promotion reference](reference-workflows/promote-into-management.md), [rules spec](specs/human-work-forms-and-rules.md).
  - **Evidence (2026-09-03):** `TestTodo_RULE_003`, `_Property`, `_Golden`, `_Mutation`, `FuzzTodo_RULE_003` in `internal/engines/rules` (`hcmnext.rules.promotion_approval_threshold` v2026.1 compiled from the reference promotion workflow and the RaiseApprovalTable example; UNKNOWN inputs map to `UNKNOWN_BLOCKED`, never to STANDARD); `go test -count=1 ./internal/engines/rules/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [ ] `RULE-004` **[GATE_B][SOL_HIGH] Re-evaluate material rules before execution.**
  - **Depends:** `RULE-003`, `GOVERN-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_RULE_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_RULE_004`; `PROPERTY=TestTodo_RULE_004_Property`; `GOLDEN=TestTodo_RULE_004_Golden`; `FUZZ=FuzzTodo_RULE_004`; `MUTATION=TestTodo_RULE_004_Mutation`.
  - **RED:** changed threshold/input leaves approval set valid and executes.
  - **GREEN:** same result confirms plan; changed material result invalidates/replans/requires approval with evidence.
  - **REFACTOR:** historical approval explanation retains original rule version.
  - **Refs:** [Reproducibility](plan.md#93-reproducibility-contract), [Gate B revalidation](execution-plan.md#gate-b-acceptance--limited-write-authority).

---

## 10. Phase 1 messaging and secure inbox slice

> **Disposition (2026-09-02):** P1A sends nothing. P1B implements `MSG-001`, `MSG-002`, `MSG-004`, `MSG-006`, `MSG-007`, `MSG-008`, `MSG-010` only if the partner's operating process needs an approval email, and only for one template, one locale, one email adapter. `MSG-003` (endpoints/preferences) and `MSG-009` (fallback work) are DESIGN; `MSG-005` is the MINIMAL CONTRACT inbox record. `MSG-011`–`MSG-013` stay Phase 2/3.

- [x] `MSG-001` **[GATE_B][SOL_HIGH] Create a semantic purpose-bound MessageIntent.**
  - **Depends:** `WORK-001`, `MODEL-023`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.EXPERIENCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_MSG_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MSG_001`; `RACE=TestTodo_MSG_001_Race`; `INTEGRATION=TestTodo_MSG_001_Integration`; `FAULT=TestTodo_MSG_001_Fault`; `SECURITY=TestTodo_MSG_001_Security`; `MUTATION=TestTodo_MSG_001_Mutation`.
  - **RED:** workflow calls provider/email address directly or intent lacks purpose/audience/template/classification/requirement/correlation/expiry.
  - **GREEN:** typed intent for approval, task, determination, notice or workflow update commits before asynchronous delivery and exposes no provider credential.
  - **REFACTOR:** Gate A does not require transactional messaging unless partner path scope-exchanges it.
  - **Refs:** [Messaging spec](specs/messaging-and-notification-plane.md), [communications slice](execution-plan.md#communications-plane-slice).
  - **Evidence (2026-09-03):** `TestTodo_MSG_001`, Declared `MSG-001` test matrix and acceptance behavior in `internal/messaging`; PASS; `go test -p 1 ./internal/messaging` and `go vet ./internal/messaging` on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `MSG-002` **[GATE_B][SOL_HIGH] Resolve current authorized audience.**
  - **Evidence (2026-09-05):** `TestTodo_MSG_002` in `internal/domains/audience` (current authorized audience resolution for a message intent from explicit refs, org-unit membership, management chains and role holders over in-memory facts ports, the caller's disclosure scope excluding unaddressable subjects with counted reasons, deduplicated, sorted and digested; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/audience/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MSG-001`, `ORG-002`, `TRUST-009`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.EXPERIENCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_MSG_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MSG_002`; `FUZZ=FuzzTodo_MSG_002`; `RACE=TestTodo_MSG_002_Race`; `INTEGRATION=TestTodo_MSG_002_Integration`; `FAULT=TestTodo_MSG_002_Fault`; `SECURITY=TestTodo_MSG_002_Security`.
  - **RED:** stale manager, unauthorized scoped role, duplicate endpoint or hidden recipient is selected.
  - **GREEN:** audience result records expression, candidates, exclusions, chosen principals, relationship/policy versions and resolution time.
  - **REFACTOR:** do not pin an email months before policy says to resolve it.
  - **Refs:** [Audience resolution](specs/messaging-and-notification-plane.md), [approval resolution](specs/workflow-runtime.md).

- [x] `MSG-003` **[DESIGN][SOL_HIGH] Resolve eligible delivery endpoints and preferences.**
  - **Evidence (2026-09-05):** `TestTodo_MSG_003` in `internal/domains/audience` (eligible delivery endpoints and preferences per audience member under a purpose's allowed channels honouring opt-outs and quiet hours, forbidden channels refused, per-member delivery plan with reasons for excluded endpoints, golden; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/audience/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MSG-002`, `TRUST-018`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.EXPERIENCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_MSG_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MSG_003`; `RACE=TestTodo_MSG_003_Race`; `INTEGRATION=TestTodo_MSG_003_Integration`; `FAULT=TestTodo_MSG_003_Fault`; `SECURITY=TestTodo_MSG_003_Security`.
  - **RED:** unverified/expired/purpose-prohibited/classification-ineligible endpoint or preference suppresses mandatory workflow message.
  - **GREEN:** delivery plan records endpoint, business/personal status, allowed purpose/classification, locale, mandatory override and fallback.
  - **REFACTOR:** endpoint identity is separate from Person/Principal identity.
  - **Refs:** [Messaging endpoints](specs/messaging-and-notification-plane.md), [communications models](data/models/connectivity-access-content.md).

- [x] `MSG-004` **[GATE_B][SOL_HIGH] Publish deterministic purpose/channel templates.**
  - **Evidence (2026-09-05):** `TestTodo_MSG_004` in `internal/engines/messagetemplate` (versioned purpose/channel templates over a closed placeholder vocabulary rendered deterministically to byte-identical output and digest, unknown placeholders, raw personal data outside declared placeholders and a channel the purpose does not allow refused, golden rendered output pinned; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/engines/messagetemplate/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-017`, `MSG-001`, `MODEL-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.EXPERIENCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_MSG_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MSG_004`; `RACE=TestTodo_MSG_004_Race`; `INTEGRATION=TestTodo_MSG_004_Integration`; `FAULT=TestTodo_MSG_004_Fault`; `SECURITY=TestTodo_MSG_004_Security`; `MUTATION=TestTodo_MSG_004_Mutation`.
  - **RED:** missing parameter/locale/classification/legal/purpose declaration, unsafe HTML, nondeterministic renderer or unauthorized field fails publication/render.
  - **GREEN:** same purpose/template/version/channel/locale/parameters produces identical rendered-content hash and disclosure-safe variants for Promotion, Leave and other governed workflows.
  - **REFACTOR:** sensitive details remain in secure inbox; email may be attention-only.
  - **Refs:** [Template contract](specs/messaging-and-notification-plane.md), [globalization](plan.md#511-globalization-is-a-business-dimension).

- [x] `MSG-005` **[GATE_B][SOL_LOW] Persist the minimal inbox record.**
  - **Evidence (2026-09-05):** `TestTodo_MSG_005`, `TestTodo_MSG_005_Security` in `internal/data/inbox` (migration `00033_inbox_record.sql`: `inbox_record` one row per (tenant, subject, recipient_message) with read/archived/pinned state under a compare-and-swap version and a foreign key to 00031's recipient_message, plus append-only `inbox_state_event` with new_version = previous_version + 1 enforced by CHECK and forbid_mutation; RLS on both; the store's every statement names subject_ref so one subject can never read or advance another's record, proven across subjects and tenants); `go test -count=1 ./migrations/ ./internal/data/inbox/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MSG-004`, `TRUST-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.EXPERIENCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_MSG_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MSG_005`; `SECURITY=TestTodo_MSG_005_Security`.
  - **RED:** a wrong principal/tenant reads the record, or revoked permission exposes the linked task.
  - **GREEN:** an inbox record with recipient, purpose, task reference, created/seen state is readable from the Promotion workspace under current AuthZ; there is no separate channel, thread, preference or provider machinery.
  - **REFACTOR:** the record is a MINIMAL CONTRACT; the full secure-inbox channel is DESIGN.
  - **Refs:** [Phase 1 messaging boundary](specs/messaging-and-notification-plane.md#phase-1-boundary).

- [x] `MSG-006` **[GATE_B][SOL_HIGH] Dispatch transactional email asynchronously.**
  - **Evidence (2026-09-05):** `TestTodo_MSG_006` in `internal/operations/messagingdelivery` (one logical recipient message has idempotent attempts, provider reference and policy-controlled attention content; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/operations/messagingdelivery/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MSG-001`–`MSG-005`, `INTG-015`, `TRUST-016`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.EXPERIENCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_MSG_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MSG_006`; `RACE=TestTodo_MSG_006_Race`; `INTEGRATION=TestTodo_MSG_006_Integration`; `FAULT=TestTodo_MSG_006_Fault`; `MUTATION=TestTodo_MSG_006_Mutation`; `SECURITY=TestTodo_MSG_006_Security`.
  - **RED:** provider call occurs before intent/outbox commit, duplicate retry sends a second logical message, or protected content bypasses DLP.
  - **GREEN:** one logical recipient message has idempotent attempts, provider reference and policy-controlled attention content.
  - **REFACTOR:** provider adapter is replaceable and health-aware.
  - **Refs:** [Async delivery](specs/messaging-and-notification-plane.md), [integration platform](specs/integration-platform.md).

- [ ] `MSG-007` **[GATE_B][SOL_HIGH] Model honest delivery and recipient states.**
  - **Depends:** `MSG-006`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.EXPERIENCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_MSG_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MSG_007`; `RACE=TestTodo_MSG_007_Race`; `INTEGRATION=TestTodo_MSG_007_Integration`; `FAULT=TestTodo_MSG_007_Fault`.
  - **RED:** `ACCEPTED_BY_PROVIDER` satisfies delivered/read/acknowledged requirement or bounced/expired attempt disappears.
  - **GREEN:** delivery attempt and recipient states remain distinct and transition through queued/submitted/accepted/delivered/bounced/failed and unseen/seen/read/acknowledged/responded.
  - **REFACTOR:** material transitions ledger; transport detail stays operational.
  - **Refs:** [Delivery semantics](specs/messaging-and-notification-plane.md), [communications models](data/models/connectivity-access-content.md).

- [ ] `MSG-008` **[GATE_B][SOL_HIGH] Emit messaging workflow signals.**
  - **Depends:** `MSG-007`, `WF-RUN-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.EXPERIENCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_MSG_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MSG_008`; `RACE=TestTodo_MSG_008_Race`; `INTEGRATION=TestTodo_MSG_008_Integration`; `FAULT=TestTodo_MSG_008_Fault`.
  - **RED:** duplicate/provider-only state resumes workflow twice or unsatisfied mandatory requirement advances.
  - **GREEN:** typed satisfied/failed/acknowledged/fallback-required signals correlate once to intent/task/workflow and preserve evidence.
  - **REFACTOR:** workflow owns waits/escalation.
  - **Refs:** [Messaging/workflow signals](specs/messaging-and-notification-plane.md).

- [ ] `MSG-009` **[DESIGN][SOL_HIGH] Reconcile failed delivery and create fallback work.**
  - **Depends:** `MSG-008`, `WORK-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.EXPERIENCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_MSG_009`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MSG_009`; `RACE=TestTodo_MSG_009_Race`; `INTEGRATION=TestTodo_MSG_009_Integration`; `FAULT=TestTodo_MSG_009_Fault`.
  - **RED:** provider outage/bounce/invalid endpoint silently satisfies communication obligation or retries forever.
  - **GREEN:** bounded retry/fallback policy creates secure-inbox alternative or HumanTask, records unsatisfied obligation and remains visible.
  - **REFACTOR:** fallback cannot broaden recipient or classification.
  - **Refs:** [Communication reconciliation](specs/messaging-and-notification-plane.md), [failure/repair](plan.md#57-failure-and-repair-are-product-experiences).

- [ ] `MSG-010` **[GATE_B][SOL_HIGH] Prove protected content never enters attention email or logs.**
  - **Depends:** `MSG-004`, `MSG-006`, `OPS-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.EXPERIENCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_MSG_010`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MSG_010`; `RACE=TestTodo_MSG_010_Race`; `INTEGRATION=TestTodo_MSG_010_Integration`; `FAULT=TestTodo_MSG_010_Fault`; `SECURITY=TestTodo_MSG_010_Security`.
  - **RED:** salary/medical/bank/case fixture appears in subject/body/provider metadata/log/trace/metric.
  - **GREEN:** redaction scan finds zero forbidden values; inbox retains authorized content and external message only approved notice.
  - **REFACTOR:** use classification-aware structured logging.
  - **Refs:** [Messaging privacy](specs/messaging-and-notification-plane.md), [DLP spec](specs/data-classification-and-dlp.md).

- [x] `MSG-011` **[PHASE_2][SOL_HIGH] Implement governed inbound replies and conversations.**
  - **Evidence (2026-09-05):** `TestTodo_MSG_011` in `internal/data/inboundmsg`, `migrations` (migration 00035_inbound_reply.sql: append-only inbound_message under forbid_mutation storing content as digest plus governed reference with provider-message-id dedupe per channel, and a CAS-fenced reply_binding resolved from the stored correlation token to exactly one same-tenant recipient_message whose recipient matches the sender, otherwise REJECTED with a reason, never silently BOUND; RLS on both; a participant lists only threads they belong to; six concurrent binds yield one winner; the downstream hostile-content quarantine and human task creation are out of this todo's roots; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/inboundmsg/ ./migrations/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MSG-005`, hostile-content/integration ingress.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.EXPERIENCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_MSG_011`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MSG_011`; `RACE=TestTodo_MSG_011_Race`; `INTEGRATION=TestTodo_MSG_011_Integration`; `FAULT=TestTodo_MSG_011_Fault`; `SECURITY=TestTodo_MSG_011_Security`.
  - **RED:** unverified sender, unmatched reply, hostile attachment/content or unauthorized participant resumes workflow.
  - **GREEN:** receipt resolves identity/thread/correlation, quarantines content, creates reviewed HumanTask response and explicit signal.
  - **REFACTOR:** remains out of Phase 1.
  - **Refs:** [Messaging replies](specs/messaging-and-notification-plane.md), [Phase 1 scope](execution-plan.md#communications-plane-slice).

- [ ] `MSG-012` **[PHASE_2][SOL_HIGH] Implement governed bulk communications.**
  - **Depends:** `MSG-001`–`MSG-009`, `ADMISSION-001`, `ADMISSION-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.EXPERIENCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_MSG_012`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MSG_012`; `RACE=TestTodo_MSG_012_Race`; `INTEGRATION=TestTodo_MSG_012_Integration`; `FAULT=TestTodo_MSG_012_Fault`.
  - **RED:** mutable audience, no cost/rate/exclusion preview, duplicate recipients or failure hidden by aggregate success.
  - **GREEN:** frozen population, per-recipient identity/result, resumable batches, pause/kill and reconciliation preserve every outcome.
  - **REFACTOR:** one-person and bulk capabilities remain distinct.
  - **Refs:** [Bulk messaging](specs/messaging-and-notification-plane.md), [batch intent model](specs/business-intent-catalog.md).

- [ ] `MSG-013` **[PHASE_3][SOL_HIGH] Implement legal-notice delivery requirements.**
  - **Depends:** `MSG-007`, `WF-STEP-013`, `PRIV-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.EXPERIENCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_MSG_013`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MSG_013`; `GOLDEN=TestTodo_MSG_013_Golden`; `RACE=TestTodo_MSG_013_Race`; `INTEGRATION=TestTodo_MSG_013_Integration`; `FAULT=TestTodo_MSG_013_Fault`; `SECURITY=TestTodo_MSG_013_Security`; `MUTATION=TestTodo_MSG_013_Mutation`.
  - **RED:** provider delivery is called legal evidence without verified recipient, required content/version/timestamp/ack/signature and jurisdiction rule.
  - **GREEN:** requirement reports `SATISFIED`, `UNSATISFIED`, `DISPUTED` or `REPAIR_REQUIRED` with immutable evidence package.
  - **REFACTOR:** notification, acknowledgement and legal signature remain distinct.
  - **Refs:** [Legal notice distinction](specs/messaging-and-notification-plane.md), [regulatory architecture](plan.md#517-resolve-jurisdictions-compose-domain-specific-regulatory-rules).

---

## 11. Authoritative data plane, ledger, projections and artifacts

> **Disposition (2026-09-02):** P1A for the M2 chronology: intent/proposal plus ledger, critical projection and outbox appended atomically with stale-head CAS (`DATA-001`–`DATA-003` and the `DB-*`/`LEDGER-*` they cite). P1B for multi-stream commit, corrections and outbox dispatch. Artifact storage is P1A only if the pilot stores an uploaded document.

`DATA-*` items are end-to-end data-plane acceptance contracts. They are not a
substitute for creating the physical database. The implementation work is
decomposed under [§26](#26-postgresql-database-and-model-materialization) and
[§27](#27-authoritative-ledger-implementation). Every `DATA-*` acceptance item
must cite the concrete `DB-*` and `LEDGER-*` work it exercises before it can be
closed.

- [x] `DATA-001` **[GATE_A][SOL_HIGH] Define PostgreSQL authoritative storage boundaries.**
  - **Evidence (2026-09-03):** `TestTodo_DATA_001` in `internal/data/schema`; `go test -count=1 ./internal/data/schema/...` PASS on windows/arm64 (Go 1.26.3); `go vet`, `gofmt -l`, `staticcheck` clean; branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-012`, `MODEL-020`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DATA_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DATA_001`; `GOLDEN=TestTodo_DATA_001_Golden`; `INTEGRATION=TestTodo_DATA_001_Integration`; `SECURITY=TestTodo_DATA_001_Security`; `MUTATION=TestTodo_DATA_001_Mutation`.
  - **RED:** schema allows tenantless authoritative row, cross-boundary FK/write, mutable event payload or external effect inside domain transaction.
  - **GREEN:** migrations declare tenant/cell/aggregate/stream ownership, constraints and transaction boundary for each correctness-bearing table.
  - **REFACTOR:** logical boundaries do not force premature microservices.
  - **Refs:** [Data plane](specs/platform-plane-model.md), [ledger specification](specs/transaction-ledger-reconciliation-and-repair.md).

- [x] `DATA-002` **[GATE_B][SOL_HIGH] Implement stream-head compare-and-swap.**
  - **Evidence (2026-09-05):** `TestTodo_DATA_002` in `internal/data/ledger` (ledger appends carry an expected stream head and are refused with a typed HEAD_MOVED conflict when the head advanced, proven under concurrent appenders; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/ledger/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Evidence (partial, 2026-09-03):** `TestTodo_DATA_002` in `internal/data/ledger` PASS; not complete: Single-stream CAS with two concurrent appenders passes with -count=5; -race class deferred to the go-core CI job; multi-stream lock ordering is LEDGER-003; `go test -count=1 ./internal/data/ledger/...`; on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DATA-001`, `LEDGER-003`, `TX-003`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DATA_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DATA_002`; `GOLDEN=TestTodo_DATA_002_Golden`; `RACE=TestTodo_DATA_002_Race`; `MUTATION=TestTodo_DATA_002_Mutation`.
  - **RED:** concurrent append with same expected sequence allows two winners, sequence gap or nondeterministic lock order.
  - **GREEN:** exactly one append advances head; loser returns expected/actual `CONFLICT_STALE_STREAM`; multi-stream locks use canonical order.
  - **REFACTOR:** stream identity remains semantic and tenant-scoped.
  - **Refs:** [Transaction coordinator](specs/transaction-plan-and-commit-coordinator.md), [stream models](data/models/kernel-governance-and-evidence.md).

- [x] `DATA-003` **[GATE_A][SOL_HIGH] Persist canonical ledger event envelopes.**
  - **Evidence (2026-09-03):** `TestTodo_DATA_003` in `internal/data/ledger`; `go test -count=1 ./internal/data/ledger/...` PASS on windows/arm64 (Go 1.26.3); `go vet`, `gofmt -l`, `staticcheck` clean; branch plan-revision-2026-09-02.
  - **Depends:** `DATA-001`, `MODEL-007`, `LEDGER-002`, `LEDGER-004`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DATA_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DATA_003`; `GOLDEN=TestTodo_DATA_003_Golden`; `FUZZ=FuzzTodo_DATA_003`; `SECURITY=TestTodo_DATA_003_Security`; `MUTATION=TestTodo_DATA_003_Mutation`.
  - **RED:** null/caller-controlled digest, missing authority class, tenant/stream/sequence/causation/correlation/time/schema or altered payload passes.
  - **GREEN:** append computes canonical digest and records `TRANSACTION_FACT`, `DOMAIN_FACT`, `EXTERNAL_OBSERVATION`, `CLAIM`, or `CORRECTION` explicitly.
  - **REFACTOR:** large sensitive bytes use governed artifact refs.
  - **Refs:** [Ledger truth classes](specs/transaction-ledger-reconciliation-and-repair.md), [truth distinction](plan.md#52-separate-different-kinds-of-truth).

- [x] `DATA-004` **[GATE_B][SOL_HIGH] Maintain per-stream hash chains.**
  - **Depends:** `DATA-002`, `DATA-003`, `MODEL-007`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DATA_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DATA_004`; `GOLDEN=TestTodo_DATA_004_Golden`.
  - **RED:** missing/reordered/altered/duplicated event or wrong prior hash verifies successfully.
  - **GREEN:** verifier locates exact first broken stream/sequence and valid chains reproduce historical head digest.
  - **REFACTOR:** wall clock is evidence, never sequence authority.
  - **Refs:** [Ledger integrity](specs/transaction-ledger-reconciliation-and-repair.md), [trusted time](plan.md#16-strategic-decisions).
  - **Evidence (2026-09-03):** `TestTodo_DATA_004` (10 subtests: gap, reorder, duplicate, altered digest, wrong prior hash, substituted event each located at the exact sequence), `TestTodo_DATA_004_Golden` in `internal/data/ledger/hashchain` (chain_hash = digest(prev_hash || event_digest) through `internal/kernel/digest`; side table `ledger_hash_chain_link` from `hashchain.SchemaDDL`, being folded into migration 00014); `go test -count=1 ./internal/data/ledger/...` PASS via embedded-postgres; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `DATA-005` **[GATE_A][SOL_HIGH] Implement bitemporal authorized event/fact queries.**
  - **Depends:** `DATA-003`, `TRUST-012`, `MODEL-004`, `LEDGER-006`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DATA_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DATA_005`; `PROPERTY=TestTodo_DATA_005_Property`; `SECURITY=TestTodo_DATA_005_Security`; `MUTATION=TestTodo_DATA_005_Mutation`.
  - **RED:** as-of query ignores known-at, correction/supersession, half-open boundary, tenant/field scope or reveals future-known fact.
  - **GREEN:** current/effective-as-of/known-as-of/between/history modes return exact authorized facts and provenance.
  - **REFACTOR:** query API labels chronology versus current authority.
  - **Refs:** [Temporal analytics](plan.md#16-strategic-decisions), [effective-date debugger](specs/hris-admin-dataops.md).
  - **Evidence (2026-09-03):** `TestTodo_DATA_005` (11 subtests), `TestTodo_DATA_005_Property`, `_Property_MonotonicKnowledge`, `_Security` (SQL-level proofs that denied fields and subjects never enter the result set), `_Mutation` in `internal/data/bitemporal` (`Query`/`AsOf`/`KnownAt` over ledger_event with five modes, correction vs supersession via self-join, keyset pagination, `BuildEvidence` query digest); `go test -count=1 ./internal/data/bitemporal/...` PASS via embedded-postgres on windows/arm64 (Go 1.26.3); optional index recorded in the package doc; branch plan-revision-2026-09-02.

- [x] `DATA-006` **[GATE_A][SOL_HIGH] Implement critical projection application.**
  - **Depends:** `DATA-003`, `DB-008`–`DB-011`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DATA_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DATA_006`; `GOLDEN=TestTodo_DATA_006_Golden`; `RACE=TestTodo_DATA_006_Race`.
  - **RED:** duplicate/out-of-order event changes result twice, stale version overwrites newer row or missing event silently advances watermark.
  - **GREEN:** idempotent CAS applies exact sequence, records source watermark/schema/version and exposes stale/disagreeing status.
  - **REFACTOR:** projections remain reconstructable and non-authoritative.
  - **Refs:** [Canonical history/read planes](plan.md#515-one-canonical-history-many-rebuildable-read-planes), [ledger projections](specs/transaction-ledger-reconciliation-and-repair.md).
  - **Evidence (2026-09-03):** `TestTodo_DATA_006`, `_Golden`, `_Race` in `internal/data/projection/critical` (`Apply` decodes generated IntentInstance/ProposalRevision protobufs into the migration-00004 row shapes under its own checkpoint, CAS-guarded upsert and append-only revision insert, out-of-order refused with `projection.ErrSequenceGap`, replay is a no-op; `Commit` composes with `outbox.Commit` in one transaction; `Verify` recomputes from the ledger and diffs); `go test -count=1 ./internal/data/projection/critical/... ./internal/data/provenance/...` PASS via embedded-postgres on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `DATA-007` **[GATE_B][SOL_HIGH] Commit ledger, critical projection and outbox atomically.**
  - **Evidence (2026-09-05):** `TestTodo_DATA_007` in `internal/data/ledger/commit` (multi-stream commits are recorded as one checkpointed unit so a crash between streams is detected on recovery and the partial append is never served as a commit; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/ledger/commit/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DATA-002`, `DATA-003`, `DATA-006`, `TX-004`, `LEDGER-008`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DATA_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DATA_007`; `RACE=TestTodo_DATA_007_Race`; `MUTATION=TestTodo_DATA_007_Mutation`.
  - **RED:** failpoint at each row leaves event without projection/outbox or projection/outbox without event.
  - **GREEN:** all correctness-bearing records commit once or all roll back; returned receipt includes stream heads and outbox IDs.
  - **REFACTOR:** noncritical projections consume events asynchronously.
  - **Refs:** [Phase 1 spine](plan.md#8-platform-plane-model-and-capabilities), [transaction coordinator](specs/transaction-plan-and-commit-coordinator.md).

- [x] `DATA-008` **[GATE_B][SOL_HIGH] Dispatch transactional outbox at least once safely.**
  - **Evidence (2026-09-05):** `TestTodo_DATA_008` in `internal/data/outbox` (outbox dispatch leases are fenced by a lease version (migration 00054) so a stale dispatcher that lost its lease cannot mark a message delivered; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/outbox/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DATA-007`, `WF-RUN-002`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DATA_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DATA_008`; `RACE=TestTodo_DATA_008_Race`; `FAULT=TestTodo_DATA_008_Fault`; `MUTATION=TestTodo_DATA_008_Mutation`.
  - **RED:** crash/restart loses row, publishes before commit, reorders same resource or creates a second logical external effect.
  - **GREEN:** lease/fence publishes with stable event/effect ID; duplicate consumer delivery is expected and idempotent.
  - **REFACTOR:** queue/broker choice does not change semantic IDs.
  - **Refs:** [Ledger/outbox](specs/transaction-ledger-reconciliation-and-repair.md), [integration operations](specs/integration-platform.md).

- [x] `DATA-009` **[GATE_B][SOL_HIGH] Persist event-consumer positions and dedupe.**
  - **Evidence (2026-09-05):** `TestTodo_DATA_009` in `internal/data/outbox` (consumer positions and semantic dedupe rows (migration 00055) give each consumer group an exactly-once position per stream and partition, with a poisoned position surfaced rather than skipped; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/outbox/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DATA-008`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DATA_009`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DATA_009`; `GOLDEN=TestTodo_DATA_009_Golden`; `RACE=TestTodo_DATA_009_Race`; `FAULT=TestTodo_DATA_009_Fault`; `MUTATION=TestTodo_DATA_009_Mutation`.
  - **RED:** duplicate/reordered delivery advances projection/effect twice or compaction drops material event.
  - **GREEN:** consumer records source partition/sequence/schema/watermark and resumes after crash without semantic duplication.
  - **REFACTOR:** each consumer owns its checkpoint and rebuild status.
  - **Refs:** [Event distribution](specs/transaction-ledger-reconciliation-and-repair.md), [Phase 2 consumer evidence](plan.md#phase-2-hcm-workflow-operating-layer).

- [x] `DATA-010` **[GATE_B][SOL_HIGH] Rebuild a projection from canonical sources.**
  - **Evidence (2026-09-05):** `TestTodo_DATA_010` in `internal/data/rebuild` (a projection rebuild replays the ledger from a checkpoint to the same digest as the live projection and refuses to publish a rebuild whose digest differs; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/rebuild/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DATA-006`, `DATA-009`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DATA_010`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DATA_010`; `GOLDEN=TestTodo_DATA_010_Golden`; `RECOVERY=TestTodo_DATA_010_Recovery`.
  - **RED:** deleting/corrupting projection cannot be reconstructed or rebuilt hash/count/watermark differs from clean expected result.
  - **GREEN:** replay produces identical semantic rows and current watermark; service reports stale/unavailable during rebuild rather than silently serving invalid state.
  - **REFACTOR:** rebuild runs in isolated shadow namespace.
  - **Refs:** [Projection rebuild](specs/transaction-ledger-reconciliation-and-repair.md), [Recovery matrix](plan.md#114-recovery-matrix).

- [ ] `DATA-011` **[PHASE_2][SOL_HIGH] Compare and promote a shadow projection version.**
  - **Depends:** `DATA-010`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DATA_011`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DATA_011`; `GOLDEN=TestTodo_DATA_011_Golden`; `SECURITY=TestTodo_DATA_011_Security`; `RECOVERY=TestTodo_DATA_011_Recovery`; `FAULT=TestTodo_DATA_011_Fault`.
  - **RED:** differing rows/authorization behavior/lag or incomplete replay permits active-pointer switch.
  - **GREEN:** comparison reports exact differences; only approved compatible result atomically promotes, with rollback pointer retained.
  - **REFACTOR:** source history is never rewritten during migration.
  - **Refs:** [Phase 2 shadow rebuild](plan.md#phase-2-hcm-workflow-operating-layer), [configuration promotion](specs/hris-admin-dataops.md).

- [ ] `DATA-012` **[GATE_B][SOL_HIGH] Continuously verify ledger and relational invariants.**
  - **Depends:** `DATA-004`, `MODEL-025`, `LEDGER-007`, `DB-COVERAGE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DATA_012`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DATA_012`; `PROPERTY=TestTodo_DATA_012_Property`; `GOLDEN=TestTodo_DATA_012_Golden`; `MUTATION=TestTodo_DATA_012_Mutation`.
  - **RED:** sequence gap/digest mismatch/orphan event/outbox/projection/provenance/duplicate settlement is missed or quality remains healthy.
  - **GREEN:** targeted/sample/partition/full scans emit exact finding, affected set/watermark/severity and open incident/repair; affected quality becomes degraded/unknown.
  - **REFACTOR:** verifier has read-only authority.
  - **Refs:** [Ledger integrity checks](specs/transaction-ledger-reconciliation-and-repair.md), [quality spec](specs/data-quality-and-invariant-evaluation.md).

- [x] `DATA-013` **[GATE_B][SOL_HIGH] Create replay checkpoints and snapshots.**
  - **Evidence (2026-09-05):** `TestTodo_DATA_013` in `internal/data/rebuild`, `internal/data/ledger/checkpoint` (replay snapshot manifests (migration 00057) are accepted only after their canonical digest is verified against the ledger, so a snapshot is a performance artifact and never the chronology; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/rebuild/ ./internal/data/ledger/checkpoint/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DATA-004`, `DATA-010`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DATA_013`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DATA_013`; `GOLDEN=TestTodo_DATA_013_Golden`; `SECURITY=TestTodo_DATA_013_Security`; `RECOVERY=TestTodo_DATA_013_Recovery`.
  - **RED:** unverified snapshot, missing source head/schema/policy or replay beginning after a gap is accepted.
  - **GREEN:** checkpoint manifest binds tenant/streams/heads/digests/schema/config/key refs and verifies before accelerated replay.
  - **REFACTOR:** snapshot is performance artifact; ledger remains chronology.
  - **Refs:** [Ledger recovery](specs/transaction-ledger-reconciliation-and-repair.md), [operations models](data/models/operations-production.md).

- [x] `DATA-014` **[GATE_A][SOL_HIGH] Complete provenance publishing.**
  - **Depends:** `MODEL-020`, `DATA-003`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DATA_014`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DATA_014`; `GOLDEN=TestTodo_DATA_014_Golden`; `RACE=TestTodo_DATA_014_Race`.
  - **RED:** pending endpoint, missing edge, duplicate publish or partial lineage reports complete.
  - **GREEN:** publisher records endpoint status, source watermark and idempotent edges; query returns `COMPLETE`, `PARTIAL`, `UNKNOWN`, or `REDACTED` honestly.
  - **REFACTOR:** rebuild compares count/hash/watermark before promotion.
  - **Refs:** [Provenance graph](specs/provenance-graph-and-lineage.md).
  - **Evidence (2026-09-03):** `TestTodo_DATA_014`, `_Golden`, `_Race` in `internal/data/provenance` (`Publish` refuses records without evidence ids or digests, deterministic record id, idempotent republish, `provenance.published` outbox message as protobuf Struct; `Lineage` reports COMPLETE/PARTIAL/UNKNOWN against the ledger stream; table `provenance_record` folded into migration 00015 with RLS); `go test -count=1 ./internal/data/projection/critical/... ./internal/data/provenance/...` PASS via embedded-postgres on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [ ] `DATA-015` **[GATE_B][SOL_HIGH] Trace Promotion through transaction and repair lineage.**
  - **Depends:** `DATA-014`, `PROMO-005`, `INTG-010`, `INTG-016`, `TX-008`, `TX-009`, `TX-010`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DATA_015`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DATA_015`; `RACE=TestTodo_DATA_015_Race`; `INTEGRATION=TestTodo_DATA_015_Integration`; `MUTATION=TestTodo_DATA_015_Mutation`.
  - **RED:** worker field cannot trace through proposal -> approval -> plan -> event -> connector operation -> observation -> repair or leaks hidden nodes.
  - **GREEN:** authorized trace is complete, redaction-safe and source/version/time attributed.
  - **REFACTOR:** UI and audit export consume the same lineage service.
  - **Refs:** [Provenance contract](specs/provenance-graph-and-lineage.md), [Promotion reference](reference-workflows/promote-into-management.md).

- [x] `DATA-016` **[GATE_A][SOL_HIGH] Enforce artifact retrieval authorization and reference accounting.**
  - **Depends:** `MODEL-029`, `TRUST-012`, `MODEL-026`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DATA_016`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DATA_016`; `GOLDEN=TestTodo_DATA_016_Golden`; `SECURITY=TestTodo_DATA_016_Security`; `RECOVERY=TestTodo_DATA_016_Recovery`.
  - **RED:** guessed digest/reference, stale grant, cross-tenant reference or held/deleted artifact is returned incorrectly.
  - **GREEN:** retrieval requires governed ref/current scope; reference inventory tracks records/holds/backups/deletion without exposing bytes.
  - **REFACTOR:** raw object-store URLs never become product authority.
  - **Refs:** [Artifact truth](plan.md#515-one-canonical-history-many-rebuildable-read-planes), [records management](specs/records-management-and-disposition.md).
  - **Evidence (2026-09-03):** `TestTodo_DATA_016`, `_Golden`, `_Security` (purpose, classification, guessed id, cross-tenant, stale grant, RLS at the DB layer), `_Recovery` in `internal/data/artifacts` (`Retrieve` is the only bytes-returning path; denials write a durable refusal row in the same transaction; append-only reference log with distinct-owner count; `SweepCandidates` uses MODEL-026 retention classes and never lists a referenced artifact; no deletion exists); PASS; branch plan-revision-2026-09-02; `go test -count=1 ./internal/data/artifacts/...` on windows/arm64 (Go 1.26.3).

- [x] `DATA-017` **[GATE_B][SOL_HIGH] Partition authoritative data without changing semantics.**
  - **Evidence (2026-09-05):** `TestTodo_DATA_017`, `TestTodo_DATA_017_{Security,Mutation}`, the partition plan tests (including the ledger event plan migration match), `TestKit_fill`, `TestQuoteIdent`, `TestValidateIdent*`, `TestPolicySetsEqual` in `internal/data/partition` (a partition plan with a stable digest proven equal to migration 00005's ledger_event layout; a shadow-table kit comparing parent and shadow rows, RLS refusals and mutation refusals across differently-hashed tenants so partitioning changes no semantics); `go test -count=1 ./internal/data/partition/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DATA-001`, `TENANT-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DATA_017`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DATA_017`; `SECURITY=TestTodo_DATA_017_Security`; `MUTATION=TestTodo_DATA_017_Mutation`.
  - **RED:** partition pruning omits tenant/time records, cross-partition transaction pretends ACID or moved partition changes stream identity.
  - **GREEN:** routing by tenant/time/cell returns complete authorized results and preserves stream/order/transaction contracts.
  - **REFACTOR:** partition policy is operational metadata, not domain schema.
  - **Refs:** [Physical data architecture](specs/platform-architecture-catalog.md), [tenant placement models](data/models/operations-production.md).

- [x] `DATA-018` **[GATE_B][SOL_HIGH] Apply records/holds to ledger, outbox and artifacts.**
  - **Evidence (2026-09-05):** `TestTodo_DATA_018` in `internal/data/recordsmeta` (records declarations enumerate every material copy (migration 00056) and hold or disposition transitions propagate to each copy link with immutable event evidence; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/recordsmeta/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-026`, `MODEL-027`, `DATA-007`, `DATA-016`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DATA_018`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DATA_018`; `GOLDEN=TestTodo_DATA_018_Golden`; `MUTATION=TestTodo_DATA_018_Mutation`.
  - **RED:** material row lacks declaration or hold freezes only canonical copy while export/artifact/derived copy is destroyed.
  - **GREEN:** inventory links every copy to record declaration; hold/disposition decision covers all tracked copies and exceptions.
  - **REFACTOR:** minimal immutable ledger metadata can survive payload crypto-erasure.
  - **Refs:** [Records management](specs/records-management-and-disposition.md), [privacy contract](plan.md#95-privacy-contract).

- [x] `DATA-019` **[GATE_B][SOL_HIGH] Prove no cross-tenant sensitive deduplication leak.**
  - **Evidence (2026-09-05):** `TestTodo_DATA_019`, `TestTodo_DATA_019_{Security,Race}` in `internal/data/artifacts` (the composite (tenant_id, content_id) key plus RLS from migration 00010 mean identical bytes in two tenants never share a row, the equality oracle cannot reveal another tenant's holding, and concurrent same-content writes across tenants stay isolated); `go test -count=1 ./internal/data/artifacts/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DATA-016`, `MODEL-023`, `TENANT-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DATA_019`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DATA_019`; `RACE=TestTodo_DATA_019_Race`; `SECURITY=TestTodo_DATA_019_Security`.
  - **RED:** equal sensitive bytes share deletable encryption/reference fate across tenants or equality oracle reveals another tenant's content.
  - **GREEN:** dedupe is prohibited or tenant-keyed/encrypted so delete/preserve/access decisions are independent.
  - **REFACTOR:** global dedupe allowed only for explicitly public/non-sensitive immutable assets.
  - **Refs:** [Records boundaries](specs/records-management-and-disposition.md), [risk register](specs/risk-register.md).

- [x] `DATA-020` **[GATE_A][TERRA] Publish data-plane health and freshness.**
  - **Depends:** `DATA-006`, `OPS-001`, `OPS-002`, `OPS-003`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DATA_020`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DATA_020`; `GOLDEN=TestTodo_DATA_020_Golden`; `RECOVERY=TestTodo_DATA_020_Recovery`.
  - **RED:** missing/stale watermark or unhealthy consumer still reports projection current.
  - **GREEN:** health exposes authoritative source head, applied head, lag, schema, rebuild status and accepted staleness; callers block/warn per policy.
  - **REFACTOR:** worker IDs are not high-cardinality metric labels.
  - **Refs:** [Operational readiness](plan.md#112-operational-readiness), [telemetry governance](data/models/operations-production.md).
  - **Evidence (2026-09-03):** `TestTodo_DATA_020`, `_Golden`, `_Recovery` (real `projection.Reconciler` flips DEGRADED to HEALTHY) in `internal/data/health` (`Probe.Run` never returns an error; UNKNOWN outranks DEGRADED outranks HEALTHY; deterministic snapshot digest; telemetry samples under the existing catalog names); `go test -count=1 ./internal/data/health/...` PASS via embedded-postgres on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `DATA-021` **[GATE_B][SOL_HIGH] Enforce projection read barriers and minimum watermarks.**
  - **Evidence (2026-09-05):** `TestTodo_DATA_021` in `internal/data/projection` (projection reads carry a read barrier that refuses to serve a projection behind the caller's required ledger position; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/projection/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DATA-006`, `DATA-007`, `DATA-009`, `DATA-020`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DATA_021`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DATA_021`; `RACE=TestTodo_DATA_021_Race`; `FAULT=TestTodo_DATA_021_Fault`; `RECOVERY=TestTodo_DATA_021_Recovery`; `MUTATION=TestTodo_DATA_021_Mutation`.
  - **RED:** dependent workflow/domain read proceeds below required transaction/stream watermark, mixes incompatible heads, waits forever during poison/rebuild, or treats missing watermark as current.
  - **GREEN:** caller declares required source transaction/stream heads and deadline; barrier returns authorized data at-or-after the minimum compatible watermark or typed `STALE`, `DEGRADED`, `UNAVAILABLE`, `REBUILDING`, or `TIMEOUT` with current heads and retry policy.
  - **REFACTOR:** critical co-committed projections satisfy from commit receipt; asynchronous projections use the same semantic contract without polling business tables ad hoc.
  - **Refs:** [Projection contract](specs/transaction-ledger-reconciliation-and-repair.md), [workflow context](workflows/_engine/workflow-context-contract.md).

- [x] `DATA-024` **[GATE_A][TERRA] Seed the deterministic HarborCare demo workforce without perturbing the frozen conformance corpus.**
  - **Depends:** `DATA-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestHarborCarePlanIsCoherentAndPhotoCoverageIsExact`.
  - **TEST MATRIX:** `PRIMARY=TestHarborCarePlanIsCoherentAndPhotoCoverageIsExact`; `GOLDEN=TestHarborCarePlanIsCoherentAndPhotoCoverageIsExact`; `PROPERTY=TestHarborCareOrganizationHierarchyHasValidParents`.
  - **RED:** the sixty-worker HarborCare plan omits a declared photo for a worker inside `PhotoCount`, duplicates a worker or org-unit key, or an org unit's parent reference does not resolve inside the plan's own hierarchy.
  - **GREEN:** `demoworkforce.Company`/`Plan` produce a deterministic sixty-worker plan under the fixed `harborcare-demo` company key and namespace UUID with exact photo coverage and a valid org-unit parent tree; `organization.go`/`seed.go` insert that plan's legal entity, org units and workers into the tenant-scoped tables through `internal/data/tenancy` and `internal/data/workforce` idempotently, reporting planned/inserted/skipped counts, and never touch the four-worker conformance corpus other Promotion regression vectors depend on.
  - **REFACTOR:** keep demo population generation and the conformance corpus on separate namespaces so expanding the demo can never move a promotion regression digest.
  - **Refs:** [Promotion reference](reference-workflows/promote-into-management.md), [tenant partitioning](specs/platform-architecture-catalog.md).
  - **Evidence (2026-09-06):** `TestHarborCarePlanIsCoherentAndPhotoCoverageIsExact`, `TestHarborCareOrganizationHierarchyHasValidParents` in `internal/data/demoworkforce`; `go test -count=1 ./internal/data/demoworkforce/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `DATA-025` **[GATE_B][SOL_HIGH] Persist and read journey-created workers alongside the frozen conformance corpus.**
  - **Depends:** `DATA-024`, `PEOPLE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestStoreCreateAndGetRoundTripEveryColumn`.
  - **TEST MATRIX:** `PRIMARY=TestStoreCreateAndGetRoundTripEveryColumn`; `SECURITY=TestStoreIsTenantIsolated`; `MUTATION=TestStoreCreateRefusesADuplicateKey`; `FAULT=TestFactsReportAbsenceRatherThanFailing`.
  - **RED:** `journey_worker` (migration `00023`) accepts a duplicate worker key or id, a stored row is mutated after insert, a cross-tenant row is readable, or `Facts` returns an error instead of `Exists=false` for a worker the created population does not hold.
  - **GREEN:** `Store.Create` is append-only and tenant-isolated through `internal/data/tenancy`, refusing a duplicate key with a typed `ErrDuplicate`; `Facts` projects a stored row onto `internal/domains/people`'s fixed worker field mask; `NewLayeredWorkerFacts` composes the frozen four-worker conformance corpus with this store so the corpus answers first, a corpus failure propagates rather than being swallowed, and a genuine gap falls through to the database only when one is wired.
  - **REFACTOR:** keep one governed read path for created workers so no second reader can disclose a field the corpus reader would withhold.
  - **Refs:** [Promotion reference](reference-workflows/promote-into-management.md), [tenant partitioning](specs/platform-architecture-catalog.md).
  - **Evidence (2026-09-06):** `TestStoreCreateAndGetRoundTripEveryColumn`, `TestStoreIsTenantIsolated`, `TestStoreCreateRefusesADuplicateKey`, `TestFactsReportAbsenceRatherThanFailing` in `internal/data/workforce`; `go test -count=1 ./internal/data/workforce/...` PASS via embedded-postgres on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

---

## 12. Integration Platform and first design-partner connector

> **Disposition (2026-09-02):** P1A: one read/observe connector to the partner HCM and one read path to the independently owned downstream system, with mapping, provenance and diff. P1B: that connector's single governed write, operation journal, redrive. Sync engine, webhooks, schema discovery and capacity manager are DESIGN.

- [x] `INTG-001` **[GATE_A][SOL_LOW] Register immutable ConnectorDefinition versions.**
  - **Depends:** `CAP-001`, `MODEL-017`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_INTG_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_INTG_001`; `GOLDEN=TestTodo_INTG_001_Golden`; `FUZZ=FuzzTodo_INTG_001`; `RACE=TestTodo_INTG_001_Race`; `INTEGRATION=TestTodo_INTG_001_Integration`; `FAULT=TestTodo_INTG_001_Fault`.
  - **RED:** missing vendor/version/object/capability/auth/read/write/event/pagination/rate/idempotency/observation/reconciliation/health contract fails publication.
  - **GREEN:** exact supported surface and maturity level resolve by stable definition/version.
  - **REFACTOR:** named connector uses shared runtime, not bespoke domain service.
  - **Refs:** [Integration Platform](specs/integration-platform.md), [integration slice](execution-plan.md#integration-platform-slice).
  - **Evidence (2026-09-03):** `TestTodo_INTG_001`, `_Golden`, `_Fault`, `_Race`, `_Integration`, `FuzzTodo_INTG_001` in `internal/connectivity` (immutable versioned `ConnectorDefinition` registry with digests); `go test -count=1 ./internal/connectivity/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `INTG-002` **[GATE_A][SOL_HIGH] Implement ConnectorConnection lifecycle.**
  - **Depends:** `INTG-001`, `TRUST-015`–`TRUST-018`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_INTG_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_INTG_002`; `FUZZ=FuzzTodo_INTG_002`; `INTEGRATION=TestTodo_INTG_002_Integration`; `FAULT=TestTodo_INTG_002_Fault`; `SECURITY=TestTodo_INTG_002_Security`.
  - **RED:** raw credentials, wrong tenant/org/environment/residency/destination, failed validation or revoked connection becomes active.
  - **GREEN:** draft -> validating -> ready -> active/degraded/suspended/quarantined/revoked records secret ref, scopes, endpoint policy and evidence.
  - **REFACTOR:** connection instance cannot broaden definition capabilities.
  - **Refs:** [Integration connections](specs/integration-platform.md), [integration models](data/models/connectivity-access-content.md).
  - **Evidence (2026-09-03):** `TestTodo_INTG_002`, `_Security`, `_Fault`, `_Integration`, `FuzzTodo_INTG_002` in `internal/connectivity` (`ConnectorConnection` lifecycle DRAFT→VALIDATING→READY→ACTIVE/DEGRADED/SUSPENDED/QUARANTINED/REVOKED with legality table and transition evidence; credentials as opaque refs only); PASS; branch plan-revision-2026-09-02; `go test -count=1 ./internal/connectivity/...` on windows/arm64 (Go 1.26.3).

- [x] `INTG-003` **[GATE_A][SOL_HIGH] Diagnose external credentials and permissions.**
  - **Depends:** `INTG-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_INTG_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_INTG_003`; `GOLDEN=TestTodo_INTG_003_Golden`; `FUZZ=FuzzTodo_INTG_003`; `INTEGRATION=TestTodo_INTG_003_Integration`; `FAULT=TestTodo_INTG_003_Fault`; `SECURITY=TestTodo_INTG_003_Security`.
  - **RED:** missing provider scope/permission/network/TLS is reported healthy or diagnostic leaks secret/token.
  - **GREEN:** probe returns authentication, reachability, required/actual scopes and exact affected capabilities/workflows without mutation.
  - **REFACTOR:** provider-specific checks normalize to shared findings.
  - **Refs:** [Connector test bench](specs/integration-platform.md), [HRIS DataOps](specs/hris-admin-dataops.md).
  - **Evidence (2026-09-03):** `TestTodo_INTG_003`, Declared `INTG-003` test matrix and acceptance behavior in `internal/connectivity/diagnostics`; PASS; `go test -p 1 ./internal/connectivity/diagnostics` and `go vet ./internal/connectivity/diagnostics` on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `INTG-004` **[GATE_A][SOL_HIGH] Ingest and quarantine external schema snapshots.**
  - **Evidence (2026-09-05):** `TestTodo_INTG_004`, `TestTodo_INTG_004_{Golden,Race,Fault,Mutation}`, `FuzzTodo_INTG_004`, validator and memory-store tests in `internal/connectivity/schemasnapshot`; `TestTodo_INTG_004_Integration`, `_Integration_Rejected`, `_Integration_ArtifactAndSnapshotCommitTogether` in `internal/connectivity/schemasnapshot/adapters/postgres` (migration `00025_schema_snapshot.sql`: `integration_schema_snapshot` born QUARANTINED under a (tenant, provider, digest) identity with a single trigger-guarded QUARANTINED→ADMITTED/REJECTED transition, and append-only `integration_schema_snapshot_evidence` written in the same Decide call; RLS tenant_isolation on both); `go test -count=1 ./migrations/ ./internal/connectivity/schemasnapshot/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `INTG-002`, `TRUST-019`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_INTG_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_INTG_004`; `GOLDEN=TestTodo_INTG_004_Golden`; `FUZZ=FuzzTodo_INTG_004`; `RACE=TestTodo_INTG_004_Race`; `INTEGRATION=TestTodo_INTG_004_Integration`; `FAULT=TestTodo_INTG_004_Fault`; `MUTATION=TestTodo_INTG_004_Mutation`.
  - **RED:** untrusted discovered schema auto-publishes or missing descriptor/version/digest/source/effective context is accepted.
  - **GREEN:** immutable `SchemaSnapshot` enters review and never becomes canonical `SchemaRelease` automatically.
  - **REFACTOR:** vendors without discovery use signed observed fixtures.
  - **Refs:** [Schema discovery](specs/integration-platform.md), [schema alias rules](data/models/registry-and-coverage-contracts.md#canonical-boundary-decisions).

- [x] `INTG-005` **[GATE_A][SOL_LOW] Diff external schema versions and impacts.**
  - **Evidence (2026-09-05):** `TestTodo_INTG_005` in `internal/connectivity/schemasnapshot/diff` (typed diff between two admitted snapshot bodies of one provider (entities and fields added, removed, type- and constraint-changed, renamed only when declared), BREAKING/ADDITIVE/COSMETIC impact against a declared consumed-field mapping set, stable diff digest, Explain, different providers or a non-admitted snapshot refused; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/connectivity/schemasnapshot/diff/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `INTG-004`, `CONFIG-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_INTG_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_INTG_005`; `GOLDEN=TestTodo_INTG_005_Golden`; `FUZZ=FuzzTodo_INTG_005`; `INTEGRATION=TestTodo_INTG_005_Integration`; `FAULT=TestTodo_INTG_005_Fault`; `CONFORMANCE=TestTodo_INTG_005_Conformance`.
  - **RED:** removed/changed field/enum/type or behavior drift is classified compatible or affected mapping/workflow/report remains undiscovered.
  - **GREEN:** diff returns added/changed/removed, compatibility/lossiness and complete known consumer impacts/blockers.
  - **REFACTOR:** unknown impact blocks promotion rather than guessing.
  - **Refs:** [Integration schema diff](specs/integration-platform.md), [DataOps impact](specs/hris-admin-dataops.md).

- [x] `INTG-006` **[GATE_A][SOL_HIGH] Compile deterministic MappingProfile versions.**
  - **Evidence (2026-09-05):** `TestTodo_INTG_006` in `internal/connectivity/mapping` (MappingProfile compiled deterministically into an immutable MappingProfileVersion with a canonical digest, field mappings naming a transformation IR digest or identity, unknown target fields, duplicate mappings and classification downgrades refused, identical input compiling to an identical digest across goroutines; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/connectivity/mapping/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `INTG-004`, `XFORM-002`, `XFORM-003`, `MODEL-019`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_INTG_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_INTG_006`; `GOLDEN=TestTodo_INTG_006_Golden`; `FUZZ=FuzzTodo_INTG_006`; `INTEGRATION=TestTodo_INTG_006_Integration`; `FAULT=TestTodo_INTG_006_Fault`; `MUTATION=TestTodo_INTG_006_Mutation`.
  - **RED:** arbitrary code, untyped/lossy undeclared transform, implicit delete/null/omit, float money, ambient locale/time or unresolved crosswalk fails.
  - **GREEN:** golden vectors cover rename/type/enum/reference/time/money/default/condition/delete semantics and emit stable output digest.
  - **REFACTOR:** mapping compiler is bounded and offline.
  - **Refs:** [Mapping engine](specs/integration-platform.md), [crosswalks](data/models/dataops-configuration.md).

- [x] `INTG-007` **[GATE_A][SOL_HIGH] Publish MappingProfile with approval and rollback evidence.**
  - **Evidence (2026-09-05):** `TestTodo_INTG_007` in `internal/connectivity/mapping` (publication of a compiled MappingProfileVersion requiring a distinct approver with reason and evidence, activation as a digested event naming the previously active version, rollback as an explicit activation of a prior published version with its own reason and approver, unpublished or superseded activation refused, ordered activation history with lineage, concurrent activations leaving exactly one active, rollback restoring the prior active after a failed activation; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/connectivity/mapping/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `INTG-005`, `INTG-006`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_INTG_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_INTG_007`; `PROPERTY=TestTodo_INTG_007_Property`; `FUZZ=FuzzTodo_INTG_007`; `INTEGRATION=TestTodo_INTG_007_Integration`; `FAULT=TestTodo_INTG_007_Fault`; `CONFORMANCE=TestTodo_INTG_007_Conformance`; `MUTATION=TestTodo_INTG_007_Mutation`.
  - **RED:** failed fixture/impact/compatibility, missing owner/approval or changed-after-review mapping activates.
  - **GREEN:** active mapping version/effective interval is immutable; rollback activates a prior approved version and preserves history.
  - **REFACTOR:** workflow/operation pins mapping version at plan time.
  - **Refs:** [Integration mappings](specs/integration-platform.md), [configuration governance](plan.md#111-environment-and-change-governance).

- [x] `INTG-008` **[GATE_A][SOL_HIGH] Implement bounded read/observe pagination and checkpoints.**
  - **Depends:** `INTG-002`, `INTG-007`, `MODEL-022`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_INTG_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_INTG_008`; `FUZZ=FuzzTodo_INTG_008`; `INTEGRATION=TestTodo_INTG_008_Integration`; `FAULT=TestTodo_INTG_008_Fault`.
  - **RED:** duplicate/missing page, unstable ordering, token expiry, throttle, partial response or restart loses/duplicates observation.
  - **GREEN:** stable tie-break pagination and fenced checkpoint resume produce one immutable observation per semantic source version.
  - **REFACTOR:** full/incremental/delta modes share checkpoint contract.
  - **Refs:** [Sync engine](specs/integration-platform.md), [Gate A connector](execution-plan.md#gate-a--paid-design-partner-observation).
  - **Evidence (2026-09-03):** `TestTodo_INTG_008`, `_Fault`, `_Security` (reflects over `Connector` to prove no mutating method), `FuzzTodo_INTG_008` in `internal/connectivity/observe`; `TestTodo_INTG_008_Integration` in `observe/adapters/postgres` (fenced checkpoints decided in one upsert, stale worker cannot rewind, unstable ordering rejected); PASS via embedded-postgres; branch plan-revision-2026-09-02; `go test -count=1 ./internal/connectivity/observe/...` on windows/arm64 (Go 1.26.3).

- [x] `INTG-009` **[GATE_A][SOL_HIGH] Persist ExternalObservation evidence.**
  - **Depends:** `INTG-008`, `DATA-003`, `MODEL-020`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_INTG_009`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_INTG_009`; `GOLDEN=TestTodo_INTG_009_Golden`; `FUZZ=FuzzTodo_INTG_009`; `INTEGRATION=TestTodo_INTG_009_Integration`; `FAULT=TestTodo_INTG_009_Fault`; `MUTATION=TestTodo_INTG_009_Mutation`.
  - **RED:** raw provider response becomes domain fact or observation lacks source/schema/mapping/authority/freshness/watermark/digest.
  - **GREEN:** observation is immutable, classified, source-attributed and returns fresh/stale/partial/unknown/unavailable honestly.
  - **REFACTOR:** protected raw payload retention is separate from normalized observation.
  - **Refs:** [External outcomes](plan.md#56-external-outcomes-must-be-observed), [integration platform](specs/integration-platform.md).
  - **Evidence (2026-09-03):** `TestTodo_INTG_009`, `_Golden`, `_Mutation`, `_Fault`, `FuzzTodo_INTG_009` in `internal/connectivity/observe`; `TestTodo_INTG_009_Integration`, `_Fault_Integration` in `observe/adapters/postgres` (append-only `external_observation` and `observation_checkpoint` tables from migration 00007, deterministic UUIDv5 observation ids, content digest excludes retrieval time, replay verification); fake incumbent HRIS in `internal/connectivity/fakeincumbent` with call log; PASS; branch plan-revision-2026-09-02; `go test -count=1 ./internal/connectivity/observe/...` on windows/arm64 (Go 1.26.3).

- [x] `INTG-010` **[GATE_A][SOL_HIGH] Reconcile intended, canonical and observed pilot fields.**
  - **Evidence (2026-09-05):** `TestTodo_INTG_010` in `internal/connectivity/observe` (internal/connectivity/observe reconciles intended, canonical and observed pilot fields per external operation into a typed verdict (MATCH, DRIFT, UNKNOWN) with the differing field named; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/connectivity/observe/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `INTG-009`, `MODEL-021`, `TX-009`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_INTG_010`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_INTG_010`; `GOLDEN=TestTodo_INTG_010_Golden`; `FUZZ=FuzzTodo_INTG_010`; `RACE=TestTodo_INTG_010_Race`; `INTEGRATION=TestTodo_INTG_010_Integration`; `FAULT=TestTodo_INTG_010_Fault`; `MUTATION=TestTodo_INTG_010_Mutation`; `SECURITY=TestTodo_INTG_010_Security`.
  - **RED:** stale/unknown/redacted observation, authority disagreement, legitimate concurrent change or mapping mismatch is reported pass.
  - **GREEN:** result distinguishes match, delivery failure, partial application, stale observation, unauthorized mutation, concurrent change and mapping disagreement.
  - **REFACTOR:** tolerance/comparison policy is versioned by field/domain.
  - **Refs:** [Reconciliation](specs/transaction-ledger-reconciliation-and-repair.md), [Gate A diff](execution-plan.md#gate-a-acceptance--paid-observation).

- [x] `INTG-011` **[GATE_B][SOL_HIGH] Persist ConnectorOperation journals before dispatch.**
  - **Evidence (2026-09-05):** `TestTodo_INTG_011` in `internal/connectivity/operation` (a ConnectorOperation journal row is written durably before any dispatch, so a crash after the journal but before the send is recoverable and a send never happens without its journal; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/connectivity/operation/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DATA-008`, `INTG-002`, `INTG-007`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_INTG_011`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_INTG_011`; `GOLDEN=TestTodo_INTG_011_Golden`; `FUZZ=FuzzTodo_INTG_011`; `RACE=TestTodo_INTG_011_Race`; `INTEGRATION=TestTodo_INTG_011_Integration`; `FAULT=TestTodo_INTG_011_Fault`; `SECURITY=TestTodo_INTG_011_Security`; `MUTATION=TestTodo_INTG_011_Mutation`.
  - **RED:** external call occurs without operation ID/business transaction/canonical input/mapped digest/connection/mapping/credential/version/idempotency/ordering/fence metadata.
  - **GREEN:** planned/queued operation is durable and classified before a worker obtains dispatch lease.
  - **REFACTOR:** raw sensitive request/response follows retention policy, not ordinary logs.
  - **Refs:** [External write journal](specs/integration-platform.md), [Gate B requirements](execution-plan.md#gate-b-acceptance--limited-write-authority).

- [x] `INTG-012` **[GATE_B][SOL_HIGH] Revalidate an external operation at dispatch lease time.**
  - **Evidence (2026-09-05):** `TestTodo_INTG_012` in `internal/connectivity/operation` (an external operation is revalidated under its dispatch lease (migration 00065) immediately before the send, refusing with a typed code when the proposal, approval or lease changed; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/connectivity/operation/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `INTG-011`, `GOVERN-003`, `TRUST-016`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_INTG_012`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_INTG_012`; `FUZZ=FuzzTodo_INTG_012`; `RACE=TestTodo_INTG_012_Race`; `INTEGRATION=TestTodo_INTG_012_Integration`; `FAULT=TestTodo_INTG_012_Fault`; `SECURITY=TestTodo_INTG_012_Security`; `MUTATION=TestTodo_INTG_012_Mutation`.
  - **RED:** stale mapping/destination/credential/authority/writer fence/cutover watermark or superseded business intent sends.
  - **GREEN:** current checks confirm exact operation or route blocked/replan/repair with zero provider call.
  - **REFACTOR:** queued work cannot rely solely on enqueue-time authority.
  - **Refs:** [Integration authority fencing](specs/integration-platform.md), [source authority](specs/source-authority-and-external-mastering.md).

- [x] `INTG-013` **[GATE_B][SOL_HIGH] Dispatch one governed external write idempotently.**
  - **Evidence (2026-09-05):** `TestTodo_INTG_013` in `internal/connectivity/operation` (one governed external write (the Promotion payroll sync) is dispatched exactly once under its semantic idempotency identity across retries and duplicate leases; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/connectivity/operation/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `INTG-012`, `TX-010`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_INTG_013`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_INTG_013`; `FUZZ=FuzzTodo_INTG_013`; `RACE=TestTodo_INTG_013_Race`; `INTEGRATION=TestTodo_INTG_013_Integration`; `FAULT=TestTodo_INTG_013_Fault`; `RECOVERY=TestTodo_INTG_013_Recovery`; `MUTATION=TestTodo_INTG_013_Mutation`.
  - **RED:** concurrent/duplicate/replayed operation, stale external version, permission denial or provider retry creates duplicate/change outside scope.
  - **GREEN:** one logical effect uses stable idempotency and conditional version where supported; attempts record normalized outcomes.
  - **REFACTOR:** provider client does transport, not business authority.
  - **Refs:** [Governed connector maturity](specs/integration-platform.md), [Gate B write path](execution-plan.md#gate-b--limited-write-authority).

- [x] `INTG-014` **[GATE_B][SOL_HIGH] Handle timeout-after-send ambiguity.**
  - **Evidence (2026-09-05):** `TestTodo_INTG_014` in `internal/connectivity/operation` (a timeout after send yields an UNKNOWN outcome with a typed observation requirement (migration 00067) rather than a guessed success or failure; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/connectivity/operation/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `INTG-013`, `WF-STEP-014`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_INTG_014`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_INTG_014`; `FUZZ=FuzzTodo_INTG_014`; `INTEGRATION=TestTodo_INTG_014_Integration`; `FAULT=TestTodo_INTG_014_Fault`.
  - **RED:** timeout triggers blind retry/compensation/alternate write or marks failure/success without observation.
  - **GREEN:** operation becomes `AMBIGUOUS/OBSERVATION_PENDING`; provider lookup/observation resolves applied/not-applied/conflict or escalates repair.
  - **REFACTOR:** retry reuses the same logical-effect key only after policy permits.
  - **Refs:** [Integration ambiguity](specs/integration-platform.md), [workflow context edge cases](workflows/samples/workflow-context-edge-cases.md).

- [ ] `INTG-015` **[GATE_B][SOL_HIGH] Enforce rate-aware fair connector scheduling.**
  - **Depends:** `INTG-011`, `ADMISSION-001`, `ADMISSION-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_INTG_015`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_INTG_015`; `FUZZ=FuzzTodo_INTG_015`; `RACE=TestTodo_INTG_015_Race`; `INTEGRATION=TestTodo_INTG_015_Integration`; `FAULT=TestTodo_INTG_015_Fault`; `SECURITY=TestTodo_INTG_015_Security`.
  - **RED:** one tenant/resource floods queue, 429 storm multiplies retries, unknown quota assumes unlimited or P3 starves P0.
  - **GREEN:** per-tenant/connection/resource/criticality limits honor provider reset/concurrency and expose queue age/predicted completion.
  - **REFACTOR:** reserved capacity never violates vendor quota.
  - **Refs:** [Rate-limit manager](specs/integration-platform.md), [platform correctness](plan.md#519-platform-correctness-is-business-correctness).

- [x] `INTG-016` **[GATE_B][SOL_HIGH] Redrive one failed operation without rerunning its parent.**
  - **Evidence (2026-09-05):** `TestTodo_INTG_016` in `internal/connectivity/operation` (one failed operation is redriven in isolation with lineage and approval evidence (migration 00068) without rerunning its siblings; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/connectivity/operation/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `INTG-014`, `TX-008`, `TX-009`, `TX-010`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_INTG_016`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_INTG_016`; `FUZZ=FuzzTodo_INTG_016`; `INTEGRATION=TestTodo_INTG_016_Integration`; `FAULT=TestTodo_INTG_016_Fault`; `SECURITY=TestTodo_INTG_016_Security`; `MUTATION=TestTodo_INTG_016_Mutation`.
  - **RED:** redrive starts parent workflow/transaction, changes semantic input silently, uses revoked credential or duplicates successful siblings.
  - **GREEN:** preview compares original/current mapping/policy/authority; approved attempt retains operation lineage and reconciles resulting observation.
  - **REFACTOR:** material semantic change requires RepairPlan approval.
  - **Refs:** [Integration redrive](specs/integration-platform.md), [HRIS DataOps redrive](specs/hris-admin-dataops.md).

- [x] `INTG-017` **[GATE_A][SOL_LOW] Publish connector health and dependency impact.**
  - **Evidence (2026-09-05):** `TestTodo_INTG_017` in `internal/connectivity/health` (health is HEALTHY, DEGRADED, INCIDENT, or UNKNOWN with causes, watermarks and impacted capabilities/workflows/deadlines; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/connectivity/health/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `INTG-003`, `INTG-008`, `INTG-010`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_INTG_017`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_INTG_017`; `GOLDEN=TestTodo_INTG_017_Golden`; `FUZZ=FuzzTodo_INTG_017`; `RACE=TestTodo_INTG_017_Race`; `INTEGRATION=TestTodo_INTG_017_Integration`; `FAULT=TestTodo_INTG_017_Fault`; `BENCHMARK=BenchmarkTodo_INTG_017`.
  - **RED:** auth/permission/schema/queue/capacity/error/webhook/sync/observation failure reports healthy or affected workflows remain unknown.
  - **GREEN:** health is `HEALTHY`, `DEGRADED`, `INCIDENT`, or `UNKNOWN` with causes, watermarks and impacted capabilities/workflows/deadlines.
  - **REFACTOR:** health projection cannot change connection authority.
  - **Refs:** [Integration health](specs/integration-platform.md), [incident management](specs/incident-management.md).

- [x] `INTG-018` **[PHASE_2][SOL_HIGH] Implement authenticated webhook receipt and replay.**
  - **Evidence (2026-09-05):** `TestTodo_INTG_018` in `internal/connectivity/webhook` (immutable receipt/disposition/dedupe persists; replay redelivers existing event and never invents business truth/effect; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/connectivity/webhook/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-017`, `TRUST-019`, `DATA-009`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_INTG_018`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_INTG_018`; `GOLDEN=TestTodo_INTG_018_Golden`; `FUZZ=FuzzTodo_INTG_018`; `RACE=TestTodo_INTG_018_Race`; `INTEGRATION=TestTodo_INTG_018_Integration`; `FAULT=TestTodo_INTG_018_Fault`; `SECURITY=TestTodo_INTG_018_Security`; `RECOVERY=TestTodo_INTG_018_Recovery`; `MUTATION=TestTodo_INTG_018_Mutation`.
  - **RED:** invalid signature/timestamp/replay window, duplicate-different bytes, oversized/unknown schema or cross-tenant subject reaches consumer.
  - **GREEN:** immutable receipt/disposition/dedupe persists; replay redelivers existing event and never invents business truth/effect.
  - **REFACTOR:** receipt bytes stay quarantined/protected.
  - **Refs:** [Webhooks](specs/integration-platform.md), [system subscriptions](specs/messaging-and-notification-plane.md).

- [ ] `INTG-019` **[PHASE_2][SOL_HIGH] Implement resumable SyncJob modes.**
  - **Depends:** `INTG-008`, `INTG-015`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_INTG_019`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_INTG_019`; `FUZZ=FuzzTodo_INTG_019`; `INTEGRATION=TestTodo_INTG_019_Integration`; `FAULT=TestTodo_INTG_019_Fault`.
  - **RED:** restart loses cursor, absence infers deletion without policy, bidirectional loop oscillates or per-item failures disappear.
  - **GREEN:** full/incremental/delta/targeted jobs preserve cursor/watermark/counts/items; ownership prevents loops and missing records become explicit candidates.
  - **REFACTOR:** unchanged fingerprint suppresses unnecessary writes.
  - **Refs:** [Sync engine](specs/integration-platform.md).

- [x] `INTG-020` **[PHASE_3][SOL_HIGH] Implement managed file-transfer lifecycle.**
  - **Evidence (2026-09-05):** `TestTodo_INTG_020` in `internal/connectivity/mft` (managed file-transfer definitions with direction, endpoint, schedule and file pattern, a typed DECLARED/STAGED/VERIFIED/DELIVERED/ACKNOWLEDGED/FAILED/QUARANTINED lifecycle whose every transition is a digested event, manifest verification by per-file sha256, count and size bounds, idempotent redelivery by manifest digest and a receipt chain over in-memory ports; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/connectivity/mft/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-029`, `TRUST-017`, `TRUST-019`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_INTG_020`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_INTG_020`; `GOLDEN=TestTodo_INTG_020_Golden`; `FUZZ=FuzzTodo_INTG_020`; `INTEGRATION=TestTodo_INTG_020_Integration`; `FAULT=TestTodo_INTG_020_Fault`; `RECOVERY=TestTodo_INTG_020_Recovery`.
  - **RED:** partial/oversized/wrong-hash/untrusted SSH/PGP/certificate/malformed file is processed; replay duplicates records.
  - **GREEN:** pickup/drop/manifest/hash/size/signature/ack/checkpoint/archive/quarantine/retention states are durable and resumable.
  - **REFACTOR:** CSV/JSON/XML parsing happens only on safe immutable artifacts.
  - **Refs:** [Managed file transfer boundary](specs/platform-responsibility-boundaries.md), [Integration Platform](specs/integration-platform.md).

## 12A. External-effect, reconciliation, repair and execution-evidence convergence

> **Disposition (2026-09-02):** P1B, with effects dispatched as sequential `OBSERVE` steps; the effect graph compiler (`EFFECT-001`) covers ordering and idempotency only until `PARALLEL` exists.

- [x] `EFFECT-001` **[GATE_B][SOL_HIGH] Compile the external-effect dependency graph.**
  - **Depends:** `TX-001`, `WF-COMP-003`, `INTG-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_EFFECT_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_EFFECT_001`; `PROPERTY=TestTodo_EFFECT_001_Property`; `RACE=TestTodo_EFFECT_001_Race`; `INTEGRATION=TestTodo_EFFECT_001_Integration`; `MUTATION=TestTodo_EFFECT_001_Mutation`.
  - **RED:** undeclared dependency, cycle, incompatible ordering keys, irreversible effect without observation/repair route, child effect that can run before prerequisite, or hidden messaging/integration cost passes compilation.
  - **GREEN:** immutable effect graph binds each semantic effect to proposal/transaction, capability/connector, prerequisites, ordering/fence/idempotency, criticality, dispatch condition, observation contract, deadline, failure/compensation/repair policy and terminal contribution.
  - **REFACTOR:** graph declares business dependencies while connector queues enforce provider capacity independently.
  - **Refs:** [Transaction plan](specs/transaction-plan-and-commit-coordinator.md), [integration operation journal](specs/integration-platform.md), [workflow side effects](specs/workflow-runtime.md).
  - **Evidence (2026-09-03):** `TestTodo_EFFECT_001`, Declared `EFFECT-001` test matrix and acceptance behavior in `internal/effectgraph`; PASS; `go test -p 1 ./internal/effectgraph` and `go vet ./internal/effectgraph` on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `RECON-001` **[GATE_B][SOL_HIGH] Implement a durable reconciliation-job lifecycle.**
  - **Evidence (2026-09-05):** `TestTodo_RECON_001` in `internal/operations/reconcile`, `migrations`, `internal/data/tenancy/storagedisposition` (one durable ReconciliationJob per committed mandatory effect and comparison policy in migration 00040's effect_reconciliation_job (job id derived from tenant, effect idempotency key and policy ref so Coordinator.Trigger is idempotent and a duplicate returns the existing job), Job carrying intended and canonical refs, required freshness, observation attempts, next check, deadline, owner and SLA ref with status in the closed set PENDING/OBSERVING/PASS/MISMATCH/PARTIAL/UNKNOWN/EXPIRED/REPAIR_REQUIRED, Coordinator.Poll verifying the caller's lease fence before any write, settling an exhausted deadline to EXPIRED or REPAIR_REQUIRED per the effect's RepairPolicy without deleting the row, an observation below required freshness never reaching the Comparer and never ending polling, MemoryStore plus PostgresStore under RLS with version CAS, restart reloading everything from rows, and an import-allowlist conformance scan proving no domain or provider package is reached; the lane's colliding 00039 no-op was removed and uuid was allowed for the package in dependency-roles.yaml; written by a Sonnet subagent and verified independently); `go test -count=1 ./internal/operations/reconcile/ ./migrations/ ./internal/data/tenancy/storagedisposition/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `EFFECT-001`, `INTG-009`, `WF-RUN-004`, `TIME-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.OPERATIONS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_RECON_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_RECON_001`; `GOLDEN=TestTodo_RECON_001_Golden`; `RACE=TestTodo_RECON_001_Race`; `FAULT=TestTodo_RECON_001_Fault`; `MUTATION=TestTodo_RECON_001_Mutation`.
  - **RED:** committed mandatory effect has no scheduled observation, restart loses deadline/backoff/owner, stale result ends polling, duplicate trigger creates parallel truth, or exhausted observation disappears.
  - **GREEN:** one job per effect/comparison policy persists intended/canonical refs, required freshness, observation attempts, next check, deadline, owner/SLA and `PENDING|OBSERVING|PASS|MISMATCH|PARTIAL|UNKNOWN|EXPIRED|REPAIR_REQUIRED`; scheduling is fenced/idempotent.
  - **REFACTOR:** reconciliation job coordinates observation and comparison; it never mutates domain or provider state.
  - **Refs:** [Reconciliation](specs/transaction-ledger-reconciliation-and-repair.md), [external observations](specs/integration-platform.md).

- [x] `RECON-002` **[GATE_B][SOL_HIGH] Evaluate reconciliation completion from freshness and policy.**
  - **Evidence (2026-09-05):** `TestTodo_RECON_002` in `internal/operations/reconcile` (internal/operations/reconcile evaluates reconciliation completion from observation freshness and the policy floor, flipping the consistency verdict only when every effect is observed within its max age; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/operations/reconcile/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `RECON-001`, `INTG-010`, `MODEL-021`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.OPERATIONS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_RECON_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_RECON_002`; `GOLDEN=TestTodo_RECON_002_Golden`; `RACE=TestTodo_RECON_002_Race`; `INTEGRATION=TestTodo_RECON_002_Integration`; `FAULT=TestTodo_RECON_002_Fault`; `MUTATION=TestTodo_RECON_002_Mutation`.
  - **RED:** provider acceptance, stale/partial/redacted/unknown observation, insufficient coverage, legitimate concurrent change or expired deadline is reported `PASS`; optional effect blocks mandatory completion without policy.
  - **GREEN:** versioned policy evaluates expected population/fields, authority, tolerance, observation freshness/coverage and effect criticality into `PASS`, `MISMATCH`, `PARTIAL`, `UNKNOWN`, `WAIVED`, or `REPAIR_REQUIRED`, with exact terminal-dimension contribution and next action.
  - **REFACTOR:** comparison result and business completion policy remain separate typed decisions.
  - **Refs:** [Multidimensional completion](plan.md#63-change-request-lifecycle), [reconciliation contract](specs/transaction-ledger-reconciliation-and-repair.md).

- [x] `REPAIR-001` **[GATE_A][SOL_HIGH] Generate a canonical RepairPlan from reconciliation findings.**
  - **Depends:** `INTG-010`, `GOVERN-002`, `TX-001`, `MODEL-014`.
  - **INTENT CONTEXT:** `ROLE=DIRECT; SETS=BI.OPERATIONS; INTENTS=hcmnext.operations.create_repair_plan/v1; FAMILY=CHANGE_REQUEST (child-bound process); WHY=implement accepted intent 463 as a non-executable governed diagnosis and plan`.
  - **TEST:** `TestTodo_REPAIR_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_REPAIR_001`; `GOLDEN=TestTodo_REPAIR_001_Golden`; `RACE=TestTodo_REPAIR_001_Race`; `FAULT=TestTodo_REPAIR_001_Fault`; `MUTATION=TestTodo_REPAIR_001_Mutation`.
  - **RED:** stale/unknown finding, unsupported causal claim, raw log suggestion, unconstrained retry, hidden sibling effect or destructive history rewrite produces executable repair.
  - **GREEN:** diagnosis records evidence/confidence/unknowns and produces immutable non-executable plan with exact target, expected/observed state, corrective capability/effects, write set, idempotency, risk, approval/SoD, observation, rollback/compensation and success criteria.
  - **REFACTOR:** automated diagnosis may propose; deterministic capability/governance contracts define what can execute.
  - **Refs:** [Repair plans](specs/transaction-ledger-reconciliation-and-repair.md), [workflow repair mode](specs/workflow-runtime.md).
  - **Evidence (2026-09-03):** `TestTodo_REPAIR_001`, `_Golden`, `_Race`, `_Fault`, `_Mutation` in `internal/domains/repair` (`CreateRepairPlan`: canonical, digested, non-executable recommendation; `Validate` rejects `Executable: true`, no Execute exists); PASS; branch plan-revision-2026-09-02; `go test -count=1 ./internal/domains/repair/...` on windows/arm64 (Go 1.26.3).

- [x] `REPAIR-002` **[GATE_B][SOL_HIGH] Revalidate a RepairPlan immediately before execution.**
  - **Evidence (2026-09-05):** `TestTodo_REPAIR_002` in `internal/operations/repair` (a RepairPlan is revalidated immediately before execution against the current proposal, approval and observed effect state, refusing with a typed code when any of them moved since the plan was raised; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/operations/repair/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `REPAIR-001`, `GOVERN-003`, `APPROVAL-005`, `INTG-012`, `TX-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.OPERATIONS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_REPAIR_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_REPAIR_002`; `GOLDEN=TestTodo_REPAIR_002_Golden`; `RACE=TestTodo_REPAIR_002_Race`; `FAULT=TestTodo_REPAIR_002_Fault`; `SECURITY=TestTodo_REPAIR_002_Security`; `MUTATION=TestTodo_REPAIR_002_Mutation`.
  - **RED:** changed finding/observation/authority/mapping/credential/policy/target version, already-satisfied state, superseding transaction or expired approval executes stale repair.
  - **GREEN:** current evidence either confirms the exact repair digest and creates a separately fenced/idempotent execution or returns `NO_LONGER_REQUIRED`, `REPLAN_REQUIRED`, `REAPPROVAL_REQUIRED`, `BLOCKED`, or `UNKNOWN` with zero corrective effect.
  - **REFACTOR:** repair execution never re-enters or reruns the parent business transaction.
  - **Refs:** [Repair and redrive](specs/transaction-ledger-reconciliation-and-repair.md), [integration revalidation](specs/integration-platform.md).

- [ ] `EVIDENCE-001` **[GATE_B][SOL_HIGH] Produce one BusinessExecutionReceipt for a closed Intent.**
  - **Depends:** `INTENT-008`, `DATA-015`, `RECON-002`, `REPAIR-002`, `LEDGER-007`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_EVIDENCE_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_EVIDENCE_001`; `GOLDEN=TestTodo_EVIDENCE_001_Golden`; `RACE=TestTodo_EVIDENCE_001_Race`; `MUTATION=TestTodo_EVIDENCE_001_Mutation`.
  - **RED:** receipt omits or cannot verify intent/request, snapshot, simulation, proposal, workflow/version, approvals, transaction/stream heads, domain revisions, effects, observations, reconciliation, repair, obligations or terminal dimensions; redaction changes verification truth.
  - **GREEN:** immutable receipt/package references exact digests and authority/provenance lineage, distinguishes absent/not-applicable/redacted/unknown, verifies integrity and exposes an authorized summary without copying unrestricted payloads.
  - **REFACTOR:** receipt is a logical evidence object assembled from owner records, not a second ledger or denormalized source of truth.
  - **Refs:** [Provenance](specs/provenance-graph-and-lineage.md), [assurance and evidence models](data/models/assurance-intelligence-platform.md), [ledger and repair](specs/transaction-ledger-reconciliation-and-repair.md).

## 13. HRIS DataOps and configuration lifecycle

> **Disposition (2026-09-02):** P1A ships four operator surfaces: cross-system diff (`DATAOPS-008`), effective-date debugger (`DATAOPS-007`), AuthZ explainer and connector test bench. `DATAOPS-001`–`DATAOPS-004` are an operator CSV seed path, not a product; `DATAOPS-005` is DEFERRED and `DATAOPS-006` Gate C. `CONFIG-001`/`002` are P1B minimal; `CONFIG-003` Gate C. Productization follows the P1A product test in the DataOps spec.

- [x] `DATAOPS-001` **[GATE_A][SOL_HIGH] Stage an immutable import batch without changing business state.**
  - **Depends:** `MODEL-022`, `MODEL-023`, `DATA-016`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_DATAOPS_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DATAOPS_001`; `GOLDEN=TestTodo_DATAOPS_001_Golden`; `FUZZ=FuzzTodo_DATAOPS_001`; `SECURITY=TestTodo_DATAOPS_001_Security`.
  - **RED:** malformed, oversized, encrypted-with-unknown-key, cross-tenant or unsupported input creates a domain write or an unclassified artifact.
  - **GREEN:** return `StagedImport{status: STAGED, row_count, source_hash, schema_candidate, classification, tenant_id}`; domain-write count remains `0`.
  - **REFACTOR:** streaming readers cap memory and preserve byte/row provenance.
  - **Refs:** [HRIS DataOps and onboarding](specs/hris-admin-dataops.md).
  - **Evidence (2026-09-03):** `TestTodo_DATAOPS_001` and matrix variants in `internal/domains/dataops/importing` (immutable content-addressed `Batch`/`Row` via `StageBatch`/`StageCSV`, no mutation API, bounded columns/cells/rows/bytes); `go test -count=1 ./internal/domains/dataops/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `DATAOPS-002` **[GATE_A][TERRA] Profile staged data deterministically.**
  - **Depends:** `DATAOPS-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_DATAOPS_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DATAOPS_002`; `GOLDEN=TestTodo_DATAOPS_002_Golden`; `FUZZ=FuzzTodo_DATAOPS_002`.
  - **RED:** two runs over identical bytes return different inferred types, null counts, distributions or anomaly samples.
  - **GREEN:** return the same versioned `DataProfile` digest with field types, completeness, uniqueness, ranges, candidate keys and bounded samples.
  - **REFACTOR:** large-file profiling remains bounded and records approximation error where sampling is used.
  - **Refs:** [Import pipeline](specs/hris-admin-dataops.md), [data quality](specs/data-quality-and-invariant-evaluation.md).
  - **Evidence (2026-09-03):** `TestTodo_DATAOPS_002` and matrix variants in `internal/domains/dataops/importing` (deterministic `Profile` with type inference, exact min/max via math/big, bounded distinct tracking, byte-stable digest); `go test -count=1 ./internal/domains/dataops/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `DATAOPS-003` **[GATE_A][SOL_HIGH] Compile source-to-canonical import mapping.**
  - **Depends:** `DATAOPS-002`, `XFORM-002`, `XFORM-003`, `MODEL-017`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_DATAOPS_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DATAOPS_003`; `GOLDEN=TestTodo_DATAOPS_003_Golden`; `FUZZ=FuzzTodo_DATAOPS_003`.
  - **RED:** unknown target, lossy conversion, missing required reference crosswalk or nondeterministic transform compiles.
  - **GREEN:** compiler returns an immutable mapping digest, typed diagnostics and declared reads/writes; the golden fixture maps every accepted cell identically.
  - **REFACTOR:** import and connector mappings share one transformation kernel.
  - **Refs:** [Mapping and crosswalk](specs/hris-admin-dataops.md), [SchemaFlux](plan.md#518-go-core-contract-first-and-open-source-first).
  - **Evidence (2026-09-03):** `TestTodo_DATAOPS_003` and matrix variants in `internal/domains/dataops/importing` (`Compile` against `internal/intent/model.Registry` with a closed transform set; unresolved property or transform is a typed compile error); `go test -count=1 ./internal/domains/dataops/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `DATAOPS-004` **[GATE_A][SOL_HIGH] Validate every staged row with stable error identity.**
  - **Depends:** `DATAOPS-003`, `RULE-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_DATAOPS_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DATAOPS_004`; `FUZZ=FuzzTodo_DATAOPS_004`; `MUTATION=TestTodo_DATAOPS_004_Mutation`.
  - **RED:** invalid identity/reference/effective-date/domain value is accepted, or row order changes diagnostic identity.
  - **GREEN:** return `VALID` or typed errors keyed by source row/field/rule/version; valid-row and invalid-row counts equal the fixture oracle.
  - **REFACTOR:** validation performs no domain writes and exposes bounded error samples plus full artifact.
  - **Refs:** [Import validation](specs/hris-admin-dataops.md), [quality layers](specs/data-quality-and-invariant-evaluation.md).
  - **Evidence (2026-09-03):** `TestTodo_DATAOPS_004` and matrix variants in `internal/domains/dataops/importing` (`ValidateBatch` with stable sha256(batch|row|property|rule) error identities and a closed rule vocabulary; re-runs yield identical error sets); `go test -count=1 ./internal/domains/dataops/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [ ] `DATAOPS-005` **[DESIGN][SOL_HIGH] Simulate an import as ordered BusinessIntents.**
  - **Depends:** `DATAOPS-004`, `INTENT-004`, `CONFLICT-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_DATAOPS_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DATAOPS_005`; `GOLDEN=TestTodo_DATAOPS_005_Golden`; `FUZZ=FuzzTodo_DATAOPS_005`; `SECURITY=TestTodo_DATAOPS_005_Security`; `RECOVERY=TestTodo_DATAOPS_005_Recovery`; `MUTATION=TestTodo_DATAOPS_005_Mutation`.
  - **RED:** preview omits no-op/conflict/authority/approval/legal/side-effect/cost counts or differs when replayed from the same snapshot.
  - **GREEN:** return a signed `ImportSimulation` with exact creates/changes/no-ops/errors/conflicts, planned writes, obligations and zero committed effects.
  - **REFACTOR:** simulation output is the proposal input, not an alternate write path.
  - **Refs:** [Data migration lifecycle](specs/hris-admin-dataops.md), [transaction simulation](specs/transaction-ledger-reconciliation-and-repair.md).

- [ ] `DATAOPS-006` **[GATE_C][SOL_HIGH] Commit an approved import resumably and reconcile every item.**
  - **Depends:** `DATAOPS-005`, `TX-010`, `WF-RUN-019`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_DATAOPS_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DATAOPS_006`; `FUZZ=FuzzTodo_DATAOPS_006`; `MUTATION=TestTodo_DATAOPS_006_Mutation`.
  - **RED:** changed batch/mapping/proposal, duplicate resume, partial checkpoint or item failure silently duplicates/skips writes.
  - **GREEN:** only proposal-bound items commit once; result counts partition input exactly and every committed item has transaction, observation and reconciliation lineage.
  - **REFACTOR:** rollback means governed corrective intents, never ledger deletion.
  - **Refs:** [Import commit and migration cutover](specs/hris-admin-dataops.md).

- [x] `DATAOPS-007` **[GATE_A][TERRA] Explain an effective-dated field history.**
  - **Depends:** `DATA-006`, `MODEL-020`, `MODEL-021`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_DATAOPS_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DATAOPS_007`; `PROPERTY=TestTodo_DATAOPS_007_Property`; `FUZZ=FuzzTodo_DATAOPS_007`; `SECURITY=TestTodo_DATAOPS_007_Security`.
  - **RED:** current/future/retro/known-at-time fixture is flattened, correction is presented as history deletion or unauthorized fields leak.
  - **GREEN:** return ordered assertions and observations with effective/recorded intervals, authority class, correction lineage and field-level redaction reasons.
  - **REFACTOR:** debugger queries temporal APIs rather than reconstructing from logs.
  - **Refs:** [Effective-date debugger](specs/hris-admin-dataops.md), [temporal semantics](data/models/wire-contract-primitives.md).
  - **Evidence (2026-09-03):** `TestTodo_DATAOPS_007`, `_Property`, `_Security`, `FuzzTodo_DATAOPS_007` in `internal/domains/dataops` (`ExplainFieldHistory` over a `FieldHistory` port using the `internal/engines/effectivedate` bitemporal timeline); `go test -count=1 ./internal/domains/... ./internal/engines/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `DATAOPS-008` **[GATE_A][TERRA] Produce a cross-system field diff with repair-safe classification.**
  - **Depends:** `INTG-010`, `DATAOPS-007`.
  - **INTENT CONTEXT:** `ROLE=DIRECT; SETS=BI.OPERATIONS; INTENTS=hcmnext.operations.detect_drift/v1; FAMILY=ANALYTICAL_REQUEST; WHY=implement accepted intent 461 from intended-authoritative-observed comparison without mutation`.
  - **TEST:** `TestTodo_DATAOPS_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DATAOPS_008`; `FUZZ=FuzzTodo_DATAOPS_008`; `SECURITY=TestTodo_DATAOPS_008_Security`.
  - **RED:** stale/unknown/redacted values are called mismatches or a mismatch is automatically repaired without authority proof.
  - **GREEN:** each comparison returns `MATCH`, `MISMATCH`, `STALE`, `UNKNOWN`, `REDACTED`, or `NOT_APPLICABLE` plus sources, timestamps and allowed next capabilities.
  - **REFACTOR:** comparison is pure; repair is a separate governed intent.
  - **Refs:** [Cross-system diff](specs/hris-admin-dataops.md), [source authority](specs/source-authority-and-external-mastering.md).
  - **Evidence (2026-09-03):** `TestTodo_DATAOPS_008`, `_Security`, `FuzzTodo_DATAOPS_008`, `TestDetectDriftReportsBoundedPopulationWithZeroEffects` in `internal/domains/dataops` (`DiffRecord` via `internal/engines/fielddiff` with seven relations and repair-safe classification; `DetectDrift` over an `ObservationReader` port, zero-effect receipt); PASS; branch plan-revision-2026-09-02; `go test -count=1 ./internal/domains/dataops/...` on windows/arm64 (Go 1.26.3).

- [x] `CONFIG-001` **[GATE_A][SOL_HIGH] Diff two configuration snapshots semantically.**
  - **Depends:** `MODEL-017`, `MODEL-020`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS,BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CONFIG_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CONFIG_001`; `GOLDEN=TestTodo_CONFIG_001_Golden`; `SECURITY=TestTodo_CONFIG_001_Security`.
  - **RED:** reordered equivalent config reports change or a semantic workflow/policy/schema/mapping change is hidden.
  - **GREEN:** return stable added/removed/changed/compatible/breaking sets and referenced capability/workflow/tenant impacts.
  - **REFACTOR:** secret values are compared by metadata/fingerprint and never disclosed.
  - **Refs:** [Configuration diff](specs/hris-admin-dataops.md), [environment governance](plan.md#111-environment-and-change-governance).
  - **Evidence (2026-09-03):** `TestTodo_CONFIG_001`, `_Golden`, `_Security` in `internal/platform/config` (`Diff` over typed snapshots: order-independent, secrets compared by fingerprint only, semantic-class and ref changes classified breaking); `go test -count=1 ./internal/platform/config/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `CONFIG-002` **[GATE_A][SOL_HIGH] Package configuration with a signed dependency manifest.**
  - **Depends:** `CONFIG-001`, `TRUST-020`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS,BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CONFIG_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CONFIG_002`; `PROPERTY=TestTodo_CONFIG_002_Property`; `GOLDEN=TestTodo_CONFIG_002_Golden`; `SECURITY=TestTodo_CONFIG_002_Security`; `CONFORMANCE=TestTodo_CONFIG_002_Conformance`.
  - **RED:** package has floating dependency, missing schema/rule/workflow/mapping version, secret material or unverifiable signature.
  - **GREEN:** return immutable bundle digest, ordered dependency graph, compatibility range, signer and provenance; verification returns `VALID`.
  - **REFACTOR:** bundle contains credential references only.
  - **Refs:** [Configuration packages](specs/platform-responsibility-boundaries.md), [config promotion](specs/hris-admin-dataops.md).
  - **Evidence (2026-09-03):** `TestTodo_CONFIG_002`, `_Property`, `_Golden`, `_Security`, `_Conformance` in `internal/platform/config` (`Bundle`, canonical digest, ed25519 `SignBundle`/`VerifyBundle` distinguishing TAMPERED from INVALID_SIGNATURE); PASS; branch plan-revision-2026-09-02; `go test -count=1 ./internal/platform/config/...` on windows/arm64 (Go 1.26.3).

- [x] `CONFIG-003` **[GATE_C][SOL_HIGH] Promote configuration through validate, simulate, approve and activate.**
  - **Evidence (2026-09-05):** `TestTodo_CONFIG_003` in `internal/platform/config/promotion` (activation is atomic by environment, pins a version set, emits evidence and preserves immediate rollback target; running instances retain their pinned versions; written by an earlier lane (Luna or Sonnet) and verified independently); `go test -count=1 ./internal/platform/config/promotion/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CONFIG-002`, `APPROVAL-005`, `WF-STEP-011`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS,BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CONFIG_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CONFIG_003`; `GOLDEN=TestTodo_CONFIG_003_Golden`; `MUTATION=TestTodo_CONFIG_003_Mutation`; `FAULT=TestTodo_CONFIG_003_Fault`; `RACE=TestTodo_CONFIG_003_Race`.
  - **RED:** unapproved, incompatible, stale, failed-simulation or changed-after-review package activates.
  - **GREEN:** activation is atomic by environment, pins a version set, emits evidence and preserves immediate rollback target; running instances retain their pinned versions.
  - **REFACTOR:** rollback is a new activation event, not history mutation.
  - **Refs:** [Configuration promotion](specs/hris-admin-dataops.md), [change governance](plan.md#111-environment-and-change-governance).

## 14. Tenant lifecycle, placement, sandbox and commercial pilot

> **Disposition (2026-09-02):** P1A for tenant isolation tests, one sandbox tenant and the commercial pilot record. Logical placement, cell epochs and residency fencing (`TENANT-001`) are Gate C; one physical cell with tenant-scoped rows is the P1A/P1B shape.

- [x] `TENANT-001` **[GATE_C][SOL_HIGH] Resolve signed logical tenant placement and residency.**
  - **Depends:** `TRUST-001`, `TRUST-008`, `MODEL-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_TENANT_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TENANT_001`; `SECURITY=TestTodo_TENANT_001_Security`.
  - **RED:** wrong tenant/region/isolation tier or stale placement epoch reaches repository, event, outbox, workload or principal context.
  - **GREEN:** every context carries the same signed `TenantPlacement{tenant, cell, region, residency_profile, isolation_tier, epoch}` or fails closed with `PLACEMENT_MISMATCH`.
  - **REFACTOR:** one physical cell is acceptable; logical fencing is not optional.
  - **Refs:** [Tenant cells](data/models/operations-production.md), [lifecycle ownership](specs/platform-responsibility-boundaries.md#tenant-provisioning-and-lifecycle).
  - **Evidence (2026-09-03):** `TestTodo_TENANT_001`, Declared `TENANT-001` test matrix and acceptance behavior in `internal/domains/tenant`; PASS; `go test -p 1 ./internal/domains/tenant` and `go vet ./internal/domains/tenant` on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `TENANT-002` **[GATE_A][SOL_HIGH] Bootstrap one pilot tenant idempotently.**
  - **Evidence (2026-09-05):** `TestTodo_TENANT_002` in `internal/domains/tenant`, `internal/data/tenancy`, `test/tenant` (migration 00029 bootstrap receipts plus the plane-verification model: ten closed planes each with a Verified record from a verifier distinct from the requester and non-empty evidence refs, ACTIVE only when every plane is VERIFIED, later failure moving the tenant to DEGRADED naming the plane, digested append-only provisioning events, idempotent re-runs, no cross-tenant receipt visibility; five planes are proven against the real repository today (tenant row and RLS isolation, tenant KEK round-trip through custody, activated configuration objects, registry versus live schema, an RLS-scoped governed read) and the other five (admin, placement, products, recovery contacts, audit) run through explicitly labelled fakes until their own todos land; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/tenant/ ./internal/data/tenancy/ ./test/tenant/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TENANT-001`, `TOOL-014`, `TRUST-019`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_TENANT_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TENANT_002`; `GOLDEN=TestTodo_TENANT_002_Golden`; `RACE=TestTodo_TENANT_002_Race`; `FAULT=TestTodo_TENANT_002_Fault`; `SECURITY=TestTodo_TENANT_002_Security`.
  - **RED:** retry duplicates admin/key/config/schema resources or activation succeeds with any required plane unhealthy/unreconciled.
  - **GREEN:** resumable `ProvisioningRun` reaches `ACTIVE` only after identity, admin, keys, placement, products, policies, schemas, recovery contacts, audit and health checks all return `VERIFIED`.
  - **REFACTOR:** per-plane steps expose independent idempotency keys and repair routes.
  - **Refs:** [Tenant provisioning](specs/platform-responsibility-boundaries.md#tenant-provisioning-and-lifecycle), [Gate A](execution-plan.md#gate-a--paid-design-partner-observation).

- [x] `COMM-001` **[GATE_A][SOL_HIGH] Resolve the fixed-price pilot entitlement snapshot.**
  - **Evidence (2026-09-05):** `TestTodo_COMM_001` in `internal/commercial` (every channel returns the same allow/deny code and immutable contract/entitlement fingerprint; amendments affect new executions only; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/commercial/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TENANT-002`, `CAP-003`, `GOVERN-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.EXPERIENCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_COMM_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_COMM_001`; `GOLDEN=TestTodo_COMM_001_Golden`; `INTEGRATION=TestTodo_COMM_001_Integration`; `SECURITY=TestTodo_COMM_001_Security`; `MUTATION=TestTodo_COMM_001_Mutation`.
  - **RED:** expired/not-yet-effective contract or out-of-scope capability is allowed by UI, HTTP, gRPC, workflow or connector path.
  - **GREEN:** every channel returns the same allow/deny code and immutable contract/entitlement fingerprint; amendments affect new executions only.
  - **REFACTOR:** no variable rating or invoice engine is introduced in Phase 1.
  - **Refs:** [Phase 1 commercial scope](plan.md#cross-phase-foundation-track), [execution exclusions](execution-plan.md#explicit-non-goals).

- [x] `COMM-002` **[GATE_A][TERRA] Record external/manual pilot invoice evidence.**
  - **Evidence (2026-09-05):** `TestTodo_COMM_002` in `internal/commercial` (append-only evidence records contract fingerprint, external invoice ID, amount/currency, dates/status, artifact hash and reconciliation state; duplicate replay returns the original record; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/commercial/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `COMM-001`, `DATA-014`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.EXPERIENCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_COMM_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_COMM_002`; `SECURITY=TestTodo_COMM_002_Security`; `RECOVERY=TestTodo_COMM_002_Recovery`.
  - **RED:** amount differs from signed contract, duplicate external reference mutates history or issued invoice artifact lacks tenant isolation/hash.
  - **GREEN:** append-only evidence records contract fingerprint, external invoice ID, amount/currency, dates/status, artifact hash and reconciliation state; duplicate replay returns the original record.
  - **REFACTOR:** invoice generation, tax calculation and payment collection remain out of Phase 1.
  - **Refs:** [Pricing hypothesis](plan.md#125-commercial-architecture-hypotheses), [Phase 1 scope](execution-plan.md#explicit-non-goals).

- [x] `COMM-003` **[GATE_A][SOL_LOW] Produce deterministic pilot economic evidence.**
  - **Evidence (2026-09-05):** `TestTodo_COMM_003` in `internal/commercial` (PilotEconomicEvidence reconciles the pilot interval and returns exactly PROCEED, RESELECT, or STOP from versioned thresholds; customer view omits confidential margin detail; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/commercial/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `COMM-002`, `WEDGE-013`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.EXPERIENCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_COMM_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_COMM_003`; `PROPERTY=TestTodo_COMM_003_Property`; `INTEGRATION=TestTodo_COMM_003_Integration`.
  - **RED:** missing price/labor/provider/infra/adoption/bypass field or nonnumeric threshold passes the gate.
  - **GREEN:** `PilotEconomicEvidence` reconciles the pilot interval and returns exactly `PROCEED`, `RESELECT`, or `STOP` from versioned thresholds; customer view omits confidential margin detail.
  - **REFACTOR:** evidence calculations remain reproducible from immutable inputs.
  - **Refs:** [Commercial proof](next-steps.md#non-negotiable-stop-rules), [Gate A evidence](execution-plan.md#gate-a-acceptance--paid-observation).

- [x] `SANDBOX-001` **[GATE_A][TERRA] Create and reset a synthetic Promotion sandbox with a hard side-effect fence.**
  - **Evidence (2026-09-05):** `TestTodo_SANDBOX_001` in `internal/platform/sandbox` (internal/platform/sandbox creates and resets a synthetic Promotion sandbox behind a hard side-effect fence that refuses every external write; written by a Sonnet subagent and verified independently); `go test -count=1 ./internal/platform/sandbox/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TENANT-002`, `TOOL-014`, `PROMO-002`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_SANDBOX_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SANDBOX_001`; `GOLDEN=TestTodo_SANDBOX_001_Golden`; `SECURITY=TestTodo_SANDBOX_001_Security`; `RECOVERY=TestTodo_SANDBOX_001_Recovery`; `RACE=TestTodo_SANDBOX_001_Race`.
  - **RED:** sandbox can resolve production credential/destination, send real message, submit filing/payment or retain data after expiry/reset.
  - **GREEN:** synthetic fixture runs, all effects route to simulators, reset restores named baseline digest and expiry emits complete destruction receipts.
  - **REFACTOR:** fence enforcement is independent of application configuration.
  - **Refs:** [Sandbox and test-data platform](specs/platform-responsibility-boundaries.md#sandbox-and-test-data-platform).

- [x] `TENANT-003` **[GATE_B][SOL_HIGH] Suspend, resume and close a tenant with capability-specific semantics.**
  - **Evidence (2026-09-05):** `TestTodo_TENANT_003` in `internal/domains/tenant` (internal/domains/tenant suspends, resumes and closes a tenant with capability-specific semantics (reads allowed, governed writes refused, timers parked) proven by the lifecycle tests; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/tenant/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TENANT-002`, `TRUST-005`, `INTG-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_TENANT_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TENANT_003`; `RACE=TestTodo_TENANT_003_Race`; `INTEGRATION=TestTodo_TENANT_003_Integration`; `SECURITY=TestTodo_TENANT_003_Security`; `MUTATION=TestTodo_TENANT_003_Mutation`.
  - **RED:** suspension leaves forbidden sessions/connectors active, blocks legally required access, drops queued work or duplicate request changes outcome.
  - **GREEN:** state transition returns exact allowed/denied capabilities, revocation receipts and pending-work disposition; resume/close is idempotent and audited.
  - **REFACTOR:** commercial, security and legal suspension reasons remain distinct.
  - **Refs:** [Tenant lifecycle](specs/platform-responsibility-boundaries.md#tenant-provisioning-and-lifecycle).

- [ ] `TENANT-004` **[GATE_B][SOL_HIGH] Execute a complete pilot exit rehearsal.**
  - **Depends:** `WEDGE-009`, `RECORDS-COPY-001`, `PRIV-EXIT-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_TENANT_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TENANT_004`; `GOLDEN=TestTodo_TENANT_004_Golden`; `FUZZ=FuzzTodo_TENANT_004`; `INTEGRATION=TestTodo_TENANT_004_Integration`; `SECURITY=TestTodo_TENANT_004_Security`; `RECOVERY=TestTodo_TENANT_004_Recovery`.
  - **RED:** export lacks schema/checksum/recipient verification or certification ignores hold, credential, webhook, provider, derived or backup copy.
  - **GREEN:** rehearsal returns export receipt, shutdown/revocation receipts, exception list, restore re-delete plan and `CERTIFIABLE` only when inventory is complete.
  - **REFACTOR:** rehearsal performs no irreversible production destruction.
  - **Refs:** [Tenant exit](specs/platform-responsibility-boundaries.md#tenant-exit-and-portability), [pilot exit](execution-plan.md#gate-b-acceptance--limited-write-authority).

- [ ] `TENANT-005` **[GATE_C][SOL_HIGH] Relocate a tenant with signed epoch fencing and rollback.**
  - **Depends:** `TENANT-001`, `DATA-020`, `WF-RUN-022`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_TENANT_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TENANT_005`; `GOLDEN=TestTodo_TENANT_005_Golden`; `RACE=TestTodo_TENANT_005_Race`; `SECURITY=TestTodo_TENANT_005_Security`; `RECOVERY=TestTodo_TENANT_005_Recovery`; `FAULT=TestTodo_TENANT_005_Fault`.
  - **RED:** old cell accepts post-cutover write, workflow/timer/signal/outbox/journal/key state is missing or rollback changes logical identity.
  - **GREEN:** freeze, snapshot, copy, catch-up, cutover and reconciliation preserve tenant/stream IDs; stale epoch rejects writes and rollback restores prior placement safely.
  - **REFACTOR:** relocation remains outside Phase 1 physical deployment scope.
  - **Refs:** [Tenant relocation](data/models/operations-production.md), [authority fencing](specs/source-authority-and-external-mastering.md).

## 15. Privacy, records and processing lifecycle

> **Disposition (2026-09-02):** `PRIV-001` (processing inventory) is P1B; retention, legal hold, subject rights and disposition are Gate C. P1A needs only the DPA/security approval captured in `WEDGE-010`.

- [x] `PRIV-001` **[GATE_B][SOL_HIGH] Publish an executable processing-activity and data-flow inventory.**
  - **Evidence (2026-09-05):** `TestTodo_PRIV_001` in `internal/governance/privacy/inventory` (every approved flow resolves to versioned ProcessingActivity and ProcessingDataFlow; each occurrence records recipient/region/category receipt; the inventory records the MONITORING_CONSENT and BREACH_NOTIFICATION obligations of the applicable rule-pack releases so the notificatio; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/governance/privacy/inventory/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-023`, `MODEL-026`, `WEDGE-010`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_PRIV_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PRIV_001`; `INTEGRATION=TestTodo_PRIV_001_Integration`; `SECURITY=TestTodo_PRIV_001_Security`; `MUTATION=TestTodo_PRIV_001_Mutation`.
  - **RED:** pilot field reaches connector/export/model/telemetry path without controller, processor/subprocessor, region, purpose, lawful basis, retention and transfer assessment.
  - **GREEN:** every approved flow resolves to versioned `ProcessingActivity` and `ProcessingDataFlow`; each occurrence records recipient/region/category receipt; the inventory records the `MONITORING_CONSENT` and `BREACH_NOTIFICATION` obligations of the applicable rule-pack releases so the notification deadlines are jurisdiction-derived, not a single global timer.
  - **REFACTOR:** inventory changes are governed configuration, not prose updates.
  - **Refs:** [Privacy and records model](specs/records-management-and-disposition.md), [governance plane](plan.md), [Legal rule packs and state configuration](specs/legal-rule-packs-and-state-configuration.md).

- [x] `PRIV-002` **[GATE_A][SOL_HIGH] Prove notice presentation and optional-processing authority.**
  - **Depends:** `PRIV-001`, `MODEL-027`, `MSG-004`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_PRIV_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PRIV_002`; `SECURITY=TestTodo_PRIV_002_Security`; `BROWSER=TestTodo_PRIV_002_Browser`; `MUTATION=TestTodo_PRIV_002_Mutation`.
  - **RED:** missing/expired notice, unsupported locale/accessibility presentation or withdrawn authority permits optional AI/RAG/analytics/communication use.
  - **GREEN:** presentation/acknowledgement binds exact version/language/scope; withdrawal returns `PROCESSING_BLOCKED` and creates applicable restrict/delete obligations.
  - **REFACTOR:** mandatory legal basis and optional consent are never conflated.
  - **Refs:** [Consent and preferences](data/models/kernel-governance-and-evidence.md), [privacy governance](specs/data-classification-and-dlp.md).
  - **Evidence (2026-09-03):** `TestTodo_PRIV_002`, `_Security`, `_Browser`, `_Mutation` in `internal/governance/privacy` (typed `Notice` with digest, `Presentation` evidence bound to notice digest/version/locale/time, `OptionalProcessing` consent with expiry and append-only withdrawal, `EvaluateAuthority` deny-by-default with mandatory-basis refusal and RESTRICT_PROCESSING/REQUIRE_DELETION obligations on withdrawal; accessible presentation document scored by the uxqual fixture); `go test -count=1 ./internal/governance/privacy/... ./tools/uxqual/forms/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `PRIV-003` **[GATE_B][SOL_HIGH] Bind processor, transfer and DLP decisions at dispatch.**
  - **Evidence (2026-09-05):** `TestTodo_PRIV_003` in `internal/governance/privacy/dispatch` (dispatch binds current signed policy snapshots to operation digest or returns EGRESS_BLOCKED with zero external call and durable evidence; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/governance/privacy/dispatch/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PRIV-001`, `TRUST-018`, `INTG-012`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_PRIV_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PRIV_003`; `GOLDEN=TestTodo_PRIV_003_Golden`; `SECURITY=TestTodo_PRIV_003_Security`; `MUTATION=TestTodo_PRIV_003_Mutation`.
  - **RED:** changed destination/processor/region/classification/purpose or revoked transfer assessment still sends data.
  - **GREEN:** dispatch binds current signed policy snapshots to operation digest or returns `EGRESS_BLOCKED` with zero external call and durable evidence.
  - **REFACTOR:** approval-time decision alone is insufficient.
  - **Refs:** [DLP and egress](specs/data-classification-and-dlp.md), [integration dispatch](specs/integration-platform.md).

- [ ] `RECORDS-COPY-001` **[GATE_B][SOL_HIGH] Maintain a complete data-copy inventory with freshness.**
  - **Depends:** `DATA-016`, `DATA-018`, `PRIV-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PRIVACY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_RECORDS_COPY_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_RECORDS_COPY_001`; `GOLDEN=TestTodo_RECORDS_COPY_001_Golden`; `INTEGRATION=TestTodo_RECORDS_COPY_001_Integration`; `SECURITY=TestTodo_RECORDS_COPY_001_Security`; `RECOVERY=TestTodo_RECORDS_COPY_001_Recovery`.
  - **RED:** canonical/derived/export/backup/provider/telemetry/cache/search/vector/agent-memory copy lacks owner, location, watermark or restore/deletion policy yet disposition certifies.
  - **GREEN:** every copy has tenant, subject, category, processor, region, key, retention, hold, deletion capability, restore policy and freshness; unknown copy blocks certification.
  - **REFACTOR:** discovery reports uncertainty explicitly.
  - **Refs:** [Records model](specs/records-management-and-disposition.md), [copy inventory](specs/data-classification-and-dlp.md).

- [ ] `RECORDS-HOLD-001` **[GATE_B][SOL_HIGH] Propagate and release legal holds across all copies.**
  - **Depends:** `RECORDS-COPY-001`, `DATA-018`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PRIVACY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_RECORDS_HOLD_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_RECORDS_HOLD_001`; `PROPERTY=TestTodo_RECORDS_HOLD_001_Property`; `GOLDEN=TestTodo_RECORDS_HOLD_001_Golden`; `RACE=TestTodo_RECORDS_HOLD_001_Race`; `INTEGRATION=TestTodo_RECORDS_HOLD_001_Integration`; `SECURITY=TestTodo_RECORDS_HOLD_001_Security`; `RECOVERY=TestTodo_RECORDS_HOLD_001_Recovery`; `MUTATION=TestTodo_RECORDS_HOLD_001_Mutation`.
  - **RED:** held canonical fact survives while related export/provider/backup is destroyed, or release fails to recalculate eligibility.
  - **GREEN:** immutable scope snapshot, notices/acknowledgements and per-copy intersections are complete; unrelated records continue disposition and release recalculates every affected copy.
  - **REFACTOR:** hold matching is deterministic and explainable.
  - **Refs:** [Legal hold](specs/records-management-and-disposition.md), [records lifecycle](specs/platform-responsibility-boundaries.md).

- [x] `RECORDS-DISP-001` **[GATE_A][SOL_HIGH] Simulate retention and disposition without deleting bytes.**
  - **Evidence (2026-09-05):** `TestTodo_RECORDS_DISP_001` in `internal/governance/records` (return per-copy action, composed schedule, cutoff/correction history and blockers as ELIGIBLE, BLOCKED_WITH_REASONS or REPAIR_REQUIRED; deletion count is 0; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/governance/records/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-026`, `PRIV-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PRIVACY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_RECORDS_DISP_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_RECORDS_DISP_001`; `SECURITY=TestTodo_RECORDS_DISP_001_Security`; `MUTATION=TestTodo_RECORDS_DISP_001_Mutation`.
  - **RED:** unknown series/custodian, schedule conflict, uncertain cutoff, missing archive acknowledgement or active hold returns eligible; or a mandatory destruction maximum shorter than another jurisdiction's minimum for the same record class is resolved silently instead of reported as a conflict.
  - **GREEN:** return per-copy action, composed schedule, cutoff/correction history and blockers as `ELIGIBLE`, `BLOCKED_WITH_REASONS` or `REPAIR_REQUIRED`; deletion count is `0`.
  - **REFACTOR:** Gate A remains non-destructive.
  - **Refs:** [Records management](specs/records-management-and-disposition.md), [retention governance](plan.md), [Per-kind composition](specs/legal-rule-packs-and-state-configuration.md#63-per-kind-composition).

- [ ] `PRIV-004` **[GATE_B][SOL_HIGH] Reapply deletion and restriction manifests during restore.**
  - **Depends:** `MODEL-028`, `RECORDS-COPY-001`, `RECOVERY-002`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_PRIV_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PRIV_004`; `GOLDEN=TestTodo_PRIV_004_Golden`; `RACE=TestTodo_PRIV_004_Race`; `FAULT=TestTodo_PRIV_004_Fault`; `SECURITY=TestTodo_PRIV_004_Security`; `RECOVERY=TestTodo_PRIV_004_Recovery`; `MUTATION=TestTodo_PRIV_004_Mutation`.
  - **RED:** restore resurrects deleted/restricted data, loses holds or non-idempotently re-deletes.
  - **GREEN:** service stays fenced until tombstone/hold epochs match; report counts, hashes and watermarks and idempotently reapply required restrictions.
  - **REFACTOR:** privacy validation is a restore acceptance gate.
  - **Refs:** [Verified deletion](specs/records-management-and-disposition.md), [recovery](data/models/operations-production.md).

- [x] `PRIV-005` **[GATE_C][SOL_HIGH] Intake and verify a data-subject request.**
  - **Evidence (2026-09-05):** `TestTodo_PRIV_005`, `TestTodo_PRIV_005_{Security,Mutation,Golden,Property,Conformance}` in `internal/domains/privacy/dsr` (closed request-kind vocabulary, subject claims separated from identity evidence with an assurance floor per kind, canonical digest of the intake, statutory clock table computed from the receipt instant, verification evidence appended never rewritten); `go test -count=1 ./internal/domains/privacy/dsr/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-027`, `TRUST-002`, `WORK-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_PRIV_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PRIV_005`; `SECURITY=TestTodo_PRIV_005_Security`; `MUTATION=TestTodo_PRIV_005_Mutation`.
  - **RED:** unverified/expired proof, unauthorized representative, duplicate or cross-tenant subject creates executable request.
  - **GREEN:** accepted ACCESS/RECTIFY/ERASE/RESTRICT/PORTABILITY/OBJECT/REVIEW request has immutable subject, deadline, jurisdiction, proof, scope, status and evidence; otherwise typed denial/clarification.
  - **REFACTOR:** identity proof artifacts remain separately protected.
  - **Refs:** [Privacy intents](specs/business-intent-catalog.md), [privacy records](specs/records-management-and-disposition.md).

- [ ] `PRIV-006` **[GATE_C][SOL_HIGH] Resolve every subject-request item and legal exception.**
  - **Depends:** `PRIV-005`, `RECORDS-COPY-001`, `RECORDS-HOLD-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_PRIV_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PRIV_006`; `GOLDEN=TestTodo_PRIV_006_Golden`; `INTEGRATION=TestTodo_PRIV_006_Integration`; `SECURITY=TestTodo_PRIV_006_Security`; `RECOVERY=TestTodo_PRIV_006_Recovery`; `MUTATION=TestTodo_PRIV_006_Mutation`.
  - **RED:** projection/search/vector/cache/telemetry/export/backup/provider/privileged/retained copy is omitted or exception has no authority.
  - **GREEN:** each item returns fulfill/partial/deny/restrict/retain/anonymize with authority, redaction reason, completeness digest and appeal route.
  - **REFACTOR:** unknown copy prevents false completeness.
  - **Refs:** [Privacy lifecycle](specs/records-management-and-disposition.md).

- [ ] `PRIV-007` **[GATE_C][SOL_HIGH] Reconcile processor acknowledgements and certify fulfillment.**
  - **Depends:** `PRIV-006`, `INTG-010`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_PRIV_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PRIV_007`; `FAULT=TestTodo_PRIV_007_Fault`; `SECURITY=TestTodo_PRIV_007_Security`; `MUTATION=TestTodo_PRIV_007_Mutation`.
  - **RED:** timeout/refusal/unknown copy/unacknowledged deletion is marked complete.
  - **GREEN:** each processor returns receipt or visible retry/escalation; signed package certifies only fully resolved items and preserves exceptions.
  - **REFACTOR:** certificate is derived from evidence, never a manual status toggle.
  - **Refs:** [Processor lifecycle](specs/records-management-and-disposition.md), [reconciliation](specs/transaction-ledger-reconciliation-and-repair.md).

- [x] `PRIV-EXIT-001` **[GATE_B][SOL_HIGH] Verify tenant exit across copies, providers, credentials and backups.**
  - **Evidence (2026-09-05):** `TestTodo_PRIV_EXIT_001` in `internal/governance/exit` (tenant inventory, shutdown/revocation receipts, pending-work disposition, hold exceptions and restore re-delete evidence determine CERTIFIABLE or explicit blockers; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/governance/exit/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `RECORDS-COPY-001`, `TENANT-003`, `PRIV-004`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_PRIV_EXIT_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PRIV_EXIT_001`; `FUZZ=FuzzTodo_PRIV_EXIT_001`; `INTEGRATION=TestTodo_PRIV_EXIT_001_Integration`; `SECURITY=TestTodo_PRIV_EXIT_001_Security`; `RECOVERY=TestTodo_PRIV_EXIT_001_Recovery`; `MUTATION=TestTodo_PRIV_EXIT_001_Mutation`.
  - **RED:** exit certifies while connector credential/webhook/provider/export/backup/derived copy/support grant remains unaccounted for.
  - **GREEN:** tenant inventory, shutdown/revocation receipts, pending-work disposition, hold exceptions and restore re-delete evidence determine `CERTIFIABLE` or explicit blockers.
  - **REFACTOR:** actual irreversible destruction remains Gate C.
  - **Refs:** [Tenant exit](specs/platform-responsibility-boundaries.md#tenant-exit-and-portability), [privacy records](specs/records-management-and-disposition.md).

- [ ] `PRIV-008` **[GATE_C][SOL_HIGH] Enforce a federal-tax-information processing boundary under Publication 1075.**
  - **Depends:** `PRIV-001`, `TRUST-028`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_PRIV_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PRIV_008`; `GOLDEN=TestTodo_PRIV_008_Golden`; `SECURITY=TestTodo_PRIV_008_Security`; `INTEGRATION=TestTodo_PRIV_008_Integration`; `MUTATION=TestTodo_PRIV_008_Mutation`.
  - **RED:** federal tax information flows through the same storage, access and transfer path as ordinary employee PII, with no validated-cryptography requirement, no strong remote-authentication requirement and no disclosure log distinguishing it.
  - **GREEN:** `PRIV-001`'s processing-activity inventory gains an FTI classification that forces `TRUST-028` envelope keys scoped to FTI, remote access gated behind step-up assurance, an append-only disclosure/redisclosure log and an annual-safeguard-review record.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §2, F-12/F-13](research/security-best-practices-2026.md#2-financial-applications-and-payment-touching-systems), [IRS Pub. 1075 encryption](https://www.irs.gov/privacy-disclosure/encryption-requirements-of-publication-1075), [IRS Publication 4557](https://www.irs.gov/pub/irs-pdf/p4557.pdf). Maps to IRS Pub. 1075 §4 (encryption)/§9 (disclosure).

- [ ] `PRIV-009` **[GATE_C][SOL_HIGH] Produce a reproducible financial-breach and notification-decision matrix.**
  - **Depends:** `PRIV-005`, `RECORDS-HOLD-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_PRIV_009`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PRIV_009`; `GOLDEN=TestTodo_PRIV_009_Golden`; `SECURITY=TestTodo_PRIV_009_Security`; `INTEGRATION=TestTodo_PRIV_009_Integration`; `MUTATION=TestTodo_PRIV_009_Mutation`.
  - **RED:** an incident touching payment, bank-detail or FTI data has no recorded discovery time, affected-data scope, jurisdiction matrix or regulator/customer notice decision, so a federal, state, GLBA or tenant-contract notice duty cannot be shown to have been evaluated.
  - **GREEN:** an incident workflow resolves affected data/population, discovery instant, jurisdiction-matrix version, consumer-count calculation and a reproducible notify/no-notify decision per duty (federal, state breach law, GLBA, tenant contract), preserving evidence before any remediation that would alter it.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §2, F-14](research/security-best-practices-2026.md#2-financial-applications-and-payment-touching-systems), [FTC data-breach response guide](https://www.ftc.gov/business-guidance/resources/data-breach-response-guide-business), [California breach reporting](https://www.oag.ca.gov/privacy/databreach/reporting). Maps to state breach-notification statutes; FTC Health Breach Notification Rule (where applicable).

- [ ] `PRIV-010` **[GATE_C][SOL_HIGH] Maintain a state comprehensive-privacy-law release gate.**
  - **Depends:** `PRIV-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_PRIV_010`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PRIV_010`; `GOLDEN=TestTodo_PRIV_010_Golden`; `SECURITY=TestTodo_PRIV_010_Security`; `INTEGRATION=TestTodo_PRIV_010_Integration`; `CONFORMANCE=TestTodo_PRIV_010_Conformance`; `MUTATION=TestTodo_PRIV_010_Mutation`.
  - **RED:** a single global consent/rights toggle is used for California, Colorado, Virginia, Texas and other enacted state laws, so an employee-data right, exemption or processor duty that differs by state is silently mishandled.
  - **GREEN:** a signed, versioned jurisdiction roster with effective dates drives rights, exemptions, sensitive-data rules, processor duties and opt-out/consent signals per state; a primary-source review is required before a new or amended state law's effective date is honored.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §3, G-12/G-10](research/security-best-practices-2026.md#3-applications-interacting-with-federal-state-and-local-government), [California CPPA regulations](https://cppa.ca.gov/regulations/), [Colorado CPA resources](https://coag.gov/resources/colorado-privacy-act/). Maps to CCPA/CPRA; Colorado CPA; Virginia VCDPA; Texas TDPSA.

- [ ] `PRIV-011` **[GATE_C][SOL_HIGH] Align records disposition with public-records and government retention schedules.**
  - **Depends:** `RECORDS-HOLD-001`, `RECORDS-DISP-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_PRIV_011`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PRIV_011`; `GOLDEN=TestTodo_PRIV_011_Golden`; `SECURITY=TestTodo_PRIV_011_Security`; `INTEGRATION=TestTodo_PRIV_011_Integration`; `CONFORMANCE=TestTodo_PRIV_011_Conformance`; `MUTATION=TestTodo_PRIV_011_Mutation`.
  - **RED:** a public-records retention schedule, a tax-retention rule and a privacy-deletion request resolve against the same disposition timer, so a record required by an approved disposition authority could be deleted early or a privacy deletion could be blocked without a named authority.
  - **GREEN:** `RECORDS-DISP-001`'s composed schedule gains a distinct public-records/government schedule-authority class alongside tax and contractual retention; deletion remains blocked while any class's hold or minimum applies, and the blocking authority is named in the response.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §3, G-13](research/security-best-practices-2026.md#3-applications-interacting-with-federal-state-and-local-government), [NARA records management](https://www.archives.gov/records-mgmt/public), [NARA scheduling](https://www.archives.gov/records-mgmt/scheduling/sch-records). Maps to NARA-approved disposition authority; state/local public-records schedules.

## 16. Operations, assurance, overload and recovery

> **Disposition (2026-09-02):** P1A: one backup restored once in non-production. P1B: `OPS-001` pilot SLOs, a named on-call owner, advisory route, redrive/rollback procedure, exit runbook, and a measured restore. Admission control, overload shedding, chaos and game days are Gate C.

- [x] `OPS-001` **[P0][SOL_LOW] Define versioned SLIs, SLOs and error-budget actions for the pilot path.**
  - **Evidence (2026-09-05):** `TestTodo_OPS_001` in `internal/operations/reliability`, `internal/operations/slo` (manifest resolves exact SLI query, target, window, owner and deterministic HEALTHY, AT_RISK, BREACHED, or UNKNOWN result for each pilot capability; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/operations/reliability/ ./internal/operations/slo/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WEDGE-005`, `WEDGE-006`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_OPS_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_OPS_001`; `GOLDEN=TestTodo_OPS_001_Golden`; `BENCHMARK=BenchmarkTodo_OPS_001`.
  - **RED:** missing denominator, staleness bound, availability/latency target, measurement window or breach action validates.
  - **GREEN:** manifest resolves exact SLI query, target, window, owner and deterministic `HEALTHY`, `AT_RISK`, `BREACHED`, or `UNKNOWN` result for each pilot capability.
  - **REFACTOR:** analytics/search outages are not allowed to redefine transaction-path availability.
  - **Refs:** [Operations model](data/models/operations-production.md), [SLO contract](specs/slo-sli-error-budget.md).

- [x] `OPS-002` **[GATE_A][SOL_HIGH] Enforce telemetry privacy, cardinality and sampling policy.**
  - **Evidence (2026-09-05):** `TestTodo_OPS_002` in `internal/operations/telemetry` (export contains only allowlisted/redacted attributes, cardinality stays under declared budget and retains 100% of configured critical failures with signed policy receipt; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/operations/telemetry/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-023`, `TRUST-018`, `OPS-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_OPS_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_OPS_002`; `RACE=TestTodo_OPS_002_Race`; `SECURITY=TestTodo_OPS_002_Security`; `MUTATION=TestTodo_OPS_002_Mutation`.
  - **RED:** fixture emits raw salary/medical/bank/case/prompt/payload data, unbounded worker label or drops required security/financial failure trace.
  - **GREEN:** export contains only allowlisted/redacted attributes, cardinality stays under declared budget and retains `100%` of configured critical failures with signed policy receipt.
  - **REFACTOR:** high-cardinality correlation belongs in traces/log indexes, not metrics labels.
  - **Refs:** [Telemetry governance](data/models/operations-production.md), [classification and DLP](specs/data-classification-and-dlp.md).

- [x] `OPS-003` **[GATE_A][TERRA] Monitor the telemetry pipeline as a dependency.**
  - **Evidence (2026-09-05):** `TestTodo_OPS_003` in `internal/operations/telemetryhealth` (pipeline watermark/queue/drop/export status yields UNKNOWN evidence quality and opens a bounded incident after the configured interval; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/operations/telemetryhealth/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `OPS-002`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_OPS_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_OPS_003`; `PROPERTY=TestTodo_OPS_003_Property`; `GOLDEN=TestTodo_OPS_003_Golden`; `RACE=TestTodo_OPS_003_Race`.
  - **RED:** collector/exporter loss or sampling-policy failure still reports product SLO `HEALTHY`.
  - **GREEN:** pipeline watermark/queue/drop/export status yields `UNKNOWN` evidence quality and opens a bounded incident after the configured interval.
  - **REFACTOR:** absence of telemetry is never interpreted as absence of failure.
  - **Refs:** [Telemetry health](data/models/operations-production.md), [incident model](specs/incident-management.md).

- [x] `OPS-004` **[GATE_A][SOL_HIGH] Implement the canonical incident state machine.**
  - **Evidence (2026-09-05):** `TestTodo_OPS_004` in `internal/operations/incidentstate` (valid transitions append evidence from DETECTED through REVIEWED; invalid transition returns INCIDENT_TRANSITION_INVALID with no state change; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/operations/incidentstate/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `OPS-001`, `DATA-003`, `WORK-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_OPS_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_OPS_004`; `PROPERTY=TestTodo_OPS_004_Property`; `GOLDEN=TestTodo_OPS_004_Golden`.
  - **RED:** incident closes without bounded affected set, owner, mitigation, reconciliation/repair link, monitoring window or review.
  - **GREEN:** valid transitions append evidence from `DETECTED` through `REVIEWED`; invalid transition returns `INCIDENT_TRANSITION_INVALID` with no state change.
  - **REFACTOR:** technical alerts aggregate into business incidents by correlation and affected set.
  - **Refs:** [Incident management](specs/incident-management.md), [operations assurance](plan.md#112-operational-readiness).

- [x] `OPS-005` **[GATE_A][SOL_HIGH] Publish tenant-scoped customer advisories from incidents.**
  - **Evidence (2026-09-05):** `TestTodo_OPS_005` in `internal/operations/advisory` (authorized audience receives versioned advisory/status history with delivery evidence and only its scoped affected facts; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/operations/advisory/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `OPS-004`, `MSG-003`, `TRUST-011`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_OPS_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_OPS_005`; `GOLDEN=TestTodo_OPS_005_Golden`; `SECURITY=TestTodo_OPS_005_Security`.
  - **RED:** advisory exposes another tenant/worker/system detail or reports resolved before incident monitoring passes.
  - **GREEN:** authorized audience receives versioned advisory/status history with delivery evidence and only its scoped affected facts.
  - **REFACTOR:** advisory wording is separate from internal forensic detail.
  - **Refs:** [Customer communications](specs/incident-management.md), [messaging](specs/messaging-and-notification-plane.md).

- [x] `ADMISSION-001` **[GATE_B][SOL_HIGH] Make deterministic tenant-aware admission decisions.**
  - **Evidence (2026-09-05):** `TestTodo_ADMISSION_001` in `internal/operations/admission` (return ADMIT, QUEUE, DEFER, DEGRADE, or REJECT with tenant/cell/criticality/quota/retry-budget evidence; same snapshot returns same decision; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/operations/admission/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TENANT-001`, `OPS-001`, `WF-COMP-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.OPERATIONS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ADMISSION_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ADMISSION_001`; `GOLDEN=TestTodo_ADMISSION_001_Golden`; `RACE=TestTodo_ADMISSION_001_Race`; `FAULT=TestTodo_ADMISSION_001_Fault`; `SECURITY=TestTodo_ADMISSION_001_Security`; `BENCHMARK=BenchmarkTodo_ADMISSION_001`.
  - **RED:** unknown quota, stale placement, exhausted capacity or noisy tenant admits best-effort work that starves P0 payroll/access-revocation reservation.
  - **GREEN:** return `ADMIT`, `QUEUE`, `DEFER`, `DEGRADE`, or `REJECT` with tenant/cell/criticality/quota/retry-budget evidence; same snapshot returns same decision.
  - **REFACTOR:** reserved capacity is explicit and cannot exceed physical/provider limits.
  - **Refs:** [Admission and criticality](data/models/operations-production.md), [production architecture](plan.md).

- [x] `ADMISSION-002` **[GATE_B][SOL_HIGH] Propagate backpressure and enforce retry budgets.**
  - **Evidence (2026-09-05):** `TestTodo_ADMISSION_002` in `internal/operations/admission` (upstream scheduling slows at declared watermark; logical operation consumes bounded retry tokens and exhaustion returns RETRY_BUDGET_EXHAUSTED once with repair/escalation route; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/operations/admission/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ADMISSION-001`, `OPS-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.OPERATIONS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ADMISSION_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ADMISSION_002`; `RACE=TestTodo_ADMISSION_002_Race`; `INTEGRATION=TestTodo_ADMISSION_002_Integration`; `FAULT=TestTodo_ADMISSION_002_Fault`; `BENCHMARK=BenchmarkTodo_ADMISSION_002`.
  - **RED:** downstream slow/429/503 state causes unbounded queue or retry amplification across workflow, connector and message workers.
  - **GREEN:** upstream scheduling slows at declared watermark; logical operation consumes bounded retry tokens and exhaustion returns `RETRY_BUDGET_EXHAUSTED` once with repair/escalation route.
  - **REFACTOR:** retry accounting is per logical operation, tenant and dependency.
  - **Refs:** [Backpressure](data/models/operations-production.md), [connector capacity](specs/integration-platform.md).

- [ ] `OPS-006` **[GATE_B][SOL_HIGH] Drain a workload at safe points with epoch fencing.**
  - **Depends:** `ADMISSION-001`, `WF-RUN-012`, `TENANT-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_OPS_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_OPS_006`; `RACE=TestTodo_OPS_006_Race`; `FAULT=TestTodo_OPS_006_Fault`; `SECURITY=TestTodo_OPS_006_Security`; `MUTATION=TestTodo_OPS_006_Mutation`.
  - **RED:** new lease starts after drain fence, in-flight unsafe region is called paused or resume accepts stale worker epoch.
  - **GREEN:** drain rejects new leases, reaches exact safe/ambiguous counts, reconciles in-flight effects and resumes only under a new epoch.
  - **REFACTOR:** node, shard, cell and capability drains share one contract.
  - **Refs:** [Drain coordination](data/models/operations-production.md), [safe points](specs/workflow-runtime.md).

- [x] `RECOVERY-001` **[GATE_A][SOL_HIGH] Define an authoritative-versus-rebuildable recovery matrix.**
  - **Evidence (2026-09-05):** `TestTodo_RECOVERY_001` in `internal/operations/recovery` (ledger/artifacts/config/keys/runtime/outbox restore and projection/search/analytics/cache rebuild contracts all resolve with owner and numeric target; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/operations/recovery/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DATA-001`, `DATA-013`, `DATA-014`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_RECOVERY_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_RECOVERY_001`; `FAULT=TestTodo_RECOVERY_001_Fault`; `RECOVERY=TestTodo_RECOVERY_001_Recovery`; `MUTATION=TestTodo_RECOVERY_001_Mutation`.
  - **RED:** plane lacks authority class, backup/rebuild method, RPO, RTO, dependency order or semantic verification.
  - **GREEN:** ledger/artifacts/config/keys/runtime/outbox restore and projection/search/analytics/cache rebuild contracts all resolve with owner and numeric target.
  - **REFACTOR:** `RPO=N/A` is valid only for demonstrably replayable data.
  - **Refs:** [Recovery matrix](data/models/operations-production.md), [data planes](plan.md).

- [x] `RECOVERY-002` **[GATE_A][SOL_HIGH] Create encrypted immutable backup sets and verify readability.**
  - **Evidence (2026-09-05):** `TestTodo_RECOVERY_002` in `internal/operations/recovery` (BackupSet inventory/hash/signature/key/retention verifies and a random block sample plus manifest traversal returns VERIFIED; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/operations/recovery/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `RECOVERY-001`, `TRUST-019`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_RECOVERY_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_RECOVERY_002`; `GOLDEN=TestTodo_RECOVERY_002_Golden`; `FAULT=TestTodo_RECOVERY_002_Fault`; `SECURITY=TestTodo_RECOVERY_002_Security`; `RECOVERY=TestTodo_RECOVERY_002_Recovery`.
  - **RED:** missing segment/key reference/manifest/signature, mutable repository, corrupt block or unbounded tenant scope reports backup success.
  - **GREEN:** `BackupSet` inventory/hash/signature/key/retention verifies and a random block sample plus manifest traversal returns `VERIFIED`.
  - **REFACTOR:** backup job success is not restore success.
  - **Refs:** [Backup plane](data/models/operations-production.md), [cryptographic integrity](specs/transaction-ledger-reconciliation-and-repair.md).

- [ ] `RECOVERY-003` **[GATE_B][SOL_HIGH] Restore the pilot into an isolated recovery environment.**
  - **Depends:** `RECOVERY-002`, `PRIV-004`, `WF-RUN-002`, `DATA-013`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_RECOVERY_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_RECOVERY_003`; `GOLDEN=TestTodo_RECOVERY_003_Golden`; `RACE=TestTodo_RECOVERY_003_Race`; `FAULT=TestTodo_RECOVERY_003_Fault`; `RECOVERY=TestTodo_RECOVERY_003_Recovery`; `MUTATION=TestTodo_RECOVERY_003_Mutation`.
  - **RED:** restore loses timer/signal/frontier/outbox/idempotency state, violates ledger/invariant checks, resurrects restricted data or can reach production destinations.
  - **GREEN:** isolated run reports measured RPO/RTO, exact restored/rebuilt counts, valid hashes/invariants, applied tombstones/holds and zero production effects.
  - **REFACTOR:** restore validation uses the reference conformance suite.
  - **Refs:** [Cyber recovery](data/models/operations-production.md), [workflow recovery](specs/workflow-runtime.md).

- [ ] `RECOVERY-004` **[GATE_C][SOL_HIGH] Execute failover, failback and recovery game-day scenarios.**
  - **Depends:** `RECOVERY-003`, `TENANT-005`, `OPS-004`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_RECOVERY_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_RECOVERY_004`; `INTEGRATION=TestTodo_RECOVERY_004_Integration`; `FAULT=TestTodo_RECOVERY_004_Fault`; `SECURITY=TestTodo_RECOVERY_004_Security`; `RECOVERY=TestTodo_RECOVERY_004_Recovery`; `MUTATION=TestTodo_RECOVERY_004_Mutation`; `RACE=TestTodo_RECOVERY_004_Race`.
  - **RED:** lost database/region/ledger shard/signing key/admin/IdP/provider scenario lacks decision owner, safe degraded mode, cutover/failback fence or reconciled outcome.
  - **GREEN:** each scenario meets declared RPO/RTO or produces deterministic failed gate, with incident, customer advisory, repair and post-game review evidence.
  - **REFACTOR:** failure injection cannot escape test/recovery cells.
  - **Refs:** [Recovery game days](data/models/operations-production.md), [business continuity](plan.md).

- [x] `TIME-001` **[GATE_A][SOL_HIGH] Establish trusted-time health and temporal evidence.**
  - **Depends:** `MODEL-003`, `TRUST-003`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_TIME_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TIME_001`; `PROPERTY=TestTodo_TIME_001_Property`; `FUZZ=FuzzTodo_TIME_001`; `MUTATION=TestTodo_TIME_001_Mutation`; `SECURITY=TestTodo_TIME_001_Security`.
  - **RED:** untrusted source, skew beyond threshold, backward jump or unknown uncertainty permits approval, ledger signature, cutoff or token-expiry decision.
  - **GREEN:** sensitive operation binds trusted source/offset/uncertainty receipt or returns `TIME_UNTRUSTED`; monotonic sequencing remains intact.
  - **REFACTOR:** business effective time remains distinct from host clock time.
  - **Refs:** [Trusted time](data/models/operations-production.md), [canonical time](data/models/wire-contract-primitives.md).
  - **Evidence (2026-09-03):** `TestTodo_TIME_001`, `_Property`, `_Mutation`, `_Security`, `FuzzTodo_TIME_001` in `internal/platform/timeauth` (`Clock`/`FakeClock`, `Monitor` health TRUSTED/DEGRADED/UNTRUSTED from drift samples, temporal `Evidence`, `RequireTrusted` refusing with `ErrTimeUntrusted`); `go test -count=1 ./internal/platform/timeauth/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `CRYPTO-001` **[GATE_B][SOL_HIGH] Prove crypto-agile dual-read/dual-sign migration.**
  - **Evidence (2026-09-05):** `TestTodo_CRYPTO_001`, `TestTodo_CRYPTO_001_{Golden,Property,Security,Recovery}` in `internal/trust/cryptoagile` (AlgorithmSuite registry ACTIVE/DUAL/RETIRED, MigrationPlan refusing overlapping or gapped windows, suite id folded into the signed bytes so a relabeled or stripped envelope fails, DualSigner emits one envelope per live suite, Resume replays only the missing transition evidence after a mid-window crash; CustodyKeySource adapts custody.Provider without raw key material; stdlib ed25519/hmac only); `go test -count=1 ./internal/trust/cryptoagile/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-019`, `TIME-001`, `DATA-004`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_CRYPTO_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CRYPTO_001`; `GOLDEN=TestTodo_CRYPTO_001_Golden`; `INTEGRATION=TestTodo_CRYPTO_001_Integration`; `MUTATION=TestTodo_CRYPTO_001_Mutation`.
  - **RED:** algorithm/version rotation makes historical ledger/document/token/connector evidence unverifiable or accepts below-floor material.
  - **GREEN:** migration fixture verifies old/new signatures during bounded window, emits re-sign/re-encrypt receipts and rejects retired algorithms after cutoff without rewriting history.
  - **REFACTOR:** algorithms are registry configuration, not scattered constants.
  - **Refs:** [Crypto agility](data/models/operations-production.md), [integrity](specs/transaction-ledger-reconciliation-and-repair.md).

- [x] `SUPPLY-001` **[P0][SOL_HIGH] Produce signed SBOM and build provenance for every Go artifact.**
  - **Evidence (2026-09-05):** `TestTodo_SUPPLY_001`, `TestTodo_SUPPLY_001_{Golden,Recovery}` in `tools/policy/provenance` and `TestTodo_SUPPLY_001_GeneratorDefaults` in `tools/policy/provenance/cmd/provgen` (`go run ./tools/policy/provenance/cmd/provgen` builds the hcmnext binary into a temp dir, hashes it and writes `definitions/supply-chain/provenance.json` referencing the checked-in CycloneDX SBOM digest; the checked-in statement verifies against the checked-in SBOM; finished by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/provenance/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TOOL-015`, `TOOL-016`, `TOOL-017`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SUPPLY_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SUPPLY_001`; `GOLDEN=TestTodo_SUPPLY_001_Golden`; `RECOVERY=TestTodo_SUPPLY_001_Recovery`.
  - **RED:** artifact lacks source/dependency/toolchain/builder provenance, contains unknown dependency or signature verification fails.
  - **GREEN:** clean hermetic rebuild yields verified provenance and dependency inventory; production admission accepts only trusted signer/policy and exact artifact digest.
  - **REFACTOR:** container tag is never an authority token.
  - **Refs:** [Supply chain](data/models/operations-production.md), [Go build policy](specs/go-only-technology-constitution.md).

- [x] `SUPPLY-002` **[GATE_A][TERRA] Resolve vulnerability impact through artifacts and tenants.**
  - **Evidence (2026-09-05):** `TestTodo_SUPPLY_002` in `tools/policy/vulnimpact` (vulnerability impact resolution over the checked-in CycloneDX SBOM and an in-memory tenant-to-artifact deployment map: affected modules, artifacts and tenants as typed findings with severity, canonical digest, Explain and a golden over the real SBOM; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/vulnimpact/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SUPPLY-001`, `TENANT-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SUPPLY_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SUPPLY_002`; `GOLDEN=TestTodo_SUPPLY_002_Golden`; `SECURITY=TestTodo_SUPPLY_002_Security`.
  - **RED:** vulnerable module cannot resolve services/artifacts/deployments/cells/tenants/jobs/agents or reports unaffected with incomplete inventory.
  - **GREEN:** query returns complete bounded affected set, confidence/watermark, remediation owner, compensating control and customer-notification decision.
  - **REFACTOR:** disclosure intake and remediation evidence share the same vulnerability identity.
  - **Refs:** [Supply-chain operations](data/models/operations-production.md), [incident management](specs/incident-management.md).

- [x] `SUPPLY-003` **[GATE_C][SOL_HIGH] Capture per-component license identity in the generated SBOM.**
  - **Evidence (2026-09-07):** `TestTodo_SUPPLY_003`, `TestTodo_SUPPLY_003_Golden`, `TestTodo_SUPPLY_003_Integration`, `TestTodo_SUPPLY_003_Mutation`, `TestTodo_SUPPLY_003_Security` in `tools/policy/sbom` (written in c000a21 on 2026-09-06 and never ticked; the named tests prove the GREEN contract: each generated `Component` resolves an SPDX license expression from the module's own `go.mod`/`LICENSE` evidence where declared, marks it `UNKNOWN` rather than guessing where it is not, and `ValidateCompleteness` refuses a document with an unexplained missing license on a component actually vendored into the release); `go test -count=1 ./tools/policy/sbom/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SUPPLY-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SUPPLY_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SUPPLY_003`; `GOLDEN=TestTodo_SUPPLY_003_Golden`; `SECURITY=TestTodo_SUPPLY_003_Security`; `INTEGRATION=TestTodo_SUPPLY_003_Integration`; `MUTATION=TestTodo_SUPPLY_003_Mutation`.
  - **RED:** `tools/policy/sbom`'s CycloneDX document names every dependency's module path, version and content hash but no license identifier, so a licensing question about a shipped artifact has no machine-readable answer.
  - **GREEN:** each generated `Component` resolves an SPDX license expression from the module's own `go.mod`/`LICENSE` evidence where declared, marks it `UNKNOWN` rather than guessing where it is not, and `ValidateCompleteness` refuses a document with an unexplained missing license on a component actually vendored into the release.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §1, E-08](research/security-best-practices-2026.md#1-enterprise-applications-in-general), [NTIA SBOM minimum elements](https://www.ntia.gov/report/2021/minimum-elements-software-bill-materials-sbom). Maps to NTIA SBOM minimum elements (license field); ASVS V14.2.

- [ ] `SUPPLY-004` **[GATE_C][SOL_HIGH] Bind vulnerability severity to a patch SLA clock and emergency-change/rollback path.**
  - **Depends:** `SUPPLY-002`, `CICD-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SUPPLY_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SUPPLY_004`; `GOLDEN=TestTodo_SUPPLY_004_Golden`; `SECURITY=TestTodo_SUPPLY_004_Security`; `INTEGRATION=TestTodo_SUPPLY_004_Integration`; `FAULT=TestTodo_SUPPLY_004_Fault`; `RECOVERY=TestTodo_SUPPLY_004_Recovery`; `MUTATION=TestTodo_SUPPLY_004_Mutation`.
  - **RED:** `SUPPLY-002`'s impact `Report` names severity and a remediation owner, but no SLA clock starts, no compensating-control decision is recorded, and there is no emergency-change path distinct from ordinary `CICD-004` admission for a critical, actively-exploited finding.
  - **GREEN:** each `Report` severity resolves a remediation-SLA deadline; an unresolved deadline escalates; a critical finding may take an emergency-change path that still passes `releaseadmission` but records the compensating control, approver and a rollback plan verified before the emergency change ships.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §1, E-18](research/security-best-practices-2026.md#1-enterprise-applications-in-general), [CIS Controls v8.1 list](https://www.cisecurity.org/controls/cis-controls-list), [OWASP Top 10:2025](https://owasp.org/Top10/). Maps to CIS Control 7 (Continuous Vulnerability Management); NIST 800-53 SI-2/CM-3.

## 17. Experience, API and accessibility

> **Disposition (2026-09-02):** P1A: the Promotion analysis/proposal workspace on GWC if `UX-QUAL-001` passes, otherwise Go server-rendered HTML; field masking before render; keyboard and screen-reader pass on that workspace. Full-process WCAG 2.2 AA evidence and accessible documents are Gate C. Generated-page and brand-pack items are Phase 2.

- [x] `UX-QUAL-001` **[GATE_A][SOL_HIGH] Run the UI qualification fixture and select GWC or the Go SSR fallback.**
  - **Depends:** `UX-001`, `TOOL-008`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=decide the workspace renderer on evidence before P1B, so accessibility gates are not blocked on an unqualified library`.
  - **TEST:** `TestWorkspaceQualificationFixture`.
  - **TEST MATRIX:** `PRIMARY=TestWorkspaceQualificationFixture`; `BROWSER=TestTodo_UX_QUAL_001_Browser`; `SECURITY=TestTodo_UX_QUAL_001_Security`; `CONFORMANCE=TestTodo_UX_QUAL_001_Conformance`.
  - **RED:** the Promotion request, approval and timeline workspace fails keyboard-only completion, a screen-reader pass (NVDA on Windows, VoiceOver on macOS), WCAG 2.2 AA contrast and reflow, or renders a field the server masked.
  - **GREEN:** either GWC passes and is pinned as the renderer, or the failure is recorded and the workspace ships as Go server-rendered HTML with progressive enhancement; the decision and evidence are in the P1B manifest. The browser test runner may be Node-based in the development toolchain.
  - **REFACTOR:** the workspace contract (`UX-001`) is renderer-independent; only the renderer changes.
  - **Refs:** [GWC qualification fixture](specs/go-only-technology-constitution.md#gwc--gowebcomponents-preferred-ui), [experience slice](execution-plan.md#experience-and-branding-slice).
  - **Evidence (2026-09-03):** `TestWorkspaceQualificationFixture`, `TestTodo_UX_QUAL_001_{Browser,Security,Conformance}` in `tools/uxqual/qual` (keyboard order, landmarks/labels/live region, WCAG 2.2 AA contrast from shared tokens, 320px reflow, masked-field never rendered, native and `GOOS=js GOARCH=wasm` builds). Decision: **GoWebComponents v5.0.1 selected**; the Go html/template SSR fallback stays green under the same fixture; record `definitions/ux/workspace-renderer-decision.yaml` (review-by 2026-10-03). Playwright pass done 2026-09-03 (Playwright 1.60.0, Chromium 148.0.7778.96, `npm run test:uxqual-browser` 10/10: both renderers pass all five criteria; recorded in the decision record). Pending: manual NVDA/VoiceOver pass; branch plan-revision-2026-09-02; `go test -count=1 ./tools/uxqual/qual/...` PASS on windows/arm64 (Go 1.26.3).

- [x] `UX-001` **[GATE_A][SOL_HIGH] Define a server-resolved Promotion workspace contract.**
  - **Depends:** `PROMO-002`, `CAP-003`, `TRUST-011`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose governed intent creation, inspection or consumption without persistence or provider bypass`.
  - **TEST:** `TestTodo_UX_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_UX_001`; `GOLDEN=TestTodo_UX_001_Golden`; `INTEGRATION=TestTodo_UX_001_Integration`; `BROWSER=TestTodo_UX_001_Browser`; `SECURITY=TestTodo_UX_001_Security`.
  - **RED:** client receives unauthorized field/action, invents business rule or channel has different capability semantics.
  - **GREEN:** `PageDefinition` contains only field-masked data, provenance, validation, available actions and capability/version refs; HTTP/gRPC/GWC resolve identical semantic results.
  - **REFACTOR:** UI is a capability client, never a hidden mutation path.
  - **Refs:** [Experience plane](specs/experience-ui-and-branding.md), [intelligence model](data/models/assurance-intelligence-platform.md).
  - **Evidence (2026-09-03):** `TestTodo_UX_001`, `TestTodo_UX_001_Security` in `tools/uxqual/contract` (renderer-independent Promotion workspace contract; unauthorized fields and actions are absent, never blanked); `go test -count=1 ./tools/uxqual/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `UX-002` **[GATE_A][TERRA] Implement the Promotion workspace using GoWebComponents.**
  - **Depends:** `UX-001`, `TOOL-008`, `TOOL-009`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose governed intent creation, inspection or consumption without persistence or provider bypass`.
  - **TEST:** `TestTodo_UX_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_UX_002`; `GOLDEN=TestTodo_UX_002_Golden`; `INTEGRATION=TestTodo_UX_002_Integration`; `BROWSER=TestTodo_UX_002_Browser`.
  - **RED:** read/analyze/draft fixture requires TypeScript/Node, bypasses grpcbridge or renders an unavailable action.
  - **GREEN:** Go-only workspace renders worker snapshot, simulation, proposal, warnings and allowed actions from the server contract; browser fixture contains no unsupported runtime.
  - **REFACTOR:** use GWC primitives without coupling domain rules to component code.
  - **Refs:** [Go-only runtime](plan.md#518-go-core-contract-first-and-open-source-first), [Gate A UX](execution-plan.md#gate-a--paid-design-partner-observation).

- [ ] `UX-003` **[GATE_A][TERRA] Pass the WCAG 2.2 AA pilot release gate.**
  - **Depends:** `UX-002`, `FORM-004`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose governed intent creation, inspection or consumption without persistence or provider bypass`.
  - **TEST:** `TestTodo_UX_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_UX_003`; `RACE=TestTodo_UX_003_Race`; `INTEGRATION=TestTodo_UX_003_Integration`; `BROWSER=TestTodo_UX_003_Browser`.
  - **RED:** keyboard, focus, screen-reader name/state, error association, contrast, 200% zoom, 400% reflow, reduced motion or accessible-auth fixture fails.
  - **GREEN:** automated and named manual scenarios pass; exceptions have owner, severity, workaround and approved expiry evidence.
  - **REFACTOR:** accessibility evidence is attached to release artifact, not a one-time audit.
  - **Refs:** [Accessibility](specs/experience-ui-and-branding.md), [Phase 1 quality](execution-plan.md).

- [x] `UX-004` **[GATE_A][TERRA] Localize and format the pilot workspace without leaking legal context into locale.**
  - **Depends:** `UX-001`, `MODEL-012`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose governed intent creation, inspection or consumption without persistence or provider bypass`.
  - **TEST:** `TestTodo_UX_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_UX_004`; `GOLDEN=TestTodo_UX_004_Golden`; `INTEGRATION=TestTodo_UX_004_Integration`; `FAULT=TestTodo_UX_004_Fault`; `SECURITY=TestTodo_UX_004_Security`; `BROWSER=TestTodo_UX_004_Browser`; `FUZZ=FuzzTodo_UX_004`.
  - **RED:** unsupported locale silently changes jurisdiction, currency/date/number parsing is ambiguous or untranslated key is hidden.
  - **GREEN:** locale fallback is explicit, round-trip formats match fixtures, missing translation is reported and `LocaleContext` contains no legal jurisdiction authority.
  - **REFACTOR:** legal resolution remains in `LegalContext`.
  - **Refs:** [Locale semantics](data/models/wire-contract-primitives.md), [globalization](plan.md).
  - **Evidence (2026-09-03):** `TestTodo_UX_004`, `TestTodo_UX_004_Golden`, `TestTodo_UX_004_Integration`, `TestTodo_UX_004_Fault`, `TestTodo_UX_004_Security`, `TestTodo_UX_004_Browser` and `FuzzTodo_UX_004` in `internal/humanwork/workspace` and `test/workspace`; strict `en-US`/`de-DE` date, currency and number round trips make unsupported-locale fallback and missing translations visible, canonicalize values before intent comparison, and keep `LocaleContext` limited to requested/resolved/fallback locale data with no legal-jurisdiction authority. `go test -count=1 ./internal/humanwork/workspace ./test/workspace`, targeted browser/integration runs, a two-second fuzz run and `go vet` PASS on windows/arm64 (Go 1.26.3); refactored; branch plan-revision-2026-09-02.

- [x] `UX-005` **[GATE_B][SOL_HIGH] Make UI error, ambiguity and recovery states truthful.**
  - **Evidence (2026-09-06):** `TestTodo_UX_005` in `internal/experience/recovery`; `go test -count=1 ./internal/experience/recovery/` PASS on windows/arm64 (Go 1.26.3); evidence recorded by the orchestrator after the front-end session ticked the entry without one.
  - **Depends:** `UX-002`, `TX-010`, `INTG-014`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose governed intent creation, inspection or consumption without persistence or provider bypass`.
  - **TEST:** `TestTodo_UX_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_UX_005`; `INTEGRATION=TestTodo_UX_005_Integration`; `FAULT=TestTodo_UX_005_Fault`; `BROWSER=TestTodo_UX_005_Browser`; `MUTATION=TestTodo_UX_005_Mutation`.
  - **RED:** timeout/partial/degraded/repair-required result is presented as generic failure or success and retry can duplicate effect.
  - **GREEN:** UI maps typed result to exact state, safe next actions and correlation/evidence reference; duplicate click returns same logical transaction.
  - **REFACTOR:** client never guesses provider outcome.
  - **Refs:** [Multidimensional completion](plan.md#63-change-request-lifecycle), [integration ambiguity](specs/integration-platform.md).

- [x] `UX-006` **[PHASE_2][SOL_HIGH] Generate universal and contextual action discovery from the feature-intent registry.**
  - **Depends:** `INTENT-010`, `CAP-003`, `TRUST-011`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose governed intent creation, inspection or consumption without persistence or provider bypass`.
  - **TEST:** `TestUniversalActionDiscoveryRejectsUnavailableOrDivergentAction`.
  - **TEST MATRIX:** `PRIMARY=TestUniversalActionDiscoveryRejectsUnavailableOrDivergentAction`; `GOLDEN=TestTodo_UX_006_Golden`; `INTEGRATION=TestTodo_UX_006_Integration`; `BROWSER=TestTodo_UX_006_Browser`; `MUTATION=TestTodo_UX_006_Mutation`; `SECURITY=TestTodo_UX_006_Security`.
  - **RED:** `TestUniversalActionDiscoveryRejectsUnavailableOrDivergentAction` shows a global action bar, command palette, worker/position/org/candidate/case/payroll menu, bulk toolbar, mobile or kiosk action that lacks a published intent/capability, is unauthorized for current subject/context or invokes different semantics by surface.
  - **GREEN:** one server-resolved action descriptor drives global/contextual discovery with feature/intent/capability versions, permitted subject types, required inputs, risk/effect/simulation availability and redaction-safe unavailability reason; all surfaces invoke the same route.
  - **REFACTOR:** UI labels, favorites and recent actions are projections over stable semantic IDs, not independent command registries.
  - **Refs:** [Experience plane](specs/experience-ui-and-branding.md), [feature-intent conformance](#6-businessintent-capabilities-governance-and-transaction-integrity).

- [x] `UX-007` **[PHASE_2][SOL_HIGH] Build one governed Intent Center for drafts, tasks, approvals, messages and lifecycle inspection.**
  - **Depends:** `INTENT-014`, `INTENT-021`, `WORK-001`, `MSG-001`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose governed intent creation, inspection or consumption without persistence or provider bypass`.
  - **TEST:** `TestIntentCenterPreservesAuthorityAndLifecycleTruth`.
  - **TEST MATRIX:** `PRIMARY=TestIntentCenterPreservesAuthorityAndLifecycleTruth`; `INTEGRATION=TestTodo_UX_007_Integration`; `FAULT=TestTodo_UX_007_Fault`; `SECURITY=TestTodo_UX_007_Security`; `BROWSER=TestTodo_UX_007_Browser`; `MUTATION=TestTodo_UX_007_Mutation`.
  - **RED:** `TestIntentCenterPreservesAuthorityAndLifecycleTruth` mixes draft/task/message/approval ownership, presents stale or generic status, opens deep link after authority loss, exposes restricted evidence or allows retry/cancel/correct outside permitted capability.
  - **GREEN:** Draft Center, Task Center, Approval Inbox, Notification/Secure Inbox, intent timeline/inspector and deep links share exact intent/relationship/proposal/work-item/message references, current authorization, multidimensional state and safe next actions.
  - **REFACTOR:** center composes owner APIs and projections; it owns no workflow, approval, messaging or intent truth.
  - **Refs:** [Human work](specs/human-work-forms-and-rules.md), [Messaging](specs/messaging-and-notification-plane.md).

- [x] `UX-008` **[PHASE_2][TERRA] Prove semantic equivalence for desktop, mobile, kiosk and accessibility-assisted intent creation.**
  - **Depends:** `UX-003`, `UX-006`, `FORM-004`, `INTENT-013`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose governed intent creation, inspection or consumption without persistence or provider bypass`.
  - **TEST:** `TestIntentCreationChannelAccessibilityParity`.
  - **TEST MATRIX:** `PRIMARY=TestIntentCreationChannelAccessibilityParity`; `GOLDEN=TestTodo_UX_008_Golden`; `INTEGRATION=TestTodo_UX_008_Integration`; `SECURITY=TestTodo_UX_008_Security`; `CONFORMANCE=TestTodo_UX_008_Conformance`; `BROWSER=TestTodo_UX_008_Browser`.
  - **RED:** `TestIntentCreationChannelAccessibilityParity` finds a channel with weaker validation/governance, missing confirmation/simulation, inaccessible required input, QR/short-link authority token, or different request digest for equivalent typed input.
  - **GREEN:** each enabled channel produces the same normalized intent payload/result/error and trusted context, with accessibility/assistance adaptations and constrained links preserving authority, privacy and evidence.
  - **REFACTOR:** channel-specific presentation never forks business rules or intent definitions.
  - **Refs:** [Accessibility](specs/experience-ui-and-branding.md), [grpcbridge parity](specs/go-only-technology-constitution.md).

- [x] `UX-009` **[GATE_B][SOL_HIGH] Drive one promotion journey through a GWC page over the executable engine.**
  - **Evidence (2026-09-05):** `TestJourneyCreatedWorkerCompletesTheWholePromotion`, `TestJourneyDecideApproveCompletesWithOneGovernedWrite`, `TestJourneyDecideRejectReachesTheRejectedTerminal`, `TestJourneyExecuteIsRefusedWithoutTheExecutionRole`, `TestJourneyCreateWorkerRequiresTheOperatorRole` in `test/bootstrap`; `TestTunnelServesAServerStreamingRPC`, `TestTunnelUpgradeRefusesACrossOriginBrowser` in `test/tunnel`; `TestWatchJourneyEmitsOneMessagePerChange` in `internal/transport/journey`; `TestStreamInterceptorRejectsAnUnauthenticatedStream` in `internal/transport/grpcserver`; 110 renderer tests in `tools/uxqual/render/journey` (AA contrast, landmarks, no script/asset, forms only) and 89 client tests in `tools/uxqual/journeyclient`; migration 00023 `journey_worker` registered, P1A manifest re-signed. Browser run 2026-09-05 06:5x UTC on the live cell: `/workspace/journey` (GWC WASM over GoGRPCBridge at `/workspace/grpc`) created worker Priya Raman, selected her, proposed OPS-HRBP2/P2 → OPS-HRBP3/P3, executed under authority, approved as `principal:promotion-approver`, ledger fact `hcmnext.workflow.PromotionOutcome/v2` shown by the WatchJourney stream. `go test -count=1 ./test/bootstrap/ ./test/tunnel/ ./test/workflow/ ./tools/planning/boundarytests/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02. Deferred: PayBand/Budget gauges receive no wire data yet; the 24.7 MB bundle size; a browser Playwright matrix (BROWSER) is manual so far.
  - **Depends:** `UX-001`, `UX-QUAL-001`, `WF-RUN-030`, `WORK-010`, `ADMIN-008`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.PEOPLE,BI.REWARDS; DIRECT=PromoteWorker; WHY=prove one governed intent can be proposed, executed under the P1B authority gate, decided by its routed approver and recorded as a ledger fact from a human-facing page, with no second path to any answer`.
  - **TEST:** `TestJourneyDrivesPromotionFromProposalToRecordedOutcome`.
  - **TEST MATRIX:** `PRIMARY=TestJourneyDrivesPromotionFromProposalToRecordedOutcome`; `GOLDEN=TestTodo_UX_009_Golden`; `INTEGRATION=TestTodo_UX_009_Integration`; `SECURITY=TestTodo_UX_009_Security`; `BROWSER=TestTodo_UX_009_Browser`.
  - **RED:** the page reaches an answer the intent service, the WorkItem store or the driver would refuse (a stage the engine is not at, an approver the assignment does not authorize, a caller without the operator role, a stale proposal digest), shows a stage the durable rows do not support, or renders with a script, external asset or an untokenized colour pair below WCAG 2.2 AA.
  - **GREEN:** `/workspace/journey` lists promote_worker intents from the intent service, proposes through CreateIntent + SimulateIntent, executes through ExecuteIntent behind the authority gate, decides through the WorkItem claim/complete and the driver's Resume as the routed approver, and shows the instance, node executions, work items, transitions, evidence ids and the one ledger fact from their own tables; the GWC tree renders to a strict-CSP document with forms only.
  - **REFACTOR:** the renderer (tools/uxqual/render/journey) knows no routes or engine; the handler projects port answers into the renderer contract; the engine adapter in internal/intent/app owns no workflow or approval truth.
  - **Refs:** [experience slice](execution-plan.md#experience-and-branding-slice), [Promotion user flow](user-flows/reference/promote-into-management.md), [executable prototype devlog](devlog/2026-09-03-executable-prototype.md).

- [x] `UXFLOW-001` **[P0][SOL_HIGH] Define the machine-readable UserFlowRecord and stage vocabulary.**
  - **Evidence (2026-09-06):** `TestUserFlowRecordRejectsMissingParticipantStateRecoveryOrSemanticReference` in `internal/experience/userflow`; `go test -count=1 ./internal/experience/userflow/` PASS on windows/arm64 (Go 1.26.3); evidence recorded by the orchestrator after the front-end session ticked the entry without one.
  - **Depends:** `UX-001`, `SLICE-001`, `WF-DISC-005`, `MODEL-009`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=describe every participant journey with exact semantic references, states, alternatives, accessibility and test hooks`.
  - **TEST:** `TestUserFlowRecordRejectsMissingParticipantStateRecoveryOrSemanticReference`.
  - **TEST MATRIX:** `PRIMARY=TestUserFlowRecordRejectsMissingParticipantStateRecoveryOrSemanticReference`; `PROPERTY=TestTodo_UXFLOW_001_Property`; `GOLDEN=TestTodo_UXFLOW_001_Golden`; `SECURITY=TestTodo_UXFLOW_001_Security`; `CONFORMANCE=TestTodo_UXFLOW_001_Conformance`; `MUTATION=TestTodo_UXFLOW_001_Mutation`.
  - **RED:** flow validates without stable flow/version/owner/job/intent references, participant and decision rights, entry/resume paths, requested-versus-resolved data, ordered stages, state matrix, failure/recovery, completion, accessibility/localization/privacy, scenarios/todos/evidence or typed `NOT_APPLICABLE` reason.
  - **GREEN:** SchemaFlux contract requires every field, stage references one semantic action/result and exact participant-visible state, unknown blocks `FLOW_CONTRACTED`, and generated Go/Protobuf/docs share one canonical digest.
  - **REFACTOR:** flow prose and diagrams are generated views over one record; they never become a parallel runtime or business-rule registry.
  - **Refs:** [user-flow record](user-flows/README.md#userflowrecord), [experience contract](specs/experience-ui-and-branding.md), [vertical-slice record](workflows/vertical-slices/README.md#complete-vertical-slice-record).

- [x] `UXFLOW-002` **[P0][SOL_HIGH] Resolve participants, representation, delegation and decision rights per flow stage.**
  - **Evidence (2026-09-06):** `TestUserFlowParticipantResolutionNeverInfersAuthorityFromPersonaOrRepresentation` in `internal/experience/participants`; `go test -count=1 ./internal/experience/participants/` PASS on windows/arm64 (Go 1.26.3); evidence recorded by the orchestrator after the front-end session ticked the entry without one.
  - **Depends:** `UXFLOW-001`, `TRUST-010`, `TRUST-013`, `TRUST-014`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=prevent persona labels, act-as modes and assisted use from silently expanding authority or disclosure`.
  - **TEST:** `TestUserFlowParticipantResolutionNeverInfersAuthorityFromPersonaOrRepresentation`.
  - **TEST MATRIX:** `PRIMARY=TestUserFlowParticipantResolutionNeverInfersAuthorityFromPersonaOrRepresentation`; `PROPERTY=TestTodo_UXFLOW_002_Property`; `GOLDEN=TestTodo_UXFLOW_002_Golden`; `SECURITY=TestTodo_UXFLOW_002_Security`; `CONFORMANCE=TestTodo_UXFLOW_002_Conformance`; `MUTATION=TestTodo_UXFLOW_002_Mutation`.
  - **RED:** manager/persona/route grants visibility or decision right, delegate inherits full principal scope, interpreter becomes subject, support view becomes impersonation, recused participant retains evidence or flow omits on-behalf-of and representation evidence.
  - **GREEN:** each stage resolves actual participant, subject, relationship, representation/delegation, assurance, purpose, allowed view/actions/decision and expiry from current governance; denial is non-disclosing and assisted actions preserve attributed evidence.
  - **REFACTOR:** flows consume PrincipalContext/governance receipts; they never define roles or authorization rules.
  - **Refs:** [participant vocabulary](user-flows/README.md#participant-vocabulary), [AuthZ](specs/organization-scope-and-authz.md), [human work](specs/human-work-forms-and-rules.md).

- [x] `UXFLOW-003` **[P0][SOL_HIGH] Give every accepted BusinessIntent an explicit user-flow disposition.**
  - **Evidence (2026-09-06):** `TestBusinessIntentUserFlowDispositionIsCompleteUniqueAndExposureSafe` in `internal/experience/disposition`; `go test -count=1 ./internal/experience/disposition/` PASS on windows/arm64 (Go 1.26.3); evidence recorded by the orchestrator after the front-end session ticked the entry without one.
  - **Depends:** `UXFLOW-001`, `SLICE-003`, `INTENT-010`, `UX-006`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=distinguish participant-facing roots and children from system-only, admin-only and no-flow intents without inventing public UI`.
  - **TEST:** `TestBusinessIntentUserFlowDispositionIsCompleteUniqueAndExposureSafe`.
  - **TEST MATRIX:** `PRIMARY=TestBusinessIntentUserFlowDispositionIsCompleteUniqueAndExposureSafe`; `PROPERTY=TestTodo_UXFLOW_003_Property`; `GOLDEN=TestTodo_UXFLOW_003_Golden`; `SECURITY=TestTodo_UXFLOW_003_Security`; `CONFORMANCE=TestTodo_UXFLOW_003_Conformance`; `MUTATION=TestTodo_UXFLOW_003_Mutation`.
  - **RED:** accepted intent lacks disposition, candidate display name is treated as stable identity, system trigger gains human action, child/internal intent is independently discoverable, material background action has no visible parent status or one intent maps to conflicting flows without participant/context qualification.
  - **GREEN:** registry assigns reviewed `PARTICIPANT_ROOT|PARTICIPANT_CHILD|VISIBLE_SYSTEM_STAGE|ADMIN_OPERATOR|NO_USER_FLOW(reason)` by stable definition and participant/context, links archetype/delta and reports baseline, extension and source-unbound denominators separately.
  - **REFACTOR:** generate action and flow reverse indexes from the intent registry; display labels remain localized projections.
  - **Refs:** [flow catalog](user-flows/catalog.md), [BusinessIntent catalog](specs/business-intent-catalog.md), [action discovery](#17-experience-api-and-accessibility).

- [x] `UXFLOW-004` **[P0][SOL_HIGH] Compile truthful participant-facing state and available actions from multidimensional intent state.**
  - **Evidence (2026-09-06):** `TestFlowStatePresentationReturnsExactTruthfulStatusAndSafeActions` in `internal/experience/presentation`; `go test -count=1 ./internal/experience/presentation/` PASS on windows/arm64 (Go 1.26.3); evidence recorded by the orchestrator after the front-end session ticked the entry without one.
  - **Depends:** `UXFLOW-001`, `INTENT-021`, `UX-005`, `UX-007`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=ensure every flow renders loading, waiting, stale, partial, unknown, repair and closure without flattening business truth`.
  - **TEST:** `TestFlowStatePresentationReturnsExactTruthfulStatusAndSafeActions`.
  - **TEST MATRIX:** `PRIMARY=TestFlowStatePresentationReturnsExactTruthfulStatusAndSafeActions`; `PROPERTY=TestTodo_UXFLOW_004_Property`; `GOLDEN=TestTodo_UXFLOW_004_Golden`; `FAULT=TestTodo_UXFLOW_004_Fault`; `SECURITY=TestTodo_UXFLOW_004_Security`; `CONFORMANCE=TestTodo_UXFLOW_004_Conformance`; `BROWSER=TestTodo_UXFLOW_004_Browser`; `MUTATION=TestTodo_UXFLOW_004_Mutation`.
  - **RED:** runtime completion displays full success while external consistency/obligation is open, timeout becomes generic failure, ambiguity offers blind retry, stale action remains enabled, empty/denied state leaks existence or completed/cancelled/corrected history is overwritten.
  - **GREEN:** resolver maps exact runtime/business/external/reconciliation/obligation/operational dimensions plus current authorization/freshness to localized state, evidence refs and only safe actions for every required state matrix row.
  - **REFACTOR:** one server-owned state presentation contract drives page, message and assistive announcements without owning lifecycle truth.
  - **Refs:** [state matrix](user-flows/README.md#state-presentation-matrix), [multidimensional completion](specs/business-intent-and-change-request.md), [truthful UX](specs/experience-ui-and-branding.md).

- [x] `UXFLOW-005` **[P0][SOL_HIGH] Define draft, autosave, validation, simulation, compare and confirmation flow mechanics.**
  - **Evidence (2026-09-06):** `TestDraftToConfirmationPreservesInputSeparatesTruthAndBindsExactProposalDigest` in `internal/experience/draftflow`; `go test -count=1 ./internal/experience/draftflow/` PASS on windows/arm64 (Go 1.26.3); evidence recorded by the orchestrator after the front-end session ticked the entry without one.
  - **Depends:** `UXFLOW-001`, `UXFLOW-004`, `FORM-001`, `INTENT-014`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=let participants safely move from requested input to an exact proposal without client-resolved truth or accidental effects`.
  - **TEST:** `TestDraftToConfirmationPreservesInputSeparatesTruthAndBindsExactProposalDigest`.
  - **TEST MATRIX:** `PRIMARY=TestDraftToConfirmationPreservesInputSeparatesTruthAndBindsExactProposalDigest`; `PROPERTY=TestTodo_UXFLOW_005_Property`; `GOLDEN=TestTodo_UXFLOW_005_Golden`; `RACE=TestTodo_UXFLOW_005_Race`; `FAULT=TestTodo_UXFLOW_005_Fault`; `SECURITY=TestTodo_UXFLOW_005_Security`; `CONFORMANCE=TestTodo_UXFLOW_005_Conformance`; `BROWSER=TestTodo_UXFLOW_005_Browser`; `MUTATION=TestTodo_UXFLOW_005_Mutation`.
  - **RED:** hidden/current-state field is trusted, autosave overwrites concurrent revision, validation loses input/focus, simulation mutates state/effect, masked compare makes material decision, confirmation binds different proposal or duplicate submit creates two intents.
  - **GREEN:** versioned draft stores requested values only, server snapshot resolves truth, errors preserve accessible field/summary state, simulation is zero-effect, compare exposes permitted provenance/uncertainty and confirmation/idempotency bind the exact proposal digest.
  - **REFACTOR:** shared mechanics own revision/conflict/presentation; domain schemas, calculations and materiality remain semantic-owner contracts.
  - **Refs:** [requested input versus truth](user-flows/README.md#requested-input-versus-server-truth), [forms](specs/human-work-forms-and-rules.md), [intent proposals](specs/business-intent-and-change-request.md).

- [x] `UXFLOW-006` **[P0][SOL_HIGH] Preserve safe interruption, deep-link, session-expiry and cross-device resume.**
  - **Evidence (2026-09-06):** `TestFlowResumeReauthorizesAndRestoresOnlySafeCurrentState` in `internal/flow`; `go test -count=1 ./internal/flow/` PASS on windows/arm64 (Go 1.26.3); evidence recorded by the orchestrator after the front-end session ticked the entry without one.
  - **Depends:** `UXFLOW-004`, `UXFLOW-005`, `TRUST-003`, `UX-007`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=make long-running HCM work resumable without embedding authority in URLs, stale pages or device storage`.
  - **TEST:** `TestFlowResumeReauthorizesAndRestoresOnlySafeCurrentState`.
  - **TEST MATRIX:** `PRIMARY=TestFlowResumeReauthorizesAndRestoresOnlySafeCurrentState`; `PROPERTY=TestTodo_UXFLOW_006_Property`; `GOLDEN=TestTodo_UXFLOW_006_Golden`; `RACE=TestTodo_UXFLOW_006_Race`; `FAULT=TestTodo_UXFLOW_006_Fault`; `SECURITY=TestTodo_UXFLOW_006_Security`; `CONFORMANCE=TestTodo_UXFLOW_006_Conformance`; `BROWSER=TestTodo_UXFLOW_006_Browser`; `MUTATION=TestTodo_UXFLOW_006_Mutation`.
  - **RED:** URL/QR/notification contains bearer authority or sensitive state, browser back repeats effect, expired session leaves data visible, resume trusts cached permission/action, two devices overwrite silently, offline draft stores prohibited fields unprotected or authority loss still opens task.
  - **GREEN:** opaque expiring locator resolves only after current identity/AuthZ/purpose, stale tokens/actions are replaced, draft CAS exposes conflict, refresh/back are idempotent, sensitive client state clears on expiry and resume returns exact current masked state plus safe alternatives.
  - **REFACTOR:** deep links locate owner resources; capability gateway reauthorizes every read/action and channel adapters own protected local storage policy.
  - **Refs:** [user-flow record](user-flows/README.md#userflowrecord), [sessions](specs/platform-foundation-gap-closure.md), [Intent Center](#17-experience-api-and-accessibility).

- [x] `UXFLOW-007` **[P0][SOL_HIGH] Design exact participant recovery for partial, unknown, ambiguous and repair-required outcomes.**
  - **Evidence (2026-09-06):** `TestFlowRecoveryNeverOffersActionThatCanDuplicateOrContradictKnownOutcome` in `internal/experience/outcome`; `go test -count=1 ./internal/experience/outcome/` PASS on windows/arm64 (Go 1.26.3); evidence recorded by the orchestrator after the front-end session ticked the entry without one.
  - **Depends:** `UXFLOW-004`, `UX-005`, `INTG-014`, `REPAIR-001`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=prevent unsafe retries and give participants truthful next actions when local business truth and external reality diverge`.
  - **TEST:** `TestFlowRecoveryNeverOffersActionThatCanDuplicateOrContradictKnownOutcome`.
  - **TEST MATRIX:** `PRIMARY=TestFlowRecoveryNeverOffersActionThatCanDuplicateOrContradictKnownOutcome`; `PROPERTY=TestTodo_UXFLOW_007_Property`; `GOLDEN=TestTodo_UXFLOW_007_Golden`; `RACE=TestTodo_UXFLOW_007_Race`; `FAULT=TestTodo_UXFLOW_007_Fault`; `SECURITY=TestTodo_UXFLOW_007_Security`; `CONFORMANCE=TestTodo_UXFLOW_007_Conformance`; `BROWSER=TestTodo_UXFLOW_007_Browser`; `RECOVERY=TestTodo_UXFLOW_007_Recovery`; `MUTATION=TestTodo_UXFLOW_007_Mutation`.
  - **RED:** provider timeout enables ordinary retry after possible application, partial effect is called failure and rolled back, repair reruns original transaction, unauthorized participant sees provider payload, observation staleness is hidden or completion closes while required verification is unknown.
  - **GREEN:** state identifies known/unknown components, last safe operation, observation freshness/deadline, business-versus-external outcome and participant-specific `WAIT|REFRESH|REQUEST_HELP|CANCEL_IF_SAFE|CORRECT|OPEN_REPAIR` actions with no duplicate effect.
  - **REFACTOR:** recovery presentation consumes operation/reconciliation/repair contracts; it does not diagnose provider state itself.
  - **Refs:** [repair user flow](user-flows/reference/dataops-reconcile-and-repair.md), [integration ambiguity](specs/integration-platform.md), [reconciliation and repair](specs/transaction-ledger-reconciliation-and-repair.md).

- [x] `UXFLOW-008` **[P0][SOL_HIGH] Compile accessibility, localization and assisted/manual continuity across complete flows.**
  - **Evidence (2026-09-06):** `TestCompleteUserFlowPreservesSemanticsDeadlinesAndEvidenceAcrossAccessibleRoutes` in `internal/experience/continuity`; `go test -count=1 ./internal/experience/continuity/` PASS on windows/arm64 (Go 1.26.3); evidence recorded by the orchestrator after the front-end session ticked the entry without one.
  - **Depends:** `UXFLOW-001`, `UX-003`, `UX-004`, `FORM-004`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=prove a participant can complete the entire governed process—not isolated pages—across language, disability, accommodation and non-digital routes`.
  - **TEST:** `TestCompleteUserFlowPreservesSemanticsDeadlinesAndEvidenceAcrossAccessibleRoutes`.
  - **TEST MATRIX:** `PRIMARY=TestCompleteUserFlowPreservesSemanticsDeadlinesAndEvidenceAcrossAccessibleRoutes`; `PROPERTY=TestTodo_UXFLOW_008_Property`; `GOLDEN=TestTodo_UXFLOW_008_Golden`; `INTEGRATION=TestTodo_UXFLOW_008_Integration`; `FAULT=TestTodo_UXFLOW_008_Fault`; `SECURITY=TestTodo_UXFLOW_008_Security`; `CONFORMANCE=TestTodo_UXFLOW_008_Conformance`; `BROWSER=TestTodo_UXFLOW_008_Browser`; `MUTATION=TestTodo_UXFLOW_008_Mutation`.
  - **RED:** keyboard/screen-reader/zoom/RTL route loses action/state/error, translation changes legal meaning, interpreter/representative is unattributed, inaccessible upload/signature blocks deadline, fallback channel weakens privacy/identity/validation or manual transcription changes request digest.
  - **GREEN:** flow scenario covers every stage and transition with WCAG evidence, reviewed locale/template versions, semantic announcements/focus, equivalent accommodations and governed assisted/manual receipt preserving subject/actor/meaning/deadline/privacy and normalized intent result.
  - **REFACTOR:** page/component checks feed flow coverage but cannot substitute for multi-step participant testing.
  - **Refs:** [maximal configuration](user-flows/README.md#maximal-configuration), [accessibility contract](specs/experience-ui-and-branding.md#accessibility-contract), [manual continuity](specs/platform-foundation-gap-closure.md#10-accessibility-assurance-platform).

- [x] `UXFLOW-009` **[PHASE_2][SOL_HIGH] Prove cross-channel flow parity for desktop, mobile, kiosk, secure message and assisted routes.**
  - **Evidence (2026-09-06):** `TestUserFlowCrossChannelParityReturnsSameNormalizedIntentAndOutcome` in `internal/experience/channelparity`; `go test -count=1 ./internal/experience/channelparity/` PASS on windows/arm64 (Go 1.26.3); evidence recorded by the orchestrator after the front-end session ticked the entry without one.
  - **Depends:** `UXFLOW-005`, `UXFLOW-006`, `UXFLOW-008`, `UX-008`, `API-001`, `MSG-001`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=ensure surface adaptation never forks BusinessIntent semantics, governance, validation, recovery or evidence`.
  - **TEST:** `TestUserFlowCrossChannelParityReturnsSameNormalizedIntentAndOutcome`.
  - **TEST MATRIX:** `PRIMARY=TestUserFlowCrossChannelParityReturnsSameNormalizedIntentAndOutcome`; `PROPERTY=TestTodo_UXFLOW_009_Property`; `GOLDEN=TestTodo_UXFLOW_009_Golden`; `INTEGRATION=TestTodo_UXFLOW_009_Integration`; `FAULT=TestTodo_UXFLOW_009_Fault`; `SECURITY=TestTodo_UXFLOW_009_Security`; `CONFORMANCE=TestTodo_UXFLOW_009_Conformance`; `BROWSER=TestTodo_UXFLOW_009_Browser`; `MUTATION=TestTodo_UXFLOW_009_Mutation`.
  - **RED:** equivalent input creates different normalized request/digest/result/error, one channel skips simulation/step-up/confirmation, message action remains valid after workflow change, kiosk leaks prior participant, mobile/offline duplicates effect or assisted route loses evidence.
  - **GREEN:** enabled channels invoke identical semantic capability/version with independently trusted context, produce the same normalized intent/result and exact channel-specific presentation/evidence differences; unsupported stage hands off safely without deadline loss.
  - **REFACTOR:** channel renderers and adapters consume generated flow/page/action contracts; no channel-specific domain handler exists.
  - **Refs:** [surface vocabulary](user-flows/README.md#surface-vocabulary), [channel parity](specs/go-only-technology-constitution.md), [messaging](specs/messaging-and-notification-plane.md).

- [x] `UXFLOW-010` **[P0][SOL_HIGH] Generate atomic TDD todos and reverse coverage from user-flow findings to a fixed point.**
  - **Evidence (2026-09-06):** `TestUserFlowGapCompilerEmitsExactOwnerKeyedTodosAndConvergesOnSecondPass` in `tools/planning/userflowgaps`; `go test -count=1 ./tools/planning/userflowgaps/` PASS on windows/arm64 (Go 1.26.3); evidence recorded by the orchestrator after the front-end session ticked the entry without one.
  - **Depends:** `UXFLOW-003`, `UXFLOW-004`, `UXFLOW-005`, `UXFLOW-006`, `UXFLOW-007`, `UXFLOW-008`, `UXFLOW-009`, `SLICE-012`, `GOV-028`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=turn every unresolved surface, state, action, accessibility, continuity and test obligation into deduplicated production work`.
  - **TEST:** `TestUserFlowGapCompilerEmitsExactOwnerKeyedTodosAndConvergesOnSecondPass`.
  - **TEST MATRIX:** `PRIMARY=TestUserFlowGapCompilerEmitsExactOwnerKeyedTodosAndConvergesOnSecondPass`; `PROPERTY=TestTodo_UXFLOW_010_Property`; `GOLDEN=TestTodo_UXFLOW_010_Golden`; `SECURITY=TestTodo_UXFLOW_010_Security`; `CONFORMANCE=TestTodo_UXFLOW_010_Conformance`; `MUTATION=TestTodo_UXFLOW_010_Mutation`.
  - **RED:** missing PageDefinition/widget/form/action/endpoint/message/state/recovery/accessibility/analytics/test produces no finding, shared gap creates one todo per flow, screen todo absorbs domain invariant, candidate lacks phase/intelligence/dependencies/exact oracle/references or unchanged second pass emits renamed/new duplicates.
  - **GREEN:** compiler resolves existing semantic owners/todos first, emits stable atomic candidates keyed by owner+contract with every consuming flow/stage/configuration reverse edge, exact browser/API/security/fault oracle and canonical digest; unchanged second pass emits zero new identities.
  - **REFACTOR:** share finding/todo schemas with vertical-slice convergence while preserving flow-specific participant-visible evidence.
  - **Refs:** [flow extraction](user-flows/README.md#flow-to-todo-and-tdd-extraction), [initial catalog](user-flows/catalog.md), [vertical-slice gap compiler](workflows/vertical-slices/gap-register.md#fixed-point-rule).

- [x] `API-001` **[GATE_A][SOL_HIGH] Publish discoverable capability descriptors across gRPC and HTTP.**
  - **Depends:** `CAP-003`, `TOOL-008`, `TOOL-009`, `INTENT-010`, `INTENT-013`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose governed intent creation, inspection or consumption without persistence or provider bypass`.
  - **TEST:** `TestTodo_API_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_API_001`; `GOLDEN=TestTodo_API_001_Golden`; `FUZZ=FuzzTodo_API_001`; `RACE=TestTodo_API_001_Race`; `INTEGRATION=TestTodo_API_001_Integration`; `SECURITY=TestTodo_API_001_Security`; `MUTATION=TestTodo_API_001_Mutation`.
  - **RED:** generated routes disagree on schema/version/AuthZ/purpose/side effects/idempotency/intent behavior, a material endpoint lacks FeatureIntentCoverage, or an unregistered/direct-domain endpoint is callable.
  - **GREEN:** descriptor digest, gRPC method and HTTP binding round-trip the same golden request/result/error schema, capability metadata and `creates|consumes|emits|observes|non_material` intent classification.
  - **REFACTOR:** grpcbridge is the only HTTP projection path for governed APIs.
  - **Refs:** [Capability parity](plan.md#59-every-product-action-is-a-governed-capability), [developer platform](specs/platform-responsibility-boundaries.md).
  - **Evidence (2026-09-03):** `TestTodo_API_001` in `internal/transport/manifest` (`RenderDiscoveryDocument`: pure projection of the manifest for gRPC and HTTP, no transport binding); PASS; serving it is part of the app wiring; branch plan-revision-2026-09-02; `go test -count=1 ./internal/transport/manifest/...` on windows/arm64 (Go 1.26.3).

- [x] `API-002` **[GATE_A][SOL_HIGH] Enforce API lifecycle compatibility and deprecation evidence.**
  - **Evidence (2026-09-05):** `TestTodo_API_002` family in `internal/transport/manifest` plus `TestTodo_API_002_BufBreakingDetectsFieldTypeChange`, `TestTodo_API_002_BufBreakingAllowsAdditiveChange`, `TestTodo_API_002_BufBreakingGolden` (live schema/proto against the checked-in descriptor baseline, regen flag HCMNEXT_COMPAT_REGEN_BASELINE=1), `TestTodo_API_002_BufBreakingBaselineIsFresh`, `TestTodo_API_002_GateRetirement`, `TestTodo_API_002_GateRetirementSecurity` in `tools/gen/compatibility`; `go test -count=1 ./tools/gen/compatibility/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `API-001`, `MODEL-017`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose governed intent creation, inspection or consumption without persistence or provider bypass`.
  - **TEST:** `TestTodo_API_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_API_002`; `FUZZ=FuzzTodo_API_002`; `INTEGRATION=TestTodo_API_002_Integration`; `SECURITY=TestTodo_API_002_Security`; `CONFORMANCE=TestTodo_API_002_Conformance`.
  - **RED:** breaking field/error/semantic change passes compatible, deprecated consumer lacks adoption record or removed version still has active tenant dependency.
  - **GREEN:** compatibility result identifies exact break and consumers; removal gate requires migration evidence and zero unsupported active consumers.
  - **REFACTOR:** transport compatibility cannot mask semantic behavior change.
  - **Refs:** [API lifecycle](data/models/registry-and-coverage-contracts.md), [version governance](plan.md).

## 18. Bounded agent and intelligence safety

> **Disposition (2026-09-02):** DESIGN. No agent capability is in P1A or P1B, so there is nothing for the gateway to gate. `AGENT-001`–`AGENT-004` are the security boundary that binds every plane once an agent capability is scheduled; `AGENT-005` is Phase 2 at the earliest.

- [x] `AGENT-001` **[DESIGN][SOL_HIGH] Route the bounded read/analyze/draft agent through a tool-security gateway.**
  - **Evidence (2026-09-07):** `TestTodo_AGENT_001`, `TestTodo_AGENT_001_Mutation`, `TestTodo_AGENT_001_Security` in `internal/agentsecurity` (written in 171cafc on 2026-09-03 and never ticked; the named tests prove the GREEN contract: gateway binds agent identity/delegation, tenant, purpose, capability/version, nonce, exact args, taint/provenance and budget; only approved read/analyze/draft calls return typed results); `go test -count=1 ./internal/agentsecurity/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-006`, `CAP-003`, `TRUST-018`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose governed intent creation, inspection or consumption without persistence or provider bypass`.
  - **TEST:** `TestTodo_AGENT_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_AGENT_001`; `SECURITY=TestTodo_AGENT_001_Security`; `MUTATION=TestTodo_AGENT_001_Mutation`.
  - **RED:** agent receives raw credential, expands tenant/purpose/tool/data/cost authority, executes write or forwards unvalidated output.
  - **GREEN:** gateway binds agent identity/delegation, tenant, purpose, capability/version, nonce, exact args, taint/provenance and budget; only approved read/analyze/draft calls return typed results.
  - **REFACTOR:** deterministic services execute all effects.
  - **Refs:** [Agent runtime](data/models/assurance-intelligence-platform.md), [Gate A bounded agent](execution-plan.md#gate-a--paid-design-partner-observation).

- [x] `AGENT-002` **[DESIGN][SOL_HIGH] Track semantic trust and prompt-injection taint.**
  - **Evidence (2026-09-08):** `TestTodo_AGENT_002`, `TestTodo_AGENT_002_Golden`, `FuzzTodo_AGENT_002`, and adversarial admission/result/inference tests; `go test -count=1 -cover ./internal/agentsecurity/` PASS (95.6%); `go vet ./internal/agentsecurity/` PASS; `go test -run '^$' -fuzz '^FuzzTodo_AGENT_002$' -fuzztime=3s ./internal/agentsecurity/` PASS (42,824 executions; documented Windows cleanup warning only). Sol and root refinement bind admissions and validated results to exact payloads, refuse fabricated canonical facts, preserve source citations and inherited taints through inference, and quarantine detector faults and detected hostile source content before derivation. This is the semantic gateway design boundary, not a claim of exhaustive injection detection or a deployed model-serving adapter.
  - **Depends:** `AGENT-001`, `MODEL-020`, `MODEL-024`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose governed intent creation, inspection or consumption without persistence or provider bypass`.
  - **TEST:** `TestTodo_AGENT_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_AGENT_002`; `GOLDEN=TestTodo_AGENT_002_Golden`; `FUZZ=FuzzTodo_AGENT_002`.
  - **RED:** resume/email/document/web content can alter system/tool instructions or untrusted/derived claim is presented as canonical fact.
  - **GREEN:** every datum carries source/trust/taint; instruction-like untrusted content is quarantined and answer distinguishes fact, observation, claim and inference with citations.
  - **REFACTOR:** detector failure defaults to lower trust, not execution authority.
  - **Refs:** [Agent security](data/models/assurance-intelligence-platform.md), [evidence model](data/models/kernel-governance-and-evidence.md).

- [ ] `AGENT-003` **[DESIGN][SOL_HIGH] Validate agent output before deterministic consumption.**
  - **Depends:** `AGENT-001`, `MODEL-017`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose governed intent creation, inspection or consumption without persistence or provider bypass`.
  - **TEST:** `TestTodo_AGENT_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_AGENT_003`; `GOLDEN=TestTodo_AGENT_003_Golden`; `FUZZ=FuzzTodo_AGENT_003`; `SECURITY=TestTodo_AGENT_003_Security`; `MUTATION=TestTodo_AGENT_003_Mutation`.
  - **RED:** malformed schema, nonexistent ID, unauthorized field, unsupported claim or hidden instruction reaches proposal/workflow/domain service.
  - **GREEN:** only SchemaFlux-valid, capability-valid, AuthZ-filtered output enters draft state; rejection returns typed diagnostics and zero effect.
  - **REFACTOR:** free-form narrative is never a transaction plan.
  - **Refs:** [Agent output gateway](data/models/assurance-intelligence-platform.md), [workflow compiler](specs/workflow-runtime.md).

- [ ] `AGENT-004` **[DESIGN][TERRA] Establish agent evaluation and kill-switch release gates.**
  - **Depends:** `AGENT-001`, `OPS-004`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose governed intent creation, inspection or consumption without persistence or provider bypass`.
  - **TEST:** `TestTodo_AGENT_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_AGENT_004`; `PROPERTY=TestTodo_AGENT_004_Property`; `RACE=TestTodo_AGENT_004_Race`; `SECURITY=TestTodo_AGENT_004_Security`.
  - **RED:** prompt/model/tool version change with failed safety/grounding/tool-selection fixture can deploy, or kill switch leaves write-capable call in flight.
  - **GREEN:** immutable eval run gates publication; disable one agent/model/tool/tenant/all-write-AI revokes leases and returns deterministic fallback while non-AI HCM remains available.
  - **REFACTOR:** AI incident links exact versions, inputs, outputs and tool attempts.
  - **Refs:** [Agent evaluation](data/models/assurance-intelligence-platform.md), [AI incidents](specs/incident-management.md).

- [ ] `AGENT-005` **[DESIGN][SOL_HIGH] Compile agent-proposed actions into governed draft BusinessIntents.**
  - **Depends:** `AGENT-001`–`AGENT-004`, `INTENT-012`, `INTENT-014`, `INTENT-020`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose governed intent creation, inspection or consumption without persistence or provider bypass`.
  - **TEST:** `TestAgentActionCreatesDraftIntentWithoutAuthorityExpansion`.
  - **TEST MATRIX:** `PRIMARY=TestAgentActionCreatesDraftIntentWithoutAuthorityExpansion`; `GOLDEN=TestTodo_AGENT_005_Golden`; `MUTATION=TestTodo_AGENT_005_Mutation`.
  - **RED:** `TestAgentActionCreatesDraftIntentWithoutAuthorityExpansion` lets model output execute a write/effect, invent definition/capability/subject, broaden delegated scope, hide uncertainty/evidence, submit a bulk loop or bypass simulation/human handoff.
  - **GREEN:** validated agent output selects a discoverable IntentDefinition and creates an attributed draft/proposal with exact arguments, sources, taint, model/tool versions, delegation/purpose, cost and required review; deterministic gateways perform all subsequent governance and execution.
  - **REFACTOR:** HCM Concierge and domain agents share the same compiler; agent branding/persona never changes authority semantics.
  - **Refs:** [Agent runtime](data/models/assurance-intelligence-platform.md), [BusinessIntent](specs/business-intent-and-change-request.md).

## 19. Reference-workflow conformance harness

> **Disposition (2026-09-02):** P1A for `CONF-001` at the depth the no-effect Promotion fixture needs (simulate mode, fixed clock, zero-effect receipt). Replay and fault schedules are P1B. The lifecycle conformance fixtures remain CONFORMANCE.

- [x] `CONF-001` **[P0][SOL_HIGH] Build a deterministic conformance runner for reference workflows.**
  - **Depends:** `MODEL-030`, `WF-COMP-004`, `SANDBOX-001`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_CONF_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CONF_001`; `GOLDEN=TestTodo_CONF_001_Golden`; `FAULT=TestTodo_CONF_001_Fault`; `CONFORMANCE=TestTodo_CONF_001_Conformance`; `RECOVERY=TestTodo_CONF_001_Recovery`; `MUTATION=TestTodo_CONF_001_Mutation`.
  - **RED:** fixture lacks inputs, clock, version set, authority map, fault schedule, expected events/states/effects or stable digest.
  - **GREEN:** runner executes `SIMULATE`, `REPLAY`, fault/recovery and evidence comparison and returns exact per-dimension result plus manifest digest.
  - **REFACTOR:** conformance invokes public capabilities only.
  - **Refs:** [Reference workflows](workflows/README.md), [architecture acceptance](reference-workflows/reference-suite.md#foundational-questions-the-suite-must-close).
  - **Evidence (2026-09-03):** `TestTodo_CONF_001`, `_Golden`, `_Conformance`, `_Fault`, `_Recovery`, `_Mutation` in `tools/conformance` (typed model parsed from planning/reference-workflows, vocabulary read live from planning/specs/workflow-runtime.md, eleven deterministic checks, JSON+markdown report with digest, `go run ./tools/conformance`; promote-into-management 11 PASS/0 FAIL); `go test -count=1 ./tools/conformance/...` PASS on windows/arm64 (Go 1.26.3); `Runner` port awaits internal/workflow; branch plan-revision-2026-09-02. Executed section: `internal/workflow/simulate` (SIMULATE-mode interpreter; `TestSimulateModeExecutesThePromotionReferenceWithZeroEffects`, `TestSimulateModeRefusesWriteClassNodes`, `TestSimulateReceiptIsByteStable`, `TestTodo_WF_SIM_001_GoldenPromotionReceipt`, `TestTodo_WF_SIM_002_GoldenObservationBranchReceipt`, `TestTodo_WF_SIM_003_PropertyTraceIsAWalkOfTheCompiledGraph`, `TestTodo_WF_SIM_004_PropertyEveryAdmittedNodeIsZeroEffect`, `TestTodo_WF_SIM_005_PropertyReceiptIgnoresInputInsertionOrder`, `TestTodo_WF_SIM_006_FaultObservationDriftRoutesToDegraded`, `TestTodo_WF_SIM_007_FaultUnreadableSourceIsUnknownNotPass`, `TestTodo_WF_SIM_008_FaultUnresolvedBudgetAuthorityBlocks`, `TestTodo_WF_SIM_009_FaultMissingContextTakesTheDeclaredBehavior`, `TestTodo_WF_SIM_010_FaultBlockedPreflightFailsTheTransform`, `TestTodo_WF_SIM_011_FaultMissingWorkflowInputIsRefused`, `TestTodo_WF_SIM_012_FaultUnboundCapabilityRegistryIsRefused`, `TestTodo_WF_SIM_013_FaultStepBudgetIsBounded`, `TestTodo_WF_SIM_014_FaultTamperedPlanIsRefused`) and `tools/conformance/runner.PlanRunner` (`TestConformanceReportGainsAnExecutedReceiptSection`); the promotion reference runs 6 nodes, 4 work items would await, lifecycle ends SIMULATED/NOT_PLANNED/NOT_STARTED/NOT_APPLICABLE/PENDING, all effect counters zero; `go test -count=1 ./internal/workflow/... ./tools/conformance/...` PASS.

- [x] `CONF-002` **[CONFORMANCE][SOL_HIGH] Prove Recruit, Hire and Onboard semantics.**
  - **Evidence (2026-09-07):** `TestTodo_CONF_002`, `TestTodo_CONF_002_Conformance`, `TestTodo_CONF_002_Mutation`, `TestTodo_CONF_002_Security` in `tools/conformance/recruit` (written in e3d0849 on 2026-09-03 and never ticked; the named tests prove the GREEN contract: golden scenario preserves candidate+worker roles, binds offer/approvals, creates employment once and returns business `COMPLETED` with explicit downstream consistency/repair states); `go test -count=1 ./tools/conformance/recruit/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CONF-001`, `WF-STEP-001`, `WF-STEP-005`, `WF-STEP-009`, `WF-STEP-013`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_CONF_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CONF_002`; `SECURITY=TestTodo_CONF_002_Security`; `CONFORMANCE=TestTodo_CONF_002_Conformance`; `MUTATION=TestTodo_CONF_002_Mutation`.
  - **RED:** duplicate person, exhausted position/budget, expired offer, missing work authorization, inaccessible form or partial IAM/payroll outcome is falsely completed.
  - **GREEN:** golden scenario preserves candidate+worker roles, binds offer/approvals, creates employment once and returns business `COMPLETED` with explicit downstream consistency/repair states.
  - **REFACTOR:** no native ATS/payroll implementation is required.
  - **Refs:** [Recruit-to-onboard](workflows/lifecycle/recruit-hire-onboard.md), [recruiting data](data/models/talent-experience-cases.md).

- [x] `CONF-003` **[CONFORMANCE][SOL_HIGH] Prove cross-company transfer semantics.**
  - **Evidence (2026-09-05):** `TestTodo_CONF_003`, `TestTodo_CONF_003_{Fault,Security,Conformance,Mutation,Race}` in `internal/workflow/conformance/transfer` (real definition through the SIMULATE interpreter: distinct source/destination authority scopes, five approvals with obligations, conflict block, bounded repair, unknown-context terminal; hidden EXTERNAL_MUTATION refused by simulate.Admit); `go test -count=1 ./internal/workflow/conformance/transfer/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CONF-001`, `WF-STEP-003`, `WF-STEP-006`, `WF-STEP-015`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_CONF_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CONF_003`; `RACE=TestTodo_CONF_003_Race`; `FAULT=TestTodo_CONF_003_Fault`; `SECURITY=TestTodo_CONF_003_Security`; `CONFORMANCE=TestTodo_CONF_003_Conformance`; `MUTATION=TestTodo_CONF_003_Mutation`.
  - **RED:** source/destination employment, jurisdiction, payroll, benefit, IAM, privacy, position or company authority is omitted or ambiguous partial effect is called failure/success.
  - **GREEN:** golden transfer composes exit/start obligations, effective-dated authority, reservations and external observations with exact multidimensional terminal state and repair route.
  - **REFACTOR:** source and destination companies remain separate authority scopes.
  - **Refs:** [Transfer workflow](workflows/workforce/org-transfer-compensation.md), [employment model](data/models/people-workforce.md).

- [ ] `CONF-004` **[CONFORMANCE][SOL_HIGH] Prove leave and return-to-work semantics.**
  - **Depends:** `CONF-001`, `LEAVE-015`, `WF-STEP-008`, `WF-STEP-009`, `WF-STEP-014`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_CONF_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CONF_004`; `PROPERTY=TestTodo_CONF_004_Property`; `GOLDEN=TestTodo_CONF_004_Golden`; `FAULT=TestTodo_CONF_004_Fault`; `SECURITY=TestTodo_CONF_004_Security`; `CONFORMANCE=TestTodo_CONF_004_Conformance`; `MUTATION=TestTodo_CONF_004_Mutation`; `RACE=TestTodo_CONF_004_Race`.
  - **RED:** client supplies derived truth; manager determines legal eligibility; sensitive evidence leaks; overlapping entitlements or paid segments compose incorrectly; planned balances post during simulation; PTO drift denies leave instead of replanning; employment becomes inactive; extension mutates original; benefits unavailability rolls back leave; or return occurs without current evidence/restriction/readiness obligations.
  - **GREEN:** hypothetical Medical Leave fixture proves intent-only ingress, mixed-source snapshot, independent program eligibility, entitlement/balance/schedule/effect planning, compartmented evidence review, immutable proposal, durable waits, partial replan, atomic Leave start with Employment `ACTIVE`, degraded benefits observation/recovery, related extension, structured restricted return, atomic Leave end, multidimensional reconciliation and execution receipt.
  - **REFACTOR:** legal rule evaluation remains external to workflow kernel.
  - **Refs:** [Leave workflow](workflows/leave/leave-return-to-work.md), [leave data](data/models/rewards-payroll-workforce.md).

- [x] `CONF-005` **[CONFORMANCE][SOL_HIGH] Prove termination and offboarding semantics.**
  - **Evidence (2026-09-05):** `TestTodo_CONF_005`, `TestTodo_CONF_005_{Fault,Conformance,Mutation}`, `TestTodo_CONF_005_HiddenEffect` in `internal/workflow/conformance/termination` (separation of duties, legal hold, independently tracked P0 access revocation with bounded retry, reinstatement as a distinct SUPERSEDED terminal); `go test -count=1 ./internal/workflow/conformance/termination/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CONF-001`, `WF-STEP-011`, `WF-STEP-014`, `WF-STEP-015`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_CONF_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CONF_005`; `FAULT=TestTodo_CONF_005_Fault`; `CONFORMANCE=TestTodo_CONF_005_Conformance`; `MUTATION=TestTodo_CONF_005_Mutation`.
  - **RED:** revoked approval, final-pay deadline, notice/evidence, privileged IAM revoke, equipment, benefits or retention obligation is omitted; irreversible ambiguity triggers blind retry.
  - **GREEN:** employment ends once, P0 access revocation is independently tracked, obligations and external effects reconcile and any degraded dimension creates bounded repair/incident evidence.
  - **REFACTOR:** cancellation after employment end becomes correction/reinstatement intent.
  - **Refs:** [Termination workflow](workflows/lifecycle/termination-offboarding.md), [access lifecycle](data/models/connectivity-access-content.md).

- [ ] `CONF-006` **[CONFORMANCE][SOL_HIGH] Prove payroll correction and retroactive repair semantics.**
  - **Depends:** `CONF-001`, `MODEL-003`, `WF-STEP-016`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_CONF_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CONF_006`; `PROPERTY=TestTodo_CONF_006_Property`; `CONFORMANCE=TestTodo_CONF_006_Conformance`; `MUTATION=TestTodo_CONF_006_Mutation`.
  - **RED:** current rule version rewrites historical calculation, gross/net/tax/accounting delta fails to balance or correction overwrites original filing/payment.
  - **GREEN:** bitemporal fixture reproduces original versions, computes exact delta, simulates/approves correction and appends reconciled pay/accounting/tax/filing evidence.
  - **REFACTOR:** no native payroll engine is required; deterministic fixtures may use a test adapter.
  - **Refs:** [Payroll correction](workflows/payroll/payroll-correction-retro.md), [payroll data](data/models/rewards-payroll-workforce.md).

- [ ] `CONF-007` **[CONFORMANCE][SOL_HIGH] Prove Manager Change as the minimal pure-HRIS workflow.**
  - **Depends:** `CONF-001`, `ORG-002`, `CONFLICT-003`, `APPROVAL-005`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_CONF_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CONF_007`; `CONFORMANCE=TestTodo_CONF_007_Conformance`; `MUTATION=TestTodo_CONF_007_Mutation`.
  - **RED:** inactive manager, self-manager, cycle, future conflict, stale authority or changed proposal reaches commit.
  - **GREEN:** fixture appends exactly proposed/validated/approved/scheduled/revalidated/changed/observed/reconciled story and ends consistent with Jane's manager `Bob` on the effective date.
  - **REFACTOR:** derived org hierarchy is projection, not an extra authority.
  - **Refs:** [Manager Change](workflows/people/manager-change.md), [organization model](data/models/people-workforce.md).

- [x] `CONF-008` **[GATE_B][SOL_HIGH] Prove Promote-to-Management cross-domain degraded completion and repair.**
  - **Evidence (2026-09-07):** `TestTodo_CONF_008`, `TestTodo_CONF_008_Conformance`, `TestTodo_CONF_008_Fault`, `TestTodo_CONF_008_Golden`, `TestTodo_CONF_008_Mutation`, `TestTodo_CONF_008_Race` in `internal/workflow/conformance/payroll` (written in 5b64e58 on 2026-09-06 and never ticked; the named tests prove the GREEN contract: golden trace proves intent-only ingress, mixed-authority snapshot, zero-effect simulation, a signed `LegalEvaluationReceipt` naming the pinned rule-pack releases, the applied and considered-not-applicable obligations, and the composition trace, immutable proposal, four governed approvals, durable effective-date wait, execution revalidation, fenced position/budget holds, one atomic People/Org/Position/Comp commit, effect DAG, fresh observations, business `COMPLETED` plus external `DEGRADED`/reconciliation `REPAIR_REQUIRED`, targeted IAM repair, final consistent closure and verifiable execution receipt); `go test -count=1 ./internal/workflow/conformance/payroll/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CONF-001`, `PROMO-008`, `INTG-016`, `WF-RUN-016`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_CONF_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CONF_008`; `GOLDEN=TestTodo_CONF_008_Golden`; `RACE=TestTodo_CONF_008_Race`; `FAULT=TestTodo_CONF_008_Fault`; `CONFORMANCE=TestTodo_CONF_008_Conformance`; `MUTATION=TestTodo_CONF_008_Mutation`.
  - **RED:** caller-owned current facts are trusted; simulation creates side effects; task is not proposal-bound; crash strands frontier; stale approval/snapshot/reservation reaches commit; failed compensation-read entitlement rolls back committed promotion; runtime completion closes the intent; or repair reruns any local Promotion mutation.
  - **GREEN:** golden trace proves intent-only ingress, mixed-authority snapshot, zero-effect simulation, a signed `LegalEvaluationReceipt` naming the pinned rule-pack releases, the applied and considered-not-applicable obligations, and the composition trace, immutable proposal, four governed approvals, durable effective-date wait, execution revalidation, fenced position/budget holds, one atomic People/Org/Position/Comp commit, effect DAG, fresh observations, business `COMPLETED` plus external `DEGRADED`/reconciliation `REPAIR_REQUIRED`, targeted IAM repair, final consistent closure and verifiable execution receipt.
  - **REFACTOR:** domain capability composition remains customer-configurable.
  - **Refs:** [Promotion workflow](workflows/rewards/promotion-into-management.md), [reference suite](reference-workflows/reference-suite.md).

## 20. Regulatory platform and content operations

> **Disposition (2026-09-02):** `LEGAL-001` is P1B at reduced depth: one versioned rule pack of customer-configured thresholds and notice obligations evaluated to `LegalEvaluationStatus` and bound through `ObligationBinding`. Jurisdiction resolution, rule packs, calculation engines and filings are Phase 2 or later; the kernel has no filing family. The Phase 2 rule-pack content model, obligation vocabulary and fifty-state configuration matrix are contracted in [legal rule packs and state configuration](specs/legal-rule-packs-and-state-configuration.md); `LEGAL-008`, `LEGAL-017` and `LEGAL-018` are the P0 planning-hygiene items that contract depends on.

- [x] `LEGAL-001` **[GATE_A][SOL_HIGH] Resolve an explicit signed LegalContext.**
  - **Depends:** `MODEL-018`, `MODEL-021`, `TRUST-011`, `TIME-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_001`; `GOLDEN=TestTodo_LEGAL_001_Golden`; `RACE=TestTodo_LEGAL_001_Race`; `SECURITY=TestTodo_LEGAL_001_Security`; `MUTATION=TestTodo_LEGAL_001_Mutation`.
  - **RED:** missing legal entity/work location/employment jurisdiction/effective time/known time/rule release returns `LEGAL_CONTEXT_UNKNOWN`; locale alone never supplies jurisdiction.
  - **GREEN:** result contains jurisdiction hierarchy, legal entity, applicable release IDs, effective/known times, provenance and uncertainty with a stable digest.
  - **REFACTOR:** contextual uncertainty blocks only capabilities whose legal contract requires certainty.
  - **Refs:** [Regulatory platform](plan.md), [context separation](data/models/wire-contract-primitives.md), [Legal rule packs and state configuration](specs/legal-rule-packs-and-state-configuration.md).
  - **Evidence (2026-09-03):** `TestTodo_LEGAL_001`, `_Golden`, `_Race`, `_Security`, `_Mutation` in `internal/governance/legal` (`Resolve` derives a signed ed25519 LegalContext from work location, legal entity and employment jurisdiction, refusing with `ErrLegalContextUnknown` when ambiguous; `Evaluate` applies versioned `RulePack`s with ten typed obligation kinds; California and New York packs seeded from `planning/research/state-employment-law/{california,new-york}.md`, every citation marked unreviewed); `go test -count=1 ./internal/governance/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02. Superseded scope: single-jurisdiction resolution is extended by `LEGAL-009` (`JurisdictionSet` with locality overlays); the ten-kind vocabulary is `vocabulary_version` 1 and is extended to twenty-two by `LEGAL-011`.

- [x] `LEGAL-002` **[PHASE_2][SOL_HIGH] Ingest, review and publish a regulatory RulePack.**
  - **Evidence (2026-09-05):** `TestTodo_LEGAL_002` in `internal/governance/legal/pipeline`, `internal/governance/legal` (ingestion of a regulatory RulePack source into a digested DRAFT, a ReviewRecord with typed findings from a reviewer distinct from the author, publication only from a reviewed draft into an immutable release with an activation window, supersession by a new version never in place, RulePack.Explain and RollbackTarget; golden over Washington's RCW 49.46.020 minimum-wage schedule and its 2027 CPI amendment; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/governance/legal/pipeline/ ./internal/governance/legal/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-018`, `MODEL-020`, `CONFIG-002`, `LEGAL-015`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_002`; `PROPERTY=TestTodo_LEGAL_002_Property`; `GOLDEN=TestTodo_LEGAL_002_Golden`; `RACE=TestTodo_LEGAL_002_Race`; `SECURITY=TestTodo_LEGAL_002_Security`; `MUTATION=TestTodo_LEGAL_002_Mutation`.
  - **RED:** unsigned/unreviewed/stale/duplicate/jurisdictionless or citationless content activates.
  - **GREEN:** approved release records source citations, separated reviewers, applicability interval, `ReviewStatus`, per-rule `ConfidenceMarker`, supersession chain, digest and rollback target; a release below the tenant's review floor returns `REVIEW_STATUS_INSUFFICIENT` rather than evaluating.
  - **REFACTOR:** historical calculation continues resolving its pinned version.
  - **Refs:** [Rule packs and regulatory platform](plan.md), [regulatory content operations](specs/platform-responsibility-boundaries.md), [Legal rule packs and state configuration](specs/legal-rule-packs-and-state-configuration.md).

- [ ] `LEGAL-003` **[PHASE_2][SOL_HIGH] Compute regulatory-change impact before activation.**
  - **Depends:** `LEGAL-001`, `LEGAL-002`, `CONFIG-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_003`; `PROPERTY=TestTodo_LEGAL_003_Property`; `GOLDEN=TestTodo_LEGAL_003_Golden`; `RACE=TestTodo_LEGAL_003_Race`; `SECURITY=TestTodo_LEGAL_003_Security`; `MUTATION=TestTodo_LEGAL_003_Mutation`.
  - **RED:** changed rule cannot identify affected tenants/workers/calculations/workflows/notices/pending approvals or silently rewrites historical results; or a changed rule is assessed against the execution date rather than the transaction's business effective date.
  - **GREEN:** deterministic report returns affected scopes and required recompute/notice/reapproval/hold actions while old outcomes retain original rule release.
  - **REFACTOR:** impact query is read-only and watermark-bound.
  - **Refs:** [Regulatory change](plan.md), [dependency graph](specs/hris-admin-dataops.md), [Time axes](specs/legal-rule-packs-and-state-configuration.md#2-jurisdiction-model).

- [ ] `LEGAL-004` **[PHASE_2][SOL_HIGH] Resolve structured legal obligations and statutory deadlines.**
  - **Depends:** `LEGAL-001`, `RULE-003`, `WF-STEP-008`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_004`; `PROPERTY=TestTodo_LEGAL_004_Property`; `GOLDEN=TestTodo_LEGAL_004_Golden`; `SECURITY=TestTodo_LEGAL_004_Security`; `MUTATION=TestTodo_LEGAL_004_Mutation`.
  - **RED:** missing authority/trigger/due-date calendar/evidence/responsible party or contradictory obligation reports satisfied.
  - **GREEN:** each obligation has type, authority, subject, trigger, due-at calculation/version, action, evidence, owner, risk and `OPEN/SATISFIED/WAIVED/OVERDUE/BLOCKED` status.
  - **REFACTOR:** waiver requires explicit authority and never deletes the original obligation; obligation types come from the rule-pack vocabulary; this todo adds deadline computation and status, not new kinds.
  - **Refs:** [Obligation engine and statutory calendar](plan.md), [Obligation kinds](specs/legal-rule-packs-and-state-configuration.md#4-obligation-kinds).

- [ ] `LEGAL-005` **[PHASE_2][SOL_HIGH] Evaluate worker classification with uncertainty and review.**
  - **Depends:** `LEGAL-001`, `MODEL-022`, `MODEL-025`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_005`; `RACE=TestTodo_LEGAL_005_Race`; `SECURITY=TestTodo_LEGAL_005_Security`; `MUTATION=TestTodo_LEGAL_005_Mutation`.
  - **RED:** incomplete/stale factors infer employee/contractor/exempt/nonexempt or work authorization conclusively.
  - **GREEN:** return `ELIGIBLE`, `INELIGIBLE`, or `REVIEW_REQUIRED` with factors, rule release, effective date and override/reviewer authority; uncertainty causes zero mutation.
  - **REFACTOR:** classification is governed computation, not UI metadata.
  - **Refs:** [Regulatory computation](plan.md), [worker roles](data/models/people-workforce.md).

- [ ] `LEGAL-006` **[PHASE_2][SOL_HIGH] Compose statutory, collective and company leave entitlements.**
  - **Depends:** `LEGAL-004`, `ELIG-003`, `RULE-003`, `MODEL-025`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_006`; `PROPERTY=TestTodo_LEGAL_006_Property`; `RACE=TestTodo_LEGAL_006_Race`; `SECURITY=TestTodo_LEGAL_006_Security`; `MUTATION=TestTodo_LEGAL_006_Mutation`.
  - **RED:** overlap/concurrency/offset/stack/most-protective semantics are collapsed, company policy narrows mandatory protection, manager approval changes statutory eligibility, or unknown evidence becomes approval/denial; or an accrued statutory leave balance is reset or forfeited by a promotion or demotion when the worker is not on leave.
  - **GREEN:** immutable composition lists each program/authority/release, eligibility result, interaction strategy, protected/paid interval constraints, required evidence/notices/effects and `VALID|CONFLICT|REVIEW_REQUIRED|UNKNOWN` state with calculation trace.
  - **REFACTOR:** workflow coordinates; entitlement engine computes.
  - **Refs:** [Rule composition](plan.md), [leave workflow](workflows/leave/leave-return-to-work.md).

- [x] `LEGAL-008` **[P0][TERRA] Register the rule-pack contract and validate the fifty-state configuration matrix.**
  - **Depends:** `GOV-004`, `LEGAL-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.REGULATORY; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestLegalMatrixCoversEveryResearchFile`.
  - **TEST MATRIX:** `PRIMARY=TestLegalMatrixCoversEveryResearchFile`; `GOLDEN=TestTodo_LEGAL_008_Golden`; `FUZZ=FuzzLegalMatrixParse`.
  - **RED:** `TestLegalMatrixCoversEveryResearchFile` fails when a matrix row has no research file, a research file has no matrix row, a cell holds a value outside `{Y,L,P,F,?}`, a stated column total disagrees with the counted cells, or the spec is absent from `specs/README.md` and `specs/specification-ownership-registry.md`.
  - **GREEN:** the parsed matrix has exactly fifty rows keyed to the fifty state files, every cell is a legend value, both column-total blocks recompute exactly, and the ownership registry names an accountable role and a review trigger for the contract.
  - **REFACTOR:** the parser is a `tools/planning` subcommand of `plancheck`, sharing the todo-registry file walker; no second Markdown scanner.
  - **Refs:** [Legal rule packs and state configuration](specs/legal-rule-packs-and-state-configuration.md), [state employment-law research](research/state-employment-law/README.md).
  - **Evidence (2026-09-03):** `TestLegalMatrixCoversEveryResearchFile`, `TestTodo_LEGAL_008_Golden`, `FuzzLegalMatrixParse` in `tools/planning/legalmatrix` (`plancheck legalmatrix`: fifty rows keyed to the fifty state files, legend tokens validated, both column-total blocks recomputed exactly, spec indexed and owned; zero violations on the live spec); `go test -count=1 ./tools/planning/legalmatrix/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `LEGAL-009` **[PHASE_2][SOL_HIGH] Resolve a JurisdictionSet with locality overlays and multi-state attribution.**
  - **Depends:** `LEGAL-001`, `LEGAL-008`, `MODEL-018`, `TIME-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_009`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_009`; `PROPERTY=TestTodo_LEGAL_009_Property`; `GOLDEN=TestTodo_LEGAL_009_Golden`; `RACE=TestTodo_LEGAL_009_Race`; `SECURITY=TestTodo_LEGAL_009_Security`; `MUTATION=TestTodo_LEGAL_009_Mutation`.
  - **RED:** work split across subdivisions with no configured `primary_work_threshold`, or with no subdivision at or above it, resolves to a single jurisdiction; a registered locality is dropped from the overlay set; an unregistered locality disappears without a receipt note; or a locality-level jurisdiction is returned as `primary`.
  - **GREEN:** `Resolve` returns one `PRIMARY` subdivision plus deduplicated `OVERLAY` jurisdictions ordered coarse to fine, records which attribution rule `A1`–`A6` fired and the remote-work policy applied, refuses with `LEGAL_CONTEXT_UNKNOWN` and reason `MULTI_STATE_UNRESOLVED` when no subdivision clears the threshold, and keeps the resolved set inside the signed `LegalContext` digest.
  - **REFACTOR:** `Jurisdiction.Locality` becomes the last element of `locality_path` with the canonical encoding distinguishing an empty path from a one-element empty string; existing single-jurisdiction callers keep compiling.
  - **Refs:** [Jurisdiction model](specs/legal-rule-packs-and-state-configuration.md#2-jurisdiction-model), [Legal and Compliance Plane](specs/platform-architecture-catalog.md#912-legal-and-compliance-plane).
  - **Evidence (2026-09-03):** `TestTodo_LEGAL_009`, Declared `LEGAL-009` test matrix and acceptance behavior in `internal/governance/legal`; PASS; `go test -p 1 ./internal/governance/legal` and `go vet ./internal/governance/legal` on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `LEGAL-010` **[PHASE_2][SOL_HIGH] Publish rule packs as a digested, signed, superseding release family.**
  - **Depends:** `LEGAL-001`, `LEGAL-008`, `MODEL-020`, `CONFIG-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_010`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_010`; `PROPERTY=TestTodo_LEGAL_010_Property`; `GOLDEN=TestTodo_LEGAL_010_Golden`; `FUZZ=FuzzPackDefinitionLoader`; `RACE=TestTodo_LEGAL_010_Race`; `SECURITY=TestTodo_LEGAL_010_Security`; `MUTATION=TestTodo_LEGAL_010_Mutation`.
  - **RED:** rewording a citation `Note` changes the release digest; changing a typed body field leaves it unchanged; a published release is editable in place; a superseding release does not close the prior effective window; a release typed against an unknown `vocabulary_version` evaluates as "no obligations"; or a forged signature over a valid digest verifies.
  - **GREEN:** the digest covers exactly the fields listed in the contract's section 3.2 and nothing else; `supersedes`/`superseded_by` link the chain and the prior window's `End` equals the successor's `Start`; an unsupported `vocabulary_version` returns `VOCABULARY_VERSION_UNSUPPORTED`; and `GetExact` keeps returning the pinned release forever.
  - **REFACTOR:** the loader produces `RulePack` from a checked-in definition file; the two seed packs are regenerated through it rather than hand-built in Go.
  - **Refs:** [Rule-pack definition family](specs/legal-rule-packs-and-state-configuration.md#3-rule-pack-definition-family), [canonical envelope and digest](specs/canonical-envelope-and-digest.md).
  - **Evidence (2026-09-03):** `TestTodo_LEGAL_010`, `TestTodo_LEGAL_010_Property`, `TestTodo_LEGAL_010_Golden` (release digest pinned; second half in `internal/governance/legal/extract` regenerates all 50 state packs byte-identically), `TestTodo_LEGAL_010_Race`, `TestTodo_LEGAL_010_Security`, `TestTodo_LEGAL_010_Mutation` (16 mutants), `FuzzPackDefinitionLoader` in `internal/governance/legal` (`packrelease.go`: §3.2 canonical digest, ed25519 signing and verification, supersession chain, review-status floor; `packdefinition.go`: JSON pack definitions under `definitions/legal/packs/{seed,states}` loaded through one schema-validated loader, California and New York seed packs regenerated through it); `TestTodo_LEGAL_CFG_Conformance` (50 subtests: Y implies kind present, F/L/P implies absent, `?` never releasable, every source file exists) in `internal/governance/legal/extract`; no federal pack by contract §10; no state pack is releasable while any `?` cell remains; `go test -count=1 ./internal/governance/legal/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `LEGAL-011` **[PHASE_2][SOL_HIGH] Add the twelve obligation kinds and bind each to a lifecycle step.**
  - **Depends:** `LEGAL-010`, `WF-STEP-008`, `MODEL-026`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_011`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_011`; `PROPERTY=TestTodo_LEGAL_011_Property`; `GOLDEN=TestTodo_LEGAL_011_Golden`; `FUZZ=FuzzTodo_LEGAL_011`; `SECURITY=TestTodo_LEGAL_011_Security`; `MUTATION=TestTodo_LEGAL_011_Mutation`.
  - **RED:** an obligation whose trigger is false is absent from the result instead of recorded as `CONSIDERED_NOT_APPLICABLE`; `LEAVE_INTERACTION` fires only when the worker is already on leave; a new kind carries no `Citation`; an obligation binds to a lifecycle step the contract does not assign it; or any binding is removable.
  - **GREEN:** the twenty-two kinds each validate their typed body, carry a complete `Citation` with a `ConfidenceMarker`, produce a `NonRemovable` `ObligationBinding` of the declared kind, and resolve into exactly one of `obligations_applied` or `obligations_not_applicable` for every registered release; the existing ten keep their wire tokens and ordinals.
  - **REFACTOR:** trigger predicates are pure functions of the proposal snapshot and the typed body, with no registry or clock access.
  - **Refs:** [Obligation kinds](specs/legal-rule-packs-and-state-configuration.md#4-obligation-kinds), [workflow runtime](specs/workflow-runtime.md).
  - **Evidence (2026-09-03):** `TestTodo_LEGAL_011`, `TestTodo_LEGAL_011_Property`, `TestTodo_LEGAL_011_Golden`, `TestTodo_LEGAL_011_Security`, `TestTodo_LEGAL_011_Mutation` (14 seeded mutants), `FuzzTodo_LEGAL_011` in `internal/governance/legal` (`vocabulary.go` vocabulary_version 2, `ConfidenceMarker`, source types and lifecycle steps; `obligation_added.go` twelve typed bodies plus `PreemptionAssertion`; `obligation_spec.go` single kind-to-trigger/lifecycle-step binding table for all twenty-two kinds; unstated figures stay empty and are refused only at release, never invented); extractor emits 599 obligations across the 50 draft packs (340 CONFIRMED / 259 VERIFY), Y cells always emit, `?` only on evidence, L/P/F never; `go test -count=1 ./internal/governance/legal/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `LEGAL-012` **[PHASE_2][SOL_HIGH] Compose obligations across overlapping jurisdictions with per-kind comparators.**
  - **Evidence (2026-09-07):** `TestTodo_LEGAL_012`, `TestTodo_LEGAL_012_Golden`, `TestTodo_LEGAL_012_Mutation`, `TestTodo_LEGAL_012_Property`, `TestTodo_LEGAL_012_Race` in `internal/governance/legal` (written in ad58d6a on 2026-09-06 and never ticked; the named tests prove the GREEN contract: each kind applies its declared comparator, composition is commutative, associative and idempotent over the jurisdiction set, mutually impossible obligations return `CONTRADICTORY_REQUIREMENTS` with both obligations and both citations, and the composition trace names the inputs, comparator and winner per kind); `go test -count=1 ./internal/governance/legal/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LEGAL-009`, `LEGAL-011`, `GOVERN-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_012`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_012`; `PROPERTY=TestTodo_LEGAL_012_Property`; `GOLDEN=TestTodo_LEGAL_012_Golden`; `RACE=TestTodo_LEGAL_012_Race`; `MUTATION=TestTodo_LEGAL_012_Mutation`.
  - **RED:** composing the same jurisdiction set in a different order changes the receipt bytes; a single global "most protective wins" rule is applied across kinds; a mandatory retention maximum shorter than another jurisdiction's minimum is silently resolved; or a non-compete voided in one jurisdiction survives composition.
  - **GREEN:** each kind applies its declared comparator, composition is commutative, associative and idempotent over the jurisdiction set, mutually impossible obligations return `CONTRADICTORY_REQUIREMENTS` with both obligations and both citations, and the composition trace names the inputs, comparator and winner per kind.
  - **REFACTOR:** comparators are registered per kind in one table; no `switch` on kind is duplicated at a call site.
  - **Refs:** [Evaluation semantics](specs/legal-rule-packs-and-state-configuration.md#6-evaluation-semantics), [governance decision and obligation composition](specs/governance-decision-and-obligation-composition.md).

- [x] `LEGAL-013` **[PHASE_2][SOL_HIGH] Enforce state preemption of locality obligations.**
  - **Evidence (2026-09-08):** `TestTodo_LEGAL_013`, `_Property`, `_Golden`, `_Mutation`, and real `Resolve`→`Evaluate` integration/boundary tests; `go test -count=1 -cover ./internal/governance/legal/` PASS (82.2%); `go vet ./internal/governance/legal/` PASS. Five state golden fixtures pin receipt hashes; preemption removes only cited locality obligations of the named kind before triggers, preserves higher-level obligations, and rejects conflicting duplicate releases. Exact overlay pinning and signed unregistered-locality evidence obey optional tenant fail-closed policy; legacy single-release canonical bytes remain unchanged. Fixtures retain unreviewed citation status and do not constitute counsel approval or deployed state-pack release evidence.
  - **Depends:** `LEGAL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_013`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_013`; `PROPERTY=TestTodo_LEGAL_013_Property`; `GOLDEN=TestTodo_LEGAL_013_Golden`; `MUTATION=TestTodo_LEGAL_013_Mutation`.
  - **RED:** a Milwaukee paid-leave obligation attaches to a Wisconsin promotion; a preemption assertion removes a subdivision- or country-level obligation; a preemption assertion for one kind removes an obligation of another kind; or a preemption is applied without a citation.
  - **GREEN:** `PreemptionAssertion` removes only `LOCALITY` obligations of the named kind before composition, each removal is recorded in the receipt with the asserting jurisdiction and the removed obligation ids, and the five asserting states (`WI`, `LA`, `TX`, `OK`, `TN`) each pass a golden vector.
  - **REFACTOR:** preemption is a distinct evaluation stage between release pinning and trigger evaluation; it never runs inside a comparator.
  - **Refs:** [Preemption](specs/legal-rule-packs-and-state-configuration.md#64-preemption), [state employment-law research](research/state-employment-law/README.md).

- [ ] `LEGAL-014` **[PHASE_2][SOL_HIGH] Emit a signed LegalEvaluationReceipt.**
  - **Depends:** `LEGAL-012`, `MODEL-023`, `MODEL-029`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_014`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_014`; `GOLDEN=TestTodo_LEGAL_014_Golden`; `RACE=TestTodo_LEGAL_014_Race`; `SECURITY=TestTodo_LEGAL_014_Security`; `RECOVERY=TestTodo_LEGAL_014_Recovery`; `MUTATION=TestTodo_LEGAL_014_Mutation`.
  - **RED:** a receipt omits the `CONSIDERED_NOT_APPLICABLE` set, the composition trace, the attribution rule that fired or the pinned release digests; a receipt stops verifying once its releases are unregistered; or a tampered receipt verifies.
  - **GREEN:** the receipt carries every field in the contract's section 6.5, verifies offline against its embedded key after a canonical round trip, remains verifiable when the registry no longer holds the releases, and is the artifact `ObligationState` transitions are evidenced by.
  - **REFACTOR:** the receipt reuses the `LegalContext` signing and verification path; no second signature implementation.
  - **Refs:** [Receipt](specs/legal-rule-packs-and-state-configuration.md#65-receipt), [provenance graph and lineage](specs/provenance-graph-and-lineage.md).

- [x] `LEGAL-015` **[PHASE_2][SOL_HIGH] Run the rule-pack authoring and review pipeline with separated signers.**
  - **Evidence (2026-09-05):** `TestTodo_LEGAL_015` in `internal/governance/legal/pipeline` (the authoring and review pipeline with separated signers: author, reviewer and publisher each sign the exact digest with the ed25519 convention, three distinct principals proven through the SoD evaluator, an AUTHORED/REVIEWED/PUBLISHED/SUPERSEDED/WITHDRAWN state machine whose every transition is a hash-chained signed event, VerifyChain re-verifying the log, coinciding roles or a bad signature refused; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/governance/legal/pipeline/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LEGAL-010`, `LEGAL-002`, `TRUST-011`, `CONFIG-002`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.REGULATORY; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_LEGAL_015`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_015`; `GOLDEN=TestTodo_LEGAL_015_Golden`; `SECURITY=TestTodo_LEGAL_015_Security`; `MUTATION=TestTodo_LEGAL_015_Mutation`.
  - **RED:** a pack reaches `COUNSEL_APPROVED` while any rule carries `VERIFY` or `DISPUTED`; the author signs their own release; an extractor broadens a research recommendation into a statutory requirement; or a release below the tenant's review floor evaluates.
  - **GREEN:** the six `ReviewStatus` values and three `ConfidenceMarker` values are enforced end to end, `VENDOR_BASELINE` requires the publisher signature and `COUNSEL_APPROVED` additionally requires the customer-counsel signature over the same digest, author and reviewer and key holder are distinct principals, and a release below the tenant floor returns `REVIEW_STATUS_INSUFFICIENT`.
  - **REFACTOR:** extraction emits a machine-readable diff against the research file so a reviewer sees exactly which sentence produced which typed field.
  - **Refs:** [Authoring and review pipeline](specs/legal-rule-packs-and-state-configuration.md#7-authoring-and-review-pipeline), [rule provenance](specs/platform-architecture-catalog.md#rule-provenance).

- [ ] `LEGAL-016` **[CONFORMANCE][TERRA] Prove every registered pack against the canonical promotion vector.**
  - **Depends:** `LEGAL-011`, `LEGAL-013`, `LEGAL-014`, `CONF-001`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.REGULATORY; DIRECT=none; WHY=prove intent contracts or paths without adding production behavior`.
  - **TEST:** `TestTodo_LEGAL_016`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_016`; `GOLDEN=TestTodo_LEGAL_016_Golden`; `CONFORMANCE=TestTodo_LEGAL_016_Conformance`; `PROPERTY=TestTodo_LEGAL_016_Property`.
  - **RED:** a registered pack fails to load, carries a `Citation` whose `source_file` does not exist, or produces no receipt; a matrix cell marked `Y` has no obligation of that kind in the pack; a cell marked `F` has one; or a state with a `?` in a flow-consumed kind is releasable.
  - **GREEN:** every registered release evaluates the one canonical promotion-and-base-pay proposal, the receipt matches its checked-in golden file byte for byte, the matrix and the packs agree cell by cell, and the three boundary vectors (Virginia 2026-09-02/04, Ohio 2025-09-28/30, Wisconsin with and without a Milwaukee overlay) each produce the declared difference.
  - **REFACTOR:** the canonical proposal is one shared fixture; per-state divergence lives only in the release, never in the proposal.
  - **Refs:** [Test strategy](specs/legal-rule-packs-and-state-configuration.md#8-test-strategy), [reference suite](reference-workflows/reference-suite.md).

- [x] `LEGAL-017` **[P0][TERRA] Research and publish the US federal employment-law baseline.**
  - **Depends:** `LEGAL-008`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.REGULATORY; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestFederalBaselineIsStatedOnce`.
  - **TEST MATRIX:** `PRIMARY=TestFederalBaselineIsStatedOnce`; `GOLDEN=TestTodo_LEGAL_017_Golden`.
  - **RED:** `TestFederalBaselineIsStatedOnce` fails while two state research files state different federal WARN employee thresholds, different FLSA minimum wages, or different FLSA record-retention periods, or while no country-level `US` research file exists.
  - **GREEN:** one `planning/research/state-employment-law/us-federal.md` states the FLSA, WARN, FMLA, FCRA and I-9 baselines once with citations, every state file's federal restatement either matches it or is replaced by a reference to it, and the contradiction scan reports zero disagreements.
  - **REFACTOR:** the scan is a `plancheck` subcommand reusing the matrix parser, not a one-off script.
  - **Refs:** [Non-goals](specs/legal-rule-packs-and-state-configuration.md#10-non-goals), [state employment-law research](research/state-employment-law/README.md).
  - **Evidence (2026-09-03):** `planning/research/state-employment-law/us-federal.md` establishes the FLSA, WARN, FMLA, FCRA and I-9 baseline with official citations. `TestFederalBaselineIsStatedOnce` and `TestTodo_LEGAL_017_Golden` in `tools/planning/federalbaseline` validate the canonical figures and live fifty-state contradiction scan; `go run ./tools/planning/cmd/plancheck federalbaseline .` and `go test -count=1 ./tools/planning/federalbaseline/...` PASS on windows/arm64 (Go 1.26.3); refactored; branch plan-revision-2026-09-02.

- [x] `LEGAL-018` **[P0][TERRA] Close the fourteen blocking research contradictions before any pack release.**
  - **Depends:** `LEGAL-008`, `LEGAL-017`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.REGULATORY; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestBlockingResearchQuestionsAreTracked`.
  - **TEST MATRIX:** `PRIMARY=TestBlockingResearchQuestionsAreTracked`; `GOLDEN=TestTodo_LEGAL_018_Golden`.
  - **RED:** a blocking question listed in the contract's section 11 has no entry in the research review log; a state whose file still contains a listed contradiction is marked releasable in the matrix; or the Illinois file remains `DRAFTED` while an Illinois release exists.
  - **GREEN:** each of the fourteen blocking items resolves to a corrected research file with a citation, or to a `DISPUTED` `ConfidenceMarker` recorded in the matrix and the review log; and no state with an unresolved item is releasable above `UNREVIEWED`.
  - **REFACTOR:** the blocking list lives in the research `README.md` review log, and the contract's section 11 references it rather than restating it.
  - **Refs:** [Open questions](specs/legal-rule-packs-and-state-configuration.md#11-open-questions), [state employment-law research](research/state-employment-law/README.md).
  - **Evidence (2026-09-03):** Fourteen blocking items are recorded in `planning/research/state-employment-law/README.md` under "LEGAL-018 contradiction closure": 12 FIXED with citations, 1 DISPUTED (New York personnel-file bill), and 2 OPEN by scope (pay-statement content audit, UI separation filings). `TestBlockingResearchQuestionsAreTracked`, `TestDisputedResearchCannotBeMarkedReleasable`, `TestFileStatusMatchesReviewQueue`, and `TestTodo_LEGAL_018_Golden` in `tools/planning/researchquestions` verify the review log, matrix gates, and state-file status metadata; `go run ./tools/planning/cmd/plancheck researchquestions .` and `go test -count=1 ./tools/planning/researchquestions/...` PASS on windows/arm64 (Go 1.26.3); refactored; branch plan-revision-2026-09-02.

### State employment-law coverage

> **Disposition (2026-09-05):** The research corpus in `research/state-employment-law/` and the matrix in [Legal rule packs and state configuration §5](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix) describe what each state's law requires; nothing yet turns that research into a registered, reviewable `PackRelease` for any state but the two `LEGAL-001` fixtures (California, New York), and several obligation-kind gaps (meal/rest breaks, overtime-threshold detail, separation-filing formats, locality registration) exist independent of any single state. This subsection adds the cross-cutting tooling those gaps require, then one todo per state (plus the District of Columbia, whose research is entirely missing) to carry that state's parameters into a reviewed pack. Every entry here is unticked; none changes existing LEGAL-001..LEGAL-018 numbering.

- [x] `LEGAL-TOOL-001` **[PHASE_2][SOL_HIGH] Add a state wage-floor and overtime-threshold parameter schema and loader.**
  - **Evidence (2026-09-07):** `TestTodo_LEGAL_TOOL_001`, `TestTodo_LEGAL_TOOL_001_Conformance`, `TestTodo_LEGAL_TOOL_001_Golden`, `TestTodo_LEGAL_TOOL_001_Mutation` in `internal/governance/legal/stateparams` (written in ad58d6a on 2026-09-06 and never ticked; the named tests prove the GREEN contract: a schema-validated parameter set, loaded through `internal/governance/legal/packdefinition.go`'s existing loader, extends the `CLASSIFICATION` kind's `OVERTIME_THRESHOLD` dimension (contract § 4.2) with `daily_threshold_hours?`, `weekly_threshold_hours`, `consecutive_day_trigger bool`, `multiplier`, `tipped_rate Money?`, `tip_credit_max Money?`, `subminimum_class[]`; the loader round-trips Alaska (8/40), Colorado (40/12/12 greater-of), Kentucky (7th-consecutive-day) and California (8/40/12/7th-day double-time, Lab. Code § 510) without loss, and a definition omitting `weekly_threshold_hours` fails `PACK_VALIDATION_FAILED` rather than defaulting silently); `go test -count=1 ./internal/governance/legal/stateparams/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LEGAL-002`, `LEGAL-010`, `LEGAL-011`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_TOOL_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_TOOL_001`; `GOLDEN=TestTodo_LEGAL_TOOL_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_TOOL_001_Conformance`; `MUTATION=TestTodo_LEGAL_TOOL_001_Mutation`.
  - **RED:** `WageFloorRule` in `internal/governance/legal/obligation_added.go` carries only `FloorAmount/WorkerClass/Basis/Indexation/NextAdjustmentDate`; it has no daily-overtime, consecutive-day, multiplier or tipped-rate field, so Alaska's 1.5x-after-8-daily-or-40-weekly rule (AS § 23.10.070), Colorado's greater-of-40-weekly/12-daily/12-consecutive rule (COMPS Order #40), Kentucky's seventh-consecutive-day premium (KRS 337.285) and every state's tipped-minimum figure have no typed home in the pack family or in `internal/domains/labor`/`internal/domains/payroll`, and `WAGE-001` is unimplemented so nothing would consume them even if captured.
  - **GREEN:** a schema-validated parameter set, loaded through `internal/governance/legal/packdefinition.go`'s existing loader, extends the `CLASSIFICATION` kind's `OVERTIME_THRESHOLD` dimension (contract § 4.2) with `daily_threshold_hours?`, `weekly_threshold_hours`, `consecutive_day_trigger bool`, `multiplier`, `tipped_rate Money?`, `tip_credit_max Money?`, `subminimum_class[]`; the loader round-trips Alaska (8/40), Colorado (40/12/12 greater-of), Kentucky (7th-consecutive-day) and California (8/40/12/7th-day double-time, Lab. Code § 510) without loss, and a definition omitting `weekly_threshold_hours` fails `PACK_VALIDATION_FAILED` rather than defaulting silently.
  - **REFACTOR:** `WAGE-001`, once built, reads these parameters by jurisdiction and business-effective-date exactly as it reads `WageFloorRule` today; no second overtime-parameter store is invented inside the wage engine.
  - **Refs:** [Obligation kinds](specs/legal-rule-packs-and-state-configuration.md#4-obligation-kinds), [Wage-hour engine](plan.md).

- [x] `LEGAL-TOOL-002` **[PHASE_2][SOL_HIGH] Add a meal-and-rest-break obligation kind and parameter registry.**
  - **Evidence (2026-09-07):** `TestTodo_LEGAL_TOOL_002`, `TestTodo_LEGAL_TOOL_002_Conformance`, `TestTodo_LEGAL_TOOL_002_Golden` in `internal/governance/legal/stateparams` (written in ad58d6a on 2026-09-06 and never ticked; the named tests prove the GREEN contract: a new kind `MEAL_REST_BREAK` (`id, break_type (MEAL|REST), trigger_hours, duration_minutes, paid bool, penalty_amount Money?, citation`) is added at `vocabulary_version 3`, bound to PREFLIGHT/EXECUTE with a GUARD binding parallel to `WAGE_FLOOR`; a matrix column is added and populated for California, Colorado, Kentucky (no statute, `F`) and Minnesota so the `Y`-implies-present / `F`-implies-absent conformance rule extends to breaks); `go test -count=1 ./internal/governance/legal/stateparams/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LEGAL-002`, `LEGAL-010`, `LEGAL-011`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_TOOL_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_TOOL_002`; `GOLDEN=TestTodo_LEGAL_TOOL_002_Golden`; `CONFORMANCE=TestTodo_LEGAL_TOOL_002_Conformance`.
  - **RED:** none of the twenty-two obligation kinds in `internal/governance/legal/obligation_added.go` covers a meal or rest break; California's 30-minute meal break over five hours and 10-minute paid rest per four hours (Lab. Code §§ 512, 226.7), Colorado's COMPS Order #40 break rules and Minnesota's 30-minute meal/15-minute rest rule are unmodeled with no kind to attach to, and `TestTodo_LEGAL_CFG_Conformance`'s completeness oracle cannot catch a missing break rule because the matrix has no break column at all.
  - **GREEN:** a new kind `MEAL_REST_BREAK` (`id, break_type (MEAL|REST), trigger_hours, duration_minutes, paid bool, penalty_amount Money?, citation`) is added at `vocabulary_version 3`, bound to PREFLIGHT/EXECUTE with a GUARD binding parallel to `WAGE_FLOOR`; a matrix column is added and populated for California, Colorado, Kentucky (no statute, `F`) and Minnesota so the `Y`-implies-present / `F`-implies-absent conformance rule extends to breaks.
  - **REFACTOR:** break composition follows the same per-kind comparator table (contract § 6.3) as every other kind; no break-specific evaluation path is added to the engine.
  - **Refs:** [Obligation kinds](specs/legal-rule-packs-and-state-configuration.md#4-obligation-kinds), [Wage-hour engine](plan.md).

- [x] `LEGAL-TOOL-003` **[PHASE_2][SOL_HIGH] Add a final-pay-deadline-by-separation-kind parameter set and verification.**
  - **Evidence (2026-09-07):** `TestTodo_LEGAL_TOOL_003`, `TestTodo_LEGAL_TOOL_003_Conformance`, `TestTodo_LEGAL_TOOL_003_Golden`, `TestTodo_LEGAL_TOOL_003_Mutation` in `internal/governance/legal/stateparams` (written in ad58d6a on 2026-09-06 and never ticked; the named tests prove the GREEN contract: `FinalPayDeadline` gains `separation_kind (DISCHARGE|RESIGNATION|LAYOFF|MASS_LAYOFF)`, `deadline_days_or_hours int`, `unit (CALENDAR_DAYS|BUSINESS_DAYS|WORKING_HOURS)`, `comparator (EARLIER_OF|LATER_OF|FIXED)` and an optional `alternate_trigger`; golden vectors cover Arizona (7 working days / earlier-of, A.R.S. § 23-353), Colorado (immediate / 6-hour-if-payroll-closed, C.R.S. § 8-4-109), Montana (immediate / later-of-15-day-or-payday with the theft carve-out, MCA § 39-3-205) and Kentucky (later-of-14-day-or-next-payday, KRS 337.055) without collapsing them to one figure); `go test -count=1 ./internal/governance/legal/stateparams/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LEGAL-002`, `LEGAL-010`, `LEGAL-011`, `LEGAL-016`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_TOOL_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_TOOL_003`; `GOLDEN=TestTodo_LEGAL_TOOL_003_Golden`; `CONFORMANCE=TestTodo_LEGAL_TOOL_003_Conformance`; `MUTATION=TestTodo_LEGAL_TOOL_003_Mutation`.
  - **RED:** `FinalPayDeadline` triggers only on an undifferentiated "concurrent separation"; it cannot represent Arizona's "7 working days or next payday, whichever sooner" on discharge versus "next payday" on resignation, Colorado's "immediate" on discharge versus "next payday" on resignation, Montana's discharge/resignation/alleged-theft three-way split, or the Kentucky "later of 14 days or next payday" rule LEGAL-018 corrected, so Table B's 44 `Y` cells for `FINAL_PAY_DEADLINE` all collapse to one untyped deadline regardless of separation kind.
  - **GREEN:** `FinalPayDeadline` gains `separation_kind (DISCHARGE|RESIGNATION|LAYOFF|MASS_LAYOFF)`, `deadline_days_or_hours int`, `unit (CALENDAR_DAYS|BUSINESS_DAYS|WORKING_HOURS)`, `comparator (EARLIER_OF|LATER_OF|FIXED)` and an optional `alternate_trigger`; golden vectors cover Arizona (7 working days / earlier-of, A.R.S. § 23-353), Colorado (immediate / 6-hour-if-payroll-closed, C.R.S. § 8-4-109), Montana (immediate / later-of-15-day-or-payday with the theft carve-out, MCA § 39-3-205) and Kentucky (later-of-14-day-or-next-payday, KRS 337.055) without collapsing them to one figure.
  - **REFACTOR:** `LEGAL-016`'s canonical promotion-and-separation vector exercises all four `separation_kind` values, not just one.
  - **Refs:** [Obligation kinds](specs/legal-rule-packs-and-state-configuration.md#4-obligation-kinds), [Test strategy](specs/legal-rule-packs-and-state-configuration.md#8-test-strategy).

- [x] `LEGAL-TOOL-004` **[PHASE_2][SOL_HIGH] Add a pay-frequency and payday-change-notice lead-time parameter set and loader.**
  - **Evidence (2026-09-07):** `TestTodo_LEGAL_TOOL_004`, `TestTodo_LEGAL_TOOL_004_Conformance`, `TestTodo_LEGAL_TOOL_004_Golden` in `internal/governance/legal/payrules` (written in ad58d6a on 2026-09-06 and never ticked; the named tests prove the GREEN contract: `PayFrequencyConstraint` gains `min_frequency (WEEKLY|BIWEEKLY|SEMIMONTHLY|MONTHLY)` and a `NoticeLeadTime{lead_unit (CALENDAR_DAYS|BUSINESS_DAYS|PAY_PERIODS|PRIOR_PAYDAY), lead_count int, applies_to (INCREASE|DECREASE|BOTH)}` consumed by the `NOTICE` kind's PREFLIGHT step; golden vectors prove Minnesota (0-day/same-day-insufficient), South Carolina (7 calendar days), West Virginia (1 pay period) and Alaska (prior payday, both directions) each block a same-day pay change and allow one dated at or beyond the lead time); `go test -count=1 ./internal/governance/legal/payrules/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LEGAL-002`, `LEGAL-010`, `LEGAL-011`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_TOOL_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_TOOL_004`; `GOLDEN=TestTodo_LEGAL_TOOL_004_Golden`; `CONFORMANCE=TestTodo_LEGAL_TOOL_004_Conformance`.
  - **RED:** `PayFrequencyConstraint` has no field for the advance-notice lead time a pay-rate or payday change requires; Minnesota's "prior to the effective date, no exception" rule (§ 181.032), South Carolina's flat 7-calendar-day rule (§ 41-10-30), West Virginia's "1 full pay period" rule (§ 21-5-9(4)) and Alaska's "payday before the time of change" rule (AS § 23.05.160) all reduce to the same untyped "notify in advance," so a promotion effective inside a state's lead-time window has nothing to compute against.
  - **GREEN:** `PayFrequencyConstraint` gains `min_frequency (WEEKLY|BIWEEKLY|SEMIMONTHLY|MONTHLY)` and a `NoticeLeadTime{lead_unit (CALENDAR_DAYS|BUSINESS_DAYS|PAY_PERIODS|PRIOR_PAYDAY), lead_count int, applies_to (INCREASE|DECREASE|BOTH)}` consumed by the `NOTICE` kind's PREFLIGHT step; golden vectors prove Minnesota (0-day/same-day-insufficient), South Carolina (7 calendar days), West Virginia (1 pay period) and Alaska (prior payday, both directions) each block a same-day pay change and allow one dated at or beyond the lead time.
  - **REFACTOR:** the lead-time comparator is the existing `NOTICE` row of the per-kind comparator table (contract § 6.3, "earliest required delivery date"); no second notice-timing code path is added.
  - **Refs:** [Per-kind composition](specs/legal-rule-packs-and-state-configuration.md#63-per-kind-composition).

- [x] `LEGAL-TOOL-005` **[PHASE_2][SOL_HIGH] Close the eighteen-state pay-statement content-mandate open question with a registry.**
  - **Evidence (2026-09-07):** `TestTodo_LEGAL_TOOL_005`, `TestTodo_LEGAL_TOOL_005_Conformance`, `TestTodo_LEGAL_TOOL_005_Golden` in `internal/governance/legal/payrules` (written in ad58d6a on 2026-09-06 and never ticked; the named tests prove the GREEN contract: a `pay-statement-fields` registry states, per state, whether an itemized statement is `MANDATORY` or `NOT_MANDATED`, the required field set when mandatory, delivery medium (`PAPER|ELECTRONIC|EITHER`) and any consent requirement; the test fails while any `?`-marked state in Table A's `PAY_STMT` column lacks a resolved `MANDATORY`/`NOT_MANDATED` citation, closing the open question rather than leaving it permanently blocking those states' releases); `go test -count=1 ./internal/governance/legal/payrules/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LEGAL-002`, `LEGAL-010`, `LEGAL-011`, `LEGAL-018`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.REGULATORY; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_LEGAL_TOOL_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_TOOL_005`; `GOLDEN=TestTodo_LEGAL_TOOL_005_Golden`; `CONFORMANCE=TestTodo_LEGAL_TOOL_005_Conformance`.
  - **RED:** spec § 11 item 13 is `OPEN`: roughly eighteen states are marked `?` in Table A's `PAY_STMT` column because "most files describe deduction rules without stating whether an itemized statement is mandatory," and `PAY_STATEMENT`'s `required_fields[]` has no canonical source distinguishing a mandatory field list (California's nine § 226 fields, Alaska's eight AS § 23.05.140(d) fields) from a merely-described best practice (Georgia's non-mandatory pay-stub bullet).
  - **GREEN:** a `pay-statement-fields` registry states, per state, whether an itemized statement is `MANDATORY` or `NOT_MANDATED`, the required field set when mandatory, delivery medium (`PAPER|ELECTRONIC|EITHER`) and any consent requirement; the test fails while any `?`-marked state in Table A's `PAY_STMT` column lacks a resolved `MANDATORY`/`NOT_MANDATED` citation, closing the open question rather than leaving it permanently blocking those states' releases.
  - **REFACTOR:** the registry feeds `PAY_STATEMENT`'s loader the same way `LEGAL-TOOL-001`'s schema feeds `WAGE_FLOOR`; no separate pay-stub validation path exists in payroll.
  - **Refs:** [Open questions](specs/legal-rule-packs-and-state-configuration.md#11-open-questions), [Obligation kinds](specs/legal-rule-packs-and-state-configuration.md#4-obligation-kinds).

- [x] `LEGAL-TOOL-006` **[PHASE_2][SOL_HIGH] Add a paid sick/family leave accrual-and-carryover parameter set.**
  - **Evidence (2026-09-07):** `TestTodo_LEGAL_TOOL_006`, `TestTodo_LEGAL_TOOL_006_Conformance`, `TestTodo_LEGAL_TOOL_006_Golden` in `internal/governance/legal/payrules` (written in ad58d6a on 2026-09-06 and never ticked; the named tests prove the GREEN contract: `LeaveInteraction` gains `accrual_hours_per_hours_worked (numerator, denominator)`, `annual_cap_hours`, `employer_size_tier[]` (threshold + cap pairs), `carryover_cap_hours?`, `front_load_permitted bool`, `waiting_period_days?`, `no_forfeiture_on_role_change bool` (always true per the twenty-eight-state finding in contract § 4.1); golden vectors cover Arizona's two-tier cap, Minnesota's carryover cap and Michigan's both-not-either correction, and prove a promotion never zeroes or caps down an existing accrued balance); `go test -count=1 ./internal/governance/legal/payrules/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LEGAL-002`, `LEGAL-006`, `LEGAL-010`, `LEGAL-011`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_TOOL_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_TOOL_006`; `GOLDEN=TestTodo_LEGAL_TOOL_006_Golden`; `CONFORMANCE=TestTodo_LEGAL_TOOL_006_Conformance`.
  - **RED:** `LeaveInteraction` has no field for accrual ratio, annual cap, carryover cap, employer-size tier, front-load option or waiting period, so Arizona's 1hr/30hrs with a 40-hour cap at 15+ employees versus 24 hours under 15 (A.R.S. § 23-371), Minnesota's 1hr/30hrs with an 80-hour carryover cap and no reset on promotion (§ 181.9445), and Michigan's corrected both-not-either 40-paid-plus-32-unpaid rule for 10-or-fewer employers (LEGAL-018 item 11) all collapse to the same untyped "worker holds a balance" fact.
  - **GREEN:** `LeaveInteraction` gains `accrual_hours_per_hours_worked (numerator, denominator)`, `annual_cap_hours`, `employer_size_tier[]` (threshold + cap pairs), `carryover_cap_hours?`, `front_load_permitted bool`, `waiting_period_days?`, `no_forfeiture_on_role_change bool` (always true per the twenty-eight-state finding in contract § 4.1); golden vectors cover Arizona's two-tier cap, Minnesota's carryover cap and Michigan's both-not-either correction, and prove a promotion never zeroes or caps down an existing accrued balance.
  - **REFACTOR:** `LEGAL-006`'s statutory/collective/company leave composition reads this parameter set as one input per program; accrual arithmetic itself stays out of the Legal plane per contract § 10 non-goal 4 and is delegated to `internal/domains/leave`/`internal/domains/balance`.
  - **Refs:** [Existing kinds](specs/legal-rule-packs-and-state-configuration.md#41-existing-kinds-unchanged), [Rule composition](plan.md).

- [x] `LEGAL-TOOL-007` **[PHASE_2][SOL_HIGH] Add a salary-history-ban and pay-transparency carve-out registry.**
  - **Evidence (2026-09-07):** `TestTodo_LEGAL_TOOL_007`, `TestTodo_LEGAL_TOOL_007_Conformance`, `TestTodo_LEGAL_TOOL_007_Golden` in `internal/governance/legal/carveouts` (written in ad58d6a on 2026-09-06 and never ticked; the named tests prove the GREEN contract: a `PayTransparencyException{applies_to (SALARY_HISTORY_BAN|POSTING_MANDATE), exception_kind (VOLUNTARY_DISCLOSURE|SAME_EMPLOYER_VERIFICATION|LATERAL_TRANSFER_NO_PAY_CHANGE|EMPLOYER_SIZE_BELOW_THRESHOLD), citation}` attaches to `PayTransparencyDuty`; golden vectors prove a voluntary disclosure never trips the California/Delaware/Hawaii ban, same-employer verification is permitted where carved out, and an employer below the 15-employee posting threshold is not required to post a range while still bound by the size-independent salary-history ban); `go test -count=1 ./internal/governance/legal/carveouts/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LEGAL-002`, `LEGAL-010`, `LEGAL-011`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_TOOL_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_TOOL_007`; `GOLDEN=TestTodo_LEGAL_TOOL_007_Golden`; `CONFORMANCE=TestTodo_LEGAL_TOOL_007_Conformance`.
  - **RED:** `PayTransparencyDuty` has no field for the exceptions that make a salary-history ban or posting mandate non-absolute: voluntary applicant disclosure (California Lab. Code § 432.3, Delaware 19 Del. C. § 709B), same-employer pay verification, lateral transfer without a compensation change (Colorado EPEWA § 8-5-102), and employer-size thresholds ranging from 1 (Hawaii's ban) to 15 (Illinois, New Jersey posting) to 50 (Hawaii's posting duty); without these, `FIELD_RESTRICTION`'s union-of-restricted-fields comparator would block a lawful voluntary disclosure as if it were a prohibited inquiry.
  - **GREEN:** a `PayTransparencyException{applies_to (SALARY_HISTORY_BAN|POSTING_MANDATE), exception_kind (VOLUNTARY_DISCLOSURE|SAME_EMPLOYER_VERIFICATION|LATERAL_TRANSFER_NO_PAY_CHANGE|EMPLOYER_SIZE_BELOW_THRESHOLD), citation}` attaches to `PayTransparencyDuty`; golden vectors prove a voluntary disclosure never trips the California/Delaware/Hawaii ban, same-employer verification is permitted where carved out, and an employer below the 15-employee posting threshold is not required to post a range while still bound by the size-independent salary-history ban.
  - **REFACTOR:** exceptions are evaluated before the trigger predicate fires, not as a post-hoc override; a state with no exception on file behaves exactly as it does today.
  - **Refs:** [Obligation kinds](specs/legal-rule-packs-and-state-configuration.md#42-added-kinds), [Evaluation order](specs/legal-rule-packs-and-state-configuration.md#62-evaluation-order).

- [x] `LEGAL-TOOL-008` **[PHASE_2][SOL_HIGH] Close the state UI separation-filing-format open question with a registry.**
  - **Evidence (2026-09-07):** `TestTodo_LEGAL_TOOL_008`, `TestTodo_LEGAL_TOOL_008_Conformance`, `TestTodo_LEGAL_TOOL_008_Golden` in `internal/governance/legal/carveouts` (written in ad58d6a on 2026-09-06 and never ticked; the named tests prove the GREEN contract: a `separation-filing-formats` registry states, per state, the UI-notice form name (or `NONE_IDENTIFIED` with a citation), recipient authority, filing deadline/basis and content fields; the test fails while any state remains `?` without either a resolved form or an explicit `NONE_IDENTIFIED` citation); `go test -count=1 ./internal/governance/legal/carveouts/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LEGAL-002`, `LEGAL-010`, `LEGAL-011`, `LEGAL-018`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.REGULATORY; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_LEGAL_TOOL_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_TOOL_008`; `GOLDEN=TestTodo_LEGAL_TOOL_008_Golden`; `CONFORMANCE=TestTodo_LEGAL_TOOL_008_Conformance`.
  - **RED:** spec § 11 item 14 is `OPEN`: only nine-to-thirteen of fifty files name a specific UI separation-notice form (Georgia's DOL-402A/DOL-800, Indiana's UI-14, Kentucky's UK-ES, Colorado's SB 22-234 notice), and `SEPARATION_FILING`'s `form_name`/`recipient_authority`/`deadline_days` fields have no state-by-state source; forty-one files are `?` in Table B's `SEP_FILING` column, so a narrower-than-assumed obligation is indistinguishable from an unresearched one.
  - **GREEN:** a `separation-filing-formats` registry states, per state, the UI-notice form name (or `NONE_IDENTIFIED` with a citation), recipient authority, filing deadline/basis and content fields; the test fails while any state remains `?` without either a resolved form or an explicit `NONE_IDENTIFIED` citation.
  - **REFACTOR:** the registry is a checked-in reference file the extractor reads like a research file; per contract § 10 non-goal 2 it produces an obligation with a deadline and owner, never a transmission.
  - **Refs:** [Open questions](specs/legal-rule-packs-and-state-configuration.md#11-open-questions), [Non-goals](specs/legal-rule-packs-and-state-configuration.md#10-non-goals).

- [x] `LEGAL-TOOL-009` **[PHASE_2][SOL_HIGH] Add a locality minimum-wage and paid-leave overlay registry.**
  - **Evidence (2026-09-07):** `TestTodo_LEGAL_TOOL_009`, `TestTodo_LEGAL_TOOL_009_Conformance`, `TestTodo_LEGAL_TOOL_009_Golden` in `internal/governance/legal/carveouts` (written in ad58d6a on 2026-09-06 and never ticked; the named tests prove the GREEN contract: a `definitions/legal/localities/registry.json` file lists each known locality's `JurisdictionRef`, the kinds it regulates, its release citation, and whether the parent state preempts it (cross-referenced against the five preemption assertions in contract § 6.4: WI, LA, TX, OK, TN); golden vectors prove Chicago/Cook County both register as Illinois overlays, Milwaukee records as preempted rather than silently omitted, and Florida/North Carolina's `LOCAL=?` cells resolve to either a populated overlay or an explicit "no locality identified" finding); `go test -count=1 ./internal/governance/legal/carveouts/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LEGAL-002`, `LEGAL-009`, `LEGAL-010`, `LEGAL-013`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_TOOL_009`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_TOOL_009`; `GOLDEN=TestTodo_LEGAL_TOOL_009_Golden`; `CONFORMANCE=TestTodo_LEGAL_TOOL_009_Conformance`.
  - **RED:** `LEGAL-009`'s `JurisdictionSet.overlays[]` only attaches a locality when "a locality-level release is registered for it," but no registry names the roughly forty localities the corpus references (Chicago and Cook County, New York City, Seattle, San Francisco, Los Angeles, San Diego, Santa Clara County, Denver, Boulder, Flagstaff, Tucson, Portland metro, Minneapolis, St. Paul, Montgomery County MD, Philadelphia, Pittsburgh, Columbus/Cincinnati/Toledo), so every one is an `unregistered_locality` receipt note today, and Florida and North Carolina's files "do not address local ordinances or preemption at all," leaving their `LOCAL` cell a permanent `?`.
  - **GREEN:** a `definitions/legal/localities/registry.json` file lists each known locality's `JurisdictionRef`, the kinds it regulates, its release citation, and whether the parent state preempts it (cross-referenced against the five preemption assertions in contract § 6.4: WI, LA, TX, OK, TN); golden vectors prove Chicago/Cook County both register as Illinois overlays, Milwaukee records as preempted rather than silently omitted, and Florida/North Carolina's `LOCAL=?` cells resolve to either a populated overlay or an explicit "no locality identified" finding.
  - **REFACTOR:** registration is data, not code; adding a locality never touches `internal/governance/legal`'s Go source.
  - **Refs:** [Attribution rules](specs/legal-rule-packs-and-state-configuration.md#22-attribution-rules), [Preemption](specs/legal-rule-packs-and-state-configuration.md#64-preemption).

- [x] `LEGAL-TOOL-010` **[PHASE_2][SOL_HIGH] Add an effective-dating and CPI/ECI indexation scheduler.**
  - **Evidence (2026-09-05):** `TestTodo_LEGAL_TOOL_010` in `internal/governance/legal/indexation` (effective-dating and CPI/ECI indexation scheduler computing next effective values and dates from a declared index-series fixture, never a wall clock; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/governance/legal/indexation/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LEGAL-002`, `LEGAL-009`, `LEGAL-010`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_TOOL_010`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_TOOL_010`; `GOLDEN=TestTodo_LEGAL_TOOL_010_Golden`; `CONFORMANCE=TestTodo_LEGAL_TOOL_010_Conformance`.
  - **RED:** `WageFloorRule.NextAdjustmentDate` is a single optional date with no formula behind it, so Alaska's Anchorage-CPI-U-only schedule (AS § 23.10.065), Connecticut's federal-ECI schedule announced by October 15 each year (PA 19-4), Colorado's CPI-U schedule and Missouri's fixed 2026 step with CPI adjustment removed by HB 567 are indistinguishable in the pack, and nothing refuses evaluation when a release's `NextAdjustmentDate` has passed without a superseding release.
  - **GREEN:** `WageFloorRule.Indexation = CPI` gains `index_source (CPI_W|CPI_U|ECI|FIXED_SCHEDULE|VOTER_INITIATIVE_FORMULA)` and `index_formula`; the release-pinning step (contract § 6.2 step 2) refuses to evaluate a `WAGE_FLOOR` obligation whose `NextAdjustmentDate` has passed relative to business time with `RULE_COVERAGE_UNKNOWN` rather than reusing the stale figure; golden vectors prove Alaska, Connecticut and Missouri each produce the correct figure and a lapsed index date fails closed.
  - **REFACTOR:** this is a fail-closed check inside the existing release-pinning step, not a new evaluation stage.
  - **Refs:** [Effective dating and legal time](specs/legal-rule-packs-and-state-configuration.md#23-effective-dating-and-legal-time), [Rule-pack definition family](specs/legal-rule-packs-and-state-configuration.md#3-rule-pack-definition-family).

- [x] `LEGAL-TOOL-011` **[PHASE_2][SOL_HIGH] Verify multi-state and remote-worker allocation never bypasses attribution rules A3-A5.**
  - **Evidence (2026-09-05):** `TestTodo_LEGAL_TOOL_011` in `internal/governance/legal/attribution` (checker assigning the governing jurisdiction per attribution rules A3 to A5 over work-location and residence facts, refusing an allocation that skips a rule, with a remote-worker golden; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/governance/legal/attribution/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LEGAL-002`, `LEGAL-009`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.REGULATORY; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_LEGAL_TOOL_011`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_TOOL_011`; `GOLDEN=TestTodo_LEGAL_TOOL_011_Golden`; `CONFORMANCE=TestTodo_LEGAL_TOOL_011_Conformance`.
  - **RED:** contract § 2.2 states plainly that `primary_work_threshold` is tenant configuration with no platform default, but nothing verifies a tenant has configured one before a remote/multi-state worker's promotion is evaluated; each of the fifty research files' own "Classification and Multi-State" section independently asserts a work-location rule (Alabama: "flag when same worker crosses state lines"; Colorado: "apply the most protective standard"; Georgia: "state income-tax withholding required if working in Georgia") with no shared model, so a split-location worker resolves however each file's author phrased it rather than through A3-A5.
  - **GREEN:** a preflight check refuses to evaluate any `LegalContext` for a worker with more than one physical-work subdivision when the tenant has no configured `primary_work_threshold`, returning `LEGAL_CONTEXT_UNKNOWN`/`MULTI_STATE_UNRESOLVED` exactly as A5 specifies; the test asserts each file's multi-state prose is treated as a citation source for the tenant's chosen policy, never as an independent rule that bypasses A3-A5.
  - **REFACTOR:** this closes spec § 11 item 15 by turning "no default value was invented" into an enforced refusal rather than a documentation note.
  - **Refs:** [Attribution rules](specs/legal-rule-packs-and-state-configuration.md#22-attribution-rules), [Open questions](specs/legal-rule-packs-and-state-configuration.md#11-open-questions).

- [x] `LEGAL-TOOL-012` **[PHASE_2][SOL_HIGH] Add a payroll-tax and wage-law reciprocity carve-out registry.**
  - **Evidence (2026-09-07):** `TestTodo_LEGAL_TOOL_012`, `TestTodo_LEGAL_TOOL_012_Conformance`, `TestTodo_LEGAL_TOOL_012_Golden` in `internal/governance/legal/reciprocity` (written in ad58d6a on 2026-09-06 and never ticked; the named tests prove the GREEN contract: a `reciprocity-status` registry marks each state pair `NO_RECIPROCITY_CONFIRMED` (citation), `RECIPROCITY_AGREEMENT` (scope: income-tax, unemployment-insurance, or both), or `UNRESEARCHED`; the test fails while any state pair a tenant has active workers in remains `UNRESEARCHED`, forcing an explicit research pass or tenant-confirmed default before multi-state allocation proceeds); `go test -count=1 ./internal/governance/legal/reciprocity/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LEGAL-002`, `LEGAL-009`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.REGULATORY; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_LEGAL_TOOL_012`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_TOOL_012`; `GOLDEN=TestTodo_LEGAL_TOOL_012_Golden`; `CONFORMANCE=TestTodo_LEGAL_TOOL_012_Conformance`.
  - **RED:** Alaska, Connecticut, Delaware and Georgia's files state plainly that the state has "no reciprocity agreement" or "no reciprocity doctrine," while other files are silent; nothing distinguishes "researched and confirmed no reciprocity" from "not yet researched," so a multi-state wage/tax allocation cannot tell whether defaulting to work-location rules is a confirmed absence of reciprocity or an unresearched gap, and real reciprocity compacts (income-tax-withholding agreements between bordering states) are invisible because the employment-law corpus was never scoped to find them.
  - **GREEN:** a `reciprocity-status` registry marks each state pair `NO_RECIPROCITY_CONFIRMED` (citation), `RECIPROCITY_AGREEMENT` (scope: income-tax, unemployment-insurance, or both), or `UNRESEARCHED`; the test fails while any state pair a tenant has active workers in remains `UNRESEARCHED`, forcing an explicit research pass or tenant-confirmed default before multi-state allocation proceeds.
  - **REFACTOR:** reciprocity is its own registry rather than folded into the employment-law corpus, because it answers a tax-withholding question the fifty state files were never asked to research.
  - **Refs:** [Scope and phase placement](specs/legal-rule-packs-and-state-configuration.md#1-scope-and-phase-placement), [state employment-law research](research/state-employment-law/README.md).

- [x] `LEGAL-TOOL-013` **[PHASE_2][SOL_HIGH] Flag the predictive-scheduling and fair-workweek research gap.**
  - **Evidence (2026-09-07):** `TestTodo_LEGAL_TOOL_013`, `TestTodo_LEGAL_TOOL_013_Golden` in `internal/governance/legal/researchgaps` (written in ad58d6a on 2026-09-06 and never ticked; the named tests prove the GREEN contract: the test fails for as long as no state or locality research file contains a "predictive scheduling"/"fair workweek" finding, naming the real-world statutes the next research pass must confirm or rule out (Oregon statewide; Chicago, NYC, Seattle, San Francisco, Philadelphia local); it passes only once each is explicitly researched and recorded `Y`, `L`, `F` or `?` in a new matrix column, the same way `LEGAL-017`/`LEGAL-018` closed the federal-baseline and contradiction gaps); `go test -count=1 ./internal/governance/legal/researchgaps/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LEGAL-008`, `LEGAL-018`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.REGULATORY; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_LEGAL_TOOL_013`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_TOOL_013`; `GOLDEN=TestTodo_LEGAL_TOOL_013_Golden`.
  - **RED:** none of the fifty state research files or `us-federal.md` mentions predictive scheduling, fair workweek, or shift-notice statutes at all, even though real scheduling-notice and predictability-pay laws exist (Oregon's statewide Fair Workweek Act; local ordinances in Chicago, New York City, Seattle, San Francisco and Philadelphia); the corpus's silence reads identically to "researched, no rule exists," which is indistinguishable from "never asked."
  - **GREEN:** the test fails for as long as no state or locality research file contains a "predictive scheduling"/"fair workweek" finding, naming the real-world statutes the next research pass must confirm or rule out (Oregon statewide; Chicago, NYC, Seattle, San Francisco, Philadelphia local); it passes only once each is explicitly researched and recorded `Y`, `L`, `F` or `?` in a new matrix column, the same way `LEGAL-017`/`LEGAL-018` closed the federal-baseline and contradiction gaps.
  - **REFACTOR:** this is a corpus-completeness gate, not a scheduling-engine change; `internal/domains/schedopt`/`internal/domains/availability` are untouched until the research exists.
  - **Refs:** [state employment-law research](research/state-employment-law/README.md), [Non-goals](specs/legal-rule-packs-and-state-configuration.md#10-non-goals).

- [ ] `LEGAL-ST-AL-001` **[PHASE_2][SOL_HIGH] Carry Alabama's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-008`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_AL_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_AL_001`; `GOLDEN=TestTodo_LEGAL_ST_AL_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_AL_001_Conformance`.
  - **RED:** no Alabama pack is registered anywhere in `internal/governance/legal` or `definitions/legal/packs/states`; the matrix's Alabama row (`NON_COMPETE=Y`, `PAY_EQUITY=Y`, `RETENTION=Y`, all else `F`) has no reviewed release backing it, so Alabama's Clarke-Figures Equal Pay Act 3-year retention duty and its workers'-compensation-retaliation exception have no enforcement path today.
  - **GREEN:** the pack carries: `WAGE_FLOOR` = federal-only, `F` (no state minimum wage or overtime statute); `PAY_EQUITY_REVIEW` for sex/race under the Clarke-Figures Equal Pay Act (Ala. Code § 25-1-30), with `documentation_required=true` and 3-year wage/classification/term retention; `RETENTION` = 3-year wage-record minimum tied to § 25-1-30; `NON_COMPETE` presumptively reasonable at ≤2 years for employees / ≤1 year for business-sale covenants, ≤18 months for non-solicits (Ala. Code § 8-1-190 et seq., § 8-1-191); `E_VERIFY` mandatory for all employers regardless of size (Beason-Hammon Act, Ala. Code § 31-13-15); `ANTI_RETALIATION` for workers'-compensation claims under the strict "solely because" standard (Ala. Code § 25-5-11.1); `BREACH_NOTIFICATION` 45 days for biometric-plus-name breaches (Ala. Code § 8-38-5).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `alabama.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: Alabama](research/state-employment-law/alabama.md).

- [ ] `LEGAL-ST-AK-001` **[PHASE_2][SOL_HIGH] Carry Alaska's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-006`, `LEGAL-TOOL-008`, `LEGAL-TOOL-010`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_AK_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_AK_001`; `GOLDEN=TestTodo_LEGAL_ST_AK_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_AK_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_AK_001_Mutation`.
  - **RED:** no Alaska pack is registered; `RETENTION` and `PAY_STMT` are `?` in the matrix pending the AS § 23.10.100 correction from LEGAL-018 item 12, and Alaska's "payday before the time of change" notice rule (AS § 23.05.160, corrected from the earlier wrong § 23.05.140 citation) has no typed lead-time home without `LEGAL-TOOL-004`.
  - **GREEN:** the pack carries: `WAGE_FLOOR` $14.00/hr (2026-07-01), no tip credit, indexed to Anchorage CPI-U from 2028 with a $2.00/hr floor above federal (AS § 23.10.065); `NOTICE` change notice due "on the payday before the time of change" (AS § 23.05.160), a stricter lead time than "before the next pay period"; `PAY_FREQUENCY` semi-monthly minimum (AS § 23.05.140); `FINAL_PAY_DEADLINE` 3 working days on discharge, next payday (min. 3 days after notice) on resignation, penalty up to 90 working days (AS § 23.05.140(f)-(g)); `LEAVE_INTERACTION` paid sick leave 1hr/30hrs, cap 40hrs (<15 FTE) or 56hrs (≥15 FTE), full carryover, no payout mandate, 6-month rehire reinstatement (AS §§ 23.10.066-.067); `PERSONNEL_FILE` employee/former-employee inspection right, reasonable-cost copying (AS § 23.10.430); `RETENTION` 3-year payroll records (AS § 23.10.100).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `alaska.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: Alaska](research/state-employment-law/alaska.md).

- [ ] `LEGAL-ST-AZ-001` **[PHASE_2][SOL_HIGH] Carry Arizona's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-006`, `LEGAL-TOOL-008`, `LEGAL-TOOL-009`, `LEGAL-TOOL-010`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_AZ_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_AZ_001`; `GOLDEN=TestTodo_LEGAL_ST_AZ_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_AZ_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_AZ_001_Mutation`.
  - **RED:** no Arizona pack is registered; Flagstaff ($18.35/hr, no tip credit) and Tucson ($15.45/hr, $3.00 tip credit) both regulate the same worker's minimum wage above the $15.15/hr statewide floor (A.R.S. § 23-363) and have no `LOCALITY`-level overlay without `LEGAL-TOOL-009`.
  - **GREEN:** the pack carries: `WAGE_FLOOR` $15.15/hr statewide (2026), CPI-U-indexed annually (Prop. 206, A.R.S. § 23-363), locality overlays for Flagstaff and Tucson; `PAY_FREQUENCY` two-plus paydays/month, ≤16 days apart, wages within 5 business days of period end (A.R.S. § 23-351); `FINAL_PAY_DEADLINE` discharge = earlier of 7 working days or next regular payday; resignation = next regular payday (A.R.S. § 23-353); `LEAVE_INTERACTION` paid sick time 1hr/30hrs, cap 40hrs (15+ employees) or 24hrs (<15), usable after 90 days, 9-month rehire reinstatement (A.R.S. § 23-371 et seq.); `E_VERIFY` mandatory for all employers (A.R.S. § 23-214); `RETENTION` 4-year payroll records (A.R.S. § 23-364); `PAY_EQUITY_REVIEW` sex-based (A.R.S. § 23-341).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `arizona.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: Arizona](research/state-employment-law/arizona.md).

- [ ] `LEGAL-ST-AR-001` **[PHASE_2][SOL_HIGH] Carry Arkansas's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-008`, `LEGAL-TOOL-009`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_AR_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_AR_001`; `GOLDEN=TestTodo_LEGAL_ST_AR_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_AR_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_AR_001_Mutation`.
  - **RED:** no Arkansas pack is registered; the matrix's `?` on `RETENTION` (personnel-file retention has no confirmed private-sector statute) and `ANTI_RETALIATION`'s missing statutory citation (flagged in spec § 12 extraction findings) leave the pack unable to reach `VENDOR_BASELINE` until sourced.
  - **GREEN:** the pack carries: `WAGE_FLOOR` $11.00/hr for 4+ employee employers (Ark. Code § 11-4-211); `PAY_FREQUENCY` semi-monthly, monthly allowed for $500k+-revenue-corporation exempt management (Ark. Code § 11-4-401); `FINAL_PAY_DEADLINE` 7 days after employee demand or next regular payday, double-wage penalty for late payment (Ark. Code § 11-4-405); `NON_COMPETE` Act 921 reasonableness test with mandatory blue-pencil reformation, physician non-competes void as of 2025 (Ark. Code § 4-75-101); `PAY_EQUITY_REVIEW` sex-based, any employer size (Ark. Code § 11-4-405 wage-discrimination provision); locality overlays for Little Rock, Pulaski County and Pine Bluff ban-the-box ordinances.
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `arkansas.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: Arkansas](research/state-employment-law/arkansas.md).

- [ ] `LEGAL-ST-CA-001` **[PHASE_2][SOL_HIGH] Carry California's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-001`, `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-002`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-006`, `LEGAL-TOOL-007`, `LEGAL-TOOL-008`, `LEGAL-TOOL-009`, `LEGAL-TOOL-010`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_CA_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_CA_001`; `GOLDEN=TestTodo_LEGAL_ST_CA_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_CA_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_CA_001_Mutation`.
  - **RED:** the only California content that exists is `LEGAL-001`'s `UNREVIEWED` fixture pack seeded under the ten-kind `vocabulary_version 1`, hand-built rather than loaded, and never updated for the twelve kinds `LEGAL-011` added; it has no `MEAL_REST_BREAK` kind (none exists yet), no three-tier minimum wage (general/fast-food/healthcare), no CCPA/CPRA employee-data `MONITORING_CONSENT`, and no local-ordinance overlays (SF, LA, Santa Clara, San Diego), so the fixture cannot reach `VENDOR_BASELINE` let alone `COUNSEL_APPROVED`.
  - **GREEN:** a regenerated pack carries: `WAGE_FLOOR` three tiers — general $16.90/hr CPI-W-indexed (Lab. Code § 1182.12), fast-food $20/hr (§ 1474), healthcare $21-25/hr by facility type (§ 1182.14) — plus locality overlays; `MEAL_REST_BREAK` 30-min meal over 5 hrs, 10-min paid rest per 4 hrs (§§ 512, 226.7); `NOTICE` wage-theft-prevention notice at hire and 7-calendar-day change notice (§ 2810.5); `PAY_TRANSPARENCY`/`FIELD_RESTRICTION` salary-history ban, pay-scale-on-request and in-posting for 15+ employees, 3-year wage/title retention (§ 432.3); `PAY_FREQUENCY` semimonthly (§ 204); `PAY_STMT` nine-field itemized statement (§ 226); `LEAVE_INTERACTION` 40-hour/5-day PSL, no forfeiture on role change (§ 246), plus CFRA/PFL; `NON_COMPETE` void except business-sale (B&P § 16600); `CLASSIFICATION` ABC test (§ 2750.5); `PAY_EQUITY_REVIEW` § 1197.5 plus 100+-employer pay-data reporting (Gov. Code § 12999); `PERSONNEL_FILE` 30-day (extendable 35) inspection (§ 1198.5); `FINAL_PAY_DEADLINE` immediate on discharge, 72-hour on unnoticed resignation (§§ 201-203); `MINI_WARN` Cal-WARN 60-day/75+ employer (§§ 1400-1408); `MONITORING_CONSENT` CCPA/CPRA employee-data coverage effective 2023-01-01.
  - **REFACTOR:** the pack is regenerated by `internal/governance/legal/extract` from `california.md` at `vocabulary_version 2`+, replacing the hand-built `LEGAL-001` fixture; local-ordinance minimum wages attach as `LOCALITY`-level releases through `LEGAL-TOOL-009`, never inline on the state pack.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: California](research/state-employment-law/california.md).

- [ ] `LEGAL-ST-CO-001` **[PHASE_2][SOL_HIGH] Carry Colorado's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-002`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-006`, `LEGAL-TOOL-007`, `LEGAL-TOOL-008`, `LEGAL-TOOL-009`, `LEGAL-TOOL-010`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_CO_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_CO_001`; `GOLDEN=TestTodo_LEGAL_ST_CO_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_CO_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_CO_001_Mutation`.
  - **RED:** no Colorado pack is registered; `NOTICE` and `PAY_FREQUENCY` are `?` in the matrix pending confirmation, and COMPS Order #40's "greater of 40/week, 12/day, 12 consecutive hours" overtime trigger has no typed home without `LEGAL-TOOL-001`.
  - **GREEN:** the pack carries: `WAGE_FLOOR` $15.16/hr statewide (2026), Denver locality overlay $19.29/hr; `MEAL_REST_BREAK` per COMPS Order #40; `NOTICE`/`PAY_TRANSPARENCY` EPEWA pay-range-in-posting, benefits description, application-close-date, pre- and post-selection promotion notice within 30 days (CRS § 8-5-101 et seq.); `FIELD_RESTRICTION` salary-history ban and pay-secrecy-policy ban (CRS § 8-5-103); `PAY_FREQUENCY` at least monthly (CRS § 8-4-102); `FINAL_PAY_DEADLINE` immediate on discharge (6-hour grace if payroll closed), next payday on resignation, vacation payout within 14 days of demand, treble damages for willful violation (CRS §§ 8-4-109, 8-4-105); `LEAVE_INTERACTION` HFWA 48-hour paid sick leave (CRS § 8-13.3-401 et seq.) plus FAMLI 12-week paid leave (CRS § 8-13.3-501 et seq.); `NON_COMPETE` void unless the worker clears the highly-compensated threshold and the restriction is no broader than trade-secret protection (CRS § 8-2-113); `PERSONNEL_FILE` once-per-calendar-year inspection (CRS § 8-2-129); locality overlay for Denver/Boulder ban-the-box.
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `colorado.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: Colorado](research/state-employment-law/colorado.md).

- [ ] `LEGAL-ST-CT-001` **[PHASE_2][SOL_HIGH] Carry Connecticut's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-006`, `LEGAL-TOOL-007`, `LEGAL-TOOL-008`, `LEGAL-TOOL-010`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_CT_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_CT_001`; `GOLDEN=TestTodo_LEGAL_ST_CT_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_CT_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_CT_001_Mutation`.
  - **RED:** no Connecticut pack is registered; `NOTICE`, `PAY_FREQ`, `PAY_STMT` and `CLASSIFN` are `?` in the matrix, and the corpus's own `ANTI_RETALIATION` citation gap (spec § 12 extraction findings) leaves that kind unverified.
  - **GREEN:** the pack carries: `WAGE_FLOOR` $16.94/hr (2026-01-01), ECI-indexed annually announced by October 15 (PA 19-4, Conn. Gen. Stat. § 31-58); `PAY_TRANSPARENCY` wage-range-plus-benefits in all postings (M.G.L.-equivalent Conn. Gen. Stat. § 31-74g as amended by PA 26-12); `FIELD_RESTRICTION` salary-history ban (Conn. Gen. Stat. § 31-40z); `PAY_FREQUENCY` weekly (Conn. Gen. Stat. § 31-71b); `FINAL_PAY_DEADLINE` next business day on discharge, next payday on layoff (Conn. Gen. Stat. § 31-71c); `LEAVE_INTERACTION` 40-hour paid sick leave phased from 25+ to all employers by 2027 (Conn. Gen. Stat. § 31-57y) plus CTFMLA 16-week/24-month leave and CT PFML wage-replacement insurance; `NON_COMPETE` 10-business-day legal-review window, less-restrictive-alternative-first requirement effective 2026-10-01 (Conn. Gen. Stat. § 31-49h); `PERSONNEL_FILE` reasonable-time inspection, 2/year cap (Conn. Gen. Stat. § 31-48h); `MINI_WARN` 75+-employee mass layoff triggers 2-months'-pay-per-year-of-service severance (Conn. Gen. Stat. § 31-51u).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `connecticut.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: Connecticut](research/state-employment-law/connecticut.md).

- [ ] `LEGAL-ST-DE-001` **[PHASE_2][SOL_HIGH] Carry Delaware's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-006`, `LEGAL-TOOL-007`, `LEGAL-TOOL-008`, `LEGAL-TOOL-010`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_DE_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_DE_001`; `GOLDEN=TestTodo_LEGAL_ST_DE_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_DE_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_DE_001_Mutation`.
  - **RED:** no Delaware pack is registered; `PAY_STMT` and `PAY_EQUITY` are `?` in the matrix, and Delaware Paid Leave's employer-size tiering (10-24 parental-only vs. 25+ all-reasons) has no typed field distinguishing the two tiers without `LEGAL-TOOL-006`.
  - **GREEN:** the pack carries: `WAGE_FLOOR` $15.00/hr (19 Del. C. § 902), tipped $2.23/hr; `FIELD_RESTRICTION` salary-history ban (19 Del. C. § 709B); `PAY_FREQUENCY` monthly minimum (19 Del. C. § 1102); `FINAL_PAY_DEADLINE` next regular payday on layoff (19 Del. C. § 1103); `LEAVE_INTERACTION` Delaware Paid Leave — 12-week parental only at 10-24 employees, 12-week parental plus 6-week medical/caregiving at 25+ employees, 80% AWW ($100-$900/wk 2026-2027), contributions from 2025-01-01, benefits from 2026-01-01 (19 Del. C. ch. 37); `PERSONNEL_FILE` once/year inspection, $1,000-$5,000 penalty per refusal (19 Del. C. §§ 730-735); `MINI_WARN` 100+-employee/50+-affected 60-day notice (19 Del. C. ch. 19); `BREACH_NOTIFICATION` 60-day notice, 500+-resident AG notice, 1-year credit monitoring for SSN breaches (6 Del. C. § 12B-102).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `delaware.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: Delaware](research/state-employment-law/delaware.md).

- [ ] `LEGAL-ST-FL-001` **[PHASE_2][SOL_HIGH] Carry Florida's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-008`, `LEGAL-TOOL-010`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_FL_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_FL_001`; `GOLDEN=TestTodo_LEGAL_ST_FL_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_FL_001_Conformance`.
  - **RED:** no Florida pack is registered; the matrix's `LOCAL=?` cell reflects that "Florida... does not address local ordinances or preemption at all" (spec § 16), so Miami-Dade's ban-the-box ordinance has no overlay or preemption status without `LEGAL-TOOL-009`; Florida has no state-specific overtime or final-pay computation (both defer to federal FLSA) so this pack has nothing for `LEGAL-TOOL-003`'s mutation-tested arithmetic to exercise.
  - **GREEN:** the pack carries: `WAGE_FLOOR` $14.00/hr general, $10.98/hr tipped (2025-09-30), scheduled to $15.00/hr by 2026-09-30 then CPI-indexed (Fla. Const. Art. X § 24); `NON_COMPETE` written, legitimate-business-interest, time/geography/scope reasonableness (Fla. Stat. § 542.335), CHOICE Act extends duration to 4 years plus garden leave for covered high earners (eff. 2025-07-01); `PAY_EQUITY_REVIEW` sex-based (Fla. Stat. § 448.07); `E_VERIFY` mandatory for 25+-employee private employers (Fla. Stat. § 448.095); `BREACH_NOTIFICATION` 30-day notice, 500+-resident AG notice (Fla. Stat. § 501.171); `ANTI_RETALIATION` whistleblower protection requires written internal notice first or protection is voided (Fla. Stat. § 448.101-105).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `florida.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: Florida](research/state-employment-law/florida.md).

- [ ] `LEGAL-ST-GA-001` **[PHASE_2][SOL_HIGH] Carry Georgia's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-006`, `LEGAL-TOOL-008`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_GA_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_GA_001`; `GOLDEN=TestTodo_LEGAL_ST_GA_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_GA_001_Conformance`.
  - **RED:** no Georgia pack is registered; the LEGAL-018 fix already corrected the retention citation from O.C.G.A. § 34-7-2 (pay frequency) to § 34-4-5 (records open to inspection, no fixed period stated), but nothing carries that corrected citation into a typed `RETENTION` obligation; Georgia has no state-specific overtime or final-pay computation (federal-only) so this pack exercises no mutation-tested arithmetic.
  - **GREEN:** the pack carries: `WAGE_FLOOR` federal-only, `F` ($5.15/hr state figure superseded by FLSA); `PAY_FREQUENCY` semi-monthly (O.C.G.A. § 34-7-2); `LEAVE_INTERACTION` paid lactation breaks, exempt below 50 employees (O.C.G.A. § 34-1-6); `NON_COMPETE` Restrictive Covenants Act, blue-pencil cannot add missing material terms per 2023 case law (O.C.G.A. § 13-8-50 et seq.); `PAY_EQUITY_REVIEW` sex-based, 10+ employees (O.C.G.A. §§ 34-5-1 to 34-5-7); `RETENTION` 4-year best-practice tied to § 34-4-5, not § 34-7-2; `E_VERIFY` mandatory for 11+ employees (O.C.G.A. § 13-10-91); `MINI_WARN` state Mass Separation Notice (DOL-402A) within 48 hours for 25+ same-day separations, distinct from federal WARN.
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `georgia.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: Georgia](research/state-employment-law/georgia.md).

- [ ] `LEGAL-ST-HI-001` **[PHASE_2][SOL_HIGH] Carry Hawaii's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-006`, `LEGAL-TOOL-007`, `LEGAL-TOOL-008`, `LEGAL-TOOL-010`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_HI_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_HI_001`; `GOLDEN=TestTodo_LEGAL_ST_HI_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_HI_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_HI_001_Mutation`.
  - **RED:** no Hawaii pack is registered; `NOTICE` and `PERSONNEL_FILE` are `?` in the matrix, though the LEGAL-018 fix already resolved personnel-file access to "no private-sector statutory right" (HRS § 89-16.5 is public-sector-union-only) — that resolution has no typed `PERSONNEL_FILE` obligation (correctly absent) to carry it.
  - **GREEN:** the pack carries: `WAGE_FLOOR` $16.00/hr (2026), scheduled to $18.00/hr (2028) (HRS § 387-2); `FIELD_RESTRICTION` salary-history ban, all employers (HRS § 378-2.4); `PAY_TRANSPARENCY` wage/salary-range posting for 50+ employers (HRS § 378-2.3 as amended by Act 203); `PAY_FREQUENCY` semi-monthly (HRS § 388-2); `FINAL_PAY_DEADLINE` at discharge or next working day if immediate payment impossible (HRS § 388-3); `LEAVE_INTERACTION` HFLL 4-week unpaid for 100+ employers, 6-month tenure (HRS § 378-71a) plus TDI wage-replacement insurance (HRS ch. 392); `NON_COMPETE` technology-worker exemption (Act 158, 2015), post-employment covenants void absent clear protective purpose (Prudential Locations v. Gagnon, 2022); `PERSONNEL_FILE` = not modeled (no private-sector statutory right, resolved 2026-09-03); `MINI_WARN` 50+-employee 60-day notice plus severance (HRS § 394B).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `hawaii.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: Hawaii](research/state-employment-law/hawaii.md).

- [ ] `LEGAL-ST-ID-001` **[PHASE_2][SOL_HIGH] Carry Idaho's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-008`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_ID_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_ID_001`; `GOLDEN=TestTodo_LEGAL_ST_ID_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_ID_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_ID_001_Mutation`.
  - **RED:** no Idaho pack is registered; `PAY_FREQUENCY` is `?` in the matrix (Idaho Code § 45-606 sets final-pay timing but no minimum frequency), and the "key employee" (top-5%-earner) gate on Idaho's non-compete statute has no typed field on `NonCompeteThreshold` today.
  - **GREEN:** the pack carries: `WAGE_FLOOR` $7.25/hr, auto-tracks federal (Idaho Code § 44-1502), $4.25/hr youth sub-minimum first 90 days; `NOTICE` written wage-reduction notice before the affected work is performed (Idaho Code § 45-610); `FINAL_PAY_DEADLINE` earlier of next regular payday or 10 calendar days, 48 hours on written employee demand (Idaho Code § 45-606); `NON_COMPETE` key-employee-only, 18-month presumption of reasonableness (Idaho Code §§ 44-2701, 44-2704); `RETENTION` 3-year employment records (Idaho Code § 45-610); `BREACH_NOTIFICATION` "as soon as possible, without unreasonable delay," 24-hour AG notice for state agencies (Idaho Code § 28-51-105).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `idaho.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: Idaho](research/state-employment-law/idaho.md).

- [ ] `LEGAL-ST-IL-001` **[PHASE_2][SOL_HIGH] Carry Illinois's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-006`, `LEGAL-TOOL-007`, `LEGAL-TOOL-008`, `LEGAL-TOOL-009`, `LEGAL-TOOL-010`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_IL_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_IL_001`; `GOLDEN=TestTodo_LEGAL_ST_IL_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_IL_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_IL_001_Mutation`.
  - **RED:** no Illinois pack is registered even though `illinois.md` itself is now `REVIEWED` (LEGAL-018 item 9 closed all seven previously-inaccessible statutes); the Cook County/Chicago locality overlay and Illinois's own state-level BIPA/AI-hiring/non-compete-threshold rules have no release carrying them into `internal/governance/legal`.
  - **GREEN:** the pack carries: `WAGE_FLOOR` state minimum with Chicago/Cook County locality overlays (820 ILCS 105); `PAY_TRANSPARENCY`/`FIELD_RESTRICTION` pay-scale-in-posting and salary-history ban for 15+ employees, Equal Pay Registration Certificate at 100+ (820 ILCS 112, eff. 2025-01-01); `NOTICE` 1-full-pay-period advance notice of any pay/basis/payday/deduction change (820 ILCS 115/3); `PAY_STMT` enhanced 2025 itemized-statement fields, 3-year retention; `LEAVE_INTERACTION` Paid Leave for All Workers Act 40-hour annual minimum, no forfeiture on role change (820 ILCS 192), Chicago/Cook County stricter local ordinances; `NON_COMPETE` void below $75,000 (non-competes) / $45,000 (non-solicits), scheduled increases through 2037, construction/union/veteran-mental-health exemptions (820 ILCS 90); `PERSONNEL_FILE` 7-business-day inspection window effective 2026-01-01 (820 ILCS 40); `MONITORING_CONSENT` BIPA written-release-before-collection, $1,000-$5,000 per-violation damages (740 ILCS 14); `AUTOMATED_DECISION` predictive-analytics-zip-code-proxy ban (775 ILCS 5, 2025 amendment).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `illinois.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: Illinois](research/state-employment-law/illinois.md).

- [ ] `LEGAL-ST-IN-001` **[PHASE_2][SOL_HIGH] Carry Indiana's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-008`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_IN_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_IN_001`; `GOLDEN=TestTodo_LEGAL_ST_IN_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_IN_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_IN_001_Mutation`.
  - **RED:** no Indiana pack is registered; `SEP_FILING` is `Y` in Table B (the state's UI-14 separation notice) but has no `form_name`/`recipient_authority` typed anywhere without `LEGAL-TOOL-008`.
  - **GREEN:** the pack carries: `WAGE_FLOOR` federal-only, `F` ($7.25/hr, IC 22-2-2, local-ordinance preemption under IC 22-2-2-10.5); `PAY_FREQUENCY` semimonthly or biweekly, wages earned within 10 business days prior (IC 22-2-5-1); `FINAL_PAY_DEADLINE` next regular payday on voluntary resignation (IC 22-2-5-2); `RETENTION` 4-year payroll-record minimum (IC 22-2-8); `NON_COMPETE` physician non-competes voided for hospital/hospital-system agreements from 2025-07-01 (IC 25-22.5-5.5, SEA 475); `SEPARATION_FILING` state UI-14 Separation Notice filed with Indiana DOL.
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `indiana.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: Indiana](research/state-employment-law/indiana.md).

- [ ] `LEGAL-ST-IA-001` **[PHASE_2][SOL_HIGH] Carry Iowa's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-008`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_IA_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_IA_001`; `GOLDEN=TestTodo_LEGAL_ST_IA_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_IA_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_IA_001_Mutation`.
  - **RED:** no Iowa pack is registered; the LEGAL-018 fix already corrected the state's own WARN-threshold restatement (`iowa.md` previously omitted the federal 100+-employer-size threshold) to point at `us-federal.md`, but Iowa's genuinely distinct state-specific mini-WARN (30-day notice for 25+-employee layoffs, Iowa Code § 84C) has no typed `MINI_WARN` obligation of its own separate from the federal one.
  - **GREEN:** the pack carries: `WAGE_FLOOR` federal-only, `F` ($7.25/hr, Iowa Code § 91D.1); `NOTICE` 3-day advance written pay-rate-change notice (Iowa Code § 91A.3); `FINAL_PAY_DEADLINE` next regular payday or within 5 days, whichever first, civil penalty for noncompliance (Iowa Code § 91A.4); `PERSONNEL_FILE` 3-business-day access (Iowa Code § 91B.1); `MINI_WARN` Iowa-specific 30-day notice for 25+-employee layoffs regardless of employer size (Iowa Code § 84C), layered on top of (not restating) the federal 100+-employer/60-day rule; discrimination coverage excludes gender identity as of SF 418 (2025-07-01, Iowa Code ch. 216).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `iowa.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: Iowa](research/state-employment-law/iowa.md).

- [ ] `LEGAL-ST-KS-001` **[PHASE_2][SOL_HIGH] Carry Kansas's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-008`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_KS_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_KS_001`; `GOLDEN=TestTodo_LEGAL_ST_KS_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_KS_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_KS_001_Mutation`.
  - **RED:** no Kansas pack is registered; `CLASSIFN` is the one `Y` cell in an otherwise mostly-`F` row, reflecting SB 241's 2025 non-solicitation carve-out (K.S.A. 50-163) that LEGAL-018 item 5 confirmed does NOT reach traditional non-competes — the pack has no typed distinction between the two without careful `NON_COMPETE`/`CLASSIFICATION` separation.
  - **GREEN:** the pack carries: `WAGE_FLOOR` federal-only, `F` ($7.25/hr, K.S.A. 44-1202); `NOTICE` written pay-rate-change notice, reduction notice must precede the affected work (K.S.A. 44-320, K.A.R. 49-20-1); `PAY_FREQUENCY` at least semimonthly (K.S.A. 44-313 to 44-327); `NON_COMPETE` common-law reasonableness only (Weber v. Tillman) — K.S.A. 50-163 does not reach non-competes; `CLASSIFICATION`/non-solicitation SB 241 presumes enforceable two narrow non-solicit categories (≤2 years, confidential-information or material-contact-customer scope) and mandates reformation of overbroad ones, eff. 2025-07-01; `BREACH_NOTIFICATION` "without unreasonable delay," typically 3-5 days (K.S.A. 50-7a01).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `kansas.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: Kansas](research/state-employment-law/kansas.md).

- [ ] `LEGAL-ST-KY-001` **[PHASE_2][SOL_HIGH] Carry Kentucky's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-008`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_KY_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_KY_001`; `GOLDEN=TestTodo_LEGAL_ST_KY_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_KY_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_KY_001_Mutation`.
  - **RED:** no Kentucky pack is registered; the LEGAL-018 fix already corrected `kentucky.md`'s final-pay rule from an internally contradictory sooner/later reading to `Max(termination + 14 days, next payday)` (KRS 337.055), but `LEGAL-TOOL-003`'s `comparator` field is the only place that correction has anywhere to attach as a typed `LATER_OF`; the file's federal-WARN-threshold restatement (wrongly "50+ employees") was also corrected under LEGAL-018 item 1 to defer to `us-federal.md`.
  - **GREEN:** the pack carries: `WAGE_FLOOR` federal-only, `F` ($7.25/hr, KRS 337.275); `CLASSIFICATION`/overtime seventh-consecutive-day premium (KRS 337.285), stricter than federal; `PAY_FREQUENCY` semi-monthly minimum (KRS 337.020); `FINAL_PAY_DEADLINE` `LATER_OF` next regular payday and termination-plus-14-days (KRS 337.055, corrected 2026-09-03); `RETENTION` 4-year payroll-record minimum (KRS 337.320); `PAY_EQUITY_REVIEW` sex-based comparable work (KRS 337.420-337.433); `MINI_WARN` = none state-specific; federal threshold only, restated via `us-federal.md` per the LEGAL-018 correction.
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `kentucky.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: Kentucky](research/state-employment-law/kentucky.md).

- [ ] `LEGAL-ST-LA-001` **[PHASE_2][SOL_HIGH] Carry Louisiana's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-013`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-008`, `LEGAL-TOOL-009`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_LA_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_LA_001`; `GOLDEN=TestTodo_LEGAL_ST_LA_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_LA_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_LA_001_Mutation`.
  - **RED:** no Louisiana pack is registered; Louisiana is one of the five states with a `PreemptionAssertion` in contract § 6.4 (La. R.S. 23:642 preempts locality `LEAVE_INTERACTION` obligations), but nothing today asserts it, so a naive composition would incorrectly attach a New Orleans paid-leave ordinance to a Louisiana promotion — exactly the failure `LEGAL-013`'s golden vector is meant to catch, with no Louisiana release yet to test against it.
  - **GREEN:** the pack carries: `WAGE_FLOOR` federal-only, `F` ($7.25/hr, no state statute); `FINAL_PAY_DEADLINE` earlier of 15 days or next regular payday, penalty interest for violation (La. R.S. 23:631-632); `NON_COMPETE` 2-year cap, reasonable scope/territory, legitimate-business-interest requirement (La. R.S. 23:921); `PAY_EQUITY_REVIEW` = none state-specific, `F`; `PreemptionAssertion` `LEAVE_INTERACTION` preempted at locality level statewide (La. R.S. 23:642), cited against the New Orleans Municipal Code rather than the wrong nolasf.org citation the 2026-09-03 review corrected; `E_VERIFY` mandatory for public contracts only (La. R.S. 42:1451, 38:2212.10).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `louisiana.md`, not hand-built; the preemption assertion is data for `LEGAL-013`'s evaluation stage, never an inline exception in a comparator.
  - **Refs:** [Preemption](specs/legal-rule-packs-and-state-configuration.md#64-preemption), [state employment-law research: Louisiana](research/state-employment-law/louisiana.md).

- [ ] `LEGAL-ST-ME-001` **[PHASE_2][SOL_HIGH] Carry Maine's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-006`, `LEGAL-TOOL-007`, `LEGAL-TOOL-008`, `LEGAL-TOOL-010`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_ME_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_ME_001`; `GOLDEN=TestTodo_LEGAL_ST_ME_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_ME_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_ME_001_Mutation`.
  - **RED:** no Maine pack is registered; `NOTICE` is `?` in the matrix, and the file's earlier "(verify)"-riddled figures (minimum wage, tip credit, PFMLA rate, non-compete threshold) were fixed in the 2026-09-03 rework to state actual numbers, none of which have a typed home yet.
  - **GREEN:** the pack carries: `WAGE_FLOOR` $15.10/hr (2026), tip credit $7.33/hr (26 M.R.S. §§ 664, 665); `FIELD_RESTRICTION` salary-history ban (26 M.R.S. § 628-A); `PAY_FREQUENCY` weekly minimum, written at-hire wage/hours/payday/leave-policy notice (26 M.R.S. § 621-A); `FINAL_PAY_DEADLINE` next regular payday (26 M.R.S. §§ 621-A, 626); `LEAVE_INTERACTION` Earned Paid Leave 1hr/40hrs, 40-hour minimum for 10+ employees, mandatory payout on termination (26 M.R.S. § 637), plus PFMLA 12-week paid leave at 90%/66% wage replacement from 2026-05-01 (26 M.R.S. § 850-A et seq.); `NON_COMPETE` void below ~$62,920/yr (400% federal poverty level), 3-business-day pre-signing notice, 1-2 year term cap (26 M.R.S. §§ 599-A, 599-B); `PERSONNEL_FILE` 6-year retention, employee access with reasonable copy fee (26 M.R.S. §§ 630-A, 631); `MINI_WARN` 100+-employee-worksite closures require 90-day notice plus 1-week-per-year-of-service severance up to 26 weeks (26 M.R.S. § 635).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `maine.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: Maine](research/state-employment-law/maine.md).

- [ ] `LEGAL-ST-MD-001` **[PHASE_2][SOL_HIGH] Carry Maryland's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-006`, `LEGAL-TOOL-007`, `LEGAL-TOOL-008`, `LEGAL-TOOL-009`, `LEGAL-TOOL-010`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_MD_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_MD_001`; `GOLDEN=TestTodo_LEGAL_ST_MD_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_MD_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_MD_001_Mutation`.
  - **RED:** no Maryland pack is registered; `PAY_FREQ`, `PAY_STMT`, `RETENTION` and `PERSONNEL_FILE` are all `?` in the matrix (spec § 12 lists Maryland among the ten states with unresolved personnel-file access, already confirmed "no private-sector statutory right"), and Montgomery County's own three-tier local minimum wage ($17.65/$16.00/$15.50 by employer size) has no overlay without `LEGAL-TOOL-009`.
  - **GREEN:** the pack carries: `WAGE_FLOOR` state floor plus Montgomery County three-tier overlay; `NOTICE` written notice at least 1 pay period before a payday/rate decrease, none required for increases (Md. Code, Lab. & Empl. § 3-504(a)); `PAY_TRANSPARENCY`/`FIELD_RESTRICTION` wage-range-plus-benefits in all postings (eff. 2024-10), salary-history ban (§§ 3-304.1, 3-304.2); `LEAVE_INTERACTION` earned sick and safe leave 1hr/30hrs, 40-hour cap, paid at 15+ employees (§ 3-1301 et seq.) plus FAMLI 12-24-week paid leave at 90% wage replacement, contributions from 2027-01-01 (Title 8.3); `NON_COMPETE` void at or below 150% state minimum wage ($22.50/hr, $46,800/yr), banned for healthcare workers under $350,000 (§ 3-716); `MINI_WARN` 50+-employee 60-day notice for 25%-or-15-worker reductions over 3 months, penalties up to $10,000/day (§ 11-301 et seq.); `PERSONNEL_FILE` = no private-sector statutory access right (confirmed).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `maryland.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: Maryland](research/state-employment-law/maryland.md).

- [ ] `LEGAL-ST-MA-001` **[PHASE_2][SOL_HIGH] Carry Massachusetts's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-006`, `LEGAL-TOOL-007`, `LEGAL-TOOL-008`, `LEGAL-TOOL-010`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_MA_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_MA_001`; `GOLDEN=TestTodo_LEGAL_ST_MA_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_MA_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_MA_001_Mutation`.
  - **RED:** no Massachusetts pack is registered; the LEGAL-018 fix already resolved the file's three-conflicting-dates pay-transparency confusion to two correct dates (posting duty 2025-10-29, pay-data-reporting duty 2025-02-01, both under M.G.L. c. 149 § 105F) and removed an erroneous CTDPA reference from the privacy section, but `PayTransparencyDuty`'s single `trigger` field cannot yet represent two distinct effective dates for two distinct duties under the same statute.
  - **GREEN:** the pack carries: `WAGE_FLOOR` state minimum, no local overlay identified; `PAY_TRANSPARENCY` two duties from one statute — pay-range posting for 25+ employers eff. 2025-10-29, pay-data reporting for 100+ employers eff. 2025-02-01 (M.G.L. c. 149 § 105F); `FIELD_RESTRICTION` salary-history ban (M.G.L. c. 149 § 105A); `LEAVE_INTERACTION` 40-hour paid sick time for 11+ employers (accrual after 90 days) plus PFML 16-week job-protected leave, 0.88% payroll contribution shifting fully to employer 2027-01-01 (c. 175M); `FINAL_PAY_DEADLINE` same-day on discharge, next payday on resignation, treble-damages strict-liability enforcement, 3-year statute of limitations (Wage Act); `NON_COMPETE` mandatory garden leave at 50% of salary, reasonable duration/geography/scope (M.G.L. c. 149 § 24L); `PERSONNEL_FILE` 10-day negative-information notice, 5-business-day access, 3-year post-termination retention for 20+ employers (c. 149 § 52C).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `massachusetts.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: Massachusetts](research/state-employment-law/massachusetts.md).

- [ ] `LEGAL-ST-MI-001` **[PHASE_2][SOL_HIGH] Carry Michigan's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-006`, `LEGAL-TOOL-008`, `LEGAL-TOOL-009`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_MI_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_MI_001`; `GOLDEN=TestTodo_LEGAL_ST_MI_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_MI_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_MI_001_Mutation`.
  - **RED:** no Michigan pack is registered; `PAY_FREQ`, `PAY_STMT` and `RETENTION` are `?` in the matrix, and the LEGAL-018 fix already corrected the small-employer ESTA entitlement from an either/or reading to "both" (40 paid hours AND 32 unpaid hours for 10-or-fewer-employee employers, eff. 2025-10-01) — `LeaveInteraction` has no typed field distinguishing an additive dual-entitlement from a single capped one without `LEGAL-TOOL-006`.
  - **GREEN:** the pack carries: `WAGE_FLOOR` $13.73/hr (2026), scheduled to $14.05/hr (2027-10), tipped wage phasing from 40% to 100% of minimum by 2030 (Improved Workforce Opportunity Wage Act, MCL 408.934a); `LEAVE_INTERACTION` Earned Sick Time Act — 72 hours paid for 11+ employees; for 10-or-fewer, 40 hours paid PLUS 32 hours unpaid (both, not either), eff. 2025-10-01 (corrected 2026-09-03); `NON_COMPETE` MCL 445.774a reasonableness test with blue-pencil; `ANTI_RETALIATION` Whistleblowers' Protection Act (MCL 15.361 et seq.); locality overlay for Detroit/Washtenaw County ban-the-box.
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `michigan.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: Michigan](research/state-employment-law/michigan.md).

- [ ] `LEGAL-ST-MN-001` **[PHASE_2][SOL_HIGH] Carry Minnesota's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-002`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-006`, `LEGAL-TOOL-007`, `LEGAL-TOOL-008`, `LEGAL-TOOL-009`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_MN_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_MN_001`; `GOLDEN=TestTodo_LEGAL_ST_MN_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_MN_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_MN_001_Mutation`.
  - **RED:** no Minnesota pack is registered; the LEGAL-018 fix already corrected the pay-range-posting citation from a repealed § 181.9414 to the operative § 181.173, but `PayTransparencyDuty` has no release carrying the corrected citation, and Minnesota's 48-hour overtime threshold (not 40) has no typed home distinct from every other state's 40-hour default without `LEGAL-TOOL-001`.
  - **GREEN:** the pack carries: `WAGE_FLOOR` $11.41/hr statewide (2026), Minneapolis/St. Paul locality overlays $16.37/hr; `MEAL_REST_BREAK` 30-min meal over 6 hrs, 15-min rest per 4 hrs; `CLASSIFICATION`/overtime 48-hour weekly threshold, not 40 (§§ 177.24-177.25); `NOTICE` written notice before the effective date of any pay/basis/payday/deduction change, no same-day exception (§ 181.032); `FIELD_RESTRICTION`/`PAY_TRANSPARENCY` salary-history ban (§ 363A.08) plus pay-range posting for 30+ employers (§ 181.173, corrected from § 181.9414); `LEAVE_INTERACTION` ESST 1hr/30hrs, 48-hour cap, 80-hour carryover, no forfeiture on promotion (§§ 181.9445-181.9448), plus PFML 12-week paid leave from 2026-01-01 (ch. 268B); `NON_COMPETE` void except business-sale/dissolution, retroactively void from 2023-07-01 (§ 181.988); `PERSONNEL_FILE` twice-yearly (current) / once-yearly (former) inspection, 30-35-day response (§§ 181.960-181.966).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `minnesota.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: Minnesota](research/state-employment-law/minnesota.md).

- [ ] `LEGAL-ST-MS-001` **[PHASE_2][SOL_HIGH] Carry Mississippi's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-008`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_MS_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_MS_001`; `GOLDEN=TestTodo_LEGAL_ST_MS_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_MS_001_Conformance`.
  - **RED:** no Mississippi pack is registered; Mississippi has no state-specific overtime or final-pay computation (both defer to federal FLSA), so this pack exercises no mutation-tested arithmetic, but its E-Verify mandate is the strictest in the fifty-state corpus (all employers, no size floor) and has no typed `EVerifyStatusCheck` distinguishing "all employers" from the far more common size-gated versions without care in extraction.
  - **GREEN:** the pack carries: `WAGE_FLOOR` federal-only, `F` ($7.25/hr, state's $5.15/hr figure preempted, Miss. Code § 71-1-51); `PAY_FREQUENCY` employer discretion except 50+-employee manufacturers/public-service corporations must pay bi-weekly or semi-monthly (Miss. Code § 71-1-35); `PAY_EQUITY_REVIEW` sex-based, 5+ employees, private-lawsuit-only enforcement (Miss. Code § 71-17-5, eff. 2022); `E_VERIFY` mandatory for ALL employers regardless of size (Miss. Code § 71-11-3); `BREACH_NOTIFICATION` "most expedient time possible," no private right of action (Miss. Code § 75-24-29).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `mississippi.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: Mississippi](research/state-employment-law/mississippi.md).
- [ ] `LEGAL-ST-MO-001` **[PHASE_2][SOL_HIGH] Carry Missouri's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-008`, `LEGAL-TOOL-010`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_MO_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_MO_001`; `GOLDEN=TestTodo_LEGAL_ST_MO_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_MO_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_MO_001_Mutation`.
  - **RED:** no Missouri pack is registered; HB 567 (eff. 2025-08-28) repealed both the CPI indexing on minimum wage and Proposition A's paid sick leave mandate after this platform would otherwise have modeled them as standing obligations, so `LEGAL-TOOL-010`'s fail-closed lapsed-index check is the only thing that would catch a stale post-repeal figure if the pack were built naively from an earlier snapshot of the law.
  - **GREEN:** the pack carries: `WAGE_FLOOR` $15.00/hr (2026-01-01), CPI indexing removed by HB 567, `index_source=NONE` going forward (RSMo § 290.502); `NOTICE` 30-day advance written notice before a pay-rate reduction, $50/employee penalty for noncompliance (RSMo § 290.100); `PAY_FREQUENCY` semi-monthly (every 16 days) for corporations (RSMo § 290.080); `FINAL_PAY_DEADLINE` immediate on discharge, 7-day-after-written-request penalty trigger capped at 60 days' continuing wages (RSMo § 290.110); `LEAVE_INTERACTION` = none, `F` (Proposition A repealed 2025-08-28); `NON_COMPETE` non-solicit safe harbor ≤1 year (RSMo § 431.202), non-competes common-law only; `PAY_EQUITY_REVIEW` sex-based (RSMo § 290.410); `E_VERIFY` mandatory for public contractors on contracts >$5,000 (RSMo § 285.530).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `missouri.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: Missouri](research/state-employment-law/missouri.md).

- [ ] `LEGAL-ST-MT-001` **[PHASE_2][SOL_HIGH] Carry Montana's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-008`, `LEGAL-TOOL-010`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_MT_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_MT_001`; `GOLDEN=TestTodo_LEGAL_ST_MT_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_MT_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_MT_001_Mutation`.
  - **RED:** no Montana pack is registered; Montana is the only state in the corpus that abolishes at-will employment by statute after probation (WDEA, MCA §§ 39-2-901 to 39-2-915), which the `JOB_SECURITY` kind's `GOOD_CAUSE_AFTER_PROBATION` standard exists specifically to model, but no release carries Montana's probation length (default 12 months, extendable to 18), and `SEP_FILING`'s missing citation (spec § 12 extraction findings) leaves that kind unverified.
  - **GREEN:** the pack carries: `JOB_SECURITY` `GOOD_CAUSE_AFTER_PROBATION`, default 12-month probation extendable once to 18 months, internal-grievance-exhaustion requirement with a 90-day safety valve (MCA §§ 39-2-904, 39-2-912); `WAGE_FLOOR` $10.85/hr (2026-01-01), CPI-U-indexed annually, rounded to nearest $0.05 (MCA § 39-3-404); `FINAL_PAY_DEADLINE` immediate on discharge (or employer-policy-extended to next payday/15 days), next payday or 15 days on resignation, alleged-theft withholding carve-out with a 7-business-day law-enforcement-report requirement (MCA § 39-3-205); `NON_COMPETE` void except business-sale/goodwill/partnership-dissolution, narrow judicial exception (MCA § 28-2-703 et seq.); `PAY_EQUITY_REVIEW` = none state-specific, `F`.
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `montana.md`, not hand-built; `JOB_SECURITY`'s probation-tracking state machine lives in the workflow/HUMAN_TASK binding, not as new Legal-plane evaluation logic.
  - **Refs:** [Added kinds](specs/legal-rule-packs-and-state-configuration.md#42-added-kinds), [state employment-law research: Montana](research/state-employment-law/montana.md).

- [ ] `LEGAL-ST-NE-001` **[PHASE_2][SOL_HIGH] Carry Nebraska's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-006`, `LEGAL-TOOL-008`, `LEGAL-TOOL-010`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_NE_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_NE_001`; `GOLDEN=TestTodo_LEGAL_ST_NE_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_NE_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_NE_001_Mutation`.
  - **RED:** no Nebraska pack is registered; the LEGAL-018 fix already corrected the non-compete citation from § 87-404 (a franchise-law statute) to plain common law, and the file's `MINI_WARN` cell (Table B: `Y 90d`) reflects Nebraska's own state-specific 90-day, 25+-employee mini-WARN (LB 921) distinct from and narrower-triggering than the federal 60-day/100+-employee rule.
  - **GREEN:** the pack carries: `WAGE_FLOOR` $15.00/hr (2026-01-01), 1.75% annual increase from 2027 published by October 15 each year (Initiative 433, Neb. Rev. Stat. § 48-1203); `NOTICE` 30-day advance written notice for payday changes (Neb. Rev. Stat. § 48-1230); `FINAL_PAY_DEADLINE` immediate on separation, accrued PTO counted as wages (Neb. Rev. Stat. § 48-1230); `LEAVE_INTERACTION` paid sick leave 1hr/30hrs after 80 hours worked, 11+-employee threshold, eff. 2025-10-01 (Initiative 436); `NON_COMPETE` = common law only (corrected from the wrong § 87-404 franchise-law citation); `MINI_WARN` state-specific 90-day notice for 25+-employee same-day separations (LB 921), layered on the federal 60-day/100+-employee rule, not restating it; `E_VERIFY` mandatory for public contractors only (Neb. Rev. Stat. § 4-114).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `nebraska.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: Nebraska](research/state-employment-law/nebraska.md).

- [ ] `LEGAL-ST-NV-001` **[PHASE_2][SOL_HIGH] Carry Nevada's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-006`, `LEGAL-TOOL-007`, `LEGAL-TOOL-008`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_NV_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_NV_001`; `GOLDEN=TestTodo_LEGAL_ST_NV_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_NV_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_NV_001_Mutation`.
  - **RED:** no Nevada pack is registered; Nevada is one of the fully `Y`-across-Table-A states (every kind the flow consumes is asserted), so it is a high-value gap: nothing in `internal/governance/legal` carries the 2021 hourly-employee non-compete ban (NRS 613.195) or the mandatory 40-hour paid-leave floor.
  - **GREEN:** the pack carries: `WAGE_FLOOR` flat $12.00/hr, no tip credit (constitutional amendment, NRS 608.250); `NOTICE`/`FIELD_RESTRICTION` post-interview wage-range disclosure, salary-history ban, pay-secrecy-policy ban (NRS 613.133, 613.330); `PAY_FREQUENCY` semimonthly on the 15th and last day (NRS 608.060); wage-decrease notice 7 days before the reduced rate takes effect (NRS 608.100); `FINAL_PAY_DEADLINE` immediate on discharge, earlier of 7 days or next payday on resignation (NRS 608.020, 608.030); `LEAVE_INTERACTION` 50+-employee paid leave accrual 0.01923 hrs/hr worked, 40-hour annual minimum, usable after 90 days (NRS 608.0197), plus separate domestic-violence leave up to 160 hours/12 months (NRS 608.0198); `NON_COMPETE` unenforceable for hourly employees, business-sale exception only (NRS 613.195); `PERSONNEL_FILE` inspection right, copy denial permitted under 60 days' tenure (NRS 608.115, 613.075); `RETENTION` 2-year payroll records.
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `nevada.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: Nevada](research/state-employment-law/nevada.md).

- [ ] `LEGAL-ST-NH-001` **[PHASE_2][SOL_HIGH] Carry New Hampshire's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-008`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_NH_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_NH_001`; `GOLDEN=TestTodo_LEGAL_ST_NH_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_NH_001_Conformance`.
  - **RED:** no New Hampshire pack is registered; `PAY_FREQ` and `PAY_STMT` are `?` in the matrix, and New Hampshire has no state-specific overtime or final-pay computation distinct from federal, so this pack exercises no mutation-tested arithmetic.
  - **GREEN:** the pack carries: `WAGE_FLOOR` federal-only, `F` ($7.25/hr, RSA 279:21); `NOTICE` written advance notice of any wage/salary/payday change with signed employee acknowledgment (RSA 275:49); `PAY_FREQUENCY` weekly or biweekly by default, Commissioner-approved exceptions for less-frequent schedules (RSA 275:43); `NON_COMPETE` void for workers at or below 200% of federal minimum wage (~$14.50/hr), additional physician/dentist restrictions (RSA 275:70-a, 329:31-a); `PAY_EQUITY_REVIEW` sex-based, wage-discussion protection (RSA 275:37, 275:41-b); `PERSONNEL_FILE` statutory inspection right, 1-year retention for 15+-employee employers (RSA 275:56); `MINI_WARN` mirrors federal WARN's 60-day/100+-employee rule rather than adding a distinct state trigger (RSA 275-F).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `new-hampshire.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: New Hampshire](research/state-employment-law/new-hampshire.md).

- [ ] `LEGAL-ST-NJ-001` **[PHASE_2][SOL_HIGH] Carry New Jersey's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-006`, `LEGAL-TOOL-007`, `LEGAL-TOOL-008`, `LEGAL-TOOL-010`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_NJ_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_NJ_001`; `GOLDEN=TestTodo_LEGAL_ST_NJ_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_NJ_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_NJ_001_Mutation`.
  - **RED:** no New Jersey pack is registered; `PERSONNEL_FILE` is `?` in the matrix, and the LEGAL-018 fix already resolved the file's fabricated "N.J.S.A. 34:8B-1 Personnel Files Act" citation — that statute is the Temporary Help Service Firms Act and New Jersey has no general personnel-file-access statute at all — so `PERSONNEL_FILE` correctly has no obligation here, but nothing currently records that the absence was confirmed rather than unresearched.
  - **GREEN:** the pack carries: `WAGE_FLOOR` $15.92/hr (6+ employees) / $15.23/hr (≤5 employees), agricultural $14.20/hr (2026); `PAY_TRANSPARENCY` wage-range-plus-benefits in postings and internal-promotion notices for 10+ employers, eff. 2025-06-01 (N.J.S.A. 34:6B-23); `FIELD_RESTRICTION` salary-history ban (N.J.S.A. 34:6B-20); `NOTICE` written notice at hire and in advance of any wage/payday/deduction change, Wage Theft Act 6-year statute of limitations and 200% liquidated damages (N.J.S.A. 34:11-4.1 et seq.); `LEAVE_INTERACTION` earned sick leave 1hr/30hrs, 40-hour cap (N.J.S.A. 34:11D-1) plus family leave/TDI paid insurance expanding to 15+ employers 2026-07 (N.J.S.A. 34:11B, 43:21-25 et seq.); `PAY_EQUITY_REVIEW` 21 protected classes (Diane B. Allen Equal Pay Act, N.J.S.A. 10:5-12); `MINI_WARN` NJ WARN 90-day notice for 100+-employee mass layoffs, mandatory 1-week-per-year severance plus 4 additional weeks for inadequate notice (N.J.S.A. 34:21-1 et seq.); `PERSONNEL_FILE` = no general private-sector statute (confirmed, corrected citation).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `new-jersey.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: New Jersey](research/state-employment-law/new-jersey.md).

- [ ] `LEGAL-ST-NM-001` **[PHASE_2][SOL_HIGH] Carry New Mexico's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-006`, `LEGAL-TOOL-008`, `LEGAL-TOOL-009`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_NM_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_NM_001`; `GOLDEN=TestTodo_LEGAL_ST_NM_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_NM_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_NM_001_Mutation`.
  - **RED:** no New Mexico pack is registered; Santa Fe ($15.00), Albuquerque ($12.00), Las Cruces ($13.01) and Bernalillo County ($12.00) all set minimum-wage floors above or at the $12.00 statewide rate, and none has a `LOCALITY`-level overlay without `LEGAL-TOOL-009`.
  - **GREEN:** the pack carries: `WAGE_FLOOR` $12.00/hr statewide with Santa Fe/Albuquerque/Las Cruces/Bernalillo County overlays (NMSA 1978 § 50-4-22); `PAY_FREQUENCY` semimonthly, paydays ≤16 days apart (NMSA 1978 § 50-4-2); `FINAL_PAY_DEADLINE` fixed wages within 5 days on discharge, other compensation within 10 days, next payday on resignation (§§ 50-4-4, 50-4-5); `LEAVE_INTERACTION` Healthy Workplaces Act 1hr/30hrs, 64-hour carryover cap, eff. 2022-07-01 (§ 50-17-1 et seq.); `NON_COMPETE` void for physicians and eight other licensed healthcare professions, 1-year non-solicit permitted (§ 50-4A-1); `ANTI_RETALIATION` ban-the-box for private employers (§ 28-2-3.1).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `new-mexico.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: New Mexico](research/state-employment-law/new-mexico.md).

- [ ] `LEGAL-ST-NY-001` **[PHASE_2][SOL_HIGH] Carry New York's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-001`, `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-006`, `LEGAL-TOOL-007`, `LEGAL-TOOL-008`, `LEGAL-TOOL-009`, `LEGAL-TOOL-010`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_NY_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_NY_001`; `GOLDEN=TestTodo_LEGAL_ST_NY_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_NY_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_NY_001_Mutation`.
  - **RED:** like California, the only New York content is `LEGAL-001`'s `UNREVIEWED` fixture pack seeded under the ten-kind `vocabulary_version 1`; it predates the twelve LEGAL-011 kinds, has no NYC Local Law 144 `AUTOMATED_DECISION` obligation, no `MONITORING_CONSENT` for Civil Rights Law § 201-i, and the file's own personnel-file-access question remains `DISPUTED` per LEGAL-018 (S.3460/A.2107 passed both chambers but signature is unconfirmed) so `PERSONNEL_FILE` cannot reach `COUNSEL_APPROVED` until that resolves.
  - **GREEN:** a regenerated pack carries: `WAGE_FLOOR` regional tiers — NYC/Long Island/Westchester $17.00/hr, rest-of-state $16.00/hr, annual CPI-W adjustment (Labor Law § 652); `NOTICE` Wage Theft Prevention Act hire notice plus 7-day pre-decrease notice (Labor Law § 195); `PAY_TRANSPARENCY`/`FIELD_RESTRICTION` good-faith wage ranges in all postings/transfers/promotions for 4+ employers (§ 194-b), salary-history ban (§ 194-a); `LEAVE_INTERACTION` 40-hour paid sick leave (§ 196-b) plus Paid Family Leave (12 weeks, 67% AWW) and Paid Prenatal Leave (20 hours/52 weeks, eff. 2025-01-01, § 196-c); `MINI_WARN` NY WARN — 50+ employees (not 100), 90-day notice (not 60), 25+-layoff trigger (Labor Law Art. 25-A); `AUTOMATED_DECISION` NYC Local Law 144 bias audit and 10-business-day candidate notice; `MONITORING_CONSENT` electronic-monitoring notice, eff. 2022-05-07 (Civil Rights Law § 201-i); `PERSONNEL_FILE` `DISPUTED` pending S.3460/A.2107 signature confirmation, not releasable above `UNREVIEWED` for that kind until resolved.
  - **REFACTOR:** the pack is regenerated by `internal/governance/legal/extract` from `new-york.md` at `vocabulary_version 2`+, replacing the hand-built `LEGAL-001` fixture.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: New York](research/state-employment-law/new-york.md).

- [ ] `LEGAL-ST-NC-001` **[PHASE_2][SOL_HIGH] Carry North Carolina's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-013`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-006`, `LEGAL-TOOL-008`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_NC_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_NC_001`; `GOLDEN=TestTodo_LEGAL_ST_NC_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_NC_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_NC_001_Mutation`.
  - **RED:** no North Carolina pack is registered; the LEGAL-018 fix already corrected the fabricated 2024 salary-history-ban claim to the real, narrower rule (Executive Order No. 93 binds state agencies only; private employers may still ask), and `LOCAL` is `?` because the file "does not address local ordinances or preemption at all," leaving no `LEGAL-TOOL-009` entry or preemption assertion either way.
  - **GREEN:** the pack carries: `WAGE_FLOOR` federal-only, `F` ($7.25/hr); `NOTICE` written pay-rate/payday notice at hire, 24-hour notice before a wage decrease (§ 95-25.13); `ANTI_RETALIATION` REDA protects workers'-comp, OSHA and wage-and-hour complainants (§ 95-240 et seq.), plus sickle-cell/genetic-testing and lawful-off-duty-product-use protections (§§ 95-28.1, 95-28.1A, 95-28.2); `LEAVE_INTERACTION` school-involvement leave 4hrs/year (§ 95-28.3), domestic-violence protective-order leave (§ 50B-5.5); `NON_COMPETE` written, signed, reasonable time/territory, strictly construed against the drafter (§ 75-4); `FIELD_RESTRICTION` = no private-sector salary-history ban (confirmed narrow correction, state-agency-only); `E_VERIFY` mandatory for 25+ employees (HB 36); `LOCAL` = `?`, unaddressed by the research and not yet resolved by `LEGAL-TOOL-009`.
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `north-carolina.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: North Carolina](research/state-employment-law/north-carolina.md).

- [ ] `LEGAL-ST-ND-001` **[PHASE_2][SOL_HIGH] Carry North Dakota's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-008`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_ND_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_ND_001`; `GOLDEN=TestTodo_LEGAL_ST_ND_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_ND_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_ND_001_Mutation`.
  - **RED:** no North Dakota pack is registered; the LEGAL-018 review noted `alaska.md`'s at-will citation once carried the wrong North Dakota-style recordkeeping-statute confusion pattern, and North Dakota's own non-compete statute (one of the strictest voidance rules in the corpus) has no `NON_COMPETE` release to test the `void`-wins comparator (contract § 6.3) against.
  - **GREEN:** the pack carries: `WAGE_FLOOR` federal-only, `F` ($7.25/hr, no state increase since 2009); `CLASSIFICATION`/overtime 40-hour weekly threshold only, paid leave excluded from the count (N.D. Admin. Code 46-03-01); `PAY_FREQUENCY` regular, predictable paydays (NDCC § 34-14-02); `FINAL_PAY_DEADLINE` earlier of next regular payday or 15 days, accrued leave payout unless a narrow at-hire/tenure/notice exception is met (NDCC § 34-14-03); `NON_COMPETE` void except business-sale/partnership-dissolution — one of the strictest voidance rules in the corpus (NDCC § 9-08-06); `ANTI_RETALIATION` lawful off-duty activity protection unless it conflicts with essential business interests (NDCC § 14-02.4).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `north-dakota.md`, not hand-built.
  - **Refs:** [Per-kind composition](specs/legal-rule-packs-and-state-configuration.md#63-per-kind-composition), [state employment-law research: North Dakota](research/state-employment-law/north-dakota.md).

- [ ] `LEGAL-ST-OH-001` **[PHASE_2][SOL_HIGH] Carry Ohio's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-006`, `LEGAL-TOOL-007`, `LEGAL-TOOL-008`, `LEGAL-TOOL-009`, `LEGAL-TOOL-010`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_OH_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_OH_001`; `GOLDEN=TestTodo_LEGAL_ST_OH_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_OH_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_OH_001_Mutation`.
  - **RED:** no Ohio pack is registered; the LEGAL-018 fix already corrected `ohio.md`'s WARN citations (which previously carried New Mexico's § 50-11/§ 26-2B-9 numbers) to R.C. ch. 3796 and 40 O.S. § 500's Ohio equivalents, and Ohio's own mini-WARN takes effect 2025-09-29 — one of nine effective-dates-within-the-year the contract's § 2.3 boundary vectors are built around (Ohio 2025-09-28 vs. 2025-09-30 is `LEGAL-016`'s required boundary pair).
  - **GREEN:** the pack carries: `WAGE_FLOOR` $11.00/hr (2026) for employers with gross receipts >$405,000, federal $7.25/hr otherwise, annually CPI-indexed (Ohio Const. Art. II § 34a); `PAY_FREQUENCY` semimonthly, 1st-15th due by month-end, 16th-31st due by the 15th (R.C. 4113.15); `PAY_STMT` itemized statement mandate, eff. 2025-04-09 (R.C. 4113.14); `LEAVE_INTERACTION` earned sick/safe time for all private employers, eff. 2025 (Issue 1); `MINI_WARN` 100+-employee/50+-affected-site 60-day notice, eff. 2025-09-29 (R.C. 4113.31) — the exact date `LEGAL-016`'s Ohio boundary vector must straddle; `PAY_EQUITY_REVIEW` sex/race/etc., all employers (R.C. 4111.17); locality overlays for Columbus/Cincinnati/Toledo salary-history and pay-range-disclosure ordinances (not statewide).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `ohio.md`, not hand-built; the 2025-09-28/2025-09-30 boundary is a shared fixture with `LEGAL-016`, not duplicated here.
  - **Refs:** [Effective dating and legal time](specs/legal-rule-packs-and-state-configuration.md#23-effective-dating-and-legal-time), [state employment-law research: Ohio](research/state-employment-law/ohio.md).

- [ ] `LEGAL-ST-OK-001` **[PHASE_2][SOL_HIGH] Carry Oklahoma's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-013`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-008`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_OK_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_OK_001`; `GOLDEN=TestTodo_LEGAL_ST_OK_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_OK_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_OK_001_Mutation`.
  - **RED:** no Oklahoma pack is registered; Oklahoma is one of the five states asserting a `PreemptionAssertion` in contract § 6.4 (40 O.S. § 160 preempts locality `LEAVE_INTERACTION` sick-leave mandates), and this is one of the golden vectors `LEGAL-013` requires but has no Oklahoma release to run it against.
  - **GREEN:** the pack carries: `WAGE_FLOOR` federal-only, `F` ($7.25/hr, State Question 832's $15/hr-by-2029 proposal rejected by voters June 2026, 40 O.S. § 197.2); `PAY_FREQUENCY` semimonthly minimum, 11-day max period-to-payday gap (40 O.S. § 165.2); `FINAL_PAY_DEADLINE` next designated payday, 2%/day liquidated-damages penalty for willful non-bona-fide withholding (40 O.S. § 165.3); `NON_COMPETE` "pure" non-competes void, non-solicitation of established customers/employees enforceable under strict compliance (15 O.S. §§ 219A, 219B); `PreemptionAssertion` `LEAVE_INTERACTION` preempted at locality level statewide (40 O.S. § 160); `DRUG_TESTING` written policy and 10-day employee notice required (40 O.S. §§ 551-563), medical-marijuana zero-tolerance carve-out for safety-sensitive roles eff. 2026-11-01 (63 O.S. § 427.8, HB 3127).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `oklahoma.md`, not hand-built; the preemption assertion is data for `LEGAL-013`'s evaluation stage, never an inline exception in a comparator.
  - **Refs:** [Preemption](specs/legal-rule-packs-and-state-configuration.md#64-preemption), [state employment-law research: Oklahoma](research/state-employment-law/oklahoma.md).

- [ ] `LEGAL-ST-OR-001` **[PHASE_2][SOL_HIGH] Carry Oregon's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-006`, `LEGAL-TOOL-007`, `LEGAL-TOOL-008`, `LEGAL-TOOL-009`, `LEGAL-TOOL-010`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`, `LEGAL-TOOL-013`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_OR_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_OR_001`; `GOLDEN=TestTodo_LEGAL_ST_OR_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_OR_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_OR_001_Mutation`.
  - **RED:** no Oregon pack is registered; Oregon's real statewide Fair Workweek/predictive-scheduling statute is the exact gap `LEGAL-TOOL-013` names but the state's own research file never mentions it, so this todo also depends on that gap being closed before the Oregon pack can claim completeness on scheduling; separately, Portland-metro/standard/nonurban three-tier minimum wage has no overlay without `LEGAL-TOOL-009`.
  - **GREEN:** the pack carries: `WAGE_FLOOR` three tiers — Portland metro $16.80/hr, standard $15.55/hr, nonurban $14.55/hr, CPI-indexed annually (eff. 2026-07-01); `FIELD_RESTRICTION` salary-history ban, voluntary-disclosure exception (ORS 659A.357); `PAY_EQUITY_REVIEW` "comparable character" standard (ORS 652.220); `LEAVE_INTERACTION` Oregon Sick Time 40 hours/year, paid at 6+ employees (ORS 653.601-661), plus Paid Leave Oregon 12-week job-protected paid leave (ORS 657B) and OFLA narrowed to bereavement/pregnancy-disability/sick-child only since SB 1515 (eff. 2024-07-01); `NON_COMPETE` void below $116,427/yr (indexed), 2-week pre-hire notice, 12-month max, 50%+ garden leave (ORS 653.295); `ANTI_RETALIATION` ban-the-box until after initial interview (ORS 659A.360); locality overlay for Portland's stricter ban-the-box.
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `oregon.md`, not hand-built; scheduling obligations wait on `LEGAL-TOOL-013` rather than being invented here.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: Oregon](research/state-employment-law/oregon.md).

- [ ] `LEGAL-ST-PA-001` **[PHASE_2][SOL_HIGH] Carry Pennsylvania's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-006`, `LEGAL-TOOL-008`, `LEGAL-TOOL-009`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_PA_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_PA_001`; `GOLDEN=TestTodo_LEGAL_ST_PA_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_PA_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_PA_001_Mutation`.
  - **RED:** no Pennsylvania pack is registered; Philadelphia's salary-history ban and paid-sick-leave ordinance, and Pittsburgh's separate paid-sick-leave ordinance (eff. 2026-01-01), are all locality-only with no statewide equivalent — `LEGAL-TOOL-009` is the only mechanism that can attach them without falsely implying a Pennsylvania-wide state rule.
  - **GREEN:** the pack carries: `WAGE_FLOOR` federal-only, `F` ($7.25/hr, 43 P.S. § 333.101 et seq.), tipped $2.83/hr; `NOTICE` written notice at hire and in advance of any wage/frequency/deduction/benefit change (43 P.S. § 260.4); `FINAL_PAY_DEADLINE` next regular payday regardless of separation reason, 25% liquidated damages plus attorney fees for withholding (WPCL, 43 P.S. § 260 et seq.); `PAY_EQUITY_REVIEW` sex-only equal-pay statute (43 P.S. § 336.1); `NON_COMPETE` common-law reasonableness, healthcare-practitioner covenants capped at 1 year (Act 74, eff. 2025-01-01); locality overlays for Philadelphia (salary-history ban, 40-hour paid sick leave) and Pittsburgh (48-72-hour paid sick leave by employer size, eff. 2026-01-01); `ANTI_RETALIATION` Human Relations Act, 4+ employees (43 P.S. § 951 et seq.).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `pennsylvania.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: Pennsylvania](research/state-employment-law/pennsylvania.md).

- [ ] `LEGAL-ST-RI-001` **[PHASE_2][SOL_HIGH] Carry Rhode Island's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-006`, `LEGAL-TOOL-007`, `LEGAL-TOOL-008`, `LEGAL-TOOL-010`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_RI_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_RI_001`; `GOLDEN=TestTodo_LEGAL_ST_RI_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_RI_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_RI_001_Mutation`.
  - **RED:** no Rhode Island pack is registered; Rhode Island is fully `Y` across nearly every Table A column, making it one of the highest-density gaps in the corpus, and its Sunday/holiday premium-pay pyramiding rule (non-retail employers stack both premiums) has no typed field distinguishing it from ordinary overtime.
  - **GREEN:** the pack carries: `WAGE_FLOOR` $16.00/hr (2026-01-01), CPI-adjusted to $17.00/hr (2027) (§ 28-12-3), no tip credit; `PAY_TRANSPARENCY`/`FIELD_RESTRICTION` wage-range disclosure at hire/promotion/on-request, salary-history ban, $10,000 special damages for violation (Pay Equity Act, § 28-6-17 et seq.); `PAY_FREQUENCY` weekly by default, bondable exceptions (§ 28-14-2); `LEAVE_INTERACTION` 40-hour paid sick/safe leave for 18+ employers, 150-day waiting period (§ 28-57-1 et seq.), plus TDI/Caregiver Insurance 7-8-week paid leave (§ 28-41-34) and Parental/Family Medical Leave 13 weeks/2 years at 50+ employers (§ 28-48-1 et seq.); `NON_COMPETE` void below $37,650/yr or for non-exempt workers (§ 28-59); `ANTI_RETALIATION` ban-the-box before first interview, 4+ employers (§ 28-5-7(7)).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `rhode-island.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: Rhode Island](research/state-employment-law/rhode-island.md).

- [ ] `LEGAL-ST-SC-001` **[PHASE_2][SOL_HIGH] Carry South Carolina's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-008`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_SC_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_SC_001`; `GOLDEN=TestTodo_LEGAL_ST_SC_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_SC_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_SC_001_Mutation`.
  - **RED:** no South Carolina pack is registered; the LEGAL-018 fix already removed the file's invented "§ 41-12" equal-pay chapter and invented "§ 41-7" non-compete exception plus the wrong NCDOL reference, leaving the corrected 7-calendar-day wage-change-notice rule (§ 41-10-30) as the pack's one hard-figured `NOTICE` obligation.
  - **GREEN:** the pack carries: `WAGE_FLOOR` federal-only, `F` ($7.25/hr); `NOTICE` written notice at hire of wages/hours/paydays/deductions, 7-calendar-day advance notice for any change (§ 41-10-30); `FINAL_PAY_DEADLINE` earlier of 48 hours or next regular payday, capped at 30 days (§ 41-10-50); `JOB_SECURITY` `HANDBOOK_DISCLAIMER` standard — an underlined-capital-letters, signed disclaimer defeats an implied-contract claim (§ 41-1-110); `NON_COMPETE` common-law only, strictly construed against the drafter, no blue-pencil (corrected — no statutory chapter exists); `E_VERIFY` mandatory for all private employers within 3 business days (§ 41-8-20(B)).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `south-carolina.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: South Carolina](research/state-employment-law/south-carolina.md).

- [ ] `LEGAL-ST-SD-001` **[PHASE_2][SOL_HIGH] Carry South Dakota's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-008`, `LEGAL-TOOL-009`, `LEGAL-TOOL-010`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_SD_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_SD_001`; `GOLDEN=TestTodo_LEGAL_ST_SD_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_SD_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_SD_001_Mutation`.
  - **RED:** no South Dakota pack is registered; South Dakota's non-decrease-year-over-year CPI-U minimum-wage rule (SDCL 60-11-3.2) is a distinct indexation shape `LEGAL-TOOL-010` must model (a floor that can rise but never fall), and Sioux Falls/Spearfish/Vermillion local ordinances extending discrimination protections beyond the state's list have no overlay without `LEGAL-TOOL-009`.
  - **GREEN:** the pack carries: `WAGE_FLOOR` $11.85/hr (2026), CPI-U-indexed annually with a no-decrease floor (SDCL 60-11-3, 60-11-3.2), tipped $5.925/hr; `PAY_FREQUENCY` monthly or regular agreed payday, communicated at hire (SDCL 60-11-9); `FINAL_PAY_DEADLINE` next regular payday or reasonable time, employer may withhold pending return of employer property, 5-day payment on written demand (SDCL 60-11-10); `NON_COMPETE` 2-year cap within a specified area where the employer continues like business, void for healthcare practitioners eff. 2023-07-01 (SDCL 53-9-11, 53-9-11.2); `PAY_EQUITY_REVIEW` "comparable work," explicitly excludes physical strength (SDCL 60-12-15); `ANTI_RETALIATION` tobacco-use protection unless a bona fide occupational requirement applies (SDCL 60-4-11).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `south-dakota.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: South Dakota](research/state-employment-law/south-dakota.md).

- [ ] `LEGAL-ST-TN-001` **[PHASE_2][SOL_HIGH] Carry Tennessee's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-013`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-006`, `LEGAL-TOOL-008`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_TN_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_TN_001`; `GOLDEN=TestTodo_LEGAL_ST_TN_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_TN_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_TN_001_Mutation`.
  - **RED:** no Tennessee pack is registered; Tennessee is one of five states asserting a `PreemptionAssertion` (Tenn. Code § 50-2-112 preempts locality `WAGE_FLOOR` and `LEAVE_INTERACTION` obligations), and its unusual "later of next payday or 21 days" final-pay rule needs `LEGAL-TOOL-003`'s `comparator=LATER_OF` field rather than the more common "earlier of" shape most states use.
  - **GREEN:** the pack carries: `WAGE_FLOOR` federal-only, `F` ($7.25/hr); `PAY_FREQUENCY` at least monthly, semi-monthly-if-chosen wages due by the 20th of the following month (§ 50-2-103); `FINAL_PAY_DEADLINE` `LATER_OF` next regular payday or 21 days after separation (§ 50-2-103); `LEAVE_INTERACTION` pregnancy/adoption accommodation and 4-month leave for 15+ employers (§ 4-21-408); `NON_COMPETE` common-law reasonableness, healthcare providers capped at 2 years/10-mile radius (§ 63-1-148); `PreemptionAssertion` `WAGE_FLOOR`, `LEAVE_INTERACTION` preempted at locality level statewide (§ 50-2-112); `E_VERIFY` mandatory for 35+-FTE private employers, eff. 2023-01-01 (§ 50-1-703); `SEPARATION_FILING` state plant-closure notification for 50-99-employee employers reducing 50+ workers (§ 50-1-601 et seq.).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `tennessee.md`, not hand-built; the preemption assertion is data for `LEGAL-013`'s evaluation stage, never an inline exception in a comparator.
  - **Refs:** [Preemption](specs/legal-rule-packs-and-state-configuration.md#64-preemption), [state employment-law research: Tennessee](research/state-employment-law/tennessee.md).

- [ ] `LEGAL-ST-TX-001` **[PHASE_2][SOL_HIGH] Carry Texas's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-013`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-008`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_TX_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_TX_001`; `GOLDEN=TestTodo_LEGAL_ST_TX_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_TX_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_TX_001_Mutation`.
  - **RED:** no Texas pack is registered; HB 2127 (the "Death Star" bill) preempts Austin/Dallas/San Antonio paid-sick-leave ordinances and is one of the five `PreemptionAssertion` states in contract § 6.4 (`LEAVE_INTERACTION`, `WAGE_FLOOR`), with the local ordinances themselves enjoined by courts since 2018-2021 — a preemption-plus-litigation-history shape none of the other four preempting states share.
  - **GREEN:** the pack carries: `WAGE_FLOOR` federal-only, `F` ($7.25/hr, § 62.151); `PAY_FREQUENCY` semi-monthly for non-exempt, monthly for exempt (§ 61.011, 61.012); `FINAL_PAY_DEADLINE` within 6 calendar days on discharge, next regular payday on resignation (§ 61.014); `NON_COMPETE` ancillary-to-enforceable-agreement requirement (§ 15.50), physicians/dentists/APRNs/RNs/LVNs/PAs capped at 1-year/5-mile radius with buyout cap, eff. 2025-09-01 (SB 1318); `PreemptionAssertion` `LEAVE_INTERACTION`, `WAGE_FLOOR` preempted at locality level statewide (HB 2127); `BREACH_NOTIFICATION` 60 days to individuals, 30 days to the Texas AG for 250+-resident breaches (§ 521.053); `ANTI_RETALIATION` workers'-compensation retaliation, 2-year limitation (§ 451.001).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `texas.md`, not hand-built; the preemption assertion is data for `LEGAL-013`'s evaluation stage, never an inline exception in a comparator.
  - **Refs:** [Preemption](specs/legal-rule-packs-and-state-configuration.md#64-preemption), [state employment-law research: Texas](research/state-employment-law/texas.md).

- [ ] `LEGAL-ST-UT-001` **[PHASE_2][SOL_HIGH] Carry Utah's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-006`, `LEGAL-TOOL-008`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_UT_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_UT_001`; `GOLDEN=TestTodo_LEGAL_ST_UT_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_UT_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_UT_001_Mutation`.
  - **RED:** no Utah pack is registered; Utah's 24-hour discharge-final-pay rule is the strictest deadline in the corpus after Colorado's immediate rule, and its flat 1-year non-compete cap (no key-employee or compensation gate, unlike Idaho's) has no typed field distinguishing an unconditional cap from a conditional one.
  - **GREEN:** the pack carries: `WAGE_FLOOR` federal-only, `F` ($7.25/hr, no tip-credit sub-minimum); `PAY_FREQUENCY` semimonthly or more frequent, monthly allowed for salaried employees (§ 34-28-3); `FINAL_PAY_DEADLINE` 24 hours on involuntary discharge, next regular payday on resignation, 60-day willful-withholding penalty accumulation (§ 34-28-5); `NON_COMPETE` flat 1-year post-employment cap, unconditional (§ 34-51-102, eff. 2016-05-10); `E_VERIFY` mandatory for 150+ employees, no penalty for non-compliance (§ 13-47-101); `BREACH_NOTIFICATION` "without unreasonable delay" if misuse is reasonably likely (§ 13-44-101).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `utah.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: Utah](research/state-employment-law/utah.md).

- [ ] `LEGAL-ST-VT-001` **[PHASE_2][SOL_HIGH] Carry Vermont's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-006`, `LEGAL-TOOL-007`, `LEGAL-TOOL-008`, `LEGAL-TOOL-010`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_VT_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_VT_001`; `GOLDEN=TestTodo_LEGAL_ST_VT_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_VT_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_VT_001_Mutation`.
  - **RED:** no Vermont pack is registered; `PAY_STMT` is `?` in the matrix, and Vermont's voluntary employer-opt-in PFML program (0.6% of employers enrolled) has no typed distinction from a mandatory program in `LeaveInteraction` today, risking the pack over-claiming a mandate that does not exist.
  - **GREEN:** the pack carries: `WAGE_FLOOR` $14.42/hr (2026), lower-of-5%-or-CPI annual index (21 V.S.A. § 384), tipped $7.21/hr; `PAY_TRANSPARENCY` wage/salary-range disclosure for 5+ employers, eff. 2025-07-01 (Act 155, § 495n); `FIELD_RESTRICTION` salary-history ban, no compensation-minimum/maximum hiring condition (§ 495m); `FINAL_PAY_DEADLINE` within 72 hours on discharge, next payday or following Friday on resignation (§ 342a); `LEAVE_INTERACTION` Earned Sick Time 1hr/52hrs for 18+-hr/week workers (§§ 481-486), unpaid parental/family leave at 10+/15+ employers (Act 32, 2025), and a voluntary (not mandated) PFML insurance program via The Hartford — `Standard=RECOMMENDED`, never modeled as a mandatory employer contribution; `ANTI_RETALIATION` ban-the-box until after interview qualification (§ 495j); `BREACH_NOTIFICATION` 45-day notice, 14-day AG notice (9 V.S.A. § 2435).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `vermont.md`, not hand-built; the voluntary PFML program is carried as a `RECOMMENDED` standard, never promoted to a statutory requirement.
  - **Refs:** [Authoring and review pipeline](specs/legal-rule-packs-and-state-configuration.md#7-authoring-and-review-pipeline), [state employment-law research: Vermont](research/state-employment-law/vermont.md).

- [ ] `LEGAL-ST-VA-001` **[PHASE_2][SOL_HIGH] Carry Virginia's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-016`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-006`, `LEGAL-TOOL-007`, `LEGAL-TOOL-008`, `LEGAL-TOOL-010`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_VA_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_VA_001`; `GOLDEN=TestTodo_LEGAL_ST_VA_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_VA_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_VA_001_Mutation`.
  - **RED:** no Virginia pack is registered; Va. Code § 40.1-28.7:12 (wage-range transparency and salary-history ban) takes effect 2026-09-03 — the exact date the contract's boundary-vector requirement pins as one of `LEGAL-016`'s two mandatory Virginia golden vectors (2026-09-02 vs. 2026-09-04) — and no release exists to produce either receipt.
  - **GREEN:** the pack carries: `WAGE_FLOOR` scheduled steps $12.77/hr (2026-2027) rising to $15.00/hr (2028-2029), then CPI-indexed (§ 40.1-28.10); `PAY_TRANSPARENCY`/`FIELD_RESTRICTION` wage-range-in-postings and salary-history ban eff. 2026-09-03, civil penalties $1,000/$5,000, 1-year private right of action (§ 40.1-28.7:12) — the pack's `window.start` for this obligation is exactly 2026-09-03T00:00:00, and `LEGAL-016`'s two boundary vectors (2026-09-02 with the obligation absent, 2026-09-04 with it present) are this pack's own golden fixtures; `NOTICE` written notice for pay decreases only, 1+ pay period advance (§ 40.1-29); `NON_COMPETE` void for low-wage and FLSA-overtime-eligible employees and all healthcare professionals, eff. 2025-07-01 (SB 1218, § 40.1-28.7:8); `PAY_FREQUENCY` biweekly/semimonthly for hourly, monthly for salaried, treble damages for knowing violations (§ 40.1-29); `LEAVE_INTERACTION` = home-health-worker-only paid sick leave, `L` scope, not general (§ 40.1-33.3 et seq.).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `virginia.md`, not hand-built; the two boundary-date golden files are checked into `testdata/legal/us-va/` per contract § 8.1, not hand-authored separately from the pack.
  - **Refs:** [Effective dating and legal time](specs/legal-rule-packs-and-state-configuration.md#23-effective-dating-and-legal-time), [Test strategy](specs/legal-rule-packs-and-state-configuration.md#8-test-strategy), [state employment-law research: Virginia](research/state-employment-law/virginia.md).

- [ ] `LEGAL-ST-WA-001` **[PHASE_2][SOL_HIGH] Carry Washington's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-006`, `LEGAL-TOOL-007`, `LEGAL-TOOL-008`, `LEGAL-TOOL-009`, `LEGAL-TOOL-010`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_WA_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_WA_001`; `GOLDEN=TestTodo_LEGAL_ST_WA_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_WA_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_WA_001_Mutation`.
  - **RED:** no Washington pack is registered; `BREACH_NOTIFICATION`'s missing citation is one of the eleven flagged in spec § 12 extraction findings, and Washington is one of the two required conformance boundary states (with Wisconsin) whose locality-overlay-present/-absent difference `LEGAL-016` must prove, yet has no release at all.
  - **GREEN:** the pack carries: `WAGE_FLOOR` $17.13/hr (2026), CPI-W-indexed annually (RCW 49.46); `PAY_TRANSPARENCY`/`FIELD_RESTRICTION` salary-history ban, pay-range posting for 15+ employers, 5-business-day cure and $100-$5,000 statutory damages, eff. 2025-07-27 (RCW 49.58, SB 5408); `FINAL_PAY_DEADLINE` all earned wages due by end of the pay period, double damages plus attorney fees for willful non-payment (RCW 49.48.010); `LEAVE_INTERACTION` paid sick leave 1hr/40hrs, immigration-proceedings use added 2025-07-27 (RCW 49.46.210), plus PFML 12-week paid leave, 25+ employers, 180-day tenure, eff. 2026-01-01 (RCW 50A); `NON_COMPETE` unenforceable below $123,394.17 (2025, indexed), 18-month presumption of unreasonableness, non-solicitation narrowed to current customers eff. 2024-06-06 (RCW 49.62.020); `PERSONNEL_FILE` 21-day access window, $250-$1,000 penalties, eff. 2025-07-27 (RCW 49.12.240/250); `MINI_WARN` state-specific 60-day notice for 50+-employee closures/layoffs, eff. 2025-07-27 (SB 5525); `BREACH_NOTIFICATION` citation to be sourced from state breach-notification statute rather than left uncited.
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `washington.md`, not hand-built; the Milwaukee-style locality-overlay boundary vector `LEGAL-016` requires is drawn from Washington's own registered/unregistered locality contrast, not invented separately.
  - **Refs:** [Test strategy](specs/legal-rule-packs-and-state-configuration.md#8-test-strategy), [state employment-law research: Washington](research/state-employment-law/washington.md).

- [ ] `LEGAL-ST-WV-001` **[PHASE_2][SOL_HIGH] Carry West Virginia's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-008`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_WV_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_WV_001`; `GOLDEN=TestTodo_LEGAL_ST_WV_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_WV_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_WV_001_Mutation`.
  - **RED:** no West Virginia pack is registered; the LEGAL-018 fix already corrected `west-virginia.md`'s federal-WARN-threshold restatement (wrongly "50+ employees") to defer to `us-federal.md`, and separately confirmed the file's personnel-file citation (W. Va. Code § 21-3-22, a public-improvement construction-safety statute) is unrelated to personnel files — West Virginia has no private-sector personnel-file-access statute at all, correctly modeled as absent.
  - **GREEN:** the pack carries: `WAGE_FLOOR` $8.75/hr for 6+-employee employers, federal $7.25/hr otherwise (W. Va. Code § 21-5C-2); `NOTICE` written notice at hire and 1-full-pay-period advance notice of any pay/period/place/method change, mandatory reimbursement of the reduction amount on the initial paycheck for noncompliance (§ 21-5-9(1)-(4)); `PAY_FREQUENCY` semi-monthly, ≤19 days between paydays (§ 21-5-3); `FINAL_PAY_DEADLINE` on or before the next regular payday (§ 21-5-4), 2x liquidated damages for wage theft (§ 21-5-4(e)); `PAY_EQUITY_REVIEW` sex-based comparable-character work (§ 21-5B); `RETENTION` 2-year minimum payroll records (§ 21-5C-5); `PERSONNEL_FILE` = no private-sector statutory right (confirmed, corrected citation).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `west-virginia.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: West Virginia](research/state-employment-law/west-virginia.md).

- [ ] `LEGAL-ST-WI-001` **[PHASE_2][SOL_HIGH] Carry Wisconsin's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-013`, `LEGAL-015`, `LEGAL-016`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-008`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_WI_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_WI_001`; `GOLDEN=TestTodo_LEGAL_ST_WI_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_WI_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_WI_001_Mutation`.
  - **RED:** no Wisconsin pack is registered; Wisconsin has the corpus's broadest `PreemptionAssertion` (`WAGE_FLOOR`, `LEAVE_INTERACTION`, `FIELD_RESTRICTION`, `PAY_TRANSPARENCY` all preempted at locality level, Wis. Stat. § 104.001(2), § 103.10(1m), AB 748), which the contract names as the concrete Milwaukee-paid-leave failure case and one of `LEGAL-016`'s two required boundary vectors (with and without a Milwaukee locality overlay registered); the LEGAL-018 review also removed the file's invented Milwaukee minimum-wage-ordinance claim that § 104.001(2) itself preempts.
  - **GREEN:** the pack carries: `WAGE_FLOOR` federal-only, `F` ($7.25/hr, tipped $2.33/hr or $2.13/hr for opportunity employees); `PreemptionAssertion` `WAGE_FLOOR`, `LEAVE_INTERACTION`, `FIELD_RESTRICTION`, `PAY_TRANSPARENCY` all preempted at locality level statewide (Wis. Stat. § 104.001(2), § 103.10(1m), AB 748) — this is the exact preemption `LEGAL-016`'s Milwaukee-with/-without-overlay boundary vector tests; `FINAL_PAY_DEADLINE` next regular payday, or 24 hours on business-closure/merger/relocation separation (§ 109.03); `NON_COMPETE` reasonableness test with blue-pencil (Runzheimer, 2015 WI 45, § 103.465); `PERSONNEL_FILE` twice/calendar-year, 7-working-day response (§ 103.13); `MINI_WARN` 50+-employee, 60-day notice for 25%-or-25-worker reductions (§ 109.07).
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `wisconsin.md`, not hand-built; the with/without-Milwaukee-overlay boundary vector is a shared fixture with `LEGAL-016`, not duplicated here.
  - **Refs:** [Preemption](specs/legal-rule-packs-and-state-configuration.md#64-preemption), [Test strategy](specs/legal-rule-packs-and-state-configuration.md#8-test-strategy), [state employment-law research: Wisconsin](research/state-employment-law/wisconsin.md).

- [ ] `LEGAL-ST-WY-001` **[PHASE_2][SOL_HIGH] Carry Wyoming's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-015`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-008`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_WY_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_WY_001`; `GOLDEN=TestTodo_LEGAL_ST_WY_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_WY_001_Conformance`; `MUTATION=TestTodo_LEGAL_ST_WY_001_Mutation`.
  - **RED:** no Wyoming pack is registered; the corpus's own extraction findings (spec § 12) flag Wyoming's `DRUG_TESTING` obligation as missing a locatable statutory section, and Wyoming's SF 107 non-compete overhaul (eff. 2025-07-01) voiding most post-employment covenants except for four named carve-outs has no `NON_COMPETE` release capturing the carve-out list.
  - **GREEN:** the pack carries: `WAGE_FLOOR` federal-only, `F` ($7.25/hr, tipped $2.13/hr with wage-guarantee top-up, Wyo. Stat. § 27-4-202); `PAY_FREQUENCY` semimonthly for industrial/factory operations, itemized stub required (§ 27-4-101); `FINAL_PAY_DEADLINE` next regular payday or employer's usual schedule, 18% statutory interest plus attorney fees if wrongfully withheld (§ 27-4-104); `NON_COMPETE` void except for executives/managerial staff, business sale, trade-secret protection, or training/relocation-expense recovery, eff. 2025-07-01 (SF 107, § 6-3-501 et seq.); `PAY_EQUITY_REVIEW` "comparable work" within the same establishment (§ 27-4-302); `JOB_SECURITY` `HANDBOOK_DISCLAIMER` standard — clear, conspicuous, prominent disclaimer required to preserve at-will status (Trabing v. Kinko's, 2002); `DRUG_TESTING` obligation marked `VERIFY` pending the missing statutory citation noted in the corpus extraction findings.
  - **REFACTOR:** the pack is generated by `internal/governance/legal/extract` from `wyoming.md`, not hand-built.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research: Wyoming](research/state-employment-law/wyoming.md).

- [ ] `LEGAL-ST-DC-001` **[PHASE_2][SOL_HIGH] Research and carry the District of Columbia's promotion/base-pay obligation parameters in a reviewed RulePack.**
  - **Depends:** `LEGAL-002`, `LEGAL-008`, `LEGAL-015`, `LEGAL-017`, `LEGAL-TOOL-001`, `LEGAL-TOOL-003`, `LEGAL-TOOL-004`, `LEGAL-TOOL-005`, `LEGAL-TOOL-008`, `LEGAL-TOOL-011`, `LEGAL-TOOL-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEGAL_ST_DC_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEGAL_ST_DC_001`; `GOLDEN=TestTodo_LEGAL_ST_DC_001_Golden`; `CONFORMANCE=TestTodo_LEGAL_ST_DC_001_Conformance`.
  - **RED:** no `planning/research/state-employment-law/district-of-columbia.md` file exists at all; the fifty-file corpus and the § 5 matrix cover the fifty states only, so the District has no row, no matrix cell of any value (not even `?`), no pack, and none of its well-known employer obligations (D.C. Wage Theft Prevention Amendment Act hire/change notice, D.C. Universal Paid Leave Act employer-funded benefit, the near-total non-compete ban under the Ban on Non-Compete Agreements Amendment Act) are captured anywhere in the platform; `LEGAL-TOOL-008`'s form registry and every other cross-cutting tool that iterates "the fifty states" silently excludes the District today.
  - **GREEN:** a `district-of-columbia.md` research file is authored following the same template as the fifty state files (README.md's twelve-section format, Sources with retrieval dates, "verify" markers for anything uncertain), and only once it exists does this pack carry typed obligations sourced from it — `WAGE_FLOOR`, `NOTICE`, `PAY_TRANSPARENCY`, `LEAVE_INTERACTION`, `NON_COMPETE`, `PAY_EQUITY_REVIEW`, `FINAL_PAY_DEADLINE` and `PERSONNEL_FILE` are each either populated with a cited D.C. Code section and confidence marker or explicitly marked `F`/`?` with the reason recorded, exactly as the fifty-state matrix requires column completeness; no figure is asserted here without that citation existing first, per the extractor's narrow-or-drop rule (contract § 7.1).
  - **REFACTOR:** once authored, the District's row extends Table A/B and `LEGAL-008`'s coverage test to fifty-one rows; `LEGAL-TOOL-008`'s separation-filing registry and `LEGAL-TOOL-009`'s locality registry both gain a District entry rather than treating it as an ordinary locality of Maryland or Virginia.
  - **Refs:** [Per-state configuration matrix](specs/legal-rule-packs-and-state-configuration.md#5-per-state-configuration-matrix), [state employment-law research README](research/state-employment-law/README.md).

- [ ] `WAGE-001` **[PHASE_3][SOL_HIGH] Evaluate wage-hour obligations deterministically.**
  - **Depends:** `LEGAL-001`, `LEGAL-002`, `CONF-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WAGE_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WAGE_001`; `PROPERTY=TestTodo_WAGE_001_Property`; `RACE=TestTodo_WAGE_001_Race`; `SECURITY=TestTodo_WAGE_001_Security`; `MUTATION=TestTodo_WAGE_001_Mutation`.
  - **RED:** missing workweek/timezone/approved time/exemption/minimum/overtime/break/rounding input produces payable output.
  - **GREEN:** return exact regular/overtime/double-time/premium/break/minimum-wage results, rule trace and explicit `UNKNOWN/REVIEW_REQUIRED`; decimal arithmetic only.
  - **REFACTOR:** calculation is pure and replayable.
  - **Refs:** [Wage-hour engine](plan.md).

- [ ] `TAX-001` **[PHASE_3][SOL_HIGH] Calculate version-pinned worker and employer taxes.**
  - **Depends:** `LEGAL-001`, `WAGE-001`, `CONF-006`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_TAX_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TAX_001`; `PROPERTY=TestTodo_TAX_001_Property`; `RACE=TestTodo_TAX_001_Race`; `SECURITY=TestTodo_TAX_001_Security`; `MUTATION=TestTodo_TAX_001_Mutation`.
  - **RED:** missing tax profile/registration/wage basis/election/rule version permits net pay or current rules alter historical fixture.
  - **GREEN:** exact golden vector returns taxable wages, employee/employer tax, authority, bases/accumulators, rounding, rule trace and balanced accounting delta.
  - **REFACTOR:** correction appends new result and delta.
  - **Refs:** [Tax engine](plan.md), [payroll correction](workflows/payroll/payroll-correction-retro.md).

- [ ] `FILING-001` **[PHASE_3][SOL_HIGH] Generate and submit an immutable government FilingPackage.**
  - **Depends:** `TAX-001`, `INTG-011`, `INTG-013`, `MODEL-029`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_FILING_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_FILING_001`; `GOLDEN=TestTodo_FILING_001_Golden`; `RACE=TestTodo_FILING_001_Race`; `INTEGRATION=TestTodo_FILING_001_Integration`; `FAULT=TestTodo_FILING_001_Fault`; `SECURITY=TestTodo_FILING_001_Security`; `MUTATION=TestTodo_FILING_001_Mutation`.
  - **RED:** wrong period/jurisdiction/schema, missing signature/source watermark/approval or duplicate filing dispatches.
  - **GREEN:** package binds report definition/version, source values, rendered/submission hashes, signer and idempotency; provider timeout remains `AMBIGUOUS`.
  - **REFACTOR:** gateway owns transport, reporting engine owns fields.
  - **Refs:** [Government reporting](plan.md), [filing gateway](specs/platform-responsibility-boundaries.md).

- [ ] `FILING-002` **[PHASE_3][SOL_HIGH] Reconcile acknowledgements and append amendments.**
  - **Depends:** `FILING-001`, `INTG-014`, `INTG-016`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_FILING_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_FILING_002`; `PROPERTY=TestTodo_FILING_002_Property`.
  - **RED:** rejected/partial/late/payment-mismatched filing is complete or amendment overwrites original.
  - **GREEN:** state distinguishes prepared/submitted/accepted/rejected/paid/amended/repair-required; amendment references original and balances to source facts.
  - **REFACTOR:** government acceptance is external observation, not inferred success.
  - **Refs:** [Filing lifecycle](plan.md), [reconciliation](specs/transaction-ledger-reconciliation-and-repair.md).

## 21. Future-domain intent conformance

> **Disposition (2026-09-02):** DEFERRED. `INTENT-CONF-001` covers the fourteen drafted definitions only. Conformance descriptors for undrafted domains are written when a domain is funded.

- [x] `INTENT-CONF-001` **[P0][SOL_HIGH] Require a finite conformance descriptor for each of the fourteen drafted definitions.**
  - **Evidence (2026-09-05):** `TestTodo_INTENT_CONF_001`, `TestTodo_INTENT_CONF_001_{Golden,Conformance,Mutation}` in `tools/planning/intentmanifests` over the published `definitions/governance/intent-conformance-descriptors.yaml` (exactly one finite descriptor per draft intent: family, side-effect profile, reads, writes, effects, authority and temporal boundaries, five-dimension lifecycle, evidence, negative-policy matrix, scenario); `go test -count=1 ./tools/planning/intentmanifests/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-016`, `CONF-001`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.PEOPLE,BI.REWARDS,BI.WORK,BI.INTELLIGENCE,BI.OPERATIONS; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_INTENT_CONF_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_INTENT_CONF_001`; `GOLDEN=TestTodo_INTENT_CONF_001_Golden`; `CONFORMANCE=TestTodo_INTENT_CONF_001_Conformance`; `MUTATION=TestTodo_INTENT_CONF_001_Mutation`.
  - **RED:** one of the fourteen definitions lacks family, entities/properties, reads/writes/effects, authority, time, five-dimension lifecycle, evidence, negative-policy matrix or scenario.
  - **GREEN:** generated manifest has exactly fourteen rows with a stable digest; conformance-only definitions (`change_manager`) are marked as such; no row exists for an undrafted name.
  - **REFACTOR:** manifest is generated from the registry, not duplicated manually.
  - **Refs:** [Draft slice](specs/business-intent-catalog.md#initial-draft-contract-slice), [covered entity set](data/models/README.md).

- [x] `CONF-009` **[CONFORMANCE][SOL_HIGH] Prove payroll run, calculation, release and settlement states.**
  - **Evidence (2026-09-05):** `TestTodo_CONF_009`, `TestTodo_CONF_009_{Property,Race,Integration,Fault,Conformance,HiddenEffect}` plus definition/environment/setup/smoke tests in `internal/workflow/conformance/payroll` (real payroll-run definition compiled under P1A and driven through the SIMULATE interpreter: calculation as exact decimal, release and settlement states with approvals awaiting one work item per requirement, reversal path, hidden EXTERNAL_MUTATION refused by simulate.Admit; finished by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/workflow/conformance/payroll/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CONF-001`, `INTENT-CONF-001`, `TIME-001`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_CONF_009`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CONF_009`; `PROPERTY=TestTodo_CONF_009_Property`; `RACE=TestTodo_CONF_009_Race`; `INTEGRATION=TestTodo_CONF_009_Integration`; `FAULT=TestTodo_CONF_009_Fault`; `CONFORMANCE=TestTodo_CONF_009_Conformance`.
  - **RED:** stale population/cutoff, duplicate run, float rounding, missing trace, partial payment or provider ambiguity is called released/settled.
  - **GREEN:** version-pinned fixtures return exact run states and line totals; payment states remain submitted/accepted/settled/rejected/ambiguous/reversed with idempotent repair.
  - **REFACTOR:** test adapter supplies computation/provider; no Phase 1 payroll engine.
  - **Refs:** [Rewards/payroll model](data/models/rewards-payroll-workforce.md), [payroll intents](specs/business-intent-catalog.md).

- [x] `CONF-010` **[CONFORMANCE][SOL_HIGH] Prove benefits eligibility, elections and carrier reconciliation.**
  - **Evidence (2026-09-05):** `TestTodo_CONF_010`, `TestTodo_CONF_010_{Property,Race,Conformance,HiddenEffect}` plus definition/environment/setup/smoke tests in `internal/workflow/conformance/benefits` (eligibility, elections with a prior-election digest, carrier reconciliation as observed reads dispatched by node id, approvals with obligations, hidden effect refused; finished by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/workflow/conformance/benefits/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CONF-001`, `INTENT-CONF-001`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_CONF_010`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CONF_010`; `PROPERTY=TestTodo_CONF_010_Property`; `RACE=TestTodo_CONF_010_Race`; `CONFORMANCE=TestTodo_CONF_010_Conformance`.
  - **RED:** disputed fact, expired window, duplicate life event, overlapping election or partial carrier/deduction outcome becomes full success.
  - **GREEN:** fixture returns eligibility trace and immutable election revisions; carrier and payroll deduction observations have independent PASS/FAIL/PARTIAL/UNKNOWN and repair states.
  - **REFACTOR:** no native carrier administration.
  - **Refs:** [Benefits model](data/models/rewards-payroll-workforce.md), [benefit intents](specs/business-intent-catalog.md).

- [x] `CONF-011` **[CONFORMANCE][SOL_HIGH] Prove time punch, timecard and payroll-bridge integrity.**
  - **Evidence (2026-09-05):** `TestTodo_CONF_011`, `TestTodo_CONF_011_{Conformance,Recovery,Mutation,HiddenEffect}` plus definition/environment/ports/setup/smoke tests in `internal/workflow/conformance/time` (punch to timecard to payroll-bridge integrity through the SIMULATE interpreter, bridge integrity refused on digest mismatch, recovery after a bounded repair, hidden effect refused; finished by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/workflow/conformance/time/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CONF-001`, `INTENT-CONF-001`, `TIME-001`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_CONF_011`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CONF_011`; `CONFORMANCE=TestTodo_CONF_011_Conformance`; `RECOVERY=TestTodo_CONF_011_Recovery`; `MUTATION=TestTodo_CONF_011_Mutation`.
  - **RED:** clock skew/DST fold/offline replay/shared-device spoof/post-lock edit or stale reopened timecard is accepted silently.
  - **GREEN:** exact ACCEPTED/REVIEW/REJECTED/DUPLICATE result preserves timezone/tzdb, attestation, correction, cutoff and bridge observation/repair evidence.
  - **REFACTOR:** no Phase 1 timekeeping product.
  - **Refs:** [Time model](data/models/rewards-payroll-workforce.md), [time intents](specs/business-intent-catalog.md).

- [x] `CONF-012` **[CONFORMANCE][SOL_HIGH] Prove talent, performance and succession evidence semantics.**
  - **Evidence (2026-09-05):** `TestTodo_CONF_012` in `internal/workflow/conformance/talent` (talent, performance and succession evidence semantics as a real definition through the SIMULATE interpreter with typed states, approvals with obligations and hidden-effect refusal; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/workflow/conformance/talent/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CONF-001`, `INTENT-CONF-001`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_CONF_012`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CONF_012`; `CONFORMANCE=TestTodo_CONF_012_Conformance`; `MUTATION=TestTodo_CONF_012_Mutation`; `SECURITY=TestTodo_CONF_012_Security`.
  - **RED:** stale population, unauthorized rating, calibration conflict, prohibited decision use or correction overwrites original assessment.
  - **GREEN:** revisioned goals/reviews/ratings distinguish FACT/HUMAN_OPINION/MODEL_INFERENCE, preserve calibration/contest lineage and return explicit unknown/rejected outcomes.
  - **REFACTOR:** recommendation is not authoritative decision.
  - **Refs:** [Talent model](data/models/talent-experience-cases.md), [talent intents](specs/business-intent-catalog.md).

- [x] `CONF-013` **[CONFORMANCE][SOL_HIGH] Prove learning, skills and credentials.**
  - **Evidence (2026-09-05):** `TestTodo_CONF_013` in `internal/workflow/conformance/learning` (learning, skills and credentials with expiring credential windows, approvals with obligations and hidden-effect refusal through the SIMULATE interpreter; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/workflow/conformance/learning/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CONF-001`, `INTENT-CONF-001`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_CONF_013`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CONF_013`; `INTEGRATION=TestTodo_CONF_013_Integration`; `FAULT=TestTodo_CONF_013_Fault`; `SECURITY=TestTodo_CONF_013_Security`; `CONFORMANCE=TestTodo_CONF_013_Conformance`; `MUTATION=TestTodo_CONF_013_Mutation`.
  - **RED:** expired equivalence/credential, inaccessible content, duplicate enrollment, invalid waiver or ambiguous provider completion satisfies requirement.
  - **GREEN:** fixture returns SATISFIED/UNSATISFIED/EXPIRING/UNKNOWN/EXEMPT with evidence validity, waiver authority and provider reconciliation.
  - **REFACTOR:** skill claim and verified evidence remain distinct.
  - **Refs:** [Learning model](data/models/talent-experience-cases.md), [learning intents](specs/business-intent-catalog.md).

- [x] `CONF-014` **[CONFORMANCE][SOL_HIGH] Prove HR case and employee-relations compartment semantics.**
  - **Evidence (2026-09-05):** `TestTodo_CONF_014` in `internal/workflow/conformance/hrcase` (HR case and employee-relations compartment semantics: compartmented visibility, separation of duties, approvals with obligations and hidden-effect refusal through the SIMULATE interpreter; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/workflow/conformance/hrcase/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CONF-001`, `INTENT-CONF-001`, `WORK-001`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_CONF_014`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CONF_014`; `RACE=TestTodo_CONF_014_Race`; `CONFORMANCE=TestTodo_CONF_014_Conformance`; `SECURITY=TestTodo_CONF_014_Security`.
  - **RED:** unauthorized participant, privilege/matter-wall breach, retaliation exposure, investigator conflict, hold race or appeal overwrites history.
  - **GREEN:** fixture preserves assignment fences, confidential notes/evidence custody, SLA, finding/review/disposition/reopen/appeal and explicit unknown/repair states.
  - **REFACTOR:** case access is relationship and purpose scoped.
  - **Refs:** [Cases and ER](data/models/talent-experience-cases.md), [case intents](specs/business-intent-catalog.md).

- [x] `CONF-015` **[CONFORMANCE][SOL_HIGH] Prove mobility, immigration and cross-border privacy semantics.**
  - **Evidence (2026-09-05):** `TestTodo_CONF_015` in `internal/workflow/conformance/mobility` (mobility, immigration and cross-border privacy semantics with jurisdiction-scoped disclosure, approvals with obligations and hidden-effect refusal through the SIMULATE interpreter; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/workflow/conformance/mobility/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CONF-001`, `INTENT-CONF-001`, `LEGAL-001`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_CONF_015`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CONF_015`; `SECURITY=TestTodo_CONF_015_Security`; `CONFORMANCE=TestTodo_CONF_015_Conformance`; `MUTATION=TestTodo_CONF_015_Mutation`.
  - **RED:** expired authorization, overlapping assignment, unknown tax/PE impact or prohibited transfer is executable/complete.
  - **GREEN:** dated mobility legs, home/host payroll, authorization milestones, tax/PE unknowns and privacy transfer decision yield explicit blocked/degraded/repair states.
  - **REFACTOR:** immigration vendor result remains observation.
  - **Refs:** [Mobility model](data/models/talent-experience-cases.md), [transfer workflow](workflows/workforce/org-transfer-compensation.md).

- [ ] `CONF-016` **[CONFORMANCE][SOL_HIGH] Prove safety, injury and workers-compartment semantics.**
  - **Depends:** `CONF-001`, `INTENT-CONF-001`, `FILING-002`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_CONF_016`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CONF_016`; `GOLDEN=TestTodo_CONF_016_Golden`; `SECURITY=TestTodo_CONF_016_Security`; `CONFORMANCE=TestTodo_CONF_016_Conformance`; `MUTATION=TestTodo_CONF_016_Mutation`.
  - **RED:** medical evidence leaks, duplicate claim/payment, uncertain reportability is definitive or restriction clears without authority.
  - **GREEN:** protected evidence, reportability revision, claim/payment/reversal, corrective action and filing amendment preserve lineage and reconciliation.
  - **REFACTOR:** safety operational facts and medical facts use separate compartments.
  - **Refs:** [Safety model](data/models/talent-experience-cases.md), [safety intents](specs/business-intent-catalog.md).

## 22. Dependency-closure additions

> **Disposition (2026-09-02):** As labelled; `CONFLICT-004` is P1B.

- [ ] `CONFLICT-004` **[GATE_B][SOL_HIGH] Re-evaluate the complete cross-workflow conflict set at execution.**
  - **Depends:** `CONFLICT-003`, `GOVERN-003`, `TX-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CONFLICT_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CONFLICT_004`; `PROPERTY=TestTodo_CONFLICT_004_Property`; `RACE=TestTodo_CONFLICT_004_Race`; `MUTATION=TestTodo_CONFLICT_004_Mutation`.
  - **RED:** a transfer, termination, leave, org move, compensation change or reservation created after approval is absent from execution preflight.
  - **GREEN:** preflight compares pinned and current footprints/effective intervals and returns `CLEAR`, `REAPPROVAL_REQUIRED`, `REPLAN_REQUIRED`, or `BLOCKED`; blocked path commits zero business effects.
  - **REFACTOR:** conflict policy is versioned by domain/property and explains every overlap.
  - **Refs:** [Conflict control](specs/transaction-ledger-reconciliation-and-repair.md), [execution revalidation](reference-workflows/reference-suite.md#shared-platform-primitives-revealed).

- [x] `APPROVAL-004` **[GATE_A][SOL_HIGH] Enforce separation of duties, delegation and fallback during approver resolution.**
  - **Depends:** `APPROVAL-001`, `TRUST-015`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORK; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_APPROVAL_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_APPROVAL_004`; `MUTATION=TestTodo_APPROVAL_004_Mutation`.
  - **RED:** requester approves own request, same principal fills two distinct roles, expired/out-of-scope delegate decides or fallback broadens authority.
  - **GREEN:** candidate set excludes invalid principals, records resolution/effective-time evidence and returns `NO_AUTHORIZED_APPROVER` when quorum cannot be formed.
  - **REFACTOR:** recipient routing and decision authority remain separate.
  - **Refs:** [Approval model](specs/workflow-runtime.md), [delegation](specs/organization-scope-and-authz.md).
  - **Evidence (2026-09-03):** `TestTodo_APPROVAL_004`, `_Mutation` in `internal/humanwork` (`Resolve`: requester/subject/dual-role/manager-chain exclusions, delegation scope and expiry, authority floor, fallback may not broaden authority, explained by rule id); PASS; branch plan-revision-2026-09-02; `go test -count=1 ./internal/humanwork/...` on windows/arm64 (Go 1.26.3).

- [x] `APPROVAL-005` **[GATE_B][SOL_HIGH] Revalidate approval authority and proposal binding immediately before execution.**
  - **Evidence (2026-09-05):** `TestTodo_APPROVAL_005` in `internal/intent/approval` (execution revalidates approver authority and the proposal binding immediately before the commit through the approval package's revalidation port, refusing with a typed APPROVAL_STALE when the approver lost authority or the proposal digest moved after the decision; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/intent/approval/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `APPROVAL-002`, `APPROVAL-003`, `APPROVAL-004`, `GOVERN-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORK; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_APPROVAL_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_APPROVAL_005`; `GOLDEN=TestTodo_APPROVAL_005_Golden`; `SECURITY=TestTodo_APPROVAL_005_Security`; `MUTATION=TestTodo_APPROVAL_005_Mutation`.
  - **RED:** changed proposal, invalidated relationship/role, expired delegation, policy/legal change or revoked step-up still authorizes commit.
  - **GREEN:** all approval requirements return current `VALID` with exact proposal/control digests or execution returns `REAPPROVAL_REQUIRED` and zero effects.
  - **REFACTOR:** task recipient is not assumed to retain authority.
  - **Refs:** [Proposal binding](specs/workflow-runtime.md), [execution revalidation](plan.md).

- [x] `GOV-016` **[P0][LUNA] Reject unresolved, prose, cyclic and phase-inverted todo dependencies.**
  - **Evidence (2026-09-05):** `TestTodo_GOV_016`, `TestTodo_GOV_016_{Golden,Property}`, `FuzzTodo_GOV_016` in `tools/planning/todogovernance` (unresolved, prose, cyclic and phase-inverted Depends rejected over the real registry; the live backlog's 291 phase-inversion edges, the LEGAL-002↔LEGAL-015 cycle and MSG-011's prose dependency are pinned in a reviewed allowlist so any new one fails; `go run ./tools/planning/cmd/todogovernance` reports them); `go test -count=1 ./tools/planning/todogovernance/.../` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `GOV-002`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_GOV_016`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_GOV_016`; `PROPERTY=TestTodo_GOV_016_Property`; `GOLDEN=TestTodo_GOV_016_Golden`; `FUZZ=FuzzTodo_GOV_016`.
  - **RED:** fixture includes unknown ID, free-form category, malformed range, cycle or Gate A dependency on Gate B/C implementation and validation reports exact file/line/code.
  - **GREEN:** the complete generated graph resolves every edge, is acyclic and proves Gate A closure contains no write-authority or deferred implementation node.
  - **REFACTOR:** explicitly declared external gates require owner, artifact, phase and acceptance digest.
  - **Refs:** [Plan hierarchy](plan.md), [execution dependencies](execution-plan.md#delivery-gates-staffing-envelope-and-critical-path).
  - **Evidence (partial, 2026-09-03):** `TestTodo_GOV_016`, `TestTodo_GOV_016_Property`, `TestTodo_GOV_016_Golden`, `FuzzTodo_GOV_016` in `tools/planning/dependencygraph` (unknown/prose/malformed-range/duplicate/cycle and direct/transitive Gate A phase-inversion diagnostics with stable ID/file/line/code); checker and `plancheck dependencygraph` adapter implemented, but the live corpus correctly remains non-green with 6,202 `GATE_A_PHASE_INVERSION` findings; `go test -count=1 ./tools/planning/dependencygraph ./tools/planning/cmd/plancheck`, `go test -fuzz=FuzzTodo_GOV_016 -fuzztime=2s ./tools/planning/dependencygraph`, and `go vet ./tools/planning/dependencygraph ./tools/planning/cmd/plancheck` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `DOC-001` **[P0][LUNA] Enforce normative planning-document integrity in CI.**
  - **Evidence (2026-09-05):** `TestTodo_DOC_001` in `tools/policy/docintegrity` (every normative document referenced from plan.md and the specs exists, every intra-repo markdown link and anchor resolves, every todo id cited in the specs resolves in the registry, orphan planning documents reported against an owner allowlist, with a cmd/docintegrity exiting non-zero; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/docintegrity/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02. Not yet wired into `npm run test:all` or CI: the live scan reports 181 pre-existing document issues (broken links, anchors and unresolved todo ids in planning/) that must be triaged first; wiring a failing gate would block every commit. Follow-up: fix or allowlist those, then add `check:doc-integrity` to package.json and the workflow step.
  - **Depends:** `GOV-004`, `GOV-016`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DOCUMENTS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_DOC_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DOC_001`; `PROPERTY=TestTodo_DOC_001_Property`; `GOLDEN=TestTodo_DOC_001_Golden`; `FUZZ=FuzzTodo_DOC_001`; `INTEGRATION=TestTodo_DOC_001_Integration`.
  - **RED:** invalid UTF-8/mojibake, unbalanced fence, broken local link/anchor, duplicate normative ID, stale generated index or invalid ASCII block passes.
  - **GREEN:** clean corpus passes and each injected defect reports stable code, file and line; generated heading/index count equals current todo registry.
  - **REFACTOR:** replace stale `todo-headings.txt` with generated output or remove it from authority.
  - **Refs:** [Documentation governance](specs/platform-foundation-gap-closure.md), [planning hierarchy](plan.md).

## 23. Document processing and signature execution

> **Disposition (2026-09-02):** P1B only `DOC-MAL-001`, and only if the pilot accepts an uploaded document. Templates, signature and evidence states are Phase 2.

- [x] `DOC-MAL-001` **[GATE_B][SOL_HIGH] Scan and quarantine uploaded artifacts before use.**
  - **Evidence (2026-09-05):** `TestTodo_DOC_MAL_001`, `TestTodo_DOC_MAL_001_Golden`, `FuzzTodo_DOC_MAL_001`, intake/scanner/sniff/store/use tests in `internal/domains/asset/quarantine`; `TestTodo_DOC_MAL_001_Integration` in `internal/data/artifacts` (migration `00030_artifact_quarantine.sql`: companion-schema `artifact_quarantine` bytes-by-content-id and append-only `artifact_quarantine_state` QUARANTINED→ADMITTED|REJECTED log ordered by an identity `seq`, since the quarantine row and its verdict share one transaction timestamp; both excluded from the disposition registry like 00010's companion tables; content-sniffed type gate, scanner port, use refused until ADMITTED); `go test -count=1 ./migrations/ ./internal/domains/asset/... ./internal/data/artifacts/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-029`, `TRUST-019`, `DATA-016`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DOCUMENTS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_DOC_MAL_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DOC_MAL_001`; `GOLDEN=TestTodo_DOC_MAL_001_Golden`; `FUZZ=FuzzTodo_DOC_MAL_001`; `INTEGRATION=TestTodo_DOC_MAL_001_Integration`.
  - **RED:** unscanned/oversized/archive-bomb/polyglot/infected/unscannable bytes reach preview, extraction, index, workflow or provider.
  - **GREEN:** scanner state is `SAFE`, `UNSAFE`, `UNSCANNABLE`, or `QUARANTINED`; only a content-addressed SAFE derivative may proceed and rescan revokes unsafe descendants.
  - **REFACTOR:** scanner/provider version and limits are evidence.
  - **Refs:** [Content safety](specs/platform-responsibility-boundaries.md), [artifact model](data/models/connectivity-access-content.md).

- [x] `DOC-INTAKE-001` **[PHASE_2][SOL_HIGH] Release a scanned upload as a governed evidence reference.**
  - **Evidence (2026-09-07):** `TestTodo_DOC_INTAKE_001`, `TestTodo_DOC_INTAKE_001_Golden`, `TestTodo_DOC_INTAKE_001_Integration`, `TestTodo_DOC_INTAKE_001_Mutation`, `TestTodo_DOC_INTAKE_001_Race`, `TestTodo_DOC_INTAKE_001_Security` in `internal/documents/intake` (written in 171cafc on 2026-09-03 and never ticked; the named tests prove the GREEN contract: intake freezes safe artifact generation, server-derived document type/metadata, `MEDICAL_SENSITIVE`-class compartment, evidence authority, provenance, purpose/access policy, retention/hold policy and returns only a typed `EvidenceRef`; later reclassification/rescan can revoke usability without rewriting history); `go test -count=1 ./internal/documents/intake/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DOC-MAL-001`, `ARTIFACT-005`, `MODEL-023`, `MODEL-026`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DOCUMENTS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_DOC_INTAKE_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DOC_INTAKE_001`; `GOLDEN=TestTodo_DOC_INTAKE_001_Golden`; `FUZZ=FuzzTodo_DOC_INTAKE_001`; `RACE=TestTodo_DOC_INTAKE_001_Race`; `INTEGRATION=TestTodo_DOC_INTAKE_001_Integration`; `SECURITY=TestTodo_DOC_INTAKE_001_Security`; `MUTATION=TestTodo_DOC_INTAKE_001_Mutation`.
  - **RED:** workflow/domain receives raw upload bytes, unsafe/unscannable artifact, caller-selected classification/retention/authority, mutable metadata or unrestricted download locator.
  - **GREEN:** intake freezes safe artifact generation, server-derived document type/metadata, `MEDICAL_SENSITIVE`-class compartment, evidence authority, provenance, purpose/access policy, retention/hold policy and returns only a typed `EvidenceRef`; later reclassification/rescan can revoke usability without rewriting history.
  - **REFACTOR:** malware scanning, byte storage, classification and domain evidence sufficiency remain separate owners.
  - **Refs:** [Document pipeline](specs/platform-responsibility-boundaries.md), [artifact models](data/models/connectivity-access-content.md), [records](specs/records-management-and-disposition.md).

- [x] `DOC-EXTRACT-001` **[PHASE_2][SOL_HIGH] Extract bounded text, OCR and metadata with source lineage.**
  - **Evidence (2026-09-05):** `TestTodo_DOC_EXTRACT_001` in `internal/engines/docextract` (bounded text and metadata extraction from in-memory plain and structured documents with an OCR port and in-memory fake, declared byte and page limits with typed refusal, every span carrying artifact digest, page and offset lineage, canonical digest of the extraction; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/engines/docextract/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DOC-MAL-001`, `MODEL-024`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DOCUMENTS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_DOC_EXTRACT_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DOC_EXTRACT_001`; `GOLDEN=TestTodo_DOC_EXTRACT_001_Golden`; `FUZZ=FuzzTodo_DOC_EXTRACT_001`; `INTEGRATION=TestTodo_DOC_EXTRACT_001_Integration`; `SECURITY=TestTodo_DOC_EXTRACT_001_Security`.
  - **RED:** parser exceeds resource limits, loses page/offset source, strips taint or partial extraction reports complete.
  - **GREEN:** result pins parser/profile version, source/derivative hashes, offsets/pages, classification/taint and `COMPLETE/PARTIAL/FAILED` state.
  - **REFACTOR:** extracted content is observation, never executable instruction.
  - **Refs:** [Document model](data/models/connectivity-access-content.md), [agent taint](data/models/assurance-intelligence-platform.md).

- [x] `DOC-REDACT-001` **[PHASE_2][SOL_HIGH] Generate deterministic policy-bound redacted derivatives.**
  - **Evidence (2026-09-05):** `TestTodo_DOC_REDACT_001` in `internal/engines/docredact` (policy-bound redacted derivatives over docextract results using dlp data classes with MASK/REMOVE/TOKENIZE styles, every redaction carrying lineage to its source span, byte-identical for identical inputs and policy version, canonical digest, unknown data class refused, golden derivative; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/engines/docredact/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DOC-EXTRACT-001`, `TRUST-018`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DOCUMENTS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_DOC_REDACT_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DOC_REDACT_001`; `GOLDEN=TestTodo_DOC_REDACT_001_Golden`; `INTEGRATION=TestTodo_DOC_REDACT_001_Integration`.
  - **RED:** redacted original still leaks through preview/thumbnail/OCR/search/export or coverage hashes disagree.
  - **GREEN:** all derivative surfaces pass the redaction oracle and evidence binds policy, source hash, covered regions/fields and output digest; mismatch returns `REDACTION_INCOMPLETE`.
  - **REFACTOR:** original remains access-controlled and historically intact.
  - **Refs:** [DLP](specs/data-classification-and-dlp.md), [document model](data/models/connectivity-access-content.md).

- [x] `DOC-TEMPLATE-001` **[GATE_B][TERRA] Publish and render immutable document templates.**
  - **Depends:** `CONFIG-002`, `UX-004`, `MODEL-029`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DOCUMENTS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_DOC_TEMPLATE_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DOC_TEMPLATE_001`; `GOLDEN=TestTodo_DOC_TEMPLATE_001_Golden`; `INTEGRATION=TestTodo_DOC_TEMPLATE_001_Integration`; `SECURITY=TestTodo_DOC_TEMPLATE_001_Security`; `BROWSER=TestTodo_DOC_TEMPLATE_001_Browser`.
  - **RED:** missing typed placeholder, wrong locale/jurisdiction/classification/accessibility metadata or changed-after-approval template renders.
  - **GREEN:** same bindings/version produce same artifact hash; publication records fixtures, approval, applicability and successor/retirement state.
  - **REFACTOR:** renderer receives field-masked typed bindings only.
  - **Refs:** [Document templates](data/models/connectivity-access-content.md), [experience localization](specs/experience-ui-and-branding.md).
  - **Evidence (2026-09-03):** `TestTodo_DOC_TEMPLATE_001`, Declared `DOC-TEMPLATE-001` test matrix and acceptance behavior in `internal/documents/template`; PASS; `go test -p 1 ./internal/documents/template` and `go vet ./internal/documents/template` on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [ ] `DOC-SIGN-001` **[PHASE_2][SOL_HIGH] Execute a proof-bound e-signature ceremony.**
  - **Depends:** `DOC-TEMPLATE-001`, `INTG-018`, `TRUST-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DOCUMENTS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_DOC_SIGN_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DOC_SIGN_001`; `GOLDEN=TestTodo_DOC_SIGN_001_Golden`; `INTEGRATION=TestTodo_DOC_SIGN_001_Integration`; `FAULT=TestTodo_DOC_SIGN_001_Fault`; `RECOVERY=TestTodo_DOC_SIGN_001_Recovery`.
  - **RED:** signer substitution, stale artifact, invalid callback/replay, expired request or provider acceptance alone reports signed.
  - **GREEN:** states distinguish requested/delivered/acknowledged/signed/declined/expired/failed/ambiguous and bind signer proof, ceremony, trusted time, exact artifact hash and provider receipt.
  - **REFACTOR:** manual fallback preserves equivalent authority/evidence.
  - **Refs:** [Signature model](data/models/connectivity-access-content.md), [integration callbacks](specs/integration-platform.md).

- [x] `DOC-EVIDENCE-001` **[PHASE_2][SOL_HIGH] Verify document and signature evidence-package completeness.**
  - **Evidence (2026-09-07):** `TestTodo_DOC_EVIDENCE_001`, `TestTodo_DOC_EVIDENCE_001_Golden`, `TestTodo_DOC_EVIDENCE_001_Integration`, `TestTodo_DOC_EVIDENCE_001_Mutation`, `TestTodo_DOC_EVIDENCE_001_Security`, `TestTodo_DOC_EVIDENCE_001_Unknown` in `internal/documents/evidence` (written in 171cafc on 2026-09-03 and never ticked; the named tests prove the GREEN contract: verifier returns `COMPLETE`, `PARTIAL`, `UNKNOWN`, or `REJECTED` with exact missing/invalid elements and canonical package digest); `go test -count=1 ./internal/documents/evidence/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DOC-SIGN-001`, `RECORDS-COPY-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DOCUMENTS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_DOC_EVIDENCE_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DOC_EVIDENCE_001`; `GOLDEN=TestTodo_DOC_EVIDENCE_001_Golden`; `INTEGRATION=TestTodo_DOC_EVIDENCE_001_Integration`; `SECURITY=TestTodo_DOC_EVIDENCE_001_Security`; `MUTATION=TestTodo_DOC_EVIDENCE_001_Mutation`.
  - **RED:** missing issuer/freshness/signature/classification/purpose/retention/hold/method/schema/rule/redaction/verifier authority passes.
  - **GREEN:** verifier returns `COMPLETE`, `PARTIAL`, `UNKNOWN`, or `REJECTED` with exact missing/invalid elements and canonical package digest.
  - **REFACTOR:** verification is independent of signature provider availability.
  - **Refs:** [Evidence model](data/models/kernel-governance-and-evidence.md), [records](specs/records-management-and-disposition.md).

## 24. Human decision safety and continuity

> **Disposition (2026-09-02):** P1B. `APPROVAL-006` is simplified to a server-held rendered-projection digest keyed by task version (Tier 1 in the context contract); a client-held receipt token is Gate C.

- [x] `APPROVAL-006` **[GATE_B][SOL_HIGH] Bind approval to the server-held rendered-projection digest for the task version.**
  - **Evidence (2026-09-05):** `TestTodo_APPROVAL_006` in `internal/intent/approval` (an approval carries the server-held rendered-projection digest of the task version it was shown; a decision against a different rendered digest is refused, so what the approver saw is what is executed; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/intent/approval/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `APPROVAL-005`, `UX-001`, `TIME-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORK; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_APPROVAL_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_APPROVAL_006`; `GOLDEN=TestTodo_APPROVAL_006_Golden`; `BROWSER=TestTodo_APPROVAL_006_Browser`; `MUTATION=TestTodo_APPROVAL_006_Mutation`.
  - **RED:** a client-supplied digest, a stale task version, a changed render, or a material unknown/redaction reaches ordinary approval.
  - **GREEN:** the server records the digest of what it rendered (visible fields, hidden-field manifest, warnings, effects) keyed by task version; a vote references the task version and the server binds the proposal digest and rendered-projection digest it holds; only `SAFE_TO_DECIDE` records a vote; re-rendering advances the task version.
  - **REFACTOR:** no client-held receipt token, session binding or expiry; those are Gate C if a threat model shows the server record is insufficient.
  - **Refs:** [Simplified receipt rule](workflows/_engine/workflow-context-contract.md#7-human-identity-approvals-and-work), [approval evidence](data/models/kernel-governance-and-evidence.md).

- [x] `APPROVAL-007` **[GATE_B][SOL_HIGH] Materialize ApprovalRequirements as governed WorkItems.**
  - **Evidence (2026-09-05):** `TestTodo_APPROVAL_007`, `TestTodo_APPROVAL_007_{Golden,Race,Fault,Mutation}` in `internal/humanwork/workitem` (MaterializeApprovalRequirements creates and routes exactly one governed WorkItem per compiled requirement in stage order through the caller's transaction, ClaimedBy threaded so OneRequirementPerPrincipal holds across the set, VisibilityForAssignment derived from how the requirement resolved; golden `testdata/approval007_golden.json`; the platform/execution call site still hand-builds a single item and is a follow-up); `go test -count=1 ./internal/humanwork/workitem/ ./internal/workflow/steps/approval/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `APPROVAL-001`, `WORK-001`, `WORK-002`, `WF-RUN-025`, `MSG-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORK; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_APPROVAL_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_APPROVAL_007`; `GOLDEN=TestTodo_APPROVAL_007_Golden`; `RACE=TestTodo_APPROVAL_007_Race`; `FAULT=TestTodo_APPROVAL_007_Fault`; `MUTATION=TestTodo_APPROVAL_007_Mutation`.
  - **RED:** resolved requirement and WorkItem diverge, retry creates duplicate task/message, candidate is treated as decision authority, proposal digest is omitted, or partial failure leaves a requirement without accountable work.
  - **GREEN:** one transaction stores requirement revision, resolver/candidate evidence, proposal/control digests and exactly one correlated WorkItem per required decision slot; committed work emits idempotent `APPROVAL_REQUIRED` MessageIntent through outbox.
  - **REFACTOR:** ApprovalTask remains a WorkItem specialization and decision-time authority is re-evaluated separately by `APPROVAL-002`/`APPROVAL-005`.
  - **Refs:** [Human work](specs/human-work-forms-and-rules.md), [approval runtime](specs/workflow-runtime.md), [messaging](specs/messaging-and-notification-plane.md).

- [x] `FORM-005` **[GATE_B][SOL_HIGH] Persist encrypted resumable form drafts without creating effects.**
  - **Evidence (2026-09-06):** `TestTodo_FORM_005` in `internal/forms/drafts`; `go test -count=1 ./internal/forms/drafts/` PASS on windows/arm64 (Go 1.26.3); evidence recorded by the orchestrator after the front-end session ticked the entry without one.
  - **Depends:** `FORM-001`, `FORM-002`, `DATA-014`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DOCUMENTS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_FORM_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_FORM_005`; `FUZZ=FuzzTodo_FORM_005`; `INTEGRATION=TestTodo_FORM_005_Integration`; `BROWSER=TestTodo_FORM_005_Browser`; `MUTATION=TestTodo_FORM_005_Mutation`.
  - **RED:** stale/expired/wrong-principal draft, hidden answer or quarantined attachment submits after interruption/offline resume.
  - **GREEN:** revisioned draft returns `CURRENT`, `REBASE_REQUIRED`, `EXPIRED`, or `DENIED`; only fresh server validation creates immutable submission and draft path creates zero workflow/domain effects.
  - **REFACTOR:** draft retention and deletion follow classification policy.
  - **Refs:** [Workflow context](workflows/_engine/workflow-context-contract.md), [forms](specs/human-work-forms-and-rules.md).

- [ ] `WORK-007` **[GATE_B][SOL_HIGH] Enforce queue fairness, deadline continuity and bulk-item visibility.**
  - **Depends:** `WORK-003`, `WORK-005`, `ADMISSION-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORK; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WORK_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WORK_007`; `RACE=TestTodo_WORK_007_Race`; `SECURITY=TestTodo_WORK_007_Security`; `BENCHMARK=BenchmarkTodo_WORK_007`; `MUTATION=TestTodo_WORK_007_Mutation`.
  - **RED:** reassignment resets legal/SLA deadline, one tenant/priority starves another or bulk success hides a denied/failed subject.
  - **GREEN:** deterministic aging/tie-break and reserved capacity meet starvation bound; deadline is unchanged and every bulk item has independent authorized/evidenced/resumable state.
  - **REFACTOR:** workload priority cannot override legal authority.
  - **Refs:** [Human work](specs/human-work-forms-and-rules.md), [workflow context](workflows/_engine/workflow-context-contract.md).

- [ ] `WORK-008` **[PHASE_2][SOL_HIGH] Enforce evidence compartments on WorkItems and derived messages.**
  - **Depends:** `WORK-001`, `TRUST-009`, `TRUST-010`, `DATA-016`, `MSG-010`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORK; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WORK_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WORK_008`; `RACE=TestTodo_WORK_008_Race`; `INTEGRATION=TestTodo_WORK_008_Integration`; `SECURITY=TestTodo_WORK_008_Security`.
  - **RED:** manager/ordinary assignee sees medical artifact, filename/metadata/thumbnail/OCR-derived fact, restricted task existence or provider content outside purpose; queue count/error/search leaks the compartment.
  - **GREEN:** WorkItem stores compartment/purpose/field/artifact access policy; LeaveAdministrator receives time-bounded authorized evidence access while manager receives only permitted operational absence facts, and every view/download/derived message records a decision receipt.
  - **REFACTOR:** assignment and decision authority remain distinct from artifact-access authority.
  - **Refs:** [Human work](specs/human-work-forms-and-rules.md), [classification and DLP](specs/data-classification-and-dlp.md), [Leave workflow](workflows/leave/leave-return-to-work.md).

- [ ] `WORK-009` **[PHASE_2][SOL_HIGH] Complete a restricted evidence-review task with a typed finding.**
  - **Depends:** `WORK-006`, `WORK-008`, `DOC-INTAKE-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORK; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_WORK_009`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WORK_009`; `PROPERTY=TestTodo_WORK_009_Property`; `RACE=TestTodo_WORK_009_Race`; `SECURITY=TestTodo_WORK_009_Security`; `MUTATION=TestTodo_WORK_009_Mutation`.
  - **RED:** reviewer returns free-form eligibility/medical diagnosis, relies on stale/quarantined artifact, exposes protected detail in ordinary output, or duplicate completion creates divergent findings.
  - **GREEN:** authorized specialist returns exactly `SUFFICIENT|INSUFFICIENT|MORE_INFORMATION_REQUIRED|UNKNOWN`, binding requirement/artifact versions, reviewed scope, safe reason, expiry and evidence receipt; finding becomes workflow input but never legal eligibility by itself.
  - **REFACTOR:** sensitive notes remain compartmented artifacts; workflow sees only the minimum typed result.
  - **Refs:** [Evidence model](data/models/kernel-governance-and-evidence.md), [workflow context](workflows/_engine/workflow-context-contract.md).
- [x] `WORK-010` **[GATE_B][SOL_HIGH] Persist approval decision and task submission content, not only their digests.**
  - **Depends:** `WORK-001`, `WF-STEP-003`, `WF-STEP-004`, `APPROVAL-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORK; DIRECT=none; WHY=who approved, with what stated reason and under which authority, and what a task submitted, must be recoverable from storage after the run`.
  - **TEST:** `TestTodo_WORK_010`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WORK_010`; `GOLDEN=TestTodo_WORK_010_Golden`; `SECURITY=TestTodo_WORK_010_Security`; `MUTATION=TestTodo_WORK_010_Mutation`.
  - **RED:** a completed approval or task work item stores only a digest and a principal id, so reason, authority decision ref, rendered-projection digest, control snapshots or the typed submission fields are unrecoverable; or stored content can be rewritten after completion.
  - **GREEN:** an append-only `work_item_decision` record stores the full typed decision or submission (digest-verified against the work item's completed output digest) in the same transaction as completion, readable through the inspector and admin API under the work item's redaction rules.
  - **REFACTOR:** the record is the artifact the inspector's output ref points at; digests stay on the work item.
  - **Refs:** [human work](specs/human-work-forms-and-rules.md), [review findings](devlog/2026-09-03-executable-prototype.md#9-review-findings-and-the-hardening-plan).
  - **Evidence (2026-09-03):** `TestTodo_WORK_010`, `TestTodo_WORK_010_Golden`, `TestTodo_WORK_010_Security`, `TestTodo_WORK_010_Mutation` in `internal/humanwork/workitem` (migration `00022_work_item_decision.sql`: append-once `work_item_decision` keyed by (tenant, work item) with the forbid_mutation trigger, registered in `definitions/storage/storage-disposition.yaml`; `RecordDecision` refuses a body whose digest differs from the item's completed output digest, a non-COMPLETED item or a kind mismatch; `steps/approval.Complete` and `steps/task.Submit` record the full typed decision or submission in the completion transaction); `go test -count=1 ./internal/humanwork/... ./internal/workflow/steps/...` PASS via embedded-postgres on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `FORM-006` **[GATE_B][SOL_HIGH] Preserve authority and deadlines through alternate human channels.**
  - **Evidence (2026-09-06):** `TestTodo_FORM_006` in `internal/humanwork/formcontinuity`; `go test -count=1 ./internal/humanwork/formcontinuity/` PASS on windows/arm64 (Go 1.26.3); evidence recorded by the orchestrator after the front-end session ticked the entry without one.
  - **Depends:** `FORM-004`, `FORM-005`, `MSG-008`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DOCUMENTS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_FORM_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_FORM_006`; `FUZZ=FuzzTodo_FORM_006`; `INTEGRATION=TestTodo_FORM_006_Integration`; `FAULT=TestTodo_FORM_006_Fault`; `SECURITY=TestTodo_FORM_006_Security`; `BROWSER=TestTodo_FORM_006_Browser`; `MUTATION=TestTodo_FORM_006_Mutation`.
  - **RED:** accessibility/RTL/channel outage or phone/in-person/postal transcription changes deadline, validation, privacy, authority or audit result.
  - **GREEN:** governed alternate route records accommodation, transcription/read-back and equivalent validation/authority/evidence while retaining the original deadline.
  - **REFACTOR:** assisted route never grants the assistant decision authority.
  - **Refs:** [Accessible experience](specs/experience-ui-and-branding.md), [workflow context](workflows/_engine/workflow-context-contract.md).

## 25. Search, metrics and outcome intelligence

> **Disposition (2026-09-02):** Phase 2. Nothing here blocks P1A or P1B.

- [x] `SEARCH-001` **[PHASE_2][SOL_HIGH] Enforce authorization before exact, fuzzy or vector ranking.**
  - **Evidence (2026-09-05):** `TestTodo_SEARCH_001` in `internal/engines/search` (prefilter binds tenant, record, relationship, field, purpose, classification, effective/known time and policy digest; each hit has source digest and suppression evidence; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/engines/search/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-009`, `TRUST-010`, `TRUST-012`, `PRIV-003`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose governed intent creation, inspection or consumption without persistence or provider bypass`.
  - **TEST:** `TestTodo_SEARCH_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SEARCH_001`; `GOLDEN=TestTodo_SEARCH_001_Golden`; `SECURITY=TestTodo_SEARCH_001_Security`; `MUTATION=TestTodo_SEARCH_001_Mutation`.
  - **RED:** unauthorized tenant/field/case content leaks through hits/snippets/counts/facets/embeddings/no-result explanation or policy changes after planning.
  - **GREEN:** prefilter binds tenant, record, relationship, field, purpose, classification, effective/known time and policy digest; each hit has source digest and suppression evidence.
  - **REFACTOR:** ranking never receives an unauthorized candidate.
  - **Refs:** [Intelligence model](data/models/assurance-intelligence-platform.md), [AuthZ](specs/organization-scope-and-authz.md).

- [ ] `SEARCH-002` **[PHASE_2][SOL_HIGH] Propagate restriction, deletion and hold state through search/vector/cache.**
  - **Depends:** `SEARCH-001`, `RECORDS-COPY-001`, `PRIV-004`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose governed intent creation, inspection or consumption without persistence or provider bypass`.
  - **TEST:** `TestTodo_SEARCH_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SEARCH_002`; `RACE=TestTodo_SEARCH_002_Race`; `SECURITY=TestTodo_SEARCH_002_Security`; `RECOVERY=TestTodo_SEARCH_002_Recovery`.
  - **RED:** stale/restricted/deleted/quarantined source remains retrievable or rebuild reintroduces it.
  - **GREEN:** index records watermark/classification/ACL epoch/retention/hold/model version; tombstones apply idempotently and stale index returns `UNAVAILABLE_STALE`.
  - **REFACTOR:** indexes remain rebuildable copies, never authority.
  - **Refs:** [Copy lifecycle](specs/records-management-and-disposition.md), [intelligence model](data/models/assurance-intelligence-platform.md).

- [ ] `METRIC-001` **[PHASE_2][SOL_HIGH] Compile and execute versioned semantic metrics.**
  - **Depends:** `MODEL-007`, `DATA-005`, `DATA-006`, `OPS-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ANALYTICS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_METRIC_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_METRIC_001`; `PROPERTY=TestTodo_METRIC_001_Property`; `GOLDEN=TestTodo_METRIC_001_Golden`.
  - **RED:** denominator/population/time/null/zero/rounding policy is implicit or missing data becomes zero.
  - **GREEN:** typed formula returns numerator, denominator, sample size, source watermarks, quality state, population/calculation digests and `UNKNOWN/PARTIAL` where applicable.
  - **REFACTOR:** metric definition is immutable and effective-dated.
  - **Refs:** [Metric model](data/models/assurance-intelligence-platform.md), [temporal data](data/models/wire-contract-primitives.md).

- [ ] `METRIC-002` **[PHASE_2][SOL_HIGH] Append metric corrections and invalidate dependent outputs.**
  - **Depends:** `METRIC-001`, `DATA-003`, `DATA-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ANALYTICS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_METRIC_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_METRIC_002`; `GOLDEN=TestTodo_METRIC_002_Golden`; `MUTATION=TestTodo_METRIC_002_Mutation`.
  - **RED:** correction overwrites original or dashboard/decision remains fresh with invalid input.
  - **GREEN:** correction preserves old/new values, authority/reason/window/population and marks all dependent reports/dashboards/decision links stale with supersession chain.
  - **REFACTOR:** replay is idempotent.
  - **Refs:** [Metric correction](data/models/assurance-intelligence-platform.md), [provenance](specs/provenance-graph-and-lineage.md).

- [x] `DISCLOSURE-001` **[PHASE_2][SOL_HIGH] Enforce analytics disclosure control and repeated-query budgets.**
  - **Evidence (2026-09-07):** `TestTodo_DISCLOSURE_001`, `TestTodo_DISCLOSURE_001_Golden`, `TestTodo_DISCLOSURE_001_Property`, `TestTodo_DISCLOSURE_001_Security` in `internal/governance/privacy` (written in 171cafc on 2026-09-03 and never ticked; the named tests prove the GREEN contract: minimum-cell, complementary suppression, rounding/noise, cumulative budget and review policy produce suppression evidence bound to query digest); `go test -count=1 ./internal/governance/privacy/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PRIV-002`, `TRUST-010`, `OPS-002`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_DISCLOSURE_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DISCLOSURE_001`; `PROPERTY=TestTodo_DISCLOSURE_001_Property`; `GOLDEN=TestTodo_DISCLOSURE_001_Golden`; `SECURITY=TestTodo_DISCLOSURE_001_Security`.
  - **RED:** small-cell/complementary/repeated-differencing/sensitive-dimension/cross-tenant query returns identifying output.
  - **GREEN:** minimum-cell, complementary suppression, rounding/noise, cumulative budget and review policy produce suppression evidence bound to query digest.
  - **REFACTOR:** privacy control happens before export/agent narration.
  - **Refs:** [Disclosure model](data/models/assurance-intelligence-platform.md), [privacy](specs/data-classification-and-dlp.md).

- [ ] `OUTCOME-001` **[PHASE_3][SOL_HIGH] Link decisions to outcomes without causal overclaim.**
  - **Depends:** `METRIC-001`, `MODEL-020`, `CONF-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ANALYTICS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_OUTCOME_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_OUTCOME_001`; `GOLDEN=TestTodo_OUTCOME_001_Golden`; `MUTATION=TestTodo_OUTCOME_001_Mutation`.
  - **RED:** missing window/population/source authority or descriptive evidence labelled causal validates.
  - **GREEN:** link records outcome definition/window, attribution class, confounders/censoring, watermarks, confidence/limitations; correction supersedes analysis.
  - **REFACTOR:** outcome observation cannot rewrite the original decision.
  - **Refs:** [Outcome model](data/models/assurance-intelligence-platform.md), [decision evidence](data/models/kernel-governance-and-evidence.md).

- [ ] `PROCESS-001` **[PHASE_3][SOL_HIGH] Build a privacy-scoped process-mining event log.**
  - **Depends:** `DATA-003`, `PRIV-001`, `DISCLOSURE-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ANALYTICS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PROCESS_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PROCESS_001`; `GOLDEN=TestTodo_PROCESS_001_Golden`; `RACE=TestTodo_PROCESS_001_Race`; `SECURITY=TestTodo_PROCESS_001_Security`; `MUTATION=TestTodo_PROCESS_001_Mutation`.
  - **RED:** log mixes tenant/workflow, exposes payload, loses causation/effective time or treats missing telemetry as completed activity.
  - **GREEN:** projection emits canonical case/activity/lifecycle/timestamp/resource with scope/redaction/watermark and completeness state.
  - **REFACTOR:** projection is read-only and rebuildable.
  - **Refs:** [Process mining](data/models/assurance-intelligence-platform.md), [ledger](specs/transaction-ledger-reconciliation-and-repair.md).

- [ ] `REPRO-001` **[PHASE_2][SOL_HIGH] Package reproducible search and analytics evidence.**
  - **Depends:** `SEARCH-001`, `METRIC-001`, `TOOL-016`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_REPRO_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_REPRO_001`; `GOLDEN=TestTodo_REPRO_001_Golden`; `SECURITY=TestTodo_REPRO_001_Security`; `RECOVERY=TestTodo_REPRO_001_Recovery`.
  - **RED:** result cannot be reconstructed from query/definition/population/source/policy/model/timezone versions.
  - **GREEN:** isolated replay from bundled digests matches output hash or reports exact changed/missing input; bundle remains tenant/classification scoped.
  - **REFACTOR:** large inputs are content-addressed references.
  - **Refs:** [Reproducibility](data/models/assurance-intelligence-platform.md), [canonical digest](specs/canonical-envelope-and-digest.md).

## 26. PostgreSQL database and model materialization

> **Disposition (2026-09-02):** P1A for the M2 migration root: tenant, intent, proposal, ledger event, stream head, projection and outbox tables with checksums, one Goose tree, pgx. Storage dispositions for entities outside the fourteen definitions are DEFERRED.

The files in [`planning/data/models`](data/models/README.md) are the semantic
source, not a suggestion to create one table per named object. SchemaFlux must
compile each registered entity into one explicit `StorageDisposition`:

```text
RELATIONAL_AGGREGATE   normalized authoritative tables
IMMUTABLE_FACT        append-only fact/revision tables
LEDGER_EVENT          canonical event envelope plus typed payload schema
CONTROL_ARTIFACT      immutable/versioned configuration tables
EVIDENCE_ARTIFACT     metadata row plus content-addressed object reference
RUNTIME_STATE         durable operational tables with CAS/lease fencing
PROJECTION            reconstructable read model
VALUE_OBJECT          embedded typed columns; no independent identity
EXTERNAL_ONLY         observation/reference only; never silently persisted as truth
```

- [x] `DB-001` **[P0][TERRA] Bootstrap the Go PostgreSQL migration and test harness.**
  - **Evidence (2026-09-03):** `TestTodo_DB_001` in `internal/data/pgtest`; `go test -count=1 ./internal/data/pgtest/...` PASS on windows/arm64 (Go 1.26.3); `go vet`, `gofmt -l`, `staticcheck` clean; branch plan-revision-2026-09-02.
  - **Depends:** `TOOL-001`, `TOOL-002`, `TOOL-014`, `LIB-004`, `LIB-008`, `LIB-009`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DB_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DB_001`; `GOLDEN=TestTodo_DB_001_Golden`; `RACE=TestTodo_DB_001_Race`; `INTEGRATION=TestTodo_DB_001_Integration`.
  - **RED:** clean database cannot migrate from zero, rollback the latest reversible migration, or run isolated per-test transactions without Node/TypeScript tooling.
  - **GREEN:** Go command uses the qualified Testcontainers harness to create digest-pinned PostgreSQL, Goose to apply SQL-first migrations and pgx for the database path; it reports schema version/digest and `go test` proves clean-up and parallel-test isolation.
  - **REFACTOR:** migration runner, application and tests share one owned database configuration path while Goose/pgx/Testcontainers types remain inside their approved adapters.
  - **Refs:** [Go technology constitution](specs/go-only-technology-constitution.md), [physical data architecture](specs/platform-architecture-catalog.md).

- [x] `DB-002` **[P0][SOL_HIGH] Generate the model-to-storage disposition manifest.**
  - **Depends:** `MODEL-011`, `MODEL-012`, `TOOL-004`, `DATA-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DB_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DB_002`; `GOLDEN=TestTodo_DB_002_Golden`; `RACE=TestTodo_DB_002_Race`; `INTEGRATION=TestTodo_DB_002_Integration`; `SECURITY=TestTodo_DB_002_Security`; `MUTATION=TestTodo_DB_002_Mutation`.
  - **RED:** any `EntityDefinition`, `PropertyDefinition`, `RelationshipDefinition`, state, transition or authority assignment from `planning/data/models` lacks exactly one physical disposition, owner and schema target.
  - **GREEN:** generated manifest covers every registered model ID, resolves all cross-model references and emits stable counts/digest plus table/event/artifact/projection/value-object classification.
  - **REFACTOR:** no handwritten second entity inventory; prose-only unregistered names are reported separately as coverage failures.
  - **Refs:** [Registry contracts](data/models/registry-and-coverage-contracts.md), [model catalog](data/models/README.md).
  - **Evidence (2026-09-03):** `TestTodo_DB_002`, `_Golden`, `_Race`, `_Integration`, `_Security`, `_Mutation` (model-to-storage disposition manifest `definitions/model/storage-disposition.yaml`; hints verified against a scan of migrations/\*.sql, unverified hints reported as MISMATCH — most business entities have no physical stream kind or table yet) in `tools/gen/storagemanifest`; `go test -count=1 ./tools/gen/storagemanifest/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `DB-003` **[P0][SOL_HIGH] Generate property-level SQL mappings from the model registry.**
  - **Depends:** `DB-002`, `MODEL-013`, `MODEL-014`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DB_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DB_003`; `PROPERTY=TestTodo_DB_003_Property`; `RACE=TestTodo_DB_003_Race`; `INTEGRATION=TestTodo_DB_003_Integration`; `FAULT=TestTodo_DB_003_Fault`; `SECURITY=TestTodo_DB_003_Security`.
  - **RED:** property lacks SQL type, presence/null/default behavior, precision/unit/currency, temporal columns, classification, encryption/search policy or constraint mapping.
  - **GREEN:** every persisted property resolves to typed column/JSON boundary plus constraint/index/encryption policy; unsupported mappings fail with model file/path/property diagnostics.
  - **REFACTOR:** generated Go/Protobuf/SQL mappings share canonical primitive definitions.
  - **Refs:** [Wire primitives](data/models/wire-contract-primitives.md), [modeling conventions](data/models/modeling-conventions.md).
  - **Evidence (2026-09-03):** `TestTodo_DB_003`, `_Property`, `_Race`, `_Integration`, `_Fault`, `_Security` (`definitions/model/property-sql-mappings.yaml`, Go type to SQL column mapping) in `tools/gen/storagemanifest`; `go test -count=1 ./tools/gen/storagemanifest/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `DB-004` **[P0][SOL_HIGH] Generate relationship and lifecycle database constraints.**
  - **Depends:** `DB-002`, `DB-003`, `MODEL-015`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DB_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DB_004`; `PROPERTY=TestTodo_DB_004_Property`; `RACE=TestTodo_DB_004_Race`; `INTEGRATION=TestTodo_DB_004_Integration`; `SECURITY=TestTodo_DB_004_Security`; `MUTATION=TestTodo_DB_004_Mutation`.
  - **RED:** unknown target, cross-tenant edge, illegal cardinality, temporal overlap, invalid initial/terminal state or forbidden transition can be inserted directly through SQL.
  - **GREEN:** FK/check/exclusion/unique constraints or mandatory transactional validators reject each negative fixture with a stable SQL/application error mapping.
  - **REFACTOR:** constraints that cannot live in SQL are named invariants with transactional enforcement and continuous verification.
  - **Refs:** [Relationship map](data/models/relationship-map.md), [registry contracts](data/models/registry-and-coverage-contracts.md).
  - **Evidence (2026-09-03):** `TestTodo_DB_004`, `_Property`, `_Race`, `_Integration`, `_Security`, `_Mutation` (`definitions/model/relationship-lifecycle-constraints.yaml`; generated DDL compared with the live IntentInstance five-dimension CHECK; integration tests insert/reject rows in embedded Postgres) in `tools/gen/storagemanifest`; `go test -count=1 ./tools/gen/storagemanifest/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `DB-005` **[GATE_A][SOL_HIGH] Create shared tenant, organization, cell and temporal database primitives.**
  - **Evidence (2026-09-03):** `TestTodo_DB_005` in `internal/data/schema`; `go test -count=1 ./internal/data/schema/...` PASS on windows/arm64 (Go 1.26.3); `go vet`, `gofmt -l`, `staticcheck` clean; branch plan-revision-2026-09-02.
  - **Depends:** `DB-001`, `DB-003`, `TENANT-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DB_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DB_005`; `PROPERTY=TestTodo_DB_005_Property`; `GOLDEN=TestTodo_DB_005_Golden`; `FUZZ=FuzzTodo_DB_005`; `RACE=TestTodo_DB_005_Race`; `INTEGRATION=TestTodo_DB_005_Integration`; `FAULT=TestTodo_DB_005_Fault`; `SECURITY=TestTodo_DB_005_Security`; `MUTATION=TestTodo_DB_005_Mutation`.
  - **RED:** authoritative row can omit tenant, use mismatched organization/cell epoch, store an invalid half-open interval or depend on database session default tenant.
  - **GREEN:** shared domains/columns/checks encode tenant/org/cell, recorded/effective/known intervals, canonical IDs and version/CAS; fixtures reject every malformed boundary.
  - **REFACTOR:** business time is explicit and never inferred from `created_at`.
  - **Refs:** [Wire temporal primitives](data/models/wire-contract-primitives.md), [tenant placement](data/models/operations-production.md).

- [x] `DB-006` **[GATE_A][SOL_HIGH] Create schema-release and migration-journal tables.**
  - **Evidence (2026-09-03):** `TestTodo_DB_006` in `internal/data/schema`; `go test -count=1 ./internal/data/schema/...` PASS on windows/arm64 (Go 1.26.3); `go vet`, `gofmt -l`, `staticcheck` clean; branch plan-revision-2026-09-02.
  - **Depends:** `DB-001`, `MODEL-017`, `TIME-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DB_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DB_006`; `GOLDEN=TestTodo_DB_006_Golden`; `RACE=TestTodo_DB_006_Race`; `INTEGRATION=TestTodo_DB_006_Integration`; `CONFORMANCE=TestTodo_DB_006_Conformance`.
  - **RED:** migration applies without artifact/source digest, tool version, compatibility class, checksum, owner, start/end status or trusted-time evidence.
  - **GREEN:** journal records planned/running/applied/failed/rolled-forward state and exact schema release; duplicate application is idempotent and checksum mismatch blocks startup.
  - **REFACTOR:** irreversible migrations declare restore/repair boundary before activation.
  - **Refs:** [Schema registry](data/models/dataops-configuration.md), [upgrade governance](specs/platform-foundation-gap-closure.md).

- [x] `DB-007` **[GATE_A][SOL_HIGH] Materialize canonical registry and control-artifact tables.**
  - **Evidence (2026-09-03):** `TestTodo_DB_007` in `internal/data/schema`; `go test -count=1 ./internal/data/schema/...` PASS on windows/arm64 (Go 1.26.3); `go vet`, `gofmt -l`, `staticcheck` clean; branch plan-revision-2026-09-02.
  - **Depends:** `DB-002`, `DB-003`, `DB-006`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DB_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DB_007`; `PROPERTY=TestTodo_DB_007_Property`; `GOLDEN=TestTodo_DB_007_Golden`; `RACE=TestTodo_DB_007_Race`; `INTEGRATION=TestTodo_DB_007_Integration`; `MUTATION=TestTodo_DB_007_Mutation`.
  - **RED:** entity/property/relationship/intent/schema/capability/workflow/rule/config definition is mutable in place or loses source/version/supersession lineage.
  - **GREEN:** generated migrations create immutable version rows, active-pointer/effective-interval tables and dependency edges; publication and rollback append history.
  - **REFACTOR:** current-version views are conveniences, not authoritative history.
  - **Refs:** [DataOps/config models](data/models/dataops-configuration.md), [registry contracts](data/models/registry-and-coverage-contracts.md).

- [x] `DB-008` **[GATE_A][SOL_HIGH] Materialize Person, identity, Worker, Employment and Assignment aggregates.**
  - **Depends:** `DB-004`, `DB-005`, `DB-007`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DB_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DB_008`; `PROPERTY=TestTodo_DB_008_Property`; `RACE=TestTodo_DB_008_Race`; `INTEGRATION=TestTodo_DB_008_Integration`; `SECURITY=TestTodo_DB_008_Security`; `MUTATION=TestTodo_DB_008_Mutation`.
  - **RED:** Candidate/Worker becomes exclusive person state, two active legal assertions overlap illegally, assignment loses employment link, merge destroys identity lineage or cross-tenant relationship inserts.
  - **GREEN:** migrations/repositories implement roots, immutable revisions, roles, identity claims, addresses/contact points, employments, assignments and relationships defined in `people-workforce.md` with CAS and temporal constraints.
  - **REFACTOR:** large/sensitive evidence remains an artifact reference.
  - **Refs:** [People/workforce models](data/models/people-workforce.md), [initial aggregates](data/models/registry-and-coverage-contracts.md).
  - **Evidence (2026-09-03):** `TestTodo_DB_008`, `_Security`, `_Mutation` in `internal/data/aggregates` (migration 00011: person, identity_claim, worker, employment, assignment as bitemporal fact logs with `PRIMARY KEY (tenant_id, row_id)`, the shared `aggregate_entity` identity spine so every entity and cross-entity reference carries a tenant-qualified foreign key, `aggregate_forbid_inplace_update` trigger, RLS per migration 00008); `go test -count=1 ./internal/data/aggregates/...` PASS via embedded-postgres on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `DB-009` **[GATE_A][SOL_HIGH] Materialize organization, legal-entity, job, position and headcount aggregates.**
  - **Depends:** `DB-004`, `DB-005`, `DB-007`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DB_009`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DB_009`; `PROPERTY=TestTodo_DB_009_Property`; `RACE=TestTodo_DB_009_Race`; `INTEGRATION=TestTodo_DB_009_Integration`; `SECURITY=TestTodo_DB_009_Security`; `BENCHMARK=BenchmarkTodo_DB_009`; `MUTATION=TestTodo_DB_009_Mutation`.
  - **RED:** manager/org cycle, duplicate parent, closed-node assignment, overlapping occupancy/capacity or reservation beyond FTE commits.
  - **GREEN:** tables/repositories preserve effective-dated org edges, legal entities, jobs, positions, capacity, occupancy, requisitions and typed reservations with named invariant tests.
  - **REFACTOR:** graph closure/path projections are rebuildable and checked against authoritative edges.
  - **Refs:** [People/workforce models](data/models/people-workforce.md), [position domain](specs/position-and-headcount-domain.md).
  - **Evidence (2026-09-03):** `TestTodo_DB_009`, `_Security` (org-cycle and FTE over-commit refused) in `internal/data/aggregates` (migration 00012: legal_entity, organization_unit, job, job_position, position_occupancy); PASS; branch plan-revision-2026-09-02; `go test -count=1 ./internal/data/aggregates/...` on windows/arm64 (Go 1.26.3).

- [x] `DB-010` **[GATE_A][SOL_HIGH] Materialize compensation and workforce-budget data needed by Promotion.**
  - **Depends:** `DB-004`, `DB-005`, `DB-007`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DB_010`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DB_010`; `PROPERTY=TestTodo_DB_010_Property`; `RACE=TestTodo_DB_010_Race`; `INTEGRATION=TestTodo_DB_010_Integration`; `MUTATION=TestTodo_DB_010_Mutation`.
  - **RED:** floating-point money, currency mismatch, overlapping active package/component, invalid band range or over-reserved budget commits.
  - **GREEN:** exact decimal money, packages/components, grade/band/range, proposal, budget authority and reservation tables implement model precision, effective dating and CAS constraints.
  - **REFACTOR:** payroll results remain external/conformance-only in Phase 1.
  - **Refs:** [Rewards models](data/models/rewards-payroll-workforce.md), [compensation domain](specs/compensation-domain.md).
  - **Evidence (2026-09-03):** `TestTodo_DB_010`, `_Security` (currency mismatch, invalid band range, budget over-commit refused; exact `values.Money`/`values.Decimal`) in `internal/data/aggregates` (migration 00013: compensation_package, compensation_component, compensation_band, workforce_budget, budget_reservation); `LoadFixtures` materialises the fixture corpus across all 15 tables; PASS; branch plan-revision-2026-09-02; `go test -count=1 ./internal/data/aggregates/...` on windows/arm64 (Go 1.26.3).

- [x] `DB-011` **[GATE_A][SOL_HIGH] Materialize BusinessIntent, proposal, decision and transaction control aggregates.**
  - **Evidence (2026-09-05):** `TestTodo_DB_011`, `TestTodo_DB_011_{Golden,Race,Integration,Mutation}` in `internal/data/intentcontrol` (migration `00024_intent_control.sql`: 21 tables, 17 append-only LEDGER rows under forbid_mutation for intent context, input snapshots, simulation results, ordinal-pinned proposal writes/effects/approvals/obligations, relationships, results, decisions, certificates, transaction plans/effects, commit and abort receipts, corrections and closure, plus 4 live CONTROL rows (change request, plan binding, ambiguity, repair plan) fenced by compare-and-swap versions; RLS on all 21; found and fixed a real commit/abort receipt race with a transaction-scoped advisory lock per plan, proven by a 6-worker race yielding exactly one receipt); `go test -count=1 ./migrations/ ./internal/data/intentcontrol/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DB-004`, `DB-005`, `DB-007`, `INTENT-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DB_011`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DB_011`; `GOLDEN=TestTodo_DB_011_Golden`; `RACE=TestTodo_DB_011_Race`; `INTEGRATION=TestTodo_DB_011_Integration`; `MUTATION=TestTodo_DB_011_Mutation`.
  - **RED:** proposal/snapshot/simulation mutates after publication, trusted origin/feature/family is caller-spoofed, intent relationship cycles or changes parentage, result overwrites revision, correction lacks target, or transaction/repair/closure loses intent/proposal/control digest.
  - **GREEN:** tables/repositories implement `IntentInstance` with FeatureIntentCoverage/origin/causation/correlation/family/mode, `HCMChangeRequest`, immutable input snapshots and simulation results, `ProposalRevision` plus write/effect/approval/obligation sets, typed immutable intent relationships/results, decisions, certificates, transaction plan/receipt/abort/ambiguity/correction, RepairPlan and closure/execution-receipt references exactly as modeled.
  - **REFACTOR:** payloads use typed schemas and governed artifact refs rather than unrestricted JSON.
  - **Refs:** [Kernel models](data/models/kernel-governance-and-evidence.md), [BusinessIntent contract](specs/business-intent-and-change-request.md).

- [x] `DB-012` **[GATE_B][SOL_HIGH] Materialize durable workflow-runtime and human-work state.**
  - **Evidence (2026-09-05):** `TestScheduling_{FrontierEntriesAreASetWithADeterministicOrder,NodeOutputIsTypedAndWrittenOnce,ReadyWorkIsOneUnitPerAttempt,VariablesAreCompareAndSwapFenced,TimersArePromisesSettledOnce,SignalDeliveryIsDedupedAndAppliedExactlyOnce,LeasesAreExclusiveAndFenced,CheckpointsAndChildLinksAreImmutableFacts}`, `TestWorkQueue_{AnItemWaitsInExactlyOneQueue,ClaimHistoryHoldsAtMostOneOpenHold,SLABreachStatesAdvanceOnlyForward,OneApprovalSlotProducesOneWorkItem}` in `internal/data/runtimestate` (migration `00026_workflow_scheduling_state.sql` completes the runtime state the earlier partial left gated: frontier entries, typed node outputs, ready work, CAS-fenced variables, timers as settle-once promises, deduped signals with receipts, fenced exclusive leases, immutable checkpoints and child links, one queue per work item, one open claim hold, forward-only SLA breach states with one-way evidence CHECKs, declared approval slots; earlier 00016–00019/00022 tables unchanged; no sweeper or scheduler ships, per WF-RUN-000; found and fixed two biconditional SLA CHECKs that contradicted SLAStore.Advance); `go test -count=1 ./migrations/ ./internal/data/runtimestate/ ./internal/workflow/... ./test/workflow/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DB-004`, `DB-005`, `DB-007`, `MODEL-014`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DB_012`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DB_012`; `RACE=TestTodo_DB_012_Race`; `INTEGRATION=TestTodo_DB_012_Integration`; `MUTATION=TestTodo_DB_012_Mutation`.
  - **RED:** duplicate node attempt/timer/signal/claim/approval slot, stale lease, invalid state transition, mutable completed work result, or node success without its complete frontier/successor/scheduling records commits.
  - **GREEN:** workflow instances, frontiers, node executions/attempts, typed output refs, ready work, variables, timers, signals/receipts/subscriptions, leases, checkpoints, child links, queues/items/claims/SLA and approval requirement/decision tables support CAS, dedupe, atomic advancement and restart tests.
  - **REFACTOR:** runtime tables answer current execution; ledger explains business history.
  - **Refs:** [Kernel workflow models](data/models/kernel-governance-and-evidence.md), [workflow runtime](specs/workflow-runtime.md).

- [x] `DB-013` **[GATE_A][SOL_HIGH] Materialize governance, AuthZ, legal and evidence control data.**
  - **Depends:** `DB-004`, `DB-005`, `DB-007`, `MODEL-015`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DB_013`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DB_013`; `PROPERTY=TestTodo_DB_013_Property`; `GOLDEN=TestTodo_DB_013_Golden`; `RACE=TestTodo_DB_013_Race`; `INTEGRATION=TestTodo_DB_013_Integration`; `SECURITY=TestTodo_DB_013_Security`; `MUTATION=TestTodo_DB_013_Mutation`.
  - **RED:** unversioned authority/policy/rule decision, cross-tenant delegation, expired evidence or missing context can authorize a capability.
  - **GREEN:** principal/authority/session/delegation, authorization decisions, access manifests, jurisdiction/rule/evaluation/obligation/evidence and governance-bundle tables preserve exact input/output digests and validity intervals.
  - **REFACTOR:** secrets and raw evidence bytes remain outside ordinary relational rows.
  - **Refs:** [Kernel governance models](data/models/kernel-governance-and-evidence.md), [security models](data/models/security-trust.md).
  - **Evidence (2026-09-03):** `TestTodo_DB_013`, `_Golden`, `_Integration`, `_Mutation`, `_Property`, `_Race`, `_Security` in `internal/data/governance` (16 tables from 00021); `go test -count=1 ./internal/data/governance/...` PASS via embedded-postgres on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `DB-014` **[GATE_B][SOL_HIGH] Materialize integration, messaging, document and artifact metadata.**
  - **Evidence (2026-09-05):** `TestTodo_DB_014`, `TestTodo_DB_014_{Golden,Property,Race,Integration,Security,Mutation}` in `internal/data/integrationmeta`; `TestTodo_DB_014_Messaging` in `internal/data/messagingmeta`; `TestTodo_DB_014_Documents` in `internal/data/documentmeta` (migration `00031_integration_messaging_document_metadata.sql`: 25 tables across external systems, connector definitions/connections/operations/attempts/observations, mapping profiles/executions, integration receipts, reconciliation, message intents, endpoints as address digests, recipient messages, delivery attempts/receipts, threads, document templates/documents/versions, signature requests/signatures, artifact references; RLS on all; schema CHECKs refuse raw secrets in connection configuration, provider acceptance as completion or satisfaction without a verified observation or receipt, and a signature not bound to the exact rendered artifact digest; append-only rows under forbid_mutation); `go test -count=1 ./migrations/ ./internal/data/integrationmeta/ ./internal/data/messagingmeta/ ./internal/data/documentmeta/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DB-004`, `DB-005`, `DB-007`, `MODEL-029`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DB_014`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DB_014`; `PROPERTY=TestTodo_DB_014_Property`; `GOLDEN=TestTodo_DB_014_Golden`; `RACE=TestTodo_DB_014_Race`; `INTEGRATION=TestTodo_DB_014_Integration`; `SECURITY=TestTodo_DB_014_Security`; `MUTATION=TestTodo_DB_014_Mutation`.
  - **RED:** connector/message/document/provider/reconciliation record stores raw secret, loses tenant/correlation/idempotency/version/classification/freshness/deadline or treats provider acceptance as business completion.
  - **GREEN:** connections, mappings, effect graph/operations/attempts/observations, reconciliation jobs/results, message intents/attempts/receipts/threads, documents/templates/signatures and artifact-reference tables match connectivity and completion-policy lifecycles.
  - **REFACTOR:** content-addressed bytes remain in the governed object store.
  - **Refs:** [Connectivity models](data/models/connectivity-access-content.md), [integration platform](specs/integration-platform.md).

- [x] `DB-015` **[GATE_B][SOL_HIGH] Materialize privacy, records, operations and assurance metadata.**
  - **Evidence (2026-09-05):** `TestTodo_DB_015`, `TestTodo_DB_015_{Race,Integration,Fault,Security,Recovery,Mutation}` in `internal/data/recordsmeta`; `TestTodo_DB_015_Privacy` in `internal/data/privacymeta`; `TestTodo_DB_015_Operations` in `internal/data/opsmeta`; `TestTodo_DB_015_Assurance` in `internal/data/assurancemeta` (migration `00032_privacy_records_operations_assurance.sql`: 14 tables for processing purposes, data-copy inventory and copies, record declarations, legal holds and intersections, retention dispositions, incidents, backup and recovery runs, SLO definitions/observations, control evidence and audit packages; RLS on all; a hold blocks disposition execution outright with the BLOCKED marker naming the hold, corrections link with self-correction refused, a completed backup is immutable, a recovery names its backup and reports actuals, a complete inventory has no unknowns, a failed control names its deficiency, a complete audit package carries evidence); `go test -count=1 ./migrations/ ./internal/data/recordsmeta/ ./internal/data/privacymeta/ ./internal/data/opsmeta/ ./internal/data/assurancemeta/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DB-004`, `DB-005`, `DB-007`, `MODEL-026`, `MODEL-027`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DB_015`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DB_015`; `RACE=TestTodo_DB_015_Race`; `INTEGRATION=TestTodo_DB_015_Integration`; `FAULT=TestTodo_DB_015_Fault`; `SECURITY=TestTodo_DB_015_Security`; `RECOVERY=TestTodo_DB_015_Recovery`; `MUTATION=TestTodo_DB_015_Mutation`.
  - **RED:** copy/hold/disposition/incident/recovery/control evidence loses tenant, scope, version, retention or correction lineage.
  - **GREEN:** declarations, copy inventory, holds, dispositions, incidents, SLO/control evidence, backup/recovery runs and audit-package metadata implement their modeled lifecycles and constraints.
  - **REFACTOR:** operational telemetry remains separate from business/assurance evidence.
  - **Refs:** [Assurance models](data/models/assurance-intelligence-platform.md), [operations models](data/models/operations-production.md).

- [x] `DB-016` **[CONFORMANCE][SOL_HIGH] Generate deferred-domain schemas without activating authority.**
  - **Evidence (2026-09-05):** `TestTodo_DB_016` in `tools/gen/deferredschema` (migration previews for the ten deferred domains under testdata/preview, never under migrations/, with a DRAFT/CONFORMANCE disposition preview in the registry row shape, a validator requiring RLS on every table and forbid_mutation on every append-only table, a check that no preview table exists in migrations or as an authoritative write capability, pgtest-applied previews rolled back, deterministic parallel validation and a pinned preview-set digest; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/gen/deferredschema/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DB-002`, `DB-003`, `INTENT-CONF-001`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_DB_016`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DB_016`; `GOLDEN=TestTodo_DB_016_Golden`; `RACE=TestTodo_DB_016_Race`; `INTEGRATION=TestTodo_DB_016_Integration`; `CONFORMANCE=TestTodo_DB_016_Conformance`; `MUTATION=TestTodo_DB_016_Mutation`.
  - **RED:** payroll/benefits/time/leave/recruiting/talent/learning/case/access/regulatory model is missing from storage manifest or accidentally exposed as production write capability.
  - **GREEN:** every future-domain entity has validated DRAFT/CONFORMANCE storage disposition and migration preview, while Phase 1 capability registry exposes zero unapproved authoritative writes.
  - **REFACTOR:** deferred migrations may stay unshipped until their authority gate.
  - **Refs:** [Intent coverage](data/models/intent-coverage-matrix.md), [all model documents](data/models/README.md).

- [x] `DB-017` **[GATE_A][SOL_HIGH] Enforce tenant isolation with PostgreSQL roles and row-level security.**
  - **Depends:** `DB-005`, `TRUST-008`, `TRUST-012`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DB_017`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DB_017`; `RACE=TestTodo_DB_017_Race`; `INTEGRATION=TestTodo_DB_017_Integration`; `FAULT=TestTodo_DB_017_Fault`; `SECURITY=TestTodo_DB_017_Security`.
  - **RED:** missing tenant context defaults open, forged session variable, SECURITY DEFINER function, FK join, maintenance role or error/count/timing oracle crosses tenant boundary.
  - **GREEN:** least-privilege application/migration/repair/read roles plus RLS policies fail closed; adversarial corpus returns no cross-tenant rows or existence signal.
  - **REFACTOR:** repository filters remain defense in depth, not the only boundary.
  - **Refs:** [Organization/AuthZ](specs/organization-scope-and-authz.md), [security models](data/models/security-trust.md).
  - **Evidence (2026-09-03):** `TestTodo_DB_017`, `_Race`, `_Integration`, `_Fault`, `_Security`, `_NilTenantRejected` in `internal/data/tenancy` (migration 00008: least-privilege `hcmnext_app` role, FORCE ROW LEVEL SECURITY with a `tenant_isolation` policy on every tenant-scoped table and each ledger_event partition, `CREATE ROLE` guarded for duplicate_object and unique_violation; `tenancy.WithTenant` sets `app.tenant_id` per transaction; cross-tenant reads return nothing); PASS; branch plan-revision-2026-09-02; `go test -count=1 ./internal/data/tenancy/...` on windows/arm64 (Go 1.26.3).

- [x] `DB-018` **[GATE_B][SOL_HIGH] Implement a Go repository and unit-of-work contract for aggregates.**
  - **Evidence (2026-09-05):** `TestTodo_DB_018` in `internal/data/uow` (UnitOfWork bound to a tenant with registered aggregate repositories, one-transaction Commit checking every staged aggregate's optimistic version with a typed conflict naming aggregate and versions, idempotent Rollback, no nesting; a generic AggregateRepository contract with Load at revision, append-only Save and ListRevisions, a pgtest-backed worker reference implementation over migration 00011 fenced by a derived revision count, an in-memory fake, and a conformance suite both pass; two concurrent units saving one aggregate commit exactly one; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/uow/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DB-008`–`DB-015`, `DB-017`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DB_018`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DB_018`; `GOLDEN=TestTodo_DB_018_Golden`; `RACE=TestTodo_DB_018_Race`; `INTEGRATION=TestTodo_DB_018_Integration`; `SECURITY=TestTodo_DB_018_Security`; `MUTATION=TestTodo_DB_018_Mutation`.
  - **RED:** repository omits tenant/org scope, bypasses expected version, returns untyped JSON, leaks transaction handle or commits partial aggregate state.
  - **GREEN:** generated/manual Go repositories expose typed load/append/revise/query operations, require context/version, map SQL errors to canonical codes and share one explicit transaction coordinator.
  - **REFACTOR:** repositories follow aggregate boundaries rather than table-shaped service APIs.
  - **Refs:** [Aggregate registry](data/models/registry-and-coverage-contracts.md), [transaction coordinator](specs/transaction-plan-and-commit-coordinator.md).

- [x] `DB-019` **[GATE_A][SOL_HIGH] Seed canonical registries and the Promotion fixture reproducibly.**
  - **Depends:** `DB-007`–`DB-011`, `WEDGE-006`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DB_019`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DB_019`; `GOLDEN=TestTodo_DB_019_Golden`; `RACE=TestTodo_DB_019_Race`; `INTEGRATION=TestTodo_DB_019_Integration`; `SECURITY=TestTodo_DB_019_Security`.
  - **RED:** seed depends on wall clock/random order, duplicates on rerun, contains production PII or produces different canonical digest.
  - **GREEN:** Go seed command inserts exact schema/capability/intent/reference/person/employment/position/compensation fixture IDs once and emits the checked-in expected counts/digest.
  - **REFACTOR:** test/design-partner seeds are separately packaged and environment fenced.
  - **Refs:** [Promotion fixture](reference-workflows/promote-into-management.md), [model registry](data/models/registry-and-coverage-contracts.md).
  - **Evidence (2026-09-03):** `TestTodo_DB_019`, `TestTodo_DB_019_Golden`, `TestTodo_DB_019_Race`, `TestTodo_DB_019_Integration`, `TestTodo_DB_019_Security`, `TestTodo_DB_019_Conformance` in `internal/data/seed`: registry registrations (schema, capability, intent, pay-band catalog) stay in `definition_version` with digest-checked conflicts, while person/worker/employment/assignment/legal-entity/org-unit/job/position/compensation fixtures load through the frozen `internal/data/aggregates` stores inside the caller's transaction, gated by an aggregate-corpus marker so exactly one transaction per tenant loads them under concurrency; `TestSeedCommandSeedsOnceAndEmitsStableReceipt` in `cmd/migrate` proves `migrate seed -tenant=<slug>` derives the same tenant UUID as the serving store, registers the tenant at a fixed fixture instant, emits exact deterministic JSON counts/digest, and is an idempotent no-op on rerun; `go test -count=1 ./cmd/migrate ./internal/data/seed` PASS via embedded-postgres on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `DB-020` **[GATE_B][SOL_HIGH] Define indexes and prove critical query plans.**
  - **Evidence (2026-09-05):** `TestTodo_DB_020`, `TestTodo_DB_020_{Golden,Integration,Security}` in `internal/data/queryplans` (a declared catalogue of six critical store queries — workforce list and get, ledger stream replay and effective-as-of, job partition list, intent context load — proven with EXPLAIN under the app role with RLS after seeding 20,000 rows across decoy tenants plus the tenant under proof, no sequential scan on the tenant-scoped table and an expected index used, plan shapes pinned as a golden; every catalogued query already had its index so no migration was needed; PROPERTY folded into PRIMARY, RACE/BENCHMARK/MUTATION not applicable to a read-path shape proof); `go test -count=1 ./migrations/ ./internal/data/queryplans/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02. Note: one integration run during verification chose a sequential scan on ledger_event_p3 for the effective-as-of query and the next run did not, so the proof is sensitive to planner statistics; a follow-up should pin the statistics target or assert the plan after a VACUUM ANALYZE of every partition.
  - **Depends:** `DB-018`, `DATA-005`, `DATA-006`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DB_020`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DB_020`; `PROPERTY=TestTodo_DB_020_Property`; `GOLDEN=TestTodo_DB_020_Golden`; `RACE=TestTodo_DB_020_Race`; `INTEGRATION=TestTodo_DB_020_Integration`; `SECURITY=TestTodo_DB_020_Security`; `BENCHMARK=BenchmarkTodo_DB_020`; `MUTATION=TestTodo_DB_020_Mutation`.
  - **RED:** tenant/effective-as-of/multi-domain snapshot/stream-head/projection-barrier/workflow-ready/outbox-ready/reconciliation-ready query performs unbounded scan or index omits tenant-leading isolation key.
  - **GREEN:** representative-scale Promotion fixtures meet declared p95 budgets and captured plans use bounded tenant-aware indexes without changing temporal, authority, watermark or completion semantics.
  - **REFACTOR:** index changes are rebuildable migrations with before/after plan evidence.
  - **Refs:** [SLO contract](specs/slo-sli-error-budget.md), [data plane](specs/platform-plane-model.md).

- [x] `DB-021` **[GATE_B][SOL_HIGH] Prove expand/backfill/shadow/cutover/contract database upgrades.**
  - **Evidence (2026-09-05):** `TestTodo_DB_021` in `internal/platform/schemaupgrade` (version matrix passes; resumable backfill records checkpoint/count/hash, shadow comparison is exact and cutover/rollback preserves ledger and active workflows; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/platform/schemaupgrade/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DB-006`, `DB-018`, `TOOL-020`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DB_021`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DB_021`; `GOLDEN=TestTodo_DB_021_Golden`; `RACE=TestTodo_DB_021_Race`; `INTEGRATION=TestTodo_DB_021_Integration`; `MUTATION=TestTodo_DB_021_Mutation`; `FAULT=TestTodo_DB_021_Fault`.
  - **RED:** old/new binaries cannot coexist, backfill skips/duplicates rows, cutover occurs with lag/digest mismatch or rollback requires history mutation.
  - **GREEN:** version matrix passes; resumable backfill records checkpoint/count/hash, shadow comparison is exact and cutover/rollback preserves ledger and active workflows.
  - **REFACTOR:** destructive contract step waits for consumer-adoption watermark.
  - **Refs:** [Foundation upgrades](specs/platform-foundation-gap-closure.md), [schema lifecycle](data/models/dataops-configuration.md).

- [x] `DB-022` **[GATE_B][SOL_HIGH] Verify database backup, PITR and tenant-scoped restore semantics.**
  - **Evidence (2026-09-05):** `TestTodo_DB_022` in `internal/operations/recovery` (isolated restore reaches requested recovery point, reports exact RPO/counts/hashes, reapplies hold/deletion manifests and remains fenced until conformance passes; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/operations/recovery/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DB-006`, `RECOVERY-002`, `PRIV-004`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DB_022`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DB_022`; `GOLDEN=TestTodo_DB_022_Golden`; `RACE=TestTodo_DB_022_Race`; `INTEGRATION=TestTodo_DB_022_Integration`; `FAULT=TestTodo_DB_022_Fault`; `SECURITY=TestTodo_DB_022_Security`; `CONFORMANCE=TestTodo_DB_022_Conformance`; `RECOVERY=TestTodo_DB_022_Recovery`; `MUTATION=TestTodo_DB_022_Mutation`.
  - **RED:** restored database violates ledger heads/FKs/invariants, loses migration journal/runtime leases or resurrects restricted data.
  - **GREEN:** isolated restore reaches requested recovery point, reports exact RPO/counts/hashes, reapplies hold/deletion manifests and remains fenced until conformance passes.
  - **REFACTOR:** tenant restore limitations and whole-cluster restore path are documented separately.
  - **Refs:** [Recovery models](data/models/operations-production.md), [records](specs/records-management-and-disposition.md).

- [ ] `DB-023` **[PHASE_2][SOL_HIGH] Materialize the Medical Leave and Return-to-Work domain state.**
  - **Depends:** `DB-004`, `DB-005`, `DB-007`, `LEAVE-002`, `BAL-001`, `MODEL-025`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DB_023`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DB_023`; `PROPERTY=TestTodo_DB_023_Property`; `GOLDEN=TestTodo_DB_023_Golden`; `RACE=TestTodo_DB_023_Race`; `INTEGRATION=TestTodo_DB_023_Integration`; `SECURITY=TestTodo_DB_023_Security`.
  - **RED:** mutable leave/eligibility/entitlement revision, overlapping incompatible active leave, orphan child intent/evidence/obligation, float or double-posted balance, unrestricted medical metadata, return without readiness revision, or leave transition that inactivates Employment commits.
  - **GREEN:** tenant-scoped typed tables/repositories materialize LeaveRequest/LeaveRecord revisions, program eligibility and entitlement plans, absence/availability relationships, balance accounts/entries/snapshots, evidence-review refs, work restrictions/readiness, obligations and related intent links with temporal/CAS/FK/compartment/idempotency constraints.
  - **REFACTOR:** common intent/workflow/ledger/integration rows remain in their existing owners; this migration adds only Leave-domain authority and typed references.
  - **Refs:** [Leave models](data/models/rewards-payroll-workforce.md), [model storage registry](data/models/registry-and-coverage-contracts.md), [Leave workflow](workflows/leave/leave-return-to-work.md).

- [x] `DB-024` **[GATE_C][SOL_HIGH] Produce a payroll reconciliation and auditor-evidence package from ledger state.**
  - **Evidence (2026-09-05):** `TestTodo_DB_024` in `internal/domains/payroll/auditpack` (internal/domains/payroll/auditpack produces a payroll reconciliation and auditor-evidence package from ledger state with a deterministic digest; written by a Sonnet subagent and verified independently); `go test -count=1 ./internal/domains/payroll/auditpack/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DB-017`, `DB-018`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DB_024`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DB_024`; `GOLDEN=TestTodo_DB_024_Golden`; `SECURITY=TestTodo_DB_024_Security`; `INTEGRATION=TestTodo_DB_024_Integration`; `RECOVERY=TestTodo_DB_024_Recovery`; `MUTATION=TestTodo_DB_024_Mutation`.
  - **RED:** a payroll run's register total, bank-file total, tax-liability total, filing acknowledgment and general-ledger tie-out have no shared idempotency key and no recorded variance decision, so a duplicate, missing, altered or misdirected transaction can pass unreconciled.
  - **GREEN:** each payroll run resolves an immutable idempotency key binding payroll-register, bank-file, tax-liability and filing-acknowledgment totals sourced from `internal/data/ledger/checkpoint`/`evidence`; an unexplained variance blocks release and an exportable, independently verifiable auditor package is produced from signed checkpoint/evidence data alone.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §2, F-08/F-09/F-10/F-15](research/security-best-practices-2026.md#2-financial-applications-and-payment-touching-systems), [IRS W-2/W-3 topic](https://www.irs.gov/taxtopics/tc752), [PCAOB AS 2201](https://pcaobus.org/oversight/standards/auditing-standards/details/AS2201). Maps to PCAOB AS 2201 (ITGC/application controls); IRS employment-tax recordkeeping.

#### Domain persistence backfill (2026-09-05)

- [x] `PERSIST-ACCESS-001` **[PHASE_3][SOL_HIGH] Add durable PostgreSQL tables for the access identity and entitlement graph.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_ACCESS_001` in `internal/data/accessstore` (internal/data/accessstore over migration 00051 persists access grants and revocations with tenant RLS; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/accessstore/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ACCESS-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist the access domain's identity, account-link and entitlement revisions and its observation evidence under tenant isolation so the graph survives process restart`.
  - **TEST:** `TestTodo_PERSIST_ACCESS_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_ACCESS_001`; `FAULT=TestTodo_PERSIST_ACCESS_001_Fault`; `INTEGRATION=TestTodo_PERSIST_ACCESS_001_Integration`; `SECURITY=TestTodo_PERSIST_ACCESS_001_Security`; `RECOVERY=TestTodo_PERSIST_ACCESS_001_Recovery`; `MUTATION=TestTodo_PERSIST_ACCESS_001_Mutation`.
  - **RED:** a row lacking `tenant_id` is accepted, a session for tenant A reads or joins a `workforce_identity`/`account_link`/`expected_entitlement` row owned by tenant B, an `expected_entitlement` references an `account_link` or `entitlement_definition` from a different tenant, a revision row is updated in place instead of appended with a new `revision`, or `external_access_observation` accepts an `UPDATE`/`DELETE`.
  - **GREEN:** five tenant-scoped tables back `internal/domains/access`. `workforce_identity` (immutable revision): `row_id uuid`, `tenant_id uuid NOT NULL`, `identity_id text NOT NULL`, `subject text NOT NULL`, `system text NOT NULL`, `worker_ref uuid NOT NULL`, `revision cas_version NOT NULL`, `authority_class text NOT NULL`, `effective_from timestamptz NOT NULL`, `effective_to timestamptz`, `known_at timestamptz NOT NULL`, `lifecycle text NOT NULL`, `digest content_digest NOT NULL`; `PRIMARY KEY (tenant_id, row_id)`, `UNIQUE (tenant_id, identity_id, revision)`. `account_link` (immutable revision): `row_id`, `tenant_id`, `link_id text NOT NULL`, `workforce_identity_ref uuid NOT NULL`, `source text NOT NULL`, `application text NOT NULL`, `account_id text NOT NULL`, `revision cas_version NOT NULL`, `effective_from/effective_to`, `digest content_digest NOT NULL`; `PRIMARY KEY (tenant_id, row_id)`, `FOREIGN KEY (tenant_id, workforce_identity_ref) REFERENCES workforce_identity (tenant_id, row_id)`. `entitlement_definition` (immutable revision): `row_id`, `tenant_id`, `definition_id text NOT NULL`, `application text NOT NULL`, `code text NOT NULL`, `version text NOT NULL`, `risk_class text NOT NULL`, `owner text NOT NULL`, `revision cas_version NOT NULL`, `effective_from/to`, `digest content_digest NOT NULL`; `UNIQUE (tenant_id, application, code, version)`. `expected_entitlement` (immutable revision): `row_id`, `tenant_id`, `expected_id text NOT NULL`, `workforce_identity_ref uuid NOT NULL`, `account_link_ref uuid`, `entitlement_ref uuid NOT NULL`, `employment_ref uuid`, `position_ref uuid`, `policy_ref uuid`, `revision cas_version NOT NULL`, `effective_from/to`, `digest content_digest NOT NULL`; every ref column is a compound `(tenant_id, ...)` foreign key into its owning table. `external_access_observation` (append-only evidence, `forbid_mutation`): `row_id`, `tenant_id`, `observed_workforce_identity_ref uuid NOT NULL`, `provider_version text NOT NULL`, `observed_state jsonb NOT NULL`, `event_sequence bigint NOT NULL`, `observed_at timestamptz NOT NULL`, `recorded_at timestamptz NOT NULL DEFAULT now()`; `UNIQUE (tenant_id, observed_workforce_identity_ref, event_sequence)`. All six carry `tenant_isolation` (`tenant_id = NULLIF(current_setting('app.tenant_id', true), '')::uuid`) with `FORCE ROW LEVEL SECURITY`, granted only to `hcmnext_app` (DB-017). Revision tables: storage-disposition role AGGREGATE, retention PERMANENT, `rebuild_source` none. `external_access_observation`: role LEDGER, retention PERMANENT, append-only. New table set numbered directly after the reconciliation-job and session-store lanes already claiming 00039-00041.
  - **REFACTOR:** reuse the bitemporal revision shape and `aggregate_forbid_inplace_update`-style guard already established for person/worker/employment rather than inventing a new versioning convention; register every table in `definitions/storage/storage-disposition.yaml`.
  - **Refs:** `internal/domains/access` (kernel revision and event vocabulary), `definitions/storage/storage-disposition.yaml` (disposition registry this entry extends).

- [x] `PERSIST-SCHEDULING-001` **[PHASE_3][SOL_HIGH] Add durable PostgreSQL tables for appointment, availability and clock-device semantics.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_SCHEDULING_001` in `internal/data/schedulingstore` (four tenant-scoped tables. appointment_requirement (immutable revision, backs appointment.Requirement): row_id uuid, tenant_id uuid NOT NULL, requirement_id text NOT NULL, version text NOT NULL, revision cas_version NOT NULL, purpose text NOT NULL, participants jsonb NOT NULL, du; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/schedulingstore/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `APPT-001`, `AVAIL-001`, `CLOCK-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist the appointment, availability and clock-device revision vocabularies under tenant isolation so scheduling facts and device policy survive process restart`.
  - **TEST:** `TestTodo_PERSIST_SCHEDULING_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_SCHEDULING_001`; `FAULT=TestTodo_PERSIST_SCHEDULING_001_Fault`; `INTEGRATION=TestTodo_PERSIST_SCHEDULING_001_Integration`; `SECURITY=TestTodo_PERSIST_SCHEDULING_001_Security`; `RECOVERY=TestTodo_PERSIST_SCHEDULING_001_Recovery`; `MUTATION=TestTodo_PERSIST_SCHEDULING_001_Mutation`.
  - **RED:** an `appointment_requirement` or `resource_type` row is updated in place rather than superseded by a new `revision`, a `worker_availability` row is deleted instead of appended, a `time_device_registration` row is readable across tenants, or any table admits a row with a null `tenant_id`.
  - **GREEN:** four tenant-scoped tables. `appointment_requirement` (immutable revision, backs `appointment.Requirement`): `row_id uuid`, `tenant_id uuid NOT NULL`, `requirement_id text NOT NULL`, `version text NOT NULL`, `revision cas_version NOT NULL`, `purpose text NOT NULL`, `participants jsonb NOT NULL`, `duration interval NOT NULL`, `window_start/window_end timestamptz`, `location_class text`, `privacy_class text NOT NULL`, `lead_time interval`, `state text NOT NULL`, `canonical_digest content_digest NOT NULL`; `PRIMARY KEY (tenant_id, row_id)`, `UNIQUE (tenant_id, requirement_id, revision)`. `resource_type` (immutable revision): `row_id`, `tenant_id`, `resource_type_id text NOT NULL`, `version text NOT NULL`, `revision cas_version NOT NULL`, `name text NOT NULL`, `kind text NOT NULL`, `capacity_mode text NOT NULL`, `capacity_limit numeric(9,2)`, `privacy_class text`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, resource_type_id, revision)`. `worker_availability` (append-only availability stream, `forbid_mutation`, backs `availability.WorkerAvailability`): `row_id`, `tenant_id`, `availability_id text NOT NULL`, `revision cas_version NOT NULL`, `worker_ref uuid NOT NULL`, `employment_ref uuid`, `assignment_ref uuid`, `scope text NOT NULL`, `state text NOT NULL`, `reason text`, `effective_from/effective_to timestamptz`, `source text NOT NULL`, `recorded_at timestamptz NOT NULL DEFAULT now()`; `UNIQUE (tenant_id, availability_id, revision)`. `time_device_registration` (immutable revision, backs `clock.Registration`/`TimeDevice`/`TimeSource`): `row_id`, `tenant_id`, `device_id text NOT NULL`, `version text NOT NULL`, `state text NOT NULL`, `owner_ref uuid`, `location_ref uuid`, `clock_trust_policy jsonb NOT NULL`, `offline_policy jsonb`, `replay_policy jsonb`, `signature_policy jsonb`, `firmware_policy jsonb`, `certificate_policy jsonb`, `retention_policy jsonb`; `UNIQUE (tenant_id, device_id, version)`. All four: `tenant_isolation` RLS, `FORCE ROW LEVEL SECURITY`, `hcmnext_app` only (DB-017). `appointment_requirement`/`resource_type`/`time_device_registration`: role AGGREGATE, retention PERMANENT. `worker_availability`: role LEDGER, retention PERMANENT, append-only.
  - **REFACTOR:** keep these three domains in one table set only because none of them import each other and each contributes at most two tables; split into separate table sets the moment any one of them grows a second dependent aggregate.
  - **Refs:** `internal/domains/appointment`, `internal/domains/availability`, `internal/domains/clock`, `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-ASSET-001` **[PHASE_3][SOL_HIGH] Add durable PostgreSQL tables for equipment inventory and custody.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_ASSET_001` in `internal/data/assetstore` (two tenant-scoped tables, distinct from the already-covered artifact_quarantine/artifact_quarantine_state companion-schema tables (00030), which back asset/quarantine, not this package. asset_inventory (immutable revision, backs asset.InventoryRevision): row_id uuid, tenant_id uu; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/assetstore/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ASSET-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist the asset domain's inventory and custody revisions under tenant isolation now that adapters are responsible for the compare-and-set boundary the kernel already defines`.
  - **TEST:** `TestTodo_PERSIST_ASSET_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_ASSET_001`; `FAULT=TestTodo_PERSIST_ASSET_001_Fault`; `INTEGRATION=TestTodo_PERSIST_ASSET_001_Integration`; `SECURITY=TestTodo_PERSIST_ASSET_001_Security`; `RECOVERY=TestTodo_PERSIST_ASSET_001_Recovery`; `MUTATION=TestTodo_PERSIST_ASSET_001_Mutation`.
  - **RED:** two `asset_custody_event` rows for the same asset share an `event_sequence`, a custody row is updated or deleted, an `asset_inventory` revision is overwritten instead of superseded, or a row from tenant A is visible to tenant B.
  - **GREEN:** two tenant-scoped tables, distinct from the already-covered `artifact_quarantine`/`artifact_quarantine_state` companion-schema tables (00030), which back `asset/quarantine`, not this package. `asset_inventory` (immutable revision, backs `asset.InventoryRevision`): `row_id uuid`, `tenant_id uuid NOT NULL`, `inventory_id text NOT NULL`, `owner_ref uuid NOT NULL`, `classification text NOT NULL`, `serial_number text`, `revision cas_version NOT NULL`, `effective_at timestamptz NOT NULL`, `status text NOT NULL`, `digest content_digest NOT NULL`; `PRIMARY KEY (tenant_id, row_id)`, `UNIQUE (tenant_id, inventory_id, revision)`. `asset_custody_event` (append-only custody stream, `forbid_mutation`, backs `asset.CustodyRevision`): `row_id`, `tenant_id`, `custody_id text NOT NULL`, `asset_ref uuid NOT NULL`, `worker_ref uuid`, `location text`, `condition text`, `assignee_receipt jsonb`, `issuer_receipt jsonb`, `revision cas_version NOT NULL`, `effective_at timestamptz NOT NULL`, `status text NOT NULL`, `event_sequence bigint NOT NULL`, `digest content_digest NOT NULL`; `UNIQUE (tenant_id, asset_ref, event_sequence)`, `FOREIGN KEY (tenant_id, asset_ref) REFERENCES asset_inventory (tenant_id, inventory_id)` deferred to the application layer since `asset_inventory`'s natural key is `(tenant_id, inventory_id)` not the surrogate `row_id`. `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY` on both, `hcmnext_app` only. `asset_inventory`: role AGGREGATE, retention PERMANENT. `asset_custody_event`: role LEDGER, retention PERMANENT, append-only.
  - **REFACTOR:** ownership boundary with 00030 stays explicit in the migration header, mirroring 00030's own companion-schema note, so a future reader does not conflate quarantined bytes with committed custody.
  - **Refs:** `internal/domains/asset`, `internal/domains/asset/quarantine` (already-covered boundary), `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-ATTESTATION-001` **[PHASE_3][SOL_HIGH] Add durable PostgreSQL tables for attestation statements and human-work approval resolution.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_ATTESTATION_001` in `internal/data/attestationstore`, `internal/domains/attestation` (internal/data/attestationstore over migration 00045 persists attestation statements and human-work approval resolutions with tenant RLS; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/attestationstore/ ./internal/domains/attestation/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ATTEST-001`, `APPROVAL-001`, `WORK-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist attestation statements, their rebind evidence, and human-work approval requirements and resolutions under tenant isolation, distinct from the already-durable work_item tables`.
  - **TEST:** `TestTodo_PERSIST_ATTESTATION_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_ATTESTATION_001`; `FAULT=TestTodo_PERSIST_ATTESTATION_001_Fault`; `INTEGRATION=TestTodo_PERSIST_ATTESTATION_001_Integration`; `SECURITY=TestTodo_PERSIST_ATTESTATION_001_Security`; `RECOVERY=TestTodo_PERSIST_ATTESTATION_001_Recovery`; `MUTATION=TestTodo_PERSIST_ATTESTATION_001_Mutation`.
  - **RED:** an `attestation_binding` row is rebound without a new `binding_version`, an `approval_resolution` row is mutated after being recorded, two resolutions for the same requirement share an `event_sequence`, or a statement/requirement row is read across a tenant boundary.
  - **GREEN:** four tenant-scoped tables. `attestation_statement` (immutable revision, backs `internal/domains/attestation.AttestationStatement`): `row_id uuid`, `tenant_id uuid NOT NULL`, `statement_id text NOT NULL`, `version cas_version NOT NULL`, `kind text NOT NULL`, `subject_ref uuid NOT NULL`, `attester jsonb NOT NULL`, `text_digest content_digest NOT NULL`, `evidence_refs jsonb`, `validity_from/validity_to timestamptz`, `jurisdiction_ref uuid`, `revocation_link jsonb`, `digest content_digest NOT NULL`; `UNIQUE (tenant_id, statement_id, version)`. `attestation_binding` (append-only rebind log, `forbid_mutation`): `row_id`, `tenant_id`, `statement_id text NOT NULL`, `statement_version cas_version NOT NULL`, `context_digest content_digest NOT NULL`, `evidence_bindings jsonb`, `binding_version bigint NOT NULL`, `bound_at timestamptz NOT NULL`, `signer uuid NOT NULL`, `digest content_digest NOT NULL`; `UNIQUE (tenant_id, statement_id, binding_version)`. `approval_requirement` (immutable revision, backs `internal/humanwork.ApprovalRequirement`): `row_id`, `tenant_id`, `requirement_id text NOT NULL`, `revision cas_version NOT NULL`, `stage text NOT NULL`, `candidates jsonb`, `authority_floor jsonb`, `quorum jsonb`, `deadline interval`, `escalation jsonb`, `separation jsonb`, `invalidators jsonb`, `source text`, `expression_digest content_digest NOT NULL`; `UNIQUE (tenant_id, requirement_id, revision)`. `approval_resolution` (append-only evidence log, `forbid_mutation`, backs `internal/humanwork.Resolution`): `row_id`, `tenant_id`, `requirement_id text NOT NULL`, `requirement_revision cas_version NOT NULL`, `outcome text NOT NULL`, `candidates jsonb`, `excluded jsonb`, `fallback_used boolean NOT NULL DEFAULT false`, `resolved_at timestamptz NOT NULL`, `effective_at timestamptz`, `directory_version text`, `expression_digest content_digest`, `requirement_digest content_digest`, `quorum_required int`, `event_sequence bigint NOT NULL`; `UNIQUE (tenant_id, requirement_id, event_sequence)`. All four: `tenant_isolation` RLS, `FORCE ROW LEVEL SECURITY`, `hcmnext_app` only. Revision tables: role AGGREGATE, retention PERMANENT. Log tables: role LEDGER, retention PERMANENT, append-only.
  - **REFACTOR:** this table set is distinct from and has no foreign key to `work_item`/`work_item_transition`/`work_item_decision` (00017/00022): a `WorkItem.ApprovalRequirementRef` names a row here by value, ownership does not merge.
  - **Refs:** `internal/domains/attestation`, `internal/humanwork` (`attestation_statement.go`, `requirement.go`, `resolve.go`), `internal/humanwork/workitem` (already-covered boundary), `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-BALANCE-001` **[PHASE_3][SOL_HIGH] Add durable PostgreSQL tables for accumulator definitions and posted balance entries.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_BALANCE_001` in `internal/data/balancestore` (internal/data/balancestore over migration 00052 persists accumulator definitions and posted balance entries with tenant RLS; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/balancestore/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `BAL-001`, `BAL-002`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist accumulator definitions and the immutable, typed balance and lifecycle entries the balance kernel already models as an append-only ledger`.
  - **TEST:** `TestTodo_PERSIST_BALANCE_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_BALANCE_001`; `FAULT=TestTodo_PERSIST_BALANCE_001_Fault`; `INTEGRATION=TestTodo_PERSIST_BALANCE_001_Integration`; `SECURITY=TestTodo_PERSIST_BALANCE_001_Security`; `RECOVERY=TestTodo_PERSIST_BALANCE_001_Recovery`; `MUTATION=TestTodo_PERSIST_BALANCE_001_Mutation`.
  - **RED:** two entries reuse the same `idempotency_key` for one tenant, a `balance_entry`/`balance_lifecycle_entry` row is updated or deleted, an entry posts against an unknown or cross-tenant `definition_id`, or the account's `event_sequence` has a gap.
  - **GREEN:** three tenant-scoped tables. `accumulator_definition` (immutable revision, backs `balance.AccumulatorDefinition`): `row_id uuid`, `tenant_id uuid NOT NULL`, `definition_id text NOT NULL`, `version text NOT NULL`, `revision cas_version NOT NULL`, `name text NOT NULL`, `unit text NOT NULL`, `currency text`, `subject text NOT NULL`, `period text NOT NULL`, `dimensions jsonb`, `entry_types jsonb`, `authority text`, `floor_policy/cap_policy/expiry_policy/rollover_policy/correction_policy jsonb`; `UNIQUE (tenant_id, definition_id, version)`. `balance_entry` (append-only ledger, `forbid_mutation`, backs `balance.BalanceEntry`): `row_id`, `tenant_id`, `account_id text NOT NULL`, `definition_id text NOT NULL`, `definition_version cas_version NOT NULL`, `unit text NOT NULL`, `currency text`, `subject text NOT NULL`, `period text NOT NULL`, `dimensions jsonb`, `kind text NOT NULL`, `amount numeric(19,4) NOT NULL`, `entry_type text NOT NULL`, `source_transaction_id text`, `idempotency_key text NOT NULL`, `effective_at/recorded_at/authorized_at timestamptz`, `event_sequence bigint NOT NULL`; `UNIQUE (tenant_id, idempotency_key)`, `UNIQUE (tenant_id, account_id, event_sequence)`. `balance_lifecycle_entry` (append-only, `forbid_mutation`, backs `balance.LifecycleEntry`): `row_id`, `tenant_id`, `entry_ref uuid NOT NULL`, `operation text NOT NULL`, `reason text`, `policy_id text`, `policy_version cas_version`, `source_period text`, `target_period text`, `source_entries jsonb`, `event_sequence bigint NOT NULL`, `recorded_at timestamptz NOT NULL DEFAULT now()`. `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY` on all three, `hcmnext_app` only. `accumulator_definition`: role AGGREGATE, retention PERMANENT. `balance_entry`/`balance_lifecycle_entry`: role LEDGER, retention PERMANENT, append-only.
  - **REFACTOR:** money stays `numeric(19,4)`, never float, matching the house convention already enforced in `compensation_component`/`compensation_band`.
  - **Refs:** `internal/domains/balance`, `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-BENEFITS-001` **[PHASE_3][SOL_HIGH] Add a durable PostgreSQL table for the benefit plan-year revision catalogue.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_BENEFITS_001` in `internal/data/benefitsstore` (one tenant-scoped table. benefit_plan_revision (immutable revision, backs benefits.PlanRevision): row_id uuid, tenant_id uuid NOT NULL, plan_id uuid NOT NULL, revision cas_version NOT NULL, supersedes uuid, plan_year int NOT NULL, name text NOT NULL, carrier_ref/provider_ref/spon; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/benefitsstore/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `BEN-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist the read-only benefit plan and plan-year revision catalogue so its supersession chain survives process restart`.
  - **TEST:** `TestTodo_PERSIST_BENEFITS_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_BENEFITS_001`; `FAULT=TestTodo_PERSIST_BENEFITS_001_Fault`; `INTEGRATION=TestTodo_PERSIST_BENEFITS_001_Integration`; `SECURITY=TestTodo_PERSIST_BENEFITS_001_Security`; `RECOVERY=TestTodo_PERSIST_BENEFITS_001_Recovery`; `MUTATION=TestTodo_PERSIST_BENEFITS_001_Mutation`.
  - **RED:** a `benefit_plan_revision` row is updated in place, `supersedes` points at a revision of a different `plan_id`, or a row is readable outside its owning tenant.
  - **GREEN:** one tenant-scoped table. `benefit_plan_revision` (immutable revision, backs `benefits.PlanRevision`): `row_id uuid`, `tenant_id uuid NOT NULL`, `plan_id uuid NOT NULL`, `revision cas_version NOT NULL`, `supersedes uuid`, `plan_year int NOT NULL`, `name text NOT NULL`, `carrier_ref/provider_ref/sponsor_ref uuid`, `jurisdiction text`, `currency text`, `coverage_tiers jsonb`, `options jsonb`, `rate_schedule_ref/eligibility_rules_ref/enrollment_rules_ref/contribution_rules_ref uuid`, `effective_from/effective_to timestamptz`, `authority_ref uuid`, `canonical_digest content_digest NOT NULL`; `PRIMARY KEY (tenant_id, row_id)`, `UNIQUE (tenant_id, plan_id, revision)`, `FOREIGN KEY (tenant_id, supersedes) REFERENCES benefit_plan_revision (tenant_id, row_id)`. `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY`, `hcmnext_app` only. Storage-disposition role AGGREGATE, retention PERMANENT, `rebuild_source` none.
  - **REFACTOR:** business meaning stays on the append-only revision chain, never on the logical `plan`/`plan_year` identity, matching the domain's own doc comment.
  - **Refs:** `internal/domains/benefits`, `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-BUDGET-001` **[PHASE_3][SOL_HIGH] Add durable PostgreSQL tables for compensation-reservation state and its transition log.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_BUDGET_001` in `internal/data/budgetstore` (internal/data/budgetstore over migration 00044 persists compensation-reservation state and its transition log under an advisory-lock CAS (fix lane corrected the NUL lock key and digest encoding); written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/budgetstore/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `BUDGET-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist the compensation-reservation state machine and its fenced transition log, distinct from the workforce_budget/budget_reservation capacity tables DB-010 already owns`.
  - **TEST:** `TestTodo_PERSIST_BUDGET_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_BUDGET_001`; `FAULT=TestTodo_PERSIST_BUDGET_001_Fault`; `INTEGRATION=TestTodo_PERSIST_BUDGET_001_Integration`; `SECURITY=TestTodo_PERSIST_BUDGET_001_Security`; `RECOVERY=TestTodo_PERSIST_BUDGET_001_Recovery`; `MUTATION=TestTodo_PERSIST_BUDGET_001_Mutation`.
  - **RED:** a `compensation_reservation` update carries a `fence` that is not exactly one greater than the row's current fence, two reservations for one tenant reuse an `idempotency_key`, a `reservation_event.sequence` skips a value, or a reservation is read across tenants.
  - **GREEN:** two tenant-scoped tables, named to avoid any collision with DB-010's `budget_reservation` (a different aggregate keyed on `budget_ref`/proposal, not a fenced state machine). `compensation_reservation` (mutable, CAS-fenced current-state row, backs `budget.CompensationReservation`): `row_id uuid`, `tenant_id uuid NOT NULL`, `budget_id text NOT NULL`, `proposal_digest content_digest`, `amount numeric(19,4) NOT NULL`, `currency text NOT NULL`, `authority_digest content_digest`, `idempotency_key text NOT NULL`, `expires_at timestamptz`, `state text NOT NULL`, `fence bigint NOT NULL DEFAULT 0`, `created_at/updated_at timestamptz NOT NULL DEFAULT now()`; `PRIMARY KEY (tenant_id, row_id)`, `UNIQUE (tenant_id, idempotency_key)`. `reservation_event` (append-only transition log, `forbid_mutation`, backs `budget.ReservationEvent`): `row_id`, `tenant_id`, `reservation_id uuid NOT NULL`, `sequence bigint NOT NULL`, `from_state text`, `to_state text NOT NULL`, `fence bigint NOT NULL`, `at timestamptz NOT NULL`; `UNIQUE (tenant_id, reservation_id, sequence)`, `FOREIGN KEY (tenant_id, reservation_id) REFERENCES compensation_reservation (tenant_id, row_id)`. `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY` on both, `hcmnext_app` only. `compensation_reservation`: role CONTROL, retention OPERATIONAL (matches the CONTROL classification already used for other CAS-fenced runtime rows). `reservation_event`: role LEDGER, retention PERMANENT, append-only.
  - **REFACTOR:** the migration header states this table's distinction from DB-010's `budget_reservation` explicitly so a future reader does not attempt to merge the two aggregates.
  - **Refs:** `internal/domains/budget`, `migrations/00013_compensation_aggregates.sql` (the `budget_reservation` table this entry is distinct from), `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-CAREER-001` **[PHASE_4][SOL_HIGH] Add durable PostgreSQL tables for career preference, target-role, development-objective and assessment revisions.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_CAREER_001` in `internal/data/careerstore`, `internal/domains/career` (four tenant-scoped tables, all backing the newer \*ProfileRevision generation in career/profile.go (the canonical one, per its added Supersedes/CanonicalDigest fields) rather than the older flat structs in preference.go/target.go/objective.go. career_preference_revision: row_id uu; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/careerstore/ ./internal/domains/career/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CAREER-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist the career domain's four revision families under tenant isolation, superseding the older non-digested generation of the same concepts`.
  - **TEST:** `TestTodo_PERSIST_CAREER_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_CAREER_001`; `FAULT=TestTodo_PERSIST_CAREER_001_Fault`; `INTEGRATION=TestTodo_PERSIST_CAREER_001_Integration`; `SECURITY=TestTodo_PERSIST_CAREER_001_Security`; `RECOVERY=TestTodo_PERSIST_CAREER_001_Recovery`; `MUTATION=TestTodo_PERSIST_CAREER_001_Mutation`.
  - **RED:** a career revision row is updated instead of superseded, `supersedes` names a revision belonging to a different worker or a different tenant, or a row is visible outside its tenant.
  - **GREEN:** four tenant-scoped tables, all backing the newer `*ProfileRevision` generation in `career/profile.go` (the canonical one, per its added `Supersedes`/`CanonicalDigest` fields) rather than the older flat structs in `preference.go`/`target.go`/`objective.go`. `career_preference_revision`: `row_id uuid`, `tenant_id uuid NOT NULL`, `preference_id uuid NOT NULL`, `revision cas_version NOT NULL`, `supersedes cas_version`, `worker_ref uuid NOT NULL`, `roles jsonb`, `locations jsonb`, `work_arrangements jsonb`, `mobility_preference text`, `travel_preference text`, `timing_constraints jsonb`, `visibility text NOT NULL`, `consent_ref uuid`, `effective_from/effective_to timestamptz`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, preference_id, revision)`. `target_role_revision`: same envelope columns plus `target_role_id uuid NOT NULL`, `worker_ref uuid NOT NULL`, `job_profile_ref uuid`, `job_profile_revision cas_version`, `priority int`, `rationale text`. `development_objective_revision`: envelope plus `objective_id uuid NOT NULL`, `worker_ref uuid NOT NULL`, `target_role_ref uuid`, `skill_refs jsonb`, `description text`, `owner text`. `career_assessment_revision`: envelope plus `assessment_id uuid NOT NULL`, `worker_ref uuid NOT NULL`, `target_role_ref uuid`, `source text`, `epistemic_class text NOT NULL`, `summary text`. All four: `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY`, `hcmnext_app` only; role AGGREGATE, retention PERMANENT, `rebuild_source` none.
  - **REFACTOR:** the migration adds only the `profile.go` generation; the older `preference.go`/`target.go`/`objective.go` structs are dead code to remove in a follow-up, not to persist twice.
  - **Refs:** `internal/domains/career` (`profile.go`), `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-CBA-001` **[PHASE_4][SOL_HIGH] Add durable PostgreSQL tables for collective-bargaining agreement, unit, membership and clause revisions.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_CBA_001` in `internal/data/cbastore`, `internal/domains/cba` (four tenant-scoped tables (none carry a TenantID field in the Go kernel today; the adapter boundary this entry builds adds tenant_id as the persistence-layer column). cba_agreement_revision: row_id uuid, tenant_id uuid NOT NULL, agreement_id text NOT NULL, revision bigint NOT NUL; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/cbastore/ ./internal/domains/cba/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CBA-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=give the CBA kernel's adapter-supplied revisions a durable backing store, since the package itself is deliberately persistence-free`.
  - **TEST:** `TestTodo_PERSIST_CBA_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_CBA_001`; `FAULT=TestTodo_PERSIST_CBA_001_Fault`; `INTEGRATION=TestTodo_PERSIST_CBA_001_Integration`; `SECURITY=TestTodo_PERSIST_CBA_001_Security`; `RECOVERY=TestTodo_PERSIST_CBA_001_Recovery`; `MUTATION=TestTodo_PERSIST_CBA_001_Mutation`.
  - **RED:** a `retired` agreement/unit/membership/clause row is un-retired by update rather than a new revision, `known_to` precedes `known_from`, or a row is readable across tenants.
  - **GREEN:** four tenant-scoped tables (none carry a `TenantID` field in the Go kernel today; the adapter boundary this entry builds adds `tenant_id` as the persistence-layer column). `cba_agreement_revision`: `row_id uuid`, `tenant_id uuid NOT NULL`, `agreement_id text NOT NULL`, `revision bigint NOT NULL`, `version text NOT NULL`, `title text NOT NULL`, `representative text`, `source text`, `effective_from/effective_to timestamptz`, `known_from/known_to timestamptz`, `precedence int NOT NULL`, `retired boolean NOT NULL DEFAULT false`; `UNIQUE (tenant_id, agreement_id, revision)`. `cba_bargaining_unit_revision`: `row_id`, `tenant_id`, `unit_id text NOT NULL`, `revision bigint NOT NULL`, `agreement_id text NOT NULL`, `name text NOT NULL`, `representative text`, `source text`, `effective_from/to`, `known_from/to`, `retired boolean NOT NULL DEFAULT false`; `UNIQUE (tenant_id, unit_id, revision)`. `cba_membership_revision`: `row_id`, `tenant_id`, `membership_id text NOT NULL`, `revision bigint NOT NULL`, `worker_id text NOT NULL`, `unit_id text NOT NULL`, `source text`, `effective_from/to`, `known_from/to`, `retired boolean NOT NULL DEFAULT false`; `UNIQUE (tenant_id, membership_id, revision)`. `cba_agreement_clause_revision`: `row_id`, `tenant_id`, `clause_id text NOT NULL`, `agreement_id text NOT NULL`, `agreement_revision bigint NOT NULL`, `revision bigint NOT NULL`, `kind text NOT NULL`, `job_codes jsonb`, `location_ids jsonb`, `effective_from/to`, `known_from/to`, `retired boolean NOT NULL DEFAULT false`; `UNIQUE (tenant_id, clause_id, revision)`. All four: `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY`, `hcmnext_app` only; role AGGREGATE, retention PERMANENT.
  - **REFACTOR:** the pinned bitemporal read the kernel evaluates stays a pure function over these rows; no evaluation logic moves into the database.
  - **Refs:** `internal/domains/cba`, `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-CONTACT-001` **[PHASE_3][SOL_HIGH] Add durable PostgreSQL tables for contact endpoint revisions and verification challenges.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_CONTACT_001` in `internal/data/contactstore` (three tenant-scoped tables backing the newer revision.go generation (ContactEndpointRevision/ContactVerificationChallenge), not the older endpoint.go/challenge.go structs. contact_endpoint_revision: row_id uuid, tenant_id uuid NOT NULL, subject_ref uuid NOT NULL, endpoint_id uuid; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/contactstore/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CONTACT-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist the newer digested contact-endpoint revision generation and its verification-challenge audit stream under tenant isolation`.
  - **TEST:** `TestTodo_PERSIST_CONTACT_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_CONTACT_001`; `FAULT=TestTodo_PERSIST_CONTACT_001_Fault`; `INTEGRATION=TestTodo_PERSIST_CONTACT_001_Integration`; `SECURITY=TestTodo_PERSIST_CONTACT_001_Security`; `RECOVERY=TestTodo_PERSIST_CONTACT_001_Recovery`; `MUTATION=TestTodo_PERSIST_CONTACT_001_Mutation`.
  - **RED:** a `contact_challenge_event` row is updated or deleted, two events for one challenge share an `event_sequence`, a raw (non-digested) contact value is stored instead of `normalized_value_digest`, or a row is readable across tenants.
  - **GREEN:** three tenant-scoped tables backing the newer `revision.go` generation (`ContactEndpointRevision`/`ContactVerificationChallenge`), not the older `endpoint.go`/`challenge.go` structs. `contact_endpoint_revision`: `row_id uuid`, `tenant_id uuid NOT NULL`, `subject_ref uuid NOT NULL`, `endpoint_id uuid NOT NULL`, `revision bigint NOT NULL`, `supersedes_revision bigint`, `kind text NOT NULL`, `purpose text NOT NULL`, `priority int`, `source text`, `normalized_value_digest content_digest NOT NULL`, `display_hint text`, `verification jsonb`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, endpoint_id, revision)`. `contact_verification_challenge` (mutable current-state row): `row_id`, `tenant_id`, `challenge_id uuid NOT NULL`, `subject_ref uuid NOT NULL`, `endpoint_id uuid NOT NULL`, `endpoint_revision_digest content_digest`, `normalized_value_digest content_digest NOT NULL`, `purpose text`, `issued_at/expires_at timestamptz NOT NULL`, `attempt_budget int NOT NULL`, `attempts int NOT NULL DEFAULT 0`, `token_digest content_digest`, `status text NOT NULL`, `canonical_digest content_digest NOT NULL`; `PRIMARY KEY (tenant_id, row_id)`, `UNIQUE (tenant_id, challenge_id)`. `contact_challenge_event` (append-only, `forbid_mutation`): `row_id`, `tenant_id`, `challenge_ref uuid NOT NULL`, `kind text NOT NULL`, `at timestamptz NOT NULL`, `attempt int NOT NULL`, `answer_digest content_digest`, `event_sequence bigint NOT NULL`, `digest content_digest NOT NULL`; `UNIQUE (tenant_id, challenge_ref, event_sequence)`, `FOREIGN KEY (tenant_id, challenge_ref) REFERENCES contact_verification_challenge (tenant_id, row_id)`. `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY` on all three, `hcmnext_app` only. `contact_endpoint_revision`: role AGGREGATE, retention PERMANENT. `contact_verification_challenge`: role CONTROL, retention OPERATIONAL. `contact_challenge_event`: role LEDGER, retention PERMANENT, append-only.
  - **REFACTOR:** the older `endpoint.go`/`challenge.go` generation is not persisted by this entry; retiring it in the kernel is a separate, later cleanup.
  - **Refs:** `internal/domains/contact` (`revision.go`), `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-CRM-001` **[PHASE_3][SOL_HIGH] Add durable PostgreSQL tables for talent-pool and pool-membership revisions.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_CRM_001` in `internal/data/crmstore` (two tenant-scoped tables. talent_pool_revision (immutable revision, backs crm.TalentPoolRevision): row_id uuid, tenant_id uuid NOT NULL, pool_id uuid NOT NULL, revision cas_version NOT NULL, purpose text NOT NULL, criteria jsonb, source jsonb NOT NULL, consent jsonb NOT NULL, sco; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/crmstore/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CRM-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist governed talent-pool and membership revisions under tenant isolation`.
  - **TEST:** `TestTodo_PERSIST_CRM_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_CRM_001`; `FAULT=TestTodo_PERSIST_CRM_001_Fault`; `INTEGRATION=TestTodo_PERSIST_CRM_001_Integration`; `SECURITY=TestTodo_PERSIST_CRM_001_Security`; `RECOVERY=TestTodo_PERSIST_CRM_001_Recovery`; `MUTATION=TestTodo_PERSIST_CRM_001_Mutation`.
  - **RED:** a `talent_pool_membership_revision` references a `pool_ref` from a different tenant, a revision row is updated in place, or consent/scope fields are dropped by an update instead of carried into a new revision.
  - **GREEN:** two tenant-scoped tables. `talent_pool_revision` (immutable revision, backs `crm.TalentPoolRevision`): `row_id uuid`, `tenant_id uuid NOT NULL`, `pool_id uuid NOT NULL`, `revision cas_version NOT NULL`, `purpose text NOT NULL`, `criteria jsonb`, `source jsonb NOT NULL`, `consent jsonb NOT NULL`, `scope jsonb NOT NULL`, `effective_from/effective_to timestamptz`, `owner_ref uuid`, `removal_policy jsonb`; `UNIQUE (tenant_id, pool_id, revision)`. `talent_pool_membership_revision` (immutable revision, backs `crm.TalentPoolMembershipRevision`): `row_id`, `tenant_id`, `membership_id uuid NOT NULL`, `revision cas_version NOT NULL`, `pool_ref uuid NOT NULL`, `subject_ref uuid NOT NULL`, `role text NOT NULL`, `purpose text`, `source jsonb`, `consent jsonb NOT NULL`, `scope jsonb`, `effective_from/to timestamptz`, `owner_ref uuid`, `removal_policy jsonb`; `UNIQUE (tenant_id, membership_id, revision)`, `FOREIGN KEY (tenant_id, pool_ref) REFERENCES talent_pool_revision (tenant_id, pool_id)` enforced at the application layer since `pool_id` is not the table's surrogate key alone. `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY` on both, `hcmnext_app` only; role AGGREGATE, retention PERMANENT.
  - **REFACTOR:** consent and removal-policy stay structured `jsonb`, not free text, so a later consent-audit query does not need to parse prose.
  - **Refs:** `internal/domains/crm`, `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-CUSTOM-001` **[PHASE_4][SOL_HIGH] Add durable PostgreSQL tables for tenant-scoped custom object and relationship definitions and their records.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_CUSTOM_001` in `internal/data/customstore` (three tenant-scoped tables. custom_object_definition: row_id uuid, tenant_id uuid NOT NULL, kind text NOT NULL, namespace text NOT NULL, version bigint NOT NULL, fields jsonb NOT NULL; PRIMARY KEY (tenant_id, row_id), UNIQUE (tenant_id, kind, namespace, version). custom_relations; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/customstore/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CUSTOM-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=give the tenant-custom-types kernel a durable dynamic-schema backing store, adding tenant_id at the persistence boundary since none of its Go types carry one today`.
  - **TEST:** `TestTodo_PERSIST_CUSTOM_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_CUSTOM_001`; `FAULT=TestTodo_PERSIST_CUSTOM_001_Fault`; `INTEGRATION=TestTodo_PERSIST_CUSTOM_001_Integration`; `SECURITY=TestTodo_PERSIST_CUSTOM_001_Security`; `RECOVERY=TestTodo_PERSIST_CUSTOM_001_Recovery`; `MUTATION=TestTodo_PERSIST_CUSTOM_001_Mutation`.
  - **RED:** a `custom_record_revision` is written against a `(kind, namespace, definition_version)` that does not exist for that tenant, a field value violates the definition's declared classification/residency policy, a definition row is updated instead of superseded by a new `version`, or a record is readable across tenants.
  - **GREEN:** three tenant-scoped tables. `custom_object_definition`: `row_id uuid`, `tenant_id uuid NOT NULL`, `kind text NOT NULL`, `namespace text NOT NULL`, `version bigint NOT NULL`, `fields jsonb NOT NULL`; `PRIMARY KEY (tenant_id, row_id)`, `UNIQUE (tenant_id, kind, namespace, version)`. `custom_relationship_definition`: `row_id`, `tenant_id`, `name text NOT NULL`, `namespace text NOT NULL`, `version bigint NOT NULL`, `source_kind text NOT NULL`, `target_kind text NOT NULL`, `cardinality text NOT NULL`, `effective_date_rule text`, `allow_cycles boolean NOT NULL DEFAULT false`; `UNIQUE (tenant_id, name, namespace, version)`. `custom_record_revision` (append-only per effective interval, backs `custom.CustomRecordRevision`): `row_id`, `tenant_id`, `object_id text NOT NULL`, `object_kind text NOT NULL`, `namespace text NOT NULL`, `definition_version bigint NOT NULL`, `effective_from/effective_to timestamptz`, `recorded_at timestamptz NOT NULL DEFAULT now()`, `known_at timestamptz NOT NULL`, `field_values jsonb NOT NULL`; `FOREIGN KEY (tenant_id, object_kind, namespace, definition_version) REFERENCES custom_object_definition (tenant_id, kind, namespace, version)`. `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY` on all three, `hcmnext_app` only. `custom_object_definition`/`custom_relationship_definition`: role REGISTRY, retention PERMANENT. `custom_record_revision`: role AGGREGATE, retention PERMANENT.
  - **REFACTOR:** per-field classification/residency/retention enforcement (`FieldPolicy`/`PolicyProjection`) stays a kernel-side authorization decision made before a write reaches this table, never a database trigger.
  - **Refs:** `internal/domains/custom`, `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-PLANNING-001` **[PHASE_3][SOL_HIGH] Add durable PostgreSQL tables for workforce demand signals, coverage requirements and scenario revisions.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_PLANNING_001` in `internal/data/planningstore`, `internal/domains/demand` (three tenant-scoped tables. demand_signal (backs demand.DemandSignal): row_id uuid, tenant_id uuid NOT NULL, signal_id text NOT NULL, work_effective_from/work_effective_to timestamptz, location text, org_unit text, quantity numeric(9,2) NOT NULL, unit text NOT NULL, skill text, r; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/planningstore/ ./internal/domains/demand/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DEMAND-001`, `SCENARIO-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist descriptive workforce-planning input (demand signals, coverage requirements, hypothetical scenario revisions) under tenant isolation, none of which grants position, assignment or budget authority`.
  - **TEST:** `TestTodo_PERSIST_PLANNING_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_PLANNING_001`; `FAULT=TestTodo_PERSIST_PLANNING_001_Fault`; `INTEGRATION=TestTodo_PERSIST_PLANNING_001_Integration`; `SECURITY=TestTodo_PERSIST_PLANNING_001_Security`; `RECOVERY=TestTodo_PERSIST_PLANNING_001_Recovery`; `MUTATION=TestTodo_PERSIST_PLANNING_001_Mutation`.
  - **RED:** a `demand_signal`/`coverage_requirement`/`scenario_revision` row is read or joined across a tenant boundary, a `scenario_revision.parent_revision` points at a revision of a different `scenario_id`, or any row is treated by a caller as authority to reserve a position or budget (it is descriptive input only, enforced at the read-port boundary, not by this table).
  - **GREEN:** three tenant-scoped tables. `demand_signal` (backs `demand.DemandSignal`): `row_id uuid`, `tenant_id uuid NOT NULL`, `signal_id text NOT NULL`, `work_effective_from/work_effective_to timestamptz`, `location text`, `org_unit text`, `quantity numeric(9,2) NOT NULL`, `unit text NOT NULL`, `skill text`, `role text`, `priority text`, `source text`, `source_ref text`, `confidence numeric(5,4)`, `confidence_class text`, `scenario_ref text`, `version text NOT NULL`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, signal_id, version)`. `coverage_requirement` (backs `demand.CoverageRequirement`): `row_id`, `tenant_id`, `requirement_id text NOT NULL`, `scenario_ref text`, `version text NOT NULL`, `signal_refs jsonb`, `supply_refs jsonb`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, requirement_id, version)`. `scenario_revision` (immutable revision, backs `scenario.ScenarioRevision`): `row_id`, `tenant_id`, `scenario_id text NOT NULL`, `revision bigint NOT NULL`, `parent_revision bigint`, `parent_digest content_digest`, `owner text`, `scope text`, `horizon_from/horizon_to timestamptz`, `assumptions jsonb`, `baseline_snapshot_ref text`, `author text`, `authority_disclaimer text NOT NULL`, `lifecycle text NOT NULL`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, scenario_id, revision)`. `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY` on all three, `hcmnext_app` only; role AGGREGATE, retention PERMANENT.
  - **REFACTOR:** `authority_disclaimer` is a required, non-empty column precisely because a scenario row must never be mistaken for authoritative state by a downstream reader.
  - **Refs:** `internal/domains/demand`, `internal/domains/scenario`, `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-EMPLOYEERELATIONS-001` **[PHASE_4][SOL_HIGH] Add durable PostgreSQL tables for the six compartmentalized employee-relations revision families.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_EMPLOYEERELATIONS_001` in `internal/data/employeerelationsstore` (six tenant-scoped tables sharing one lineage envelope: row_id uuid, tenant_id uuid NOT NULL, case_ref uuid NOT NULL, compartment_ref uuid NOT NULL, revision bigint NOT NULL, parent_revision bigint, parent_digest content_digest, participants jsonb NOT NULL, canonical_digest conten; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/employeerelationsstore/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ER-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist allegation, investigation, interview, finding, discipline and grievance revisions with their append-only lineage and participant compartmentalization intact`.
  - **TEST:** `TestTodo_PERSIST_EMPLOYEERELATIONS_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_EMPLOYEERELATIONS_001`; `FAULT=TestTodo_PERSIST_EMPLOYEERELATIONS_001_Fault`; `INTEGRATION=TestTodo_PERSIST_EMPLOYEERELATIONS_001_Integration`; `SECURITY=TestTodo_PERSIST_EMPLOYEERELATIONS_001_Security`; `RECOVERY=TestTodo_PERSIST_EMPLOYEERELATIONS_001_Recovery`; `MUTATION=TestTodo_PERSIST_EMPLOYEERELATIONS_001_Mutation`.
  - **RED:** a row is updated instead of appended as a new `revision` with `parent_revision`/`parent_digest` set, a principal not named in `participants` reads a row for a `compartment_ref` they are not part of, or a `discipline_revision`/`grievance_revision` row references a `finding_ref`/`decision_ref` from a different `case_ref`.
  - **GREEN:** six tenant-scoped tables sharing one lineage envelope: `row_id uuid`, `tenant_id uuid NOT NULL`, `case_ref uuid NOT NULL`, `compartment_ref uuid NOT NULL`, `revision bigint NOT NULL`, `parent_revision bigint`, `parent_digest content_digest`, `participants jsonb NOT NULL`, `canonical_digest content_digest NOT NULL`, plus per-table columns. `er_allegation_revision`: `+ reporter_ref uuid, subject_ref uuid, summary text NOT NULL, status text NOT NULL, retaliation_safeguard boolean NOT NULL DEFAULT true`. `er_investigation_revision`: `+ allegation_ref uuid, investigator_ref uuid, authority_ref uuid, purpose text, scope text, status text NOT NULL`. `er_interview_revision`: `+ investigation_ref uuid, interviewer_ref uuid, interviewee_ref uuid, statement text, status text NOT NULL`. `er_finding_revision`: `+ investigation_ref uuid, investigator_ref uuid, subject_ref uuid, reporter_ref uuid, evidence_standard text, evidence_refs jsonb, disposition text NOT NULL, rationale text`. `er_discipline_revision`: `+ finding_ref uuid, subject_ref uuid, action text NOT NULL, legal_review_ref uuid, representation_review_ref uuid, status text NOT NULL`. `er_grievance_revision`: `+ decision_ref uuid, grievant_ref uuid, grounds text, outcome text, status text NOT NULL`. Each: `UNIQUE (tenant_id, case_ref, revision)` scoped per table's own identity column (e.g. an `allegation_id` surrogate distinct from `row_id` where a case holds more than one allegation). All six: `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY`, `hcmnext_app` only; role LEDGER, retention PERMANENT, `forbid_mutation` (each revision chain is itself the append-only fact log). A compartment-scoped row-visibility policy beyond bare tenant isolation (only `participants` may read a given `compartment_ref`) is flagged as follow-up design work this table set does not yet implement.
  - **REFACTOR:** keep the six tables sharing one lineage envelope column-for-column so a shared Go repository type can serve all six without per-table special-casing.
  - **Refs:** `internal/domains/employeerelations`, `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-EQUITY-001` **[PHASE_4][SOL_HIGH] Add durable PostgreSQL tables for equity plan, grant and acceptance-evidence revisions.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_EQUITY_001` in `internal/data/equitystore`, `internal/domains/equity` (three tenant-scoped tables. equity_plan_revision: row_id uuid, tenant_id uuid NOT NULL, plan_id text NOT NULL, revision bigint NOT NULL, name text NOT NULL, pool_ref text, authorized_quantity numeric(19,4), currency text, instrument_kinds jsonb, approval_ref text, parent_digest c; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/equitystore/ ./internal/domains/equity/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `EQUITY-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist the equity kernel's immutable plan and grant revisions and acceptance-evidence log, none of which the kernel itself is permitted to write`.
  - **TEST:** `TestTodo_PERSIST_EQUITY_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_EQUITY_001`; `FAULT=TestTodo_PERSIST_EQUITY_001_Fault`; `INTEGRATION=TestTodo_PERSIST_EQUITY_001_Integration`; `SECURITY=TestTodo_PERSIST_EQUITY_001_Security`; `RECOVERY=TestTodo_PERSIST_EQUITY_001_Recovery`; `MUTATION=TestTodo_PERSIST_EQUITY_001_Mutation`.
  - **RED:** an `equity_grant` references a `plan_digest` that does not match the named `plan_revision`'s stored digest, an `equity_acceptance_event` row is updated or deleted, two acceptance events for one grant share an `event_sequence`, or a row is readable across tenants.
  - **GREEN:** three tenant-scoped tables. `equity_plan_revision`: `row_id uuid`, `tenant_id uuid NOT NULL`, `plan_id text NOT NULL`, `revision bigint NOT NULL`, `name text NOT NULL`, `pool_ref text`, `authorized_quantity numeric(19,4)`, `currency text`, `instrument_kinds jsonb`, `approval_ref text`, `parent_digest content_digest`, `supersedes_revision bigint`, `canonical_digest content_digest NOT NULL`, `digest content_digest NOT NULL`; `UNIQUE (tenant_id, plan_id, revision)`. `equity_grant`: `row_id`, `tenant_id`, `grant_id text NOT NULL`, `revision bigint NOT NULL`, `plan_digest content_digest NOT NULL`, `plan_revision bigint NOT NULL`, `pool_ref text`, `worker_ref uuid NOT NULL`, `instrument_kind text NOT NULL`, `quantity numeric(19,4) NOT NULL`, `grant_date date`, `strike_price numeric(19,4)`, `currency text`, `vesting jsonb`, `state text NOT NULL`, `acceptance_digest content_digest`, `approval_ref text`, `evidence_ref text`, `parent_digest content_digest`, `supersedes_revision bigint`, `canonical_digest content_digest NOT NULL`, `digest content_digest NOT NULL`; `UNIQUE (tenant_id, grant_id, revision)`, `FOREIGN KEY (tenant_id, plan_id, plan_revision) REFERENCES equity_plan_revision (tenant_id, plan_id, revision)`. `equity_acceptance_event` (append-only, `forbid_mutation`): `row_id`, `tenant_id`, `event_id text NOT NULL`, `grant_digest content_digest NOT NULL`, `grant_revision bigint NOT NULL`, `accepted_by uuid NOT NULL`, `accepted_at timestamptz NOT NULL`, `evidence_ref text`, `canonical_digest content_digest NOT NULL`, `digest content_digest NOT NULL`, `event_sequence bigint NOT NULL`; `UNIQUE (tenant_id, grant_digest, event_sequence)`. `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY` on all three, `hcmnext_app` only. Revision tables: role AGGREGATE, retention PERMANENT. `equity_acceptance_event`: role LEDGER, retention PERMANENT, append-only.
  - **REFACTOR:** allocation, broker calls and document generation stay outside this table set, matching the kernel's own stated boundary.
  - **Refs:** `internal/domains/equity`, `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-FX-001` **[PHASE_3][SOL_HIGH] Add durable PostgreSQL tables for FX rate-source, quote and conversion-profile revisions.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_FX_001` in `internal/data/fxstore`, `internal/domains/fx` (three tenant-scoped tables. fx_rate_source_revision: row_id uuid, tenant_id uuid NOT NULL, source_id text NOT NULL, revision bigint NOT NULL, parent_revision bigint, parent_digest content_digest, effective_from/effective_to timestamptz, canonical_digest content_digest NOT NULL; U; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/fxstore/ ./internal/domains/fx/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `FX-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist the governed exchange-rate vocabulary payroll and rewards consume under tenant isolation`.
  - **TEST:** `TestTodo_PERSIST_FX_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_FX_001`; `FAULT=TestTodo_PERSIST_FX_001_Fault`; `INTEGRATION=TestTodo_PERSIST_FX_001_Integration`; `SECURITY=TestTodo_PERSIST_FX_001_Security`; `RECOVERY=TestTodo_PERSIST_FX_001_Recovery`; `MUTATION=TestTodo_PERSIST_FX_001_Mutation`.
  - **RED:** an `fx_quote_revision` references a `source_id`/`source_revision` pair absent from `fx_rate_source_revision`, a revision row is updated in place, or a row is readable across tenants.
  - **GREEN:** three tenant-scoped tables. `fx_rate_source_revision`: `row_id uuid`, `tenant_id uuid NOT NULL`, `source_id text NOT NULL`, `revision bigint NOT NULL`, `parent_revision bigint`, `parent_digest content_digest`, `effective_from/effective_to timestamptz`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, source_id, revision)`. `fx_quote_revision`: `row_id`, `tenant_id`, `quote_id text NOT NULL`, `source_id text NOT NULL`, `source_revision bigint NOT NULL`, `as_of/effective_at/observed_at/known_at timestamptz`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, quote_id)`, `FOREIGN KEY (tenant_id, source_id, source_revision) REFERENCES fx_rate_source_revision (tenant_id, source_id, revision)`. `fx_conversion_profile_revision`: `row_id`, `tenant_id`, `profile_id text NOT NULL`, `revision bigint NOT NULL`, `parent_revision bigint`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, profile_id, revision)`. `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY` on all three, `hcmnext_app` only; role AGGREGATE, retention PERMANENT.
  - **REFACTOR:** rate-source and quote stay two tables, not one, so a source's identity is stable while its quotes accumulate without limit.
  - **Refs:** `internal/domains/fx`, `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-HRCASE-001` **[PHASE_3][SOL_HIGH] Add a durable, append-only PostgreSQL table for the HR case revision stream.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_HRCASE_001` in `internal/data/hrcasestore` (one tenant-scoped table. hr_case_revision (append-only, forbid_mutation, backs hrcase.CaseRevision/CaseAggregate): row_id uuid, tenant_id uuid NOT NULL, case_id text NOT NULL, revision bigint NOT NULL, definition jsonb NOT NULL, requester text, subject text, purpose text, classif; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/hrcasestore/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CASE-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist the HR case identity and lifecycle-state stream as the append-only sequence the kernel's CaseAggregate already models in memory`.
  - **TEST:** `TestTodo_PERSIST_HRCASE_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_HRCASE_001`; `FAULT=TestTodo_PERSIST_HRCASE_001_Fault`; `INTEGRATION=TestTodo_PERSIST_HRCASE_001_Integration`; `SECURITY=TestTodo_PERSIST_HRCASE_001_Security`; `RECOVERY=TestTodo_PERSIST_HRCASE_001_Recovery`; `MUTATION=TestTodo_PERSIST_HRCASE_001_Mutation`.
  - **RED:** a `hr_case_revision` row is updated or deleted, two revisions for one case share an `event_sequence`, `previous_revision` does not name the immediately prior sequence for that case, or a row is readable across tenants.
  - **GREEN:** one tenant-scoped table. `hr_case_revision` (append-only, `forbid_mutation`, backs `hrcase.CaseRevision`/`CaseAggregate`): `row_id uuid`, `tenant_id uuid NOT NULL`, `case_id text NOT NULL`, `revision bigint NOT NULL`, `definition jsonb NOT NULL`, `requester text`, `subject text`, `purpose text`, `classification text`, `retention text`, `participants jsonb NOT NULL`, `state text NOT NULL`, `previous_revision bigint`, `disposition text`, `digest content_digest NOT NULL`, `event_sequence bigint NOT NULL`; `PRIMARY KEY (tenant_id, row_id)`, `UNIQUE (tenant_id, case_id, event_sequence)`. `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY`, `hcmnext_app` only; role LEDGER, retention PERMANENT, `rebuild_source` none.
  - **REFACTOR:** "current state" is read as the row with the highest `event_sequence` for a `case_id`, matching the `issuer_profile`/`issuer_state_event` convention, never a mutable status column.
  - **Refs:** `internal/domains/hrcase`, `migrations/00038_issuer_registry.sql` (the append-only convention this table follows), `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-INCENTIVE-001` **[PHASE_4][SOL_HIGH] Add durable PostgreSQL tables for incentive plan revisions, attainment observations and award calculations.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_INCENTIVE_001` in `internal/data/incentivestore` (three tenant-scoped tables. incentive_plan_revision: row_id uuid, tenant_id uuid NOT NULL, plan_id text NOT NULL, revision bigint NOT NULL, supersedes_revision bigint, parent_digest content_digest, canonical_digest content_digest NOT NULL, digest content_digest NOT NULL; UNIQUE (; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/incentivestore/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `INCENTIVE-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist incentive-plan lineage, attainment evidence and award-calculation state, none of which the kernel is permitted to approve or pay`.
  - **TEST:** `TestTodo_PERSIST_INCENTIVE_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_INCENTIVE_001`; `FAULT=TestTodo_PERSIST_INCENTIVE_001_Fault`; `INTEGRATION=TestTodo_PERSIST_INCENTIVE_001_Integration`; `SECURITY=TestTodo_PERSIST_INCENTIVE_001_Security`; `RECOVERY=TestTodo_PERSIST_INCENTIVE_001_Recovery`; `MUTATION=TestTodo_PERSIST_INCENTIVE_001_Mutation`.
  - **RED:** an `attainment_observation` row is updated or deleted, two observations for one worker/plan share an `event_sequence`, an `award_calculation` names a `plan_digest` inconsistent with the plan revision it cites, or a row is readable across tenants.
  - **GREEN:** three tenant-scoped tables. `incentive_plan_revision`: `row_id uuid`, `tenant_id uuid NOT NULL`, `plan_id text NOT NULL`, `revision bigint NOT NULL`, `supersedes_revision bigint`, `parent_digest content_digest`, `canonical_digest content_digest NOT NULL`, `digest content_digest NOT NULL`; `UNIQUE (tenant_id, plan_id, revision)`. `attainment_observation` (append-only, `forbid_mutation`): `row_id`, `tenant_id`, `observation_id text NOT NULL`, `plan_id text NOT NULL`, `plan_revision bigint NOT NULL`, `worker_ref uuid NOT NULL`, `value numeric(19,4) NOT NULL`, `as_of date NOT NULL`, `known_at timestamptz NOT NULL`, `source_ref text`, `digest content_digest NOT NULL`, `event_sequence bigint NOT NULL`; `UNIQUE (tenant_id, worker_ref, plan_id, event_sequence)`. `award_calculation`: `row_id`, `tenant_id`, `calculation_id text NOT NULL`, `worker_ref uuid NOT NULL`, `plan_digest content_digest NOT NULL`, `plan_revision bigint NOT NULL`, `state text NOT NULL`, `revision bigint NOT NULL`, `supersedes_revision bigint`, `canonical_digest content_digest NOT NULL`, `digest content_digest NOT NULL`; `UNIQUE (tenant_id, calculation_id, revision)`. `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY` on all three, `hcmnext_app` only. `incentive_plan_revision`/`award_calculation`: role AGGREGATE, retention PERMANENT. `attainment_observation`: role LEDGER, retention PERMANENT, append-only.
  - **REFACTOR:** payment authority stays outside this table set; an `award_calculation.state` of e.g. `CALCULATED` is not itself a payment instruction.
  - **Refs:** `internal/domains/incentive`, `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-CONTENTREGISTRY-001` **[PHASE_3][SOL_HIGH] Add durable PostgreSQL tables for the industry-pack manifest catalogue, per-tenant bindings, and versioned knowledge articles.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_CONTENTREGISTRY_001` in `internal/data/contentregistrystore` (seven tables. Two platform catalogs carry no tenant_id, following the jurisdiction precedent (cross-tenant reference data, no RLS, read-only grant to hcmnext_app): industry_pack_manifest: row_id uuid PRIMARY KEY, pack_id text NOT NULL, version int NOT NULL, parent_version int, pa; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/contentregistrystore/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PACK-001`, `KNOW-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist the industry-pack manifest/content platform catalogue, its per-tenant composition bindings, and the versioned, localized knowledge-article lifecycle`.
  - **TEST:** `TestTodo_PERSIST_CONTENTREGISTRY_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_CONTENTREGISTRY_001`; `FAULT=TestTodo_PERSIST_CONTENTREGISTRY_001_Fault`; `INTEGRATION=TestTodo_PERSIST_CONTENTREGISTRY_001_Integration`; `SECURITY=TestTodo_PERSIST_CONTENTREGISTRY_001_Security`; `RECOVERY=TestTodo_PERSIST_CONTENTREGISTRY_001_Recovery`; `MUTATION=TestTodo_PERSIST_CONTENTREGISTRY_001_Mutation`.
  - **RED:** an `industry_pack_binding` names a `pack_id`/`version` absent from `industry_pack_manifest`, a `knowledge_lifecycle_event` row is updated or deleted, an `activation_epoch` moves backward for the same article/locale, or any tenant-scoped row is readable across tenants.
  - **GREEN:** seven tables. Two platform catalogs carry no `tenant_id`, following the `jurisdiction` precedent (cross-tenant reference data, no RLS, read-only grant to `hcmnext_app`): `industry_pack_manifest`: `row_id uuid PRIMARY KEY`, `pack_id text NOT NULL`, `version int NOT NULL`, `parent_version int`, `parent_digest content_digest`, `canonical_digest content_digest NOT NULL`; `UNIQUE (pack_id, version)`. `content_registry_entry`: `row_id uuid PRIMARY KEY`, `ref text NOT NULL`, `effective_from/effective_to timestamptz`, `digest content_digest NOT NULL`, `dependencies jsonb`; `UNIQUE (ref)`. Five tenant-scoped tables carry `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY`, `hcmnext_app` only: `industry_pack_binding`: `row_id uuid`, `tenant_id uuid NOT NULL`, `packs jsonb NOT NULL`, `contents jsonb NOT NULL`, `canonical_digest content_digest NOT NULL`. `knowledge_article_revision`: `row_id`, `tenant_id`, `article_id text NOT NULL`, `revision bigint NOT NULL`, `locale text NOT NULL`, `audience_scope jsonb`, `classification text NOT NULL`, `owner text`, `source_authority text`, `source_refs jsonb`, `jurisdiction text`, `effective_from/effective_to timestamptz`, `known_from/known_to timestamptz`, `body_digest content_digest NOT NULL`, `title text NOT NULL`, `summary text`, `review jsonb`, `supersession jsonb`; `UNIQUE (tenant_id, article_id, revision)`. `knowledge_localized_revision`: `row_id`, `tenant_id`, `article_id text NOT NULL`, `revision bigint NOT NULL`, `locale text NOT NULL`, `reviewer text`, `source_revision_digest content_digest`, `body_digest content_digest NOT NULL`, `title text NOT NULL`, `summary text`, `created_at timestamptz NOT NULL`, `digest content_digest NOT NULL`; `UNIQUE (tenant_id, article_id, locale, revision)`. `knowledge_lifecycle_event` (append-only, `forbid_mutation`): `row_id`, `tenant_id`, `event_id text NOT NULL`, `kind text NOT NULL`, `previous_state text`, `state text NOT NULL`, `article_id text NOT NULL`, `revision bigint`, `locale text`, `reviewer text`, `bundle_id text`, `bundle_digest content_digest`, `receipt_digest content_digest`, `activation_epoch bigint`, `at timestamptz NOT NULL`, `digest content_digest NOT NULL`, `event_sequence bigint NOT NULL`; `UNIQUE (tenant_id, article_id, event_sequence)`. `knowledge_activation`: `row_id`, `tenant_id`, `article_id text NOT NULL`, `revision bigint NOT NULL`, `locale text NOT NULL`, `bundle_id text`, `bundle_digest content_digest`, `activation_epoch bigint NOT NULL`, `activated_at timestamptz NOT NULL`; `UNIQUE (tenant_id, article_id, locale)`. Disposition: `industry_pack_manifest`/`content_registry_entry`: role REGISTRY, retention PERMANENT, tenant column none. `industry_pack_binding`/`knowledge_article_revision`/`knowledge_localized_revision`/`knowledge_activation`: role AGGREGATE, retention PERMANENT. `knowledge_lifecycle_event`: role LEDGER, retention PERMANENT, append-only.
  - **REFACTOR:** industry-pack and knowledge stay one table set only because both are versioned content/config registries feeding the same product surface; split them the moment either grows a dependency the other does not share.
  - **Refs:** `internal/domains/industrypack`, `internal/domains/knowledge`, `migrations/00021_governance_authz_legal_evidence.sql` (the `jurisdiction` no-tenant-column precedent), `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-JOBARCH-001` **[PHASE_3][SOL_HIGH] Add durable PostgreSQL tables for job-family, level, grade, profile and architecture revisions.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_JOBARCH_001` in `internal/data/jobarchstore`, `internal/domains/jobarch` (internal/data/jobarchstore over migration 00048 persists job architecture versions with tenant RLS; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/jobarchstore/ ./internal/domains/jobarch/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `JOBARCH-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist the five job-architecture revision families and their lineage under tenant isolation`.
  - **TEST:** `TestTodo_PERSIST_JOBARCH_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_JOBARCH_001`; `FAULT=TestTodo_PERSIST_JOBARCH_001_Fault`; `INTEGRATION=TestTodo_PERSIST_JOBARCH_001_Integration`; `SECURITY=TestTodo_PERSIST_JOBARCH_001_Security`; `RECOVERY=TestTodo_PERSIST_JOBARCH_001_Recovery`; `MUTATION=TestTodo_PERSIST_JOBARCH_001_Mutation`.
  - **RED:** a `job_architecture_revision` names a family/level/grade/profile reference outside its own tenant, a revision row is updated in place instead of chained through `lineage`, or a row is readable across tenants.
  - **GREEN:** five tenant-scoped tables. Four share one envelope, backing `jobarch.JobFamilyRevision`/`JobLevelRevision`/`JobGradeRevision`/`JobProfileRevision`: `row_id uuid`, `tenant_id uuid NOT NULL`, `<x>_id text NOT NULL`, `revision text NOT NULL`, `parent_id text`, `lifecycle text NOT NULL`, `effective_from/effective_to timestamptz`, `known_from/known_to timestamptz`, `lineage jsonb NOT NULL`; `UNIQUE (tenant_id, <x>_id, revision)` for `job_family_revision`, `job_level_revision`, `job_grade_revision`, `job_profile_revision`. `job_architecture_revision` (the binding snapshot): `row_id`, `tenant_id`, `architecture_id text NOT NULL`, `revision text NOT NULL`, `supersedes_revision text`, `families/levels/grades/profiles jsonb NOT NULL` (each an array of `{id, revision}` refs resolved against the four tables above), `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, architecture_id, revision)`. `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY` on all five, `hcmnext_app` only; role AGGREGATE, retention PERMANENT, `rebuild_source` none.
  - **REFACTOR:** position compatibility checks stay a pure read over these five tables; no compatibility logic moves into a database function.
  - **Refs:** `internal/domains/jobarch`, `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-LOCATION-001` **[PHASE_3][SOL_HIGH] Add durable PostgreSQL tables for work-location, worksite and jurisdiction-table revisions.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_LOCATION_001` in `internal/data/locationstore` (internal/data/locationstore over migration 00049 persists locations with hex digests, uuid keys, nullable columns and a CAS on version (fix lane); written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/locationstore/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LOCATION-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist canonical postal addresses, effective-dated work locations and worksites, and the declared jurisdiction table they resolve against`.
  - **TEST:** `TestTodo_PERSIST_LOCATION_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_LOCATION_001`; `FAULT=TestTodo_PERSIST_LOCATION_001_Fault`; `INTEGRATION=TestTodo_PERSIST_LOCATION_001_Integration`; `SECURITY=TestTodo_PERSIST_LOCATION_001_Security`; `RECOVERY=TestTodo_PERSIST_LOCATION_001_Recovery`; `MUTATION=TestTodo_PERSIST_LOCATION_001_Mutation`.
  - **RED:** a `worksite_revision` names a `work_location_ref` from a different tenant, a revision row is updated in place, a jurisdiction lookup resolves against an unversioned or superseded `jurisdiction_table` row, or a row is readable across tenants.
  - **GREEN:** three tables. `work_location_revision` (tenant-scoped, immutable revision): `row_id uuid`, `tenant_id uuid NOT NULL`, `location_id text NOT NULL`, `revision bigint NOT NULL`, `parent_revision bigint`, `parent_digest content_digest`, `address jsonb NOT NULL`, `source_authority text`, `confidence text`, `effective_from/effective_to timestamptz`, `known_at timestamptz NOT NULL`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, location_id, revision)`. `worksite_revision` (tenant-scoped, immutable revision): `row_id`, `tenant_id`, `worksite_id text NOT NULL`, `revision bigint NOT NULL`, `parent_revision bigint`, `work_location_ref text NOT NULL`, `name text NOT NULL`, `address jsonb`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, worksite_id, revision)`, `FOREIGN KEY (tenant_id, work_location_ref) REFERENCES work_location_revision (tenant_id, location_id)` enforced at the application layer. `jurisdiction_table` (platform reference data, no `tenant_id`, following the `jurisdiction` precedent — no RLS, read-only grant): `row_id uuid PRIMARY KEY`, `table_id text NOT NULL`, `version text NOT NULL`, `rules jsonb NOT NULL`, `canonical_digest content_digest NOT NULL`; `UNIQUE (table_id, version)`. `work_location_revision`/`worksite_revision`: `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY`, `hcmnext_app` only; role AGGREGATE, retention PERMANENT. `jurisdiction_table`: role REGISTRY, retention PERMANENT, tenant column none.
  - **REFACTOR:** no geocoder call or tenant-default inference happens in this table set, matching the kernel's stated boundary; a location resolves only against `jurisdiction_table`.
  - **Refs:** `internal/domains/location`, `migrations/00021_governance_authz_legal_evidence.sql` (the `jurisdiction` no-tenant-column precedent), `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-MERIT-001` **[PHASE_4][SOL_HIGH] Add durable PostgreSQL tables for merit-cycle, recommendation and frozen-population-snapshot revisions.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_MERIT_001` in `internal/data/meritstore` (three tenant-scoped tables. merit_population_snapshot: row_id uuid, tenant_id uuid NOT NULL, snapshot_id text NOT NULL, revision bigint NOT NULL, members jsonb NOT NULL, watermark text, frozen boolean NOT NULL DEFAULT false, frozen_at timestamptz, canonical_digest content_digest ; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/meritstore/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MERIT-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist merit-cycle, per-participant recommendation and frozen-population-snapshot state, none of which writes compensation itself`.
  - **TEST:** `TestTodo_PERSIST_MERIT_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_MERIT_001`; `FAULT=TestTodo_PERSIST_MERIT_001_Fault`; `INTEGRATION=TestTodo_PERSIST_MERIT_001_Integration`; `SECURITY=TestTodo_PERSIST_MERIT_001_Security`; `RECOVERY=TestTodo_PERSIST_MERIT_001_Recovery`; `MUTATION=TestTodo_PERSIST_MERIT_001_Mutation`.
  - **RED:** a `merit_population_snapshot` marked `frozen` is updated, a `merit_recommendation` names a `cycle_revision` inconsistent with its cycle's current revision, or a row is readable across tenants.
  - **GREEN:** three tenant-scoped tables. `merit_population_snapshot`: `row_id uuid`, `tenant_id uuid NOT NULL`, `snapshot_id text NOT NULL`, `revision bigint NOT NULL`, `members jsonb NOT NULL`, `watermark text`, `frozen boolean NOT NULL DEFAULT false`, `frozen_at timestamptz`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, snapshot_id, revision)`. `merit_cycle_revision`: `row_id`, `tenant_id`, `cycle_id text NOT NULL`, `revision bigint NOT NULL`, `parent_revision bigint`, `parent_digest content_digest`, `population_ref uuid NOT NULL`, `guidelines jsonb NOT NULL`, `budget numeric(19,4) NOT NULL`, `state text NOT NULL`, `effective_at/known_at timestamptz`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, cycle_id, revision)`, `FOREIGN KEY (tenant_id, population_ref) REFERENCES merit_population_snapshot (tenant_id, row_id)`. `merit_recommendation`: `row_id`, `tenant_id`, `cycle_id text NOT NULL`, `cycle_revision bigint NOT NULL`, `participant_id text NOT NULL`, `base_pay numeric(19,4)`, `state text NOT NULL`, `adjustments jsonb`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, cycle_id, participant_id, cycle_revision)`. `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY` on all three, `hcmnext_app` only; role AGGREGATE, retention PERMANENT.
  - **REFACTOR:** the frozen-snapshot boundary (`frozen`/`frozen_at`) is enforced by application-level compare-and-set on write, not a database trigger, matching the kernel's own immutability contract.
  - **Refs:** `internal/domains/merit`, `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-MOBILITY-001` **[PHASE_4][SOL_HIGH] Add durable PostgreSQL tables for mobility-plan revisions and immigration milestones.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_MOBILITY_001` in `internal/data/mobilitystore` (two tenant-scoped tables. mobility_plan (immutable revision): row_id uuid, tenant_id uuid NOT NULL, mobility_id text NOT NULL, revision bigint NOT NULL, parent_revision bigint, parent_digest content_digest, worker_ref uuid NOT NULL, home_assignment jsonb, host_assignment jsonb, l; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/mobilitystore/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MOBILITY-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist cross-border mobility plans and their immigration-milestone log, none of which the kernel is permitted to call a provider or make an immigration decision from`.
  - **TEST:** `TestTodo_PERSIST_MOBILITY_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_MOBILITY_001`; `FAULT=TestTodo_PERSIST_MOBILITY_001_Fault`; `INTEGRATION=TestTodo_PERSIST_MOBILITY_001_Integration`; `SECURITY=TestTodo_PERSIST_MOBILITY_001_Security`; `RECOVERY=TestTodo_PERSIST_MOBILITY_001_Recovery`; `MUTATION=TestTodo_PERSIST_MOBILITY_001_Mutation`.
  - **RED:** a `mobility_immigration_milestone` row is updated or deleted, two milestones for one process share an `event_sequence`, a `mobility_plan` revision is updated in place instead of superseded, or a row is readable across tenants.
  - **GREEN:** two tenant-scoped tables. `mobility_plan` (immutable revision): `row_id uuid`, `tenant_id uuid NOT NULL`, `mobility_id text NOT NULL`, `revision bigint NOT NULL`, `parent_revision bigint`, `parent_digest content_digest`, `worker_ref uuid NOT NULL`, `home_assignment jsonb`, `host_assignment jsonb`, `legs jsonb`, `relocation jsonb`, `immigration jsonb`, `obligations jsonb`, `status text NOT NULL`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, mobility_id, revision)`. `mobility_immigration_milestone` (append-only milestone log, `forbid_mutation`): `row_id`, `tenant_id`, `milestone_id text NOT NULL`, `process_ref text NOT NULL`, `kind text NOT NULL`, `jurisdiction text`, `at date NOT NULL`, `source_ref text`, `evidence_ref text`, `expires_on date`, `canonical_digest content_digest NOT NULL`, `event_sequence bigint NOT NULL`; `UNIQUE (tenant_id, process_ref, event_sequence)`. `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY` on both, `hcmnext_app` only. `mobility_plan`: role AGGREGATE, retention PERMANENT. `mobility_immigration_milestone`: role LEDGER, retention PERMANENT, append-only.
  - **REFACTOR:** relocation-package and obligation detail stay `jsonb` inside `mobility_plan` for now; split into dedicated child tables only once a consumer needs to query across plans by relocation milestone.
  - **Refs:** `internal/domains/mobility`, `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-PAYGL-001` **[PHASE_3][SOL_HIGH] Add durable PostgreSQL tables for labor cost rules and payroll-to-GL accounting rules and mapping results.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_PAYGL_001` in `internal/data/payglstore`, `internal/domains/paygl` (internal/data/payglstore over migration 00047 persists labor cost rules and payroll-to-GL accounting rules with tenant RLS; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/payglstore/ ./internal/domains/paygl/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PAYGL-001`, `LABOR-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist labor-cost allocation rules and the payroll-to-GL accounting rules and mapping results that consume them, since paygl imports labor directly for its dimension vocabulary`.
  - **TEST:** `TestTodo_PERSIST_PAYGL_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_PAYGL_001`; `FAULT=TestTodo_PERSIST_PAYGL_001_Fault`; `INTEGRATION=TestTodo_PERSIST_PAYGL_001_Integration`; `SECURITY=TestTodo_PERSIST_PAYGL_001_Security`; `RECOVERY=TestTodo_PERSIST_PAYGL_001_Recovery`; `MUTATION=TestTodo_PERSIST_PAYGL_001_Mutation`.
  - **RED:** a `paygl_mapping_result` row is updated or deleted after being recorded, an `accounting_rule`/`labor_rule` row is updated in place instead of superseded by a new `version`, or a row is readable across tenants.
  - **GREEN:** three tenant-scoped tables. `labor_rule` (backs `labor.LaborRule`): `row_id uuid`, `tenant_id uuid NOT NULL`, `rule_id text NOT NULL`, `version text NOT NULL`, `effective_from/effective_to timestamptz`, `dimensions jsonb NOT NULL`, `base_rate numeric(19,6)`, `differential_rate numeric(19,6)`, `overtime_multiplier numeric(9,4)`, `employer_burden_rate numeric(9,6)`, `supersedes text`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, rule_id, version)`. `paygl_accounting_rule` (backs `paygl.AccountingRule`): `row_id`, `tenant_id`, `rule_id text NOT NULL`, `version text NOT NULL`, `effective_from/to timestamptz`, `component_kind text`, `component_code text`, `debit_account text NOT NULL`, `credit_account text NOT NULL`, `supersedes text`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, rule_id, version)`. `paygl_mapping_result` (append-only per run, `forbid_mutation`, backs `paygl.MappingResult`): `row_id`, `tenant_id`, `run_id text NOT NULL`, `run_revision bigint NOT NULL`, `run_digest content_digest`, `mappings jsonb NOT NULL`, `digest content_digest NOT NULL`, `event_sequence bigint NOT NULL`; `UNIQUE (tenant_id, run_id, event_sequence)`. `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY` on all three, `hcmnext_app` only. `labor_rule`/`paygl_accounting_rule`: role AGGREGATE, retention PERMANENT. `paygl_mapping_result`: role LEDGER, retention PERMANENT, append-only.
  - **REFACTOR:** labor and paygl stay one table set only because paygl directly imports labor's dimension/allocation vocabulary; split them if a third consumer of `labor_rule` appears that has no GL relationship.
  - **Refs:** `internal/domains/labor`, `internal/domains/paygl`, `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-PAYINPUT-001` **[PHASE_5][SOL_HIGH] Add durable PostgreSQL tables for earning/deduction definitions and worker input assignments.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_PAYINPUT_001` in `internal/data/payinputstore` (two tenant-scoped tables. payinput_definition (immutable revision, backs payinput.Definition): row_id uuid, tenant_id uuid NOT NULL, definition_id text NOT NULL, code text NOT NULL, kind text NOT NULL, taxability jsonb, calculation_basis jsonb, limits jsonb, effective_from/effect; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/payinputstore/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PAYINPUT-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist immutable, effective-dated earning and deduction definitions and their per-worker assignments under tenant isolation`.
  - **TEST:** `TestTodo_PERSIST_PAYINPUT_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_PAYINPUT_001`; `FAULT=TestTodo_PERSIST_PAYINPUT_001_Fault`; `INTEGRATION=TestTodo_PERSIST_PAYINPUT_001_Integration`; `SECURITY=TestTodo_PERSIST_PAYINPUT_001_Security`; `RECOVERY=TestTodo_PERSIST_PAYINPUT_001_Recovery`; `MUTATION=TestTodo_PERSIST_PAYINPUT_001_Mutation`.
  - **RED:** a `payinput_worker_assignment` references a `definition_ref`/revision that does not exist for that tenant, a definition row is updated in place instead of superseded, or a row is readable across tenants.
  - **GREEN:** two tenant-scoped tables. `payinput_definition` (immutable revision, backs `payinput.Definition`): `row_id uuid`, `tenant_id uuid NOT NULL`, `definition_id text NOT NULL`, `code text NOT NULL`, `kind text NOT NULL`, `taxability jsonb`, `calculation_basis jsonb`, `limits jsonb`, `effective_from/effective_to timestamptz`, `version text NOT NULL`, `revision bigint NOT NULL`, `state text NOT NULL`, `supersedes_digest content_digest`, `supersedes_revision bigint`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, definition_id, revision)`. `payinput_worker_assignment` (backs `payinput.WorkerAssignment`): `row_id`, `tenant_id`, `assignment_id text NOT NULL`, `worker_ref uuid NOT NULL`, `definition_ref text NOT NULL`, `definition_revision bigint NOT NULL`, `effective_from/effective_to timestamptz`, `amount numeric(19,4)`, `rate numeric(19,6)`, `recurrence text`, `canonical_digest content_digest NOT NULL`; `FOREIGN KEY (tenant_id, definition_ref, definition_revision) REFERENCES payinput_definition (tenant_id, definition_id, revision)`. `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY` on both, `hcmnext_app` only; role AGGREGATE, retention PERMANENT.
  - **REFACTOR:** payroll calculation itself stays out of this table set; these two tables are inputs, not calculated output.
  - **Refs:** `internal/domains/payinput`, `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-PAYMETHOD-001` **[PHASE_4][SOL_HIGH] Add durable PostgreSQL tables for payment destinations, bank-detail-change controls and settlement payment instructions.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_PAYMETHOD_001` in `internal/data/paymethodstore` (seven tenant-scoped tables. paymethod*destination (immutable revision, backs paymethod.Destination): row_id uuid, tenant_id uuid NOT NULL, destination_id text NOT NULL, worker_ref uuid NOT NULL, rail text NOT NULL, risk_class text, governed_ref/provider_ref/bank_detail_ref/token*; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/paymethodstore/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PAYMETHOD-001`, `SETTLE-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist the payment-destination, verification, bank-detail-change and settlement-lifecycle vocabulary sharing one payment rail, none of which is permitted to hold raw bank details`.
  - **TEST:** `TestTodo_PERSIST_PAYMETHOD_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_PAYMETHOD_001`; `FAULT=TestTodo_PERSIST_PAYMETHOD_001_Fault`; `INTEGRATION=TestTodo_PERSIST_PAYMETHOD_001_Integration`; `SECURITY=TestTodo_PERSIST_PAYMETHOD_001_Security`; `RECOVERY=TestTodo_PERSIST_PAYMETHOD_001_Recovery`; `MUTATION=TestTodo_PERSIST_PAYMETHOD_001_Mutation`.
  - **RED:** a `paymethod_protected_account` row stores a raw account number instead of an `opaque_reference`/`value_digest`, a `paymethod_bank_detail_change` is confirmed before its `cooling_off` interval elapses, two `paymethod_verification_event`/`paymethod_change_confirmation` rows for one destination/change share an `event_sequence`, a `settlement_payment_instruction` is updated in place instead of superseded, or a row is readable across tenants.
  - **GREEN:** seven tenant-scoped tables. `paymethod_destination` (immutable revision, backs `paymethod.Destination`): `row_id uuid`, `tenant_id uuid NOT NULL`, `destination_id text NOT NULL`, `worker_ref uuid NOT NULL`, `rail text NOT NULL`, `risk_class text`, `governed_ref/provider_ref/bank_detail_ref/token_ref text`, `verification_state text NOT NULL`, `effective_from/effective_to timestamptz`, `state text NOT NULL`, `revision bigint NOT NULL`, `supersedes_revision bigint`, `supersedes_digest content_digest`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, destination_id, revision)`. `paymethod_bank_detail_change` (CAS-fenced via `revision`): `row_id`, `tenant_id`, `change_id text NOT NULL`, `destination_id text NOT NULL`, `worker_ref uuid NOT NULL`, `before_digest/after_digest content_digest`, `requested_by text`, `approver text`, `requested_at/confirmed_at/available_at timestamptz`, `cooling_off interval NOT NULL`, `status text NOT NULL`, `revision bigint NOT NULL`, `supersedes_revision bigint`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, change_id, revision)`. `paymethod_verification_event` (append-only, `forbid_mutation`): `row_id`, `tenant_id`, `challenge_id text NOT NULL`, `destination_id text NOT NULL`, `method text NOT NULL`, `attempt int NOT NULL`, `verified_at timestamptz`, `evidence_digest content_digest`, `canonical_digest content_digest NOT NULL`, `event_sequence bigint NOT NULL`; `UNIQUE (tenant_id, destination_id, event_sequence)`. `paymethod_change_confirmation` (append-only, `forbid_mutation`): `row_id`, `tenant_id`, `change_digest content_digest NOT NULL`, `destination_id text NOT NULL`, `dispatched_at timestamptz NOT NULL`, `event_sequence bigint NOT NULL`; `UNIQUE (tenant_id, destination_id, event_sequence)`. `paymethod_protected_account` (backs `paymethod.ProtectedAccount`, holds no raw account data): `row_id`, `tenant_id`, `account_id text NOT NULL`, `mode text NOT NULL`, `opaque_reference text`, `value_digest content_digest`; `PRIMARY KEY (tenant_id, row_id)`. `paymethod_account_validation` (backs `AccountValidationRecord`): `row_id`, `tenant_id`, `account_id text NOT NULL`, `method text NOT NULL`, `validated_at timestamptz NOT NULL`, `result text NOT NULL`, `revision bigint NOT NULL`, `supersedes_digest content_digest`; `UNIQUE (tenant_id, account_id, revision)`. `settlement_payment_instruction` (immutable revision, backs `settlement.PaymentInstruction`): `row_id`, `tenant_id`, `instruction_id text NOT NULL`, `payroll_run_ref text`, `payee_ref uuid NOT NULL`, `amount numeric(19,4) NOT NULL`, `currency text NOT NULL`, `funding_source_ref text`, `rail text NOT NULL`, `bank_detail_ref text`, `schedule_ref text`, `value_date date`, `state text NOT NULL`, `revision bigint NOT NULL`, `supersedes_revision bigint`, `evidence_ref text`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, instruction_id, revision)`. `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY` on all seven, `hcmnext_app` only. Revision/mutable-state tables: role AGGREGATE (or CONTROL for `paymethod_bank_detail_change`), retention PERMANENT (OPERATIONAL for the change-control row). Event tables: role LEDGER, retention PERMANENT, append-only.
  - **REFACTOR:** paymethod and settlement stay one table set only because settlement's `Rail` type is `paymethod`'s own `SettlementRail` and a payment instruction always resolves to a `paymethod_destination`; split them if settlement ever gains a rail-independent instruction shape.
  - **Refs:** `internal/domains/paymethod`, `internal/domains/settlement`, `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-PAYROLL-001` **[PHASE_3][SOL_HIGH] Add durable PostgreSQL tables for payroll-run and frozen-population lifecycle state.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_PAYROLL_001` in `internal/data/payrollstore`, `internal/domains/payroll` (internal/data/payrollstore over migration 00046 persists payroll-run and frozen-population lifecycle state with tenant RLS; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/payrollstore/ ./internal/domains/payroll/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PAYRUN-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist the payroll-run and frozen-population lifecycle the calculation kernel conforms to, excluding payroll/auditpack which is already ledger-backed under DB-024`.
  - **TEST:** `TestTodo_PERSIST_PAYROLL_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_PAYROLL_001`; `FAULT=TestTodo_PERSIST_PAYROLL_001_Fault`; `INTEGRATION=TestTodo_PERSIST_PAYROLL_001_Integration`; `SECURITY=TestTodo_PERSIST_PAYROLL_001_Security`; `RECOVERY=TestTodo_PERSIST_PAYROLL_001_Recovery`; `MUTATION=TestTodo_PERSIST_PAYROLL_001_Mutation`.
  - **RED:** a `payroll_frozen_population` row marked frozen is amended by update instead of a new `payroll_population_amendment` row, two amendments for one run share an `event_sequence`, a `payroll_run` row is updated in place instead of superseded, or a row is readable across tenants.
  - **GREEN:** three tenant-scoped tables. `payroll_run` (immutable revision): `row_id uuid`, `tenant_id uuid NOT NULL`, `run_id text NOT NULL`, `revision bigint NOT NULL`, `state text NOT NULL`, `pay_group_ref text NOT NULL`, `period jsonb NOT NULL`, `population_binding_ref text`, `calculation_input_digest/calculation_digest/release_digest/reversal_digest content_digest`, `supersedes_revision bigint`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, run_id, revision)`. `payroll_frozen_population`: `row_id`, `tenant_id`, `run_id text NOT NULL`, `run_revision bigint NOT NULL`, `pay_group_ref text`, `binding jsonb`, `as_of timestamptz NOT NULL`, `members jsonb NOT NULL`, `revision bigint NOT NULL`, `state text NOT NULL`, `supersedes_digest/amendment_digest content_digest`, `digest content_digest NOT NULL`; `UNIQUE (tenant_id, run_id, revision)`, `FOREIGN KEY (tenant_id, run_id, run_revision) REFERENCES payroll_run (tenant_id, run_id, revision)`. `payroll_population_amendment` (append-only, `forbid_mutation`): `row_id`, `tenant_id`, `run_id text NOT NULL`, `kind text NOT NULL`, `member jsonb NOT NULL`, `reason text`, `effective_as_of timestamptz`, `digest content_digest NOT NULL`, `event_sequence bigint NOT NULL`; `UNIQUE (tenant_id, run_id, event_sequence)`. `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY` on all three, `hcmnext_app` only. `payroll_run`/`payroll_frozen_population`: role AGGREGATE, retention PERMANENT. `payroll_population_amendment`: role LEDGER, retention PERMANENT, append-only.
  - **REFACTOR:** `payroll/auditpack` keeps its existing ledger-backed reconciliation path (DB-024); this table set never duplicates checkpoint/evidence data it already owns.
  - **Refs:** `internal/domains/payroll`, `internal/domains/payroll/auditpack` (already-covered boundary), `migrations/00024_intent_control.sql` header (DB-024 boundary note), `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-PERFORMANCE-001` **[PHASE_3][SOL_HIGH] Add durable PostgreSQL tables for performance-cycle, rating, calibration, review and outcome-link state.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_PERFORMANCE_001` in `internal/data/performancestore`, `internal/domains/performance` (seven tenant-scoped tables. performance_cycle: row_id uuid, tenant_id uuid NOT NULL, cycle_id text NOT NULL, revision bigint NOT NULL, state text NOT NULL, supersedes_revision bigint, canonical_digest content_digest NOT NULL; UNIQUE (tenant_id, cycle_id, revision). performance_ra; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/performancestore/ ./internal/domains/performance/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PERFORMANCE-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist the performance-cycle, rating-case, calibration-session, review and outcome-link vocabulary the kernel treats as immutable values`.
  - **TEST:** `TestTodo_PERSIST_PERFORMANCE_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_PERFORMANCE_001`; `FAULT=TestTodo_PERSIST_PERFORMANCE_001_Fault`; `INTEGRATION=TestTodo_PERSIST_PERFORMANCE_001_Integration`; `SECURITY=TestTodo_PERSIST_PERFORMANCE_001_Security`; `RECOVERY=TestTodo_PERSIST_PERFORMANCE_001_Recovery`; `MUTATION=TestTodo_PERSIST_PERFORMANCE_001_Mutation`.
  - **RED:** a `performance_rating_event` row is updated or deleted, two events for one case share an `event_sequence`, a `performance_final_rating` is written for a case not marked `finalized`, or a row is readable across tenants.
  - **GREEN:** seven tenant-scoped tables. `performance_cycle`: `row_id uuid`, `tenant_id uuid NOT NULL`, `cycle_id text NOT NULL`, `revision bigint NOT NULL`, `state text NOT NULL`, `supersedes_revision bigint`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, cycle_id, revision)`. `performance_rating_case`: `row_id`, `tenant_id`, `case_id text NOT NULL`, `participant_id text NOT NULL`, `finalized boolean NOT NULL DEFAULT false`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, case_id)`. `performance_rating_event` (append-only, `forbid_mutation`): `row_id`, `tenant_id`, `case_id text NOT NULL`, `kind text NOT NULL`, `actor_id text`, `at timestamptz NOT NULL`, `contest_digest/correction_digest content_digest`, `digest content_digest NOT NULL`, `event_sequence bigint NOT NULL`; `UNIQUE (tenant_id, case_id, event_sequence)`. `performance_final_rating`: `row_id`, `tenant_id`, `participant_id text NOT NULL`, `cycle_id text NOT NULL`, `final_rating numeric(5,2) NOT NULL`, `finalized_at timestamptz NOT NULL`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, participant_id, cycle_id)`. `performance_calibration_session`: `row_id`, `tenant_id`, `session_id text NOT NULL`, `cycle_id text NOT NULL`, `cycle_revision bigint NOT NULL`, `graph jsonb`, `adjustments jsonb`, `canonical_digest content_digest NOT NULL`. `performance_review`: `row_id`, `tenant_id`, `review_id text NOT NULL`, `reviewer_id text NOT NULL`, `participant_id text NOT NULL`, `cycle_id text NOT NULL`, `cycle_revision bigint NOT NULL`, `review_revision bigint NOT NULL`, `supersedes_review_revision bigint`, `submitted_at timestamptz`; `UNIQUE (tenant_id, review_id, review_revision)`. `performance_outcome_link`: `row_id`, `tenant_id`, `rating_ref uuid NOT NULL`, `rating_digest content_digest`, `rating_revision bigint`, `action text NOT NULL`, `effective_at timestamptz`, `digest content_digest NOT NULL`. `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY` on all seven, `hcmnext_app` only. `performance_rating_event`: role LEDGER, retention PERMANENT, append-only. The other six: role AGGREGATE, retention PERMANENT.
  - **REFACTOR:** a cycle stays an immutable revision end to end; no lifecycle operation in this table set updates a `performance_cycle` row in place.
  - **Refs:** `internal/domains/performance`, `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-POSITION-001` **[PHASE_3][SOL_HIGH] Add durable PostgreSQL tables for pre-commit position reservations and their transition log.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_POSITION_001` in `internal/data/positionstore`, `internal/domains/position` (internal/data/positionstore over migration 00043 persists positions and their occupancy with tenant RLS; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/positionstore/ ./internal/domains/position/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `POSITION-003`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist the pre-commit position-reservation state machine DB-009's position_occupancy table does not cover, since occupancy models only committed assignment, not a reservation held against a proposal`.
  - **TEST:** `TestTodo_PERSIST_POSITION_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_POSITION_001`; `FAULT=TestTodo_PERSIST_POSITION_001_Fault`; `INTEGRATION=TestTodo_PERSIST_POSITION_001_Integration`; `SECURITY=TestTodo_PERSIST_POSITION_001_Security`; `RECOVERY=TestTodo_PERSIST_POSITION_001_Recovery`; `MUTATION=TestTodo_PERSIST_POSITION_001_Mutation`.
  - **RED:** a `position_reservation` update carries a `fence` not exactly one greater than the row's current fence, two `position_reservation_event` rows for one reservation share a `sequence`, a reservation names a `position_revision_ref` from a different tenant, or a row is readable across tenants.
  - **GREEN:** two tenant-scoped tables, distinct from and holding no foreign key into DB-009's `position_occupancy` (committed occupancy is a different aggregate). `position_reservation` (mutable, CAS-fenced current-state row): `row_id uuid`, `tenant_id uuid NOT NULL`, `reservation_id text NOT NULL`, `request jsonb NOT NULL`, `position_revision_ref uuid`, `state text NOT NULL`, `fence bigint NOT NULL DEFAULT 0`, `created_at/updated_at timestamptz NOT NULL DEFAULT now()`; `PRIMARY KEY (tenant_id, row_id)`, `UNIQUE (tenant_id, reservation_id)`. `position_reservation_event` (append-only, `forbid_mutation`): `row_id`, `tenant_id`, `sequence bigint NOT NULL`, `reservation_id text NOT NULL`, `from_state text`, `to_state text NOT NULL`, `reason text`, `fence bigint NOT NULL`, `at timestamptz NOT NULL`; `UNIQUE (tenant_id, reservation_id, sequence)`, `FOREIGN KEY (tenant_id, reservation_id) REFERENCES position_reservation (tenant_id, reservation_id)`. `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY` on both, `hcmnext_app` only. `position_reservation`: role CONTROL, retention OPERATIONAL. `position_reservation_event`: role LEDGER, retention PERMANENT, append-only.
  - **REFACTOR:** the migration header states the boundary with `position_occupancy` explicitly: a reservation converts into occupancy only through the existing DB-009 write path, never by mutating this table.
  - **Refs:** `internal/domains/position`, `migrations/00012_organization_aggregates.sql` (the `position_occupancy` table this entry is distinct from), `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-IDENTITYPRIVACY-001` **[PHASE_4][SOL_HIGH] Add durable PostgreSQL tables for identity-proofing sessions, pseudonym escrow, and data-subject requests.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_IDENTITYPRIVACY_001` in `internal/data/identityprivacystore` (eight tenant-scoped tables. proofing_session (immutable revision, backs proofing.ProofingSession): row_id uuid, tenant_id uuid NOT NULL, session_id text NOT NULL, subject_ref uuid NOT NULL, purpose text, target_assurance text NOT NULL, evidence jsonb, verifier_principal text, out; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/identityprivacystore/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PROOF-001`, `ANON-002`, `PRIV-005`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist identity-proofing evidence, pseudonym identity/escrow/revelation evidence, and data-subject-request intake and trail under tenant isolation, three identity-and-privacy governance boundaries that share no aggregate today but do share a restricted-evidence handling model`.
  - **TEST:** `TestTodo_PERSIST_IDENTITYPRIVACY_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_IDENTITYPRIVACY_001`; `FAULT=TestTodo_PERSIST_IDENTITYPRIVACY_001_Fault`; `INTEGRATION=TestTodo_PERSIST_IDENTITYPRIVACY_001_Integration`; `SECURITY=TestTodo_PERSIST_IDENTITYPRIVACY_001_Security`; `RECOVERY=TestTodo_PERSIST_IDENTITYPRIVACY_001_Recovery`; `MUTATION=TestTodo_PERSIST_IDENTITYPRIVACY_001_Mutation`.
  - **RED:** a `pseudonym_escrow_record` is read without a matching `pseudonym_escrow_event` authorizing the release, a `pseudonym_evidence_record`/`data_subject_request_evidence`/`pseudonym_escrow_event` row is updated or deleted, two events for one key share an `event_sequence`, a `data_subject_request` deadline is computed from the wrong jurisdiction clock, or a row is readable across tenants.
  - **GREEN:** eight tenant-scoped tables. `proofing_session` (immutable revision, backs `proofing.ProofingSession`): `row_id uuid`, `tenant_id uuid NOT NULL`, `session_id text NOT NULL`, `subject_ref uuid NOT NULL`, `purpose text`, `target_assurance text NOT NULL`, `evidence jsonb`, `verifier_principal text`, `outcome text`, `expires_at timestamptz`, `revision bigint NOT NULL`, `supersedes_revision bigint`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, session_id, revision)`. `work_authorization_evidence` (immutable revision): `row_id`, `tenant_id`, `evidence_id text NOT NULL`, `subject_ref uuid NOT NULL`, `revision bigint NOT NULL`, `supersedes_revision bigint`, `document_class text`, `verification_method text`, `jurisdiction text`, `category text`, `valid_from/valid_until date`, `reverification_due date`, `evidence_digest content_digest`, `source_ref text`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, evidence_id, revision)`. `pseudonym_identity` (backs `pseudonym.Pseudonym`): `row_id`, `tenant_id`, `pseudonym_id text NOT NULL`, `value_digest content_digest NOT NULL`, `generation int NOT NULL`, `scope text`, `purpose text`; `UNIQUE (tenant_id, pseudonym_id, generation)`. `pseudonym_escrow_record` (custody-restricted, ciphertext-only): `row_id`, `tenant_id`, `pseudonym_id text NOT NULL`, `generation int NOT NULL`, `ciphertext bytea NOT NULL`, `escrow_custodian text NOT NULL`; `PRIMARY KEY (tenant_id, row_id)`, granted only to the custody-service role, never `hcmnext_app` directly. `pseudonym_evidence_record` (append-only, `forbid_mutation`): `row_id`, `tenant_id`, `pseudonym_id text NOT NULL`, `generation int NOT NULL`, `intake_content_digest content_digest`, `intake_at timestamptz NOT NULL`, `operation text NOT NULL`, `request_digest content_digest`, `event_sequence bigint NOT NULL`; `UNIQUE (tenant_id, pseudonym_id, event_sequence)`. `pseudonym_escrow_event` (append-only, `forbid_mutation`): `row_id`, `tenant_id`, `pseudonym_id text NOT NULL`, `operation text NOT NULL`, `requested_by text`, `escrow_custodian text`, `outcome text NOT NULL`, `digest content_digest NOT NULL`, `event_sequence bigint NOT NULL`; `UNIQUE (tenant_id, pseudonym_id, event_sequence)`. `data_subject_request`: `row_id`, `tenant_id`, `request_id text NOT NULL`, `kind text NOT NULL`, `claims jsonb NOT NULL`, `jurisdiction text NOT NULL`, `received_at timestamptz NOT NULL`, `deadline timestamptz NOT NULL`, `verification_state text NOT NULL`, `identity_evidence_ref text`, `identity_assurance text`, `verified_at timestamptz`, `duplicate_of text`, `evidence_id text`; `PRIMARY KEY (tenant_id, row_id)`, `UNIQUE (tenant_id, request_id)`. `data_subject_request_evidence` (append-only trail, `forbid_mutation`, backs `privacy/dsr.Evidence`): `row_id`, `tenant_id`, `request_digest content_digest NOT NULL`, `kind text NOT NULL`, `at timestamptz NOT NULL`, `detail jsonb`, `event_sequence bigint NOT NULL`; `UNIQUE (tenant_id, request_digest, event_sequence)`. `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY` on all eight, `hcmnext_app` only, plus the tighter custody-role grant noted on `pseudonym_escrow_record`. Revision/current-state tables: role AGGREGATE, retention PERMANENT (`data_subject_request`: role CONTROL, retention OPERATIONAL, since it is a mutable in-flight case). Event/trail tables: role LEDGER, retention PERMANENT, append-only.
  - **REFACTOR:** the three domains share one table set only because each governs identity or privacy evidence under a restricted-disclosure model; correlation across `proofing_session`, `pseudonym_identity` and `data_subject_request` stays a cross-table query, never a shared foreign key, since the kernels themselves declare no relationship between them.
  - **Refs:** `internal/domains/proofing`, `internal/domains/pseudonym`, `internal/domains/privacy/dsr`, `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-SAFETY-001` **[PHASE_4][SOL_HIGH] Add durable PostgreSQL tables for the six compartmentalized workplace-safety revision families.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_SAFETY_001` in `internal/data/safetystore` (six tenant-scoped tables sharing one lineage envelope: row_id uuid, tenant_id uuid NOT NULL, case_ref uuid NOT NULL, compartment_ref uuid NOT NULL, revision bigint NOT NULL, parent_revision bigint, parent_digest content_digest, canonical_digest content_digest NOT NULL, plus per-t; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/safetystore/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SAFETY-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist incident, injury, reportability, claim, work-restriction and corrective-action revisions with their append-only lineage and compartment separation intact`.
  - **TEST:** `TestTodo_PERSIST_SAFETY_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_SAFETY_001`; `FAULT=TestTodo_PERSIST_SAFETY_001_Fault`; `INTEGRATION=TestTodo_PERSIST_SAFETY_001_Integration`; `SECURITY=TestTodo_PERSIST_SAFETY_001_Security`; `RECOVERY=TestTodo_PERSIST_SAFETY_001_Recovery`; `MUTATION=TestTodo_PERSIST_SAFETY_001_Mutation`.
  - **RED:** a row is updated instead of appended as a new `revision` with `parent_revision`/`parent_digest` set, a `safety_reportability_revision.deadline` is computed against the wrong regulatory clock, a `safety_claim_revision` names an `incident_ref` from a different `case_ref`, or a row is readable outside its owning tenant or compartment.
  - **GREEN:** six tenant-scoped tables sharing one lineage envelope: `row_id uuid`, `tenant_id uuid NOT NULL`, `case_ref uuid NOT NULL`, `compartment_ref uuid NOT NULL`, `revision bigint NOT NULL`, `parent_revision bigint`, `parent_digest content_digest`, `canonical_digest content_digest NOT NULL`, plus per-table columns. `safety_incident_revision`: `+ incident_at timestamptz NOT NULL, kind text NOT NULL, worker_ref uuid, reporter_ref uuid, location_ref uuid, description text, status text NOT NULL`. `safety_injury_revision`: `+ incident_ref uuid NOT NULL, worker_ref uuid NOT NULL, kind text NOT NULL, medical_evidence_ref uuid, severity text`. `safety_reportability_revision`: `+ incident_ref uuid NOT NULL, incident_at timestamptz NOT NULL, class text NOT NULL, clock jsonb NOT NULL, rule_citation text, deadline timestamptz NOT NULL, rationale text`. `safety_claim_revision`: `+ incident_ref uuid NOT NULL, worker_ref uuid NOT NULL, claim_ref text, authority_ref uuid, status text NOT NULL`. `safety_work_restriction_revision`: `+ incident_ref uuid NOT NULL, worker_ref uuid NOT NULL, kind text NOT NULL, medical_evidence_ref uuid, status text NOT NULL`. `safety_corrective_action_revision`: `+ incident_ref uuid NOT NULL, owner_ref uuid, due_rule text, verification_evidence_ref uuid, action text NOT NULL, status text NOT NULL`. Each: `UNIQUE (tenant_id, case_ref, <own_id>, revision)`. All six: `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY`, `hcmnext_app` only; role LEDGER, retention PERMANENT, `forbid_mutation` (the revision chain is itself the append-only fact log, matching `internal/domains/employeerelations`'s pattern). Compartment-scoped row visibility beyond bare tenant isolation is flagged as follow-up design work.
  - **REFACTOR:** keep this table set's shape identical column-for-column to `PERSIST-EMPLOYEERELATIONS-001`'s lineage envelope so one shared Go repository type serves both compartmentalized-evidence domains.
  - **Refs:** `internal/domains/safety`, `internal/domains/employeerelations` (the shared lineage-envelope pattern), `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-SKILL-001` **[PHASE_3][SOL_HIGH] Add durable PostgreSQL tables for the skill ontology, skill-definition revisions and worker-skill evidence.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_SKILL_001` in `internal/data/skillstore` (three tenant-scoped tables. skill_ontology_revision: row_id uuid, tenant_id uuid NOT NULL, ontology_id text NOT NULL, revision bigint NOT NULL, canonical_digest content_digest NOT NULL; UNIQUE (tenant_id, ontology_id, revision). skill_definition_revision: row_id, tenant_id, skill; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/skillstore/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SKILL-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist the canonical skill ontology and evidence-backed proficiency vocabulary under tenant isolation`.
  - **TEST:** `TestTodo_PERSIST_SKILL_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_SKILL_001`; `FAULT=TestTodo_PERSIST_SKILL_001_Fault`; `INTEGRATION=TestTodo_PERSIST_SKILL_001_Integration`; `SECURITY=TestTodo_PERSIST_SKILL_001_Security`; `RECOVERY=TestTodo_PERSIST_SKILL_001_Recovery`; `MUTATION=TestTodo_PERSIST_SKILL_001_Mutation`.
  - **RED:** a `worker_skill_evidence` row is updated or deleted, two evidence rows for one worker/skill share an `event_sequence`, a `skill_definition_revision.supersedes` names a revision of a different `skill_ref`, or a row is readable across tenants.
  - **GREEN:** three tenant-scoped tables. `skill_ontology_revision`: `row_id uuid`, `tenant_id uuid NOT NULL`, `ontology_id text NOT NULL`, `revision bigint NOT NULL`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, ontology_id, revision)`. `skill_definition_revision`: `row_id`, `tenant_id`, `skill_ref text NOT NULL`, `ontology_ref text NOT NULL`, `revision bigint NOT NULL`, `supersedes bigint`, `name text NOT NULL`, `parent_refs jsonb`, `aliases jsonb`, `proficiency_scale jsonb`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, skill_ref, revision)`. `worker_skill_evidence` (append-only, `forbid_mutation`, backs `skill.WorkerSkillEvidence`): `row_id`, `tenant_id`, `evidence_id text NOT NULL`, `worker_ref uuid NOT NULL`, `skill_ref text NOT NULL`, `level text`, `proficiency numeric(5,2)`, `evidence_kind text NOT NULL`, `evidence_ref text`, `verified boolean NOT NULL DEFAULT false`, `disputed boolean NOT NULL DEFAULT false`, `effective_from/effective_to timestamptz`, `canonical_digest content_digest NOT NULL`, `event_sequence bigint NOT NULL`; `UNIQUE (tenant_id, worker_ref, skill_ref, event_sequence)`. `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY` on all three, `hcmnext_app` only. `skill_ontology_revision`/`skill_definition_revision`: role AGGREGATE, retention PERMANENT. `worker_skill_evidence`: role LEDGER, retention PERMANENT, append-only.
  - **REFACTOR:** proficiency resolution stays a pure read over `worker_skill_evidence` filtered by `verified`/`disputed`; no resolution logic moves into the database.
  - **Refs:** `internal/domains/skill`, `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-SUBSCRIPTION-001` **[PHASE_3][SOL_HIGH] Add durable PostgreSQL tables for tenant-scoped event subscriptions and their authorization log.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_SUBSCRIPTION_001` in `internal/data/subscriptionstore` (two tenant-scoped tables. event_subscription (immutable revision, backs subscription.EventSubscription): row_id uuid, tenant_id uuid NOT NULL, subscription_id text NOT NULL, revision bigint NOT NULL, state text NOT NULL, requester text, approver text, subscriber text NOT NULL, ev; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/subscriptionstore/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `INTENT-028`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist the event-subscription vocabulary and its authorization-decision log under tenant isolation, holding no event payloads`.
  - **TEST:** `TestTodo_PERSIST_SUBSCRIPTION_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_SUBSCRIPTION_001`; `FAULT=TestTodo_PERSIST_SUBSCRIPTION_001_Fault`; `INTEGRATION=TestTodo_PERSIST_SUBSCRIPTION_001_Integration`; `SECURITY=TestTodo_PERSIST_SUBSCRIPTION_001_Security`; `RECOVERY=TestTodo_PERSIST_SUBSCRIPTION_001_Recovery`; `MUTATION=TestTodo_PERSIST_SUBSCRIPTION_001_Mutation`.
  - **RED:** an `event_subscription` row is updated in place instead of superseded by a new `revision`, a `subscription_authorization_event` row is updated or deleted, two authorization events for one subscription share an `event_sequence`, or a row is readable across tenants.
  - **GREEN:** two tenant-scoped tables. `event_subscription` (immutable revision, backs `subscription.EventSubscription`): `row_id uuid`, `tenant_id uuid NOT NULL`, `subscription_id text NOT NULL`, `revision bigint NOT NULL`, `state text NOT NULL`, `requester text`, `approver text`, `subscriber text NOT NULL`, `event_kinds jsonb NOT NULL`, `declared_fields jsonb`, `filter jsonb`, `delivery_endpoint_ref text`, `delivery_guarantee text`, `tenant_scope text`, `organization_scope_ref text`, `population_scope_ref text`, `digest content_digest NOT NULL`; `UNIQUE (tenant_id, subscription_id, revision)`. `subscription_authorization_event` (append-only, `forbid_mutation`): `row_id`, `tenant_id`, `subscription_id text NOT NULL`, `revision bigint NOT NULL`, `allowed boolean NOT NULL`, `rule text`, `reason text`, `grant_digest content_digest`, `decision_digest content_digest`, `digest content_digest NOT NULL`, `event_sequence bigint NOT NULL`; `UNIQUE (tenant_id, subscription_id, event_sequence)`. `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY` on both, `hcmnext_app` only. `event_subscription`: role AGGREGATE, retention PERMANENT. `subscription_authorization_event`: role LEDGER, retention PERMANENT, append-only.
  - **REFACTOR:** payload delivery and provider calls stay entirely outside this table set, matching the kernel's stated boundary.
  - **Refs:** `internal/domains/subscription`, `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-SUCCESSION-001` **[PHASE_4][SOL_HIGH] Add durable PostgreSQL tables for critical-role, successor-readiness and succession-slate revisions.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_SUCCESSION_001` in `internal/data/successionstore` (three tenant-scoped tables. succession_critical_role: row_id uuid, tenant_id uuid NOT NULL, role_id text NOT NULL, revision bigint NOT NULL, parent_revision bigint, parent_digest content_digest, position_ref text, job_revision_ref text, owner_ref text, authority_ref text, effecti; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/successionstore/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SUCCESSION-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist evidence-gated critical-role, readiness and slate revisions, none of which the kernel is permitted to turn into an employment decision`.
  - **TEST:** `TestTodo_PERSIST_SUCCESSION_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_SUCCESSION_001`; `FAULT=TestTodo_PERSIST_SUCCESSION_001_Fault`; `INTEGRATION=TestTodo_PERSIST_SUCCESSION_001_Integration`; `SECURITY=TestTodo_PERSIST_SUCCESSION_001_Security`; `RECOVERY=TestTodo_PERSIST_SUCCESSION_001_Recovery`; `MUTATION=TestTodo_PERSIST_SUCCESSION_001_Mutation`.
  - **RED:** a `succession_slate` names a `critical_role_revision` inconsistent with the role's current revision, a revision row is updated in place, or a row is readable across tenants.
  - **GREEN:** three tenant-scoped tables. `succession_critical_role`: `row_id uuid`, `tenant_id uuid NOT NULL`, `role_id text NOT NULL`, `revision bigint NOT NULL`, `parent_revision bigint`, `parent_digest content_digest`, `position_ref text`, `job_revision_ref text`, `owner_ref text`, `authority_ref text`, `effective_at/known_at timestamptz`, `evidence_refs jsonb`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, role_id, revision)`. `succession_readiness_revision`: `row_id`, `tenant_id`, `successor_id text NOT NULL`, `revision bigint NOT NULL`, `parent_revision bigint`, `readiness text NOT NULL`, `vacancy_risk text`, `assessed_by text`, `assessment_source text`, `evidence_refs jsonb`, `effective_at/known_at timestamptz`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, successor_id, revision)`. `succession_slate`: `row_id`, `tenant_id`, `slate_id text NOT NULL`, `revision bigint NOT NULL`, `parent_revision bigint`, `critical_role_id text NOT NULL`, `critical_role_revision bigint NOT NULL`, `position_ref text`, `job_revision_ref text`, `nominator_id text`, `visibility text NOT NULL`, `candidates jsonb`, `effective_at/known_at timestamptz`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, slate_id, revision)`, `FOREIGN KEY (tenant_id, critical_role_id, critical_role_revision) REFERENCES succession_critical_role (tenant_id, role_id, revision)`. `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY` on all three, `hcmnext_app` only; role AGGREGATE, retention PERMANENT.
  - **REFACTOR:** `visibility` stays a required column on `succession_slate` since candidate names are the most access-sensitive field in this table set.
  - **Refs:** `internal/domains/succession`, `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-SURVEY-001` **[PHASE_3][SOL_HIGH] Add durable PostgreSQL tables for survey question banks, campaigns, launches, samples and responses.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_SURVEY_001` in `internal/data/surveystore` (six tenant-scoped tables. survey_question_bank_revision: row_id uuid, tenant_id uuid NOT NULL, bank_id text NOT NULL, revision bigint NOT NULL, created_at timestamptz NOT NULL, questions jsonb NOT NULL; UNIQUE (tenant_id, bank_id, revision). survey_revision: row_id, tenant_id, su; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/surveystore/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SURVEY-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist question-bank, survey, campaign, launch, sample and response revisions under tenant isolation, since storage, transmission and retention are the kernel's own stated caller responsibility`.
  - **TEST:** `TestTodo_PERSIST_SURVEY_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_SURVEY_001`; `FAULT=TestTodo_PERSIST_SURVEY_001_Fault`; `INTEGRATION=TestTodo_PERSIST_SURVEY_001_Integration`; `SECURITY=TestTodo_PERSIST_SURVEY_001_Security`; `RECOVERY=TestTodo_PERSIST_SURVEY_001_Recovery`; `MUTATION=TestTodo_PERSIST_SURVEY_001_Mutation`.
  - **RED:** a `survey_response_record` is written for a `campaign_id` whose `survey_campaign_sample` does not include the responding member, a `survey_launch_record`/`survey_response_record` row is updated or deleted, two responses for one campaign/member share an `event_sequence`, a response is retained past a campaign's `anonymity_threshold` cohort-size guarantee, or a row is readable across tenants.
  - **GREEN:** six tenant-scoped tables. `survey_question_bank_revision`: `row_id uuid`, `tenant_id uuid NOT NULL`, `bank_id text NOT NULL`, `revision bigint NOT NULL`, `created_at timestamptz NOT NULL`, `questions jsonb NOT NULL`; `UNIQUE (tenant_id, bank_id, revision)`. `survey_revision`: `row_id`, `tenant_id`, `survey_id text NOT NULL`, `revision bigint NOT NULL`, `title text NOT NULL`, `description text`, `question_bank_ref text NOT NULL`, `created_at timestamptz NOT NULL`; `UNIQUE (tenant_id, survey_id, revision)`. `survey_campaign_revision`: `row_id`, `tenant_id`, `campaign_id text NOT NULL`, `revision bigint NOT NULL`, `survey_ref text NOT NULL`, `population_snapshot_ref text`, `window_start/window_end timestamptz NOT NULL`, `anonymity_threshold int NOT NULL`, `channel_refs jsonb`, `created_at timestamptz NOT NULL`; `UNIQUE (tenant_id, campaign_id, revision)`. `survey_campaign_sample` (frozen sampling snapshot): `row_id`, `tenant_id`, `campaign_id text NOT NULL`, `binding jsonb`, `rule jsonb`, `member_ids jsonb NOT NULL`, `membership_protected boolean NOT NULL DEFAULT true`, `count int NOT NULL`, `frozen_at timestamptz NOT NULL`, `digest content_digest NOT NULL`; `UNIQUE (tenant_id, campaign_id)`. `survey_launch_record` (append-only, `forbid_mutation`): `row_id`, `tenant_id`, `campaign_id text NOT NULL`, `campaign_revision bigint NOT NULL`, `campaign_digest content_digest`, `purpose text`, `sample_digest content_digest`, `membership_protected boolean NOT NULL`, `template_key/template_version/template_digest text`, `audience_digest content_digest`, `delivery_plan_digest content_digest`, `launched_by text`, `approver text`, `launched_at timestamptz NOT NULL`, `reminder_policy jsonb`, `digest content_digest NOT NULL`, `event_sequence bigint NOT NULL`; `UNIQUE (tenant_id, campaign_id, event_sequence)`. `survey_response_record` (append-only, `forbid_mutation`): `row_id`, `tenant_id`, `campaign_id text NOT NULL`, `campaign_revision bigint NOT NULL`, `form_definition jsonb NOT NULL`, `response_key text NOT NULL`, `response_revision int NOT NULL`, `answers jsonb NOT NULL`, `validation_digest content_digest`, `submitted_at timestamptz NOT NULL`, `digest content_digest NOT NULL`, `event_sequence bigint NOT NULL`; `UNIQUE (tenant_id, campaign_id, response_key, event_sequence)`. `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY` on all six, `hcmnext_app` only. `survey_launch_record`/`survey_response_record`: role LEDGER, retention PERMANENT, append-only. The other four: role AGGREGATE, retention PERMANENT.
  - **REFACTOR:** cohort-safe aggregation (the `Aggregate` read model) stays a pure read requiring `count >= anonymity_threshold`; that guard is enforced by the read port, not a database view, so it cannot be bypassed by a direct query using an elevated role.
  - **Refs:** `internal/domains/survey`, `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-TAXPROFILE-001` **[PHASE_4][SOL_HIGH] Add durable PostgreSQL tables for worker tax-profile, registration, withholding-election and exemption revisions.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_TAXPROFILE_001` in `internal/data/taxprofilestore`, `internal/domains/taxprofile` (four tenant-scoped tables. worker_tax_profile_revision: row_id uuid, tenant_id uuid NOT NULL, worker_ref uuid NOT NULL, revision bigint NOT NULL, parent_revision bigint, parent_digest content_digest, residence_jurisdictions jsonb, work_jurisdictions jsonb, filing_status text, cla; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/taxprofilestore/ ./internal/domains/taxprofile/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TAXPROFILE-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist the append-only worker tax-profile vocabulary (registrations, elections, exemptions, classification) under tenant isolation`.
  - **TEST:** `TestTodo_PERSIST_TAXPROFILE_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_TAXPROFILE_001`; `FAULT=TestTodo_PERSIST_TAXPROFILE_001_Fault`; `INTEGRATION=TestTodo_PERSIST_TAXPROFILE_001_Integration`; `SECURITY=TestTodo_PERSIST_TAXPROFILE_001_Security`; `RECOVERY=TestTodo_PERSIST_TAXPROFILE_001_Recovery`; `MUTATION=TestTodo_PERSIST_TAXPROFILE_001_Mutation`.
  - **RED:** a revision row in any of the four tables is updated in place instead of chained through `parent_revision`/`parent_digest`, a `withholding_election_revision`/`tax_exemption_revision` names a `worker_ref` from a different tenant, or a row is readable across tenants.
  - **GREEN:** four tenant-scoped tables. `worker_tax_profile_revision`: `row_id uuid`, `tenant_id uuid NOT NULL`, `worker_ref uuid NOT NULL`, `revision bigint NOT NULL`, `parent_revision bigint`, `parent_digest content_digest`, `residence_jurisdictions jsonb`, `work_jurisdictions jsonb`, `filing_status text`, `classification text NOT NULL`, `classification_evidence_ref text`, `effective_from timestamptz`, `known_at timestamptz NOT NULL`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, worker_ref, revision)`. `tax_registration_revision`: `row_id`, `tenant_id`, `registration_id text NOT NULL`, `jurisdiction text NOT NULL`, `authority_ref text`, `revision bigint NOT NULL`, `parent_revision bigint`, `parent_digest content_digest`, `effective_from timestamptz`, `known_at timestamptz NOT NULL`, `raw_registration_id text`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, registration_id, revision)`. `withholding_election_revision`: `row_id`, `tenant_id`, `election_id text NOT NULL`, `worker_ref uuid NOT NULL`, `jurisdiction text NOT NULL`, `kind text NOT NULL`, `form_revision_ref text`, `evidence_ref text`, `amount numeric(19,4)`, `effective_from timestamptz`, `known_at timestamptz NOT NULL`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, election_id, effective_from)`. `tax_exemption_revision`: `row_id`, `tenant_id`, `exemption_id text NOT NULL`, `worker_ref uuid NOT NULL`, `jurisdiction text NOT NULL`, `kind text NOT NULL`, `evidence_refs jsonb`, `expires_at timestamptz`, `effective_from timestamptz`, `known_at timestamptz NOT NULL`, `canonical_digest content_digest NOT NULL`; `UNIQUE (tenant_id, exemption_id, effective_from)`. `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY` on all four, `hcmnext_app` only; role AGGREGATE, retention PERMANENT.
  - **REFACTOR:** `worker_ref` stays a soft reference to the already-durable `worker` table (00011); no foreign key is added here since `worker`'s natural key is `(tenant_id, entity_id)` on a different identity spine than this table's surrogate `worker_ref`.
  - **Refs:** `internal/domains/taxprofile`, `migrations/00011_people_aggregates.sql` (the `worker` table this entry references), `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-TENANT-001` **[PHASE_3][SOL_HIGH] Add durable PostgreSQL tables for tenant placement, provisioning evidence and government-authorization profiles.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_TENANT_001` in `internal/data/tenantstore`, `internal/domains/tenant` (internal/data/tenantstore over migration 00050 persists tenant lifecycle state with capability-specific suspension semantics; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/tenantstore/ ./internal/domains/tenant/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TENANT-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist signed logical tenant placement, its provisioning-event evidence, and the government-authorization compliance profile, none of which the already-durable tenant and tenant_bootstrap_receipt tables cover`.
  - **TEST:** `TestTodo_PERSIST_TENANT_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_TENANT_001`; `FAULT=TestTodo_PERSIST_TENANT_001_Fault`; `INTEGRATION=TestTodo_PERSIST_TENANT_001_Integration`; `SECURITY=TestTodo_PERSIST_TENANT_001_Security`; `RECOVERY=TestTodo_PERSIST_TENANT_001_Recovery`; `MUTATION=TestTodo_PERSIST_TENANT_001_Mutation`.
  - **RED:** a `tenant_placement.epoch` moves backward or is set without a valid `signature`, a `tenant_provisioning_event` row is updated or deleted, two provisioning events for one tenant share an `event_sequence`, or a row is readable across tenants.
  - **GREEN:** three tables, none duplicating the base `tenant` row (00002) or `tenant_bootstrap_receipt` (00029, already TENANT-002). `tenant_placement` (mutable, epoch-fenced, one row per tenant): `tenant_id uuid PRIMARY KEY REFERENCES tenant (tenant_id)`, `cell text NOT NULL`, `region text NOT NULL`, `residency_profile text NOT NULL`, `isolation_tier text NOT NULL`, `epoch bigint NOT NULL`, `signature bytea NOT NULL`, `updated_at timestamptz NOT NULL DEFAULT now()`; a `BEFORE UPDATE` trigger rejects any update where `NEW.epoch <= OLD.epoch`. `tenant_provisioning_event` (append-only, `forbid_mutation`): `row_id uuid`, `tenant_id uuid NOT NULL`, `kind text NOT NULL`, `plane text NOT NULL`, `verifier_principal text`, `reason text`, `at timestamptz NOT NULL`, `event_sequence bigint NOT NULL`; `UNIQUE (tenant_id, event_sequence)`. `government_authorization_profile`: `row_id`, `tenant_id`, `schema_version text NOT NULL`, `revision bigint NOT NULL`, `integration_id text NOT NULL`, `applicable_programs jsonb`, `system_boundary jsonb`, `inherited_controls jsonb`, `evidence jsonb`, `assessor_status text`, `review_date date`, `fedramp jsonb`, `cms jsonb`, `procurement_answers jsonb`, `revision_digest content_digest NOT NULL`; `UNIQUE (tenant_id, integration_id, revision)`. `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY` on all three, `hcmnext_app` only. `tenant_placement`: role CONTROL, retention OPERATIONAL. `tenant_provisioning_event`: role LEDGER, retention PERMANENT, append-only. `government_authorization_profile`: role AGGREGATE, retention PERMANENT.
  - **REFACTOR:** the migration header states explicitly that `tenant_bootstrap_receipt` (00029) already satisfies TENANT-002 and is not touched by this entry, so a future reader does not attempt to add a second bootstrap-evidence table.
  - **Refs:** `internal/domains/tenant`, `internal/domains/tenant/govauth`, `migrations/00002_tenant_primitives.sql`, `migrations/00029_tenant_bootstrap_receipt.sql` (both already-covered boundaries), `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-COMMERCIAL-001` **[PHASE_3][SOL_HIGH] Add durable PostgreSQL tables for commercial contracts, entitlement snapshots and partner-application installations.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_COMMERCIAL_001` in `internal/data/commercialstore` (seven tables. commercial_contract_revision (tenant-scoped, immutable revision, backs commercial.Contract/ContractRevision): row_id uuid, tenant_id uuid NOT NULL, contract_id text NOT NULL, revision bigint NOT NULL, effective_from/effective_to timestamptz, capabilities jsonb NOT N; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/commercialstore/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `COMMERCIAL-001`, `APP-001`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist the commercial contract/entitlement boundary and the partner-application version and installation-grant lifecycle it authorizes into a tenant, kept separate from runtime billing authority`.
  - **TEST:** `TestTodo_PERSIST_COMMERCIAL_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_COMMERCIAL_001`; `FAULT=TestTodo_PERSIST_COMMERCIAL_001_Fault`; `INTEGRATION=TestTodo_PERSIST_COMMERCIAL_001_Integration`; `SECURITY=TestTodo_PERSIST_COMMERCIAL_001_Security`; `RECOVERY=TestTodo_PERSIST_COMMERCIAL_001_Recovery`; `MUTATION=TestTodo_PERSIST_COMMERCIAL_001_Mutation`.
  - **RED:** a `commercial_contract_revision`/`partner_installation` row is amended without a monotonically increasing `revision`, a `partner_installation_event` row is updated or deleted, two installation events for one installation share an `event_sequence`, an `entitlement_snapshot`'s `fingerprint` does not match its named contract revision, or a tenant-scoped row is readable across tenants.
  - **GREEN:** seven tables. `commercial_contract_revision` (tenant-scoped, immutable revision, backs `commercial.Contract`/`ContractRevision`): `row_id uuid`, `tenant_id uuid NOT NULL`, `contract_id text NOT NULL`, `revision bigint NOT NULL`, `effective_from/effective_to timestamptz`, `capabilities jsonb NOT NULL`, `bound boolean NOT NULL DEFAULT false`, `price_cents bigint`, `currency text`, `fingerprint content_digest NOT NULL`; `UNIQUE (tenant_id, contract_id, revision)`. `entitlement_snapshot` (tenant-scoped, frozen): `row_id`, `tenant_id`, `contract_id text NOT NULL`, `revision bigint NOT NULL`, `fingerprint content_digest NOT NULL`, `frozen_at timestamptz NOT NULL`; `PRIMARY KEY (tenant_id, row_id)`, `FOREIGN KEY (tenant_id, contract_id, revision) REFERENCES commercial_contract_revision (tenant_id, contract_id, revision)`. Two platform catalogs carry no `tenant_id`, following the `jurisdiction` precedent: `partner_application`: `row_id uuid PRIMARY KEY`, `partner_ref text NOT NULL`, `application_id text NOT NULL`, `agreement_ref text`, `declared_capabilities jsonb`, `data_classes jsonb`, `redirect_endpoints jsonb`, `callback_endpoints jsonb`, `contact_ref text`, `legal_ref text`; `UNIQUE (application_id)`. `partner_application_version_revision`: `row_id uuid PRIMARY KEY`, `application_id text NOT NULL`, `version text NOT NULL`, `revision bigint NOT NULL`, `state text NOT NULL`, `review jsonb`, `successor_version text`, `digest content_digest NOT NULL`; `UNIQUE (application_id, version, revision)`. Three tenant-scoped tables: `partner_installation`: `row_id`, `tenant_id`, `installation_id text NOT NULL`, `revision bigint NOT NULL`, `state text NOT NULL`, `version_binding jsonb NOT NULL`, `organization_scope text`, `population_scope text`, `data_classes jsonb`, `field_scopes jsonb`, `requester text`, `approver_evidence jsonb`, `review_ref text`, `digest content_digest NOT NULL`; `UNIQUE (tenant_id, installation_id, revision)`. `partner_installation_event` (append-only, `forbid_mutation`): `row_id`, `tenant_id`, `installation_id text NOT NULL`, `revision bigint NOT NULL`, `previous_digest content_digest`, `previous_state text`, `state text NOT NULL`, `actor text`, `evidence_ref text`, `review_ref text`, `installation_digest content_digest`, `digest content_digest NOT NULL`, `event_sequence bigint NOT NULL`; `UNIQUE (tenant_id, installation_id, event_sequence)`. `partner_data_processing_review`: `row_id`, `tenant_id`, `application_id text NOT NULL`, `version text NOT NULL`, `submitter text`, `owner_ref text`, `reviewer text`, `reviewed_at/expires_at timestamptz`, `data_classes jsonb`, `scope_refs/destination_refs/processor_refs/residency_refs jsonb`, `items jsonb`, `digest content_digest NOT NULL`. RLS: `partner_application`/`partner_application_version_revision` carry no tenant column and no RLS (platform catalog, read-only grant); the other five carry `tenant_isolation` + `FORCE ROW LEVEL SECURITY`, `hcmnext_app` only. Disposition: `commercial_contract_revision`/`entitlement_snapshot`/`partner_installation`/`partner_data_processing_review`: role AGGREGATE, retention PERMANENT. `partner_application`/`partner_application_version_revision`: role REGISTRY, retention PERMANENT, tenant column none. `partner_installation_event`: role LEDGER, retention PERMANENT, append-only.
  - **REFACTOR:** commercial and partnerapp stay one table set because a partner installation is itself a product-boundary grant governed by the same entitlement logic as a commercial contract; split them if partner installations ever need a lifecycle independent of any contract.
  - **Refs:** `internal/commercial`, `internal/domains/partnerapp`, `migrations/00021_governance_authz_legal_evidence.sql` (the `jurisdiction` no-tenant-column precedent), `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-LEGALEVIDENCE-001` **[PHASE_3][SOL_HIGH] Add durable PostgreSQL tables for rule-pack review records, jurisdiction-composition receipts and the authoring pipeline's hash-chained event log.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_LEGALEVIDENCE_001` in `internal/data/legalevidencestore` (internal/data/legalevidencestore over migration 00053 persists rule-pack review records and jurisdiction-composition receipts with tenant RLS; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/legalevidencestore/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LEGAL-015`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist the review, composition and hash-chained authoring evidence the legal rule-pack pipeline already produces but does not yet durably store, complementing the already-durable jurisdiction/legal_rule_pack/rule_evaluation/obligation tables`.
  - **TEST:** `TestTodo_PERSIST_LEGALEVIDENCE_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_LEGALEVIDENCE_001`; `FAULT=TestTodo_PERSIST_LEGALEVIDENCE_001_Fault`; `INTEGRATION=TestTodo_PERSIST_LEGALEVIDENCE_001_Integration`; `SECURITY=TestTodo_PERSIST_LEGALEVIDENCE_001_Security`; `RECOVERY=TestTodo_PERSIST_LEGALEVIDENCE_001_Recovery`; `MUTATION=TestTodo_PERSIST_LEGALEVIDENCE_001_Mutation`.
  - **RED:** a `legal_pipeline_event.prev_digest` does not equal the previous row's `digest` for that pack, a `legal_review_record` names the same principal as both author and reviewer, any of the three tables accepts an `UPDATE`/`DELETE`, or a row is readable across tenants.
  - **GREEN:** three tenant-scoped tables (following `legal_rule_pack`'s own precedent of carrying `tenant_id` even though rule packs are largely shared reference content). `legal_review_record` (append-only, `forbid_mutation`): `row_id uuid`, `tenant_id uuid NOT NULL`, `pack_digest content_digest NOT NULL`, `author_id text NOT NULL`, `reviewer_id text NOT NULL`, `status text NOT NULL`, `findings jsonb`, `digest content_digest NOT NULL`, `event_sequence bigint NOT NULL`; `UNIQUE (tenant_id, pack_digest, event_sequence)`, `CHECK (author_id <> reviewer_id)`. `legal_composition_receipt` (append-only, `forbid_mutation`): `row_id`, `tenant_id`, `status text NOT NULL`, `jurisdictions jsonb NOT NULL`, `inputs jsonb`, `obligations jsonb`, `traces jsonb`, `contradictions jsonb`, `digest content_digest NOT NULL`, `event_sequence bigint NOT NULL`; `UNIQUE (tenant_id, digest)`. `legal_pipeline_event` (hash-chained append-only log, `forbid_mutation`): `row_id`, `tenant_id`, `stage text NOT NULL`, `principal_id text NOT NULL`, `role text NOT NULL`, `artifact_digest content_digest NOT NULL`, `prev_digest content_digest`, `digest content_digest NOT NULL`, `signature bytea NOT NULL`, `event_sequence bigint NOT NULL`; `UNIQUE (tenant_id, event_sequence)`; the chain invariant (`prev_digest` of row N equals `digest` of row N-1) is verified at the application layer on read, the same way `ledger_hash_chain_link` is verified, not by a database constraint. `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY` on all three, `hcmnext_app` only; role LEDGER, retention PERMANENT, append-only.
  - **REFACTOR:** this entry adds no columns to and takes no dependency on `jurisdiction`/`legal_rule_pack`/`rule_evaluation`/`obligation`/`obligation_binding` (00021); it only fills the evidence gap those tables leave around review, composition and authoring-pipeline provenance.
  - **Refs:** `internal/governance/legal` (`review_record.go`, `compose.go`), `internal/governance/legal/pipeline`, `migrations/00021_governance_authz_legal_evidence.sql`, `migrations/00014_ledger_hash_chain.sql` (the verified-on-read hash-chain precedent), `definitions/storage/storage-disposition.yaml`.

- [x] `PERSIST-TRUST-001` **[PHASE_4][SOL_HIGH] Add durable PostgreSQL tables for step-up proof evidence, JIT operator grants and periodic access-review evidence.**
  - **Evidence (2026-09-05):** `TestTodo_PERSIST_TRUST_001` in `internal/data/truststore` (six tenant-scoped tables. stepup_proof_log (append-only, forbid_mutation — table name taken verbatim from internal/trust/stepup/sqlstore.go's own doc comment): row_id uuid, tenant_id uuid NOT NULL, proof_id text NOT NULL, outcome text NOT NULL, consumed_at timestamptz NOT NULL, e; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/truststore/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SECARCH-001`, `TRUST-021`, `SECARCH-003`, `DB-017`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=persist step-up proof consumption, time-bounded JIT operator-access evidence, and periodic access-review evidence, complementing the already-durable principal/delegation_grant tables and the trust_session tables`.
  - **TEST:** `TestTodo_PERSIST_TRUST_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERSIST_TRUST_001`; `FAULT=TestTodo_PERSIST_TRUST_001_Fault`; `INTEGRATION=TestTodo_PERSIST_TRUST_001_Integration`; `SECURITY=TestTodo_PERSIST_TRUST_001_Security`; `RECOVERY=TestTodo_PERSIST_TRUST_001_Recovery`; `MUTATION=TestTodo_PERSIST_TRUST_001_Mutation`.
  - **RED:** a `stepup_proof_id` is consumed twice, a `jit_grant` is used past its `expires_at` or after `revoked` is set, a `jit_evidence_record`/`accessreview_review_record` row is updated or deleted, an `accessreview_grant` has no corresponding `accessreview_schedule` entry covering its review cadence, or a row is readable across tenants.
  - **GREEN:** six tenant-scoped tables. `stepup_proof_log` (append-only, `forbid_mutation` — table name taken verbatim from `internal/trust/stepup/sqlstore.go`'s own doc comment): `row_id uuid`, `tenant_id uuid NOT NULL`, `proof_id text NOT NULL`, `outcome text NOT NULL`, `consumed_at timestamptz NOT NULL`, `event_sequence bigint NOT NULL`; `UNIQUE (tenant_id, proof_id)`. `jit_grant`: `row_id`, `tenant_id`, `grant_id text NOT NULL`, `revision bigint NOT NULL`, `state text NOT NULL`, `requester text NOT NULL`, `approver text`, `scope jsonb NOT NULL`, `not_before/expires_at timestamptz NOT NULL`, `revoked boolean NOT NULL DEFAULT false`; `UNIQUE (tenant_id, grant_id, revision)`. `jit_evidence_record` (append-only, `forbid_mutation`): `row_id`, `tenant_id`, `grant_id text NOT NULL`, `evidence_kind text NOT NULL`, `detail jsonb`, `at timestamptz NOT NULL`, `event_sequence bigint NOT NULL`; `UNIQUE (tenant_id, grant_id, event_sequence)`. `accessreview_grant`: `row_id`, `tenant_id`, `grant_id text NOT NULL`, `principal_id text NOT NULL`, `capabilities jsonb NOT NULL`, `granted_at timestamptz NOT NULL`, `expires_at timestamptz`, `revoked boolean NOT NULL DEFAULT false`; `PRIMARY KEY (tenant_id, row_id)`, `UNIQUE (tenant_id, grant_id)`. `accessreview_schedule`: `row_id`, `tenant_id`, `schedule_id text NOT NULL`, `entries jsonb NOT NULL`, `digest content_digest NOT NULL`; `UNIQUE (tenant_id, schedule_id)`. `accessreview_review_record` (append-only, `forbid_mutation`): `row_id`, `tenant_id`, `schedule_id text NOT NULL`, `reviewer_id text NOT NULL`, `decision text NOT NULL`, `ticket_ref text`, `justification text`, `capabilities jsonb`, `at timestamptz NOT NULL`, `event_sequence bigint NOT NULL`; `UNIQUE (tenant_id, schedule_id, event_sequence)`. `tenant_isolation` RLS + `FORCE ROW LEVEL SECURITY` on all six, `hcmnext_app` only. `jit_grant`/`accessreview_grant`/`accessreview_schedule`: role CONTROL, retention OPERATIONAL. `stepup_proof_log`/`jit_evidence_record`/`accessreview_review_record`: role LEDGER, retention PERMANENT, append-only.
  - **REFACTOR:** `internal/trust/session` stays entirely out of scope, since `trust_session`/`trust_session_pointer`/`trust_session_refresh_generation`/`trust_session_evidence` (00041) already cover it in full; this entry only fills the stepup/jit/accessreview gap the same trust package leaves open.
  - **Refs:** `internal/trust/stepup`, `internal/trust/jit`, `internal/trust/accessreview`, `migrations/00021_governance_authz_legal_evidence.sql`, `migrations/00041_session_store.sql` (already-covered boundary), `definitions/storage/storage-disposition.yaml`.

## 27. Authoritative ledger implementation

> **Disposition (2026-09-02):** P1A for stream, head, event and payload-schema tables and the single-stream append with CAS. P1B for multi-stream commit, hash chains and corrections. Integrity epochs and external anchors are Gate C.

- [x] `LEDGER-001` **[GATE_A][SOL_HIGH] Create ledger stream, head, event and payload-schema tables.**
  - **Evidence (2026-09-03):** `TestTodo_LEDGER_001` in `internal/data/schema`; `go test -count=1 ./internal/data/schema/...` PASS on windows/arm64 (Go 1.26.3); `go vet`, `gofmt -l`, `staticcheck` clean; branch plan-revision-2026-09-02.
  - **Depends:** `DB-005`, `DB-006`, `DB-007`, `MODEL-007`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_LEDGER_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEDGER_001`; `GOLDEN=TestTodo_LEDGER_001_Golden`; `RACE=TestTodo_LEDGER_001_Race`; `SECURITY=TestTodo_LEDGER_001_Security`; `MUTATION=TestTodo_LEDGER_001_Mutation`.
  - **RED:** duplicate tenant/stream/sequence, sequence below one, missing assertion/source/schema/time/digest or mutable event update/delete succeeds.
  - **GREEN:** migrations create tenant-scoped `ledger_stream`, `stream_head`, partitioned `ledger_event` and typed payload-schema references with append-only permissions and exact constraints.
  - **REFACTOR:** stream name is semantic identity; physical partition is replaceable.
  - **Refs:** [Ledger models](data/models/kernel-governance-and-evidence.md), [ledger spec](specs/transaction-ledger-reconciliation-and-repair.md).

- [x] `LEDGER-002` **[GATE_A][SOL_HIGH] Implement the canonical single-stream append API in Go.**
  - **Evidence (2026-09-03):** `TestTodo_LEDGER_002, FuzzTodo_LEDGER_002` in `internal/data/ledger`; `go test -count=1 ./internal/data/ledger/...` PASS on windows/arm64 (Go 1.26.3); `go vet`, `gofmt -l`, `staticcheck` clean; branch plan-revision-2026-09-02.
  - **Depends:** `LEDGER-001`, `DB-018`, `TIME-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_LEDGER_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEDGER_002`; `GOLDEN=TestTodo_LEDGER_002_Golden`; `FUZZ=FuzzTodo_LEDGER_002`; `RACE=TestTodo_LEDGER_002_Race`; `RECOVERY=TestTodo_LEDGER_002_Recovery`; `MUTATION=TestTodo_LEDGER_002_Mutation`; `INTEGRATION=TestTodo_LEDGER_002_Integration`.
  - **RED:** caller supplies digest/sequence/recorded time, expected head is stale, payload schema fails, authority class is invalid or duplicate idempotency key has different bytes.
  - **GREEN:** database transaction locks head, allocates next sequence, canonicalizes/hashes envelope, appends once, advances head and returns `AppendReceipt`; exact replay returns original receipt.
  - **REFACTOR:** API accepts typed generated payloads, not `map[string]any`.
  - **Refs:** [Canonical envelope](specs/canonical-envelope-and-digest.md), [ledger assertion classes](specs/transaction-ledger-reconciliation-and-repair.md).

- [x] `LEDGER-003` **[GATE_B][SOL_HIGH] Implement atomic multi-stream append with canonical lock ordering.**
  - **Evidence (2026-09-05):** `TestTodo_LEDGER_003` in `internal/data/ledger` (AppendMulti writes to several streams atomically under canonical stream-key lock ordering, so concurrent multi-stream commits cannot deadlock and either all heads advance or none do; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/ledger/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LEDGER-002`, `TX-003`, `DB-018`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_LEDGER_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEDGER_003`; `GOLDEN=TestTodo_LEDGER_003_Golden`; `RACE=TestTodo_LEDGER_003_Race`; `MUTATION=TestTodo_LEDGER_003_Mutation`.
  - **RED:** one expected head is stale, duplicate stream appears, lock order differs, failpoint after first append leaves partial events or two concurrent plans both win.
  - **GREEN:** all expected heads validate and all events/heads commit once or none do; receipt contains every before/after head ordered by canonical stream key.
  - **REFACTOR:** multi-stream transaction stays within one PostgreSQL consistency boundary.
  - **Refs:** [Multi-stream plan](data/models/kernel-governance-and-evidence.md), [commit coordinator](specs/transaction-plan-and-commit-coordinator.md).

- [x] `LEDGER-004` **[GATE_A][SOL_HIGH] Enforce assertion authority and typed payload references.**
  - **Evidence (2026-09-03):** `TestTodo_LEDGER_004` in `internal/data/ledger`; `go test -count=1 ./internal/data/ledger/...` PASS on windows/arm64 (Go 1.26.3); `go vet`, `gofmt -l`, `staticcheck` clean; branch plan-revision-2026-09-02.
  - **Depends:** `LEDGER-002`, `MODEL-020`, `MODEL-021`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_LEDGER_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEDGER_004`; `PROPERTY=TestTodo_LEDGER_004_Property`; `FUZZ=FuzzTodo_LEDGER_004`; `RACE=TestTodo_LEDGER_004_Race`; `MUTATION=TestTodo_LEDGER_004_Mutation`; `SECURITY=TestTodo_LEDGER_004_Security`.
  - **RED:** external observation is stored as domain fact without authority, claim is silently promoted, correction lacks target or sensitive large bytes enter the envelope.
  - **GREEN:** assertion class plus authority assignment/effective interval validates; event stores typed payload or governed artifact ref and exposes explicit provenance.
  - **REFACTOR:** recording an assertion never implies authority over its subject.
  - **Refs:** [Assertion classes](specs/transaction-ledger-reconciliation-and-repair.md), [source authority](specs/source-authority-and-external-mastering.md).

- [x] `LEDGER-005` **[GATE_A][SOL_HIGH] Implement append-only correction and supersession lineage.**
  - **Depends:** `LEDGER-002`, `MODEL-020`, `MODEL-025`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_LEDGER_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEDGER_005`; `PROPERTY=TestTodo_LEDGER_005_Property`; `RACE=TestTodo_LEDGER_005_Race`; `SECURITY=TestTodo_LEDGER_005_Security`; `MUTATION=TestTodo_LEDGER_005_Mutation`.
  - **RED:** correction updates/deletes target, points across tenant, creates a correction cycle or omits reason/authority/downstream-impact policy.
  - **GREEN:** new event references exact assertion, preserves original, produces acyclic supersession graph and current-view resolver returns corrected fact with full history.
  - **REFACTOR:** current truth is a projection over immutable chronology.
  - **Refs:** [Correction models](data/models/kernel-governance-and-evidence.md), [business correction](specs/transaction-ledger-reconciliation-and-repair.md).
  - **Evidence (2026-09-03):** `TestTodo_LEDGER_005`, `_Property`, `_Security` in `internal/data/ledger/lineage` (corrections and supersessions over the existing corrects_stream_key/corrects_sequence columns; `Ancestors`/`Descendants`/`EffectiveCurrent` deterministic and cycle-guarded; `Append` validates targets in-tenant; direct UPDATE/DELETE refused with rows proven byte-identical); PASS; branch plan-revision-2026-09-02; `go test -count=1 ./internal/data/ledger/lineage/...` on windows/arm64 (Go 1.26.3).

- [x] `LEDGER-006` **[GATE_A][SOL_HIGH] Implement bitemporal ledger query and state reconstruction APIs.**
  - **Evidence (2026-09-05):** `TestTodo_LEDGER_006`, `TestTodo_LEDGER_006_{Property,Race,Mutation,Security}` in `internal/data/ledger/temporal` (bitemporal query over effective-at and known-at with typed authority classes, state reconstruction as of any recorded instant, digest-stable results, tenant-isolated) plus `TestTemporalAdapterSatisfiesThePort`, `TestTemporalModesAreTheAdaptersOwn`, `TestOnlyDomainAndTransactionTruthArePromotable` in `internal/ledger`; read-only, no table; `go test -count=1 ./internal/ledger/ ./internal/data/ledger/temporal/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LEDGER-004`, `LEDGER-005`, `TRUST-012`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_LEDGER_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEDGER_006`; `PROPERTY=TestTodo_LEDGER_006_Property`; `RACE=TestTodo_LEDGER_006_Race`; `MUTATION=TestTodo_LEDGER_006_Mutation`; `SECURITY=TestTodo_LEDGER_006_Security`.
  - **RED:** future-known correction leaks into historical known-at query, half-open boundary duplicates fact, unauthorized field appears or external observation is presented as domain truth.
  - **GREEN:** current/effective-as-of/known-as-of/between/history/reconstruct modes return exact authorized assertion set, authority labels and source event IDs.
  - **REFACTOR:** query plans use projections where safe but preserve ledger-derived answer equivalence.
  - **Refs:** [Temporal primitives](data/models/wire-contract-primitives.md), [effective history](specs/hris-admin-dataops.md).

- [x] `LEDGER-007` **[GATE_B][SOL_HIGH] Compute and verify per-stream hash chains.**
  - **Depends:** `LEDGER-002`, `MODEL-007`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_LEDGER_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEDGER_007`; `GOLDEN=TestTodo_LEDGER_007_Golden`; `RACE=TestTodo_LEDGER_007_Race`; `SECURITY=TestTodo_LEDGER_007_Security`; `MUTATION=TestTodo_LEDGER_007_Mutation`.
  - **RED:** deletion/reorder/alteration/duplication/wrong previous hash verifies or verifier skips inaccessible partition silently.
  - **GREEN:** verifier reports exact first invalid tenant/stream/sequence and expected/actual digest; intact stream reproduces head digest from genesis.
  - **REFACTOR:** crypto algorithm/version is recorded per event/epoch.
  - **Refs:** [Ledger integrity](specs/transaction-ledger-reconciliation-and-repair.md), [crypto models](data/models/security-trust.md).
  - **Evidence (2026-09-03):** `TestTodo_LEDGER_007`, Declared `LEDGER-007` test matrix and acceptance behavior in `internal/data/ledger/hashchain`; PASS; `go test -p 1 ./internal/data/ledger/hashchain` and `go vet ./internal/data/ledger/hashchain` on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `LEDGER-008` **[GATE_B][SOL_HIGH] Atomically append event, critical projection, outbox and provenance roots.**
  - **Evidence (2026-09-05):** `TestTodo_LEDGER_008` in `internal/data/ledger/commit` (internal/data/ledger/commit records a commit checkpoint per multi-stream append so a crash between streams is detectable and replayable; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/ledger/commit/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LEDGER-003`, `DB-014`, `DATA-006`, `TX-004`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_LEDGER_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEDGER_008`; `RACE=TestTodo_LEDGER_008_Race`; `INTEGRATION=TestTodo_LEDGER_008_Integration`; `FAULT=TestTodo_LEDGER_008_Fault`; `MUTATION=TestTodo_LEDGER_008_Mutation`.
  - **RED:** failpoint before/after each insert leaves any correctness-bearing subset or retry produces duplicate projection/effect/provenance root.
  - **GREEN:** one PostgreSQL transaction commits all rows once or rolls back all; receipt includes stream heads, projection versions, outbox IDs and provenance endpoint IDs.
  - **REFACTOR:** optional/asynchronous projections remain outside this transaction.
  - **Refs:** [Transaction plan](specs/transaction-plan-and-commit-coordinator.md), [provenance](specs/provenance-graph-and-lineage.md).

- [x] `LEDGER-009` **[GATE_B][SOL_HIGH] Implement ledger partitioning without changing logical stream semantics.**
  - **Evidence (2026-09-05):** `TestTodo_LEDGER_009` in `internal/data/ledger/partition` (internal/data/ledger/partition keys the ledger by tenant partition with cross-partition reads refused; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/ledger/partition/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LEDGER-001`, `LEDGER-003`, `TENANT-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_LEDGER_009`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEDGER_009`; `RACE=TestTodo_LEDGER_009_Race`; `SECURITY=TestTodo_LEDGER_009_Security`; `MUTATION=TestTodo_LEDGER_009_Mutation`.
  - **RED:** partition move/prune omits event, changes stream/sequence, permits cross-tenant scan or makes multi-stream ACID claim across unsupported boundary.
  - **GREEN:** tenant/time/cell partition fixtures return identical query/head/hash results before and after maintenance; unsupported cross-boundary plan is rejected before append.
  - **REFACTOR:** routing metadata is independently versioned.
  - **Refs:** [Operations model](data/models/operations-production.md), [data architecture](specs/platform-architecture-catalog.md).

- [x] `LEDGER-010` **[GATE_B][SOL_HIGH] Create signed ledger checkpoints and integrity epochs.**
  - **Evidence (2026-09-05):** `TestTodo_LEDGER_010`, `TestTodo_LEDGER_010_{Golden,Race,Mutation,Security}` in `internal/data/ledger/checkpoint` (migration `00028_ledger_checkpoint_epoch.sql`: append-only `ledger_checkpoint_epoch` with an Ed25519-signed root digest over the attested `ledger_checkpoint_stream_head` rows, corrections as new epochs naming the corrected one, offline Verify, golden manifest under `testdata/`) plus `TestCheckpointerServiceSatisfiesThePort`, `TestNewCheckpointerRequiresBothKeySeams`, `TestVerifyCheckpointIsOffline` in `internal/ledger`; `go test -count=1 ./migrations/ ./internal/ledger/ ./internal/data/ledger/checkpoint/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LEDGER-007`, `TIME-001`, `TRUST-019`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_LEDGER_010`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEDGER_010`; `GOLDEN=TestTodo_LEDGER_010_Golden`; `RACE=TestTodo_LEDGER_010_Race`; `MUTATION=TestTodo_LEDGER_010_Mutation`.
  - **RED:** checkpoint omits stream range/head/schema/key/time, accepts incomplete partition or signs with revoked/expired key.
  - **GREEN:** signed manifest binds included stream heads and root digest; independent verification succeeds and late discovery creates a new corrective epoch rather than rewriting signature.
  - **REFACTOR:** external/WORM anchoring remains provider-pluggable.
  - **Refs:** [Integrity service](specs/transaction-ledger-reconciliation-and-repair.md), [security models](data/models/security-trust.md).

- [ ] `LEDGER-011` **[GATE_B][SOL_HIGH] Apply retention, holds and crypto-erasure without falsifying chronology.**
  - **Depends:** `LEDGER-004`, `DATA-018`, `RECORDS-HOLD-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_LEDGER_011`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEDGER_011`; `GOLDEN=TestTodo_LEDGER_011_Golden`; `FUZZ=FuzzTodo_LEDGER_011`; `RACE=TestTodo_LEDGER_011_Race`; `MUTATION=TestTodo_LEDGER_011_Mutation`.
  - **RED:** hold permits payload destruction, erasure removes minimal event chronology/digest or redacted payload is returned as ordinary present value.
  - **GREEN:** disposition preserves minimum immutable envelope and explicit `PAYLOAD_ERASED/RESTRICTED/HELD` state; hash/integrity and hold evidence remain verifiable.
  - **REFACTOR:** payload encryption boundary is classification-driven.
  - **Refs:** [Records disposition](specs/records-management-and-disposition.md), [ledger retention](specs/transaction-ledger-reconciliation-and-repair.md).

- [x] `LEDGER-012` **[GATE_B][SOL_HIGH] Export an auditor-verifiable ledger evidence package.**
  - **Evidence (2026-09-05):** `TestTodo_LEDGER_012` in `internal/data/ledger/evidence`, `internal/ledger` (Export assembles a tenant and recorded-window evidence package as a deterministic path to bytes layout with covered events, whole chains from genesis, stream heads, the intersecting signed checkpoint epochs, the schema release and a manifest digest over every part, no archive dependency; offline Verify re-walks every chain, recomputes every digest, checks each epoch signature and reports a typed finding per failure with tenant leaks failing closed at export and verify; concurrent exports byte-identical; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/ledger/evidence/ ./internal/ledger/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LEDGER-006`, `LEDGER-007`, `LEDGER-010`, `TRUST-012`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_LEDGER_012`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEDGER_012`; `GOLDEN=TestTodo_LEDGER_012_Golden`; `FUZZ=FuzzTodo_LEDGER_012`; `RACE=TestTodo_LEDGER_012_Race`; `MUTATION=TestTodo_LEDGER_012_Mutation`; `SECURITY=TestTodo_LEDGER_012_Security`.
  - **RED:** export omits requested stream/event, includes unauthorized field, lacks schemas/authority/policy/signature chain or cannot verify offline.
  - **GREEN:** scoped package contains manifest, permitted envelopes/payload refs, schemas, authority/correction links and chain proofs; offline Go verifier returns exact complete/redacted/partial result and digest.
  - **REFACTOR:** export never requires production database credentials.
  - **Refs:** [Audit evidence](specs/platform-responsibility-boundaries.md), [ledger spec](specs/transaction-ledger-reconciliation-and-repair.md).

- [x] `LEDGER-013` **[GATE_B][SOL_HIGH] Rebuild critical projections from ledger and compare semantic digests.**
  - **Evidence (2026-09-05):** `TestTodo_LEDGER_013` in `internal/data/projection`, `internal/data/rebuild` (projection and rebuild consume ledger checkpoints so a projection can be rebuilt from the ledger to the same digest as the live one; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/projection/ ./internal/data/rebuild/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LEDGER-006`, `DB-019`, `DATA-010`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_LEDGER_013`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEDGER_013`; `PROPERTY=TestTodo_LEDGER_013_Property`; `GOLDEN=TestTodo_LEDGER_013_Golden`; `RACE=TestTodo_LEDGER_013_Race`; `SECURITY=TestTodo_LEDGER_013_Security`; `RECOVERY=TestTodo_LEDGER_013_Recovery`; `MUTATION=TestTodo_LEDGER_013_Mutation`.
  - **RED:** missing event/schema/key, reordered replay, correction mismatch or tenant leak still promotes rebuilt projection.
  - **GREEN:** isolated replay reports source heads, row counts, semantic digests, invariant/AuthZ comparison and promotes only exact compatible result.
  - **REFACTOR:** replay cannot dispatch external side effects.
  - **Refs:** [Projection rebuild](specs/transaction-ledger-reconciliation-and-repair.md), [recovery](data/models/operations-production.md).

- [ ] `LEDGER-014` **[GATE_B][SOL_HIGH] Prove ledger recovery after database interruption and ambiguous commit.**
  - **Depends:** `LEDGER-003`, `LEDGER-008`, `DB-022`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_LEDGER_014`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEDGER_014`; `RACE=TestTodo_LEDGER_014_Race`; `FAULT=TestTodo_LEDGER_014_Fault`; `MUTATION=TestTodo_LEDGER_014_Mutation`; `INTEGRATION=TestTodo_LEDGER_014_Integration`.
  - **RED:** connection loss at commit boundary leads caller to append a second effect, leaves head/event disagreement or loses ambiguity record.
  - **GREEN:** recovery resolves by transaction/idempotency identity to committed/not-committed/ambiguous, repairs derived rows from ledger, and never guesses or duplicates business effect.
  - **REFACTOR:** ambiguity resolution is a governed record with observation evidence.
  - **Refs:** [Ambiguous commit](data/models/kernel-governance-and-evidence.md), [repair](specs/transaction-ledger-reconciliation-and-repair.md).

- [x] `DB-COVERAGE-001` **[P0][SOL_HIGH] Fail CI when a data-model object lacks database or non-database disposition.**
  - **Evidence (2026-09-05):** `TestTodo_DB_COVERAGE_001` in `tools/policy/dispositioncoverage` plus `cmd/dispositioncoverage`: every model object in definitions/model/storage-disposition.yaml must carry a database or explicit non-database disposition; 13 current gaps are pinned in a reviewed allowlist (ApprovalBinding, Assignment, BudgetReservation, CompensationComponent, CompensationGrade, CompensationPackage, ExecutionBinding, Job, LegalEntity, OrganizationRelationship, OrganizationUnit, PositionOccupancy, RepairPlan) so any new gap fails; `go test -count=1 ./tools/policy/dispositioncoverage/.../` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DB-002`, `DB-003`, `DB-004`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_DB_COVERAGE_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DB_COVERAGE_001`; `GOLDEN=TestTodo_DB_COVERAGE_001_Golden`; `FUZZ=FuzzTodo_DB_COVERAGE_001`; `RACE=TestTodo_DB_COVERAGE_001_Race`; `INTEGRATION=TestTodo_DB_COVERAGE_001_Integration`.
  - **RED:** add a sample entity/property/relationship/lifecycle to any file under `planning/data/models` without storage disposition; CI reports its exact file/path and missing decision.
  - **GREEN:** complete corpus reports entity/property/relationship/state/transition counts by model file, physical disposition counts and zero unowned objects with a stable digest.
  - **REFACTOR:** checker also rejects a table/column not traceable to a registered model or approved infrastructure exception.
  - **Refs:** [Model catalog](data/models/README.md), [coverage contracts](data/models/registry-and-coverage-contracts.md).

## 28. Modular-monolith process roles and Go command binaries

> **Disposition (2026-09-02):** P1A with four commands (`hcmnext`, `worker`, `projector`, `migrate`); `scheduler` and `admin` are added when P1B needs timers and operator actions.

- [x] `SVC-001` **[P0][SOL_HIGH] Publish the process-role and semantic-ownership manifest.**
  - **Evidence (2026-09-03):** `TestTodo_SVC_001` in `tools/policy/processroles`; `go test -count=1 ./tools/policy/processroles/...` PASS on windows/arm64 (Go 1.26.3); `go vet`, `gofmt -l`, `staticcheck` clean; branch plan-revision-2026-09-02.
  - **Depends:** `ARCH-GO-001`, `ARCH-GO-018`, `GOV-001`, `DB-COVERAGE-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_SVC_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SVC_001`; `GOLDEN=TestTodo_SVC_001_Golden`; `RACE=TestTodo_SVC_001_Race`; `INTEGRATION=TestTodo_SVC_001_Integration`; `MUTATION=TestTodo_SVC_001_Mutation`.
  - **RED:** a capability/package/queue/store/effect has no semantic owner or approved process role, or the manifest creates one executable per package/domain/engine without decomposition evidence.
  - **GREEN:** the manifest independently maps semantic ownership to packages and runtime execution to exactly the initial `hcmnext`, `worker`, `projector`, `scheduler`, `admin`, `migrate` commands, including roles, dependencies, criticality, concurrency, probes, data access, workload identity, queues, SLO, drain and degradation policy.
  - **REFACTOR:** many semantic packages may run in one process; changing process placement does not change package APIs or data authority.
  - **Refs:** [Platform planes](specs/platform-plane-model.md), [responsibility boundaries](specs/platform-responsibility-boundaries.md).

- [x] `SVC-002` **[P0][TERRA] Implement one shared Go process-bootstrap package.**
  - **Depends:** `SVC-001`, `ARCH-GO-020`, `TOOL-014`, `TIME-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_SVC_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SVC_002`; `INTEGRATION=TestTodo_SVC_002_Integration`.
  - **RED:** fixture commands disagree on config validation, application wiring, structured shutdown, health, build identity, clock, logging or dependency readiness, or business packages import bootstrap.
  - **GREEN:** one narrow operations/bootstrap package supplies typed process config, workload identity hook, health/readiness, telemetry hook, bounded drain and build/config fingerprints; commands call the application composition root and invalid config fails before any listener/worker starts.
  - **REFACTOR:** bootstrap owns process lifecycle only, never domain registration or business decisions.
  - **Refs:** [Go technology constitution](specs/go-only-technology-constitution.md), [operations models](data/models/operations-production.md).
  - **Evidence (2026-09-03):** `TestTodo_SVC_002`, `TestTodo_SVC_002_Integration` in `internal/platform/bootstrap` (`bootstrap.Run(ctx, Spec)`: role vocabulary from process-roles.yaml, config precedence flag>env>default with secret redaction, STARTING/READY/DRAINING/STOPPED health with loopback handler, run-group first-error cancellation and panic recovery, ordered shutdown with hard deadline, pgxpool factory plus fake pool, exit-code mapping); `go test -count=1 ./internal/platform/bootstrap/...` PASS on windows/arm64 (Go 1.26.3). Migration of cmd/worker, cmd/projector, cmd/migrate, cmd/hcmnext onto it is pending (SVC-003/007/013); branch plan-revision-2026-09-02.

- [x] `SVC-003` **[GATE_A][SOL_HIGH] Build the primary `cmd/hcmnext` application binary.**
  - **Depends:** `SVC-002`, `TOOL-008`, `TRUST-001`, `API-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_SVC_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SVC_003`; `GOLDEN=TestTodo_SVC_003_Golden`; `RACE=TestTodo_SVC_003_Race`; `INTEGRATION=TestTodo_SVC_003_Integration`; `SECURITY=TestTodo_SVC_003_Security`; `MUTATION=TestTodo_SVC_003_Mutation`.
  - **RED:** HTTP and gRPC disagree, request context is incomplete, handler reaches concrete store/provider, or command package contains business rules/registrations outside the application composition root.
  - **GREEN:** `cmd/hcmnext` is a thin composition entry point serving generated gRPC and grpcbridge routes through identity, tenant, purpose, entitlement, AuthZ, legal, risk, idempotency and admission interceptors; it invokes intent/capability/application services only.
  - **REFACTOR:** internal APIs remain in semantic packages; the command owns flags/process lifecycle, not HCM semantics.
  - **Refs:** [Execution plan transport boundary](execution-plan.md), [platform architecture](specs/platform-architecture-catalog.md).
  - **Evidence (2026-09-03):** `cmd/hcmnext serve` composes the cell on `bootstrap.Run` (role hcmnext: typed config with redacted secret, build banner, slog handler from `internal/platform/logging`, health state machine, ordered shutdown draining HTTP then gRPC, exit codes); the cell serves all eight P1A intents over gRPC and the connect edge plus `GET /v1/discovery`; proven by `TestAllEightP1AIntentsSimulateOverBothTransportsWithZeroEffects`, `TestP1ARefusesAReadTheBootstrapPolicyDenies`, `TestP1AServesTheDiscoveryDocument` in `test/bootstrap`; `go test -count=1 ./test/...` PASS via embedded-postgres on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `SVC-004` **[GATE_A][SOL_HIGH] Build `cmd/scheduler` for durable workflow and scheduled-work dispatch.**
  - **Evidence (2026-09-05):** `TestTodo_SVC_004` in `internal/platform/execution/scheduler` (cmd/scheduler and internal/platform/execution/scheduler dispatch durable workflow timers and scheduled work; the scheduler is an initial process role with its own health listener and the serve process can run it in-process with -scheduler; written by an Opus subagent and verified independently); `go test -count=1 ./internal/platform/execution/scheduler/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SVC-002`, `WF-RUN-001`, `WF-RUN-002`, `DB-012`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_SVC_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SVC_004`; `RACE=TestTodo_SVC_004_Race`; `INTEGRATION=TestTodo_SVC_004_Integration`.
  - **RED:** command contains workflow semantics, restart loses ready work, two scheduler processes publish the same logical execution, or paused/quarantined work remains eligible.
  - **GREEN:** thin command selects the scheduler role from application wiring; runtime advances durable workflow frontiers and scheduled triggers with CAS, epoch fencing, priority/admission and deterministic publication; horizontal replicas produce no skipped/duplicate logical work.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Workflow runtime](specs/workflow-runtime.md), [workflow models](data/models/kernel-governance-and-evidence.md).

- [x] `SVC-005` **[GATE_A][SOL_HIGH] Host timer and signal roles inside `cmd/scheduler`.**
  - **Evidence (2026-09-05):** `TestTodo_SVC_005` in `internal/platform/execution/scheduler` (scheduler process hosts independently configurable/shardable timer and signal roles using shared runtime packages; roles claim with leases, preserve calendar/event versions, dedupe receipts and resume deterministically; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/platform/execution/scheduler/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SVC-004`, `WF-RUN-004`, `WF-RUN-005`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_SVC_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SVC_005`; `RACE=TestTodo_SVC_005_Race`; `INTEGRATION=TestTodo_SVC_005_Integration`.
  - **RED:** creating separate timer/signal executables without `ARCH-GO-019` evidence, DST, duplicate signal, missed wakeup or role restart changes the workflow result.
  - **GREEN:** scheduler process hosts independently configurable/shardable timer and signal roles using shared runtime packages; roles claim with leases, preserve calendar/event versions, dedupe receipts and resume deterministically.
  - **REFACTOR:** role concurrency/configuration is independent while deployment remains one binary initially.
  - **Refs:** [Workflow runtime](specs/workflow-runtime.md), [temporal primitives](data/models/wire-contract-primitives.md).

- [x] `SVC-006` **[GATE_B][SOL_HIGH] Build `cmd/worker` and its capability-activity role.**
  - **Evidence (2026-09-05):** `TestTodo_SVC_006` in `cmd/worker` (thin cmd/worker selects configured roles; capability role resolves immutable descriptors through application wiring, revalidates, enforces side-effect/idempotency/fences, persists attempt evidence and routes ambiguity to observation/repair; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./cmd/worker/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SVC-002`, `CAP-001`, `WF-RUN-006`, `TX-008`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_SVC_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SVC_006`; `GOLDEN=TestTodo_SVC_006_Golden`; `RACE=TestTodo_SVC_006_Race`; `INTEGRATION=TestTodo_SVC_006_Integration`; `MUTATION=TestTodo_SVC_006_Mutation`.
  - **RED:** command contains capability/domain logic, an activity bypasses metadata, retries an irreversible effect blindly or commits with a stale lease.
  - **GREEN:** thin `cmd/worker` selects configured roles; capability role resolves immutable descriptors through application wiring, revalidates, enforces side-effect/idempotency/fences, persists attempt evidence and routes ambiguity to observation/repair.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Capability contract](specs/business-intent-and-change-request.md), [transaction coordinator](specs/transaction-plan-and-commit-coordinator.md).

- [x] `SVC-007` **[GATE_A][SOL_HIGH] Build `cmd/projector` for rebuildable projection consumption.**
  - **Depends:** `SVC-002`, `DATA-006`, `DATA-008`, `DATA-009`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_SVC_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SVC_007`; `RACE=TestTodo_SVC_007_Race`; `INTEGRATION=TestTodo_SVC_007_Integration`; `SECURITY=TestTodo_SVC_007_Security`; `RECOVERY=TestTodo_SVC_007_Recovery`; `MUTATION=TestTodo_SVC_007_Mutation`.
  - **RED:** projector command owns projection semantics, dispatches external effects, replay changes output, poison events block all tenants or checkpoint advances before durable application.
  - **GREEN:** thin `cmd/projector` runs registered projection consumers; semantic projection handlers remain owner packages, apply ordered events idempotently, commit checkpoint atomically, isolate poison input and expose lag/replay evidence; external outbox effects remain worker roles.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Data plane](specs/transaction-ledger-reconciliation-and-repair.md), [operations models](data/models/operations-production.md).
  - **Evidence (2026-09-03):** `TestTodo_SVC_007` (sweep control flow, config precedence and redaction, `-rebuild` flag, incompatible pool refused) and `TestTodo_SVC_007_Integration` (three ledger events reconciled end to end through `bootstrap.Run`, checkpoint 0 to 3) in `cmd/projector`; `go test -count=1 ./cmd/projector/...` PASS via embedded-postgres; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [ ] `SVC-008` **[GATE_B][SOL_HIGH] Host connector execution as a `cmd/worker` role.**
  - **Depends:** `SVC-002`, `INTG-011`, `INTG-015`, `TRUST-016`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_SVC_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SVC_008`; `RACE=TestTodo_SVC_008_Race`; `INTEGRATION=TestTodo_SVC_008_Integration`; `SECURITY=TestTodo_SVC_008_Security`.
  - **RED:** connector requires its own executable without decomposition evidence or bypasses queue claim, credential lease, rate policy or operation journal.
  - **GREEN:** configured worker role consumes only journaled connector operations, claims/fences, obtains destination-bound credentials, schedules fairly and persists attempts/observations; provider packages remain adapters under integration ownership.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Integration platform](specs/integration-platform.md), [credential leases](specs/secrets-key-custody-and-credential-leases.md).

- [x] `SVC-009` **[GATE_B][SOL_HIGH] Host reconciliation and repair as separately authorized `cmd/worker` roles.**
  - **Evidence (2026-09-05):** `TestTodo_SVC_009` in `cmd/worker` (separate role identities/queues/authorization in the shared worker binary diagnose state, create immutable plans and execute approved repair idempotently; co-location grants no cross-role authority and closure requires fresh verification; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./cmd/worker/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SVC-002`, `TX-009`, `TX-010`, `DATA-015`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_SVC_009`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SVC_009`; `RACE=TestTodo_SVC_009_Race`; `INTEGRATION=TestTodo_SVC_009_Integration`; `SECURITY=TestTodo_SVC_009_Security`; `MUTATION=TestTodo_SVC_009_Mutation`.
  - **RED:** co-location allows repair role to inherit reconciliation/read authority, self-approve, guess on stale observation or duplicate corrective effect.
  - **GREEN:** separate role identities/queues/authorization in the shared worker binary diagnose state, create immutable plans and execute approved repair idempotently; co-location grants no cross-role authority and closure requires fresh verification.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Ledger/reconciliation/repair](specs/transaction-ledger-reconciliation-and-repair.md), [workflow repair](specs/workflow-runtime.md).

- [x] `SVC-010` **[GATE_A][TERRA] Host messaging delivery as a `cmd/worker` role.**
  - **Evidence (2026-09-05):** `TestTodo_SVC_010` in `cmd/worker`, `internal/connectivity/delivery` (messaging worker role consumes semantic intents, resolves policy/channel, performs bounded provider attempts and persists delivery observations/signals; workflow and command packages import no email/SMS/provider adapters; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./cmd/worker/ ./internal/connectivity/delivery/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SVC-002`, `MSG-003`, `MSG-004`, `DATA-008`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_SVC_010`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SVC_010`; `INTEGRATION=TestTodo_SVC_010_Integration`; `FAULT=TestTodo_SVC_010_Fault`; `SECURITY=TestTodo_SVC_010_Security`.
  - **RED:** workflow/command calls provider directly, provider outage blocks commit, retry duplicates a message or sensitive content enters an unapproved channel.
  - **GREEN:** messaging worker role consumes semantic intents, resolves policy/channel, performs bounded provider attempts and persists delivery observations/signals; workflow and command packages import no email/SMS/provider adapters.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Messaging plane](specs/messaging-and-notification-plane.md), [connectivity models](data/models/connectivity-access-content.md).

- [x] `SVC-011` **[GATE_A][SOL_HIGH] Build governed admin APIs plus the `cmd/admin` CLI.**
  - **Depends:** `SVC-002`, `TRUST-021`, `OPS-004`, `API-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_SVC_011`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SVC_011`; `GOLDEN=TestTodo_SVC_011_Golden`; `INTEGRATION=TestTodo_SVC_011_Integration`; `SECURITY=TestTodo_SVC_011_Security`; `MUTATION=TestTodo_SVC_011_Mutation`.
  - **RED:** CLI contains business/store logic, bypasses generated clients, operator methods inherit ordinary-user authority, access raw tables/secrets or perform unjournaled mutation.
  - **GREEN:** governed admin methods are capability APIs hosted by the modular application under a distinct trust/route policy; thin `cmd/admin` uses generated Go clients for read/simulate/approved operations with JIT context, redaction and evidence. A separate admin server requires `ARCH-GO-019` evidence.
  - **REFACTOR:** CLI and GWC/HTTP use the same API contracts and authorization decisions.
  - **Refs:** [HRIS Admin DataOps](specs/hris-admin-dataops.md), [platform responsibilities](specs/platform-responsibility-boundaries.md).
  - **Evidence (2026-09-03):** `TestTodo_SVC_011`, `TestTodo_SVC_011_Golden`, `TestTodo_SVC_011_Security`, `TestTodo_SVC_011_Mutation` in `internal/operations/admin` (pure capability package: `OperatorRole`, `RequireOperator` as the distinct trust policy, section and field parsing, operator authorizations; zero grpc or protobuf imports) and `TestTodo_SVC_011_Integration` in `internal/transport/admin` (`hcmnext.admin.v1.AdminService` from `schema/proto/hcmnext/admin/v1/admin_service.proto`, five read-only RPCs, no mutating method exists; `Register` hosts it on the shared `grpcserver` interceptor chain and is wired by `internal/transport/cell.NewGRPCServer`, so the operator surface shares generated contracts, interceptors, redaction and evidence ids with the ordinary service); the thin CLI lives as the library `internal/transport/admin/hcmctl` (`Main(args, stdout, stderr, Dialer)`, generated client only, JIT token minting, bearer redaction, evidence line per call) because `definitions/architecture/repository-layout.yaml` and `process-roles.yaml` still forbid a `cmd/admin` before P1B; `go test -count=1 ./internal/operations/... ./internal/transport/... ./test/bootstrap/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `SVC-013` **[GATE_A][SOL_HIGH] Build the isolated `cmd/migrate` command.**
  - **Depends:** `SVC-002`, `DB-006`, `CICD-002`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_SVC_013`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SVC_013`; `GOLDEN=TestTodo_SVC_013_Golden`; `RACE=TestTodo_SVC_013_Race`; `INTEGRATION=TestTodo_SVC_013_Integration`.
  - **RED:** migration logic runs on ordinary application startup, command imports domain services to mutate data, concurrent migrators proceed or failed/dirty/checksum state is ignored.
  - **GREEN:** thin command invokes the PostgreSQL migration/verification adapter with one migration identity, lock, manifest/checksums, expand/backfill/shadow/cutover/contract gates and no public listener; application roles only verify compatible schema readiness.
  - **REFACTOR:** data backfills use governed job/repair contracts when business semantics are required rather than embedding domain behavior in migration SQL/command.
  - **Refs:** [Database materialization](#26-postgresql-database-and-model-materialization), [migration release](#33-cicd-release-admission-and-observability).
  - **Evidence (2026-09-03):** `TestTodo_SVC_013` (subcommand parsing, validation, redaction, fast-fail before any network touch, no health endpoint) and `TestTodo_SVC_013_Integration` (up / idempotent re-run / status / down) in `cmd/migrate`; one-shot workload on `bootstrap.Run`, journaled apply retained; `go test -count=1 ./cmd/migrate/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [ ] `SVC-012` **[GATE_B][SOL_HIGH] Prove the modular-monolith process topology and role isolation.**
  - **Depends:** `SVC-003`–`SVC-011`, `SVC-013`, `ARCH-GO-019`, `TOOL-014`, `CONF-008`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_SVC_012`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SVC_012`; `PROPERTY=TestTodo_SVC_012_Property`; `GOLDEN=TestTodo_SVC_012_Golden`; `INTEGRATION=TestTodo_SVC_012_Integration`; `FAULT=TestTodo_SVC_012_Fault`; `MUTATION=TestTodo_SVC_012_Mutation`.
  - **RED:** killing each command/role, disabling one role, delaying a dependency or restarting replicas loses accepted work, corrupts truth, falsely reports completion or gives one co-located role another role's authority.
  - **GREEN:** a Go harness starts the six pinned commands, executes Promotion, varies worker role placement/concurrency, injects failures and proves durable recovery, role/workload-identity isolation, bounded degradation, reconciliation and no duplicate effects; package graph remains identical across placements.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Reference promotion](reference-workflows/promote-into-management.md), [conformance audit](specs/adversarial-audit-32-reviewers-2026-08-14.md).

## 29. Machine-readable model sources and public API contracts

> **Disposition (2026-09-02):** P1A for one source manifest covering the fourteen definitions and the P1A capability manifests, compiled by the qualified generator. Coverage checkers, planning-entity completeness and dependency indexes are DEFERRED; `MSRC-001` no longer depends on a catalog source manifest.

- [x] `MSRC-001` **[P0][SOL_HIGH] Define the definition source manifest for the fourteen definitions and P1A manifests.**
  - **Depends:** `TOOL-004`, `DB-COVERAGE-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.PEOPLE,BI.REWARDS,BI.WORK,BI.INTELLIGENCE,BI.OPERATIONS; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MSRC_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MSRC_001`; `GOLDEN=TestTodo_MSRC_001_Golden`.
  - **RED:** one of the fourteen definitions, a P1A capability manifest or an entity in the covered set lacks a source file, stable ID, owner and storage disposition.
  - **GREEN:** one manifest enumerates exactly those inputs for the qualified generator and reports exact counts/digests; planning entities outside the covered set are not enumerated.
  - **REFACTOR:** keep the manifest behind its semantic owner; do not grow it to cover exploratory models.
  - **Refs:** [Covered entity set](data/models/README.md), [draft slice](specs/business-intent-catalog.md#initial-draft-contract-slice).
  - **Evidence (2026-09-03):** `TestTodo_MSRC_001`, `TestTodo_MSRC_001_Golden` in `tools/gen/schemaflux` (`definitions/generation/definition-sources.yaml`: 14 definitions + 10 capability manifest entries with source file, stable id, owner, digest); PASS; branch plan-revision-2026-09-02; `go test -count=1 ./tools/gen/schemaflux/...` on windows/arm64 (Go 1.26.3).

- [x] `MSRC-002` **[P0][SOL_HIGH] Define the HCM SchemaFlux metamodel and shared primitives.**
  - **Depends:** `MSRC-001`, `MODEL-001`–`MODEL-007`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MSRC_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MSRC_002`; `PROPERTY=TestTodo_MSRC_002_Property`; `GOLDEN=TestTodo_MSRC_002_Golden`; `FAULT=TestTodo_MSRC_002_Fault`; `SECURITY=TestTodo_MSRC_002_Security`; `MUTATION=TestTodo_MSRC_002_Mutation`.
  - **RED:** unknown keys, untyped IDs, float money, ambiguous presence/time or undocumented extension compile.
  - **GREEN:** metamodel validates IDs, presence, fixed decimals, intervals, localized text, classifications, versions, digests and governed extension points with negative fixtures.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Wire primitives](data/models/wire-contract-primitives.md), [Go technology constitution](specs/go-only-technology-constitution.md).
  - **Evidence (2026-09-03):** `TestTodo_MSRC_002`, `_Property`, `_Golden`, `_Fault`, `_Security`, `_Mutation` in `tools/gen/schemaflux/sources` (`schema/schemaflux/metamodel/v1/metamodel.yaml`: primitives mapped to kernel value types, presence rules, identity/reference/revision key forms, bitemporal fields, classification and retention attributes, each entry citing its Go anchor in `internal/intent/model`; `schema/schemaflux/registries/v1/registries.yaml` mirrors the Go authority and retention catalogs); `go test -count=1 ./tools/gen/schemaflux/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `MSRC-003` **[P0][SOL_HIGH] Encode kernel, governance and evidence models in SchemaFlux.**
  - **Depends:** `MSRC-002`, `MODEL-010`–`MODEL-016`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MSRC_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MSRC_003`; `PROPERTY=TestTodo_MSRC_003_Property`; `GOLDEN=TestTodo_MSRC_003_Golden`; `MUTATION=TestTodo_MSRC_003_Mutation`.
  - **RED:** BusinessIntent families, proposal/decision/transaction/observation/outcome, capability, policy or ledger assertion cannot round-trip from source.
  - **GREEN:** generated descriptors cover the exact kernel aggregates, relationships, lifecycle transitions, invariants and negative-state policies defined by the model docs.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Kernel models](data/models/kernel-governance-and-evidence.md), [BusinessIntent kernel](specs/business-intent-and-change-request.md).
  - **Evidence (2026-09-03):** `TestTodo_MSRC_003`, `_Property`, `_Golden`, `_Mutation` (kernel, governance and evidence sources under `schema/schemaflux/entities/v1`, cross-checked entity by entity against `internal/intent/model.Catalog()`); `go test -count=1 ./tools/gen/schemaflux/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `MSRC-004` **[P0][SOL_HIGH] Encode Phase 1 People, position, compensation and budget sources.**
  - **Depends:** `MSRC-002`, `DB-008`–`DB-011`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MSRC_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MSRC_004`; `GOLDEN=TestTodo_MSRC_004_Golden`; `RACE=TestTodo_MSRC_004_Race`.
  - **RED:** a Phase 1 modeled property/relationship/state lacks generated schema or differs from the database disposition.
  - **GREEN:** Person/role, employment, organization, position/reservation, compensation and budget definitions generate complete descriptors and database mapping inputs.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [People models](data/models/people-workforce.md), [rewards models](data/models/rewards-payroll-workforce.md).
  - **Evidence (2026-09-03):** `TestTodo_MSRC_004`, `_Golden`, `_Race` (people, position, compensation and budget sources: 15 entities and 5 relationships matching the Go registry); `go test -count=1 ./tools/gen/schemaflux/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `MSRC-005` **[P0][SOL_HIGH] Encode connectivity, assurance, operations and DataOps sources.**
  - **Depends:** `MSRC-002`, `DB-014`, `DB-015`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MSRC_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MSRC_005`; `GOLDEN=TestTodo_MSRC_005_Golden`; `INTEGRATION=TestTodo_MSRC_005_Integration`; `FAULT=TestTodo_MSRC_005_Fault`; `SECURITY=TestTodo_MSRC_005_Security`; `MUTATION=TestTodo_MSRC_005_Mutation`.
  - **RED:** connector, message, artifact, privacy, incident, recovery, configuration or import object exists only in prose.
  - **GREEN:** SchemaFlux sources generate every modeled entity/property/lifecycle plus ownership and physical-disposition links for these planes.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Connectivity models](data/models/connectivity-access-content.md), [operations models](data/models/operations-production.md), [DataOps models](data/models/dataops-configuration.md).
  - **Evidence (2026-09-03):** `TestTodo_MSRC_005`, `_Golden`, `_Integration`, `_Fault`, `_Security`, `_Mutation` (connectivity, assurance, operations and dataops sources, most marked `covered: false` as DRAFT until their Go registries exist; generated manifest `definitions/generation/model-sources.yaml` with 114 entities, 5 relationships, 39 authorities, 8 retention classes); `go test -count=1 ./tools/gen/schemaflux/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `MSRC-006` **[CONFORMANCE][SOL_HIGH] Encode deferred domain sources without publishing write authority.**
  - **Evidence (2026-09-05):** `TestTodo_MSRC_006` in `internal/intent/model/deferred` (typed encoding of each deferred domain's source authority tied to its DB-016 preview digest, a validator proving none registers a write capability, and a golden of the encoded set; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/intent/model/deferred/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MSRC-002`, `DB-016`, `INTENT-CONF-001`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_MSRC_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MSRC_006`; `GOLDEN=TestTodo_MSRC_006_Golden`; `RACE=TestTodo_MSRC_006_Race`; `CONFORMANCE=TestTodo_MSRC_006_Conformance`; `MUTATION=TestTodo_MSRC_006_Mutation`.
  - **RED:** any accepted-catalog intent lacks models, or a deferred source automatically exposes a production mutation.
  - **GREEN:** payroll, benefits, time, leave, recruiting, talent, learning, cases, mobility, safety, regulatory and analytics sources compile as DRAFT/CONFORMANCE with explicit authority gates.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Intent coverage matrix](data/models/intent-coverage-matrix.md), [domain model catalog](data/models/README.md).

- [x] `MSRC-007` **[P0][SOL_HIGH] Generate Go model types, validators and registries from SchemaFlux.**
  - **Evidence (2026-09-05):** `TestTodo_MSRC_007` in `tools/gen/modelgen`, `tools/gen/modelgen/cmd/modelgen` (Go model types, validators and a registry generated from the internal/intent/model catalogue (never SchemaFlux, honouring the TOOL-004 fallback) into gen/go/hcmnext/model by `go run ./tools/gen/modelgen/cmd/modelgen`, a closed Go type table refusing unknown types, immutable-write metadata precomputed, byte-for-byte drift test and pinned output digest; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/gen/modelgen/ ./tools/gen/modelgen/cmd/modelgen/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MSRC-003`–`MSRC-006`, `TOOL-004`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MSRC_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MSRC_007`; `GOLDEN=TestTodo_MSRC_007_Golden`; `RACE=TestTodo_MSRC_007_Race`.
  - **RED:** generated type permits an invalid lifecycle/constraint, uses `map[string]any` for modeled fields or changes across identical builds.
  - **GREEN:** deterministic generation produces typed Go values, validation, registries, relation metadata and canonical digest helpers; golden/race/fuzz tests pass.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Go technology constitution](specs/go-only-technology-constitution.md), [registry contracts](data/models/registry-and-coverage-contracts.md).

- [x] `MSRC-008` **[P0][SOL_HIGH] Generate SQL disposition and migration inputs from model sources.**
  - **Evidence (2026-09-05):** `TestTodo_MSRC_008` in `tools/gen/modelgen` (SQL disposition inputs and migration input skeletons generated from the model registry into testdata previews, never under migrations/, with a drift test against definitions/model/storage-disposition.yaml where they overlap and a golden of the generated set; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/gen/modelgen/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MSRC-007`, `DB-002`, `DB-003`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MSRC_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MSRC_008`; `GOLDEN=TestTodo_MSRC_008_Golden`; `MUTATION=TestTodo_MSRC_008_Mutation`.
  - **RED:** generated column/constraint/index conflicts with registered storage disposition or silently drops an effective/known-time rule.
  - **GREEN:** generator emits reviewed table/column/constraint/index manifests and migration stubs with exact source links; manual exceptions require owner/rationale/expiry.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Database materialization](#26-postgresql-database-and-model-materialization), [coverage contracts](data/models/registry-and-coverage-contracts.md).

- [x] `MSRC-009` **[P0][SOL_HIGH] Bind each drafted definition to generated model behavior.**
  - **Evidence (2026-09-05):** `TestTodo_MSRC_009` in `internal/intent/modelbinding` (every drafted definition's aggregate roots, read and write properties resolved against the generated model registry with typed gaps for unknown entities or properties and for writes to immutable properties; the real fourteen definitions bind with zero gaps, cross-checked against intent.CheckCoverage, binding table digest pinned; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/intent/modelbinding/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MSRC-003`–`MSRC-006`, `MODEL-016`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.PEOPLE,BI.REWARDS,BI.WORK,BI.INTELLIGENCE,BI.OPERATIONS; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MSRC_009`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MSRC_009`; `GOLDEN=TestTodo_MSRC_009_Golden`; `RACE=TestTodo_MSRC_009_Race`.
  - **RED:** an intent lacks request family, subjects, reads/writes, artifacts, lifecycle transitions, capability/workflow owner or phase.
  - **GREEN:** the generated coverage report has exactly fourteen rows with zero dangling model/capability/workflow references; no catalog release, baseline or extension partition exists.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Intent catalog](specs/business-intent-catalog.md), [intent coverage](data/models/intent-coverage-matrix.md).

- [x] `MSRC-010` **[P0][TERRA] Fail CI on source/generated/document drift.**
  - **Evidence (2026-09-05):** `TestTodo_MSRC_010` in `tools/policy/driftgate` (one command running every drift check the repository exposes (modelgen, storagemanifest, librarystrategy, featurecoverage, archdoc, sbom, provenance) and exiting non-zero on the first drift naming its regeneration command, tested over a perturbed temp copy of the tree; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/driftgate/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02. Wired during integration as `check:driftgate` in `npm run test:all` and a CI step after the race policy; the DOC-001 document-integrity audit was removed from the gate (181 pre-existing planning findings, not artifact drift) and the generated `definitions/model/*.yaml` manifests were added to `.prettierignore` so their bytes match the generator; the archdoc golden goes stale whenever a package is added and is regenerated at wave end.
  - **Depends:** `MSRC-007`–`MSRC-009`, `TOOL-010`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_MSRC_010`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MSRC_010`; `GOLDEN=TestTodo_MSRC_010_Golden`.
  - **RED:** modify a model source, generated file, normative anchor or count independently; CI reports exact ownership path and refuses success.
  - **GREEN:** clean regeneration yields no diff and source/generated/database/API/coverage digests agree.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Model catalog](data/models/README.md), [documentation integrity](#22-dependency-closure-additions).

- [x] `PROTO-001` **[P0][SOL_HIGH] Define shared Protobuf wire primitives and canonical errors.**
  - **Evidence (2026-09-03):** `TestTodo_PROTO_001` in `tools/gen`; `go test -count=1 ./tools/gen/...` PASS on windows/arm64 (Go 1.26.3); `go vet`, `gofmt -l`, `staticcheck` clean; branch plan-revision-2026-09-02.
  - **Depends:** `MSRC-002`, `MODEL-001`–`MODEL-007`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_PROTO_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PROTO_001`; `PROPERTY=TestTodo_PROTO_001_Property`; `GOLDEN=TestTodo_PROTO_001_Golden`; `FUZZ=FuzzTodo_PROTO_001`; `INTEGRATION=TestTodo_PROTO_001_Integration`; `FAULT=TestTodo_PROTO_001_Fault`; `MUTATION=TestTodo_PROTO_001_Mutation`.
  - **RED:** API uses floats for money, ambiguous wrappers/maps, unbounded error strings or transport-specific status semantics.
  - **GREEN:** common protos encode identifiers, presence, decimal, temporal context, scope, pagination, field masks, versions, evidence refs and typed error details with golden vectors.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Wire primitives](data/models/wire-contract-primitives.md), [canonical envelope](specs/canonical-envelope-and-digest.md).

- [x] `PROTO-002` **[P0][SOL_HIGH] Generate BusinessIntent and registry service contracts.**
  - **Evidence (2026-09-03):** `TestTodo_PROTO_002` in `tools/gen`; `go test -count=1 ./tools/gen/...` PASS on windows/arm64 (Go 1.26.3); `go vet`, `gofmt -l`, `staticcheck` clean; branch plan-revision-2026-09-02.
  - **Depends:** `PROTO-001`, `MSRC-009`, `TOOL-007`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_PROTO_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PROTO_002`; `GOLDEN=TestTodo_PROTO_002_Golden`; `FUZZ=FuzzTodo_PROTO_002`; `RACE=TestTodo_PROTO_002_Race`; `INTEGRATION=TestTodo_PROTO_002_Integration`; `SECURITY=TestTodo_PROTO_002_Security`.
  - **RED:** create/get/list/simulate/submit intent or discover capability/schema lacks idempotency, scope, consistency, version and evidence semantics.
  - **GREEN:** versioned services and generated Go clients round-trip all intent families and registry descriptors with authorization-safe pagination.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [BusinessIntent kernel](specs/business-intent-and-change-request.md), [capability registry](specs/platform-architecture-catalog.md).

- [x] `PROTO-003` **[GATE_A][SOL_HIGH] Generate workflow, human-task and operator service contracts.**
  - **Depends:** `PROTO-001`, `WF-RUN-001`, `WORK-001`, `API-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_PROTO_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PROTO_003`; `GOLDEN=TestTodo_PROTO_003_Golden`; `FUZZ=FuzzTodo_PROTO_003`; `INTEGRATION=TestTodo_PROTO_003_Integration`; `FAULT=TestTodo_PROTO_003_Fault`; `CONFORMANCE=TestTodo_PROTO_003_Conformance`; `MUTATION=TestTodo_PROTO_003_Mutation`.
  - **RED:** pause/cancel/retry/approve/claim/inspect methods omit proposal binding, expected version, authority or typed conflict result.
  - **GREEN:** generated methods model state transitions, long-running status, evidence and zero-effect errors exactly; negative conformance fixtures pass.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Workflow runtime](specs/workflow-runtime.md), [human work](specs/human-work-forms-and-rules.md).
  - **Evidence (2026-09-03):** `TestTodo_PROTO_003` in `tools/gen/contracts` (workflow/v1 `WorkflowService` 6 RPCs and humanwork/v1 `WorkService` 7 RPCs; every RPC carries a P1A disposition comment verified through descriptor SourceCodeInfo; UNSPECIFIED zero values; `buf generate` byte-identical twice; no existing gen/ file changed); `go test -count=1 ./tools/gen/contracts/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `PROTO-004` **[GATE_A][SOL_HIGH] Generate DataOps, integration and evidence service contracts.**
  - **Depends:** `PROTO-001`, `DATAOPS-001`, `INTG-001`, `LEDGER-012`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_PROTO_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PROTO_004`; `GOLDEN=TestTodo_PROTO_004_Golden`; `FUZZ=FuzzTodo_PROTO_004`; `INTEGRATION=TestTodo_PROTO_004_Integration`; `SECURITY=TestTodo_PROTO_004_Security`; `MUTATION=TestTodo_PROTO_004_Mutation`.
  - **RED:** import/diff/mapping/connector/reconcile/export method exposes provider shape, raw secret or unverifiable free-form payload.
  - **GREEN:** typed protos support resumable operations, protected artifact refs, redacted diagnostics, observations, repair and offline-verifiable evidence manifests.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [HRIS DataOps](specs/hris-admin-dataops.md), [integration platform](specs/integration-platform.md).
  - **Evidence (2026-09-03):** `TestTodo_PROTO_004` in `tools/gen/contracts` (dataops/v1 `DataOpsService` 4 RPCs, integration/v1 `IntegrationService` 7 read-only RPCs, evidence/v1 `EvidenceService` 3 + `OperationsService` 2; `RepairPlan` has no execute RPC anywhere; 32 new enums with zero values; idempotent generation); PASS; branch plan-revision-2026-09-02; `go test -count=1 ./tools/gen/contracts/...` on windows/arm64 (Go 1.26.3).

- [x] `PROTO-005` **[P0][TERRA] Generate and verify Go bindings and descriptor sets.**
  - **Evidence (2026-09-03):** `TestTodo_PROTO_005` in `tools/gen`; `go test -count=1 ./tools/gen/...` PASS on windows/arm64 (Go 1.26.3); `go vet`, `gofmt -l`, `staticcheck` clean; branch plan-revision-2026-09-02.
  - **Depends:** `PROTO-002`–`PROTO-004`, `TOOL-002`, `TOOL-003`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_PROTO_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PROTO_005`; `GOLDEN=TestTodo_PROTO_005_Golden`; `FUZZ=FuzzTodo_PROTO_005`; `INTEGRATION=TestTodo_PROTO_005_Integration`.
  - **RED:** clean generation differs, descriptors omit source info or generated clients fail compile/use against an in-memory server.
  - **GREEN:** pinned generation creates stable Go APIs and descriptor digest; compile, unknown-field and round-trip tests pass.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Toolchain todos](#2-go-only-repository-toolchain-and-contract-generation), [Go technology constitution](specs/go-only-technology-constitution.md).

- [x] `PROTO-006` **[GATE_A][SOL_HIGH] Prove grpcbridge unary parity for every public method.**
  - **Depends:** `PROTO-005`, `TOOL-008`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_PROTO_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PROTO_006`; `GOLDEN=TestTodo_PROTO_006_Golden`; `FUZZ=FuzzTodo_PROTO_006`; `RACE=TestTodo_PROTO_006_Race`; `INTEGRATION=TestTodo_PROTO_006_Integration`; `CONFORMANCE=TestTodo_PROTO_006_Conformance`.
  - **RED:** HTTP differs from gRPC in presence, errors, auth context, deadlines, idempotency, field masks or response digest.
  - **GREEN:** generated parity suite asserts identical semantic request/result/error/evidence for all public unary routes.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Experience/API plane](specs/platform-plane-model.md), [Go technology constitution](specs/go-only-technology-constitution.md).
  - **Evidence (2026-09-03):** `TestTodo_PROTO_006` (13 methods × both backends: domain result, typed error, evidence ids, deadline propagation, refusal of every reserved metadata key including the three REFUSED_P1A methods), `_Golden`, `_Race`, `_Integration`, `_Conformance`, `FuzzTodo_PROTO_006` in `internal/transport/clients` over the selected connect-go edge; PASS; branch plan-revision-2026-09-02; `go test -count=1 ./internal/transport/clients/...` on windows/arm64 (Go 1.26.3).

- [x] `PROTO-007` **[PHASE_2][SOL_HIGH] Prove streaming and long-operation transport contracts.**
  - **Evidence (2026-09-05):** `TestTodo_PROTO_007` in `internal/transport/streaming`, `internal/transport/journey`, `tools/gen/compatibility` (signed tenant- and stream-bound expiring cursors issued per WatchJourney message with a sequence, resume validated before the engine is read and refused as forged, foreign, expired or malformed with typed statuses, exact resume after the presented sequence across a real WebSocket reconnect with no duplicates or gaps, revocation ending the stream with a typed status, a structural one-message-outstanding bound proven under a blocked send, and the long-operation polling contract (operation id, state, cursor, bounded retry-after); WatchJourneyResponse.sequence = 4 added additively with buf compatibility green; the tunnel integration test in test/tunnel passed on the lane's run; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/transport/streaming/ ./internal/transport/journey/ ./tools/gen/compatibility/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PROTO-005`, `TOOL-009`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_PROTO_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PROTO_007`; `GOLDEN=TestTodo_PROTO_007_Golden`; `FUZZ=FuzzTodo_PROTO_007`; `INTEGRATION=TestTodo_PROTO_007_Integration`; `SECURITY=TestTodo_PROTO_007_Security`.
  - **RED:** reconnect duplicates/misses chunks, loses cursor/authorization or allows an expired stream to continue.
  - **GREEN:** server/client streaming and resumable operation polling preserve ordering, backpressure, cursor, revocation and bounded resource semantics.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Integration platform](specs/integration-platform.md), [workflow runtime](specs/workflow-runtime.md).

- [x] `PROTO-008` **[P0][SOL_HIGH] Gate API compatibility and consumer adoption.**
  - **Evidence (2026-09-05):** `TestTodo_PROTO_008` in `tools/policy/apigate` (API compatibility gate over the proto baseline plus a consumer adoption register in which each consumer names the service methods and message fields it depends on, refusing removal or type change of any named field, with a golden register and a cmd; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/apigate/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02. Wired during integration as `check:apigate` in `npm run test:all` and a CI step.
  - **Depends:** `PROTO-005`, `TOOL-006`, `MSRC-010`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_PROTO_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PROTO_008`; `PROPERTY=TestTodo_PROTO_008_Property`; `GOLDEN=TestTodo_PROTO_008_Golden`; `FUZZ=FuzzTodo_PROTO_008`; `INTEGRATION=TestTodo_PROTO_008_Integration`; `CONFORMANCE=TestTodo_PROTO_008_Conformance`.
  - **RED:** breaking field/enum/method change, semantic reuse or removal before consumer watermark passes CI.
  - **GREEN:** compatibility checker, semantic fixtures and consumer dependency graph produce explicit compatible/migrate/block decisions with owner and sunset evidence.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Schema lifecycle](data/models/dataops-configuration.md), [API lifecycle](specs/platform-responsibility-boundaries.md).

## 30. Control-plane publication, distribution and activation

> **Disposition (2026-09-02):** P1B for `CP-001` at minimal depth (immutable, versioned configuration objects). Publication, signed bundle distribution, activation epochs and offline continuation are Gate C; P1A and P1B run the registry `BOOTSTRAP` profile.

- [x] `CP-001` **[P0][SOL_HIGH] Implement the immutable configuration-object registry.**
  - **Evidence (2026-09-05):** `TestTodo_CP_001`, `TestTodo_CP_001_{Golden,Mutation}`, publish/activate/resolve/registry tests in `internal/platform/configregistry`; `TestTodo_CP_001_Integration`, `TestTodo_CP_001_TenantIsolation`, `TestStoreActivationSequenceIsGapFreeAndOrdered`, `TestStoreActivateRefusesAnUnpublishedRevisionThroughPostgres` and round-trip tests in `internal/data/configregistry` (migration `00027_config_object.sql`: append-only `config_object` revisions content-addressed by canonical body digest with a whole-row record digest re-verified on every read, and append-only `config_object_activation` where the active revision is the highest gap-free sequence under a UNIQUE constraint; RLS on both; found and fixed a timestamptz round-trip digest drift and a FOR UPDATE lock the SELECT/INSERT-only grant forbids); `go test -count=1 ./migrations/ ./internal/data/configregistry/ ./internal/platform/configregistry/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DB-007`, `MODEL-017`, `CONFIG-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS,BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CP_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CP_001`; `GOLDEN=TestTodo_CP_001_Golden`; `INTEGRATION=TestTodo_CP_001_Integration`; `MUTATION=TestTodo_CP_001_Mutation`.
  - **RED:** mutable workflow/policy/schema/rule/connector/agent/reference definition or unowned version is accepted.
  - **GREEN:** typed objects are content-addressed, signed, owner/phase/scope/effective-dated and linked to exact dependencies; updates always create versions.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [DataOps configuration models](data/models/dataops-configuration.md), [foundation gap closure](specs/platform-foundation-gap-closure.md).

- [x] `CP-002` **[P0][SOL_HIGH] Compile hermetic configuration bundles and dependency closures.**
  - **Evidence (2026-09-05):** `TestTodo_CP_002` in `internal/platform/configbundle` (hermetic ConfigurationBundle compiled from pinned configuration object refs by walking declared dependencies into a closed closure, unpinned refs, cycles and unpublished or superseded objects refused, an immutable bundle with a canonical digest over every included object digest, a bundle diff naming added, removed and re-versioned objects, and an order-independence property against a registry snapshot; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/platform/configbundle/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CP-001`, `MSRC-010`, `PROTO-008`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS,BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CP_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CP_002`; `GOLDEN=TestTodo_CP_002_Golden`; `INTEGRATION=TestTodo_CP_002_Integration`; `CONFORMANCE=TestTodo_CP_002_Conformance`.
  - **RED:** bundle resolves ambient latest versions, omits transitive dependency or changes digest across builds.
  - **GREEN:** compiler pins complete workflow/policy/schema/rule/capability/connector/reference closure, target scope and minimum runtime compatibility into one canonical digest.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Configuration packaging](specs/hris-admin-dataops.md), [canonical digest](specs/canonical-envelope-and-digest.md).

- [x] `CP-003` **[GATE_A][SOL_HIGH] Sign bundles and enforce anti-replay activation epochs.**
  - **Evidence (2026-09-05):** `TestTodo_CP_003` in `internal/platform/configbundle` (Ed25519 bundle signing with key resolution and revocation, activation epochs, replay protection, signed receipts, typed refusals and audit-safe explanations; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/platform/configbundle/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CP-002`, `TOOL-018`, `TIME-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS,BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CP_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CP_003`; `GOLDEN=TestTodo_CP_003_Golden`; `SECURITY=TestTodo_CP_003_Security`; `RECOVERY=TestTodo_CP_003_Recovery`.
  - **RED:** unsigned, expired, revoked, wrong-scope or below-floor bundle activates.
  - **GREEN:** verifier binds signer/trust profile, tenant/cell/environment, epoch, issued/expiry and anti-rollback floor; denial is typed and has zero activation.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Foundation publication](specs/platform-foundation-gap-closure.md), [security models](data/models/security-trust.md).

- [x] `CP-004` **[GATE_A][SOL_HIGH] Implement desired-state distribution with offline bootstrap.**
  - **Evidence (2026-09-05):** `TestTodo_CP_004` in `internal/platform/configbundle` (distributor publishes by tenant/cell; services verify and atomically apply bundles or use a bounded last-known-good snapshot with explicit freshness policy; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/platform/configbundle/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CP-003`, `SVC-002`, `TENANT-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS,BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CP_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CP_004`; `GOLDEN=TestTodo_CP_004_Golden`; `SECURITY=TestTodo_CP_004_Security`.
  - **RED:** a service starts with unknown config, partial bundle or stale placement without declaring degraded/blocked state.
  - **GREEN:** distributor publishes by tenant/cell; services verify and atomically apply bundles or use a bounded last-known-good snapshot with explicit freshness policy.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Platform foundation](specs/platform-foundation-gap-closure.md), [tenant lifecycle](data/models/assurance-intelligence-platform.md).

- [x] `CP-005` **[GATE_A][SOL_HIGH] Record application receipts and adoption watermarks.**
  - **Evidence (2026-09-05):** `TestTodo_CP_005` in `internal/platform/configbundle` (signed receipt records desired/applied digest, epoch, service/build/cell and validation result; aggregate watermark drives rollout and schema retirement; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/platform/configbundle/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CP-004`, `DATA-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS,BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CP_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CP_005`; `GOLDEN=TestTodo_CP_005_Golden`.
  - **RED:** control plane reports active before required consumers apply, or cannot identify stale service/version.
  - **GREEN:** signed receipt records desired/applied digest, epoch, service/build/cell and validation result; aggregate watermark drives rollout and schema retirement.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Configuration promotion](specs/hris-admin-dataops.md), [operations models](data/models/operations-production.md).

- [x] `CP-006` **[GATE_A][SOL_HIGH] Implement local feature evaluation from signed state.**
  - **Evidence (2026-09-05):** `TestTodo_CP_006` in `internal/platform/configbundle` (Go evaluator returns versioned allow/deny/variant from signed bundle with deterministic rollout key and kill-switch precedence; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/platform/configbundle/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CP-004`, `TENANT-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS,BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CP_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CP_006`; `GOLDEN=TestTodo_CP_006_Golden`; `SECURITY=TestTodo_CP_006_Security`; `MUTATION=TestTodo_CP_006_Mutation`.
  - **RED:** feature decision depends on remote availability, nondeterministic bucketing or missing tenant/org/user scope.
  - **GREEN:** Go evaluator returns versioned allow/deny/variant from signed bundle with deterministic rollout key and kill-switch precedence.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Platform responsibilities](specs/platform-responsibility-boundaries.md), [control plane models](data/models/dataops-configuration.md).

- [x] `CP-007` **[GATE_B][SOL_HIGH] Execute canary activation with automatic pause.**
  - **Evidence (2026-09-05):** `TestTodo_CP_007` in `internal/platform/configbundle` (bounded cohort activation evaluates declared health/evidence windows and deterministically pauses/promotes with signed decision; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/platform/configbundle/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CP-005`, `CP-006`, `OPS-001`, `DATA-020`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS,BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CP_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CP_007`; `GOLDEN=TestTodo_CP_007_Golden`; `CONFORMANCE=TestTodo_CP_007_Conformance`.
  - **RED:** rollout continues through telemetry unknown, SLO breach, reconciliation drift or failed conformance.
  - **GREEN:** bounded cohort activation evaluates declared health/evidence windows and deterministically pauses/promotes with signed decision.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [SLO contract](specs/slo-sli-error-budget.md), [foundation activation](specs/platform-foundation-gap-closure.md).

- [x] `CP-008` **[GATE_B][SOL_HIGH] Implement scoped emergency kill switches.**
  - **Evidence (2026-09-05):** `TestTodo_CP_008` in `internal/platform/configbundle` (signed highest-precedence revocation targets tenant/cell/service/capability/model/connector, propagates within SLO and produces applied receipts; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/platform/configbundle/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CP-004`, `TRUST-021`, `OPS-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS,BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CP_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CP_008`; `GOLDEN=TestTodo_CP_008_Golden`; `INTEGRATION=TestTodo_CP_008_Integration`; `FAULT=TestTodo_CP_008_Fault`; `SECURITY=TestTodo_CP_008_Security`.
  - **RED:** disabling one agent/tool/connector/capability requires full outage or a stale service continues unsafe work.
  - **GREEN:** signed highest-precedence revocation targets tenant/cell/service/capability/model/connector, propagates within SLO and produces applied receipts.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Agent safety](specs/platform-architecture-catalog.md), [operations models](data/models/operations-production.md).

- [ ] `CP-009` **[GATE_B][SOL_HIGH] Roll back configuration as a new governed activation.**
  - **Depends:** `CP-005`, `CP-007`, `CONFIG-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS,BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CP_009`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CP_009`; `GOLDEN=TestTodo_CP_009_Golden`; `FAULT=TestTodo_CP_009_Fault`.
  - **RED:** rollback mutates history, selects a mutable label or revives revoked dependencies.
  - **GREEN:** exact prior bundle is revalidated, re-signed for a new epoch, applied with receipts and reconciled; pinned live workflows retain declared version policy.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Configuration lifecycle](specs/hris-admin-dataops.md), [workflow migration](specs/workflow-runtime.md).

- [ ] `CP-010` **[GATE_B][SOL_HIGH] Prove control-plane outage, corruption and recovery.**
  - **Depends:** `CP-004`–`CP-009`, `RECOVERY-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS,BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CP_010`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CP_010`; `GOLDEN=TestTodo_CP_010_Golden`; `FAULT=TestTodo_CP_010_Fault`; `RECOVERY=TestTodo_CP_010_Recovery`; `SECURITY=TestTodo_CP_010_Security`.
  - **RED:** outage/corrupt bundle causes split-brain activation, unsafe default or inability to revoke critical behavior.
  - **GREEN:** fault schedule proves bounded last-known-good operation, fail-closed sensitive actions, epoch convergence and isolated restore with exact adoption receipts.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Recovery models](data/models/operations-production.md), [foundation gap closure](specs/platform-foundation-gap-closure.md).

## 31. Infrastructure as code and physical data services

> **Disposition (2026-09-02):** P1A for one runnable dev/sandbox cell with PostgreSQL and object storage. HA, PITR, disaster recovery and multi-region are Gate C.

- [ ] `IAC-001` **[P0][SOL_HIGH] Define provider-neutral infrastructure resource contracts.**
  - **Depends:** `SVC-001`, `RECOVERY-001`, `TENANT-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_IAC_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_IAC_001`; `RACE=TestTodo_IAC_001_Race`; `INTEGRATION=TestTodo_IAC_001_Integration`; `FAULT=TestTodo_IAC_001_Fault`; `SECURITY=TestTodo_IAC_001_Security`; `RECOVERY=TestTodo_IAC_001_Recovery`; `BENCHMARK=BenchmarkTodo_IAC_001`.
  - **RED:** a cell resource lacks owner, region/residency, classification, SLO, backup, cost, capacity or replacement strategy.
  - **GREEN:** machine-readable catalog covers network, compute, PostgreSQL, object, queue, cache, telemetry, secrets and edge resources without embedding a single-cloud semantic dependency.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Platform architecture](specs/platform-architecture-catalog.md), [operations models](data/models/operations-production.md).

- [ ] `IAC-002` **[P0][TERRA] Create reproducible dev, test, stage and production-cell stacks.**
  - **Depends:** `IAC-001`, `TOOL-014`, `TENANT-002`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_IAC_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_IAC_002`; `PROPERTY=TestTodo_IAC_002_Property`; `GOLDEN=TestTodo_IAC_002_Golden`; `INTEGRATION=TestTodo_IAC_002_Integration`; `SECURITY=TestTodo_IAC_002_Security`.
  - **RED:** environment differs by undocumented manual step or shares credentials/data unintentionally.
  - **GREEN:** one pinned IaC module graph produces isolated environments from reviewed variables with deterministic plan summaries and explicit cost/resource bounds.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Execution plan](execution-plan.md), [sandbox responsibilities](specs/platform-responsibility-boundaries.md).

- [x] `IAC-003` **[P0][TERRA] Gate formatting, validation, policy and plan review in CI.**
  - **Evidence (2026-09-05):** `TestTodo_IAC_003` in `tools/policy/iac` (lint/validate/policy tests and golden plan diff fail closed; approved exception requires owner, rationale and expiry; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/iac/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `IAC-002`, `TOOL-011`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_IAC_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_IAC_003`; `INTEGRATION=TestTodo_IAC_003_Integration`; `RECOVERY=TestTodo_IAC_003_Recovery`.
  - **RED:** public database, wildcard egress, mutable image, missing encryption/backup/tags or destructive change passes.
  - **GREEN:** lint/validate/policy tests and golden plan diff fail closed; approved exception requires owner, rationale and expiry.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security models](data/models/security-trust.md), [operations production](data/models/operations-production.md).

- [x] `IAC-004` **[GATE_A][SOL_HIGH] Provision segmented cell networking, ingress, DNS and egress.**
  - **Evidence (2026-09-05):** `TestTodo_IAC_004` in `internal/connectivity/iac` (explicit ingress, service dependency, DNS and egress policies enforce cell/tenant trust boundaries and expose tested fail-closed paths; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/connectivity/iac/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `IAC-003`, `TRUST-007`, `TRUST-017`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_IAC_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_IAC_004`; `FUZZ=FuzzTodo_IAC_004`; `INTEGRATION=TestTodo_IAC_004_Integration`; `FAULT=TestTodo_IAC_004_Fault`; `SECURITY=TestTodo_IAC_004_Security`.
  - **RED:** default east-west/egress access, untrusted proxy header, DNS rebinding or cross-cell route succeeds.
  - **GREEN:** explicit ingress, service dependency, DNS and egress policies enforce cell/tenant trust boundaries and expose tested fail-closed paths.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Foundation endpoint discovery](specs/platform-foundation-gap-closure.md), [integration security](specs/integration-platform.md).

- [x] `IAC-005` **[GATE_A][SOL_HIGH] Provision digest-pinned Go workloads with probes and budgets.**
  - **Evidence (2026-09-05):** `TestTodo_IAC_005` in `internal/platform/workload` (every manifest pins signature-verified digest, non-root identity, resources, disruption/drain, liveness/readiness and least-privilege service account; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/platform/workload/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `IAC-003`, `SVC-002`, `TOOL-018`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_IAC_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_IAC_005`; `GOLDEN=TestTodo_IAC_005_Golden`; `RACE=TestTodo_IAC_005_Race`; `INTEGRATION=TestTodo_IAC_005_Integration`.
  - **RED:** unsigned/mutable image, missing request/limit/probe, privileged container or non-graceful drain deploys.
  - **GREEN:** every manifest pins signature-verified digest, non-root identity, resources, disruption/drain, liveness/readiness and least-privilege service account.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Go technology constitution](specs/go-only-technology-constitution.md), [operations models](data/models/operations-production.md).

- [x] `IAC-006` **[GATE_A][SOL_HIGH] Provision PostgreSQL with HA, PITR and migration fencing.**
  - **Evidence (2026-09-05):** `TestTodo_IAC_006` in `internal/platform/database` (isolated cluster/config enforces TLS, roles/RLS, pooling, HA, backups, monitoring and one migration lock; restore test verifies ledger heads; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/platform/database/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `IAC-003`, `DB-001`, `DB-006`, `DB-022`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_IAC_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_IAC_006`; `RACE=TestTodo_IAC_006_Race`; `INTEGRATION=TestTodo_IAC_006_Integration`; `RECOVERY=TestTodo_IAC_006_Recovery`; `MUTATION=TestTodo_IAC_006_Mutation`.
  - **RED:** unencrypted connection/storage, public access, unbounded pool, missing PITR or concurrent migration writer is accepted.
  - **GREEN:** isolated cluster/config enforces TLS, roles/RLS, pooling, HA, backups, monitoring and one migration lock; restore test verifies ledger heads.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Database section](#26-postgresql-database-and-model-materialization), [recovery matrix](data/models/operations-production.md).

- [x] `IAC-007` **[GATE_A][SOL_HIGH] Provision S3-compatible artifact storage and lifecycle controls.**
  - **Evidence (2026-09-05):** `TestTodo_IAC_007` in `internal/connectivity/artifactstore` (provider-neutral endpoint has private access, versioning/immutability, tenant key refs, lifecycle/hold controls and restore/read verification; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/connectivity/artifactstore/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `IAC-003`, `MODEL-029`, `DATA-018`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_IAC_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_IAC_007`; `INTEGRATION=TestTodo_IAC_007_Integration`; `SECURITY=TestTodo_IAC_007_Security`; `RECOVERY=TestTodo_IAC_007_Recovery`.
  - **RED:** public bucket, mutable overwrite, wrong-region replication, unenforced retention or unencrypted object succeeds.
  - **GREEN:** provider-neutral endpoint has private access, versioning/immutability, tenant key refs, lifecycle/hold controls and restore/read verification.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Artifact models](data/models/connectivity-access-content.md), [records management](specs/records-management-and-disposition.md).

- [x] `IAC-008` **[GATE_A][SOL_HIGH] Provision workload identity, secret and certificate bindings.**
  - **Evidence (2026-09-05):** `TestTodo_IAC_008` in `internal/platform/identitybinding` (workloads receive reference-only secret leases and short-lived identities/certs scoped to exact service/cell/destination; plan/state scans find no raw secret; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/platform/identitybinding/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `IAC-003`, `TRUST-006`, `TRUST-015`, `TRUST-023`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_IAC_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_IAC_008`; `GOLDEN=TestTodo_IAC_008_Golden`; `RACE=TestTodo_IAC_008_Race`; `INTEGRATION=TestTodo_IAC_008_Integration`; `SECURITY=TestTodo_IAC_008_Security`.
  - **RED:** static secret in state/manifest, shared service identity or expired certificate allows startup.
  - **GREEN:** workloads receive reference-only secret leases and short-lived identities/certs scoped to exact service/cell/destination; plan/state scans find no raw secret.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Credential leases](specs/secrets-key-custody-and-credential-leases.md), [security models](data/models/security-trust.md).

- [x] `IAC-009` **[GATE_A][TERRA] Provision portable telemetry and quota infrastructure.**
  - **Evidence (2026-09-05):** `TestTodo_IAC_009` in `internal/platform/telemetry` (OTLP/Prometheus-compatible stack and bounded quota state are private, tenant/cell scoped, retained and independently health-monitored; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/platform/telemetry/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `IAC-003`, `OPS-002`, `ADMISSION-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_IAC_009`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_IAC_009`; `INTEGRATION=TestTodo_IAC_009_Integration`; `FAULT=TestTodo_IAC_009_Fault`; `SECURITY=TestTodo_IAC_009_Security`.
  - **RED:** telemetry backend accepts raw sensitive content/unbounded cardinality or quota store failure defaults unlimited.
  - **GREEN:** OTLP/Prometheus-compatible stack and bounded quota state are private, tenant/cell scoped, retained and independently health-monitored.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Operations models](data/models/operations-production.md), [SLO contract](specs/slo-sli-error-budget.md).

- [ ] `IAC-010` **[GATE_B][SOL_HIGH] Implement immutable backup and isolated-recovery stacks.**
  - **Depends:** `IAC-006`–`IAC-009`, `RECOVERY-002`, `RECOVERY-003`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_IAC_010`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_IAC_010`; `INTEGRATION=TestTodo_IAC_010_Integration`; `FAULT=TestTodo_IAC_010_Fault`; `RECOVERY=TestTodo_IAC_010_Recovery`.
  - **RED:** compromised production identity can alter all recovery copies or restored cell can contact production dependencies.
  - **GREEN:** separate trust/admin boundary stores immutable copies and provisions fenced recovery cell; automated validation proves known-good data/config/key refs before traffic.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Recovery models](data/models/operations-production.md), [platform responsibilities](specs/platform-responsibility-boundaries.md).

- [ ] `IAC-011` **[GATE_B][SOL_HIGH] Detect infrastructure drift and protect destructive changes.**
  - **Depends:** `IAC-003`, `IAC-010`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_IAC_011`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_IAC_011`; `GOLDEN=TestTodo_IAC_011_Golden`; `INTEGRATION=TestTodo_IAC_011_Integration`; `FAULT=TestTodo_IAC_011_Fault`.
  - **RED:** console mutation, state mismatch, broad destroy or protected database/key/bucket replacement remains undetected.
  - **GREEN:** scheduled drift report maps change to owner/capability; protected resources require explicit two-person break-glass and recovery evidence.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Continuous controls](data/models/operations-production.md), [trust models](data/models/security-trust.md).

- [ ] `IAC-012` **[GATE_B][SOL_HIGH] Prove cell deployment, drain, failover and teardown.**
  - **Depends:** `IAC-004`–`IAC-011`, `OPS-006`, `RECOVERY-004`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_IAC_012`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_IAC_012`; `INTEGRATION=TestTodo_IAC_012_Integration`; `FAULT=TestTodo_IAC_012_Fault`; `CONFORMANCE=TestTodo_IAC_012_Conformance`; `RECOVERY=TestTodo_IAC_012_Recovery`.
  - **RED:** clean deployment needs manual mutation, failover loses accepted work or teardown can target the wrong environment.
  - **GREEN:** automated rehearsal deploys a cell, runs conformance, drains/fails over/fails back and destroys only a verified disposable stack with signed evidence.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Tenant cell architecture](specs/platform-architecture-catalog.md), [recovery](specs/platform-responsibility-boundaries.md).

- [x] `STORE-001` **[P0][SOL_HIGH] Publish the physical storage-disposition registry.**
  - **Depends:** `DB-COVERAGE-001`, `IAC-001`, `RECOVERY-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_STORE_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_STORE_001`; `INTEGRATION=TestTodo_STORE_001_Integration`; `FAULT=TestTodo_STORE_001_Fault`; `RECOVERY=TestTodo_STORE_001_Recovery`; `MUTATION=TestTodo_STORE_001_Mutation`.
  - **RED:** entity/artifact/event/index/cache/telemetry has no truth class, physical system, consistency, retention, RPO/RTO and rebuild/restore decision.
  - **GREEN:** registry covers every model and service dependency and CI rejects unregistered storage or accidental second authority.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Data plane](specs/platform-plane-model.md), [model catalog](data/models/README.md).
  - **Evidence (2026-09-03):** `TestTodo_STORE_001`, `_Integration`, `_Fault`, `_Recovery`, `_Mutation` in `internal/data/tenancy/storagedisposition` over `definitions/storage/storage-disposition.yaml` (31 live base tables across migrations 00001–00014 with owner, plane, tenant column, retention and encryption class; cross-checked against the live schema); PASS; branch plan-revision-2026-09-02; `go test -count=1 ./internal/data/tenancy/storagedisposition/...` on windows/arm64 (Go 1.26.3).

- [x] `STORE-002` **[GATE_A][SOL_HIGH] Enforce tenant, cell and encryption boundaries in every store adapter.**
  - **Evidence (2026-09-05):** `TestTodo_STORE_002`, `TestTodo_STORE_002_{Integration,Fault,Security}`, SQL-scanner unit tests in `tools/policy/storeboundaries` (AST-based scan of every store package: tenant-scope evidence on each statement including Tier-B dynamic table names, pool imports outside adapters, encryption-class gaps, reviewed expiring exceptions in `allowlist.yaml`); `go test -count=1 ./tools/policy/storeboundaries/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `STORE-001`, `TRUST-008`, `TRUST-012`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_STORE_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_STORE_002`; `INTEGRATION=TestTodo_STORE_002_Integration`; `FAULT=TestTodo_STORE_002_Fault`; `SECURITY=TestTodo_STORE_002_Security`.
  - **RED:** missing context, forged key prefix/index filter/cache key or maintenance credential crosses tenant/cell.
  - **GREEN:** common Go adapter contract requires scope and classification, applies physical isolation and returns no existence oracle on denial.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Organization/AuthZ](specs/organization-scope-and-authz.md), [security models](data/models/security-trust.md).

- [x] `STORE-003` **[GATE_A][TERRA] Publish health, freshness, saturation and recovery state for every store.**
  - **Depends:** `STORE-001`, `DATA-020`, `OPS-003`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_STORE_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_STORE_003`; `GOLDEN=TestTodo_STORE_003_Golden`; `INTEGRATION=TestTodo_STORE_003_Integration`; `FAULT=TestTodo_STORE_003_Fault`; `RECOVERY=TestTodo_STORE_003_Recovery`; `BENCHMARK=BenchmarkTodo_STORE_003`.
  - **RED:** missing telemetry reports healthy or serving/derived store lag is indistinguishable from truth unavailability.
  - **GREEN:** typed status includes authority class, freshness watermark, capacity, errors, rebuild/restore progress and `UNKNOWN` handling.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Operations models](data/models/operations-production.md), [SLOs](specs/slo-sli-error-budget.md).
  - **Evidence (2026-09-03):** `TestTodo_STORE_003` (stale projection and unacked-outbox RED clauses), `_Golden`, `_Integration`, `_Fault` (probe error yields UNKNOWN), `_Recovery`, `BenchmarkTodo_STORE_003` in `internal/data/health` (per-store freshness, saturation against declared budgets and pool stats, recovery state); PASS; branch plan-revision-2026-09-02; `go test -count=1 ./internal/data/health/...` on windows/arm64 (Go 1.26.3).

- [x] `EVENT-001` **[GATE_A][SOL_HIGH] Implement the PostgreSQL outbox queue schema and claim protocol.**
  - **Evidence (2026-09-05):** `TestTodo_EVENT_001` in `internal/data/outbox` (tenant/criticality/available-at/ordering keys, lease/fence/attempt state and indexes support at-least-once delivery with exact operation identity; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/outbox/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LEDGER-008`, `DATA-008`, `DB-020`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_EVENT_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_EVENT_001`; `GOLDEN=TestTodo_EVENT_001_Golden`; `RACE=TestTodo_EVENT_001_Race`; `INTEGRATION=TestTodo_EVENT_001_Integration`; `FAULT=TestTodo_EVENT_001_Fault`; `SECURITY=TestTodo_EVENT_001_Security`; `MUTATION=TestTodo_EVENT_001_Mutation`.
  - **RED:** claim races, stale lease or failure between publish/ack loses or duplicates logical effect invisibly.
  - **GREEN:** tenant/criticality/available-at/ordering keys, lease/fence/attempt state and indexes support at-least-once delivery with exact operation identity.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Ledger spec](specs/transaction-ledger-reconciliation-and-repair.md), [operations models](data/models/operations-production.md).

- [ ] `EVENT-002` **[GATE_B][SOL_HIGH] Implement consumer-group checkpoints, dedupe and poison isolation.**
  - **Depends:** `EVENT-001`, `DATA-009`, `DATA-010`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_EVENT_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_EVENT_002`; `RACE=TestTodo_EVENT_002_Race`; `INTEGRATION=TestTodo_EVENT_002_Integration`; `FAULT=TestTodo_EVENT_002_Fault`; `SECURITY=TestTodo_EVENT_002_Security`; `RECOVERY=TestTodo_EVENT_002_Recovery`; `MUTATION=TestTodo_EVENT_002_Mutation`.
  - **RED:** poison event blocks unrelated tenant, checkpoint advances before effect or replay dispatches external effects.
  - **GREEN:** consumer/partition checkpoint commits with idempotent application, poison entries have owner/expiry/repair route and replay is side-effect fenced.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Projection rebuild](specs/transaction-ledger-reconciliation-and-repair.md), [workflow operations](specs/workflow-runtime.md).

- [ ] `EVENT-003` **[GATE_B][SOL_HIGH] Enforce queue priority, backpressure and global retry budgets.**
  - **Depends:** `EVENT-001`, `ADMISSION-001`, `ADMISSION-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_EVENT_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_EVENT_003`; `RACE=TestTodo_EVENT_003_Race`; `INTEGRATION=TestTodo_EVENT_003_Integration`; `FAULT=TestTodo_EVENT_003_Fault`; `SECURITY=TestTodo_EVENT_003_Security`.
  - **RED:** background flood starves payroll/IAM-critical work or nested layers multiply retries.
  - **GREEN:** scheduler admits by criticality/tenant/resource, propagates slow-down and accounts one logical retry budget across all attempts.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Operations production](data/models/operations-production.md), [SLO contract](specs/slo-sli-error-budget.md).

- [ ] `EVENT-004` **[GATE_B][SOL_HIGH] Prove queue outage, ambiguous publish and recovery.**
  - **Depends:** `EVENT-001`–`EVENT-003`, `RECOVERY-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_EVENT_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_EVENT_004`; `RACE=TestTodo_EVENT_004_Race`; `INTEGRATION=TestTodo_EVENT_004_Integration`; `FAULT=TestTodo_EVENT_004_Fault`; `MUTATION=TestTodo_EVENT_004_Mutation`.
  - **RED:** connection loss at publish boundary causes missing or duplicated business effect, or recovery guesses.
  - **GREEN:** operation identity resolves committed/not committed/ambiguous; dispatcher resumes from durable state and reconciliation verifies downstream outcome.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Ambiguous effects](specs/transaction-ledger-reconciliation-and-repair.md), [recovery](data/models/operations-production.md).

- [ ] `EVENT-005` **[PHASE_2][SOL_HIGH] Define and test the broker-neutral event adapter.**
  - **Depends:** `EVENT-004`, `PROTO-008`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_EVENT_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_EVENT_005`; `RACE=TestTodo_EVENT_005_Race`; `INTEGRATION=TestTodo_EVENT_005_Integration`; `FAULT=TestTodo_EVENT_005_Fault`; `CONFORMANCE=TestTodo_EVENT_005_Conformance`; `RECOVERY=TestTodo_EVENT_005_Recovery`; `SECURITY=TestTodo_EVENT_005_Security`.
  - **RED:** broker-specific offset/topic semantics leak into domain/workflow code or migration changes ordering/dedupe results.
  - **GREEN:** adapter models logical stream, partition/order key, consumer/checkpoint, retry/DLQ and replay; Postgres and one OSS broker test double pass identical conformance.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Platform architecture](specs/platform-architecture-catalog.md), [integration event runtime](specs/integration-platform.md).

- [ ] `EVENT-006` **[PHASE_2][SOL_HIGH] Gate promotion from PostgreSQL outbox to a broker.**
  - **Depends:** `EVENT-005`, `PERF-ENV-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_EVENT_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_EVENT_006`; `FUZZ=FuzzTodo_EVENT_006`; `RACE=TestTodo_EVENT_006_Race`; `INTEGRATION=TestTodo_EVENT_006_Integration`; `FAULT=TestTodo_EVENT_006_Fault`; `CONFORMANCE=TestTodo_EVENT_006_Conformance`; `RECOVERY=TestTodo_EVENT_006_Recovery`; `BENCHMARK=BenchmarkTodo_EVENT_006`.
  - **RED:** broker is introduced without measured capacity need, recovery/ordering parity, ownership, cost and exit evidence.
  - **GREEN:** signed decision demonstrates breached outbox envelope and conformance/restore/operability of selected low-cost OSS option; otherwise Postgres remains authoritative transport.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Architecture principles](plan.md), [operations production](data/models/operations-production.md).

- [x] `CACHE-001` **[GATE_A][SOL_HIGH] Implement a rebuildable tenant-safe cache contract.**
  - **Evidence (2026-09-05):** `TestTodo_CACHE_001` in `internal/platform/cache` (in-process or Valkey-compatible adapter uses bounded TTL/size, versioned keys, post-cache AuthZ and safe miss/failure behavior; written by an earlier lane (Luna or Sonnet) and verified independently); `go test -count=1 ./internal/platform/cache/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `STORE-002`, `TRUST-012`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_CACHE_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CACHE_001`; `RACE=TestTodo_CACHE_001_Race`; `INTEGRATION=TestTodo_CACHE_001_Integration`; `SECURITY=TestTodo_CACHE_001_Security`; `RECOVERY=TestTodo_CACHE_001_Recovery`.
  - **RED:** cache key omits tenant/policy/version, stale sensitive value bypasses authorization or cache loss changes correctness.
  - **GREEN:** in-process or Valkey-compatible adapter uses bounded TTL/size, versioned keys, post-cache AuthZ and safe miss/failure behavior.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Data plane](specs/platform-plane-model.md), [AuthZ](specs/organization-scope-and-authz.md).

- [x] `RETRIEVAL-001` **[PHASE_2][SOL_HIGH] Implement authorized lexical search before specialized search infrastructure.**
  - **Evidence (2026-09-05):** `TestTodo_RETRIEVAL_001` in `internal/data/search`, `migrations` (migration 00037_lexical_search.sql: a tenant-scoped, GIN-indexed tsvector search_projection built only from a closed allowlist of classification-cleared people fields, rebuildable as a pure function of the source facts, with an append-only search_projection_event evidence log under forbid_mutation; Query enforces scope grant before any statement, drops every subject the caller's discloser withholds, and returns subject refs and rank only; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/search/ ./migrations/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `STORE-002`, `DB-018`, `MODEL-023`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ANALYTICS,BI.INTELLIGENCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_RETRIEVAL_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_RETRIEVAL_001`; `INTEGRATION=TestTodo_RETRIEVAL_001_Integration`; `SECURITY=TestTodo_RETRIEVAL_001_Security`; `CONFORMANCE=TestTodo_RETRIEVAL_001_Conformance`; `RECOVERY=TestTodo_RETRIEVAL_001_Recovery`; `MUTATION=TestTodo_RETRIEVAL_001_Mutation`.
  - **RED:** indexed result leaks restricted field/existence, stale authority or cross-tenant item.
  - **GREEN:** PostgreSQL search projection applies tenant/field/purpose filters, freshness labels, deterministic ranking and rebuild parity; specialized search requires a measured gate.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Search governance](specs/platform-responsibility-boundaries.md), [data classification](specs/data-classification-and-dlp.md).

- [x] `ANALYTICS-001` **[PHASE_2][SOL_HIGH] Build a reproducible analytical export and query substrate.**
  - **Evidence (2026-09-07):** `TestTodo_ANALYTICS_001`, `TestTodo_ANALYTICS_001_Golden`, `TestTodo_ANALYTICS_001_Security` in `internal/data/analytics` (written in 171cafc on 2026-09-03 and never ticked; the named tests prove the GREEN contract: governed snapshot produces partitioned Parquet and DuckDB-compatible catalog with provenance, deletion/hold propagation and deterministic metric fixtures; OLAP service promotion is evidence-gated); `go test -count=1 ./internal/data/analytics/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `STORE-001`, `DATA-014`, `PRIV-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ANALYTICS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ANALYTICS_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ANALYTICS_001`; `GOLDEN=TestTodo_ANALYTICS_001_Golden`; `SECURITY=TestTodo_ANALYTICS_001_Security`.
  - **RED:** analytics changes authoritative state, loses source watermark/policy or emits unauthorized row/field.
  - **GREEN:** governed snapshot produces partitioned Parquet and DuckDB-compatible catalog with provenance, deletion/hold propagation and deterministic metric fixtures; OLAP service promotion is evidence-gated.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Intelligence plane](specs/platform-plane-model.md), [privacy lifecycle](specs/records-management-and-disposition.md).

## 32. Production identity, key custody and edge enforcement

> **Disposition (2026-09-02):** P1A for the federation issuer registry and edge authentication. Key custody, certificate lifecycle, workload identity and mTLS east-west are Gate C.

- [x] `AUTHN-001` **[GATE_A][SOL_HIGH] Implement a tenant federation issuer registry.**
  - **Evidence (2026-09-05):** `TestTodo_AUTHN_001` in `internal/authn/issuerregistry`, `internal/authn/federation`, `internal/trust/federation`, `migrations` (tenant federation issuer registry: immutable Issuer revisions (issuer URL, audience, pinned JWKS keys or a pinned trust-bundle ref, closed algorithm set, claim mappings, skew and staleness bounds) with a DRAFT/ACTIVE/SUSPENDED/RETIRED lifecycle, activation requiring a distinct approver, Lookup refusing inactive or foreign-tenant issuers, a resolver handing the federation port the active issuer's verification material through a new IssuerResolver port, migration 00038_issuer_registry.sql with immutable issuer_profile revisions and an append-only issuer_state_event log under RLS, fuzzed and pgtest-backed; the other session's authn/federation registry received its missing tests; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/authn/issuerregistry/ ./internal/authn/federation/ ./internal/trust/federation/ ./migrations/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-001`, `TRUST-002`, `CP-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_AUTHN_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_AUTHN_001`; `FUZZ=FuzzTodo_AUTHN_001`; `RACE=TestTodo_AUTHN_001_Race`; `INTEGRATION=TestTodo_AUTHN_001_Integration`; `SECURITY=TestTodo_AUTHN_001_Security`; `MUTATION=TestTodo_AUTHN_001_Mutation`.
  - **RED:** Reject unknown issuer, algorithm, audience, stale metadata, wrong tenant and key rollover races.
  - **GREEN:** Accept only signed versioned OIDC/SAML profiles with owner, scopes, assurance and expiry.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Foundation identity](specs/platform-foundation-gap-closure.md), [security models](data/models/security-trust.md).

- [x] `AUTHN-002` **[GATE_A][SOL_HIGH] Implement OIDC authorization-code with PKCE.**
  - **Evidence (2026-09-05):** `TestTodo_AUTHN_002` in `internal/authn/oidc` (stdlib-only authorization-code flow with PKCE per LIB-010's go-oidc and x/oauth2 deferral: ClientRegistration pins client id, redirect URI and endpoints per tenant and issuer, the code verifier is derived by HMAC from a flow secret and never persisted, take-once StateStore is the replay guard, own compact-JWS verification for RS256, ES256 and EdDSA with the issuer's declared algorithm set (downgrade refused), keys through trust/federation's IssuerResolver, iss, aud, exp, iat, nbf under the issuer skew bound, nonce and at_hash checks, claims mapped onto trust.PrincipalSpec through issuerregistry claim mappings with unmapped claims never leaking, HTTP token exchange behind a port; the workspace dev login is not yet wired to it (follow-up: BeginAuthorization and HandleCallback routes ahead of serveLoginSubmit in internal/humanwork/workspace); written by a Sonnet subagent and verified independently); `go test -count=1 ./internal/authn/oidc/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `AUTHN-001`, `TRUST-003`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_AUTHN_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_AUTHN_002`; `FUZZ=FuzzTodo_AUTHN_002`; `INTEGRATION=TestTodo_AUTHN_002_Integration`; `SECURITY=TestTodo_AUTHN_002_Security`; `RECOVERY=TestTodo_AUTHN_002_Recovery`; `MUTATION=TestTodo_AUTHN_002_Mutation`.
  - **RED:** Reject state/nonce/PKCE replay, redirect confusion and token substitution.
  - **GREEN:** Issue tenant-bound session only after exact issuer/audience/time/assurance validation.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Foundation identity](specs/platform-foundation-gap-closure.md), [trust models](data/models/security-trust.md).

- [x] `AUTHN-003` **[GATE_A][SOL_HIGH] Implement governed federation subject linking.**
  - **Evidence (2026-09-05):** `TestTodo_AUTHN_003` in `internal/authn/subjectlink` (Exact subject binding or reviewed candidate creates versioned principal link with separation path; written by an earlier lane (Luna or Sonnet) and verified independently); `go test -count=1 ./internal/authn/subjectlink/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `AUTHN-001`, `MODEL-022`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_AUTHN_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_AUTHN_003`; `FUZZ=FuzzTodo_AUTHN_003`; `INTEGRATION=TestTodo_AUTHN_003_Integration`; `SECURITY=TestTodo_AUTHN_003_Security`; `MUTATION=TestTodo_AUTHN_003_Mutation`.
  - **RED:** Never auto-link on email/display name or cross tenant.
  - **GREEN:** Exact subject binding or reviewed candidate creates versioned principal link with separation path.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Identity resolution](specs/identity-resolution-and-entity-linkage.md), [security models](data/models/security-trust.md).

- [x] `AUTHN-004` **[GATE_A][SOL_HIGH] Implement session families, rotation and revocation.**
  - **Evidence (2026-09-05):** `TestTodo_AUTHN_004` in `internal/trust/session` (Rotating family records device/session context, absolute/idle expiry and revocation epoch; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/trust/session/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `AUTHN-002`, `TRUST-003`, `TIME-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_AUTHN_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_AUTHN_004`; `FUZZ=FuzzTodo_AUTHN_004`; `INTEGRATION=TestTodo_AUTHN_004_Integration`; `SECURITY=TestTodo_AUTHN_004_Security`; `RECOVERY=TestTodo_AUTHN_004_Recovery`; `MUTATION=TestTodo_AUTHN_004_Mutation`.
  - **RED:** Refresh replay, expired assurance, revoked session and clock skew fail closed.
  - **GREEN:** Rotating family records device/session context, absolute/idle expiry and revocation epoch.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Foundation identity](specs/platform-foundation-gap-closure.md), [workflow revalidation](specs/workflow-runtime.md).

- [x] `AUTHN-005` **[GATE_B][SOL_HIGH] Enforce step-up and sender-constrained sensitive operations.**
  - **Evidence (2026-09-05):** `TestTodo_AUTHN_005` in `internal/trust/stepup` (Proof binds principal/session/action/proposal/scope and required assurance with single-use replay prevention; written by an earlier lane (Luna or Sonnet) and verified independently); `go test -count=1 ./internal/trust/stepup/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `AUTHN-004`, `TRUST-004`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_AUTHN_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_AUTHN_005`; `FUZZ=FuzzTodo_AUTHN_005`; `INTEGRATION=TestTodo_AUTHN_005_Integration`; `SECURITY=TestTodo_AUTHN_005_Security`; `RECOVERY=TestTodo_AUTHN_005_Recovery`; `MUTATION=TestTodo_AUTHN_005_Mutation`.
  - **RED:** Stolen bearer token cannot approve/repair/export.
  - **GREEN:** Proof binds principal/session/action/proposal/scope and required assurance with single-use replay prevention.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [AuthZ](specs/organization-scope-and-authz.md), [human work](specs/human-work-forms-and-rules.md).

- [x] `AUTHN-006` **[GATE_A][SOL_HIGH] Issue and verify workload identities over mTLS.**
  - **Evidence (2026-09-05):** `TestTodo_AUTHN_006` in `internal/trust/workload` (Short-lived workload identity and trust-bundle version propagate to every service call; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/trust/workload/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-006`, `TRUST-023`, `IAC-008`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_AUTHN_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_AUTHN_006`; `FUZZ=FuzzTodo_AUTHN_006`; `INTEGRATION=TestTodo_AUTHN_006_Integration`; `SECURITY=TestTodo_AUTHN_006_Security`; `MUTATION=TestTodo_AUTHN_006_Mutation`.
  - **RED:** Wrong SAN/cell/service/tenant, expired/revoked cert or shared identity fails.
  - **GREEN:** Short-lived workload identity and trust-bundle version propagate to every service call.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Secrets/key custody](specs/secrets-key-custody-and-credential-leases.md), [foundation trust](specs/platform-foundation-gap-closure.md).

- [x] `AUTHN-007` **[GATE_B][SOL_HIGH] Prove IdP outage and post-outage reconciliation.**
  - **Evidence (2026-09-05):** `TestTodo_AUTHN_007` in `internal/authn/outage` (Declared cached/read-only/manual modes, revocation reconciliation and incident evidence pass fault fixtures; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/authn/outage/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `AUTHN-004`, `TRUST-020`, `OPS-004`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_AUTHN_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_AUTHN_007`; `FUZZ=FuzzTodo_AUTHN_007`; `INTEGRATION=TestTodo_AUTHN_007_Integration`; `FAULT=TestTodo_AUTHN_007_Fault`; `SECURITY=TestTodo_AUTHN_007_Security`; `MUTATION=TestTodo_AUTHN_007_Mutation`.
  - **RED:** Outage never silently elevates or extends stale high-risk authority.
  - **GREEN:** Declared cached/read-only/manual modes, revocation reconciliation and incident evidence pass fault fixtures.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Incident management](specs/incident-management.md), [trust models](data/models/security-trust.md).

- [x] `AUTHN-008` **[PHASE_2][SOL_HIGH] Decide and gate SAML/SCIM support from customer evidence.**
  - **Evidence (2026-09-05):** `TestTodo_AUTHN_008` in `internal/authn/enterprisegate` (Approved path gets full metadata/signature/deprovision conformance; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/authn/enterprisegate/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `AUTHN-001`, `WEDGE-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_AUTHN_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_AUTHN_008`; `FUZZ=FuzzTodo_AUTHN_008`; `INTEGRATION=TestTodo_AUTHN_008_Integration`; `SECURITY=TestTodo_AUTHN_008_Security`; `CONFORMANCE=TestTodo_AUTHN_008_Conformance`; `MUTATION=TestTodo_AUTHN_008_Mutation`.
  - **RED:** No SAML/SCIM implementation enters Phase 1 without design-partner requirement, threat/operability tests and named owner.
  - **GREEN:** Approved path gets full metadata/signature/deprovision conformance.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Execution plan](execution-plan.md), [foundation identity](specs/platform-foundation-gap-closure.md).

- [x] `AUTHN-010` **[GATE_C][SOL_HIGH] Map government identity-assurance requirements to tenant and role.**
  - **Evidence (2026-09-05):** `TestTodo_AUTHN_010` in `internal/authn/issuerregistry` (AUTHN-001's issuer registry gains an assurance-contract field naming required IAL/AAL/FAL per government tenant, resolved against SECARCH-001's credential-assurance table; a claim downgrade or external deprovision revokes access and produces durable evidence; ; written by an earlier lane (Luna or Sonnet) and verified independently); `go test -count=1 ./internal/authn/issuerregistry/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `AUTHN-001`, `SECARCH-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_AUTHN_010`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_AUTHN_010`; `GOLDEN=TestTodo_AUTHN_010_Golden`; `SECURITY=TestTodo_AUTHN_010_Security`; `INTEGRATION=TestTodo_AUTHN_010_Integration`; `MUTATION=TestTodo_AUTHN_010_Mutation`.
  - **RED:** a government-tenant login accepts an external assertion with no recorded IAL/AAL/FAL, or a Login.gov/state-IdP claim change (deprovision, assurance downgrade) does not revoke the mapped role.
  - **GREEN:** `AUTHN-001`'s issuer registry gains an assurance-contract field naming required IAL/AAL/FAL per government tenant, resolved against `SECARCH-001`'s credential-assurance table; a claim downgrade or external deprovision revokes access and produces durable evidence; reauthentication is required for sensitive work below the mapped floor.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security research §3, G-18](research/security-best-practices-2026.md#3-applications-interacting-with-federal-state-and-local-government), [NIST SP 800-63-4](https://pages.nist.gov/800-63-4/), [Login.gov rules of use](https://www.login.gov/policy/rules-of-use/). Maps to NIST 800-63-4 IAL/AAL/FAL; FedRAMP identity-assurance baseline.

- [x] `TRUST-026` **[P0][SOL_HIGH] Define provider-neutral key, secret and certificate custody interfaces.**
  - **Evidence (2026-09-05):** `TestTodo_TRUST_026` in `internal/trust/custody` (provider-neutral key, secret and certificate custody interfaces (Get by reference, Rotate, Revoke, Attest) with an in-memory fake and a conformance suite every provider adapter must pass, no raw key material crossing the interface; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/trust/custody/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-015`, `TRUST-023`, `CRYPTO-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_026`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_026`; `FUZZ=FuzzTodo_TRUST_026`; `RACE=TestTodo_TRUST_026_Race`; `INTEGRATION=TestTodo_TRUST_026_Integration`; `SECURITY=TestTodo_TRUST_026_Security`; `CONFORMANCE=TestTodo_TRUST_026_Conformance`; `MUTATION=TestTodo_TRUST_026_Mutation`.
  - **RED:** Application cannot request raw long-lived material.
  - **GREEN:** Typed handles support encrypt/decrypt/sign/verify/lease/rotate/revoke with tenant/purpose/destination context and fake-provider conformance.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Credential leases](specs/secrets-key-custody-and-credential-leases.md), [security models](data/models/security-trust.md).

- [x] `TRUST-027` **[GATE_A][SOL_HIGH] Implement secret-zero workload bootstrap.**
  - **Evidence (2026-09-05):** `TestTodo_TRUST_027` in `internal/trust/bootstrap` (Attested workload identity obtains bounded initial lease and denial leaves process unready; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/trust/bootstrap/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-026`, `AUTHN-006`, `IAC-008`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_027`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_027`; `FUZZ=FuzzTodo_TRUST_027`; `RACE=TestTodo_TRUST_027_Race`; `SECURITY=TestTodo_TRUST_027_Security`; `MUTATION=TestTodo_TRUST_027_Mutation`.
  - **RED:** No static bootstrap credential exists in image/config/state.
  - **GREEN:** Attested workload identity obtains bounded initial lease and denial leaves process unready.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Credential leases](specs/secrets-key-custody-and-credential-leases.md), [Go services](#28-executable-topology-and-go-service-binaries).

- [x] `TRUST-028` **[GATE_A][SOL_HIGH] Implement envelope-encryption hierarchy and tenant key separation.**
  - **Evidence (2026-09-05):** `TestTodo_TRUST_028` in `internal/trust/envelope` (root to per-tenant KEK to per-object DEK hierarchy with DEKs wrapped through the custody interface and AES-GCM data encryption, every ciphertext header naming tenant, KEK version and DEK id, a DEK wrapped under one tenant never unwrapping with another's KEK, KEK rotation rewrapping without re-encrypting data; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/trust/envelope/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-026`, `MODEL-023`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_028`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_028`; `FUZZ=FuzzTodo_TRUST_028`; `SECURITY=TestTodo_TRUST_028_Security`; `MUTATION=TestTodo_TRUST_028_Mutation`.
  - **RED:** Wrong tenant/AAD/version, nonce reuse, revoked key and tampering fail.
  - **GREEN:** Data/domain keys and wrapped key refs rotate without plaintext persistence.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security models](data/models/security-trust.md), [records](specs/records-management-and-disposition.md).

- [x] `TRUST-029` **[GATE_B][SOL_HIGH] Implement machine credential leases and revocation epochs.**
  - **Evidence (2026-09-05):** `TestTodo_TRUST_029` in `internal/trust/lease` (machine credential leases for workload identities with a monotonic per-tenant revocation epoch: any lease minted before the current epoch is refused at use, epoch bumps are digested events with evidence; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/trust/lease/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-026`, `TRUST-016`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_029`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_029`; `FUZZ=FuzzTodo_TRUST_029`; `RACE=TestTodo_TRUST_029_Race`; `SECURITY=TestTodo_TRUST_029_Security`; `MUTATION=TestTodo_TRUST_029_Mutation`.
  - **RED:** Wrong workload/tenant/destination/operation, reuse after expiry/revoke or old epoch fails.
  - **GREEN:** Leases are short-lived, reference-only and fully evidenced.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Credential leases](specs/secrets-key-custody-and-credential-leases.md), [integration platform](specs/integration-platform.md).

- [x] `TRUST-030` **[GATE_B][SOL_HIGH] Implement CA hierarchy, trust bundles and certificate rotation.**
  - **Evidence (2026-09-05):** `TestTodo_TRUST_030` in `internal/trust/bundle` (CA hierarchy with root, intermediates and leaves under declared path constraints issued through the custody certificate interface, intermediate rotation with overlap so leaves under either validate during the window, and a Verify refusing a leaf whose chain crosses a revoked or expired intermediate; stdlib x509; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/trust/bundle/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-026`, `TRUST-023`, `TIME-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_030`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_030`; `FUZZ=FuzzTodo_TRUST_030`; `SECURITY=TestTodo_TRUST_030_Security`; `MUTATION=TestTodo_TRUST_030_Mutation`.
  - **RED:** Expired/wrong-SAN/unknown/revoked/downgrade handshakes fail.
  - **GREEN:** Overlapping trust bundles permit zero-downtime rotation and emergency revocation within SLO.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Foundation certificate lifecycle](specs/platform-foundation-gap-closure.md), [security models](data/models/security-trust.md).

- [x] `TRUST-031` **[GATE_C][SOL_HIGH] Implement KMS/HSM/BYOK key-handle lifecycle.**
  - **Evidence (2026-09-05):** `TestTodo_TRUST_031` in `internal/trust/custody` (provider-neutral key-handle lifecycle (KMS/HSM/BYOK class, tenant, purpose, version; PENDING/ACTIVE/ROTATING/DISABLED/DESTROYED as digested events), BYOK import requiring a tenant wrapping proof and attestation, destruction requiring a distinct approver and a retention-hold check, no raw key material crossing the interface, the in-memory fake and conformance suite extended so any provider must pass; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/trust/custody/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-028`, `TENANT-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_031`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_031`; `FUZZ=FuzzTodo_TRUST_031`; `INTEGRATION=TestTodo_TRUST_031_Integration`; `SECURITY=TestTodo_TRUST_031_Security`; `MUTATION=TestTodo_TRUST_031_Mutation`.
  - **RED:** Provider/tenant key confusion, revoked BYOK use and unsupported residency fail.
  - **GREEN:** Create/import/activate/rotate/suspend/destroy retain evidence without exporting key material.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Platform responsibilities](specs/platform-responsibility-boundaries.md), [security models](data/models/security-trust.md).

- [ ] `TRUST-032` **[GATE_B][SOL_HIGH] Prove key/secret service outage and sealed recovery.**
  - **Depends:** `TRUST-027`–`TRUST-031`, `RECOVERY-003`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestTodo_TRUST_032`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_TRUST_032`; `FUZZ=FuzzTodo_TRUST_032`; `FAULT=TestTodo_TRUST_032_Fault`; `SECURITY=TestTodo_TRUST_032_Security`; `RECOVERY=TestTodo_TRUST_032_Recovery`; `MUTATION=TestTodo_TRUST_032_Mutation`.
  - **RED:** Cache does not silently outlive policy or expose plaintext.
  - **GREEN:** Outage modes are explicit and isolated recovery restores references/trust without copying forbidden secret material.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Recovery](data/models/operations-production.md), [credential leases](specs/secrets-key-custody-and-credential-leases.md).

- [x] `EDGE-001` **[GATE_A][SOL_HIGH] Enforce canonical grpcbridge edge admission.**
  - **Evidence (2026-09-05):** `TestTodo_EDGE_001`, `TestTodo_EDGE_001_{Golden,Integration,Security}`, `FuzzTodo_EDGE_001` in `test/edge` (eight-row refusal matrix with exactly two successes over the real Connect edge and gRPC surfaces; the Golden exposed that `commonv1.ErrorDetail` never carried the owned reason ref across the wire, fixed by adding `reason_ref = 6` to `schema/proto/hcmnext/common/v1/common.proto`, regenerating, and wiring `Error.Detail`/`FromDetail` in `internal/transport/envelope`) plus the eleven `TestRefusalMatrix_*`/`TestConformanceScenario_*`/`TestAssertConformant_*` tests in `internal/transport/transporttest`; `go test -count=1 ./test/edge/ ./internal/transport/transporttest/ ./internal/transport/envelope/ ./tools/gen/compatibility/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SVC-003`, `TOOL-008`, `TRUST-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.SECURITY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_EDGE_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_EDGE_001`; `GOLDEN=TestTodo_EDGE_001_Golden`; `FUZZ=FuzzTodo_EDGE_001`; `INTEGRATION=TestTodo_EDGE_001_Integration`; `SECURITY=TestTodo_EDGE_001_Security`.
  - **RED:** Reject oversize headers/body, missing deadline, untrusted forwarded host and malformed framing before domain work.
  - **GREEN:** Accepted envelope has normalized route/context/correlation and identical gRPC semantics.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Experience boundary](specs/platform-plane-model.md), [execution plan](execution-plan.md).

- [x] `EDGE-002` **[GATE_A][SOL_HIGH] Enforce WAF/request-shape and DDoS admission policy.**
  - **Evidence (2026-09-05):** `TestTodo_EDGE_002` in `internal/transport/edge` (Normal corpus meets latency budget; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/transport/edge/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `EDGE-001`, `ADMISSION-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.SECURITY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_EDGE_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_EDGE_002`; `FUZZ=FuzzTodo_EDGE_002`; `FAULT=TestTodo_EDGE_002_Fault`; `SECURITY=TestTodo_EDGE_002_Security`; `BENCHMARK=BenchmarkTodo_EDGE_002`; `MUTATION=TestTodo_EDGE_002_Mutation`.
  - **RED:** Smuggling, ambiguous encoding, decompression bomb, forbidden method/type and connection burst return typed reject/throttle with zero domain effect.
  - **GREEN:** Normal corpus meets latency budget.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Foundation hostile ingress](specs/platform-foundation-gap-closure.md), [operations models](data/models/operations-production.md).

- [x] `EDGE-003` **[GATE_A][SOL_HIGH] Enforce tenant/principal/route quotas and fairness.**
  - **Evidence (2026-09-05):** `TestTodo_EDGE_003` in `internal/transport/edge` (Decisions expose retry-after, scope, criticality and budget evidence; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/transport/edge/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `EDGE-001`, `ADMISSION-001`, `ADMISSION-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.SECURITY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_EDGE_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_EDGE_003`; `FUZZ=FuzzTodo_EDGE_003`; `FAULT=TestTodo_EDGE_003_Fault`; `SECURITY=TestTodo_EDGE_003_Security`.
  - **RED:** Quota-key spoof/reset and noisy tenant cannot starve reserved critical work.
  - **GREEN:** Decisions expose retry-after, scope, criticality and budget evidence.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [SLO contract](specs/slo-sli-error-budget.md), [platform architecture](specs/platform-architecture-catalog.md).

- [x] `EDGE-004` **[GATE_A][SOL_HIGH] Enforce browser Origin, Host, redirect and CSRF policy.**
  - **Evidence (2026-09-05):** `TestTodo_EDGE_004` in `internal/transport/edge` (BrowserPolicy middleware mounted on the cell mux enforces Host allow-list, same-origin Origin/Referer and Sec-Fetch-Site rules, safe redirect targets and CSRF on state-changing browser requests; cross-site is refused only when no Origin is present so framed dev logins still work; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/transport/edge/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `EDGE-001`, `AUTHN-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.SECURITY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_EDGE_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_EDGE_004`; `FUZZ=FuzzTodo_EDGE_004`; `INTEGRATION=TestTodo_EDGE_004_Integration`; `SECURITY=TestTodo_EDGE_004_Security`; `BROWSER=TestTodo_EDGE_004_Browser`; `RECOVERY=TestTodo_EDGE_004_Recovery`.
  - **RED:** Mismatched/null origin, host confusion, unregistered redirect and CSRF replay fail.
  - **GREEN:** State-changing browser calls use session/action-bound single-use proof.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Foundation identity](specs/platform-foundation-gap-closure.md), [Go-only transport](specs/go-only-technology-constitution.md).

- [x] `EDGE-005` **[GATE_A][SOL_HIGH] Enforce a centralized DNS/IP-aware egress proxy.**
  - **Evidence (2026-09-05):** `TestTodo_EDGE_005` in `internal/connectivity/egress` (Every allowed hop revalidates destination, TLS, purpose and DLP; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/connectivity/egress/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TRUST-017`, `TRUST-018`, `IAC-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.SECURITY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_EDGE_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_EDGE_005`; `FUZZ=FuzzTodo_EDGE_005`; `SECURITY=TestTodo_EDGE_005_Security`.
  - **RED:** Loopback/private/link-local/metadata/alternate-IP/DNS-rebinding/unsafe redirect/proxy bypass sends zero bytes.
  - **GREEN:** Every allowed hop revalidates destination, TLS, purpose and DLP.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Outbound trust](specs/platform-foundation-gap-closure.md), [integration security](specs/integration-platform.md).

- [x] `EDGE-006` **[GATE_A][SOL_HIGH] Generate east-west policy from service dependency manifests.**
  - **Evidence (2026-09-05):** `TestTodo_EDGE_006` in `internal/transport/eastwest` (Declared workload-to-capability paths pass mTLS plus service AuthZ; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/transport/eastwest/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SVC-001`, `AUTHN-006`, `IAC-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.SECURITY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_EDGE_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_EDGE_006`; `GOLDEN=TestTodo_EDGE_006_Golden`; `FUZZ=FuzzTodo_EDGE_006`; `SECURITY=TestTodo_EDGE_006_Security`.
  - **RED:** Unknown workload/wrong cell/stale endpoint/unauthorized method is unreachable and denied.
  - **GREEN:** Declared workload-to-capability paths pass mTLS plus service AuthZ.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Foundation endpoint discovery](specs/platform-foundation-gap-closure.md), [service AuthZ](specs/organization-scope-and-authz.md).

- [ ] `EDGE-007` **[GATE_B][SOL_HIGH] Propagate overload, retry and circuit state end to end.**
  - **Depends:** `EDGE-003`, `EDGE-005`, `ADMISSION-002`, `INTG-015`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.SECURITY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_EDGE_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_EDGE_007`; `FUZZ=FuzzTodo_EDGE_007`; `RACE=TestTodo_EDGE_007_Race`; `FAULT=TestTodo_EDGE_007_Fault`; `SECURITY=TestTodo_EDGE_007_Security`; `MUTATION=TestTodo_EDGE_007_Mutation`.
  - **RED:** Nested 429/503/timeouts consume one bounded logical retry budget and produce `ADMIT|QUEUE|DEFER|DEGRADE|REJECT`
  - **GREEN:** No retry storm or duplicate effect.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Integration scheduling](specs/integration-platform.md), [operations models](data/models/operations-production.md).

- [x] `EDGE-008` **[GATE_B][SOL_HIGH] Run adversarial edge/network conformance.**
  - **Evidence (2026-09-05):** `TestTodo_EDGE_008` in `internal/connectivity/edge` (Pinned attack/fuzz/chaos corpus must find zero cross-tenant disclosure/effect, fail-open trust decision or unbounded resource path and emit signed release evidence; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/connectivity/edge/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `EDGE-001`–`EDGE-007`, `TRUST-025`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.SECURITY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_EDGE_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_EDGE_008`; `FUZZ=FuzzTodo_EDGE_008`; `RACE=TestTodo_EDGE_008_Race`; `SECURITY=TestTodo_EDGE_008_Security`; `CONFORMANCE=TestTodo_EDGE_008_Conformance`; `MUTATION=TestTodo_EDGE_008_Mutation`.
  - **RED:** Seeded defect: oversized or slow client traffic passes declared ingress limits for EDGE-008; the primary test must return EDGE_008_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Pinned attack/fuzz/chaos corpus must find zero cross-tenant disclosure/effect, fail-open trust decision or unbounded resource path and emit signed release evidence.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Adversarial model audit](data/models/adversarial-model-audit-2026-08-14.md), [conformance audit](specs/adversarial-audit-32-reviewers-2026-08-14.md).

## 33. CI/CD, release admission and observability

> **Disposition (2026-09-02):** P1A for clean-checkout Go verification and generation drift. SBOM, signed admission and provenance are P1B. Telemetry privacy gateways and cardinality budgets are Gate C.

- [x] `CICD-001` **[P0][TERRA] Make clean-checkout Go verification authoritative.**
  - **Evidence (2026-09-05):** `TestTodo_CICD_001` in `tools/policy/cleancheckout` (a verifier that exports the tracked tree into a temp directory (read-only git ls-files), runs build, vet and the policy trio there, reports tracked-tree gaps the working tree hides, keeps a declared table of build-time artifacts expected absent from a clean checkout with the command that produces each, proves a missing tracked file is reported and an expected artifact is not, and exits non-zero on new gaps against an owner allowlist; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/cleancheckout/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02. Not wired into test:all or CI yet: on the live tree the verifier exits non-zero because it reports tracked-tree gaps outside its allowlist (files the working tree needs that are not tracked); follow-up: triage those findings, then add `check:cleancheckout` and the workflow step.
  - **Depends:** `TOOL-001`, `TOOL-010`–`TOOL-015`, `ARCH-GO-003`, `ARCH-GO-018`, `LIB-002`, `LIB-013`, `GOV-017`, `GOV-018`, `GOV-020`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_CICD_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CICD_001`; `PROPERTY=TestTodo_CICD_001_Property`; `GOLDEN=TestTodo_CICD_001_Golden`; `RACE=TestTodo_CICD_001_Race`.
  - **RED:** CI fails if any package/generated output/import-boundary check or one of `cmd/hcmnext`, `cmd/worker`, `cmd/projector`, `cmd/scheduler`, `cmd/admin`, `cmd/migrate` is omitted, if a deferred package enters the Phase 1 graph, or if legacy Node/npm remains authoritative.
  - **GREEN:** Pinned pipeline runs generation drift, architecture graph, fmt/vet/static, unit/integration/race/fuzz and builds all six commands from one root module.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Go technology constitution](specs/go-only-technology-constitution.md), [toolchain](#2-go-only-repository-toolchain-and-contract-generation).

- [x] `CICD-002` **[GATE_A][SOL_HIGH] Rehearse migration manifests in CI.**
  - **Evidence (2026-09-05):** `TestTodo_CICD_002` in `tools/policy/migrationci` (Clean and upgrade paths run expand/backfill/shadow/cutover/contract without history loss; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/migrationci/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DB-006`, `DB-021`, `CICD-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_CICD_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CICD_002`; `GOLDEN=TestTodo_CICD_002_Golden`.
  - **RED:** Checksum/dirty/lock/backfill/mixed-version/abort failures are detected.
  - **GREEN:** Clean and upgrade paths run expand/backfill/shadow/cutover/contract without history loss.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Database materialization](#26-postgresql-database-and-model-materialization), [configuration lifecycle](data/models/dataops-configuration.md).

- [x] `CICD-003` **[GATE_A][SOL_HIGH] Publish immutable release artifacts and evidence.**
  - **Evidence (2026-09-05):** `TestTodo_CICD_003` in `tools/policy/release`, `tools/policy/release/cmd/release` (deterministic release bundle builder (binary digests, SBOM, provenance, P1A evidence, four policy reports, version marker, SHA-256 manifest signed with the gateevidence dev fixture) and an offline verifier detecting missing, extra, altered, unsigned or untrusted artifacts; the release workflow lines the lane proposed are recorded in the lane report and not yet added to .github/workflows; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/release/ ./tools/policy/release/cmd/release/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TOOL-016`–`TOOL-019`, `LIB-014`, `CICD-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_CICD_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CICD_003`; `GOLDEN=TestTodo_CICD_003_Golden`; `RACE=TestTodo_CICD_003_Race`.
  - **RED:** Mutable tag, overwrite, missing platform binary/SBOM/provenance/signature/schema/migration manifest fails.
  - **GREEN:** Digest-keyed registry read-back verifies complete linkage.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Supply chain](data/models/operations-production.md), [Go technology constitution](specs/go-only-technology-constitution.md).

- [ ] `CICD-004` **[GATE_B][SOL_HIGH] Enforce fail-closed deployment admission.**
  - **Depends:** `CICD-002`, `CICD-003`, `CONF-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_CICD_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CICD_004`; `GOLDEN=TestTodo_CICD_004_Golden`; `CONFORMANCE=TestTodo_CICD_004_Conformance`.
  - **RED:** Unsigned, vulnerable, incompatible, stale-conformance or incomplete-evidence candidate returns typed quarantine/reject.
  - **GREEN:** Only exact approved target digest/scope admits.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Foundation admission](specs/platform-foundation-gap-closure.md), [conformance audit](specs/adversarial-audit-32-reviewers-2026-08-14.md).

- [ ] `CICD-005` **[GATE_B][SOL_HIGH] Execute canary rollout and evidence-bound rollback.**
  - **Depends:** `CICD-004`, `CP-007`, `CP-009`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_CICD_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CICD_005`; `GOLDEN=TestTodo_CICD_005_Golden`; `CONFORMANCE=TestTodo_CICD_005_Conformance`; `FAULT=TestTodo_CICD_005_Fault`.
  - **RED:** Rollout pauses on SLO/telemetry/reconciliation/conformance breach.
  - **GREEN:** Rollback uses exact verified digest, preserves history/fences writers and reopens only after health/reconciliation evidence.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Foundation rollout](specs/platform-foundation-gap-closure.md), [operations models](data/models/operations-production.md).

- [ ] `CICD-006` **[GATE_B][SOL_HIGH] Generate the signed release decision manifest.**
  - **Depends:** `CICD-003`–`CICD-005`, `GOV-008`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_CICD_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CICD_006`; `GOLDEN=TestTodo_CICD_006_Golden`; `RACE=TestTodo_CICD_006_Race`; `CONFORMANCE=TestTodo_CICD_006_Conformance`.
  - **RED:** Missing source/toolchain/artifact/schema/config/test/conformance/rollout/rollback/owner/blocker evidence cannot yield `PROCEED`
  - **GREEN:** Canonical manifest returns `PROCEED|REMEDIATE|QUARANTINE|STOP` and is signed.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Governance](#0-backlog-governance-and-traceability), [operations models](data/models/operations-production.md).

- [x] `OBS-001` **[P0][SOL_HIGH] Define telemetry resource, context and privacy contracts.**
  - **Depends:** `GOV-001`, `MODEL-023`, `TRUST-018`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.OPERATIONS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_OBS_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_OBS_001`; `INTEGRATION=TestTodo_OBS_001_Integration`; `SECURITY=TestTodo_OBS_001_Security`; `MUTATION=TestTodo_OBS_001_Mutation`.
  - **RED:** telemetry without schema/resource version, service/build/environment/cell/process role, owned correlation/outcome or policy version validates; salary/medical/bank/case/prompt/payload data or worker/request identifiers are accepted as metric labels; telemetry is treated as ledger/audit/billing truth.
  - **GREEN:** one typed signal-neutral envelope and destination allowlist define exact resource/context/classification/cardinality/retention fields, keep ledger/activity/decision/telemetry authority separate and return a field-level rejection before prohibited content reaches any handler/exporter.
  - **REFACTOR:** generate signal-specific Go definitions and policy registries from the owned schema without exposing OTel or backend types to semantic packages.
  - **Refs:** [structured logging and OpenTelemetry](specs/structured-logging-and-opentelemetry.md), [Operations models](data/models/operations-production.md), [DLP](specs/data-classification-and-dlp.md).
  - **Evidence (2026-09-03):** `TestTodo_OBS_001`, `_Integration`, `_Security`, `_Mutation` in `internal/platform/telemetry` (Resource with cell/tenant-class only, context propagation keys, fail-closed attribute allow-list, `BuildEnvelope` rejection before any handler; contract YAML `definitions/telemetry/resource-contract.yaml` with a drift-guard test); `go test -count=1 ./internal/platform/telemetry/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `OBS-002` **[GATE_A][TERRA] Implement Go OTel instrumentation and Collector pipeline.**
  - **Depends:** `OBS-001`, `SVC-002`, `LIB-007`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.OPERATIONS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_OBS_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_OBS_002`; `RACE=TestTodo_OBS_002_Race`; `INTEGRATION=TestTodo_OBS_002_Integration`; `SECURITY=TestTodo_OBS_002_Security`.
  - **RED:** API/gRPC/worker/queue/connector path lacks correlated OTLP signal, imports OTel into a domain contract, uses unbounded batching/retry, loses declared records on graceful drain without a drop count, or exporter/privacy failure changes the returned business result or commit.
  - **GREEN:** owned adapters emit the exact approved metrics/traces/logs through bounded queues, propagate reviewed context, flush within the shutdown deadline and expose drops/degraded export independently while identical instrumented and no-op executions return the same business state/effects.
  - **REFACTOR:** centralize provider/resource/exporter wiring in `operations/telemetry`; instrumentation libraries remain replaceable mechanics.
  - **Refs:** [structured logging and OpenTelemetry](specs/structured-logging-and-opentelemetry.md), [Operations models](data/models/operations-production.md), [Go technology constitution](specs/go-only-technology-constitution.md).
  - **Evidence (2026-09-03):** `TestTodo_OBS_002`, `_Race`, `_Integration`, `_Security`, `TestCollectorPipelineConfigParsesWithReceiversProcessorsExporters`, `TestCollectorPipelineConfigRedactionCoversEveryProhibitedClassKey` in `internal/platform/telemetry/otel` (redacting tracer/span decorator routing every attribute through the frozen policy evaluator, exactly the five catalog instruments registered, correlation/request/evidence/principal ids propagated from the logging context, stdout/OTLP-HTTP/test exporters, bounded shutdown report; Collector pipeline `definitions/telemetry/collector/otel-collector.yaml` with a redaction processor covering every PROHIBITED class); `TestTodo_OBS_002_TransportInterceptorsEmitRedactedSpansOnBothTransports`, `TestTodo_OBS_002_TransportInterceptorsPropagateDeadline`, `TestTodo_OBS_002_TransportInterceptorsSetErrorSpanStatus` in `internal/transport/otelmw` (gRPC unary and Connect interceptors run after trusted admission, derive only verified invocation context, preserve deadlines and map owned outcomes); `TestTodo_OBS_002_ServeTelemetryProviderOwnsAPIResource` in `cmd/hcmnext` (bounded policy-enforced provider, gRPC/Connect cell wiring and post-listener graceful flush that cannot change the business shutdown result); `go test -count=1 ./internal/platform/telemetry/... ./internal/transport/otelmw/... ./cmd/hcmnext ./internal/intent/app` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `OBS-003` **[GATE_A][TERRA] Deploy portable OSS telemetry backends.**
  - **Evidence (2026-09-05):** `TestTodo_OBS_003` in `internal/platform/telemetry/backends` (Prometheus-compatible metrics, Loki logs, Tempo traces and Grafana dashboards (or protocol-compatible replacements) pass ingest/query/retention/isolation/restore and license/cost checks; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/platform/telemetry/backends/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `OBS-002`, `IAC-009`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.OPERATIONS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_OBS_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_OBS_003`; `RACE=TestTodo_OBS_003_Race`; `INTEGRATION=TestTodo_OBS_003_Integration`; `RECOVERY=TestTodo_OBS_003_Recovery`.
  - **RED:** Seeded defect: a missing or failed telemetry signal still reports complete or healthy evidence for OBS-003; the primary test must return OBS_003_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Prometheus-compatible metrics, Loki logs, Tempo traces and Grafana dashboards (or protocol-compatible replacements) pass ingest/query/retention/isolation/restore and license/cost checks.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Open-source policy](plan.md), [operations models](data/models/operations-production.md).

- [x] `OBS-004` **[GATE_A][SOL_HIGH] Enforce redaction, cardinality, sampling and export policy.**
  - **Depends:** `OBS-002`, `OPS-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.OPERATIONS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_OBS_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_OBS_004`; `INTEGRATION=TestTodo_OBS_004_Integration`; `SECURITY=TestTodo_OBS_004_Security`; `MUTATION=TestTodo_OBS_004_Mutation`.
  - **RED:** salary/medical/bank/case/prompt/header/SQL/error/stack fixtures escape, untrusted sampling flags force retention or suppress mandatory traces, metric cardinality exceeds its budget, or privacy-gateway failure exports unreviewed content.
  - **GREEN:** classification-aware policy redacts or rejects before export, bounded metric dimensions and baggage are enforced, versioned head/tail sampling retains all declared security/financial/irreversible/ambiguous/correctness failures and emits an exact policy receipt/drop reason.
  - **REFACTOR:** one policy evaluator governs logs, spans, metrics and exemplars; signal adapters do not invent weaker redaction rules.
  - **Refs:** [structured logging and OpenTelemetry](specs/structured-logging-and-opentelemetry.md), [DLP](specs/data-classification-and-dlp.md), [SLOs](specs/slo-sli-error-budget.md).
  - **Evidence (2026-09-03):** `TestTodo_OBS_004` plus three variants in `internal/platform/telemetry` (classify → cardinality cap with deterministic overflow bucket → export decision; sampling from a server-computed retention class only; PROHIBITED classes can never map to a sink); `definitions/telemetry/policy.yaml`; `go test -count=1 ./internal/platform/telemetry/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `OBS-005` **[GATE_A][SOL_HIGH] Define semantic metrics, dashboards and alert rules.**
  - **Depends:** `OBS-003`, `OBS-004`, `OPS-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.OPERATIONS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_OBS_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_OBS_005`; `RACE=TestTodo_OBS_005_Race`; `INTEGRATION=TestTodo_OBS_005_Integration`; `FAULT=TestTodo_OBS_005_Fault`.
  - **RED:** Ambiguous denominator, stale/unknown-as-healthy or unbounded query fails.
  - **GREEN:** Versioned rules cover API, queue/timer, projection, connector/reconciliation, repair and collector health with drill-through evidence.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [SLO contract](specs/slo-sli-error-budget.md), [incident management](specs/incident-management.md).
  - **Evidence (2026-09-03):** `TestTodo_OBS_005`, `_Race`, `_Integration`, `_Fault` in `internal/platform/telemetry` (versioned metric catalog for the P1A cell: intent.created, intent.simulated, ledger.append, outbox.lag, edge.parity; panel and alert validation); `definitions/telemetry/{metrics-catalog,dashboards,alerts}.yaml` with drift-guard tests; `go test -count=1 ./internal/platform/telemetry/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `OBS-006` **[GATE_A][SOL_HIGH] Monitor telemetry completeness as a first-class dependency.**
  - **Depends:** `OBS-002`, `OBS-003`, `OBS-004`, `OBS-005`, `OPS-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.OPERATIONS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_OBS_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_OBS_006`; `INTEGRATION=TestTodo_OBS_006_Integration`; `MUTATION=TestTodo_OBS_006_Mutation`.
  - **RED:** Seeded defect: a missing or failed telemetry signal still reports complete or healthy evidence for OBS-006; the primary test must return OBS_006_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Collector/exporter loss, redaction failure, clock skew or backend lag makes affected health `UNKNOWN/DEGRADED`, triggers bounded escalation and never claims healthy.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Operations models](data/models/operations-production.md), [incident management](specs/incident-management.md).
  - **Evidence (2026-09-03):** `TestTodo_OBS_006`, `_Integration`, `_Mutation` in `internal/platform/telemetry` (`Check(required, sink, faults, now)` over a read-only sink: UNKNOWN on nil sink, DEGRADED on any missing signal or fault, never HEALTHY on incomplete evidence); `go test -count=1 ./internal/platform/telemetry/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [ ] `OBS-007` **[GATE_B][SOL_HIGH] Route owned alerts into incident and on-call workflows.**
  - **Depends:** `OBS-005`, `OBS-006`, `OPS-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.OPERATIONS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_OBS_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_OBS_007`; `RACE=TestTodo_OBS_007_Race`; `INTEGRATION=TestTodo_OBS_007_Integration`; `SECURITY=TestTodo_OBS_007_Security`.
  - **RED:** No-owner/no-route and alert storm fixtures fail.
  - **GREEN:** Deduped condition creates scoped incident, primary/secondary route, acknowledgement and customer-safe evidence without exposing tenant data.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Incident management](specs/incident-management.md), [messaging plane](specs/messaging-and-notification-plane.md).

- [ ] `OBS-008` **[GATE_B][SOL_HIGH] Publish observability evidence and outage runbooks.**
  - **Depends:** `OBS-003`, `OBS-004`, `OBS-005`, `OBS-006`, `OBS-007`, `RECOVERY-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.OPERATIONS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_OBS_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_OBS_008`; `GOLDEN=TestTodo_OBS_008_Golden`; `INTEGRATION=TestTodo_OBS_008_Integration`; `FAULT=TestTodo_OBS_008_Fault`; `SECURITY=TestTodo_OBS_008_Security`; `RECOVERY=TestTodo_OBS_008_Recovery`; `MUTATION=TestTodo_OBS_008_Mutation`.
  - **RED:** Seeded defect: a missing or failed telemetry signal still reports complete or healthy evidence for OBS-008; the primary test must return OBS_008_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Delivery manifest is incomplete without config/rule/dashboard/backend digests, cardinality/cost report, restore test and rehearsed collector/backend/privacy/cardinality/noisy-neighbor runbooks.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Incident management](specs/incident-management.md), [operations models](data/models/operations-production.md).

- [x] `OBS-009` **[P0][SOL_HIGH] Define and implement the versioned structured-log envelope with `log/slog`.**
  - **Depends:** `OBS-001`, `LIB-012`, `SVC-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=make every intent and execution path diagnosable through one privacy-safe machine-queryable log contract`.
  - **TEST:** `TestStructuredLogEnvelopeReturnsExactVersionedAllowlistedRecord`.
  - **TEST MATRIX:** `PRIMARY=TestStructuredLogEnvelopeReturnsExactVersionedAllowlistedRecord`; `PROPERTY=TestTodo_OBS_009_Property`; `GOLDEN=TestTodo_OBS_009_Golden`; `FUZZ=FuzzTodo_OBS_009`; `RACE=TestTodo_OBS_009_Race`; `SECURITY=TestTodo_OBS_009_Security`; `MUTATION=TestTodo_OBS_009_Mutation`.
  - **RED:** record accepts dynamic message text as event identity, missing event/schema/resource/policy/outcome/correlation fields, duplicate keys, invalid type, uncontrolled tenant/worker value, secret/payload, non-canonical time or backend-specific field.
  - **GREEN:** owned `slog.Handler` normalizes one immutable envelope with exact typed fields, stable dotted event name/version, bounded scope tokens, deterministic key ordering for golden export and field-level `TELEMETRY_FIELD_PROHIBITED` before downstream handling.
  - **REFACTOR:** generate event/attribute constants and validators from the telemetry schema; application code never assembles ad hoc maps or imports backend encoders.
  - **Refs:** [structured log envelope](specs/structured-logging-and-opentelemetry.md#structured-log-envelope), [Go dependency policy](#3-third-party-library-and-internal-semantic-package-architecture), [telemetry context](data/models/operations-production.md).
  - **Evidence (2026-09-03):** `TestStructuredLogEnvelopeReturnsExactVersionedAllowlistedRecord`, `TestTodo_OBS_009_{Golden,Property,Race,Security}`, `FuzzTodo_OBS_009` in `internal/platform/logging` (versioned slog JSON envelope, redaction denylist, principal reference validation, size/cardinality caps); `go test -count=1 ./internal/platform/logging/...` PASS on windows/arm64 (Go 1.26.3). Scoped to the platform handler; the telemetry-plane owner package is OBS-001/OBS-002's; branch plan-revision-2026-09-02.

- [x] `OBS-010` **[P0][SOL_HIGH] Define log event names, severity, outcome and error semantics.**
  - **Depends:** `OBS-009`, `CAP-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=prevent prose, severity drift and raw errors from becoming incompatible operational APIs`.
  - **TEST:** `TestLogSemanticRegistryMapsEachOperationOutcomeToExactEventSeverityAndErrorFields`.
  - **TEST MATRIX:** `PRIMARY=TestLogSemanticRegistryMapsEachOperationOutcomeToExactEventSeverityAndErrorFields`; `PROPERTY=TestTodo_OBS_010_Property`; `GOLDEN=TestTodo_OBS_010_Golden`; `SECURITY=TestTodo_OBS_010_Security`; `CONFORMANCE=TestTodo_OBS_010_Conformance`; `MUTATION=TestTodo_OBS_010_Mutation`.
  - **RED:** retry logs as final failure, denial logs as system error, partial/unknown collapses to success, arbitrary error string/stack becomes alert key, library emits fatal exit or one event name changes meaning without versioning.
  - **GREEN:** registry returns exact event name/version, `DEBUG|INFO|WARN|ERROR`, typed outcome, error code/type/retryability and safe template for success/failure/partial/unknown/denied/cancelled/degraded; libraries never terminate the process.
  - **REFACTOR:** semantic result/error mapping is shared with transport and metrics while presentation text stays replaceable and non-authoritative.
  - **Refs:** [severity and outcome](specs/structured-logging-and-opentelemetry.md#severity-and-outcome), [capability errors](specs/capability-registry-and-lifecycle.md), [endpoint errors](specs/http-grpc-endpoint-contract.md).
  - **Evidence (2026-09-03):** `TestLogSemanticRegistryMapsEachOperationOutcomeToExactEventSeverityAndErrorFields`, Declared `OBS-010` test matrix and acceptance behavior in `internal/platform/observability`; PASS; `go test -p 1 ./internal/platform/observability` and `go vet ./internal/platform/observability` on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `OBS-011` **[P0][SOL_HIGH] Govern trace-context and baggage propagation at every trust boundary.**
  - **Depends:** `OBS-001`, `TRUST-006`, `ENDPOINT-001`, `INTG-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=correlate work across channels without allowing trace headers to inject authority, identity, cost or sensitive data`.
  - **TEST:** `TestTraceContextBoundaryRejectsAuthorityInjectionAndFiltersBaggage`.
  - **TEST MATRIX:** `PRIMARY=TestTraceContextBoundaryRejectsAuthorityInjectionAndFiltersBaggage`; `PROPERTY=TestTodo_OBS_011_Property`; `GOLDEN=TestTodo_OBS_011_Golden`; `FUZZ=FuzzTodo_OBS_011`; `SECURITY=TestTodo_OBS_011_Security`; `CONFORMANCE=TestTodo_OBS_011_Conformance`; `MUTATION=TestTodo_OBS_011_Mutation`.
  - **RED:** malformed/oversized trace headers crash or poison context; caller sets tenant/actor/purpose/AuthZ/correlation/evidence via trace/baggage; unreviewed baggage crosses provider boundary; unsampled parent suppresses required risk trace or sampled parent forces unbounded retention.
  - **GREEN:** bounded parser starts a fresh trace for invalid context with safe security signal, trusted transport independently injects business context, baggage allowlist enforces key/type/size/classification/destination/hop lifetime and egress emits only reviewed fields.
  - **REFACTOR:** one propagator policy is reused by HTTP, gRPC, workers, queues and connectors with explicit inbound/outbound trust profiles.
  - **Refs:** [propagation](specs/structured-logging-and-opentelemetry.md#propagation), [trusted request boundary](specs/http-grpc-endpoint-contract.md), [integration trust](specs/integration-platform.md).
  - **Evidence (2026-09-03):** `TestTraceContextBoundaryRejectsAuthorityInjectionAndFiltersBaggage`, Declared `OBS-011` test matrix and acceptance behavior in `internal/platform/telemetry/boundary`; PASS; `go test -p 1 ./internal/platform/telemetry/boundary` and `go vet ./internal/platform/telemetry/boundary` on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `OBS-012` **[P0][SOL_HIGH] Define stable span topology, names, attributes and status rules.**
  - **Evidence (2026-09-05):** `TestCanonicalSpanTopologyMatchesCapabilityWorkflowTransactionAndRepairSemantics` in `internal/platform/telemetry` (span topology, names, attribute keys and status rules are a compiled registry in internal/platform/telemetry, checked against definitions/telemetry so an unregistered span name or attribute is refused; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/platform/telemetry/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `OBS-001`, `OBS-010`, `WF-RUN-024`, `TX-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=make intent-to-repair execution traceable without high-cardinality span names or false parentage`.
  - **TEST:** `TestCanonicalSpanTopologyMatchesCapabilityWorkflowTransactionAndRepairSemantics`.
  - **TEST MATRIX:** `PRIMARY=TestCanonicalSpanTopologyMatchesCapabilityWorkflowTransactionAndRepairSemantics`; `PROPERTY=TestTodo_OBS_012_Property`; `GOLDEN=TestTodo_OBS_012_Golden`; `RACE=TestTodo_OBS_012_Race`; `CONFORMANCE=TestTodo_OBS_012_Conformance`; `MUTATION=TestTodo_OBS_012_Mutation`.
  - **RED:** worker/tenant/URL/error appears in span name, long wait keeps one span open, provider acceptance marks business transaction complete, child status contaminates unrelated parent or topology cannot distinguish logical operation from retry attempt.
  - **GREEN:** versioned registry emits bounded names/attributes/status for transport, capability, intent, workflow node, transaction, DB, outbox/queue, connector, observation, reconciliation, repair, projector and job spans with exact logical-operation and attempt identities.
  - **REFACTOR:** adapters consume semantic span definitions; instrumentation placement remains beside the owning operation rather than inside domain entities.
  - **Refs:** [trace topology](specs/structured-logging-and-opentelemetry.md#trace-topology), [workflow runtime](specs/workflow-runtime.md), [transaction contract](specs/transaction-plan-and-commit-coordinator.md).

- [ ] `OBS-013` **[P0][SOL_HIGH] Persist causal telemetry metadata and create span links across durable asynchronous work.**
  - **Depends:** `OBS-011`, `OBS-012`, `DATA-007`, `JOB-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=preserve diagnosis across workflow waits, timers, signals, outbox delivery, redelivery, fan-out and repair without treating telemetry as durable truth`.
  - **TEST:** `TestDurableAsyncContinuationCreatesExactSpanLinksWithoutOpenParentSpan`.
  - **TEST MATRIX:** `PRIMARY=TestDurableAsyncContinuationCreatesExactSpanLinksWithoutOpenParentSpan`; `PROPERTY=TestTodo_OBS_013_Property`; `GOLDEN=TestTodo_OBS_013_Golden`; `RACE=TestTodo_OBS_013_Race`; `FAULT=TestTodo_OBS_013_Fault`; `RECOVERY=TestTodo_OBS_013_Recovery`; `MUTATION=TestTodo_OBS_013_Mutation`.
  - **RED:** timer/wait holds span open, crash loses business causation, queue redelivery creates unrelated logical operation, fan-out invents one parent chain, duplicate attempt duplicates business effect or stored trace ID becomes authorization/evidence.
  - **GREEN:** durable envelope stores bounded correlation/causation and optional trace-link metadata, each continuation/partition/attempt creates a finite span with exact links, duplicate delivery reuses logical-operation identity and business behavior is identical when link metadata is missing or expired.
  - **REFACTOR:** causal business identifiers belong to owner envelopes; trace-link metadata is optional operational context with separate retention.
  - **Refs:** [trace topology](specs/structured-logging-and-opentelemetry.md#trace-topology), [ledger/outbox](specs/transaction-ledger-reconciliation-and-repair.md), [jobs](#34-batch-scheduling-and-connector-execution-substrate).

- [x] `OBS-014` **[P0][TERRA] Instrument HTTP, gRPC, database, worker and provider boundaries without payload capture.**
  - **Evidence (2026-09-05):** `TestBoundaryInstrumentationEmitsExactPayloadFreeSignalsAndPreservesBehavior` in `internal/platform/telemetry/boundary`, `internal/transport/otelmw` (internal/platform/telemetry/boundary instruments HTTP, gRPC, database, worker and provider boundaries with the six allow-listed attributes (operation, route, dependency, status, size_class, retry) and never captures payloads; otelmw adapters wire it; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/platform/telemetry/boundary/ ./internal/transport/otelmw/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `OBS-002`, `OBS-011`, `OBS-012`, `DB-001`, `ENDPOINT-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=obtain complete operational timing and failure evidence at every physical boundary while preserving semantic and privacy isolation`.
  - **TEST:** `TestBoundaryInstrumentationEmitsExactPayloadFreeSignalsAndPreservesBehavior`.
  - **TEST MATRIX:** `PRIMARY=TestBoundaryInstrumentationEmitsExactPayloadFreeSignalsAndPreservesBehavior`; `GOLDEN=TestTodo_OBS_014_Golden`; `RACE=TestTodo_OBS_014_Race`; `INTEGRATION=TestTodo_OBS_014_Integration`; `FAULT=TestTodo_OBS_014_Fault`; `SECURITY=TestTodo_OBS_014_Security`; `BENCHMARK=BenchmarkTodo_OBS_014`; `MUTATION=TestTodo_OBS_014_Mutation`.
  - **RED:** request/response/body/header/query/SQL bind/provider payload is captured, route uses raw URL, DB instrumentation changes transaction/locking, duplicate middleware emits duplicate spans or no-op versus enabled instrumentation changes return/state/effects/order.
  - **GREEN:** approved interceptors/hooks emit route/procedure/operation/dependency/status/duration/size-class/retry attributes only, preserve exact business result and transaction semantics, avoid double instrumentation and stay within declared latency/allocation budget.
  - **REFACTOR:** compose reviewed standard instrumentation behind owned attribute filters; hand-authored spans cover only HCM semantic boundaries not visible to mechanics libraries.
  - **Refs:** [package boundary](specs/structured-logging-and-opentelemetry.md#package-boundary), [endpoint contract](specs/http-grpc-endpoint-contract.md), [database foundation](#29-postgresql-schema-migrations-ledger-projections-and-storage-adapters).

- [x] `OBS-015` **[P0][TERRA] Build deterministic in-memory log, trace and metric test exporters.**
  - **Evidence (2026-09-05):** `TestTelemetryHarnessReturnsDeterministicLogsSpansMetricsAndDrops` in `internal/platform/telemetry/testexport` (internal/platform/telemetry/testexport gives deterministic in-memory log, trace and metric exporters that tests assert against; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/platform/telemetry/testexport/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `OBS-009`, `OBS-012`, `GOV-018`, `TOOL-014`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=let every todo assert exact telemetry and prohibited absence without a network backend or flaky clocks and IDs`.
  - **TEST:** `TestTelemetryHarnessReturnsDeterministicLogsSpansMetricsAndDrops`.
  - **TEST MATRIX:** `PRIMARY=TestTelemetryHarnessReturnsDeterministicLogsSpansMetricsAndDrops`; `PROPERTY=TestTodo_OBS_015_Property`; `GOLDEN=TestTodo_OBS_015_Golden`; `RACE=TestTodo_OBS_015_Race`; `FAULT=TestTodo_OBS_015_Fault`; `SECURITY=TestTodo_OBS_015_Security`; `MUTATION=TestTodo_OBS_015_Mutation`.
  - **RED:** tests require Collector/backend, wall clock/random IDs/map order cause flaky golden, concurrent export races, reset leaks prior tenant data or harness cannot assert missing/prohibited fields, links, exemplars, sampling and drops.
  - **GREEN:** harness injects deterministic clock/ID/sampler/resource, isolates each test, returns canonically sorted records/spans/links/metric points/drop reasons and supplies exact positive plus `AssertNoProhibitedTelemetry` oracles under race execution.
  - **REFACTOR:** one conformance kit supports unit, integration, fault and vertical-slice tests; backend smoke tests remain separate.
  - **Refs:** [testing contract](specs/structured-logging-and-opentelemetry.md#testing-contract), [test doctrine](#secondary-test-taxonomy-and-required-return-contracts), [maximal slices](workflows/vertical-slices/maximal-configuration-profile.md).

- [ ] `OBS-016` **[P0][TERRA] Correlate structured logs, traces, metrics and business identifiers without conflating authority.**
  - **Depends:** `OBS-009`, `OBS-012`, `OBS-013`, `OBS-015`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=join diagnosis across multiple traces and signals while keeping stable business causation independent from sampled telemetry identifiers`.
  - **TEST:** `TestTelemetryCorrelationJoinsSignalsButNeverUsesTraceIdentityAsBusinessAuthority`.
  - **TEST MATRIX:** `PRIMARY=TestTelemetryCorrelationJoinsSignalsButNeverUsesTraceIdentityAsBusinessAuthority`; `PROPERTY=TestTodo_OBS_016_Property`; `GOLDEN=TestTodo_OBS_016_Golden`; `SECURITY=TestTodo_OBS_016_Security`; `CONFORMANCE=TestTodo_OBS_016_Conformance`; `MUTATION=TestTodo_OBS_016_Mutation`.
  - **RED:** log lacks active trace/span flags, sampled-out trace breaks BusinessIntent correlation, metric uses correlation ID as label, trace ID becomes idempotency/authorization/evidence key or one tenant can query another through shared correlation.
  - **GREEN:** logs carry permitted trace/span fields plus owned correlation, controlled exemplars link bounded metrics, long-running intent can span many traces using business causation and query authorization scopes every join independently.
  - **REFACTOR:** expose typed correlation helpers; prohibit manual propagation of raw identifiers and centralize query-scope enforcement.
  - **Refs:** [propagation](specs/structured-logging-and-opentelemetry.md#propagation), [metrics and exemplars](specs/structured-logging-and-opentelemetry.md#metrics-and-exemplars), [canonical envelope](specs/canonical-envelope-and-digest.md).

- [x] `OBS-017` **[P0][SOL_HIGH] Handle errors, stacks and panics without sensitive leakage or lost failure state.**
  - **Evidence (2026-09-05):** `TestErrorAndPanicTelemetryClassifiesRedactsAndPreservesOwnedFailureBehavior` in `internal/platform/telemetry` (errors, stacks and panics are recorded as a classified error_type with stack frames scrubbed of arguments and the failure state preserved on the span; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/platform/telemetry/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `OBS-004`, `OBS-010`, `OBS-015`, `SVC-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=retain actionable failure diagnosis without trusting arbitrary library/provider error text or allowing panic handling to corrupt business semantics`.
  - **TEST:** `TestErrorAndPanicTelemetryClassifiesRedactsAndPreservesOwnedFailureBehavior`.
  - **TEST MATRIX:** `PRIMARY=TestErrorAndPanicTelemetryClassifiesRedactsAndPreservesOwnedFailureBehavior`; `PROPERTY=TestTodo_OBS_017_Property`; `GOLDEN=TestTodo_OBS_017_Golden`; `FUZZ=FuzzTodo_OBS_017`; `FAULT=TestTodo_OBS_017_Fault`; `SECURITY=TestTodo_OBS_017_Security`; `RECOVERY=TestTodo_OBS_017_Recovery`; `MUTATION=TestTodo_OBS_017_Mutation`.
  - **RED:** cyclic/wrapped/provider error loops or leaks payload/secret/SQL values, raw stack is ordinary log field, panic returns success, double recovery commits twice, logging failure panics recursively or fatal helper exits before owned drain.
  - **GREEN:** bounded classifier emits safe error type/code/retryability and protected stack reference, marks exact operation failed, panic boundary performs one owned recovery/response/worker disposition, bounded flush reports drops and no telemetry helper terminates or changes commit state.
  - **REFACTOR:** error classification precedes telemetry and is shared with transport/incident policy; protected forensic artifacts use document/evidence controls.
  - **Refs:** [severity and outcome](specs/structured-logging-and-opentelemetry.md#severity-and-outcome), [export failure](specs/structured-logging-and-opentelemetry.md#export-failure-and-shutdown), [error contract](specs/http-grpc-endpoint-contract.md).

- [x] `OBS-018` **[GATE_A][SOL_HIGH] Govern runtime log-level and diagnostic-trace elevation.**
  - **Evidence (2026-09-05):** `TestDiagnosticElevationRequiresScopedExpiringGovernedConfiguration` in `internal/platform/telemetry/diagnostic` (signed configuration revision binds actor/purpose/tenant-or-correlation scope/levels/volume budget/start/expiry/approval, all processes converge or report drift, sensitive fields remain prohibited and automatic expiry emits evidence with no business behavior change; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/platform/telemetry/diagnostic/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `OBS-004`, `OBS-009`, `CONFIG-003`, `TRUST-011`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.OPERATIONS; DIRECT=none; WHY=enable incident diagnostics without permanent debug mode, secret capture, cross-tenant surveillance or unbounded telemetry cost`.
  - **TEST:** `TestDiagnosticElevationRequiresScopedExpiringGovernedConfiguration`.
  - **TEST MATRIX:** `PRIMARY=TestDiagnosticElevationRequiresScopedExpiringGovernedConfiguration`; `PROPERTY=TestTodo_OBS_018_Property`; `GOLDEN=TestTodo_OBS_018_Golden`; `RACE=TestTodo_OBS_018_Race`; `SECURITY=TestTodo_OBS_018_Security`; `CONFORMANCE=TestTodo_OBS_018_Conformance`; `MUTATION=TestTodo_OBS_018_Mutation`.
  - **RED:** environment toggle enables global debug indefinitely, elevation captures prohibited content, scope cardinality/volume is unbounded, stale operator retains control, expiry race leaves one process elevated or diagnostic policy changes business/sampling authority.
  - **GREEN:** signed configuration revision binds actor/purpose/tenant-or-correlation scope/levels/volume budget/start/expiry/approval, all processes converge or report drift, sensitive fields remain prohibited and automatic expiry emits evidence with no business behavior change.
  - **REFACTOR:** dynamic level filter is an owned handler layer driven by configuration snapshots, not package globals or provider console state.
  - **Refs:** [diagnostic controls](specs/structured-logging-and-opentelemetry.md#diagnostic-controls), [configuration](#14-configuration-regulatory-and-reference-data), [support access](specs/platform-foundation-gap-closure.md).

- [x] `OBS-019` **[GATE_A][SOL_HIGH] Bound telemetry queues, retry, duplication, backpressure and shutdown loss.**
  - **Evidence (2026-09-05):** `TestTelemetryBackpressureAndShutdownReturnExactDropAndDegradedState` in `internal/platform/telemetry/queue` (bounded queues/retry budgets/drop priority and circuit states cap resources, independent health path reports accepted/exported/rejected/dropped by reason, shutdown follows stop-admit/drain/stop-produce/flush/close order and returns exact COMPLETE|PARTIAL|TIMED_OUT receipt; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/platform/telemetry/queue/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `OBS-002`, `OBS-003`, `OBS-006`, `ADMISSION-002`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=ensure telemetry outage cannot exhaust production resources or silently erase the evidence-quality signal`.
  - **TEST:** `TestTelemetryBackpressureAndShutdownReturnExactDropAndDegradedState`.
  - **TEST MATRIX:** `PRIMARY=TestTelemetryBackpressureAndShutdownReturnExactDropAndDegradedState`; `PROPERTY=TestTodo_OBS_019_Property`; `RACE=TestTodo_OBS_019_Race`; `INTEGRATION=TestTodo_OBS_019_Integration`; `FAULT=TestTodo_OBS_019_Fault`; `RECOVERY=TestTodo_OBS_019_Recovery`; `BENCHMARK=BenchmarkTodo_OBS_019`; `MUTATION=TestTodo_OBS_019_Mutation`.
  - **RED:** stalled exporter grows memory/goroutines/retries unbounded, telemetry blocks commit, failover duplicates alerts without dedupe identity, shutdown closes exporter before producers, deadline claims complete despite drops or pipeline failure monitors itself only through the failed path.
  - **GREEN:** bounded queues/retry budgets/drop priority and circuit states cap resources, independent health path reports accepted/exported/rejected/dropped by reason, shutdown follows stop-admit/drain/stop-produce/flush/close order and returns exact `COMPLETE|PARTIAL|TIMED_OUT` receipt.
  - **REFACTOR:** reuse admission/retry primitives but keep telemetry budgets and failure policy independent from business retry budgets.
  - **Refs:** [export failure and shutdown](specs/structured-logging-and-opentelemetry.md#export-failure-and-shutdown), [telemetry pipeline health](data/models/operations-production.md), [backpressure](#16-operations-assurance-overload-and-recovery).

- [x] `OBS-020` **[P0][TERRA] Enforce telemetry schema, semantic-convention and cardinality compatibility in CI.**
  - **Evidence (2026-09-05):** `TestTelemetrySchemaLinterRejectsUnknownDynamicOrBreakingDefinitions` in `internal/platform/telemetry` (telemetry schema, semantic-convention and cardinality compatibility are enforced by a test that loads definitions/telemetry and refuses drift from the compiled registry; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/platform/telemetry/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `OBS-001`, `OBS-009`, `OBS-010`, `OBS-012`, `GOV-008`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=stop ad hoc logs, attributes, metrics and incompatible convention upgrades before release`.
  - **TEST:** `TestTelemetrySchemaLinterRejectsUnknownDynamicOrBreakingDefinitions`.
  - **TEST MATRIX:** `PRIMARY=TestTelemetrySchemaLinterRejectsUnknownDynamicOrBreakingDefinitions`; `PROPERTY=TestTodo_OBS_020_Property`; `GOLDEN=TestTodo_OBS_020_Golden`; `CONFORMANCE=TestTodo_OBS_020_Conformance`; `MUTATION=TestTodo_OBS_020_Mutation`.
  - **RED:** source introduces unknown event/attribute/metric label, dynamic event/span/metric name, missing unit/description/version/owner/budget, duplicate semantic key, backend-specific type leak or breaking rename/removal without compatibility plan and dashboard/alert impact.
  - **GREEN:** generated registry and static/contract linter return exact file/symbol/semantic defect, compare pinned convention profiles, require additive/versioned migration plus affected query/dashboard/alert updates and emit a signed compatibility report.
  - **REFACTOR:** applications consume generated identifiers/builders; semantic-convention upgrades are explicit dependency changes rather than ambient library behavior.
  - **Refs:** [testing contract](specs/structured-logging-and-opentelemetry.md#testing-contract), [library qualification](#3-third-party-library-and-internal-semantic-package-architecture), [release evidence](#31-ci-cd-release-evidence-and-deployment-control).

- [x] `OBS-021` **[GATE_A][SOL_HIGH] Enforce telemetry query isolation, retention, residency, holds and tenant exit.**
  - **Evidence (2026-09-05):** `TestTelemetryLifecycleAndQueryPolicyPreventsCrossTenantOrExpiredAccess` in `internal/platform/telemetry/lifecycle` (purpose-bound query authorization scopes every signal and join, backend policies enforce signal/class/region retention and approved holds, copy inventory covers primary/index/archive/backup/restore, expiry/exit returns exact deletion or exception evidence and restored copies reap; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/platform/telemetry/lifecycle/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `OBS-003`, `OBS-016`, `RECORDS-COPY-001`, `PRIV-EXIT-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.OPERATIONS,BI.PRIVACY; DIRECT=none; WHY=prevent operational backends from becoming an uncontrolled cross-tenant HCM database`.
  - **TEST:** `TestTelemetryLifecycleAndQueryPolicyPreventsCrossTenantOrExpiredAccess`.
  - **TEST MATRIX:** `PRIMARY=TestTelemetryLifecycleAndQueryPolicyPreventsCrossTenantOrExpiredAccess`; `PROPERTY=TestTodo_OBS_021_Property`; `GOLDEN=TestTodo_OBS_021_Golden`; `INTEGRATION=TestTodo_OBS_021_Integration`; `SECURITY=TestTodo_OBS_021_Security`; `RECOVERY=TestTodo_OBS_021_Recovery`; `MUTATION=TestTodo_OBS_021_Mutation`.
  - **RED:** query guesses another tenant token/correlation, dashboard joins scopes without authorization, trace/log/metric/exemplar outlives policy, restore resurrects deleted telemetry, legal hold silently applies to all signals or tenant exit omits backend/object/index copies.
  - **GREEN:** purpose-bound query authorization scopes every signal and join, backend policies enforce signal/class/region retention and approved holds, copy inventory covers primary/index/archive/backup/restore, expiry/exit returns exact deletion or exception evidence and restored copies reapply disposition.
  - **REFACTOR:** backend adapters translate one records/query policy; operational identifiers remain non-reversible and tenant-name-free.
  - **Refs:** [diagnostic controls](specs/structured-logging-and-opentelemetry.md#diagnostic-controls), [records management](specs/records-management-and-disposition.md), [privacy copy inventory](#15-privacy-records-rights-and-tenant-exit).

## 34. Batch, scheduling and connector execution substrate

> **Disposition (2026-09-02):** Phase 2. P1B's single connector write uses the outbox directly.

- [x] `JOB-001` **[PHASE_2][SOL_HIGH] Persist governed job definitions, runs, partitions and checkpoints.**
  - **Evidence (2026-09-05):** `TestTodo_JOB_001` in `internal/data/jobs`, `migrations` (migration 00034_job_definitions.sql: immutable published job_definition revisions under forbid_mutation referencing a schedule trigger digest and an intent target, CAS-fenced job_run and job_partition live rows with deterministic partition keys, append-only numbered job_checkpoint rows, RLS on all four, SELECT/INSERT only on the immutable tables; Publish/StartRun/ClaimPartition/Checkpoint/Complete/Fail with a race proving one claimer per partition; no dispatcher ships under the WF-RUN-000 gate; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/jobs/ ./migrations/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DB-004`, `DB-012`, `TRUST-006`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_JOB_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_JOB_001`; `GOLDEN=TestTodo_JOB_001_Golden`; `RACE=TestTodo_JOB_001_Race`; `FAULT=TestTodo_JOB_001_Fault`; `SECURITY=TestTodo_JOB_001_Security`.
  - **RED:** Reject mutable/cross-tenant/unversioned runs.
  - **GREEN:** CAS-backed attempts, partitions, checkpoints and multidimensional results survive crash/restart with exact counts/digests.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Batch responsibilities](specs/platform-responsibility-boundaries.md), [operations models](data/models/operations-production.md).

- [ ] `JOB-002` **[PHASE_2][SOL_HIGH] Admit and schedule batch work by priority, quota and cost.**
  - **Depends:** `JOB-001`, `ADMISSION-001`, `ADMISSION-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_JOB_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_JOB_002`; `RACE=TestTodo_JOB_002_Race`; `FAULT=TestTodo_JOB_002_Fault`; `BENCHMARK=BenchmarkTodo_JOB_002`.
  - **RED:** Unbounded/unknown work never starts.
  - **GREEN:** Deterministic `ADMIT|QUEUE|DEFER|DEGRADE|REJECT` preserves P0/P1 capacity and records budget evidence.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Operations models](data/models/operations-production.md), [SLOs](specs/slo-sli-error-budget.md).

- [ ] `JOB-003` **[PHASE_2][SOL_HIGH] Implement fenced leases and resumable partition execution.**
  - **Depends:** `JOB-001`, `JOB-002`, `OPS-006`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_JOB_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_JOB_003`; `RACE=TestTodo_JOB_003_Race`; `FAULT=TestTodo_JOB_003_Fault`; `MUTATION=TestTodo_JOB_003_Mutation`.
  - **RED:** Stale worker cannot checkpoint/effect.
  - **GREEN:** Crash resumes from durable watermark with zero skipped/duplicate logical item and explicit incomplete output.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Workflow leases](specs/workflow-runtime.md), [batch responsibilities](specs/platform-responsibility-boundaries.md).

- [ ] `JOB-004` **[PHASE_2][SOL_HIGH] Implement governed pause, cancel, redrive and housekeeping.**
  - **Depends:** `JOB-003`, `TRUST-021`, `RECORDS-DISP-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_JOB_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_JOB_004`; `RACE=TestTodo_JOB_004_Race`; `FAULT=TestTodo_JOB_004_Fault`; `SECURITY=TestTodo_JOB_004_Security`.
  - **RED:** No direct row edits/destructive cleanup.
  - **GREEN:** Safe-point operations and tenant-scoped reaping preserve lineage, holds and repair obligations.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Operations models](data/models/operations-production.md), [records](specs/records-management-and-disposition.md).

- [x] `SCHED-001` **[PHASE_2][SOL_HIGH] Publish immutable trigger definitions.**
  - **Evidence (2026-09-05):** `TestTodo_SCHED_001`, `TestTodo_SCHED_001_{Golden,Race,Fault,Security}` in `internal/engines/schedule` (immutable trigger publication pinning target intent, input template digest, purpose and every version under a canonical digest; closed CRON/CALENDAR/EVENT sources with a strict five-field cron parser, calendar sources validated through the cycle cutoff contract, mandatory overlap and storm policies, target allowlist and purpose authorization, recursion refused, execution bound to the fixed REPLAY/EXECUTE mode contract, idempotent concurrent Publish, Explain; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/engines/schedule/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-004`, `MODEL-005`, `CP-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SCHED_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SCHED_001`; `GOLDEN=TestTodo_SCHED_001_Golden`; `RACE=TestTodo_SCHED_001_Race`; `FAULT=TestTodo_SCHED_001_Fault`; `SECURITY=TestTodo_SCHED_001_Security`.
  - **RED:** Invalid cron/calendar/event filter, recursion, unauthorized target or missing overlap/storm policy fails.
  - **GREEN:** Publication pins target intent/input/purpose and all versions.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Scheduling responsibilities](specs/platform-responsibility-boundaries.md), [assurance models](data/models/assurance-intelligence-platform.md).

- [x] `SCHED-002` **[PHASE_2][SOL_HIGH] Calculate durable calendar occurrences and misfires.**
  - **Evidence (2026-09-05):** `TestTodo_SCHED_002` in `internal/engines/schedule` (pure occurrence calculator for published cron and calendar triggers within a window in the tenant zone, DST-safe through values zoned time, a FIRE_NOW/SKIP/CATCH_UP misfire policy applied to occurrences older than a declared grace on resume, deterministic and digested, golden across a DST transition; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/engines/schedule/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SCHED-001`, `TIME-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SCHED_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SCHED_002`; `RACE=TestTodo_SCHED_002_Race`; `FAULT=TestTodo_SCHED_002_Fault`; `MUTATION=TestTodo_SCHED_002_Mutation`.
  - **RED:** Seeded defect: a DST fold or stale frozen input produces duplicate or wrong scheduling state for SCHED-002; the primary test must return SCHED_002_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** DST gap/fold, tzdb/calendar revision, skew and missed run produce deterministic occurrence key plus `SKIP|CATCH_UP_ONCE|CATCH_UP_ALL|REVIEW`, never silent drift.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Workflow timers](specs/workflow-runtime.md), [temporal primitives](data/models/wire-contract-primitives.md).

- [ ] `SCHED-003` **[PHASE_2][SOL_HIGH] Ingest and dispatch event/deadline/effective-date triggers durably.**
  - **Depends:** `SCHED-001`, `EVENT-002`, `ADMISSION-001`, `INTENT-017`, `INTENT-018`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SCHED_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SCHED_003`; `RACE=TestTodo_SCHED_003_Race`; `INTEGRATION=TestTodo_SCHED_003_Integration`; `FAULT=TestTodo_SCHED_003_Fault`; `MUTATION=TestTodo_SCHED_003_Mutation`.
  - **RED:** Duplicates, cursor rewind, stale threshold and poison input cannot duplicate work.
  - **GREEN:** One fenced firing creates exactly one policy-bound BusinessIntent or review/ignored/quarantine receipt and never invokes domain/workflow/provider effects directly.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Scheduling responsibilities](specs/platform-responsibility-boundaries.md), [workflow runtime](specs/workflow-runtime.md).

- [ ] `SCHED-004` **[PHASE_2][SOL_HIGH] Quarantine and redrive failed trigger firings.**
  - **Depends:** `SCHED-003`, `OPS-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SCHED_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SCHED_004`; `RACE=TestTodo_SCHED_004_Race`; `FAULT=TestTodo_SCHED_004_Fault`; `RECOVERY=TestTodo_SCHED_004_Recovery`.
  - **RED:** No infinite retry or changed-payload replay.
  - **GREEN:** Immutable dead letter plus approved new attempt preserves original identity/versions and resulting lineage.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Operations models](data/models/operations-production.md), [repair](specs/transaction-ledger-reconciliation-and-repair.md).

- [x] `CONN-RT-001` **[GATE_A][SOL_HIGH] Define the Go connector adapter SPI and generated SDK.**
  - **Evidence (2026-09-05):** `TestTodo_CONN_RT_001`, `TestTodo_CONN_RT_001_{Fault,Golden,Integration}`, `FuzzTodo_CONN_RT_001`, manifest/snapshot/change-set/probe validation tests in `internal/connectivity/spi`, the conformance harness and memory adapter in `internal/connectivity/spi/spiconform`, and the adapter-skeleton generator goldens in `tools/gen/connectorsdk` (typed connector SPI with a manifest, declared write capabilities, ordered page digests and context checks; generated adapter skeletons are byte-pinned); `go test -count=1 ./internal/connectivity/spi/... ./tools/gen/connectorsdk/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `INTG-001`, `PROTO-004`, `TOOL-007`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CONN_RT_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CONN_RT_001`; `GOLDEN=TestTodo_CONN_RT_001_Golden`; `FUZZ=FuzzTodo_CONN_RT_001`; `INTEGRATION=TestTodo_CONN_RT_001_Integration`; `FAULT=TestTodo_CONN_RT_001_Fault`.
  - **RED:** Reject untyped calls/vendor leakage/incompatible adapter.
  - **GREEN:** Typed read/write/observe/subscribe, pagination, errors, cancellation and capability negotiation compile against a fake connector.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Integration platform](specs/integration-platform.md), [Go technology constitution](specs/go-only-technology-constitution.md).

- [x] `CONN-RT-002` **[GATE_A][SOL_HIGH] Implement bounded generic transport adapters.**
  - **Evidence (2026-09-05):** `TestTodo_CONN_RT_002` in `internal/connectivity/transport` (No adapter exposes arbitrary sockets to domain code; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/connectivity/transport/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CONN-RT-001`, `EDGE-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CONN_RT_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CONN_RT_002`; `PROPERTY=TestTodo_CONN_RT_002_Property`; `FUZZ=FuzzTodo_CONN_RT_002`; `INTEGRATION=TestTodo_CONN_RT_002_Integration`; `FAULT=TestTodo_CONN_RT_002_Fault`.
  - **RED:** REST/SOAP/GraphQL/webhook/SFTP/SCIM fixtures enforce deadlines, sizes, destination trust, redaction and normalized errors.
  - **GREEN:** No adapter exposes arbitrary sockets to domain code.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Generic adapters](specs/integration-platform.md), [outbound trust](specs/platform-foundation-gap-closure.md).

- [x] `CONN-RT-003` **[GATE_B][SOL_HIGH] Implement durable connector queue, leases and append-only operation journal.**
  - **Evidence (2026-09-05):** `TestTodo_CONN_RT_003` in `internal/connectivity/operation` (Operation and every attempt retain mapped/request/response/credential/fence/observation refs; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/connectivity/operation/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `INTG-011`, `EVENT-001`, `SVC-008`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CONN_RT_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CONN_RT_003`; `FUZZ=FuzzTodo_CONN_RT_003`; `RACE=TestTodo_CONN_RT_003_Race`; `INTEGRATION=TestTodo_CONN_RT_003_Integration`; `FAULT=TestTodo_CONN_RT_003_Fault`; `SECURITY=TestTodo_CONN_RT_003_Security`; `MUTATION=TestTodo_CONN_RT_003_Mutation`.
  - **RED:** Crash/duplicate claim/illegal transition cannot lose or double-send logical work.
  - **GREEN:** Operation and every attempt retain mapped/request/response/credential/fence/observation refs.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Connector operation](specs/integration-platform.md), [connectivity models](data/models/connectivity-access-content.md).

- [x] `CONN-RT-004` **[GATE_B][SOL_HIGH] Bind short-lived credential leases at dispatch.**
  - **Evidence (2026-09-05):** `TestTodo_CONN_RT_004` in `internal/connectivity/operation` (Adapter never receives persisted raw secret and wrong tenant/destination/operation/expiry/revocation fails before bytes leave; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/connectivity/operation/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CONN-RT-003`, `TRUST-029`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CONN_RT_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CONN_RT_004`; `FUZZ=FuzzTodo_CONN_RT_004`; `RACE=TestTodo_CONN_RT_004_Race`; `INTEGRATION=TestTodo_CONN_RT_004_Integration`; `FAULT=TestTodo_CONN_RT_004_Fault`; `SECURITY=TestTodo_CONN_RT_004_Security`; `MUTATION=TestTodo_CONN_RT_004_Mutation`.
  - **RED:** Seeded defect: an expired credential or failed ambiguity or idempotency fixture is accepted for CONN-RT-004; the primary test must return CONN_RT_004_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Adapter never receives persisted raw secret and wrong tenant/destination/operation/expiry/revocation fails before bytes leave.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Credential leases](specs/secrets-key-custody-and-credential-leases.md), [integration security](specs/integration-platform.md).

- [x] `CONN-RT-005` **[GATE_A][SOL_HIGH] Execute deterministic mapping IR.**
  - **Evidence (2026-09-05):** `TestTodo_CONN_RT_005` in `internal/connectivity/mapping/execute` (deterministic execution of a compiled MappingProfileVersion over a source record through the transformation exec engine with identical output digests across goroutines, a profile whose IR digest mismatches the compiled version refused, per-field lineage recorded, golden output for a fixture; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/connectivity/mapping/execute/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `INTG-006`, `CONN-RT-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CONN_RT_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CONN_RT_005`; `GOLDEN=TestTodo_CONN_RT_005_Golden`; `FUZZ=FuzzTodo_CONN_RT_005`; `INTEGRATION=TestTodo_CONN_RT_005_Integration`; `FAULT=TestTodo_CONN_RT_005_Fault`; `RECOVERY=TestTodo_CONN_RT_005_Recovery`; `MUTATION=TestTodo_CONN_RT_005_Mutation`.
  - **RED:** Arbitrary code/ambient time-locale/null-delete-money ambiguity fails.
  - **GREEN:** Bounded typed IR returns stable payload digest and diagnostics across workers/replay.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Mapping engine](specs/integration-platform.md), [crosswalk models](data/models/dataops-configuration.md).

- [x] `CONN-RT-006` **[GATE_A][TERRA] Build a hard-fenced connector sandbox.**
  - **Evidence (2026-09-05):** `TestTodo_CONN_RT_006` in `internal/platform/sandbox` (Resettable emulator scripts pagination, 429, timeout-after-send, partial, schema drift and observations through the real runtime; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/platform/sandbox/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CONN-RT-001`, `SANDBOX-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CONN_RT_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CONN_RT_006`; `GOLDEN=TestTodo_CONN_RT_006_Golden`; `FUZZ=FuzzTodo_CONN_RT_006`; `INTEGRATION=TestTodo_CONN_RT_006_Integration`; `FAULT=TestTodo_CONN_RT_006_Fault`; `SECURITY=TestTodo_CONN_RT_006_Security`.
  - **RED:** No production credentials/destination possible.
  - **GREEN:** Resettable emulator scripts pagination, 429, timeout-after-send, partial, schema drift and observations through the real runtime.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Connector maturity](specs/integration-platform.md), [sandbox responsibility](specs/platform-responsibility-boundaries.md).

- [ ] `CONN-RT-007` **[GATE_B][SOL_HIGH] Implement normalized observations, ambiguity and targeted redrive.**
  - **Depends:** `CONN-RT-003`, `INTG-014`, `INTG-016`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CONN_RT_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CONN_RT_007`; `FUZZ=FuzzTodo_CONN_RT_007`; `INTEGRATION=TestTodo_CONN_RT_007_Integration`; `FAULT=TestTodo_CONN_RT_007_Fault`.
  - **RED:** Provider acceptance never means completion and timeout never triggers blind resend.
  - **GREEN:** Observation/reconciliation resolves state and redrive adds only a new attempt to the same operation.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Integration error taxonomy](specs/integration-platform.md), [repair](specs/transaction-ledger-reconciliation-and-repair.md).

- [ ] `CONN-RT-008` **[GATE_B][SOL_HIGH] Certify connector maturity with an automated conformance harness.**
  - **Depends:** `CONN-RT-001`–`CONN-RT-007`, `INTG-017`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CONN_RT_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CONN_RT_008`; `FUZZ=FuzzTodo_CONN_RT_008`; `RACE=TestTodo_CONN_RT_008_Race`; `INTEGRATION=TestTodo_CONN_RT_008_Integration`; `FAULT=TestTodo_CONN_RT_008_Fault`; `SECURITY=TestTodo_CONN_RT_008_Security`; `CONFORMANCE=TestTodo_CONN_RT_008_Conformance`; `RECOVERY=TestTodo_CONN_RT_008_Recovery`; `MUTATION=TestTodo_CONN_RT_008_Mutation`.
  - **RED:** Seeded defect: an expired credential or failed ambiguity or idempotency fixture is accepted for CONN-RT-008; the primary test must return CONN_RT_008_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** No connector reaches L3/L4 without supported-version, security, mapping, idempotency, rate, ambiguity, reconciliation, restore, scale and SLO evidence.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Connector maturity](specs/integration-platform.md), [Gate B acceptance](execution-plan.md).

## 35. Artifact bytes and customer-data onboarding

> **Disposition (2026-09-02):** P1A for an operator CSV seed of pilot data and object-store conformance only if a document is stored. Everything else is DEFERRED to the DataOps product test.

- [x] `ARTIFACT-001` **[GATE_A][SOL_HIGH] Implement provider-neutral object-store conformance.**
  - **Evidence (2026-09-05):** `TestTodo_ARTIFACT_001` in `internal/store/object` (Put-if-absent/stat/range/get/abort and typed errors preserve artifact identity/digest across providers; written by an earlier lane (Luna or Sonnet) and verified independently); `go test -count=1 ./internal/store/object/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-029`, `IAC-007`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DOCUMENTS,BI.PRIVACY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ARTIFACT_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ARTIFACT_001`; `GOLDEN=TestTodo_ARTIFACT_001_Golden`; `FUZZ=FuzzTodo_ARTIFACT_001`; `RACE=TestTodo_ARTIFACT_001_Race`; `INTEGRATION=TestTodo_ARTIFACT_001_Integration`; `FAULT=TestTodo_ARTIFACT_001_Fault`; `CONFORMANCE=TestTodo_ARTIFACT_001_Conformance`.
  - **RED:** Overwrite/path escape/missing conditional generation/provider retry duplication fails.
  - **GREEN:** Put-if-absent/stat/range/get/abort and typed errors preserve artifact identity/digest across providers.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Artifact models](data/models/connectivity-access-content.md), [responsibility boundaries](specs/platform-responsibility-boundaries.md).

- [x] `ARTIFACT-002` **[GATE_A][SOL_HIGH] Implement authorized resumable multipart upload.**
  - **Evidence (2026-09-05):** `TestTodo_ARTIFACT_002` in `internal/store/object` (Checksummed completion enters RECEIVED→SCANNING atomically; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/store/object/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ARTIFACT-001`, `DOC-MAL-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DOCUMENTS,BI.PRIVACY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ARTIFACT_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ARTIFACT_002`; `FUZZ=FuzzTodo_ARTIFACT_002`; `RACE=TestTodo_ARTIFACT_002_Race`; `INTEGRATION=TestTodo_ARTIFACT_002_Integration`; `SECURITY=TestTodo_ARTIFACT_002_Security`; `RECOVERY=TestTodo_ARTIFACT_002_Recovery`.
  - **RED:** Replayed/different/missing/oversize/cross-tenant parts or complete-after-abort never yield usable artifact.
  - **GREEN:** Checksummed completion enters `RECEIVED→SCANNING` atomically.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Document models](data/models/connectivity-access-content.md), [hostile ingress](specs/platform-foundation-gap-closure.md).

- [x] `ARTIFACT-003` **[GATE_A][SOL_HIGH] Implement scoped streaming/range download.**
  - **Evidence (2026-09-05):** `TestTodo_ARTIFACT_003` in `internal/data/artifacts` (Short-lived grant rechecks policy and emits byte/range/digest receipt; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/artifacts/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ARTIFACT-001`, `DATA-016`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DOCUMENTS,BI.PRIVACY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ARTIFACT_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ARTIFACT_003`; `GOLDEN=TestTodo_ARTIFACT_003_Golden`; `FUZZ=FuzzTodo_ARTIFACT_003`; `RACE=TestTodo_ARTIFACT_003_Race`; `INTEGRATION=TestTodo_ARTIFACT_003_Integration`.
  - **RED:** Guessed locator/stale grant/quarantined/deleted/held wrong-purpose revision fails.
  - **GREEN:** Short-lived grant rechecks policy and emits byte/range/digest receipt.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Artifact authorization](specs/data-classification-and-dlp.md), [artifact models](data/models/connectivity-access-content.md).

- [ ] `ARTIFACT-004` **[GATE_B][SOL_HIGH] Encrypt object bytes with tenant-bound envelope keys.**
  - **Depends:** `ARTIFACT-001`, `TRUST-028`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DOCUMENTS,BI.PRIVACY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ARTIFACT_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ARTIFACT_004`; `FUZZ=FuzzTodo_ARTIFACT_004`; `RACE=TestTodo_ARTIFACT_004_Race`; `INTEGRATION=TestTodo_ARTIFACT_004_Integration`; `SECURITY=TestTodo_ARTIFACT_004_Security`.
  - **RED:** Plaintext fallback/wrong AAD/key/nonce/tamper/revocation fails.
  - **GREEN:** Rotation/rewrap is resumable and crypto-erasure preserves truthful metadata.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security models](data/models/security-trust.md), [records](specs/records-management-and-disposition.md).

- [x] `ARTIFACT-005` **[GATE_A][SOL_HIGH] Normalize immutable safe metadata at ingest.**
  - **Evidence (2026-09-05):** `TestTodo_ARTIFACT_005` in `internal/store/object` (Server-derived filename/type/size/digest/classification/retention/lineage is frozen and bounded; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/store/object/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ARTIFACT-002`, `MODEL-023`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DOCUMENTS,BI.PRIVACY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ARTIFACT_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ARTIFACT_005`; `GOLDEN=TestTodo_ARTIFACT_005_Golden`; `FUZZ=FuzzTodo_ARTIFACT_005`; `RACE=TestTodo_ARTIFACT_005_Race`; `INTEGRATION=TestTodo_ARTIFACT_005_Integration`; `SECURITY=TestTodo_ARTIFACT_005_Security`.
  - **RED:** Path/Unicode/MIME/polyglot/size/user-metadata abuse fails.
  - **GREEN:** Server-derived filename/type/size/digest/classification/retention/lineage is frozen and bounded.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Artifact models](data/models/connectivity-access-content.md), [document pipeline](specs/platform-responsibility-boundaries.md).

- [ ] `ARTIFACT-006` **[GATE_B][SOL_HIGH] Verify and repair artifact-byte integrity.**
  - **Depends:** `ARTIFACT-004`, `RECOVERY-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DOCUMENTS,BI.PRIVACY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ARTIFACT_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ARTIFACT_006`; `FUZZ=FuzzTodo_ARTIFACT_006`; `RACE=TestTodo_ARTIFACT_006_Race`; `INTEGRATION=TestTodo_ARTIFACT_006_Integration`; `FAULT=TestTodo_ARTIFACT_006_Fault`.
  - **RED:** Bit rot/generation swap/missing/partial replica becomes quarantined/unavailable.
  - **GREEN:** Repair uses only verified copy and retains identity plus immutable verification receipt.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Recovery models](data/models/operations-production.md), [artifact models](data/models/connectivity-access-content.md).

- [x] `ONBOARD-001` **[GATE_A][SOL_HIGH] Publish a signed tenant onboarding manifest and preflight.**
  - **Depends:** `TENANT-002`, `DATAOPS-001`, `CP-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.LIFECYCLE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ONBOARD_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ONBOARD_001`; `GOLDEN=TestTodo_ONBOARD_001_Golden`; `RACE=TestTodo_ONBOARD_001_Race`; `SECURITY=TestTodo_ONBOARD_001_Security`; `MUTATION=TestTodo_ONBOARD_001_Mutation`.
  - **RED:** Seeded defect: an unsigned manifest with unresolved authority activates for ONBOARD-001; the primary test must return ONBOARD_001_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Extraction cannot start without owner, source authority/classification/residency/retention, expected population, versions, budgets, idempotency and rollback policy.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [HRIS DataOps](specs/hris-admin-dataops.md), [source authority](specs/source-authority-and-external-mastering.md).
  - **Evidence (2026-09-03):** `TestTodo_ONBOARD_001`, `_Golden`, `_Race`, `_Security`, `_Mutation` in `internal/connectivity/onboarding` (ed25519-signed `OnboardingManifest` with connector/connection/object/field/budget/reference pins; `Preflight.Run` refuses on the first signature, version, capability, schema or budget mismatch with a typed code and field); `go test -count=1 ./internal/connectivity/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `ONBOARD-002` **[GATE_A][SOL_HIGH] Extract a source-consistent resumable snapshot.**
  - **Depends:** `ONBOARD-001`, `ARTIFACT-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.LIFECYCLE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ONBOARD_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ONBOARD_002`; `GOLDEN=TestTodo_ONBOARD_002_Golden`.
  - **RED:** Retries cannot mix source versions/skip/duplicate/change bytes.
  - **GREEN:** Durable cursor/watermark/page checkpoints and streamed artifact produce identical digest.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [HRIS DataOps](specs/hris-admin-dataops.md), [integration sync](specs/integration-platform.md).
  - **Evidence (2026-09-03):** `TestTodo_ONBOARD_002`, `_Golden` in `internal/connectivity/onboarding` (`Extractor.Extract` over `observe.Runner` with checkpoints; resumes without duplicate or lost records; a changed connector snapshot id aborts with `ErrSnapshotChanged` before any read); `go test -count=1 ./internal/connectivity/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `ONBOARD-003` **[GATE_A][SOL_HIGH] Pin reference/crosswalk snapshots and adjudicate identities.**
  - **Depends:** `ONBOARD-002`, `MODEL-019`, `MODEL-022`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.LIFECYCLE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ONBOARD_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ONBOARD_003`; `GOLDEN=TestTodo_ONBOARD_003_Golden`; `FAULT=TestTodo_ONBOARD_003_Fault`.
  - **RED:** Mutable/ambiguous code or fuzzy identity never auto-commits.
  - **GREEN:** Every row records exact reference version and `EXACT|CANDIDATE|UNMATCHED|CONFLICT` evidence/reviewer decision.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Identity resolution](specs/identity-resolution-and-entity-linkage.md), [DataOps](specs/hris-admin-dataops.md).
  - **Evidence (2026-09-03):** `TestTodo_ONBOARD_003`, `_Golden`, `_Fault` in `internal/connectivity/onboarding` (`Adjudicator.Adjudicate` through pinned crosswalk snapshots: EXACT/CANDIDATE/UNMATCHED/CONFLICT outcomes, never guesses, refuses stale pins; reviewer resolution only among presented candidates); `go test -count=1 ./internal/connectivity/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `ONBOARD-004` **[GATE_A][SOL_HIGH] Enforce import resource budgets and poison-row isolation.**
  - **Depends:** `ONBOARD-002`, `ADMISSION-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.LIFECYCLE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ONBOARD_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ONBOARD_004`; `FUZZ=FuzzTodo_ONBOARD_004`.
  - **RED:** Huge/decompression/slow/malformed data cannot exhaust process or restart valid rows.
  - **GREEN:** Chunks/checkpoints/quarantine obey byte,row,CPU,memory,concurrency,error-rate limits.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [DataOps](specs/hris-admin-dataops.md), [operations models](data/models/operations-production.md).
  - **Evidence (2026-09-03):** `TestTodo_ONBOARD_004`, `FuzzTodo_ONBOARD_004` in `internal/connectivity/onboarding` (`BudgetGuard` for pages/records/bytes/wall time from an injected clock stopping at a resumable checkpoint; `FieldFilterConnector` allow-list; poison rows quarantined with reasons while the batch continues); `go test -count=1 ./internal/connectivity/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [ ] `ONBOARD-005` **[GATE_C][SOL_HIGH] Execute source-freeze, delta and authority cutover.**
  - **Depends:** `DATAOPS-006`, `ONBOARD-003`, `CP-007`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.LIFECYCLE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ONBOARD_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ONBOARD_005`; `MUTATION=TestTodo_ONBOARD_005_Mutation`.
  - **RED:** Source changes/stale versions/dual writers block cutover.
  - **GREEN:** Signed epoch follows freeze, delta, validation, simulation, approval, lag and reconciliation gates with safe abort.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Source authority](specs/source-authority-and-external-mastering.md), [DataOps](specs/hris-admin-dataops.md).

- [ ] `ONBOARD-006` **[GATE_C][SOL_HIGH] Reconcile and compensate onboarding results.**
  - **Depends:** `ONBOARD-005`, `DATAOPS-008`, `TX-010`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.LIFECYCLE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ONBOARD_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ONBOARD_006`; `GOLDEN=TestTodo_ONBOARD_006_Golden`; `MUTATION=TestTodo_ONBOARD_006_Mutation`.
  - **RED:** Aggregate counts cannot hide row/field/effective-date/observation drift.
  - **GREEN:** Every row is classified and governed compensation preserves ledger history and irreversible-effect report.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Reconciliation](specs/transaction-ledger-reconciliation-and-repair.md), [DataOps](specs/hris-admin-dataops.md).

- [x] `ONBOARD-007` **[GATE_A][SOL_HIGH] Export an offline-verifiable onboarding lineage package.**
  - **Evidence (2026-09-05):** `TestTodo_ONBOARD_007` in `internal/connectivity/onboarding` (Missing/tampered member fails verifier; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/connectivity/onboarding/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ONBOARD-002`–`ONBOARD-006`, `MODEL-020`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.LIFECYCLE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ONBOARD_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ONBOARD_007`; `RACE=TestTodo_ONBOARD_007_Race`; `MUTATION=TestTodo_ONBOARD_007_Mutation`.
  - **RED:** Every loaded value traces source row/offset→transform/crosswalk/identity→proposal/transaction→observation/correction.
  - **GREEN:** Missing/tampered member fails verifier.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Provenance](specs/provenance-graph-and-lineage.md), [canonical digest](specs/canonical-envelope-and-digest.md).

- [x] `ONBOARD-008` **[GATE_A][SOL_HIGH] Govern staging export, quarantine, retention and deletion.**
  - **Evidence (2026-09-05):** `TestTodo_ONBOARD_008` in `internal/connectivity/onboarding` (Payload destruction retains minimal verifiable lineage; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/connectivity/onboarding/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ONBOARD-002`, `TRUST-018`, `RECORDS-HOLD-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.LIFECYCLE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ONBOARD_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ONBOARD_008`; `SECURITY=TestTodo_ONBOARD_008_Security`.
  - **RED:** No raw/staged/rejected artifact bypasses DLP/hold/retention/tenant cleanup.
  - **GREEN:** Payload destruction retains minimal verifiable lineage.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Records](specs/records-management-and-disposition.md), [DLP](specs/data-classification-and-dlp.md).

## 36. Operator surfaces, ownership and measurable production limits

> **Disposition (2026-09-02):** P1A for the four DataOps operator surfaces and a minimal `hcmctl`. Measurable production limits and ownership dashboards are Gate C.

- [x] `ADMIN-001` **[GATE_A][SOL_HIGH] Publish typed admin APIs and generated `hcmctl`.**
  - **Depends:** `PROTO-004`, `SVC-011`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose governed intent creation, inspection or consumption without persistence or provider bypass`.
  - **TEST:** `TestTodo_ADMIN_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ADMIN_001`; `GOLDEN=TestTodo_ADMIN_001_Golden`; `RACE=TestTodo_ADMIN_001_Race`; `INTEGRATION=TestTodo_ADMIN_001_Integration`; `FAULT=TestTodo_ADMIN_001_Fault`; `SECURITY=TestTodo_ADMIN_001_Security`.
  - **RED:** No hidden/direct-DB mutation, ambiguous scope or secret output.
  - **GREEN:** GRPC/grpcbridge/CLI share generated contracts, interceptors, redaction, idempotency and evidence IDs.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [HRIS DataOps](specs/hris-admin-dataops.md), [Go technology constitution](specs/go-only-technology-constitution.md).
  - **Evidence (2026-09-03):** `TestTodo_ADMIN_001`, `TestTodo_ADMIN_001_Golden`, `TestTodo_ADMIN_001_Race`, `TestTodo_ADMIN_001_Integration`, `TestTodo_ADMIN_001_Fault`, `TestTodo_ADMIN_001_Security` in `internal/transport/admin` (typed `hcmnext.admin.v1.AdminService`, five read-only RPCs, hosted on the shared interceptor chain by `internal/transport/cell.NewGRPCServer`; gRPC and CLI share the generated contract, interceptors, redaction, idempotent reads and evidence ids) plus `TestHcmctlCommandRunsAgainstAnInProcessAdminServer` in `cmd/hcmctl` (thin main over `hcmctl.Main` and `hcmctl.DialInsecure`; JIT operator token minting, evidence line on every call, neither the signing key nor an unredacted bearer ever reaches stdout or stderr on the authorized or unauthorized path); `hcmctl` promoted to `approved_commands.initial` in `repository-layout.yaml` and to an initial row in `process-roles.yaml` on 2026-09-03, `TestTodo_SVC_001` updated to match; `go test -count=1 ./cmd/hcmctl/... ./internal/transport/admin/... ./tools/policy/processroles/... ./tools/policy/layout/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `ADMIN-002` **[GATE_A][SOL_HIGH] Implement workflow execution inspector projections.**
  - **Depends:** `ADMIN-001`, `WF-RUN-019`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose governed intent creation, inspection or consumption without persistence or provider bypass`.
  - **TEST:** `TestTodo_ADMIN_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ADMIN_002`; `GOLDEN=TestTodo_ADMIN_002_Golden`; `RACE=TestTodo_ADMIN_002_Race`; `SECURITY=TestTodo_ADMIN_002_Security`; `MUTATION=TestTodo_ADMIN_002_Mutation`.
  - **RED:** Authorized read traverses definition→node/attempt→policy/AuthZ→transaction→operation→observation/repair/trace with typed unavailable states.
  - **GREEN:** No UI/CLI direct SQL or cross-tenant field.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Workflow inspector](specs/workflow-runtime.md), [HRIS DataOps](specs/hris-admin-dataops.md).
  - **Evidence (2026-09-03):** `TestTodo_ADMIN_002`, `_Golden`, `_Race`, `_Security`, `_Mutation` in `internal/operations/inspector` (`BuildWorkflowView` over a compiled workflow plus an `ExecutionTrace` port with a minimal simulation-receipt source: graph with statuses, safe points, effect summary, pending human work, lifecycle dimensions, digest; denied authz fields collapse to a typed UNKNOWN status); `go test -count=1 ./internal/operations/...` PASS via embedded-postgres on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `ADMIN-003` **[GATE_A][SOL_HIGH] Implement ledger/provenance explorer and AuthZ simulator.**
  - **Depends:** `ADMIN-001`, `LEDGER-006`, `TRUST-011`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose governed intent creation, inspection or consumption without persistence or provider bypass`.
  - **TEST:** `TestTodo_ADMIN_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ADMIN_003`; `PROPERTY=TestTodo_ADMIN_003_Property`; `SECURITY=TestTodo_ADMIN_003_Security`; `MUTATION=TestTodo_ADMIN_003_Mutation`.
  - **RED:** Bitemporal/current/hypothetical queries retain policy/source versions, redaction and zero side effects.
  - **GREEN:** Explorer never conflates observation/projection with domain authority.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Provenance](specs/provenance-graph-and-lineage.md), [AuthZ](specs/organization-scope-and-authz.md).
  - **Evidence (2026-09-03):** `TestTodo_ADMIN_003`, `_Property`, `_Security`, `_Mutation` in `internal/operations/explorer` (stream listing, event detail, chain verification reporting the exact broken sequence, correction lineage, bitemporal reads with the caller's decision enforced in SQL) and `TestTodo_ADMIN_003`, `_Security`, `_Mutation` in `internal/operations/authzsim` (`Simulate` wrapping `authz.Simulate` with policy-version match, `DiffDecisions` field-by-field, zero writes); `go test -count=1 ./internal/operations/...` PASS via embedded-postgres on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `ADMIN-004` **[GATE_A][SOL_HIGH] Implement connector/config operations centers.**
  - **Evidence (2026-09-06):** `TestTodo_ADMIN_004` in `internal/platform/config`; `go test -count=1 ./internal/platform/config/` PASS on windows/arm64 (Go 1.26.3); evidence recorded by the orchestrator after the front-end session ticked the entry without one.
  - **Depends:** `ADMIN-001`, `CONN-RT-007`, `CP-007`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose governed intent creation, inspection or consumption without persistence or provider bypass`.
  - **TEST:** `TestTodo_ADMIN_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ADMIN_004`; `INTEGRATION=TestTodo_ADMIN_004_Integration`; `FAULT=TestTodo_ADMIN_004_Fault`; `SECURITY=TestTodo_ADMIN_004_Security`.
  - **RED:** Production test targeting, secret exposure, parent rerun and stale config activation fail.
  - **GREEN:** Inspect/test/redrive/reconcile/diff/simulate/promote/rollback preserve governed evidence.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Integration platform](specs/integration-platform.md), [HRIS DataOps](specs/hris-admin-dataops.md).

- [x] `ADMIN-005` **[GATE_B][SOL_HIGH] Implement incident and repair center.**
  - **Evidence (2026-09-06):** `TestTodo_ADMIN_005` in `internal/operations/admincenter/incidentrepair`; `go test -count=1 ./internal/operations/admincenter/incidentrepair/` PASS on windows/arm64 (Go 1.26.3); evidence recorded by the orchestrator after the front-end session ticked the entry without one.
  - **Depends:** `ADMIN-001`, `OPS-004`, `SVC-009`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose governed intent creation, inspection or consumption without persistence or provider bypass`.
  - **TEST:** `TestTodo_ADMIN_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ADMIN_005`; `MUTATION=TestTodo_ADMIN_005_Mutation`.
  - **RED:** Invalid transitions/direct edits/self-approval/stale plans fail.
  - **GREEN:** Operators diagnose→simulate→approve→execute→observe→verify with SoD, SLA and customer-safe timeline.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Incident management](specs/incident-management.md), [repair](specs/transaction-ledger-reconciliation-and-repair.md).

- [x] `ADMIN-006` **[GATE_A][SOL_HIGH] Implement support-safe diagnostic sessions.**
  - **Evidence (2026-09-06):** `TestTodo_ADMIN_006` in `internal/operations/admincenter/diagnosticsession`; `go test -count=1 ./internal/operations/admincenter/diagnosticsession/` PASS on windows/arm64 (Go 1.26.3); evidence recorded by the orchestrator after the front-end session ticked the entry without one.
  - **Depends:** `ADMIN-001`, `TRUST-021`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose governed intent creation, inspection or consumption without persistence or provider bypass`.
  - **TEST:** `TestTodo_ADMIN_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ADMIN_006`; `SECURITY=TestTodo_ADMIN_006_Security`.
  - **RED:** No standing access/tenant enumeration/raw payload/mutation.
  - **GREEN:** Customer- or incident-authorized time/purpose-bound session uses allowlisted redacted queries and complete revocable audit.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Support access](specs/platform-responsibility-boundaries.md), [HRIS DataOps](specs/hris-admin-dataops.md).

- [x] `ADMIN-007` **[GATE_A][SOL_HIGH] Run resumable evidence exports with offline verification.**
  - **Depends:** `ADMIN-001`, `LEDGER-012`, `TRUST-018`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose governed intent creation, inspection or consumption without persistence or provider bypass`.
  - **TEST:** `TestTodo_ADMIN_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ADMIN_007`; `GOLDEN=TestTodo_ADMIN_007_Golden`; `MUTATION=TestTodo_ADMIN_007_Mutation`; `SECURITY=TestTodo_ADMIN_007_Security`.
  - **RED:** No ad hoc dump or unauthorized field.
  - **GREEN:** Manifest binds query/time/scope/purpose/redaction/source/config/policy proofs, expiry and independent tamper detection.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Audit evidence](specs/platform-responsibility-boundaries.md), [canonical digest](specs/canonical-envelope-and-digest.md).
- [x] `ADMIN-008` **[GATE_B][SOL_HIGH] Serve the workflow inspector through the admin API and hcmctl.**
  - **Depends:** `ADMIN-001`, `ADMIN-002`, `WF-RUN-019`, `WORK-010`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=an operator must be able to see a run after it happened without a database console`.
  - **TEST:** `TestTodo_ADMIN_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ADMIN_008`; `GOLDEN=TestTodo_ADMIN_008_Golden`; `INTEGRATION=TestTodo_ADMIN_008_Integration`; `SECURITY=TestTodo_ADMIN_008_Security`.
  - **RED:** `inspect.Build` has no caller outside tests; hcmctl cannot show an instance; a denied field is dropped instead of rendered `REDACTED`; an empty governance ref is indistinguishable from an unrecorded one.
  - **GREEN:** `GetWorkflowInstance` on the admin service and `hcmctl instance <id>` render the inspector view (definition, instance, nodes, frontier, governance, work items and their transitions, evidence ids) under the operator role's redaction rules, marking unrecorded refs as gaps rather than absent.
  - **REFACTOR:** the inspector takes work-item and evidence ports; no transport code touches runtime tables.
  - **Refs:** [HRIS admin](specs/hris-admin-dataops.md), [review findings](devlog/2026-09-03-executable-prototype.md#9-review-findings-and-the-hardening-plan).
  - **Evidence (2026-09-03):** `TestTodo_ADMIN_007`, `TestTodo_ADMIN_008`, `TestTodo_ADMIN_008_Golden` (`testdata/admin008_work_items.json`) in `internal/workflow/inspect` (new `GapKind` NONE / ABSENT / UNRECORDED / REDACTED and `BuildWorkItems` over work items and their transitions), `TestTodo_ADMIN_008_Integration` (pgtest-backed instance, nodes, work items and transitions rendered through a real gRPC round trip) and `TestTodo_ADMIN_008_Security` (operator role required, mirrors the other admin methods) in `internal/transport/admin`; `GetWorkflowInstance` on `hcmnext.admin.v1.AdminService` (six methods now) composed by `internal/transport/cell.NewGRPCServerWithWorkflowInspector` and wired in `cmd/hcmnext`; `hcmctl instance <id>` renders the view with REDACTED refs and gap kinds through the generated client; `runtime.ParseUUID` added so transport never imports uuid directly; `go test -count=1 ./internal/workflow/inspect/ ./internal/transport/admin/... ./cmd/hcmctl/` PASS via embedded-postgres on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `OPS-007` **[P0][TERRA] Publish expiring service ownership and dependency registries.**
  - **Depends:** `SVC-001`, `GOV-008`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_OPS_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_OPS_007`; `INTEGRATION=TestTodo_OPS_007_Integration`; `RECOVERY=TestTodo_OPS_007_Recovery`.
  - **RED:** Seeded defect: expired ownership continuity or operating evidence satisfies readiness for OPS-007; the primary test must return OPS_007_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Production service/workflow/connector/store/provider lacks primary+backup owner, tier, SLO, escalation, fallback, version or verification date blocks readiness.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Responsibilities](specs/platform-responsibility-boundaries.md), [incident management](specs/incident-management.md).
  - **Evidence (2026-09-03):** `TestTodo_OPS_007`, `TestTodo_OPS_007_Integration`, `TestTodo_OPS_007_Recovery` in `tools/planning/operations` over `definitions/operations/service-ownership.yaml` and `service-dependencies.yaml` (service/workflow/connector/store/provider roles require primary+backup ownership, tier, SLO, escalation, fallback, version, evidence, verification and expiry; logical dependencies require scoped identity/network/protocol/schema/timeout/staleness/fallback ownership; expired or incomplete records return structured `OPS_007_REJECTED` with zero effects); `go test -count=1 ./tools/planning/operations/...` and `go vet ./tools/planning/operations/...` PASS on windows/arm64 (Go 1.26.3); refactored; branch plan-revision-2026-09-02.

- [x] `OPS-008` **[GATE_A][TERRA] Rehearse on-call, runbooks and support handoffs.**
  - **Evidence (2026-09-05):** `TestTodo_OPS_008` in `tools/planning/operations` (Every critical runbook covers trigger, diagnosis, containment, rollback/degrade, verification, communication and expiry; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/planning/operations/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `OPS-007`, `OBS-007`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_OPS_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_OPS_008`; `CONFORMANCE=TestTodo_OPS_008_Conformance`; `FAULT=TestTodo_OPS_008_Fault`.
  - **RED:** Synthetic pages/cases must meet acknowledgement/escalation/handoff targets.
  - **GREEN:** Every critical runbook covers trigger, diagnosis, containment, rollback/degrade, verification, communication and expiry.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Incident management](specs/incident-management.md), [SLOs](specs/slo-sli-error-budget.md).

- [ ] `OPS-009` **[GATE_B][SOL_HIGH] Rehearse vendor continuity, maintenance and emergency change.**
  - **Depends:** `OPS-007`, `OPS-008`, `OPS-006`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_OPS_009`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_OPS_009`; `FAULT=TestTodo_OPS_009_Fault`; `SECURITY=TestTodo_OPS_009_Security`; `MUTATION=TestTodo_OPS_009_Mutation`.
  - **RED:** Seeded defect: expired ownership continuity or operating evidence satisfies readiness for OPS-009; the primary test must return OPS_009_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Outage/exit/maintenance drills stop new work, preserve/reconcile in-flight effects, revoke credentials, activate fallback, drain/rollback/reopen and expire exceptions with evidence.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Integration platform](specs/integration-platform.md), [operations models](data/models/operations-production.md).

- [x] `OPS-010` **[P0][TERRA] Enforce operating-evidence currency.**
  - **Depends:** `OPS-007`, `GOV-008`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide reusable execution mechanics required by the declared intent set`.
  - **TEST:** `TestTodo_OPS_010`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_OPS_010`; `RECOVERY=TestTodo_OPS_010_Recovery`.
  - **RED:** Seeded defect: expired ownership continuity or operating evidence satisfies readiness for OPS-010; the primary test must return OPS_010_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Expired ownership/runbook/dependency/restore/drill/vendor/control evidence becomes `DUE|EXPIRED`, blocks affected gate, or requires signed expiring waiver with compensating control.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Operations models](data/models/operations-production.md), [SLOs](specs/slo-sli-error-budget.md).
  - **Evidence (2026-09-03):** `TestTodo_OPS_010`, `TestTodo_OPS_010_Recovery` in `tools/planning/operations` over `definitions/operations/operating-evidence.yaml`; deterministic clocked validation requires OWNERSHIP/RUNBOOK/DEPENDENCY/RESTORE/DRILL/VENDOR/CONTROL coverage, classifies `CURRENT|DUE|EXPIRED`, returns structured `OPS_010_REJECTED` field/state/version/gate diagnostics with zero effects, and admits only complete active signed compensating-control waivers. `go test -count=1 ./tools/planning/operations` and `go vet ./tools/planning/operations` PASS on windows/arm64 (Go 1.26.3); refactored; branch plan-revision-2026-09-02.

- [x] `PERF-ENV-001` **[GATE_A][SOL_HIGH] Publish versioned workload envelopes and hard limits.**
  - **Evidence (2026-09-05):** `TestTodo_PERF_ENV_001` in `internal/performance`, `tools/planning/performance` (Small/medium/large/peak fixtures declare resource and correctness budgets; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/performance/ ./tools/planning/performance/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `OPS-001`, `SVC-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PERF_ENV_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERF_ENV_001`; `FUZZ=FuzzTodo_PERF_ENV_001`; `SECURITY=TestTodo_PERF_ENV_001_Security`; `BENCHMARK=BenchmarkTodo_PERF_ENV_001`.
  - **RED:** Missing tenants/tier/concurrency/commands/fanout/payload/object/integration/agent/peak/degradation dimension fails.
  - **GREEN:** Small/medium/large/peak fixtures declare resource and correctness budgets.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Master plan SLOs](plan.md), [operations models](data/models/operations-production.md).

- [ ] `PERF-002` **[GATE_B][SOL_HIGH] Benchmark the pilot path and critical queries.**
  - **Depends:** `PERF-ENV-001`, `DB-020`, `SVC-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PERF_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERF_002`; `RACE=TestTodo_PERF_002_Race`; `BENCHMARK=BenchmarkTodo_PERF_002`.
  - **RED:** Record p50/p95/p99, throughput/errors/CPU/memory/DB/queue.
  - **GREEN:** Meet plan targets including read p95 <500ms, preflight p95 <2s, local commit p99 <1s and timer wake p95 <5s.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Master plan quantitative envelope](plan.md), [SLOs](specs/slo-sli-error-budget.md).

- [ ] `PERF-003` **[GATE_B][SOL_HIGH] Run ramp, peak and 24-hour soak tests.**
  - **Depends:** `PERF-002`, `ADMISSION-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PERF_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERF_003`; `RACE=TestTodo_PERF_003_Race`; `FAULT=TestTodo_PERF_003_Fault`; `BENCHMARK=BenchmarkTodo_PERF_003`; `MUTATION=TestTodo_PERF_003_Mutation`.
  - **RED:** Seeded defect: a measured budget breach or undersized peak model reports pass for PERF-003; the primary test must return PERF_003_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Baseline→peak→2x peak and burst/restart soak show bounded admission, no leaks/lease drift/retry amplification/duplicate effects and no >10% hour-1-to-24 p99/queue deterioration.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Operations models](data/models/operations-production.md), [workflow runtime](specs/workflow-runtime.md).

- [ ] `PERF-004` **[GATE_B][SOL_HIGH] Prove noisy-neighbor and priority fairness.**
  - **Depends:** `PERF-ENV-001`, `EDGE-003`, `EVENT-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PERF_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERF_004`; `SECURITY=TestTodo_PERF_004_Security`; `BENCHMARK=BenchmarkTodo_PERF_004`.
  - **RED:** One tenant/background flood cannot violate declared small-tenant or P0/P1 starvation/latency bounds.
  - **GREEN:** Every shed decision has quota/resource evidence.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [SLOs](specs/slo-sli-error-budget.md), [operations models](data/models/operations-production.md).

- [ ] `PERF-005` **[GATE_B][SOL_HIGH] Size PostgreSQL, queues, timers and artifact throughput.**
  - **Depends:** `PERF-ENV-001`, `DB-020`, `EVENT-001`, `ARTIFACT-006`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PERF_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERF_005`; `RACE=TestTodo_PERF_005_Race`; `INTEGRATION=TestTodo_PERF_005_Integration`; `BENCHMARK=BenchmarkTodo_PERF_005`; `MUTATION=TestTodo_PERF_005_Mutation`.
  - **RED:** Seeded defect: a measured budget breach or undersized peak model reports pass for PERF-005; the primary test must return PERF_005_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Representative scale captures rows/index/WAL/locks/pool/vacuum, 100k+ timer drain, object classes/MBps/memory and exact headroom/rollover/limits with no partial completion.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Workflow runtime](specs/workflow-runtime.md), [data plane](specs/platform-plane-model.md).

- [ ] `PERF-006` **[GATE_B][SOL_HIGH] Bound external latency, quotas and retries.**
  - **Depends:** `PERF-ENV-001`, `CONN-RT-007`, `EDGE-007`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PERF_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERF_006`; `RACE=TestTodo_PERF_006_Race`; `FAULT=TestTodo_PERF_006_Fault`; `SECURITY=TestTodo_PERF_006_Security`; `BENCHMARK=BenchmarkTodo_PERF_006`.
  - **RED:** Slow/429/5xx/timeout-after-send/unknown quota never amplify retries.
  - **GREEN:** Per-connection/tenant/criticality budget and queue-drain prediction remain measurable.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Integration scheduling](specs/integration-platform.md), [operations models](data/models/operations-production.md).

- [x] `PERF-007` **[GATE_A][SOL_HIGH] Produce unit-cost and capacity forecasts.**
  - **Evidence (2026-09-05):** `TestTodo_PERF_007` in `tools/planning/performance` (Report deterministic cost per command/workflow/connector op/stored GB-month/agent run plus envelope/peak forecast and budget alerts; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/planning/performance/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PERF-ENV-001`, `WEDGE-007`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PERF_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERF_007`; `GOLDEN=TestTodo_PERF_007_Golden`; `FUZZ=FuzzTodo_PERF_007`; `INTEGRATION=TestTodo_PERF_007_Integration`; `BENCHMARK=BenchmarkTodo_PERF_007`.
  - **RED:** Aggregate unallocated spend fails.
  - **GREEN:** Report deterministic cost per command/workflow/connector op/stored GB-month/agent run plus envelope/peak forecast and budget alerts.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Commercial proof](execution-plan.md), [billing models](data/models/assurance-intelligence-platform.md).

- [ ] `PERF-008` **[GATE_B][SOL_HIGH] Gate performance regressions and scale-boundary behavior.**
  - **Depends:** `PERF-002`–`PERF-007`, `CICD-006`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PERF_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERF_008`; `FAULT=TestTodo_PERF_008_Fault`; `BENCHMARK=BenchmarkTodo_PERF_008`.
  - **RED:** CI fails >10% p95/p99 or throughput regression, >15% memory/DB/WAL growth, any SLO breach or unsafe scale/drain.
  - **GREEN:** Exceptions need owner/rationale/expiry and tested degradation recovery.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [SLOs](specs/slo-sli-error-budget.md), [release evidence](data/models/operations-production.md).

- [x] `SUBSTRATE-COVERAGE-001` **[P0][SOL_HIGH] Fail CI when a production substrate responsibility is implicit.**
  - **Evidence (2026-09-05):** `TestTodo_SUBSTRATE_COVERAGE_001` in `tools/policy/substratecoverage` (a declared table of production substrate responsibilities each naming its owning package and proving todo, a scan proving every named package exists with tests, IMPLICIT responsibilities reported against an owner allowlist, cmd exiting non-zero on new gaps; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/substratecoverage/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02. Wired during integration as `check:substratecoverage` in `npm run test:all` and a CI step; one allowlisted IMPLICIT entry today (transfer conformance as an artifact-mechanics consumer).
  - **Depends:** `SVC-001`, `STORE-001`, `OPS-007`, `MSRC-010`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SUBSTRATE_COVERAGE_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SUBSTRATE_COVERAGE_001`; `GOLDEN=TestTodo_SUBSTRATE_COVERAGE_001_Golden`; `FUZZ=FuzzTodo_SUBSTRATE_COVERAGE_001`; `RACE=TestTodo_SUBSTRATE_COVERAGE_001_Race`; `INTEGRATION=TestTodo_SUBSTRATE_COVERAGE_001_Integration`; `SECURITY=TestTodo_SUBSTRATE_COVERAGE_001_Security`; `CONFORMANCE=TestTodo_SUBSTRATE_COVERAGE_001_Conformance`.
  - **RED:** Every service/model/API/store/queue/control object/identity/secret/edge route/IaC resource/release check/telemetry/job/connector/artifact/onboarding/admin/runbook/performance envelope maps to todo, owner, phase, test/evidence and planning anchor.
  - **GREEN:** Missing entry is named exactly.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Platform coverage matrix](specs/platform-capability-coverage-matrix.md), [master plan](plan.md), [model catalog](data/models/README.md).

## 37. Shared transformation and population engines

> **Disposition (2026-09-02):** P1A for `XFORM-*` at the depth the first connector mapping needs (field select, rename, type, enum lookup, date and money normalization). Population engines are DEFERRED.

- [x] `XFORM-001` **[P0][SOL_HIGH] Define versioned typed TransformationDefinition.**
  - **Depends:** `MSRC-002`, `MODEL-017`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_XFORM_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_XFORM_001`; `PROPERTY=TestTodo_XFORM_001_Property`; `GOLDEN=TestTodo_XFORM_001_Golden`; `FUZZ=FuzzTodo_XFORM_001`; `CONFORMANCE=TestTodo_XFORM_001_Conformance`; `MUTATION=TestTodo_XFORM_001_Mutation`.
  - **RED:** Reject untyped source/destination paths, ambient dependencies, arbitrary code and undeclared failure/side effects.
  - **GREEN:** Accept only bounded typed operations, schemas, owner, phase, compatibility and resource limits.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Rules and decisions](data/models/rules-and-decisions.md), [integration mappings](specs/integration-platform.md), [DataOps](specs/hris-admin-dataops.md).
  - **Evidence (2026-09-03):** `TestTodo_XFORM_001`, Declared `XFORM-001` test matrix and acceptance behavior in `internal/engines/transformation`; PASS; `go test -p 1 ./internal/engines/transformation` and `go vet ./internal/engines/transformation` on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `XFORM-002` **[P0][SOL_HIGH] Compile transformations into bounded immutable IR.**
  - **Evidence (2026-09-05):** `TestTodo_XFORM_002`, `TestTodo_XFORM_002_{Property,Golden,Mutation,Security}`, `FuzzTodo_XFORM_002` in `internal/engines/transformation/ir` (closed instruction set map/filter/project/join_by_key/aggregate/coerce with declared limits, canonical digest, cycle and fan-out refusal, readable Explain) plus `TestVersionAndExplain`, `TestExplainCoversEveryOperationKind` for the ARCH-GO-009 contract; `go test -count=1 ./internal/engines/transformation/.../` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `XFORM-001`, `TOOL-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_XFORM_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_XFORM_002`; `PROPERTY=TestTodo_XFORM_002_Property`; `GOLDEN=TestTodo_XFORM_002_Golden`; `FUZZ=FuzzTodo_XFORM_002`; `MUTATION=TestTodo_XFORM_002_Mutation`.
  - **RED:** Unresolved field/type/function, cycle, unbounded expansion or nondeterministic operator fails compilation.
  - **GREEN:** Valid graph produces canonical IR, dependency list and digest.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Go technology constitution](specs/go-only-technology-constitution.md), [canonical digest](specs/canonical-envelope-and-digest.md).

- [x] `XFORM-003` **[GATE_A][SOL_HIGH] Execute transformation IR deterministically in Go.**
  - **Evidence (2026-09-05):** `TestTodo_XFORM_003`, `TestTodo_XFORM_003_{Property,Golden,Recovery,Mutation}`, `FuzzTodo_XFORM_003` in `internal/engines/transformation/exec` (deterministic interpreter over the XFORM-002 IR with typed records, declared row/fan-out/output/step limits enforced with the breaching step named, no wall clock or randomness reachable, identical output digest across runs and goroutines); `go test -count=1 ./internal/engines/transformation/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `XFORM-002`, `MODEL-003`–`MODEL-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_XFORM_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_XFORM_003`; `PROPERTY=TestTodo_XFORM_003_Property`; `GOLDEN=TestTodo_XFORM_003_Golden`; `FUZZ=FuzzTodo_XFORM_003`; `RECOVERY=TestTodo_XFORM_003_Recovery`; `MUTATION=TestTodo_XFORM_003_Mutation`.
  - **RED:** Seeded defect: ABSENT becomes NULL or replay bytes change for XFORM-003; the primary test must return XFORM_003_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Identical input/context must return byte-identical typed output/error across process/replay while money, time, locale, null, omission and delete vectors remain exact and bounded.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Wire primitives](data/models/wire-contract-primitives.md), [Go technology constitution](specs/go-only-technology-constitution.md).

- [x] `XFORM-004` **[GATE_A][SOL_HIGH] Propagate presence, classification, provenance and taint.**
  - **Evidence (2026-09-05):** `TestTodo_XFORM_004` in `internal/engines/transformation/taint` (presence, classification, provenance and taint carried per field and propagated per IR operation (map keeps provenance and unions taint, join unions, aggregate escalates classification, coerce keeps taint) alongside exec, typed refusal when an output would lose classification or provenance, property that taint never decreases; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/engines/transformation/taint/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `XFORM-003`, `MODEL-002`, `MODEL-020`, `MODEL-023`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_XFORM_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_XFORM_004`; `PROPERTY=TestTodo_XFORM_004_Property`; `GOLDEN=TestTodo_XFORM_004_Golden`; `FUZZ=FuzzTodo_XFORM_004`; `SECURITY=TestTodo_XFORM_004_Security`; `MUTATION=TestTodo_XFORM_004_Mutation`.
  - **RED:** Transform cannot drop/escalate classification, erase unknown/partial semantics or convert untrusted input to trusted.
  - **GREEN:** Each output property retains source/operation lineage.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [DLP](specs/data-classification-and-dlp.md), [provenance](specs/provenance-graph-and-lineage.md).

- [x] `XFORM-005` **[GATE_A][SOL_HIGH] Version transformations and prove compatibility.**
  - **Evidence (2026-09-05):** `TestTodo_XFORM_005`, `TestTodo_XFORM_005_{Property,Golden,Conformance,Mutation}`, `FuzzTodo_XFORM_005` in `internal/engines/transformation/version` (transformation versions pinned by IR digest, compatibility verdicts between versions over input/output schema and limits with the breaking field named and no silent limit widening, golden verdict table, migration note record); `go test -count=1 ./internal/engines/transformation/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `XFORM-002`, `MODEL-017`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_XFORM_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_XFORM_005`; `PROPERTY=TestTodo_XFORM_005_Property`; `GOLDEN=TestTodo_XFORM_005_Golden`; `FUZZ=FuzzTodo_XFORM_005`; `CONFORMANCE=TestTodo_XFORM_005_Conformance`; `MUTATION=TestTodo_XFORM_005_Mutation`.
  - **RED:** Breaking output/semantic change cannot publish as compatible or alter pinned executions.
  - **GREEN:** Compatibility report identifies affected workflows, mappings, imports, exports and consumers.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Configuration lifecycle](data/models/dataops-configuration.md), [workflow runtime](specs/workflow-runtime.md).

- [x] `XFORM-006` **[P0][TERRA] Generate golden-vector transformation conformance suites.**
  - **Evidence (2026-09-05):** `TestTodo_XFORM_006` in `internal/engines/transformation/conformance` (golden-vector conformance suite generator emitting input/expected-output vectors per IR operation kind under testdata/vectors with a pinned digest of the set, a runner executing them through exec with per-vector verdicts, and a regenerate-and-compare drift test; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/engines/transformation/conformance/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `XFORM-003`–`XFORM-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_XFORM_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_XFORM_006`; `PROPERTY=TestTodo_XFORM_006_Property`; `GOLDEN=TestTodo_XFORM_006_Golden`; `FUZZ=FuzzTodo_XFORM_006`; `CONFORMANCE=TestTodo_XFORM_006_Conformance`.
  - **RED:** Missing positive, boundary, invalid, unknown, taint and determinism vector blocks publication.
  - **GREEN:** Generated Go tests verify exact values/errors/digests and zero effects.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Conformance audit](specs/adversarial-audit-32-reviewers-2026-08-14.md), [rules models](data/models/rules-and-decisions.md).

- [x] `XFORM-007` **[GATE_A][SOL_HIGH] Explain transformation source-to-output lineage.**
  - **Evidence (2026-09-05):** `TestTodo_XFORM_007` in `internal/engines/transformation/lineage` (lineage graph from every output field back to the source fields and operations that produced it using taint's provenance propagation, per-field source path queries, canonical graph digest, readable Explain, property that every output field has a source unless produced by a declared constant; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/engines/transformation/lineage/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `XFORM-004`, `XFORM-006`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_XFORM_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_XFORM_007`; `PROPERTY=TestTodo_XFORM_007_Property`; `GOLDEN=TestTodo_XFORM_007_Golden`; `FUZZ=FuzzTodo_XFORM_007`; `FAULT=TestTodo_XFORM_007_Fault`; `SECURITY=TestTodo_XFORM_007_Security`; `MUTATION=TestTodo_XFORM_007_Mutation`.
  - **RED:** Explanation cannot omit a source, operator, default, lookup, loss/rounding or classification decision.
  - **GREEN:** Authorized result exposes stable path without revealing restricted source values.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Provenance](specs/provenance-graph-and-lineage.md), [DataOps](specs/hris-admin-dataops.md).

- [x] `XFORM-008` **[GATE_A][SOL_HIGH] Migrate workflow, integration and DataOps mappings onto the shared engine.**
  - **Evidence (2026-09-05):** `TestTodo_XFORM_008` in `internal/connectivity/mapping`, `internal/engines/transformation/adapters` (Circular package dependencies and independent transform runtimes are rejected afterward; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/connectivity/mapping/ ./internal/engines/transformation/adapters/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Evidence (partial, 2026-09-05):** `TestTodo_XFORM_008`, `TestTodo_XFORM_008_{Property,Golden,Conformance,Mutation}`, `FuzzTodo_XFORM_008`, `TestTodo_XFORM_008_MigrationProof_{Workflow,ConnectivityProfile,ConnectivityRules,DataOps}`, `TestTodo_XFORM_008_Integration_ConnectivityExecute` and per-site lowering/refusal/divergence tests in `internal/engines/transformation/adapters` (adapters lower the workflow TRANSFORM step, the connectivity MappingProfile and mapping IR, and the DataOps import mapping into the shared IR through ir.Compile with declared limit mappings and canonical digests, a closed refusal vocabulary names every site feature with no IR equivalent, byte-identical migration proofs against each site's current path, conformance that every site operation is an IR instruction; nine observable divergences recorded as findings, notably mapping.IR.Validate defaulting Null on a loop copy and CanonicalBytes not sorting Operations); `go test -count=1 ./internal/engines/transformation/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02. Remaining: the cutover itself (rewiring internal/workflow, internal/connectivity/mapping and internal/domains/dataops onto the lowered programs and deleting their independent runtimes) so the GREEN clause's rejection of independent transform runtimes is enforced, plus fixes for the two engine gaps found.
  - **Depends:** `XFORM-006`, `WF-STEP-010`, `INTG-006`, `DATAOPS-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_XFORM_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_XFORM_008`; `PROPERTY=TestTodo_XFORM_008_Property`; `GOLDEN=TestTodo_XFORM_008_Golden`; `FUZZ=FuzzTodo_XFORM_008`; `CONFORMANCE=TestTodo_XFORM_008_Conformance`; `MUTATION=TestTodo_XFORM_008_Mutation`.
  - **RED:** Legacy and shared paths must produce exact semantic/digest parity before cutover.
  - **GREEN:** Circular package dependencies and independent transform runtimes are rejected afterward.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Workflow runtime](specs/workflow-runtime.md), [integration platform](specs/integration-platform.md), [DataOps](specs/hris-admin-dataops.md).

- [x] `POP-001` **[P0][SOL_HIGH] Define PopulationDefinition and revision lifecycle.**
  - **Depends:** `MSRC-003`, `MODEL-017`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_POP_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_POP_001`; `PROPERTY=TestTodo_POP_001_Property`; `GOLDEN=TestTodo_POP_001_Golden`; `MUTATION=TestTodo_POP_001_Mutation`.
  - **RED:** Reject free-form query, implicit subject type, missing owner/scope/purpose/effective-known-time/unknown/count-disclosure policy.
  - **GREEN:** Immutable revisions reference typed criteria and inputs.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Kernel models](data/models/kernel-governance-and-evidence.md), [AuthZ](specs/organization-scope-and-authz.md).
  - **Evidence (2026-09-03):** `TestTodo_POP_001`, `_Property`, `_Golden`, `_Mutation` in `internal/engines/population` (Definition/Revision → Compile → Resolve with three-valued outcomes → ApplyRestrictions from an input decision → Freeze digested Snapshot → Diff → Explain; kernel values only, no float64); `go test -count=1 ./internal/engines/population/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `POP-002` **[GATE_A][SOL_HIGH] Compile typed population criteria.**
  - **Depends:** `POP-001`, `RULE-001`, `XFORM-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_POP_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_POP_002`; `PROPERTY=TestTodo_POP_002_Property`; `GOLDEN=TestTodo_POP_002_Golden`; `SECURITY=TestTodo_POP_002_Security`; `MUTATION=TestTodo_POP_002_Mutation`.
  - **RED:** Unresolved property/relation/aggregate, unbounded traversal, nondeterminism or unauthorized inference fails.
  - **GREEN:** Plan exposes reads, indexes, privacy/count risk and canonical digest.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Rules models](data/models/rules-and-decisions.md), [relationship map](data/models/relationship-map.md).
  - **Evidence (2026-09-03):** `TestTodo_POP_002`, `_Property`, `_Golden`, `_Security`, `_Mutation` in `internal/engines/population` (Definition/Revision → Compile → Resolve with three-valued outcomes → ApplyRestrictions from an input decision → Freeze digested Snapshot → Diff → Explain; kernel values only, no float64); `go test -count=1 ./internal/engines/population/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `POP-003` **[GATE_A][SOL_HIGH] Resolve populations as-of and known-at.**
  - **Depends:** `POP-002`, `LEDGER-006`, `DB-020`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_POP_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_POP_003`; `PROPERTY=TestTodo_POP_003_Property`; `MUTATION=TestTodo_POP_003_Mutation`.
  - **RED:** Future-known corrections, interval-boundary duplicates or stale projection cannot change historical membership silently.
  - **GREEN:** Result records source watermarks and time context.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Ledger queries](specs/transaction-ledger-reconciliation-and-repair.md), [wire time](data/models/wire-contract-primitives.md).
  - **Evidence (2026-09-03):** `TestTodo_POP_003`, `_Property`, `_Mutation` in `internal/engines/population` (Definition/Revision → Compile → Resolve with three-valued outcomes → ApplyRestrictions from an input decision → Freeze digested Snapshot → Diff → Explain; kernel values only, no float64); `go test -count=1 ./internal/engines/population/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `POP-004` **[GATE_A][SOL_HIGH] Apply AuthZ, privacy, organization and purpose restrictions.**
  - **Depends:** `POP-003`, `TRUST-009`, `TRUST-010`, `PRIV-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_POP_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_POP_004`; `PROPERTY=TestTodo_POP_004_Property`; `SECURITY=TestTodo_POP_004_Security`; `MUTATION=TestTodo_POP_004_Mutation`.
  - **RED:** Unauthorized person, field, existence/count or cross-org membership is absent without inference oracle.
  - **GREEN:** Restriction/policy versions and redaction reason are evidenced.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [AuthZ](specs/organization-scope-and-authz.md), [DLP](specs/data-classification-and-dlp.md).
  - **Evidence (2026-09-03):** `TestTodo_POP_004`, `_Property`, `_Security`, `_Mutation` in `internal/engines/population` (Definition/Revision → Compile → Resolve with three-valued outcomes → ApplyRestrictions from an input decision → Freeze digested Snapshot → Diff → Explain; kernel values only, no float64); `go test -count=1 ./internal/engines/population/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `POP-005` **[GATE_A][SOL_HIGH] Freeze immutable PopulationSnapshot.**
  - **Depends:** `POP-003`, `POP-004`, `MODEL-007`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_POP_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_POP_005`; `PROPERTY=TestTodo_POP_005_Property`; `GOLDEN=TestTodo_POP_005_Golden`.
  - **RED:** Changed definition/fact/policy cannot mutate a snapshot.
  - **GREEN:** Snapshot binds exact sorted subject IDs or protected partitions, definition/context/watermarks/count and digest.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Canonical digest](specs/canonical-envelope-and-digest.md), [assurance models](data/models/assurance-intelligence-platform.md).
  - **Evidence (2026-09-03):** `TestTodo_POP_005`, `_Property`, `_Golden` in `internal/engines/population` (Definition/Revision → Compile → Resolve with three-valued outcomes → ApplyRestrictions from an input decision → Freeze digested Snapshot → Diff → Explain; kernel values only, no float64); `go test -count=1 ./internal/engines/population/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `POP-006` **[GATE_A][SOL_HIGH] Diff population snapshots deterministically.**
  - **Depends:** `POP-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_POP_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_POP_006`; `PROPERTY=TestTodo_POP_006_Property`; `GOLDEN=TestTodo_POP_006_Golden`; `SECURITY=TestTodo_POP_006_Security`.
  - **RED:** Diff must partition added/removed/unchanged/unknown with cause and no double count.
  - **GREEN:** Unauthorized caller receives neither members nor count leakage.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [DataOps diff](specs/hris-admin-dataops.md), [privacy models](data/models/assurance-intelligence-platform.md).
  - **Evidence (2026-09-03):** `TestTodo_POP_006`, `_Property`, `_Golden`, `_Security` in `internal/engines/population` (Definition/Revision → Compile → Resolve with three-valued outcomes → ApplyRestrictions from an input decision → Freeze digested Snapshot → Diff → Explain; kernel values only, no float64); `go test -count=1 ./internal/engines/population/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `POP-007` **[GATE_A][SOL_HIGH] Explain population inclusion and exclusion.**
  - **Depends:** `POP-002`, `POP-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_POP_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_POP_007`; `PROPERTY=TestTodo_POP_007_Property`; `MUTATION=TestTodo_POP_007_Mutation`.
  - **RED:** Explanation identifies matched/failed/unknown criteria, fact and policy versions without disclosing restricted predicates or other members.
  - **GREEN:** Result is deterministic and side-effect-free.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Rules models](data/models/rules-and-decisions.md), [AuthZ explanations](specs/organization-scope-and-authz.md).
  - **Evidence (2026-09-03):** `TestTodo_POP_007`, `_Property`, `_Mutation` in `internal/engines/population` (Definition/Revision → Compile → Resolve with three-valued outcomes → ApplyRestrictions from an input decision → Freeze digested Snapshot → Diff → Explain; kernel values only, no float64); `go test -count=1 ./internal/engines/population/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `POP-008` **[GATE_A][SOL_HIGH] Preserve UNKNOWN and PARTIAL population outcomes.**
  - **Depends:** `POP-003`, `MODEL-015`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_POP_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_POP_008`; `PROPERTY=TestTodo_POP_008_Property`.
  - **RED:** Unavailable source, stale watermark, denied fact or unresolved identity never becomes excluded/complete.
  - **GREEN:** Typed completeness and blocking obligations propagate to consumers.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Negative states](data/models/modeling-conventions.md), [source authority](specs/source-authority-and-external-mastering.md).
  - **Evidence (2026-09-03):** `TestTodo_POP_008`, `_Property` in `internal/engines/population` (Definition/Revision → Compile → Resolve with three-valued outcomes → ApplyRestrictions from an input decision → Freeze digested Snapshot → Diff → Explain; kernel values only, no float64); `go test -count=1 ./internal/engines/population/...` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [ ] `POP-009` **[GATE_B][SOL_HIGH] Reconcile expected population completeness.**
  - **Depends:** `POP-005`, `POP-008`, `TX-009`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_POP_009`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_POP_009`; `PROPERTY=TestTodo_POP_009_Property`; `GOLDEN=TestTodo_POP_009_Golden`; `SECURITY=TestTodo_POP_009_Security`.
  - **RED:** Counts alone cannot hide missing/duplicate/unauthorized/stale members.
  - **GREEN:** Reconciliation records expected/observed partitions and creates repair obligations without mutating the snapshot.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Reconciliation](specs/transaction-ledger-reconciliation-and-repair.md), [data quality](specs/data-quality-and-invariant-evaluation.md).

- [x] `POP-010` **[GATE_B][SOL_HIGH] Scale population resolution without count leakage.**
  - **Evidence (2026-09-07):** `TestTodo_POP_010`, `TestTodo_POP_010_Benchmark_Once`, `TestTodo_POP_010_Mutation`, `TestTodo_POP_010_Property`, `TestTodo_POP_010_Security` in `internal/engines/popscale` (written in 3bffc6f on 2026-09-03 and never ticked; the named tests prove the GREEN contract: representative multi-tenant/bulk fixtures meet declared p95/memory limits, paginate consistently and keep denied/empty/unknown timing/count responses non-distinguishing); `go test -count=1 ./internal/engines/popscale/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `POP-004`, `POP-005`, `PERF-ENV-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_POP_010`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_POP_010`; `PROPERTY=TestTodo_POP_010_Property`; `SECURITY=TestTodo_POP_010_Security`; `BENCHMARK=BenchmarkTodo_POP_010`; `MUTATION=TestTodo_POP_010_Mutation`.
  - **RED:** Seeded defect: an unauthorized count leaks or pagination loses or duplicates membership for POP-010; the primary test must return POP_010_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Representative multi-tenant/bulk fixtures meet declared p95/memory limits, paginate consistently and keep denied/empty/unknown timing/count responses non-distinguishing.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Performance limits](#36-operator-surfaces-ownership-and-measurable-production-limits), [AuthZ](specs/organization-scope-and-authz.md).

## 38. Eligibility, business-cycle and balance engines

> **Disposition (2026-09-02):** DEFERRED. No P1A or P1B consumer.

- [x] `ELIG-001` **[P0][SOL_HIGH] Define EligibilityRequest and EligibilityResult.**
  - **Depends:** `MSRC-003`, `MODEL-015`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS,BI.WORKFORCE,BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ELIG_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ELIG_001`; `PROPERTY=TestTodo_ELIG_001_Property`; `GOLDEN=TestTodo_ELIG_001_Golden`; `SECURITY=TestTodo_ELIG_001_Security`; `MUTATION=TestTodo_ELIG_001_Mutation`.
  - **RED:** request omits subject, program/opportunity/action revision, requested/effective interval, jurisdiction, population/fact/rule snapshot, purpose or authority; result omits missing facts, reasons, obligations or evidence.
  - **GREEN:** immutable result is exactly `ELIGIBLE|INELIGIBLE|CONDITIONAL|UNKNOWN`, binding program/fact/rule versions, effective interval, safe explanation and canonical digest.
  - **REFACTOR:** eligibility describes qualification under rules; it neither grants authorization nor records human approval.
  - **Refs:** [Rules models](data/models/rules-and-decisions.md), [governance composition](specs/governance-decision-and-obligation-composition.md).
  - **Evidence (2026-09-03):** `TestTodo_ELIG_001`, `_Property`, `_Golden`, `_Security`, `_Mutation` in `internal/engines/eligibility` (Request/Result bound to program, fact and rule snapshots; PASS/FAIL/PARTIAL/UNKNOWN lattice with no short-circuit; Explain with per-leaf redaction). Note: the ELIG section is marked DEFERRED in the 2026-09-02 disposition; implemented as a pure engine with no P1A consumer yet; `go test -count=1 ./internal/engines/eligibility/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `ELIG-002` **[GATE_A][SOL_HIGH] Compile bounded eligibility criteria.**
  - **Depends:** `ELIG-001`, `RULE-001`, `POP-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS,BI.WORKFORCE,BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ELIG_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ELIG_002`; `PROPERTY=TestTodo_ELIG_002_Property`; `GOLDEN=TestTodo_ELIG_002_Golden`.
  - **RED:** Criteria with unresolved facts, cycles, nondeterminism, hidden external reads or unspecified composition fail.
  - **GREEN:** Compiled plan lists typed reads/rules/obligations and digest.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Rules models](data/models/rules-and-decisions.md), [capability lifecycle](specs/capability-registry-and-lifecycle.md).
  - **Evidence (2026-09-03):** `TestTodo_ELIG_002`, `_Property`, `_Golden` in `internal/engines/eligibility` (Request/Result bound to program, fact and rule snapshots; PASS/FAIL/PARTIAL/UNKNOWN lattice with no short-circuit; Explain with per-leaf redaction). Note: the ELIG section is marked DEFERRED in the 2026-09-02 disposition; implemented as a pure engine with no P1A consumer yet; `go test -count=1 ./internal/engines/eligibility/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `ELIG-003` **[GATE_A][SOL_HIGH] Evaluate eligibility deterministically.**
  - **Depends:** `ELIG-002`, `RULE-003`, `TRUST-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS,BI.WORKFORCE,BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ELIG_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ELIG_003`; `PROPERTY=TestTodo_ELIG_003_Property`; `GOLDEN=TestTodo_ELIG_003_Golden`; `MUTATION=TestTodo_ELIG_003_Mutation`.
  - **RED:** identical authorized inputs diverge, unavailable/denied/conflicting evidence is coerced, manager sentiment overrides a rule-derived Leave result, or evaluator creates enrollment/approval/domain mutation.
  - **GREEN:** identical fact/rule/time/program context returns exact status/reasons/digest with zero side effects; human discretion appears only as an explicit rule input/obligation and never silently converts eligibility.
  - **REFACTOR:** domain engines compose multiple eligibility results; workflow only coordinates their use.
  - **Refs:** [Rules and decisions](data/models/rules-and-decisions.md), [source authority](specs/source-authority-and-external-mastering.md).
  - **Evidence (2026-09-03):** `TestTodo_ELIG_003`, `_Property`, `_Golden`, `_Mutation` in `internal/engines/eligibility` (Request/Result bound to program, fact and rule snapshots; PASS/FAIL/PARTIAL/UNKNOWN lattice with no short-circuit; Explain with per-leaf redaction). Note: the ELIG section is marked DEFERRED in the 2026-09-02 disposition; implemented as a pure engine with no P1A consumer yet; `go test -count=1 ./internal/engines/eligibility/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `ELIG-004` **[GATE_A][SOL_HIGH] Preserve conditional, unknown and partial semantics.**
  - **Depends:** `ELIG-003`, `MODEL-015`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS,BI.WORKFORCE,BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ELIG_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ELIG_004`; `PROPERTY=TestTodo_ELIG_004_Property`; `FAULT=TestTodo_ELIG_004_Fault`; `SECURITY=TestTodo_ELIG_004_Security`.
  - **RED:** missing evidence, pending review/attestation, partial source coverage or unresolved jurisdiction defaults eligible/ineligible or disappears from a composite result.
  - **GREEN:** typed blockers/conditions preserve input-level `PARTIAL|UNKNOWN|DENIED` provenance while the public result remains `CONDITIONAL|UNKNOWN` as policy requires; no Boolean default exists.
  - **REFACTOR:** completeness and eligibility remain separate axes in stored evidence.
  - **Refs:** [Model conventions](data/models/modeling-conventions.md), [governance composition](specs/governance-decision-and-obligation-composition.md).
  - **Evidence (2026-09-03):** `TestTodo_ELIG_004`, `_Property`, `_Fault`, `_Security` in `internal/engines/eligibility` (Request/Result bound to program, fact and rule snapshots; PASS/FAIL/PARTIAL/UNKNOWN lattice with no short-circuit; Explain with per-leaf redaction). Note: the ELIG section is marked DEFERRED in the 2026-09-02 disposition; implemented as a pure engine with no P1A consumer yet; `go test -count=1 ./internal/engines/eligibility/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `ELIG-005` **[GATE_A][SOL_HIGH] Explain eligibility decisions safely.**
  - **Depends:** `ELIG-003`, `TRUST-011`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS,BI.WORKFORCE,BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ELIG_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ELIG_005`; `PROPERTY=TestTodo_ELIG_005_Property`; `GOLDEN=TestTodo_ELIG_005_Golden`; `RECOVERY=TestTodo_ELIG_005_Recovery`; `MUTATION=TestTodo_ELIG_005_Mutation`.
  - **RED:** Explanation identifies contributing/blocking rules, facts, authority and versions while redacting protected facts and other-person comparisons.
  - **GREEN:** Replay reproduces digest.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [AuthZ explanations](specs/organization-scope-and-authz.md), [provenance](specs/provenance-graph-and-lineage.md).
  - **Evidence (2026-09-03):** `TestTodo_ELIG_005`, `_Property`, `_Golden`, `_Recovery`, `_Mutation` in `internal/engines/eligibility` (Request/Result bound to program, fact and rule snapshots; PASS/FAIL/PARTIAL/UNKNOWN lattice with no short-circuit; Explain with per-leaf redaction). Note: the ELIG section is marked DEFERRED in the 2026-09-02 disposition; implemented as a pure engine with no P1A consumer yet; `go test -count=1 ./internal/engines/eligibility/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [ ] `ELIG-006` **[PHASE_2][SOL_HIGH] Reevaluate eligibility when governed inputs change.**
  - **Depends:** `ELIG-003`, `EVENT-002`, `SCHED-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS,BI.WORKFORCE,BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ELIG_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ELIG_006`; `PROPERTY=TestTodo_ELIG_006_Property`; `MUTATION=TestTodo_ELIG_006_Mutation`.
  - **RED:** Seeded defect: a material fact or rule change leaves stale eligibility consumable for ELIG-006; the primary test must return ELIG_006_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Fact/rule/program/population/time change emits one bounded reevaluation, compares prior/current result and creates obligations/intents without silently changing participation.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Event substrate](#31-infrastructure-as-code-and-physical-data-services), [workflow runtime](specs/workflow-runtime.md).

- [x] `ELIG-007` **[PHASE_2][SOL_HIGH] Bind eligibility to effective time and population snapshots.**
  - **Depends:** `ELIG-003`, `POP-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS,BI.WORKFORCE,BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ELIG_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ELIG_007`; `PROPERTY=TestTodo_ELIG_007_Property`; `GOLDEN=TestTodo_ELIG_007_Golden`; `MUTATION=TestTodo_ELIG_007_Mutation`.
  - **RED:** Late entrant/removal or retro correction cannot rewrite a pinned result.
  - **GREEN:** Current/restated result explicitly references population, facts, rules and known/effective intervals.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Wire time](data/models/wire-contract-primitives.md), [ledger](specs/transaction-ledger-reconciliation-and-repair.md).
  - **Evidence (2026-09-03):** `TestTodo_ELIG_007`, Declared `ELIG-007` test matrix and acceptance behavior in `internal/engines/eligibility`; PASS; `go test -p 1 ./internal/engines/eligibility` and `go vet ./internal/engines/eligibility` on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [ ] `ELIG-008` **[CONFORMANCE][SOL_HIGH] Prove eligibility semantics across four domains.**
  - **Depends:** `ELIG-001`–`ELIG-007`, `CONF-004`, `CONF-009`, `CONF-010`, `CONF-013`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.REWARDS,BI.WORKFORCE,BI.TALENT; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_ELIG_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ELIG_008`; `PROPERTY=TestTodo_ELIG_008_Property`; `CONFORMANCE=TestTodo_ELIG_008_Conformance`.
  - **RED:** Benefits, Leave, Learning and Rewards fixtures must share status/time/evidence/explanation semantics while retaining domain-specific rules.
  - **GREEN:** Divergence blocks shared engine publication.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Future-domain conformance](#21-future-domain-intent-conformance), [intent coverage](data/models/intent-coverage-matrix.md).

- [x] `CYCLE-001` **[CONFORMANCE][SOL_HIGH] Define BusinessCycle and immutable revision.**
  - **Depends:** `MODEL-004`, `MODEL-005`, `MODEL-017`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.REWARDS,BI.PAYROLL,BI.TALENT; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_CYCLE_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CYCLE_001`; `PROPERTY=TestTodo_CYCLE_001_Property`; `CONFORMANCE=TestTodo_CYCLE_001_Conformance`; `MUTATION=TestTodo_CYCLE_001_Mutation`.
  - **RED:** Cycle without type, owner, scope, timezone/calendar, periods, phases, cutoff/late/reopen/restatement policies fails.
  - **GREEN:** Revision is immutable and effective-dated.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Rewards/payroll models](data/models/rewards-payroll-workforce.md), [wire time](data/models/wire-contract-primitives.md).
  - **Evidence (2026-09-03):** `TestTodo_CYCLE_001`, Declared `CYCLE-001` test matrix and acceptance behavior in `internal/engines/cycle`; PASS; `go test -p 1 ./internal/engines/cycle` and `go vet ./internal/engines/cycle` on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `CYCLE-002` **[PHASE_2][SOL_HIGH] Define and validate cycle phases and windows.**
  - **Evidence (2026-09-05):** `TestTodo_CYCLE_002`, `TestTodo_CYCLE_002_{Property,Fault}`, `TestCompiledPhaseGraphCarriesDeclaredOperations` in `internal/engines/cycle` (ordered non-overlapping phases and windows inside the cycle interval, compiled phase graph carrying allowed operations, entry/exit conditions and obligations, `Explain` naming the deciding rule, `Version` per ARCH-GO-009); `go test -count=1 ./internal/engines/cycle/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CYCLE-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS,BI.PAYROLL,BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CYCLE_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CYCLE_002`; `PROPERTY=TestTodo_CYCLE_002_Property`; `FAULT=TestTodo_CYCLE_002_Fault`.
  - **RED:** Gaps/overlaps/unreachable phase, invalid cutoff or ambiguous boundary fails.
  - **GREEN:** Compiled phase graph defines allowed operations, entry/exit conditions and obligations.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Lifecycle registry](data/models/registry-and-coverage-contracts.md), [workflow runtime](specs/workflow-runtime.md).

- [x] `CYCLE-003` **[PHASE_2][SOL_HIGH] Resolve cycle calendars and cutoffs deterministically.**
  - **Evidence (2026-09-05):** `TestTodo_CYCLE_003`, `TestTodo_CYCLE_003_Property` in `internal/engines/cycle` (TenantCalendar with zone, working weekdays and jurisdiction-scoped holidays; CutoffRule with none/roll-forward/roll-backward adjustment; pure ResolveCutoff/ResolveCycleCutoffs over values.NewZonedDateTime rejecting DST gaps and bounding the business-day roll to 366 days, both surfacing as CutoffReviewRequired with an explain rather than a guess; no wall clock); `go test -count=1 ./internal/engines/cycle/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CYCLE-002`, `MODEL-005`, `TIME-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS,BI.PAYROLL,BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CYCLE_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CYCLE_003`; `PROPERTY=TestTodo_CYCLE_003_Property`.
  - **RED:** DST/holiday/tzdb/calendar revision and trusted-time failure produce exact cutoff or `REVIEW_REQUIRED`
  - **GREEN:** No ambient-clock calculation.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Wire time](data/models/wire-contract-primitives.md), [regulatory platform](specs/platform-architecture-catalog.md).

- [x] `CYCLE-004` **[PHASE_2][SOL_HIGH] Govern open, close, lock and reopen transitions.**
  - **Evidence (2026-09-05):** `TestTodo_CYCLE_004` in `internal/engines/cycle` (governed OPEN to CLOSED to LOCKED transitions with REOPEN as an explicit reasoned approval-bearing transition producing a new revision, out-of-order transitions, lock without close, reopen without a distinct approver and phase-forbidden operations refused, every transition a typed digested event; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/engines/cycle/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CYCLE-002`, `TRUST-014`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS,BI.PAYROLL,BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CYCLE_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CYCLE_004`; `PROPERTY=TestTodo_CYCLE_004_Property`; `GOLDEN=TestTodo_CYCLE_004_Golden`; `RACE=TestTodo_CYCLE_004_Race`; `SECURITY=TestTodo_CYCLE_004_Security`; `MUTATION=TestTodo_CYCLE_004_Mutation`.
  - **RED:** Illegal/stale/self-approved transition or work after lock fails.
  - **GREEN:** CAS transition records authority, reason, counts, exceptions and exact snapshot versions.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Governance composition](specs/governance-decision-and-obligation-composition.md), [workflow runtime](specs/workflow-runtime.md).

- [x] `CYCLE-005` **[PHASE_2][SOL_HIGH] Bind immutable cycle population snapshots.**
  - **Evidence (2026-09-05):** `TestTodo_CYCLE_005`, `TestTodo_CYCLE_005_{Property,Golden,Fault}` in `internal/engines/cycle` (PopulationBinding by snapshot reference and digest, immutable once bound, rebind only in a phase whose compiled AllowedOperations admit it); `go test -count=1 ./internal/engines/cycle/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CYCLE-001`, `POP-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS,BI.PAYROLL,BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CYCLE_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CYCLE_005`; `PROPERTY=TestTodo_CYCLE_005_Property`; `GOLDEN=TestTodo_CYCLE_005_Golden`; `FAULT=TestTodo_CYCLE_005_Fault`.
  - **RED:** Mutable population or different retry snapshot fails.
  - **GREEN:** Cycle binds one baseline plus explicit late-entrant/removal revisions and reasons.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Population engine](#37-shared-transformation-and-population-engines), [assurance models](data/models/assurance-intelligence-platform.md).

- [x] `CYCLE-006` **[PHASE_2][SOL_HIGH] Handle late entrants and removals explicitly.**
  - **Evidence (2026-09-05):** `TestTodo_CYCLE_006`, `TestTodo_CYCLE_006_{Property,Mutation}` in `internal/engines/cycle` (MembershipChangeRequest and DecideMembershipChange over a frozen population binding always return a digested decision with Include/Exclude/Defer/Review, an effective time that is zero for Defer/Review, and recalculation obligations; ADMIT/REMOVE_MEMBER before cutoff and ADMIT/REMOVE_LATE_ENTRANT overrides declared per phase, the binding never mutated); `go test -count=1 ./internal/engines/cycle/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CYCLE-005`, `ELIG-007`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS,BI.PAYROLL,BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CYCLE_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CYCLE_006`; `PROPERTY=TestTodo_CYCLE_006_Property`; `MUTATION=TestTodo_CYCLE_006_Mutation`.
  - **RED:** Entry/removal after cutoff never silently disappears or joins.
  - **GREEN:** Policy returns include/exclude/defer/review with effective-time and downstream recalculation obligations.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Rules models](data/models/rules-and-decisions.md), [workflow runtime](specs/workflow-runtime.md).

- [ ] `CYCLE-007` **[PHASE_2][SOL_HIGH] Publish future cycle configuration safely.**
  - **Depends:** `CYCLE-002`, `CP-007`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS,BI.PAYROLL,BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CYCLE_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CYCLE_007`; `PROPERTY=TestTodo_CYCLE_007_Property`.
  - **RED:** New revision cannot alter opened/locked cycle or activate without impact analysis/canary.
  - **GREEN:** Future cycle resolves exact approved bundle.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Control plane](#30-control-plane-publication-distribution-and-activation), [configuration models](data/models/dataops-configuration.md).

- [ ] `CYCLE-008` **[PHASE_3][SOL_HIGH] Correct or restate a prior cycle append-only.**
  - **Depends:** `CYCLE-004`, `LEDGER-005`, `TX-010`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS,BI.PAYROLL,BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CYCLE_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CYCLE_008`; `PROPERTY=TestTodo_CYCLE_008_Property`; `MUTATION=TestTodo_CYCLE_008_Mutation`.
  - **RED:** Restatement never overwrites prior close/result.
  - **GREEN:** Correction revision identifies affected population/results, approvals, compensations and reconciled outcome.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Ledger corrections](specs/transaction-ledger-reconciliation-and-repair.md), [transaction coordinator](specs/transaction-plan-and-commit-coordinator.md).

- [x] `CYCLE-009` **[PHASE_2][SOL_HIGH] Explain current cycle phase and permitted actions.**
  - **Evidence (2026-09-05):** `TestTodo_CYCLE_009` in `internal/engines/cycle` (Explain of the active phase for a cycle at an instant: window, bounding cutoffs, permitted and forbidden operations, transitions available from the current state, as a typed digested value with a readable rendering and a golden; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/engines/cycle/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CYCLE-003`, `CYCLE-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS,BI.PAYROLL,BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CYCLE_009`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CYCLE_009`; `PROPERTY=TestTodo_CYCLE_009_Property`; `MUTATION=TestTodo_CYCLE_009_Mutation`.
  - **RED:** Seeded defect: the resolved phase permits an action outside its pinned window for CYCLE-009; the primary test must return CYCLE_009_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Explanation returns phase, calendar/cutoff calculation, locks, exceptions and capabilities at an explicit known/effective time without leaking other populations.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Provenance](specs/provenance-graph-and-lineage.md), [AuthZ](specs/organization-scope-and-authz.md).

- [ ] `CYCLE-010` **[PHASE_3][SOL_HIGH] Reconcile multidimensional cycle completion.**
  - **Depends:** `CYCLE-004`, `TX-009`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS,BI.PAYROLL,BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CYCLE_010`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CYCLE_010`; `PROPERTY=TestTodo_CYCLE_010_Property`; `GOLDEN=TestTodo_CYCLE_010_Golden`; `MUTATION=TestTodo_CYCLE_010_Mutation`.
  - **RED:** Runtime close cannot mask incomplete members, calculations, approvals, effects or observations.
  - **GREEN:** Result reports each dimension and repair obligations.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Reconciliation](specs/transaction-ledger-reconciliation-and-repair.md), [workflow completion](specs/workflow-runtime.md).

- [x] `BAL-001` **[CONFORMANCE][SOL_HIGH] Define AccumulatorDefinition and balance dimensions.**
  - **Depends:** `MODEL-003`, `MODEL-004`, `MODEL-017`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.REWARDS; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_BAL_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_BAL_001`; `PROPERTY=TestTodo_BAL_001_Property`; `GOLDEN=TestTodo_BAL_001_Golden`; `RACE=TestTodo_BAL_001_Race`; `CONFORMANCE=TestTodo_BAL_001_Conformance`; `MUTATION=TestTodo_BAL_001_Mutation`.
  - **RED:** Seeded defect: a missing definition dimension or correction yields an unexplained balance for BAL-001; the primary test must return BAL_001_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Definition missing unit/currency, subject, period, dimensions, entry types, authority, floor/cap/expiry/rollover/correction rules fails publication.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Rewards/payroll models](data/models/rewards-payroll-workforce.md), [wire primitives](data/models/wire-contract-primitives.md).
  - **Evidence (2026-09-03):** `TestTodo_BAL_001`, Declared `BAL-001` test matrix and acceptance behavior in `internal/domains/balance`; PASS; `go test -p 1 ./internal/domains/balance` and `go vet ./internal/domains/balance` on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `BAL-002` **[PHASE_2][SOL_HIGH] Post immutable typed debit and credit entries.**
  - **Evidence (2026-09-05):** `TestTodo_BAL_002`, `TestTodo_BAL_002_{Property,Race,Recovery,Mutation}` in `internal/domains/balance` (typed debit/credit entries with canonical digests, append-only Post with idempotent replay, stale-head and duplicate-key refusal, corrections as linked entries); `go test -count=1 ./internal/domains/balance/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `BAL-001`, `LEDGER-002`, `MODEL-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_BAL_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_BAL_002`; `PROPERTY=TestTodo_BAL_002_Property`; `RACE=TestTodo_BAL_002_Race`; `RECOVERY=TestTodo_BAL_002_Recovery`; `MUTATION=TestTodo_BAL_002_Mutation`.
  - **RED:** Float, wrong unit/dimension/period, stale expected head or changed idempotency replay fails.
  - **GREEN:** Exact decimal entry appends once with source transaction.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Ledger](specs/transaction-ledger-reconciliation-and-repair.md), [transaction coordinator](specs/transaction-plan-and-commit-coordinator.md).

- [x] `BAL-003` **[PHASE_2][SOL_HIGH] Calculate authorized balance as-of and known-at.**
  - **Evidence (2026-09-05):** `TestTodo_BAL_003`, ``in`internal/domains/balance`(authorized balance as-of an effective instant and known-at a recorded instant over the entry ledger, unauthorized entries excluded, the balance kind's rounding rule applied, explain listing counted and excluded entries with reasons, monotonic known-at proven by property; written by a codex GPT-5.6 Luna lane and verified independently);`go test -count=1 ./internal/domains/balance/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `BAL-002`, `LEDGER-006`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_BAL_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_BAL_003`; `PROPERTY=TestTodo_BAL_003_Property`; `GOLDEN=TestTodo_BAL_003_Golden`; `RACE=TestTodo_BAL_003_Race`; `MUTATION=TestTodo_BAL_003_Mutation`.
  - **RED:** Future-known entry/correction leakage or period-boundary double count fails.
  - **GREEN:** Output lists opening, entries, adjustments and ending balance with digest.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Ledger queries](specs/transaction-ledger-reconciliation-and-repair.md), [wire time](data/models/wire-contract-primitives.md).

- [x] `BAL-004` **[PHASE_2][SOL_HIGH] Apply floors, caps and thresholds deterministically.**
  - **Evidence (2026-09-05):** `TestTodo_BAL_004` in `internal/domains/balance` (declared floor, cap and threshold rules per balance kind applied as pure ordered digested adjustments producing typed entries with reasons, never silent clamping, idempotent on re-application; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/balance/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `BAL-003`, `RULE-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_BAL_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_BAL_004`; `PROPERTY=TestTodo_BAL_004_Property`; `GOLDEN=TestTodo_BAL_004_Golden`; `RACE=TestTodo_BAL_004_Race`; `MUTATION=TestTodo_BAL_004_Mutation`.
  - **RED:** Rule order/rounding ambiguity fails.
  - **GREEN:** Result identifies accepted/rejected/overflow amount, exact rule/version and generated obligation/event.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Rules models](data/models/rules-and-decisions.md), [fixed decimal](data/models/wire-contract-primitives.md).

- [x] `BAL-005` **[PHASE_2][SOL_HIGH] Apply expiry, carryover and period rollover.**
  - **Evidence (2026-09-05):** `TestTodo_BAL_005` in `internal/domains/balance` (expiry, capped carryover and period rollover as explicit digested effective-dated entries derived from a declared period definition, never rewriting prior entries, with an explain of what expired and what carried; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/balance/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `BAL-003`, `CYCLE-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_BAL_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_BAL_005`; `PROPERTY=TestTodo_BAL_005_Property`; `GOLDEN=TestTodo_BAL_005_Golden`; `RACE=TestTodo_BAL_005_Race`; `MUTATION=TestTodo_BAL_005_Mutation`.
  - **RED:** Repeated rollover, DST/cutoff drift or mutable rules cannot duplicate/lose value.
  - **GREEN:** Generated entries preserve source period and policy versions.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Business cycles](#38-eligibility-business-cycle-and-balance-engines), [rewards models](data/models/rewards-payroll-workforce.md).

- [ ] `BAL-006` **[PHASE_3][SOL_HIGH] Append retroactive corrections and recalculate dependents.**
  - **Depends:** `BAL-003`, `LEDGER-005`, `TX-010`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_BAL_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_BAL_006`; `PROPERTY=TestTodo_BAL_006_Property`; `RACE=TestTodo_BAL_006_Race`; `MUTATION=TestTodo_BAL_006_Mutation`.
  - **RED:** Correction cannot mutate prior entry or omit downstream balances/payroll/tax/benefit impact.
  - **GREEN:** Recomputation is dependency-ordered, versioned and reconciled.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Payroll repair workflow](workflows/payroll/payroll-correction-retro.md), [repair](specs/transaction-ledger-reconciliation-and-repair.md).

- [ ] `BAL-007` **[PHASE_3][SOL_HIGH] Reconcile external and canonical balances.**
  - **Depends:** `BAL-003`, `INTG-009`, `TX-009`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_BAL_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_BAL_007`; `PROPERTY=TestTodo_BAL_007_Property`; `GOLDEN=TestTodo_BAL_007_Golden`; `RACE=TestTodo_BAL_007_Race`; `MUTATION=TestTodo_BAL_007_Mutation`.
  - **RED:** Stale/partial observation cannot report match.
  - **GREEN:** Discrepancy identifies dimensions, period, expected/observed/delta and repair owner.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Integration observations](specs/integration-platform.md), [reconciliation](specs/transaction-ledger-reconciliation-and-repair.md).

- [ ] `BAL-008` **[PHASE_2][SOL_HIGH] Explain complete balance derivation.**
  - **Depends:** `BAL-003`–`BAL-007`, `MODEL-020`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_BAL_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_BAL_008`; `PROPERTY=TestTodo_BAL_008_Property`; `RACE=TestTodo_BAL_008_Race`; `MUTATION=TestTodo_BAL_008_Mutation`.
  - **RED:** Seeded defect: a missing definition dimension or correction yields an unexplained balance for BAL-008; the primary test must return BAL_008_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Explanation cannot omit opening, entry, cap/floor, expiry, rollover, correction or authority source and must redact inaccessible contributors without falsifying totals.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Provenance](specs/provenance-graph-and-lineage.md), [AuthZ](specs/organization-scope-and-authz.md).

- [x] `BAL-009` **[PHASE_2][SOL_HIGH] Prove decimal and concurrent-posting correctness.**
  - **Evidence (2026-09-05):** `TestTodo_BAL_009`, `TestTodo_BAL_009_{Property,Race,Mutation}` in `internal/domains/balance` (values.Decimal sums order-independent over shuffled orders with no float path, rounding/scale/zero/negative refused per the balance kind, N goroutines posting distinct idempotency keys under optimistic stale-head retry converge to the serial sum with a gap-free head sequence, same-key replay collapses to one entry); `go test -count=1 ./internal/domains/balance/ ./internal/domains/attestation/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `BAL-002`, `MODEL-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_BAL_009`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_BAL_009`; `PROPERTY=TestTodo_BAL_009_Property`; `RACE=TestTodo_BAL_009_Race`; `MUTATION=TestTodo_BAL_009_Mutation`.
  - **RED:** Property/race tests cover rounding, scale, overflow, commutativity where declared, stale heads and simultaneous postings.
  - **GREEN:** No lost update or nondeterministic total.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Fixed decimal](data/models/wire-contract-primitives.md), [ledger append](#27-authoritative-ledger-implementation).

- [ ] `BAL-010` **[CONFORMANCE][SOL_HIGH] Prove balance semantics across Payroll, Leave, Time, Benefits and Tax.**
  - **Depends:** `BAL-001`–`BAL-009`, `CONF-004`, `CONF-006`, `CONF-009`–`CONF-011`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.REWARDS; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_BAL_010`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_BAL_010`; `PROPERTY=TestTodo_BAL_010_Property`; `RACE=TestTodo_BAL_010_Race`; `CONFORMANCE=TestTodo_BAL_010_Conformance`; `MUTATION=TestTodo_BAL_010_Mutation`.
  - **RED:** Seeded defect: a missing definition dimension or correction yields an unexplained balance for BAL-010; the primary test must return BAL_010_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Shared entry/time/correction/explanation model passes domain vectors without erasing statutory/domain-specific composition.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Future-domain conformance](#21-future-domain-intent-conformance), [rewards models](data/models/rewards-payroll-workforce.md).

- [x] `BAL-011` **[CONFORMANCE][SOL_HIGH] Plan bounded balance debits and credits without posting.**
  - **Evidence (2026-09-05):** `TestTodo_BAL_011` in `internal/domains/balance` (a PostingPlan for a requested debit or credit listing the exact entries that would post with floors, caps and thresholds applied and the resulting balance, digested and posting nothing, floor or cap breaches refused naming the rule, property that executing the plan through Post yields exactly the planned entries and balance; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/balance/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `BAL-001`, `BAL-003`, `BAL-004`, `SNAPSHOT-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.REWARDS; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_BAL_011`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_BAL_011`; `PROPERTY=TestTodo_BAL_011_Property`; `GOLDEN=TestTodo_BAL_011_Golden`; `RACE=TestTodo_BAL_011_Race`; `CONFORMANCE=TestTodo_BAL_011_Conformance`; `MUTATION=TestTodo_BAL_011_Mutation`.
  - **RED:** plan exceeds available/floor/cap, mixes units/periods, omits expected account revision, mutates an entry/account, or simulation reports a planned amount as posted.
  - **GREEN:** immutable `BalancePlan` binds account/definition revisions, opening balance, ordered proposed entries, accepted/rejected/remainder amounts, resulting balance, rules and source proposal digest while producing zero ledger/domain writes.
  - **REFACTOR:** Leave chooses which eligible programs fund intervals; Balance engine only validates and plans exact account movements.
  - **Refs:** [Balance models](data/models/rewards-payroll-workforce.md), [simulation contract](workflows/_shared/exploration-contract.md).

- [ ] `BAL-012` **[PHASE_2][SOL_HIGH] Post a BalancePlan atomically with its owning business transaction.**
  - **Depends:** `BAL-011`, `BAL-002`, `TX-004`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_BAL_012`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_BAL_012`; `PROPERTY=TestTodo_BAL_012_Property`; `RACE=TestTodo_BAL_012_Race`; `MUTATION=TestTodo_BAL_012_Mutation`.
  - **RED:** stale account head, changed plan/proposal, failpoint between leave fact and one of several balance entries, or repeated transaction produces partial/double debit.
  - **GREEN:** every planned entry, account head, domain event, projection and outbox participant commits under the owning `BusinessTransaction` once or none do; receipt identifies opening/ending heads and unused remainder.
  - **REFACTOR:** balance posting participates through typed transaction events rather than nested autonomous commits.
  - **Refs:** [Transaction coordinator](specs/transaction-plan-and-commit-coordinator.md), [ledger multi-stream append](#27-authoritative-ledger-implementation).

- [ ] `BAL-013` **[PHASE_2][SOL_HIGH] Invalidate dependent future balance plans when availability changes.**
  - **Depends:** `BAL-011`, `EVENT-002`, `MODEL-020`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_BAL_013`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_BAL_013`; `PROPERTY=TestTodo_BAL_013_Property`; `RACE=TestTodo_BAL_013_Race`; `MUTATION=TestTodo_BAL_013_Mutation`.
  - **RED:** intervening debit/correction/expiry/rollover leaves a future approved plan executable, invalidates unrelated plans, or silently changes paid/unpaid allocation.
  - **GREEN:** dependency index identifies affected plan components, compares old/new available amount and emits one typed `REPLAN_REQUIRED` finding with source revisions; unaffected plans remain valid.
  - **REFACTOR:** recalculation creates successor plans/proposals and never edits the approved plan.
  - **Refs:** [Provenance dependencies](specs/provenance-graph-and-lineage.md), [workflow revalidation](specs/workflow-runtime.md).

## 38A. Medical Leave and Return-to-Work convergence slice

> **Disposition (2026-09-02):** CONFORMANCE only, as next-steps.md states; it must not pull `JOIN`, `SUBWORKFLOW`, provider writes or legal authority into P1A/P1B.

- [x] `AVAIL-001` **[CONFORMANCE][SOL_HIGH] Define effective-dated WorkerAvailability and AbsenceImpact contracts.**
  - **Depends:** `MODEL-004`, `MODEL-005`, `MODEL-013`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.WORKFORCE; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_AVAIL_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_AVAIL_001`; `PROPERTY=TestTodo_AVAIL_001_Property`; `CONFORMANCE=TestTodo_AVAIL_001_Conformance`; `MUTATION=TestTodo_AVAIL_001_Mutation`.
  - **RED:** availability is inferred from Employment status, lacks source/reason/interval/scope/revision, or schedule assignment and actual availability become one mutable field.
  - **GREEN:** typed availability revisions distinguish `AVAILABLE|UNAVAILABLE|RESTRICTED|UNKNOWN`, preserve employment/assignment identity and describe requested/approved absence separately from schedule source and coverage effects.
  - **REFACTOR:** Leave, scheduling and qualification consume the same availability facts without sharing domain rules.
  - **Refs:** [People/workforce models](data/models/people-workforce.md), [wire time](data/models/wire-contract-primitives.md).
  - **Evidence (2026-09-03):** `TestTodo_AVAIL_001`, Declared `AVAIL-001` test matrix and acceptance behavior in `internal/domains/availability`; PASS; `go test -p 1 ./internal/domains/availability` and `go vet ./internal/domains/availability` on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `AVAIL-002` **[CONFORMANCE][SOL_HIGH] Simulate absence against a versioned work schedule.**
  - **Evidence (2026-09-05):** `TestTodo_AVAIL_002` in `internal/domains/availability` (simulation of a requested absence window against a versioned work schedule (working days, shifts, jurisdiction holidays, timezone) yielding scheduled hours consumed per day, non-working days skipped, conflicts with existing absences and a digested result, mutating no schedule or balance, golden across a DST transition; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/availability/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `AVAIL-001`, `SNAPSHOT-003`, `RULE-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.WORKFORCE; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_AVAIL_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_AVAIL_002`; `PROPERTY=TestTodo_AVAIL_002_Property`; `GOLDEN=TestTodo_AVAIL_002_Golden`; `CONFORMANCE=TestTodo_AVAIL_002_Conformance`.
  - **RED:** ambient calendar/timezone, stale schedule, DST/holiday boundary, overlapping assignment, unknown shift or unscheduled day produces invented hours; simulation mutates schedule/availability.
  - **GREEN:** immutable impact lists exact scheduled intervals/hours/shifts removed, source schedule/calendar/tzdb revisions, conflicts/unknowns and a typed coverage-demand condition while producing zero state change.
  - **REFACTOR:** coverage condition may trigger WFM intent; Leave workflow does not optimize replacement staffing.
  - **Refs:** [Leave workflow](workflows/leave/leave-return-to-work.md), [time primitives](data/models/wire-contract-primitives.md).

- [ ] `AVAIL-003` **[PHASE_2][SOL_HIGH] Append an availability revision through the owning transaction.**
  - **Depends:** `AVAIL-002`, `TX-004`, `CONFLICT-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_AVAIL_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_AVAIL_003`; `PROPERTY=TestTodo_AVAIL_003_Property`; `RACE=TestTodo_AVAIL_003_Race`; `RECOVERY=TestTodo_AVAIL_003_Recovery`; `MUTATION=TestTodo_AVAIL_003_Mutation`.
  - **RED:** stale schedule/availability head, overlapping incompatible interval, duplicate leave/return effect or failpoint produces availability without its owning Leave event.
  - **GREEN:** bounded transaction appends unavailable/restricted/restored successor once with source intent/proposal/reason/effective interval and queues schedule/WFM reconciliation effect; Employment and authored schedule history are not overwritten.
  - **REFACTOR:** provider schedule mutation remains an external effect unless its domain is explicitly co-located and authoritative.
  - **Refs:** [Transaction coordinator](specs/transaction-plan-and-commit-coordinator.md), [conflict contract](specs/cross-workflow-conflict-and-write-intent.md).

- [x] `LEAVE-001` **[CONFORMANCE][SOL_HIGH] Publish the intent-only `leave.request` ProcessRequest contract.**
  - **Depends:** `TOOL-004`, `INTENT-001`, `TRUST-001`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.WORKFORCE; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_LEAVE_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEAVE_001`; `PROPERTY=TestTodo_LEAVE_001_Property`; `GOLDEN=TestTodo_LEAVE_001_Golden`; `SECURITY=TestTodo_LEAVE_001_Security`; `CONFORMANCE=TestTodo_LEAVE_001_Conformance`; `MUTATION=TestTodo_LEAVE_001_Mutation`.
  - **RED:** caller asserts eligibility, balances, manager, benefits/payroll treatment, legal context or derived entitlement; selects trusted context; omits expected worker revision/client request ID; or request is modeled as one undifferentiated ChangeRequest.
  - **GREEN:** generated request accepts worker, requested leave type/interval/mode/reason and governed evidence refs only; trusted boundary injects contexts and `RequestLeave` is a `ProcessRequest` that may produce bounded typed child intents for leave, availability, balances, payroll, benefits, schedule, access and return.
  - **REFACTOR:** gRPC, grpcbridge, agent and admin surfaces invoke the same capability and resolve current truth server-side.
  - **Refs:** [Leave workflow](workflows/leave/leave-return-to-work.md), [BusinessIntent catalog](specs/business-intent-catalog.md).
  - **Evidence (2026-09-03):** `TestTodo_LEAVE_001`, Declared `LEAVE-001` test matrix and acceptance behavior in `internal/domains/leave`; PASS; `go test -p 1 ./internal/domains/leave` and `go vet ./internal/domains/leave` on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `LEAVE-002` **[CONFORMANCE][SOL_HIGH] Define immutable LeaveProgram revisions and interaction metadata.**
  - **Evidence (2026-09-05):** `TestTodo_LEAVE_002`, `TestTodo_LEAVE_002_Conformance`, `TestTodo_LEAVE_002_Fault`, `TestTodo_LEAVE_002_Mutation`, `TestTodo_LEAVE_002_Property`, `TestTodo_LEAVE_002_Security` in `internal/domains/leave` (immutable, source-attributed LeaveProgramRevision with interaction metadata, caller digests discarded, append-only catalog); `go test -count=1 ./internal/domains/leave/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ELIG-001`, `LEGAL-001`, `MODEL-017`, `MODEL-018`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.WORKFORCE; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_LEAVE_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEAVE_002`; `PROPERTY=TestTodo_LEAVE_002_Property`; `FAULT=TestTodo_LEAVE_002_Fault`; `SECURITY=TestTodo_LEAVE_002_Security`; `CONFORMANCE=TestTodo_LEAVE_002_Conformance`; `MUTATION=TestTodo_LEAVE_002_Mutation`.
  - **RED:** program lacks authority/type/scope/eligibility/effective interval, protected/paid semantics, evidence/notices, interaction/concurrency priority, balance source, return obligations or unknown policy.
  - **GREEN:** statutory, collective and company program revisions are immutable, source-attributed and declare composition behavior without encoding a real jurisdiction in platform defaults.
  - **REFACTOR:** rule packs provide jurisdiction content; Leave domain owns program semantics and composition inputs.
  - **Refs:** [Regulatory models](data/models/rules-and-decisions.md), [leave data](data/models/rewards-payroll-workforce.md).

- [ ] `LEAVE-003` **[CONFORMANCE][SOL_HIGH] Build the immutable Leave input snapshot.**
  - **Depends:** `LEAVE-001`, `LEAVE-002`, `SNAPSHOT-003`, `DOC-INTAKE-001`, `BAL-003`, `AVAIL-001`, `LEGAL-001`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.WORKFORCE; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_LEAVE_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEAVE_003`; `PROPERTY=TestTodo_LEAVE_003_Property`; `GOLDEN=TestTodo_LEAVE_003_Golden`; `SECURITY=TestTodo_LEAVE_003_Security`; `CONFORMANCE=TestTodo_LEAVE_003_Conformance`; `MUTATION=TestTodo_LEAVE_003_Mutation`.
  - **RED:** snapshot omits worker/employment/assignment/location/manager, schedule/calendar, balances, benefits, payroll context, legal context, evidence usability, source authority, freshness or effective/known time; restricted bytes enter the snapshot.
  - **GREEN:** `LeaveInputSnapshot` binds exact revisions/watermarks for every required input and typed restricted `EvidenceRef`; each unsatisfied input remains `PARTIAL|STALE|UNKNOWN|DENIED` and canonical digest changes on material input change.
  - **REFACTOR:** evidence content stays in its compartment and external/native facts share source-neutral snapshot entries.
  - **Refs:** [Leave workflow](workflows/leave/leave-return-to-work.md), [source authority](specs/source-authority-and-external-mastering.md).

- [ ] `LEAVE-004` **[CONFORMANCE][SOL_HIGH] Resolve applicable LeavePrograms and eligibility independently of approval.**
  - **Depends:** `LEAVE-003`, `ELIG-003`, `ELIG-004`, `LEGAL-006`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.WORKFORCE; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_LEAVE_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEAVE_004`; `PROPERTY=TestTodo_LEAVE_004_Property`; `RACE=TestTodo_LEAVE_004_Race`; `SECURITY=TestTodo_LEAVE_004_Security`; `CONFORMANCE=TestTodo_LEAVE_004_Conformance`; `MUTATION=TestTodo_LEAVE_004_Mutation`.
  - **RED:** manager/reviewer can determine statutory entitlement, ineligible/unknown program disappears, missing legal/evidence fact becomes denial, or program results lose independent rule/fact/version traces.
  - **GREEN:** each applicable program returns its own `ELIGIBLE|INELIGIBLE|CONDITIONAL|UNKNOWN` result and the composition input preserves all contributing authorities, blockers and obligations with zero human approval or domain mutation.
  - **REFACTOR:** evidence sufficiency finding is an input fact, not a substitute for eligibility rules.
  - **Refs:** [Eligibility engine](#38-eligibility-business-cycle-and-balance-engines), [legal composition](#20-regulatory-platform-and-content-operations).

- [ ] `LEAVE-005` **[CONFORMANCE][SOL_HIGH] Compose an immutable LeaveEntitlementPlan.**
  - **Depends:** `LEAVE-004`, `BAL-011`, `AVAIL-002`, `RULE-003`, `MODEL-005`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.WORKFORCE; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_LEAVE_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEAVE_005`; `PROPERTY=TestTodo_LEAVE_005_Property`; `RACE=TestTodo_LEAVE_005_Race`; `CONFORMANCE=TestTodo_LEAVE_005_Conformance`; `MUTATION=TestTodo_LEAVE_005_Mutation`.
  - **RED:** overlapping programs double-count or erase protection, paid hours exceed schedule/balance, holiday/calendar/timezone changes result implicitly, unknown segment is treated unpaid, or plan posts balances/effects.
  - **GREEN:** plan partitions the requested interval into non-overlapping protected/paid/unpaid/unknown segments, identifies concurrent programs, exact scheduled hours and planned balance debits, benefit/job-protection/return obligations and payroll/benefit/WFM effect plans with calculation trace and zero mutation.
  - **REFACTOR:** Workflow never calculates entitlement, balances or scheduled hours.
  - **Refs:** [Leave workflow](workflows/leave/leave-return-to-work.md), [balance engine](#38-eligibility-business-cycle-and-balance-engines).

- [ ] `LEAVE-006` **[CONFORMANCE][SOL_HIGH] Persist a side-effect-free Leave simulation and ProposalRevision.**
  - **Depends:** `LEAVE-005`, `GOVERN-002`, `INTENT-005`, `WF-RUN-012`, `EFFECT-001`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.WORKFORCE; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_LEAVE_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEAVE_006`; `PROPERTY=TestTodo_LEAVE_006_Property`; `GOLDEN=TestTodo_LEAVE_006_Golden`; `CONFORMANCE=TestTodo_LEAVE_006_Conformance`; `MUTATION=TestTodo_LEAVE_006_Mutation`; `SECURITY=TestTodo_LEAVE_006_Security`.
  - **RED:** simulation creates leave/availability/schedule/balance rows, WorkItem, MessageIntent, outbox or external call; proposal omits entitlement/balance/evidence/notice/return/effect component or sensitive detail leaks into ordinary artifact.
  - **GREEN:** immutable simulation binds snapshot, eligibility/entitlement/balance/schedule/effect plans, obligations, restricted evidence refs and explicit `NOT_EXECUTED`; successor ProposalRevision freezes every material component under one digest.
  - **REFACTOR:** safe presentation is a field-masked view of typed artifacts, not copied medical evidence.
  - **Refs:** [Simulation contract](workflows/_shared/exploration-contract.md), [proposal contract](specs/business-intent-and-change-request.md).

- [ ] `LEAVE-007` **[CONFORMANCE][SOL_HIGH] Partially replan Leave after material effective-date drift.**
  - **Depends:** `LEAVE-006`, `BAL-013`, `REPLAN-004`, `ELIG-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.WORKFORCE; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_LEAVE_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEAVE_007`; `PROPERTY=TestTodo_LEAVE_007_Property`; `GOLDEN=TestTodo_LEAVE_007_Golden`; `CONFORMANCE=TestTodo_LEAVE_007_Conformance`; `MUTATION=TestTodo_LEAVE_007_Mutation`.
  - **RED:** intervening PTO debit denies otherwise valid leave, silently changes pay, retains dependent acknowledgement, invalidates unrelated evidence/eligibility, or executes old proposal digest.
  - **GREEN:** current snapshot reevaluates eligibility and affected funding segments; example `PTO 72→56` creates successor plan/proposal with additional unpaid hours, preserves unaffected protection/evidence decisions by explicit policy and routes exact reacknowledgement/reapproval before execution.
  - **REFACTOR:** changed paid treatment and leave entitlement remain separate outcome dimensions.
  - **Refs:** [Workflow revalidation](specs/workflow-runtime.md), [proposal replanning](specs/business-intent-and-change-request.md).

- [ ] `LEAVE-008` **[CONFORMANCE][SOL_HIGH] Compile the bounded Leave-start TransactionPlan and invariants.**
  - **Depends:** `LEAVE-006`, `LEAVE-007`, `TX-001`, `BAL-011`, `AVAIL-002`, `CONFLICT-002`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.WORKFORCE; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_LEAVE_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEAVE_008`; `PROPERTY=TestTodo_LEAVE_008_Property`; `CONFORMANCE=TestTodo_LEAVE_008_Conformance`; `MUTATION=TestTodo_LEAVE_008_Mutation`.
  - **RED:** plan changes Employment to terminated/inactive, omits leave/absence/availability/balance streams, treats payroll/benefits/WFM as local ACID, lacks expected heads or allows overlapping incompatible leave/availability.
  - **GREEN:** plan declares local LeaveRecord/absence relationship/availability and exact balance entries under one admitted boundary while Employment remains `ACTIVE`; payroll, benefits and schedule/WFM remain ordered external effects with observation/reconciliation policies.
  - **REFACTOR:** native scheduling may later join the local boundary only through explicit authority and consistency admission.
  - **Refs:** [Transaction coordinator](specs/transaction-plan-and-commit-coordinator.md), [leave data](data/models/rewards-payroll-workforce.md).

- [ ] `LEAVE-009` **[PHASE_2][SOL_HIGH] Commit Leave start and balance entries atomically.**
  - **Depends:** `LEAVE-008`, `LEAVE-014`, `LEAVE-017`, `BAL-012`, `AVAIL-003`, `TX-004`, `DATA-007`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEAVE_009`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEAVE_009`; `PROPERTY=TestTodo_LEAVE_009_Property`; `MUTATION=TestTodo_LEAVE_009_Mutation`.
  - **RED:** failpoint yields active leave without availability/balance/event/outbox, balance debit without LeaveStarted, duplicate start, or employment-state mutation.
  - **GREEN:** leave revision, active absence relationship, unavailable interval, exact balance entries, ledger events, critical projections and effect outbox commit once or none do; Business state becomes `LEAVE_ACTIVE` independently of external consistency.
  - **REFACTOR:** historical employment/leave/balance revisions remain append-only.
  - **Refs:** [Ledger and outbox](specs/transaction-ledger-reconciliation-and-repair.md), [multi-stream commit](#27-authoritative-ledger-implementation).

- [ ] `LEAVE-010` **[PHASE_2][SOL_HIGH] Model extend, shorten and cancel as related successor intents.**
  - **Depends:** `LEAVE-007`, `LEAVE-009`, `INTENT-002`, `CONFLICT-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEAVE_010`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEAVE_010`; `PROPERTY=TestTodo_LEAVE_010_Property`; `GOLDEN=TestTodo_LEAVE_010_Golden`; `SECURITY=TestTodo_LEAVE_010_Security`; `MUTATION=TestTodo_LEAVE_010_Mutation`.
  - **RED:** original request/revision is mutated, extension bypasses snapshot/eligibility/legal/balance/effect review, overlapping child changes both commit, or cancellation rewrites an already-consumed balance.
  - **GREEN:** `ExtendLeave|ShortenLeave|CancelLeave` references the LeaveRecord and prior intent/proposal, executes full governed replanning, resolves conflicts deterministically and appends successor facts/corrections with complete lineage.
  - **REFACTOR:** related intent graph explains chronology without one giant mutable LeaveRequest.
  - **Refs:** [BusinessIntent relationships](specs/business-intent-catalog.md), [conflict contract](specs/cross-workflow-conflict-and-write-intent.md).

- [ ] `LEAVE-011` **[PHASE_2][SOL_HIGH] Reconcile Leave-start external effects without rolling back valid leave.**
  - **Depends:** `LEAVE-009`, `EFFECT-001`, `RECON-002`, `REPAIR-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEAVE_011`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEAVE_011`; `PROPERTY=TestTodo_LEAVE_011_Property`; `GOLDEN=TestTodo_LEAVE_011_Golden`; `INTEGRATION=TestTodo_LEAVE_011_Integration`; `MUTATION=TestTodo_LEAVE_011_Mutation`.
  - **RED:** unavailable benefits observation rolls back local leave, provider acceptance reports pass, optional/mandatory effects collapse, or repair reruns LeaveStarted/balance entries.
  - **GREEN:** payroll/WFM may `PASS` while benefits remains `UNKNOWN`; dimensions report Business `LEAVE_ACTIVE`, ConsistencyState `DEGRADED`, ObligationState `PENDING` and targeted reconciliation/repair proceeds without a second local leave transaction.
  - **REFACTOR:** each external effect has independent freshness, deadline, criticality and repair ownership.
  - **Refs:** [Reconciliation](specs/transaction-ledger-reconciliation-and-repair.md), [integration observations](specs/integration-platform.md).

- [ ] `LEAVE-012` **[CONFORMANCE][SOL_HIGH] Evaluate domain-specific Return-to-Work readiness and restrictions.**
  - **Depends:** `LEAVE-010`, `WORK-009`, `QUAL-004`, `LEGAL-004`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.WORKFORCE; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_LEAVE_012`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEAVE_012`; `SECURITY=TestTodo_LEAVE_012_Security`; `CONFORMANCE=TestTodo_LEAVE_012_Conformance`; `MUTATION=TestTodo_LEAVE_012_Mutation`.
  - **RED:** missing/expired/quarantined clearance, unresolved restriction, unavailable job requirement or unknown schedule/access state returns ready; free-text restriction directly mutates assignment.
  - **GREEN:** result is exactly `READY|READY_WITH_RESTRICTIONS|NOT_READY|UNKNOWN`, binding leave/evidence/job/schedule/access revisions; structured restriction produces governed `EstablishWorkRestriction` and accommodation/reassignment follow-up intents when qualification is conditional.
  - **REFACTOR:** this remains Leave-domain readiness until cross-domain conformance proves a shared engine.
  - **Refs:** [Leave workflow](workflows/leave/leave-return-to-work.md), [qualification engine](#39-qualification-demand-matching-and-scenario-engines).

- [ ] `LEAVE-013` **[PHASE_2][SOL_HIGH] Commit ReturnFromLeave and reconcile restored effects.**
  - **Depends:** `LEAVE-012`, `AVAIL-003`, `TX-004`, `EFFECT-001`, `RECON-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEAVE_013`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEAVE_013`; `RACE=TestTodo_LEAVE_013_Race`; `INTEGRATION=TestTodo_LEAVE_013_Integration`; `RECOVERY=TestTodo_LEAVE_013_Recovery`; `MUTATION=TestTodo_LEAVE_013_Mutation`.
  - **RED:** return before readiness, duplicate return, active restriction is discarded, absence ends without availability restoration, or payroll/benefits/schedule/access provider failure rewrites local return truth.
  - **GREEN:** local transaction appends LeaveEnded and availability restoration once, preserves active restrictions, queues normal-pay/benefit/schedule/access effects and closes only after versioned obligation/reconciliation policy.
  - **REFACTOR:** external return treatment remains observed effects, not proof of the local Leave end.
  - **Refs:** [Outcome management](plan.md#63-change-request-lifecycle), [workflow runtime](specs/workflow-runtime.md).

- [ ] `LEAVE-014` **[PHASE_2][SOL_HIGH] Generate, deliver and reconcile the Leave determination notice.**
  - **Depends:** `LEAVE-006`, `LEAVE-017`, `LEGAL-004`, `DOC-TEMPLATE-001`, `MSG-001`–`MSG-009`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEAVE_014`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEAVE_014`; `PROPERTY=TestTodo_LEAVE_014_Property`; `INTEGRATION=TestTodo_LEAVE_014_Integration`; `SECURITY=TestTodo_LEAVE_014_Security`; `MUTATION=TestTodo_LEAVE_014_Mutation`.
  - **RED:** notice uses mutable/unapproved template, exposes medical evidence to manager/email/provider, omits program/interval/paid-unpaid/rights/obligations required by the hypothetical policy, treats provider acceptance as delivery, or Leave starts despite an unsatisfied blocking notice requirement.
  - **GREEN:** deterministic localized determination artifact binds proposal/legal/program versions and safe worker-facing explanation; secure inbox plus approved attention channel follows `DELIVERED|ACKNOWLEDGED` policy, failures create fallback work, and workflow receives an evidenced obligation signal before the configured execution boundary.
  - **REFACTOR:** legal engine defines required content/delivery semantics, document renders it, messaging transports it and workflow coordinates the obligation.
  - **Refs:** [Messaging plane](specs/messaging-and-notification-plane.md), [document templates](data/models/connectivity-access-content.md), [Leave workflow](workflows/leave/leave-return-to-work.md).

- [ ] `LEAVE-015` **[CONFORMANCE][SOL_HIGH] Prove the canonical Medical Leave logical-persistence trace.**
  - **Depends:** `LEAVE-001`–`LEAVE-014`, `LEAVE-016`, `LEAVE-017`, `WF-RUN-023`–`WF-RUN-025`, `EVIDENCE-001`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.WORKFORCE; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_LEAVE_015`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEAVE_015`; `PROPERTY=TestTodo_LEAVE_015_Property`; `GOLDEN=TestTodo_LEAVE_015_Golden`; `RACE=TestTodo_LEAVE_015_Race`; `FAULT=TestTodo_LEAVE_015_Fault`; `SECURITY=TestTodo_LEAVE_015_Security`; `CONFORMANCE=TestTodo_LEAVE_015_Conformance`.
  - **RED:** T0…Tn fixture observes a forbidden early mutation, raw medical bytes outside compartment, lost timer/signal/work continuation, overwritten proposal/leave/balance revision, employment inactivity, false external success or unexplained intent closure.
  - **GREEN:** deterministic trace asserts logical inserts/transitions from intent/evidence intake through snapshot, eligibility/composition, simulation/proposal, restricted review, wait/replan, atomic leave start, degraded benefits observation/recovery, extension, return evidence/restriction/readiness, atomic return, reconciliation and final execution receipt.
  - **REFACTOR:** conformance uses hypothetical rules and public capabilities; it does not certify any real jurisdiction.
  - **Refs:** [Leave workflow](workflows/leave/leave-return-to-work.md), [reference suite](reference-workflows/reference-suite.md).

- [ ] `LEAVE-016` **[PHASE_2][SOL_HIGH] Create the Leave process anchor without changing workforce state.**
  - **Depends:** `LEAVE-001`, `DB-023`, `INTENT-002`, `LEDGER-002`, `TX-006`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEAVE_016`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEAVE_016`; `PROPERTY=TestTodo_LEAVE_016_Property`; `RACE=TestTodo_LEAVE_016_Race`; `INTEGRATION=TestTodo_LEAVE_016_Integration`; `MUTATION=TestTodo_LEAVE_016_Mutation`.
  - **RED:** duplicate request creates two intents/LeaveRequests, request creation changes employment/availability/schedule/balance, dispatches provider effects, or loses request/evidence/subject/effective interval correlation.
  - **GREEN:** one idempotent capability invocation persists `RequestLeave` IntentInstance as `CHANGE_REQUEST` (child-bound process), requested LeaveRequest revision and `LeaveRequested` chronology/provenance while all workforce/domain mutation and external-effect counts remain zero.
  - **REFACTOR:** requested process state is not an approved entitlement or active LeaveRecord.
  - **Refs:** [BusinessIntent contract](specs/business-intent-and-change-request.md), [Leave workflow](workflows/leave/leave-return-to-work.md).

- [ ] `LEAVE-017` **[PHASE_2][SOL_HIGH] Execute the restricted evidence review and request-more-information loop.**
  - **Depends:** `LEAVE-006`, `WORK-008`, `WORK-009`, `WF-STEP-004`, `WF-STEP-006`, `MSG-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEAVE_017`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEAVE_017`; `PROPERTY=TestTodo_LEAVE_017_Property`; `FUZZ=FuzzTodo_LEAVE_017`; `SECURITY=TestTodo_LEAVE_017_Security`; `MUTATION=TestTodo_LEAVE_017_Mutation`.
  - **RED:** obligation creates an ordinary manager task, insufficient evidence ends as denial without policy, repeated request duplicates tasks/messages, raw reply/upload resumes workflow, or new evidence bypasses scan/classification/current-authority checks.
  - **GREEN:** proposal-bound restricted WorkItem routes to LeaveAdministrator; `MORE_INFORMATION_REQUIRED` creates one minimal-disclosure worker message and durable signal subscription; a new governed `EvidenceRef` resumes once and reevaluates sufficiency/eligibility under current context.
  - **REFACTOR:** workflow coordinates the loop while Document, Work, Messaging and Eligibility retain their own semantics.
  - **Refs:** [Human work](specs/human-work-forms-and-rules.md), [workflow signals](specs/workflow-runtime.md), [messaging](specs/messaging-and-notification-plane.md).

## 38B. Readiness abstraction proof before engine extraction

> **Disposition (2026-09-02):** DEFERRED.

- [x] `READINESS-CONF-001` **[CONFORMANCE][SOL_HIGH] Prove a shared Readiness abstraction is real across four domains.**
  - **Evidence (2026-09-07):** `TestTodo_READINESS_CONF_001`, `TestTodo_READINESS_CONF_001_Conformance`, `TestTodo_READINESS_CONF_001_Fault`, `TestTodo_READINESS_CONF_001_Golden`, `TestTodo_READINESS_CONF_001_Race` in `internal/engines/readiness` (written in 66318d1 on 2026-09-06 and never ticked; the named tests prove the GREEN contract: conformance report identifies the exact common kernel and domain-owned extensions for `READY|CONDITIONAL|NOT_READY|UNKNOWN`; if no stable kernel exists, generic engine todos remain blocked and domains keep local evaluators); `go test -count=1 ./internal/engines/readiness/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LEAVE-012`, `CONF-002`, `CONF-009`, `RECOVERY-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.WORKFORCE; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_READINESS_CONF_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_READINESS_CONF_001`; `GOLDEN=TestTodo_READINESS_CONF_001_Golden`; `RACE=TestTodo_READINESS_CONF_001_Race`; `FAULT=TestTodo_READINESS_CONF_001_Fault`; `CONFORMANCE=TestTodo_READINESS_CONF_001_Conformance`.
  - **RED:** Return-to-Work, onboarding, payroll release and recovery/go-live cannot share requirement/evidence/status/follow-up semantics without erasing domain rules or leaking protected evidence.
  - **GREEN:** conformance report identifies the exact common kernel and domain-owned extensions for `READY|CONDITIONAL|NOT_READY|UNKNOWN`; if no stable kernel exists, generic engine todos remain blocked and domains keep local evaluators.
  - **REFACTOR:** similarity of names is not sufficient evidence for abstraction.
  - **Refs:** [Modeling conventions](data/models/modeling-conventions.md), [reference workflows](workflows/README.md).

- [x] `READINESS-001` **[PHASE_3][SOL_HIGH] Define ReadinessRequirement only after conformance proof.**
  - **Evidence (2026-09-05):** `TestTodo_READINESS_001` in `internal/engines/readiness` (minimal immutable requirement schema covers only the demonstrated common kernel and records originating domain/capability/version; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/engines/readiness/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `READINESS-CONF-001`, `MODEL-015`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_READINESS_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_READINESS_001`; `GOLDEN=TestTodo_READINESS_001_Golden`; `CONFORMANCE=TestTodo_READINESS_001_Conformance`.
  - **RED:** definition cannot represent all proven required evidence, time, owner, blocking/conditional policy and domain extension points or admits arbitrary untyped predicates.
  - **GREEN:** minimal immutable requirement schema covers only the demonstrated common kernel and records originating domain/capability/version.
  - **REFACTOR:** domain-specific eligibility, legality and qualification remain external inputs.
  - **Refs:** [Rules models](data/models/rules-and-decisions.md), [capability lifecycle](specs/capability-registry-and-lifecycle.md).

- [x] `READINESS-002` **[PHASE_3][SOL_HIGH] Resolve authorized readiness evidence.**
  - **Evidence (2026-09-05):** `TestTodo_READINESS_002` in `internal/engines/readiness` (resolver returns requirement-level SATISFIED|UNSATISFIED|CONDITIONAL|UNKNOWN with authorized evidence refs, freshness and provenance; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/engines/readiness/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `READINESS-001`, `SNAPSHOT-003`, `TRUST-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_READINESS_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_READINESS_002`; `GOLDEN=TestTodo_READINESS_002_Golden`; `FUZZ=FuzzTodo_READINESS_002`.
  - **RED:** stale/denied/untrusted/quarantined evidence counts, hidden evidence leaks through explanation, or current facts are read without pinned versions.
  - **GREEN:** resolver returns requirement-level `SATISFIED|UNSATISFIED|CONDITIONAL|UNKNOWN` with authorized evidence refs, freshness and provenance.
  - **REFACTOR:** engine receives typed evidence descriptors, never raw medical/document content.
  - **Refs:** [Provenance](specs/provenance-graph-and-lineage.md), [classification](specs/data-classification-and-dlp.md).

- [x] `READINESS-003` **[PHASE_3][SOL_HIGH] Evaluate and explain readiness deterministically.**
  - **Evidence:** Luna implementation, Sol refinement, and integration review on 2026-09-07: `TestTodo_READINESS_003`, `TestTodo_READINESS_003_Property`, `TestTodo_READINESS_003_Golden` and input/integrity regressions in `internal/engines/readiness`; `go test -count=1 -p 1 -cover ./internal/engines/readiness/` PASS on windows/arm64 (75.8%); `go vet ./internal/engines/readiness/` PASS. Pure domain evaluation binds requirement/resolution digests, preserves unknown/conditional outcomes, and emits only bounded reason codes; no runtime publication or UI behavior is claimed.
  - **Depends:** `READINESS-002`, `RULE-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_READINESS_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_READINESS_003`; `PROPERTY=TestTodo_READINESS_003_Property`; `GOLDEN=TestTodo_READINESS_003_Golden`.
  - **RED:** unknown/conditional requirement becomes ready, same inputs diverge, or explanation discloses restricted evidence.
  - **GREEN:** versioned policy returns exactly `READY|CONDITIONAL|NOT_READY|UNKNOWN`, blockers/conditions, validity interval and redaction-safe explanation digest.
  - **REFACTOR:** evaluation has zero business effects.
  - **Refs:** [Model conventions](data/models/modeling-conventions.md), [governance decisions](specs/governance-decision-and-obligation-composition.md).

- [x] `READINESS-004` **[PHASE_3][SOL_HIGH] Compile readiness follow-up requirements into governed intents.**
  - **Evidence (2026-09-08):** `TestTodo_READINESS_004`, `TestTodo_READINESS_004_Golden`, `TestTodo_READINESS_004_Race`, and `TestTodo_READINESS_004_Mutation`; `go test -count=1 -cover ./internal/engines/readiness/` PASS (76.8%); `go vet ./internal/engines/readiness/` PASS. Bounded draft compilation pins requirement, evaluation, evidence and policy, rejects cross-domain configuration, emits nothing for READY, and binds explicit human-review/governance requirements into idempotent semantic IDs. Sol refinement and root review covered mutated gates/identity and concurrent success against a known-good digest. This pure compiler grants no execution authority and performs no persistence or business mutation.
  - **Depends:** `READINESS-003`, `INTENT-001`, `GOVERN-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_READINESS_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_READINESS_004`; `GOLDEN=TestTodo_READINESS_004_Golden`; `RACE=TestTodo_READINESS_004_Race`; `MUTATION=TestTodo_READINESS_004_Mutation`.
  - **RED:** missing qualification/evidence/approval automatically performs irreversible action, duplicate evaluation creates duplicate intent, or generated intent exceeds declared domain authority.
  - **GREEN:** configured unmet/conditional requirement produces bounded idempotent draft intents/tasks/obligations with owner, scope, risk and evidence; human/governance gates remain explicit.
  - **REFACTOR:** readiness recommends/compiles next work and never bypasses capability execution.
  - **Refs:** [BusinessIntent catalog](specs/business-intent-catalog.md), [workflow runtime](specs/workflow-runtime.md).

## 39. Qualification, demand, matching and scenario engines

> **Disposition (2026-09-02):** DEFERRED.

- [x] `QUAL-001` **[CONFORMANCE][SOL_HIGH] Define QualificationRequirement.**
  - **Evidence (2026-09-05):** `TestTodo_QUAL_001` in `internal/domains/qualification` (versioned digested QualificationRequirement with subject scope, required credential and skill refs with levels, accepted evidence kinds, validity window and renewal rule, evaluation against held credentials yielding SATISFIED/EXPIRING/UNSATISFIED with the gap named, and Explain; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/qualification/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MSRC-006`, `MODEL-013`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.TALENT; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_QUAL_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_QUAL_001`; `PROPERTY=TestTodo_QUAL_001_Property`; `SECURITY=TestTodo_QUAL_001_Security`; `CONFORMANCE=TestTodo_QUAL_001_Conformance`; `MUTATION=TestTodo_QUAL_001_Mutation`.
  - **RED:** Seeded defect: expired or missing evidence returns QUALIFIED or domain results diverge for QUAL-001; the primary test must return QUAL_001_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Requirement missing assignment/context, skills, credentials, experience, authorization, availability, equivalency, validity or unknown policy fails.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Talent models](data/models/talent-experience-cases.md), [relationship map](data/models/relationship-map.md).

- [x] `QUAL-002` **[PHASE_2][SOL_HIGH] Resolve authorized worker qualification evidence.**
  - **Evidence (2026-09-05):** `TestTodo_QUAL_002` in `internal/domains/qualification` (authorized resolution of a worker's qualification evidence at an as-of over a CredentialFacts port with an in-memory fake, each credential disclosed under the caller's declared scope so a caller without qualification-read scope receives WITHHELD with the reason, the QUAL-001 evaluation run over the disclosed set only, digested result and an Explain that never repeats evidence contents; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/qualification/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `QUAL-001`, `TRUST-012`, `MODEL-021`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_QUAL_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_QUAL_002`; `PROPERTY=TestTodo_QUAL_002_Property`; `FUZZ=FuzzTodo_QUAL_002`; `MUTATION=TestTodo_QUAL_002_Mutation`; `SECURITY=TestTodo_QUAL_002_Security`.
  - **RED:** Untrusted/expired/unauthorized evidence cannot count.
  - **GREEN:** Result pins source authority, effective interval, verification and freshness.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Source authority](specs/source-authority-and-external-mastering.md), [talent models](data/models/talent-experience-cases.md).

- [ ] `QUAL-003` **[PHASE_2][SOL_HIGH] Evaluate validity, equivalencies and substitutions.**
  - **Depends:** `QUAL-002`, `RULE-003`, `LEGAL-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_QUAL_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_QUAL_003`; `PROPERTY=TestTodo_QUAL_003_Property`; `FAULT=TestTodo_QUAL_003_Fault`; `SECURITY=TestTodo_QUAL_003_Security`; `MUTATION=TestTodo_QUAL_003_Mutation`.
  - **RED:** Ambiguous equivalency, missing jurisdiction or future/expired credential produces conditional/unknown rather than false qualification.
  - **GREEN:** Exact rule chain is recorded.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Governance composition](specs/governance-decision-and-obligation-composition.md), [rules models](data/models/rules-and-decisions.md).

- [ ] `QUAL-004` **[PHASE_2][SOL_HIGH] Return typed qualification status and explanation.**
  - **Depends:** `QUAL-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_QUAL_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_QUAL_004`; `PROPERTY=TestTodo_QUAL_004_Property`; `MUTATION=TestTodo_QUAL_004_Mutation`.
  - **RED:** Seeded defect: expired or missing evidence returns QUALIFIED or domain results diverge for QUAL-004; the primary test must return QUAL_004_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Output is exactly `QUALIFIED|CONDITIONAL|NOT_QUALIFIED|UNKNOWN`, listing satisfied/missing/expiring/restricted requirements without disclosing protected evidence.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Model conventions](data/models/modeling-conventions.md), [AuthZ](specs/organization-scope-and-authz.md).

- [ ] `QUAL-005` **[PHASE_2][SOL_HIGH] Reevaluate qualification on credential/fact expiry.**
  - **Depends:** `QUAL-004`, `SCHED-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_QUAL_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_QUAL_005`; `PROPERTY=TestTodo_QUAL_005_Property`; `SECURITY=TestTodo_QUAL_005_Security`.
  - **RED:** Expiration/change emits one reevaluation and bounded warning/restriction/removal intent.
  - **GREEN:** No silent continuation or automatic irreversible action.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Workflow runtime](specs/workflow-runtime.md), [talent models](data/models/talent-experience-cases.md).

- [ ] `QUAL-006` **[CONFORMANCE][SOL_HIGH] Prove qualification across Job, Scheduling, Safety, Access and restricted Return-to-Work.**
  - **Depends:** `QUAL-001`–`QUAL-005`, `CONF-004`, `CONF-013`, `CONF-016`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.TALENT; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_QUAL_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_QUAL_006`; `PROPERTY=TestTodo_QUAL_006_Property`; `SECURITY=TestTodo_QUAL_006_Security`; `CONFORMANCE=TestTodo_QUAL_006_Conformance`; `MUTATION=TestTodo_QUAL_006_Mutation`.
  - **RED:** Seeded defect: expired or missing evidence returns QUALIFIED or domain results diverge for QUAL-006; the primary test must return QUAL_006_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Five domain fixtures share evidence/status/effective-time semantics while retaining domain constraints and restricted medical evidence never enters ordinary qualification output.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Future-domain conformance](#21-future-domain-intent-conformance), [intent coverage](data/models/intent-coverage-matrix.md).

- [x] `DEMAND-001` **[CONFORMANCE][SOL_HIGH] Define versioned DemandSignal and CoverageRequirement.**
  - **Evidence (2026-09-05):** `TestTodo_DEMAND_001` in `internal/domains/demand` (versioned digested DemandSignal with source, org unit, role or skill ref, quantity, effective window and confidence class, CoverageRequirement derived per window against supply refs, coverage evaluation with typed shortfalls, and Explain; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/demand/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MSRC-006`, `MODEL-004`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.WORKFORCE; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_DEMAND_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DEMAND_001`; `PROPERTY=TestTodo_DEMAND_001_Property`; `CONFORMANCE=TestTodo_DEMAND_001_Conformance`.
  - **RED:** Demand missing work interval/location/quantity/unit/skill/priority/source/confidence/scenario/version fails.
  - **GREEN:** Immutable requirement composes signals explicitly.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [People/workforce models](data/models/people-workforce.md), [wire time](data/models/wire-contract-primitives.md).

- [x] `DEMAND-002` **[PHASE_3][SOL_HIGH] Aggregate demand signals deterministically.**
  - **Evidence (2026-09-05):** `TestTodo_DEMAND_002` in `internal/domains/demand` (deterministic aggregation of demand signals per org unit, role or skill ref and window bucket under declared bucket rules with confidence-weighted exact-decimal quantities, duplicates collapsed by natural key, order-independent identical digests by property, overlapping windows from one source refused, Explain; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/demand/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DEMAND-001`, `XFORM-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_DEMAND_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DEMAND_002`; `PROPERTY=TestTodo_DEMAND_002_Property`; `FAULT=TestTodo_DEMAND_002_Fault`.
  - **RED:** Incompatible units/dimensions, double-counted source or ambiguous overlap fails.
  - **GREEN:** Aggregation returns source partition, formula/version and exact totals.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Rules models](data/models/rules-and-decisions.md), [provenance](specs/provenance-graph-and-lineage.md).

- [ ] `DEMAND-003` **[PHASE_3][SOL_HIGH] Compare required demand with assigned supply.**
  - **Depends:** `DEMAND-002`, `POP-005`, `QUAL-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_DEMAND_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DEMAND_003`; `PROPERTY=TestTodo_DEMAND_003_Property`; `GOLDEN=TestTodo_DEMAND_003_Golden`.
  - **RED:** Unqualified/unavailable worker cannot satisfy requirement.
  - **GREEN:** Result partitions covered/short/excess/unknown by interval/dimension and source snapshot.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [People/workforce models](data/models/people-workforce.md), [population engine](#37-shared-transformation-and-population-engines).

- [ ] `DEMAND-004` **[PHASE_3][SOL_HIGH] Explain shortages, excess and contributing demand.**
  - **Depends:** `DEMAND-003`, `MODEL-020`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_DEMAND_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DEMAND_004`; `PROPERTY=TestTodo_DEMAND_004_Property`; `RACE=TestTodo_DEMAND_004_Race`; `SECURITY=TestTodo_DEMAND_004_Security`; `MUTATION=TestTodo_DEMAND_004_Mutation`.
  - **RED:** Seeded defect: overlapping demand is double-counted or protected counts leak for DEMAND-004; the primary test must return DEMAND_004_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Explanation traces each gap to signals, assumptions, qualifications and assignments while respecting population/count privacy.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Provenance](specs/provenance-graph-and-lineage.md), [DLP](specs/data-classification-and-dlp.md).

- [ ] `DEMAND-005` **[PHASE_3][SOL_HIGH] Reconcile forecast demand against actuals.**
  - **Depends:** `DEMAND-003`, `TX-009`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_DEMAND_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DEMAND_005`; `PROPERTY=TestTodo_DEMAND_005_Property`.
  - **RED:** Stale/partial actuals cannot close comparison.
  - **GREEN:** Exact forecast/actual/delta/error interval and model/source versions are recorded.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Reconciliation](specs/transaction-ledger-reconciliation-and-repair.md), [assurance models](data/models/assurance-intelligence-platform.md).

- [ ] `DEMAND-006` **[PHASE_3][SOL_HIGH] Emit bounded operational intents from coverage gaps.**
  - **Depends:** `DEMAND-003`, `INTENT-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_DEMAND_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_DEMAND_006`; `PROPERTY=TestTodo_DEMAND_006_Property`; `GOLDEN=TestTodo_DEMAND_006_Golden`; `MUTATION=TestTodo_DEMAND_006_Mutation`.
  - **RED:** Gaps never directly mutate schedules/headcount.
  - **GREEN:** Governed proposal creates named intent with scope, risk, snapshot, cost and human approval policy.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [BusinessIntent](specs/business-intent-and-change-request.md), [transaction coordinator](specs/transaction-plan-and-commit-coordinator.md).

- [x] `MATCH-001` **[CONFORMANCE][SOL_HIGH] Define MatchRequest and CandidateMatch.**
  - **Evidence (2026-09-05):** `TestTodo_MATCH_001` in `internal/domains/matching` (versioned digested MatchRequest with requester scope, target role or position, required qualifications and a closed constraint set, CandidateMatch with per-constraint satisfaction and typed reasons and an explainable factor-bearing score, deterministic ranking with ties broken by candidate ref, unknown constraint kinds refused, Explain; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/matching/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `POP-001`, `QUAL-001`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.WORKFORCE,BI.RECRUITING,BI.TALENT; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_MATCH_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MATCH_001`; `PROPERTY=TestTodo_MATCH_001_Property`; `GOLDEN=TestTodo_MATCH_001_Golden`; `CONFORMANCE=TestTodo_MATCH_001_Conformance`; `MUTATION=TestTodo_MATCH_001_Mutation`.
  - **RED:** Request missing opportunity, candidate source, hard/soft constraints, fairness, snapshot/time, ranking/explanation policy fails.
  - **GREEN:** Result never claims assignment authority.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Talent models](data/models/talent-experience-cases.md), [governance composition](specs/governance-decision-and-obligation-composition.md).

- [x] `MATCH-002` **[PHASE_3][SOL_HIGH] Resolve and freeze authorized candidate populations.**
  - **Evidence (2026-09-05):** `TestTodo_MATCH_002` in `internal/domains/matching` (candidate population for a MatchRequest resolved at an as-of over a facts port with the requester's disclosure scope excluding candidates by counted reason, frozen as an immutable digested CandidatePopulation, a match run against an unfrozen or superseded population refused; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/matching/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MATCH-001`, `POP-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE,BI.RECRUITING,BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_MATCH_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MATCH_002`; `PROPERTY=TestTodo_MATCH_002_Property`; `SECURITY=TestTodo_MATCH_002_Security`.
  - **RED:** Mutable/unauthorized/partial candidate set cannot produce final ranking.
  - **GREEN:** Result binds population and unknown-completeness evidence.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Population engine](#37-shared-transformation-and-population-engines), [AuthZ](specs/organization-scope-and-authz.md).

- [ ] `MATCH-003` **[PHASE_3][SOL_HIGH] Apply qualification and hard constraints.**
  - **Depends:** `MATCH-002`, `QUAL-004`, `LEGAL-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE,BI.RECRUITING,BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_MATCH_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MATCH_003`; `PROPERTY=TestTodo_MATCH_003_Property`; `SECURITY=TestTodo_MATCH_003_Security`; `MUTATION=TestTodo_MATCH_003_Mutation`.
  - **RED:** Any legal/availability/qualification hard failure excludes or marks unknown with reason.
  - **GREEN:** No soft score overrides a hard constraint.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Qualification engine](#39-qualification-demand-matching-and-scenario-engines), [governance composition](specs/governance-decision-and-obligation-composition.md).

- [ ] `MATCH-004` **[PHASE_3][SOL_HIGH] Rank soft preferences deterministically.**
  - **Depends:** `MATCH-003`, `RULE-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE,BI.RECRUITING,BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_MATCH_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MATCH_004`; `PROPERTY=TestTodo_MATCH_004_Property`; `SECURITY=TestTodo_MATCH_004_Security`.
  - **RED:** Hidden feature, nondeterministic tie, unstable normalization or unauthorized sensitive proxy fails.
  - **GREEN:** Score components, weights, tie policy and version reproduce exactly.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Rules models](data/models/rules-and-decisions.md), [talent models](data/models/talent-experience-cases.md).

- [ ] `MATCH-005` **[PHASE_3][SOL_HIGH] Enforce fairness and policy constraints.**
  - **Depends:** `MATCH-004`, `PRIV-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE,BI.RECRUITING,BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_MATCH_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MATCH_005`; `PROPERTY=TestTodo_MATCH_005_Property`.
  - **RED:** Prohibited attribute/proxy, cohort too small or fairness policy breach returns blocked/review result.
  - **GREEN:** Engine never auto-accuses or silently changes ranking.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [DLP](specs/data-classification-and-dlp.md), [agent/intelligence models](data/models/assurance-intelligence-platform.md).

- [ ] `MATCH-006` **[PHASE_3][SOL_HIGH] Explain inclusion, ranking and exclusion safely.**
  - **Depends:** `MATCH-003`–`MATCH-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE,BI.RECRUITING,BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_MATCH_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MATCH_006`; `PROPERTY=TestTodo_MATCH_006_Property`; `MUTATION=TestTodo_MATCH_006_Mutation`.
  - **RED:** Seeded defect: a protected attribute changes rank or a hard constraint lacks explanation for MATCH-006; the primary test must return MATCH_006_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Explanation covers candidate source, hard constraints, score components, tie/fairness rules and unknowns while preventing comparison leakage.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Provenance](specs/provenance-graph-and-lineage.md), [AuthZ](specs/organization-scope-and-authz.md).

- [ ] `MATCH-007` **[CONFORMANCE][SOL_HIGH] Prove matching across Scheduling, Recruiting, Mobility and Learning.**
  - **Depends:** `MATCH-001`–`MATCH-006`, `CONF-012`, `CONF-013`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.WORKFORCE,BI.RECRUITING,BI.TALENT; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_MATCH_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_MATCH_007`; `PROPERTY=TestTodo_MATCH_007_Property`; `CONFORMANCE=TestTodo_MATCH_007_Conformance`; `MUTATION=TestTodo_MATCH_007_Mutation`.
  - **RED:** Seeded defect: a protected attribute changes rank or a hard constraint lacks explanation for MATCH-007; the primary test must return MATCH_007_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Shared candidate/filter/rank/explain contracts pass four domains without turning recommendations into transactions.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Intent coverage](data/models/intent-coverage-matrix.md), [future conformance](#21-future-domain-intent-conformance).

- [x] `SCENARIO-001` **[PHASE_2][SOL_HIGH] Define immutable ScenarioRevision and assumptions.**
  - **Evidence (2026-09-05):** `TestTodo_SCENARIO_001` in `internal/domains/scenario` (immutable ScenarioRevision with typed assumptions carrying units and provenance refs, baseline snapshot ref, author and digest, forks producing a new revision with lineage to the parent, assumptions without provenance refused, and Explain; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/scenario/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MSRC-006`, `MODEL-017`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ANALYTICS,BI.INTELLIGENCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SCENARIO_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SCENARIO_001`; `PROPERTY=TestTodo_SCENARIO_001_Property`; `GOLDEN=TestTodo_SCENARIO_001_Golden`; `MUTATION=TestTodo_SCENARIO_001_Mutation`.
  - **RED:** Scenario missing baseline snapshot, owner/scope/horizon, typed assumptions/deltas, authority disclaimer or lifecycle fails.
  - **GREEN:** Revisions never mutate authoritative facts.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Assurance/intelligence models](data/models/assurance-intelligence-platform.md), [BusinessIntent](specs/business-intent-and-change-request.md).

- [ ] `SCENARIO-002` **[PHASE_2][SOL_HIGH] Apply hypothetical domain changes in an isolated state model.**
  - **Depends:** `SCENARIO-001`, `TX-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ANALYTICS,BI.INTELLIGENCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SCENARIO_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SCENARIO_002`; `PROPERTY=TestTodo_SCENARIO_002_Property`; `MUTATION=TestTodo_SCENARIO_002_Mutation`.
  - **RED:** Scenario capability cannot dispatch effects/write truth or read unapproved fields.
  - **GREEN:** Typed deltas evaluate against pinned baseline and return explicit unknowns.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Transaction coordinator](specs/transaction-plan-and-commit-coordinator.md), [DLP](specs/data-classification-and-dlp.md).

- [ ] `SCENARIO-003` **[PHASE_2][SOL_HIGH] Calculate workforce, headcount and cost impact.**
  - **Depends:** `SCENARIO-002`, `MODEL-003`, `POP-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ANALYTICS,BI.INTELLIGENCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SCENARIO_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SCENARIO_003`; `PROPERTY=TestTodo_SCENARIO_003_Property`.
  - **RED:** Output binds formula/rates/population/time and exact assumptions.
  - **GREEN:** Missing cost/rule source produces ranges/unknown, never fabricated precision.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Budget authority](specs/workforce-budget-authority.md), [people models](data/models/people-workforce.md).

- [ ] `SCENARIO-004` **[PHASE_2][SOL_HIGH] Compare scenarios and explain assumptions.**
  - **Depends:** `SCENARIO-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ANALYTICS,BI.INTELLIGENCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SCENARIO_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SCENARIO_004`; `PROPERTY=TestTodo_SCENARIO_004_Property`; `RACE=TestTodo_SCENARIO_004_Race`.
  - **RED:** Comparison normalizes baseline/horizon/units, partitions differences and traces each metric to assumptions.
  - **GREEN:** Incomparable versions return typed error.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Assurance models](data/models/assurance-intelligence-platform.md), [provenance](specs/provenance-graph-and-lineage.md).

- [ ] `SCENARIO-005` **[PHASE_3][SOL_HIGH] Approve a selected scenario into an immutable Plan.**
  - **Depends:** `SCENARIO-004`, `APPROVAL-002`, `TRUST-014`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ANALYTICS,BI.INTELLIGENCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SCENARIO_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SCENARIO_005`; `PROPERTY=TestTodo_SCENARIO_005_Property`; `MUTATION=TestTodo_SCENARIO_005_Mutation`.
  - **RED:** Changed scenario/baseline, stale approver or self-approval fails.
  - **GREEN:** Plan binds exact revision, assumptions, scope, decision and validity.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Human work](specs/human-work-forms-and-rules.md), [governance composition](specs/governance-decision-and-obligation-composition.md).

- [ ] `SCENARIO-006` **[PHASE_3][SOL_HIGH] Compile approved Plan deltas into BusinessIntents.**
  - **Depends:** `SCENARIO-005`, `INTENT-001`, `TX-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ANALYTICS,BI.INTELLIGENCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SCENARIO_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SCENARIO_006`; `PROPERTY=TestTodo_SCENARIO_006_Property`.
  - **RED:** Compiler cannot bypass governance or combine conflicting deltas.
  - **GREEN:** Outputs bounded ordered intents with proposal/write sets, dependencies and simulation links.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [BusinessIntent](specs/business-intent-and-change-request.md), [transaction coordinator](specs/transaction-plan-and-commit-coordinator.md).

- [ ] `SCENARIO-007` **[PHASE_3][SOL_HIGH] Reconcile executed Plan against approved scenario.**
  - **Depends:** `SCENARIO-006`, `TX-009`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ANALYTICS,BI.INTELLIGENCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SCENARIO_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SCENARIO_007`; `PROPERTY=TestTodo_SCENARIO_007_Property`; `GOLDEN=TestTodo_SCENARIO_007_Golden`.
  - **RED:** Completion reports executed/deferred/rejected/failed/unknown deltas and actual outcome/cost variance.
  - **GREEN:** It never rewrites scenario assumptions.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Reconciliation](specs/transaction-ledger-reconciliation-and-repair.md), [outcome models](data/models/assurance-intelligence-platform.md).

## 40. Attestation and conformance-gated program engines

> **Disposition (2026-09-02):** DEFERRED. `ATTEST-*` items labelled `GATE_B` depended on the retired form engine and are not P1B.

- [x] `ATTEST-001` **[GATE_B][SOL_HIGH] Define versioned AttestationStatement.**
  - **Evidence (2026-09-05):** `TestTodo_ATTEST_001`, `TestTodo_ATTEST_001_{Mutation,Golden,Property,Security}` in `internal/domains/attestation` (versioned AttestationStatement over a text digest with identity-assurance-bound attester, validity window, jurisdiction, revocation link, Signer port); `go test -count=1 ./internal/domains/attestation/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-017`, `FORM-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE,BI.WORK,BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ATTEST_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ATTEST_001`; `MUTATION=TestTodo_ATTEST_001_Mutation`.
  - **RED:** Seeded defect: statement or evidence binding changes or tampered package verifies for ATTEST-001; the primary test must return ATTEST_001_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Statement missing exact text/locale, subject/facts/period, purpose, required assurance, response options, evidence and correction/refusal policy fails publication.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Human work/forms](specs/human-work-forms-and-rules.md), [connectivity models](data/models/connectivity-access-content.md).

- [x] `ATTEST-002` **[GATE_B][SOL_HIGH] Bind attestation to exact evidence and context.**
  - **Evidence (2026-09-05):** `TestTodo_ATTEST_002`, `TestTodo_ATTEST_002_{Golden,Mutation,Security}` in `internal/domains/attestation` (Binding pins evidence digests and a context digest over subject-as-of, jurisdiction, locale and rendered text; Verify names the drifted component via VerifyDriftReason across nine drift scenarios; rebinding yields a new statement version and digest, never a mutation); `go test -count=1 ./internal/domains/balance/ ./internal/domains/attestation/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ATTEST-001`, `MODEL-020`, `MODEL-007`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE,BI.WORK,BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ATTEST_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ATTEST_002`; `GOLDEN=TestTodo_ATTEST_002_Golden`.
  - **RED:** Changed statement/facts/attachments/time context invalidates binding.
  - **GREEN:** Immutable request digest includes every presented artifact and relevant version.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Canonical digest](specs/canonical-envelope-and-digest.md), [provenance](specs/provenance-graph-and-lineage.md).

- [x] `ATTEST-003` **[GATE_B][SOL_HIGH] Verify attestor identity, authority and assurance.**
  - **Evidence (2026-09-05):** `TestTodo_ATTEST_003` in `internal/trust/attest` (Decision records proof and current AuthZ; written by an earlier lane (Luna or Sonnet) and verified independently); `go test -count=1 ./internal/trust/attest/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ATTEST-002`, `AUTHN-005`, `TRUST-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE,BI.WORK,BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ATTEST_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ATTEST_003`; `SECURITY=TestTodo_ATTEST_003_Security`; `MUTATION=TestTodo_ATTEST_003_Mutation`.
  - **RED:** Proxy/delegated/revoked/expired/insufficient-assurance response fails unless statement explicitly permits that mode.
  - **GREEN:** Decision records proof and current AuthZ.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [AuthZ](specs/organization-scope-and-authz.md), [human work](specs/human-work-forms-and-rules.md).

- [x] `ATTEST-004` **[GATE_B][SOL_HIGH] Record response at trusted time exactly once.**
  - **Evidence (2026-09-05):** `TestTodo_ATTEST_004` in `internal/data/attestationstore`, `internal/trust/attest` (Append-only accepted/refused/unknown response binds trusted time and evidence receipt; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/attestationstore/ ./internal/trust/attest/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ATTEST-003`, `TIME-001`, `LEDGER-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE,BI.WORK,BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ATTEST_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ATTEST_004`; `FUZZ=FuzzTodo_ATTEST_004`; `RACE=TestTodo_ATTEST_004_Race`; `RECOVERY=TestTodo_ATTEST_004_Recovery`; `MUTATION=TestTodo_ATTEST_004_Mutation`.
  - **RED:** Changed idempotency replay, untrusted clock or mutable response fails.
  - **GREEN:** Append-only accepted/refused/unknown response binds trusted time and evidence receipt.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Ledger](specs/transaction-ledger-reconciliation-and-repair.md), [wire time](data/models/wire-contract-primitives.md).

- [x] `ATTEST-005` **[GATE_B][SOL_HIGH] Preserve refusal, unknown, correction and revocation.**
  - **Evidence (2026-09-05):** `TestTodo_ATTEST_005` in `internal/trust/attest` (Corrective/revocation assertion links reason, authority and affected obligations; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/trust/attest/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ATTEST-004`, `LEDGER-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE,BI.WORK,BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ATTEST_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ATTEST_005`; `MUTATION=TestTodo_ATTEST_005_Mutation`.
  - **RED:** Refusal/unknown cannot be coerced to agreement and prior response is never overwritten.
  - **GREEN:** Corrective/revocation assertion links reason, authority and affected obligations.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Ledger corrections](specs/transaction-ledger-reconciliation-and-repair.md), [model conventions](data/models/modeling-conventions.md).

- [x] `ATTEST-006` **[GATE_B][SOL_HIGH] Enforce required attestation before dependent effects.**
  - **Evidence (2026-09-05):** `TestTodo_ATTEST_006` in `internal/trust/attest` (Valid response is revalidated at execution and linked to transaction; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/trust/attest/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ATTEST-004`, `WF-COMP-003`, `TX-008`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE,BI.WORK,BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ATTEST_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ATTEST_006`; `MUTATION=TestTodo_ATTEST_006_Mutation`.
  - **RED:** Missing/stale/wrong-statement response blocks effect with exact obligation.
  - **GREEN:** Valid response is revalidated at execution and linked to transaction.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Workflow runtime](specs/workflow-runtime.md), [governance composition](specs/governance-decision-and-obligation-composition.md).

- [x] `ATTEST-007` **[GATE_B][SOL_HIGH] Export and verify attestation evidence packages.**
  - **Evidence (2026-09-05):** `TestTodo_ATTEST_007` in `internal/trust/attest` (Package missing statement, presentation, subject, proof, response, time, corrections or chain fails offline verification and applies authorization/redaction; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/trust/attest/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ATTEST-002`–`ATTEST-006`, `LEDGER-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE,BI.WORK,BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ATTEST_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ATTEST_007`; `SECURITY=TestTodo_ATTEST_007_Security`; `MUTATION=TestTodo_ATTEST_007_Mutation`.
  - **RED:** Seeded defect: statement or evidence binding changes or tampered package verifies for ATTEST-007; the primary test must return ATTEST_007_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Package missing statement, presentation, subject, proof, response, time, corrections or chain fails offline verification and applies authorization/redaction.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Audit evidence](specs/platform-responsibility-boundaries.md), [canonical digest](specs/canonical-envelope-and-digest.md).

- [x] `ATTEST-008` **[CONFORMANCE][SOL_HIGH] Prove attestation across Time, Payroll and Legal acknowledgements.**
  - **Evidence (2026-09-05):** `TestTodo_ATTEST_008` in `internal/trust/attest` (Timecard accuracy, meal-break, payroll-input completeness and factual acknowledgement share evidence semantics while remaining distinct from approval/signature/form submission; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/trust/attest/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ATTEST-001`–`ATTEST-007`, `CONF-009`, `CONF-011`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.WORKFORCE,BI.WORK,BI.REGULATORY; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_ATTEST_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ATTEST_008`; `SECURITY=TestTodo_ATTEST_008_Security`; `CONFORMANCE=TestTodo_ATTEST_008_Conformance`; `MUTATION=TestTodo_ATTEST_008_Mutation`.
  - **RED:** Seeded defect: statement or evidence binding changes or tampered package verifies for ATTEST-008; the primary test must return ATTEST_008_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Timecard accuracy, meal-break, payroll-input completeness and factual acknowledgement share evidence semantics while remaining distinct from approval/signature/form submission.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Future conformance](#21-future-domain-intent-conformance), [human work/forms](specs/human-work-forms-and-rules.md).

- [ ] `PROGRAM-CONF-001` **[CONFORMANCE][SOL_HIGH] Prove a shared Program abstraction across four domains.**
  - **Depends:** `POP-005`, `ELIG-008`, `CYCLE-005`, `BAL-010`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.REWARDS; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_PROGRAM_CONF_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PROGRAM_CONF_001`; `PROPERTY=TestTodo_PROGRAM_CONF_001_Property`; `GOLDEN=TestTodo_PROGRAM_CONF_001_Golden`; `CONFORMANCE=TestTodo_PROGRAM_CONF_001_Conformance`.
  - **RED:** Benefit, Bonus, Learning and Leave fixtures must share definition/revision/population/eligibility/cycle/participation/outcome semantics.
  - **GREEN:** Any forced/meaningless field rejects abstraction.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Intent coverage](data/models/intent-coverage-matrix.md), [adversarial audit](specs/adversarial-audit-32-reviewers-2026-08-14.md).

- [ ] `PROGRAM-001` **[PHASE_3][SOL_HIGH] Define ProgramDefinition only after conformance passes.**
  - **Depends:** `PROGRAM-CONF-001`, `MODEL-017`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PROGRAM_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PROGRAM_001`; `PROPERTY=TestTodo_PROGRAM_001_Property`; `GOLDEN=TestTodo_PROGRAM_001_Golden`; `CONFORMANCE=TestTodo_PROGRAM_001_Conformance`.
  - **RED:** Implementation remains blocked until signed conformance proves shared invariants and named variation points.
  - **GREEN:** Definition then records type, owner, scope, funding, outcomes and extensions.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Model conventions](data/models/modeling-conventions.md), [registry contracts](data/models/registry-and-coverage-contracts.md).

- [ ] `PROGRAM-002` **[PHASE_3][SOL_HIGH] Resolve effective ProgramRevision.**
  - **Depends:** `PROGRAM-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PROGRAM_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PROGRAM_002`; `FAULT=TestTodo_PROGRAM_002_Fault`; `SECURITY=TestTodo_PROGRAM_002_Security`; `MUTATION=TestTodo_PROGRAM_002_Mutation`.
  - **RED:** Overlapping/ambiguous revisions or ambient latest fails.
  - **GREEN:** Resolver returns exact version for tenant/org/jurisdiction/effective-known context.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Wire time](data/models/wire-contract-primitives.md), [configuration models](data/models/dataops-configuration.md).

- [ ] `PROGRAM-003` **[PHASE_3][SOL_HIGH] Bind program population, eligibility and cycle.**
  - **Depends:** `PROGRAM-002`, `POP-005`, `ELIG-007`, `CYCLE-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PROGRAM_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PROGRAM_003`; `PROPERTY=TestTodo_PROGRAM_003_Property`; `GOLDEN=TestTodo_PROGRAM_003_Golden`.
  - **RED:** Mismatched snapshot/time/version or partial population cannot silently enroll.
  - **GREEN:** Binding exposes explicit completeness and late-entry policy.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Population engine](#37-shared-transformation-and-population-engines), [eligibility/cycles](#38-eligibility-business-cycle-and-balance-engines).

- [ ] `PROGRAM-004` **[PHASE_3][SOL_HIGH] Govern participate, enroll and withdraw lifecycle.**
  - **Depends:** `PROGRAM-003`, `TX-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PROGRAM_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PROGRAM_004`; `PROPERTY=TestTodo_PROGRAM_004_Property`; `SECURITY=TestTodo_PROGRAM_004_Security`; `MUTATION=TestTodo_PROGRAM_004_Mutation`.
  - **RED:** Illegal/stale/duplicate transition or withdrawal contrary to obligations fails.
  - **GREEN:** Transaction records eligibility, elections, dates, reason and downstream effects.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Transaction coordinator](specs/transaction-plan-and-commit-coordinator.md), [lifecycle registry](data/models/registry-and-coverage-contracts.md).

- [ ] `PROGRAM-005` **[PHASE_3][SOL_HIGH] Calculate and explain program outcome.**
  - **Depends:** `PROGRAM-003`, `RULE-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PROGRAM_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PROGRAM_005`; `PROPERTY=TestTodo_PROGRAM_005_Property`; `GOLDEN=TestTodo_PROGRAM_005_Golden`.
  - **RED:** Output binds participant/snapshot/rules/funding/inputs/time and returns value/status/obligations/unknowns with safe explanation.
  - **GREEN:** No domain-specific formula hides in engine core.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Rules models](data/models/rules-and-decisions.md), [provenance](specs/provenance-graph-and-lineage.md).

- [ ] `PROGRAM-006` **[PHASE_3][SOL_HIGH] Reconcile program participation and outcomes.**
  - **Depends:** `PROGRAM-004`, `PROGRAM-005`, `TX-009`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PROGRAM_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PROGRAM_006`; `PROPERTY=TestTodo_PROGRAM_006_Property`; `MUTATION=TestTodo_PROGRAM_006_Mutation`.
  - **RED:** Seeded defect: stale participation or outcome is reported reconciled for PROGRAM-006; the primary test must return PROGRAM_006_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Expected/actual enrollment, balance/effect and external observation discrepancies remain explicit and create domain-owned repair plans.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Reconciliation](specs/transaction-ledger-reconciliation-and-repair.md), [integration platform](specs/integration-platform.md).

## 41. Event subscription, partner application and custom-object engines

> **Disposition (2026-09-02):** DEFERRED.

- [x] `SUB-001` **[PHASE_2][SOL_HIGH] Define EventSubscription and revision lifecycle.**
  - **Evidence (2026-09-05):** `TestTodo_SUB_001` in `internal/domains/subscription` (EventSubscription with subscriber, closed event kinds, a filter over declared fields only, endpoint ref, guarantee class and tenant scope, an immutable DRAFT/ACTIVE/PAUSED/REVOKED revision lifecycle, activation requiring a distinct approver, digested revisions and a matcher deciding which active subscriptions an event digest matches without raw payloads; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/subscription/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PROTO-008`, `INTG-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SUB_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SUB_001`; `GOLDEN=TestTodo_SUB_001_Golden`; `RACE=TestTodo_SUB_001_Race`; `INTEGRATION=TestTodo_SUB_001_Integration`; `FAULT=TestTodo_SUB_001_Fault`.
  - **RED:** Subscription missing subscriber identity, event/filter/org scope, purpose, destination, schema/order/retry/DLQ/signature/retention policy fails.
  - **GREEN:** Versions are immutable.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Integration platform](specs/integration-platform.md), [connectivity models](data/models/connectivity-access-content.md).

- [x] `SUB-002` **[PHASE_2][SOL_HIGH] Authorize event, resource and filter scope.**
  - **Evidence (2026-09-05):** `TestTodo_SUB_002` in `internal/domains/subscription` (event, resource and filter scope authorization for subscriptions; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/subscription/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SUB-001`, `TRUST-009`, `TRUST-010`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SUB_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SUB_002`; `GOLDEN=TestTodo_SUB_002_Golden`; `RACE=TestTodo_SUB_002_Race`; `INTEGRATION=TestTodo_SUB_002_Integration`; `FAULT=TestTodo_SUB_002_Fault`.
  - **RED:** Wildcard/field/filter/count inference or changed entitlement fails at create and dispatch.
  - **GREEN:** Access manifest records permitted schema fields/population/purpose.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [AuthZ](specs/organization-scope-and-authz.md), [DLP](specs/data-classification-and-dlp.md).

- [x] `SUB-003` **[PHASE_2][SOL_HIGH] Freeze outbound event schema and canonical envelope.**
  - **Evidence (2026-09-05):** `TestTodo_SUB_003` in `internal/domains/subscription` (frozen outbound event schema and canonical envelope, fuzzed; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/subscription/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SUB-001`, `PROTO-008`, `MODEL-007`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SUB_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SUB_003`; `GOLDEN=TestTodo_SUB_003_Golden`; `FUZZ=FuzzTodo_SUB_003`; `RACE=TestTodo_SUB_003_Race`; `INTEGRATION=TestTodo_SUB_003_Integration`; `FAULT=TestTodo_SUB_003_Fault`; `SECURITY=TestTodo_SUB_003_Security`.
  - **RED:** Ambient schema/latest projection or missing event/idempotency/time/tenant/version digest fails.
  - **GREEN:** Envelope is canonical, signed and minimally disclosed.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Canonical digest](specs/canonical-envelope-and-digest.md), [API lifecycle](specs/platform-responsibility-boundaries.md).

- [x] `SUB-004` **[PHASE_2][SOL_HIGH] Deliver outbound events idempotently with declared ordering.**
  - **Evidence (2026-09-05):** `TestTodo_SUB_004` in `internal/domains/subscription` (Per-subscription operation journal records ordering key, attempts and provider receipt; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/subscription/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SUB-002`, `SUB-003`, `EVENT-003`, `EDGE-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SUB_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SUB_004`; `RACE=TestTodo_SUB_004_Race`; `INTEGRATION=TestTodo_SUB_004_Integration`; `FAULT=TestTodo_SUB_004_Fault`.
  - **RED:** Crash/retry/reorder/timeout cannot create undetected duplicate/gap.
  - **GREEN:** Per-subscription operation journal records ordering key, attempts and provider receipt.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Integration event runtime](specs/integration-platform.md), [event substrate](#31-infrastructure-as-code-and-physical-data-services).

- [ ] `SUB-005` **[PHASE_2][SOL_HIGH] Retry, dead-letter, pause and resume subscriptions.**
  - **Depends:** `SUB-004`, `OPS-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SUB_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SUB_005`; `RACE=TestTodo_SUB_005_Race`; `INTEGRATION=TestTodo_SUB_005_Integration`; `FAULT=TestTodo_SUB_005_Fault`; `SECURITY=TestTodo_SUB_005_Security`.
  - **RED:** Poison endpoint never retries forever or blocks other tenants.
  - **GREEN:** Pause/resume is fenced and DLQ has owner, expiry, reason and repair route.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Integration platform](specs/integration-platform.md), [incident management](specs/incident-management.md).

- [ ] `SUB-006` **[PHASE_2][SOL_HIGH] Replay existing events without inventing business history.**
  - **Depends:** `SUB-004`, `LEDGER-006`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SUB_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SUB_006`; `GOLDEN=TestTodo_SUB_006_Golden`; `RACE=TestTodo_SUB_006_Race`; `INTEGRATION=TestTodo_SUB_006_Integration`; `FAULT=TestTodo_SUB_006_Fault`; `SECURITY=TestTodo_SUB_006_Security`; `RECOVERY=TestTodo_SUB_006_Recovery`; `MUTATION=TestTodo_SUB_006_Mutation`.
  - **RED:** Seeded defect: replay invents a new event identity or business history for SUB-006; the primary test must return SUB_006_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Replay uses original event ID/schema/payload digest plus new delivery attempt/epoch, rechecks current subscription AuthZ and never appends a new domain event.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Ledger](specs/transaction-ledger-reconciliation-and-repair.md), [integration platform](specs/integration-platform.md).

- [x] `SUB-007` **[PHASE_2][SOL_HIGH] Rotate destination credentials and signatures.**
  - **Evidence (2026-09-05):** `TestTodo_SUB_007` in `internal/domains/subscription` (Overlap rotation preserves delivery and recipients can verify exact signing profile/version; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/subscription/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SUB-004`, `TRUST-029`, `TRUST-030`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SUB_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SUB_007`; `RACE=TestTodo_SUB_007_Race`; `INTEGRATION=TestTodo_SUB_007_Integration`; `FAULT=TestTodo_SUB_007_Fault`; `SECURITY=TestTodo_SUB_007_Security`.
  - **RED:** Expired/revoked/wrong-destination key fails.
  - **GREEN:** Overlap rotation preserves delivery and recipients can verify exact signing profile/version.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Credential leases](specs/secrets-key-custody-and-credential-leases.md), [security models](data/models/security-trust.md).

- [ ] `SUB-008` **[PHASE_2][SOL_HIGH] Reconcile subscriber delivery completeness.**
  - **Depends:** `SUB-004`–`SUB-007`, `TX-009`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.INTEGRATION; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SUB_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SUB_008`; `RACE=TestTodo_SUB_008_Race`; `INTEGRATION=TestTodo_SUB_008_Integration`; `FAULT=TestTodo_SUB_008_Fault`.
  - **RED:** Provider acceptance alone never satisfies requirement.
  - **GREEN:** Expected event range versus attempts/acks/gaps/unknowns produces repair or verified completion.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Reconciliation](specs/transaction-ledger-reconciliation-and-repair.md), [integration observations](specs/integration-platform.md).

- [x] `APP-001` **[PHASE_3][SOL_HIGH] Register immutable PartnerApplication and versions.**
  - **Evidence (2026-09-05):** `TestTodo_APP_001` in `internal/domains/partnerapp` (immutable PartnerApplication versions declaring capabilities and data classes by ref with endpoints by digest, a distinct-approver review before ACTIVE, deprecation naming a successor, revocation as a digested event listing invalidated subscriptions and leases, and a check refusing capabilities or data classes the partner agreement does not cover; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/partnerapp/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CP-001`, `PROTO-008`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose governed intent creation, inspection or consumption without persistence or provider bypass`.
  - **TEST:** `TestTodo_APP_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_APP_001`; `SECURITY=TestTodo_APP_001_Security`; `CONFORMANCE=TestTodo_APP_001_Conformance`; `MUTATION=TestTodo_APP_001_Mutation`.
  - **RED:** Seeded defect: an unreviewed mutable partner application version publishes for APP-001; the primary test must return APP_001_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** App/version missing publisher, software identity, requested capabilities/data, callbacks, regions, SBOM/provenance, support/exit or compatibility fails publication.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Capability lifecycle](specs/capability-registry-and-lifecycle.md), [operations models](data/models/operations-production.md).

- [x] `APP-002` **[PHASE_3][SOL_HIGH] Perform application security and data-processing review.**
  - **Evidence (2026-09-05):** `TestTodo_APP_002` in `internal/domains/partnerapp` (application security and data-processing review record gating partner apps; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/partnerapp/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `APP-001`, `TRUST-018`, `SUPPLY-001`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose governed intent creation, inspection or consumption without persistence or provider bypass`.
  - **TEST:** `TestTodo_APP_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_APP_002`; `SECURITY=TestTodo_APP_002_Security`.
  - **RED:** Unreviewed scopes/destinations/processors/residency/retention/threats/vulnerabilities cannot install.
  - **GREEN:** Signed review has controls, owner and expiry.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [DLP](specs/data-classification-and-dlp.md), [security models](data/models/security-trust.md).

- [x] `APP-003` **[PHASE_3][SOL_HIGH] Install application with tenant/org/data scopes.**
  - **Evidence (2026-09-05):** `TestTodo_APP_003` in `internal/domains/partnerapp` (Installation binds exact version, tenant/org/population/fields/purpose/capabilities and approver evidence; written by an earlier lane (Luna or Sonnet) and verified independently); `go test -count=1 ./internal/domains/partnerapp/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `APP-002`, `TRUST-008`, `TRUST-010`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose governed intent creation, inspection or consumption without persistence or provider bypass`.
  - **TEST:** `TestTodo_APP_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_APP_003`; `SECURITY=TestTodo_APP_003_Security`.
  - **RED:** Requested grant never implies approved grant.
  - **GREEN:** Installation binds exact version, tenant/org/population/fields/purpose/capabilities and approver evidence.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [AuthZ](specs/organization-scope-and-authz.md), [tenant lifecycle](specs/platform-responsibility-boundaries.md).

- [x] `APP-004` **[PHASE_3][SOL_HIGH] Issue and rotate application workload identity.**
  - **Evidence (2026-09-05):** `TestTodo_APP_004` in `internal/domains/partnerapp` (Credentials remain reference-only and destination/sender constrained; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/partnerapp/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `APP-003`, `AUTHN-006`, `TRUST-029`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose governed intent creation, inspection or consumption without persistence or provider bypass`.
  - **TEST:** `TestTodo_APP_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_APP_004`; `SECURITY=TestTodo_APP_004_Security`.
  - **RED:** Client identity cannot exceed installation grant or survive revoke/expiry/version change.
  - **GREEN:** Credentials remain reference-only and destination/sender constrained.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Credential leases](specs/secrets-key-custody-and-credential-leases.md), [security models](data/models/security-trust.md).

- [x] `APP-005` **[PHASE_3][SOL_HIGH] Upgrade, revoke and quarantine installations.**
  - **Evidence (2026-09-05):** `TestTodo_APP_005` in `internal/connectivity/application` (Revoke/quarantine stops tokens/subscriptions/effects within SLO while preserving evidence and bounded recovery path; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/connectivity/application/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `APP-003`, `CP-007`, `CP-008`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose governed intent creation, inspection or consumption without persistence or provider bypass`.
  - **TEST:** `TestTodo_APP_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_APP_005`; `FAULT=TestTodo_APP_005_Fault`; `SECURITY=TestTodo_APP_005_Security`; `MUTATION=TestTodo_APP_005_Mutation`.
  - **RED:** Incompatible/new scopes cannot auto-upgrade.
  - **GREEN:** Revoke/quarantine stops tokens/subscriptions/effects within SLO while preserving evidence and bounded recovery path.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Control plane](#30-control-plane-publication-distribution-and-activation), [incident management](specs/incident-management.md).

- [ ] `APP-006` **[PHASE_3][SOL_HIGH] Certify marketplace applications.**
  - **Depends:** `APP-001`–`APP-005`, `CONN-RT-008`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose governed intent creation, inspection or consumption without persistence or provider bypass`.
  - **TEST:** `TestTodo_APP_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_APP_006`; `FAULT=TestTodo_APP_006_Fault`; `SECURITY=TestTodo_APP_006_Security`; `CONFORMANCE=TestTodo_APP_006_Conformance`.
  - **RED:** Certification requires repeatable security, scope, compatibility, tenancy, scale, outage, revocation, upgrade and exit evidence.
  - **GREEN:** Missing/expired evidence removes certified status.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Integration maturity](specs/integration-platform.md), [operations evidence](data/models/operations-production.md).

- [x] `CUSTOM-001` **[PHASE_4][SOL_HIGH] Define and compile CustomObjectType schemas.**
  - **Depends:** `MSRC-002`, `MODEL-017`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS,BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CUSTOM_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CUSTOM_001`; `GOLDEN=TestTodo_CUSTOM_001_Golden`.
  - **RED:** Arbitrary code/reserved names/unbounded fields or unsupported primitive fails.
  - **GREEN:** Typed schema, owner, namespace, limits, version and digest compile via SchemaFlux.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Model conventions](data/models/modeling-conventions.md), [Go technology constitution](specs/go-only-technology-constitution.md).
  - **Evidence (2026-09-03):** `TestTodo_CUSTOM_001`, Declared `CUSTOM-001` test matrix and acceptance behavior in `internal/customobject`; PASS; `go test -p 1 ./internal/customobject` and `go vet ./internal/customobject` on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `CUSTOM-002` **[PHASE_4][SOL_HIGH] Define typed custom relationships and effective dating.**
  - **Evidence (2026-09-05):** `TestTodo_CUSTOM_002`, `TestTodo_CUSTOM_002_{Property,Golden,Fault,Mutation}` in `internal/domains/custom` (typed relationship definitions with endpoints, cardinality, effective-dating rule and cycle control; canonical digests); `go test -count=1 ./internal/domains/custom/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CUSTOM-001`, `MODEL-013`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS,BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CUSTOM_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CUSTOM_002`; `PROPERTY=TestTodo_CUSTOM_002_Property`; `GOLDEN=TestTodo_CUSTOM_002_Golden`; `FAULT=TestTodo_CUSTOM_002_Fault`; `MUTATION=TestTodo_CUSTOM_002_Mutation`.
  - **RED:** Dangling/cyclic/invalid cardinality or ambiguous interval fails.
  - **GREEN:** Relationship definition declares endpoints, scope, inverse, temporal and lifecycle semantics.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Relationship map](data/models/relationship-map.md), [wire time](data/models/wire-contract-primitives.md).

- [x] `CUSTOM-003` **[PHASE_4][SOL_HIGH] Apply AuthZ, classification, residency and retention.**
  - **Evidence (2026-09-05):** `TestTodo_CUSTOM_003`, `TestTodo_CUSTOM_003_{Fault,Security,PolicyProjection}` in `internal/domains/custom` (per-field AuthZ domain, classification, residency and retention required; PolicyProjection refuses an unclassified field; custom data never defaults public); `go test -count=1 ./internal/domains/custom/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CUSTOM-001`, `TRUST-010`, `MODEL-023`, `MODEL-026`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS,BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CUSTOM_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CUSTOM_003`; `FAULT=TestTodo_CUSTOM_003_Fault`; `SECURITY=TestTodo_CUSTOM_003_Security`.
  - **RED:** Custom data never defaults public/unclassified/unretained or bypasses field/purpose controls.
  - **GREEN:** Publication fails without complete policies.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [DLP](specs/data-classification-and-dlp.md), [records](specs/records-management-and-disposition.md).

- [x] `CUSTOM-004` **[PHASE_4][SOL_HIGH] Append canonical custom-object events and projections.**
  - **Evidence (2026-09-05):** `TestTodo_CUSTOM_004` in `internal/domains/custom` (Ledger/projection/outbox commit atomically and rebuild identically; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/custom/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CUSTOM-002`, `CUSTOM-003`, `LEDGER-008`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS,BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CUSTOM_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CUSTOM_004`; `GOLDEN=TestTodo_CUSTOM_004_Golden`; `RECOVERY=TestTodo_CUSTOM_004_Recovery`; `MUTATION=TestTodo_CUSTOM_004_Mutation`.
  - **RED:** Create/change/correct/retire obey expected version, schema and authority.
  - **GREEN:** Ledger/projection/outbox commit atomically and rebuild identically.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Ledger](specs/transaction-ledger-reconciliation-and-repair.md), [transaction coordinator](specs/transaction-plan-and-commit-coordinator.md).

- [x] `CUSTOM-005` **[PHASE_4][SOL_HIGH] Generate governed custom-object capabilities and clients.**
  - **Evidence (2026-09-05):** `TestTodo_CUSTOM_005` in `internal/domains/custom` (Generated create/read/change/correct/retire capabilities declare AuthZ, purpose, side effects, schemas and evidence with grpcbridge parity; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/custom/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CUSTOM-004`, `PROTO-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS,BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CUSTOM_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CUSTOM_005`; `GOLDEN=TestTodo_CUSTOM_005_Golden`; `INTEGRATION=TestTodo_CUSTOM_005_Integration`; `SECURITY=TestTodo_CUSTOM_005_Security`; `CONFORMANCE=TestTodo_CUSTOM_005_Conformance`; `MUTATION=TestTodo_CUSTOM_005_Mutation`.
  - **RED:** No generic unrestricted CRUD.
  - **GREEN:** Generated create/read/change/correct/retire capabilities declare AuthZ, purpose, side effects, schemas and evidence with grpcbridge parity.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Capability lifecycle](specs/capability-registry-and-lifecycle.md), [Go technology constitution](specs/go-only-technology-constitution.md).

- [x] `CUSTOM-006` **[PHASE_4][SOL_HIGH] Search and report custom objects safely.**
  - **Evidence (2026-09-05):** `TestTodo_CUSTOM_006` in `internal/domains/custom` (Rebuild returns exact authorized semantic result; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/custom/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CUSTOM-003`, `CUSTOM-004`, `RETRIEVAL-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS,BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CUSTOM_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CUSTOM_006`; `GOLDEN=TestTodo_CUSTOM_006_Golden`; `RECOVERY=TestTodo_CUSTOM_006_Recovery`.
  - **RED:** Search/report cannot infer restricted objects/fields/counts and exposes freshness/schema version.
  - **GREEN:** Rebuild returns exact authorized semantic result.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [DLP](specs/data-classification-and-dlp.md), [intelligence plane](specs/platform-plane-model.md).

- [ ] `CUSTOM-007` **[PHASE_4][SOL_HIGH] Version, migrate and retire custom-object types.**
  - **Depends:** `CUSTOM-001`, `CUSTOM-004`, `DB-021`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS,BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CUSTOM_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CUSTOM_007`; `INTEGRATION=TestTodo_CUSTOM_007_Integration`; `FAULT=TestTodo_CUSTOM_007_Fault`; `RECOVERY=TestTodo_CUSTOM_007_Recovery`; `MUTATION=TestTodo_CUSTOM_007_Mutation`.
  - **RED:** Breaking migration cannot orphan history/live references or remove held data.
  - **GREEN:** Expand/backfill/shadow/cutover/retire has impact, rollback and adoption evidence.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Schema lifecycle](data/models/dataops-configuration.md), [records](specs/records-management-and-disposition.md).

## 42. Confidential actors, abuse detection and progressive rollout

> **Disposition (2026-09-02):** DEFERRED; items labelled `GATE_B`/`GATE_C` here are Gate C at the earliest.

- [x] `ANON-001` **[PHASE_3][SOL_HIGH] Define confidential actor disclosure modes.**
  - **Depends:** `MODEL-015`, `TRUST-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.CASES,BI.PRIVACY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ANON_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ANON_001`; `FAULT=TestTodo_ANON_001_Fault`; `SECURITY=TestTodo_ANON_001_Security`.
  - **RED:** Every intake explicitly selects `KNOWN|PSEUDONYMOUS|ANONYMOUS|ESCROWED`, allowed identity evidence, abuse controls, revelation policy and downstream disclosure limits.
  - **GREEN:** No implicit default.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Talent/experience/case models](data/models/talent-experience-cases.md), [security models](data/models/security-trust.md).
  - **Evidence (2026-09-03):** `TestTodo_ANON_001`, Declared `ANON-001` test matrix and acceptance behavior in `internal/trust/confidentialactor`; PASS; `go test -p 1 ./internal/trust/confidentialactor` and `go vet ./internal/trust/confidentialactor` on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `ANON-002` **[PHASE_3][SOL_HIGH] Create unlinkable scoped pseudonymous subjects.**
  - **Evidence (2026-09-05):** `TestTodo_ANON_002` in `internal/domains/pseudonym` (scoped pseudonymous subjects derived per tenant, scope and purpose through a custody-keyed HMAC, unlinkable across scopes by property, re-identification only through an explicit evidenced distinct-approver request, rotation producing a new pseudonym generation with the mapping held only under the custody key; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/pseudonym/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ANON-001`, `TRUST-028`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.CASES,BI.PRIVACY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ANON_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ANON_002`; `SECURITY=TestTodo_ANON_002_Security`; `MUTATION=TestTodo_ANON_002_Mutation`.
  - **RED:** Seeded defect: a pseudonym correlates across tenant or program scope for ANON-002; the primary test must return ANON_002_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Identifier cannot correlate across tenant/program/campaign/case without authorized escrow operation, remains stable only within declared scope and resists offline guessing.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Privacy models](data/models/assurance-intelligence-platform.md), [security models](data/models/security-trust.md).

- [x] `ANON-003` **[PHASE_3][SOL_HIGH] Protect identity escrow under separate custody.**
  - **Evidence (2026-09-05):** `TestTodo_ANON_003` in `internal/domains/pseudonym` (identity escrow holding the pseudonym-to-subject mapping only as ciphertext under a custody key handle distinct from the derivation key and every tenant KEK, two-party release by requester and a distinct escrow custodian with purpose, evidence ref and TTL, every release a digested event, Explain never carrying the mapping; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/pseudonym/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02. During integration the escrow's direct Release was gated: it now refuses with ErrRevelationEvidenceRequired (wrapping ErrEscrowReleaseDenied) and records the refusal, so ReleaseWithEvidence with a validated one-time ANON-004 receipt is the only path that opens the mapping; the ANON-003 tests were moved onto that path.
  - **Depends:** `ANON-001`, `TRUST-031`, `TRUST-014`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.CASES,BI.PRIVACY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ANON_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ANON_003`; `RACE=TestTodo_ANON_003_Race`; `SECURITY=TestTodo_ANON_003_Security`.
  - **RED:** Ordinary case/admin/search keys cannot decrypt linkage.
  - **GREEN:** Dual-control, purpose-bound, time-limited escrow stores minimal identity and complete access evidence.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Credential custody](specs/secrets-key-custody-and-credential-leases.md), [case models](data/models/talent-experience-cases.md).

- [x] `ANON-004` **[PHASE_3][SOL_HIGH] Authorize and evidence identity revelation.**
  - **Evidence (2026-09-05):** `TestTodo_ANON_004` in `internal/domains/pseudonym` (revelation authorization against a declared policy requiring a closed purpose, a distinct custodian approver, a legal basis ref, one-generation scope and a TTL, producing a digested RevelationEvidence the escrow release must present, Explain never carrying the identity; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/pseudonym/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ANON-003`, `TRUST-021`, `APPROVAL-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.CASES,BI.PRIVACY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ANON_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ANON_004`; `RACE=TestTodo_ANON_004_Race`; `SECURITY=TestTodo_ANON_004_Security`; `MUTATION=TestTodo_ANON_004_Mutation`.
  - **RED:** Stale/self-approved/overbroad revelation fails.
  - **GREEN:** Exact case/legal purpose, fields, recipients, expiry and notification policy bind a one-time disclosure receipt.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [AuthZ](specs/organization-scope-and-authz.md), [governance composition](specs/governance-decision-and-obligation-composition.md).

- [x] `ANON-005` **[PHASE_3][SOL_HIGH] Prevent unauthorized correlation and existence inference.**
  - **Evidence (2026-09-05):** `TestTodo_ANON_005` in `internal/domains/pseudonym` (correlation and existence-inference protection: single-pseudonym membership probes indistinguishable for present and absent subjects by property, a declared minimum cohort on aggregates, cross-scope joins refused, per-principal query budgets, every refusal recorded as evidence; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/pseudonym/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ANON-002`, `TRUST-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.CASES,BI.PRIVACY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ANON_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ANON_005`; `SECURITY=TestTodo_ANON_005_Security`.
  - **RED:** Joins, exports, logs, timing, counts, search and support tools cannot correlate identities or reveal participation outside authorized mode.
  - **GREEN:** Adversarial corpus returns no oracle.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [DLP](specs/data-classification-and-dlp.md), [adversarial audit](specs/adversarial-audit-32-reviewers-2026-08-14.md).

- [ ] `ANON-006` **[PHASE_3][SOL_HIGH] Bound duplicate and spam abuse without deanonymizing.**
  - **Depends:** `ANON-002`, `EDGE-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.CASES,BI.PRIVACY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ANON_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ANON_006`; `SECURITY=TestTodo_ANON_006_Security`; `RECOVERY=TestTodo_ANON_006_Recovery`.
  - **RED:** Scoped rate/replay/proof controls reject duplicates/automation while retaining no reusable global identity or forbidden fingerprint.
  - **GREEN:** False-positive review is available.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Edge controls](#32-production-identity-key-custody-and-edge-enforcement), [privacy models](data/models/assurance-intelligence-platform.md).

- [x] `ANON-007` **[PHASE_3][SOL_HIGH] Preserve confidential evidence and correction history.**
  - **Evidence (2026-09-05):** `TestTodo_ANON_007` in `internal/domains/pseudonym` (confidential evidence keyed by pseudonym generation as an append-only in-memory shape with corrections as new records naming the corrected one and a reason, full correction-chain history, and a rule that a revelation never rewrites evidence recorded under the pseudonym; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/pseudonym/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ANON-003`, `LEDGER-005`, `MODEL-020`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.CASES,BI.PRIVACY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ANON_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ANON_007`; `SECURITY=TestTodo_ANON_007_Security`; `MUTATION=TestTodo_ANON_007_Mutation`.
  - **RED:** Evidence package proves intake/time/content/corrections/custody without ordinary actor disclosure.
  - **GREEN:** Revelation/non-revelation status is explicit.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Ledger](specs/transaction-ledger-reconciliation-and-repair.md), [provenance](specs/provenance-graph-and-lineage.md).

- [ ] `ANON-008` **[CONFORMANCE][SOL_HIGH] Test analytics and search against re-identification.**
  - **Depends:** `ANON-005`, `RETRIEVAL-001`, `ANALYTICS-001`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.CASES,BI.PRIVACY; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_ANON_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ANON_008`; `SECURITY=TestTodo_ANON_008_Security`; `CONFORMANCE=TestTodo_ANON_008_Conformance`.
  - **RED:** Small cohorts, differencing, repeated filters, embeddings, exports and cross-dataset joins cannot re-identify.
  - **GREEN:** Unsafe result suppresses/generalizes or returns review requirement.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Intelligence models](data/models/assurance-intelligence-platform.md), [DLP](specs/data-classification-and-dlp.md).

- [x] `ABUSE-001` **[GATE_C][SOL_HIGH] Define governed activity signals and detector versions.**
  - **Evidence (2026-09-05):** `TestTodo_ABUSE_001`, `TestTodo_ABUSE_001_{Golden,Property,Conformance,Security,Mutation}`, `TestABUSE001SecurityRawContentAndUndeclaredKindRefused`, registry and explain tests in `internal/engines/abuse` (closed signal vocabulary with classifications, raw content refused, immutable detector versions with idempotent publish and one active version, Explain naming the declared input; `Version` for ARCH-GO-009); `go test -count=1 ./internal/engines/abuse/.../` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `OBS-001`, `MODEL-023`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.SECURITY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ABUSE_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ABUSE_001`; `SECURITY=TestTodo_ABUSE_001_Security`; `MUTATION=TestTodo_ABUSE_001_Mutation`.
  - **RED:** Seeded defect: seeded privileged or sensitive-data abuse is missed or auto-accuses for ABUSE-001; the primary test must return ABUSE_001_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Signal/detector missing purpose, features, source/quality, retention, protected-attribute policy, owner, threshold, action and evaluation plan fails publication.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security models](data/models/security-trust.md), [operations models](data/models/operations-production.md).

- [x] `ABUSE-002` **[GATE_C][SOL_HIGH] Detect anomalous privileged and sensitive-data activity.**
  - **Evidence (2026-09-05):** `TestTodo_ABUSE_002` in `internal/engines/abuse` (detectors for anomalous privileged activity (out-of-hours, new principal, bursts) and sensitive-data access anomalies (volume against a rolling baseline, subjects outside declared scope) as immutable detector versions with Explain naming declared inputs, no raw content, typed findings with severity; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/engines/abuse/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ABUSE-001`, `TRUST-021`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.SECURITY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ABUSE_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ABUSE_002`; `SECURITY=TestTodo_ABUSE_002_Security`; `MUTATION=TestTodo_ABUSE_002_Mutation`.
  - **RED:** Seeded defect: seeded privileged or sensitive-data abuse is missed or auto-accuses for ABUSE-002; the primary test must return ABUSE_002_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Fixtures cover unusual time/location/volume/scope/sequence for privileged reads/exports while expected approved batch/incident work avoids false accusation.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Support/JIT access](specs/platform-responsibility-boundaries.md), [DLP](specs/data-classification-and-dlp.md).

- [x] `ABUSE-003` **[GATE_C][SOL_HIGH] Detect bulk export and high-risk payroll/access changes.**
  - **Evidence (2026-09-05):** `TestTodo_ABUSE_003`, ``in`internal/engines/abuse`(bulk-export volume/velocity detectors per principal and tenant over declared windows plus high-risk payroll (bank detail, pay rate, net-pay redirection) and access (self-grant, escalation) detectors as immutable detector versions over the ABUSE-001 signal vocabulary, Explain naming declared inputs, no raw content, typed findings with severity; written by a codex GPT-5.6 Luna lane and verified independently);`go test -count=1 ./internal/engines/abuse/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ABUSE-001`, `ADMIN-007`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.SECURITY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ABUSE_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ABUSE_003`; `SECURITY=TestTodo_ABUSE_003_Security`.
  - **RED:** Mass compensation export, unusual bank changes, fake-worker pattern and privilege burst yield evidence-backed finding.
  - **GREEN:** Ordinary merit/payroll/import batches classify with approved context.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Security models](data/models/security-trust.md), [rewards models](data/models/rewards-payroll-workforce.md).

- [x] `ABUSE-004` **[GATE_C][SOL_HIGH] Risk-score activity without automatic accusation.**
  - **Evidence (2026-09-05):** `TestTodo_ABUSE_004` in `internal/engines/abuse` (bounded risk score per principal and window under a versioned weighting table, a RiskAssessment carrying score, contributing finding refs, confidence and a review-required flag but never an accusation state, a human review record required before any consequence marker, Explain naming only contributing findings, golden scores; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/engines/abuse/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ABUSE-002`, `ABUSE-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.SECURITY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ABUSE_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ABUSE_004`; `SECURITY=TestTodo_ABUSE_004_Security`.
  - **RED:** Score exposes bounded contributing signals, uncertainty, version and calibration.
  - **GREEN:** It never becomes a misconduct fact or irreversible employment action.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Model conventions](data/models/modeling-conventions.md), [decision/outcome models](data/models/assurance-intelligence-platform.md).

- [x] `ABUSE-005` **[GATE_C][SOL_HIGH] Trigger step-up, review or temporary containment.**
  - **Evidence (2026-09-05):** `TestTodo_ABUSE_005` in `internal/engines/abuse` (False positive has human review route; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/engines/abuse/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ABUSE-004`, `AUTHN-005`, `CP-008`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.SECURITY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ABUSE_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ABUSE_005`; `SECURITY=TestTodo_ABUSE_005_Security`; `MUTATION=TestTodo_ABUSE_005_Mutation`.
  - **RED:** Action follows explicit risk/authority policy, is minimal/time-bounded/reversible and cannot silently terminate employment/payroll.
  - **GREEN:** False positive has human review route.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Governance composition](specs/governance-decision-and-obligation-composition.md), [incident management](specs/incident-management.md).

- [ ] `ABUSE-006` **[GATE_C][SOL_HIGH] Investigate and disposition abuse findings.**
  - **Depends:** `ABUSE-004`, `OPS-004`, `WORK-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.SECURITY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ABUSE_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ABUSE_006`; `SECURITY=TestTodo_ABUSE_006_Security`; `MUTATION=TestTodo_ABUSE_006_Mutation`.
  - **RED:** Finding lifecycle preserves compartment, evidence, investigator SoD, substantiated/unsubstantiated/inconclusive result and correction.
  - **GREEN:** No alert equals guilt.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Case models](data/models/talent-experience-cases.md), [human work](specs/human-work-forms-and-rules.md).

- [ ] `ABUSE-007` **[PHASE_3][SOL_HIGH] Evaluate detector outcomes and drift.**
  - **Depends:** `ABUSE-006`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.SECURITY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ABUSE_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ABUSE_007`; `SECURITY=TestTodo_ABUSE_007_Security`.
  - **RED:** Metrics bind reviewed outcomes, sampling bias, false-positive/negative/unknown rates and version.
  - **GREEN:** Degraded detector pauses or requires review rather than silently broadening containment.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Outcome models](data/models/assurance-intelligence-platform.md), [SLOs](specs/slo-sli-error-budget.md).

- [ ] `ABUSE-008` **[CONFORMANCE][SOL_HIGH] Run insider-risk abuse simulations.**
  - **Depends:** `ABUSE-001`–`ABUSE-007`, `TRUST-025`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.SECURITY; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_ABUSE_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ABUSE_008`; `SECURITY=TestTodo_ABUSE_008_Security`; `CONFORMANCE=TestTodo_ABUSE_008_Conformance`; `MUTATION=TestTodo_ABUSE_008_Mutation`.
  - **RED:** Seeded defect: seeded privileged or sensitive-data abuse is missed or auto-accuses for ABUSE-008; the primary test must return ABUSE_008_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Signed corpus covers compromised admin, authorized mass read, bank/payroll/access manipulation, collusion, false positive and telemetry gaps with no cross-tenant leak or automatic accusation.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Adversarial audit](specs/adversarial-audit-32-reviewers-2026-08-14.md), [security models](data/models/security-trust.md).

- [ ] `ROLLOUT-001` **[GATE_B][SOL_HIGH] Define a multi-artifact RolloutPlan.**
  - **Depends:** `CP-002`, `OPS-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.OPERATIONS,BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ROLLOUT_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ROLLOUT_001`; `GOLDEN=TestTodo_ROLLOUT_001_Golden`; `FUZZ=FuzzTodo_ROLLOUT_001`; `INTEGRATION=TestTodo_ROLLOUT_001_Integration`.
  - **RED:** Plan missing artifact type/version, target expression, stages, health windows, stop/expand/rollback/kill criteria, owner or expiry fails.
  - **GREEN:** Supports workflow/rule/capability/connector/agent/UI/schema.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Control plane](#30-control-plane-publication-distribution-and-activation), [configuration models](data/models/dataops-configuration.md).

- [ ] `ROLLOUT-002` **[GATE_B][SOL_HIGH] Resolve and freeze rollout target cohorts.**
  - **Depends:** `ROLLOUT-001`, `POP-005`, `TENANT-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.OPERATIONS,BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ROLLOUT_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ROLLOUT_002`; `GOLDEN=TestTodo_ROLLOUT_002_Golden`; `SECURITY=TestTodo_ROLLOUT_002_Security`.
  - **RED:** Target resolution respects tenant/org/population/residency and prevents count leakage.
  - **GREEN:** Cohort snapshot/digest is immutable per stage.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Population engine](#37-shared-transformation-and-population-engines), [tenant placement](specs/platform-architecture-catalog.md).

- [ ] `ROLLOUT-003` **[GATE_B][SOL_HIGH] Activate canary with exact control snapshot.**
  - **Depends:** `ROLLOUT-002`, `CP-003`, `CP-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.OPERATIONS,BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ROLLOUT_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ROLLOUT_003`; `GOLDEN=TestTodo_ROLLOUT_003_Golden`.
  - **RED:** Wrong artifact/dependency/epoch/cohort cannot activate.
  - **GREEN:** Every subject/service can explain exact active version and receipt.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Foundation publication](specs/platform-foundation-gap-closure.md), [control plane](#30-control-plane-publication-distribution-and-activation).

- [ ] `ROLLOUT-004` **[GATE_B][SOL_HIGH] Evaluate rollout health and expand deterministically.**
  - **Depends:** `ROLLOUT-003`, `OBS-005`, `OBS-006`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.OPERATIONS,BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ROLLOUT_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ROLLOUT_004`; `SECURITY=TestTodo_ROLLOUT_004_Security`.
  - **RED:** Telemetry unknown, SLO burn, correctness/reconciliation/security regression or insufficient window blocks expansion.
  - **GREEN:** Decision stores metrics, thresholds and evidence.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [SLOs](specs/slo-sli-error-budget.md), [operations models](data/models/operations-production.md).

- [ ] `ROLLOUT-005` **[GATE_B][SOL_HIGH] Pause and resume rollout with fencing.**
  - **Depends:** `ROLLOUT-004`, `OPS-006`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.OPERATIONS,BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ROLLOUT_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ROLLOUT_005`; `MUTATION=TestTodo_ROLLOUT_005_Mutation`.
  - **RED:** Pause stops new activations and running unsafe work at declared safe points.
  - **GREEN:** Resume revalidates plan, health, authority and cohort.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Workflow safe points](specs/workflow-runtime.md), [operations models](data/models/operations-production.md).

- [ ] `ROLLOUT-006` **[GATE_B][SOL_HIGH] Roll back or emergency-kill active behavior.**
  - **Depends:** `ROLLOUT-003`, `CP-008`, `CP-009`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.OPERATIONS,BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ROLLOUT_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ROLLOUT_006`; `RACE=TestTodo_ROLLOUT_006_Race`; `INTEGRATION=TestTodo_ROLLOUT_006_Integration`; `FAULT=TestTodo_ROLLOUT_006_Fault`; `RECOVERY=TestTodo_ROLLOUT_006_Recovery`; `MUTATION=TestTodo_ROLLOUT_006_Mutation`.
  - **RED:** Rollback uses exact verified prior version/new epoch and reconciles.
  - **GREEN:** Kill switch propagates within SLO and does not rewrite prior history.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Control plane](#30-control-plane-publication-distribution-and-activation), [incident management](specs/incident-management.md).

- [x] `ROLLOUT-007` **[GATE_B][SOL_HIGH] Explain active version for every subject and execution.**
  - **Evidence (2026-09-07):** `TestTodo_ROLLOUT_007`, `TestTodo_ROLLOUT_007_Mutation`, `TestTodo_ROLLOUT_007_Rejects` in `internal/platform/versionexplain` (written in fe89a37 on 2026-09-03 and never ticked; the named tests prove the GREEN contract: query returns target/cohort/stage/bundle/epoch/receipt/override/kill state at known/effective time without exposing other cohort membership); `go test -count=1 ./internal/platform/versionexplain/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ROLLOUT-003`–`ROLLOUT-006`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.OPERATIONS,BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ROLLOUT_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ROLLOUT_007`; `MUTATION=TestTodo_ROLLOUT_007_Mutation`.
  - **RED:** Seeded defect: one snapshot resolves inconsistent versions or pause semantics for ROLLOUT-007; the primary test must return ROLLOUT_007_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Query returns target/cohort/stage/bundle/epoch/receipt/override/kill state at known/effective time without exposing other cohort membership.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Provenance](specs/provenance-graph-and-lineage.md), [AuthZ](specs/organization-scope-and-authz.md).

- [ ] `ROLLOUT-008` **[GATE_B][SOL_HIGH] Conform service and configuration rollout implementations.**
  - **Depends:** `ROLLOUT-001`–`ROLLOUT-007`, `CICD-005`, `CP-007`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.OPERATIONS,BI.TENANT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ROLLOUT_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ROLLOUT_008`; `PROPERTY=TestTodo_ROLLOUT_008_Property`; `MUTATION=TestTodo_ROLLOUT_008_Mutation`.
  - **RED:** Seeded defect: one snapshot resolves inconsistent versions or pause semantics for ROLLOUT-008; the primary test must return ROLLOUT_008_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Binary, configuration and semantic artifacts pass identical target/epoch/health/pause/rollback/explain invariants while retaining artifact-specific activation hooks.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Release admission](#33-cicd-release-admission-and-observability), [foundation gap closure](specs/platform-foundation-gap-closure.md).

- [x] `EDGE-009` **[PHASE_2][SOL_HIGH] Protect streaming endpoints and long-lived edge sessions.**
  - **Evidence (2026-09-05):** `TestTodo_EDGE_009` in `internal/connectivity/edge` (Backpressure, reauthorization and edge evidence preserve zero unauthorized continuation; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/connectivity/edge/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PROTO-007`, `EDGE-001`–`EDGE-008`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.SECURITY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_EDGE_009`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_EDGE_009`; `FUZZ=FuzzTodo_EDGE_009`; `SECURITY=TestTodo_EDGE_009_Security`; `RECOVERY=TestTodo_EDGE_009_Recovery`.
  - **RED:** Unbounded stream count/message/rate/lifetime, revoked session, slow consumer and reconnect replay are bounded.
  - **GREEN:** Backpressure, reauthorization and edge evidence preserve zero unauthorized continuation.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Experience/API plane](specs/platform-plane-model.md), [edge controls](#32-production-identity-key-custody-and-edge-enforcement).

## 43. Deep Payroll, accounting, settlement and garnishment engines

> **Disposition (2026-09-02):** DEFERRED; a separate product line with its own gate.

- [x] `PAYRUN-001` **[CONFORMANCE][SOL_HIGH] Define PayrollRun and immutable revision lifecycle.**
  - **Evidence (2026-09-05):** `TestTodo_PAYRUN_001` in `internal/domains/payroll` (PayrollRun with an immutable revision lifecycle DRAFT/CALCULATED/RELEASED/SETTLED/REVERSED as new revisions, period and population binding refs, calculation input digest, out-of-order transitions and settlement without release refused; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/payroll/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CYCLE-001`, `BAL-001`, `CONF-009`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.PAYROLL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_PAYRUN_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PAYRUN_001`; `PROPERTY=TestTodo_PAYRUN_001_Property`; `RACE=TestTodo_PAYRUN_001_Race`; `INTEGRATION=TestTodo_PAYRUN_001_Integration`; `FAULT=TestTodo_PAYRUN_001_Fault`; `CONFORMANCE=TestTodo_PAYRUN_001_Conformance`; `MUTATION=TestTodo_PAYRUN_001_Mutation`.
  - **RED:** Seeded defect: an illegal payroll-run transition is accepted for PAYRUN-001; the primary test must return PAYRUN_001_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Model must cover pay group/period, population, inputs, calculations, exceptions, approvals, locks, release, correction, completion dimensions and authority without exposing a Phase 1 write.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Rewards/payroll models](data/models/rewards-payroll-workforce.md), [intent coverage](data/models/intent-coverage-matrix.md).

- [x] `PAYRUN-002` **[PHASE_5][SOL_HIGH] Freeze payroll population.**
  - **Evidence (2026-09-05):** `TestTodo_PAYRUN_002` in `internal/domains/payroll` (frozen payroll population per run resolved from a population binding ref at a declared as-of, digested and immutable after freeze, late entrants and removals only as explicit digested amendments creating a new frozen revision, calculation against an unfrozen or superseded population refused; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/payroll/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PAYRUN-001`, `POP-005`, `CYCLE-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PAYRUN_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PAYRUN_002`; `PROPERTY=TestTodo_PAYRUN_002_Property`; `GOLDEN=TestTodo_PAYRUN_002_Golden`; `RACE=TestTodo_PAYRUN_002_Race`; `INTEGRATION=TestTodo_PAYRUN_002_Integration`; `FAULT=TestTodo_PAYRUN_002_Fault`; `MUTATION=TestTodo_PAYRUN_002_Mutation`; `SECURITY=TestTodo_PAYRUN_002_Security`.
  - **RED:** Mutable/partial/unauthorized population or duplicate employment/pay group ambiguity blocks run.
  - **GREEN:** Snapshot records late-entry policy and exact source versions.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Population engine](#37-shared-transformation-and-population-engines), [source authority](specs/source-authority-and-external-mastering.md).

- [ ] `PAYRUN-003` **[PHASE_5][SOL_HIGH] Collect and lock payroll inputs at cutoff.**
  - **Depends:** `PAYRUN-002`, `CYCLE-003`, `ATTEST-006`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PAYRUN_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PAYRUN_003`; `PROPERTY=TestTodo_PAYRUN_003_Property`; `GOLDEN=TestTodo_PAYRUN_003_Golden`; `RACE=TestTodo_PAYRUN_003_Race`; `INTEGRATION=TestTodo_PAYRUN_003_Integration`; `FAULT=TestTodo_PAYRUN_003_Fault`; `MUTATION=TestTodo_PAYRUN_003_Mutation`.
  - **RED:** Stale/unapproved/unattested/late input is classified, never silently included.
  - **GREEN:** Locked manifest binds earnings, time, deductions, tax, benefits and corrections.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Business cycles](#38-eligibility-business-cycle-and-balance-engines), [attestation](#40-attestation-and-conformance-gated-program-engines).

- [ ] `PAYRUN-004` **[PHASE_5][SOL_HIGH] Execute deterministic trial payroll calculation.**
  - **Depends:** `PAYRUN-003`, `BAL-003`, `LEGAL-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PAYRUN_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PAYRUN_004`; `PROPERTY=TestTodo_PAYRUN_004_Property`; `RACE=TestTodo_PAYRUN_004_Race`; `INTEGRATION=TestTodo_PAYRUN_004_Integration`; `FAULT=TestTodo_PAYRUN_004_Fault`; `SECURITY=TestTodo_PAYRUN_004_Security`; `MUTATION=TestTodo_PAYRUN_004_Mutation`.
  - **RED:** Same input/rule/period returns exact worker/run totals, traces and exceptions.
  - **GREEN:** Missing jurisdiction/rule/balance is unknown/blocking, never zero.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Regulatory platform](specs/platform-architecture-catalog.md), [fixed decimal](data/models/wire-contract-primitives.md).

- [ ] `PAYRUN-005` **[PHASE_5][SOL_HIGH] Resolve payroll exceptions and recalculate affected scope.**
  - **Depends:** `PAYRUN-004`, `WORK-001`, `TX-010`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PAYRUN_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PAYRUN_005`; `PROPERTY=TestTodo_PAYRUN_005_Property`; `RACE=TestTodo_PAYRUN_005_Race`; `INTEGRATION=TestTodo_PAYRUN_005_Integration`; `FAULT=TestTodo_PAYRUN_005_Fault`; `MUTATION=TestTodo_PAYRUN_005_Mutation`.
  - **RED:** Exception has owner/severity/affected workers/repair action.
  - **GREEN:** Correction invalidates only dependent calculations and preserves every prior attempt.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Human work](specs/human-work-forms-and-rules.md), [payroll repair](specs/transaction-ledger-reconciliation-and-repair.md).

- [x] `PAYRUN-006` **[PHASE_5][SOL_HIGH] Approve and lock payroll with separation of duties.**
  - **Evidence (2026-09-05):** `TestTodo_PAYRUN_006` in `internal/domains/payroll` (Requester/calculator/releaser constraints and quorum bind exact run digest; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/payroll/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PAYRUN-005`, `APPROVAL-002`, `TRUST-014`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PAYRUN_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PAYRUN_006`; `PROPERTY=TestTodo_PAYRUN_006_Property`; `GOLDEN=TestTodo_PAYRUN_006_Golden`; `RACE=TestTodo_PAYRUN_006_Race`; `INTEGRATION=TestTodo_PAYRUN_006_Integration`; `FAULT=TestTodo_PAYRUN_006_Fault`; `MUTATION=TestTodo_PAYRUN_006_Mutation`.
  - **RED:** Changed totals/exceptions/rules/population invalidate approvals.
  - **GREEN:** Requester/calculator/releaser constraints and quorum bind exact run digest.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Governance composition](specs/governance-decision-and-obligation-composition.md), [human work](specs/human-work-forms-and-rules.md).

- [ ] `PAYRUN-007` **[PHASE_5][SOL_HIGH] Finalize and release payroll effects.**
  - **Depends:** `PAYRUN-006`, `TX-008`, `SETTLE-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PAYRUN_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PAYRUN_007`; `PROPERTY=TestTodo_PAYRUN_007_Property`; `GOLDEN=TestTodo_PAYRUN_007_Golden`; `RACE=TestTodo_PAYRUN_007_Race`; `INTEGRATION=TestTodo_PAYRUN_007_Integration`; `FAULT=TestTodo_PAYRUN_007_Fault`; `MUTATION=TestTodo_PAYRUN_007_Mutation`.
  - **RED:** Stale lock, funding/filing/settlement obligation or duplicate release fails.
  - **GREEN:** Immutable release compiles exact payments, statements, balances, accounting and reporting effects.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Transaction coordinator](specs/transaction-plan-and-commit-coordinator.md), [reconciliation](specs/transaction-ledger-reconciliation-and-repair.md).

- [ ] `PAYRUN-008` **[PHASE_5][SOL_HIGH] Reopen or correct a finalized payroll append-only.**
  - **Depends:** `PAYRUN-007`, `LEDGER-005`, `TX-010`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PAYRUN_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PAYRUN_008`; `PROPERTY=TestTodo_PAYRUN_008_Property`; `RACE=TestTodo_PAYRUN_008_Race`; `INTEGRATION=TestTodo_PAYRUN_008_Integration`; `FAULT=TestTodo_PAYRUN_008_Fault`; `MUTATION=TestTodo_PAYRUN_008_Mutation`.
  - **RED:** Finalized run is never mutated.
  - **GREEN:** Off-cycle/correction run links causal entries, deltas, approvals, reversals and restated obligations.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Payroll repair reference](workflows/payroll/payroll-correction-retro.md), [ledger corrections](specs/transaction-ledger-reconciliation-and-repair.md).

- [ ] `PAYRUN-009` **[PHASE_5][SOL_HIGH] Reconcile multidimensional payroll completion.**
  - **Depends:** `PAYRUN-007`, `TX-009`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PAYRUN_009`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PAYRUN_009`; `PROPERTY=TestTodo_PAYRUN_009_Property`; `GOLDEN=TestTodo_PAYRUN_009_Golden`; `RACE=TestTodo_PAYRUN_009_Race`; `INTEGRATION=TestTodo_PAYRUN_009_Integration`; `FAULT=TestTodo_PAYRUN_009_Fault`; `MUTATION=TestTodo_PAYRUN_009_Mutation`.
  - **RED:** Employee results, payments, GL, tax/filings, statements and external observations each report `CONSISTENT|DEGRADED|REPAIR_REQUIRED|UNKNOWN`
  - **GREEN:** One success cannot mask another failure.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Reconciliation](specs/transaction-ledger-reconciliation-and-repair.md), [workflow completion](specs/workflow-runtime.md).

- [x] `PAYGL-001` **[CONFORMANCE][SOL_HIGH] Define payroll accounting rules and versions.**
  - **Evidence (2026-09-05):** `TestTodo_PAYGL_001` in `internal/domains/paygl` (effective-dated versioned payroll accounting rules mapping earning, deduction and tax codes and labor dimensions to accounts, superseded only by new versions, a posting derivation producing balanced exact-decimal debit and credit postings proven by property, and Explain; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/paygl/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PAYRUN-001`, `MODEL-017`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.PAYROLL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_PAYGL_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PAYGL_001`; `PROPERTY=TestTodo_PAYGL_001_Property`; `CONFORMANCE=TestTodo_PAYGL_001_Conformance`; `MUTATION=TestTodo_PAYGL_001_Mutation`.
  - **RED:** Seeded defect: an unbalanced or incomplete accounting rule or journal publishes for PAYGL-001; the primary test must return PAYGL_001_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Rule missing earning/deduction/tax/employer component, debit/credit account, dimensions, effective interval, rounding and suspense policy fails.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Rewards/payroll models](data/models/rewards-payroll-workforce.md), [rules models](data/models/rules-and-decisions.md).

- [x] `PAYGL-002` **[PHASE_5][SOL_HIGH] Map payroll components to accounts deterministically.**
  - **Evidence (2026-09-05):** `TestTodo_PAYGL_002` in `internal/domains/paygl` (every payroll component of a calculated run resolved to exactly one debit and one credit account under the active rule version, labor-dimension splits in exact decimal with largest-remainder deterministic remainders, unmapped components and non-summing splits refused, a digested mapping result byte-identical across runs and goroutines, golden for a fixture run, property that splits sum to components; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/paygl/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PAYGL-001`, `XFORM-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PAYGL_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PAYGL_002`; `PROPERTY=TestTodo_PAYGL_002_Property`; `FAULT=TestTodo_PAYGL_002_Fault`; `MUTATION=TestTodo_PAYGL_002_Mutation`.
  - **RED:** Unmapped/ambiguous component returns blocking/suspense obligation.
  - **GREEN:** Exact rule/version/input yields stable account/dimensions and lineage.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Transformation engine](#37-shared-transformation-and-population-engines), [provenance](specs/provenance-graph-and-lineage.md).

- [ ] `PAYGL-003` **[PHASE_5][SOL_HIGH] Allocate labor dimensions.**
  - **Depends:** `PAYGL-002`, `LABOR-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PAYGL_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PAYGL_003`; `PROPERTY=TestTodo_PAYGL_003_Property`; `MUTATION=TestTodo_PAYGL_003_Mutation`.
  - **RED:** Allocation percentages/amounts must sum exactly under fixed-decimal policy and bind cost center/project/location/worktag versions.
  - **GREEN:** Residual is explicit.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Fixed decimal](data/models/wire-contract-primitives.md), [people/workforce models](data/models/people-workforce.md).

- [ ] `PAYGL-004` **[PHASE_5][SOL_HIGH] Generate and validate balanced journals.**
  - **Depends:** `PAYGL-002`, `PAYGL-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PAYGL_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PAYGL_004`; `PROPERTY=TestTodo_PAYGL_004_Property`; `MUTATION=TestTodo_PAYGL_004_Mutation`.
  - **RED:** Debits must equal credits by currency/entity/ledger and no missing dimension can hide.
  - **GREEN:** Journal is immutable, content-addressed and source-run linked.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Canonical digest](specs/canonical-envelope-and-digest.md), [rewards/payroll models](data/models/rewards-payroll-workforce.md).

- [ ] `PAYGL-005` **[PHASE_5][SOL_HIGH] Correct prior payroll journals append-only.**
  - **Depends:** `PAYGL-004`, `LEDGER-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PAYGL_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PAYGL_005`; `PROPERTY=TestTodo_PAYGL_005_Property`; `MUTATION=TestTodo_PAYGL_005_Mutation`.
  - **RED:** Seeded defect: an unbalanced or incomplete accounting rule or journal publishes for PAYGL-005; the primary test must return PAYGL_005_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Correction creates reversal/adjustment with exact prior-line links and never overwrites exported journal history.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Ledger corrections](specs/transaction-ledger-reconciliation-and-repair.md), [transaction coordinator](specs/transaction-plan-and-commit-coordinator.md).

- [ ] `PAYGL-006` **[PHASE_5][SOL_HIGH] Reconcile ERP journal posting.**
  - **Depends:** `PAYGL-004`, `INTG-009`, `TX-009`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PAYGL_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PAYGL_006`; `PROPERTY=TestTodo_PAYGL_006_Property`; `INTEGRATION=TestTodo_PAYGL_006_Integration`; `MUTATION=TestTodo_PAYGL_006_Mutation`.
  - **RED:** Provider accepted is insufficient.
  - **GREEN:** Compare journal/lines/totals/dimensions/external IDs and route rejects/partials/unknowns to repair.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Integration observations](specs/integration-platform.md), [reconciliation](specs/transaction-ledger-reconciliation-and-repair.md).

- [x] `SETTLE-001` **[CONFORMANCE][SOL_HIGH] Define PaymentInstruction and settlement lifecycle.**
  - **Evidence (2026-09-05):** `TestTodo_SETTLE_001` in `internal/domains/settlement` (PaymentInstruction with exact-decimal amount and currency, funding source and rail from a closed vocabulary, bank detail by governed reference only, an immutable INSTRUCTED/SUBMITTED/ACKNOWLEDGED/SETTLED/RETURNED/REVERSED lifecycle as new revisions, settlement without a released payroll run refused, natural-key idempotency and Explain; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/settlement/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PAYRUN-001`, `MODEL-015`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.PAYROLL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_SETTLE_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SETTLE_001`; `RACE=TestTodo_SETTLE_001_Race`; `INTEGRATION=TestTodo_SETTLE_001_Integration`; `FAULT=TestTodo_SETTLE_001_Fault`; `SECURITY=TestTodo_SETTLE_001_Security`; `CONFORMANCE=TestTodo_SETTLE_001_Conformance`; `MUTATION=TestTodo_SETTLE_001_Mutation`.
  - **RED:** Seeded defect: acceptance is called settlement or funding and return totals mismatch for SETTLE-001; the primary test must return SETTLE_001_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Model covers payee/tokenized destination, amount/currency, funding, rail, schedule, authorization, submission, acceptance, settlement, return/reversal and unknown states without raw bank storage.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Rewards/payroll models](data/models/rewards-payroll-workforce.md), [security models](data/models/security-trust.md).

- [ ] `SETTLE-002` **[PHASE_5][SOL_HIGH] Build and verify payroll funding requirement.**
  - **Depends:** `SETTLE-001`, `PAYRUN-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SETTLE_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SETTLE_002`; `RACE=TestTodo_SETTLE_002_Race`; `INTEGRATION=TestTodo_SETTLE_002_Integration`; `FAULT=TestTodo_SETTLE_002_Fault`; `MUTATION=TestTodo_SETTLE_002_Mutation`.
  - **RED:** Seeded defect: acceptance is called settlement or funding and return totals mismatch for SETTLE-002; the primary test must return SETTLE_002_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Worker/instruction/control totals reconcile exactly by entity/currency/date and missing/late funding source blocks release with explicit obligation.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Fixed decimal](data/models/wire-contract-primitives.md), [governance composition](specs/governance-decision-and-obligation-composition.md).

- [x] `SETTLE-003` **[PHASE_5][SOL_HIGH] Authorize payment release with dual control.**
  - **Evidence (2026-09-05):** `TestTodo_SETTLE_003` in `internal/domains/settlement` (Decision binds exact batch digest and limits; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/settlement/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SETTLE-002`, `APPROVAL-002`, `TRUST-014`, `AUTHN-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SETTLE_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SETTLE_003`; `GOLDEN=TestTodo_SETTLE_003_Golden`; `RACE=TestTodo_SETTLE_003_Race`; `INTEGRATION=TestTodo_SETTLE_003_Integration`; `FAULT=TestTodo_SETTLE_003_Fault`; `MUTATION=TestTodo_SETTLE_003_Mutation`.
  - **RED:** Changed instruction/funding/payee, self-approval or insufficient step-up invalidates release.
  - **GREEN:** Decision binds exact batch digest and limits.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Human work](specs/human-work-forms-and-rules.md), [AuthZ](specs/organization-scope-and-authz.md).

- [ ] `SETTLE-004` **[PHASE_5][SOL_HIGH] Submit payments idempotently.**
  - **Depends:** `SETTLE-003`, `CONN-RT-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SETTLE_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SETTLE_004`; `RACE=TestTodo_SETTLE_004_Race`; `INTEGRATION=TestTodo_SETTLE_004_Integration`; `FAULT=TestTodo_SETTLE_004_Fault`; `SECURITY=TestTodo_SETTLE_004_Security`.
  - **RED:** Timeout-after-send never triggers blind duplicate.
  - **GREEN:** Instruction/batch identity, credential lease, attempt and provider response are journaled before observation.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Integration platform](specs/integration-platform.md), [credential leases](specs/secrets-key-custody-and-credential-leases.md).

- [ ] `SETTLE-005` **[PHASE_5][SOL_HIGH] Observe acceptance and settlement separately.**
  - **Depends:** `SETTLE-004`, `INTG-009`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SETTLE_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SETTLE_005`; `RACE=TestTodo_SETTLE_005_Race`; `INTEGRATION=TestTodo_SETTLE_005_Integration`; `FAULT=TestTodo_SETTLE_005_Fault`; `MUTATION=TestTodo_SETTLE_005_Mutation`.
  - **RED:** Seeded defect: acceptance is called settlement or funding and return totals mismatch for SETTLE-005; the primary test must return SETTLE_005_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Accepted/pending/settled/rejected/returned/unknown are distinct evidence states and only authoritative rail observation advances settlement.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Source authority](specs/source-authority-and-external-mastering.md), [integration observations](specs/integration-platform.md).

- [ ] `SETTLE-006` **[PHASE_5][SOL_HIGH] Handle payment return and reversal.**
  - **Depends:** `SETTLE-005`, `TX-010`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SETTLE_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SETTLE_006`; `PROPERTY=TestTodo_SETTLE_006_Property`; `GOLDEN=TestTodo_SETTLE_006_Golden`; `RACE=TestTodo_SETTLE_006_Race`; `INTEGRATION=TestTodo_SETTLE_006_Integration`; `FAULT=TestTodo_SETTLE_006_Fault`; `SECURITY=TestTodo_SETTLE_006_Security`.
  - **RED:** Return/reversal never deletes original payment.
  - **GREEN:** Governed corrective intent updates payable/balance/funding/reporting and communicates without leaking bank data.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Repair](specs/transaction-ledger-reconciliation-and-repair.md), [messaging](specs/messaging-and-notification-plane.md).

- [ ] `SETTLE-007` **[PHASE_5][SOL_HIGH] Reconcile funding, instructions and settlements.**
  - **Depends:** `SETTLE-002`–`SETTLE-006`, `TX-009`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SETTLE_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SETTLE_007`; `RACE=TestTodo_SETTLE_007_Race`; `INTEGRATION=TestTodo_SETTLE_007_Integration`; `FAULT=TestTodo_SETTLE_007_Fault`; `MUTATION=TestTodo_SETTLE_007_Mutation`.
  - **RED:** Seeded defect: acceptance is called settlement or funding and return totals mismatch for SETTLE-007; the primary test must return SETTLE_007_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Totals and per-payment states classify exact settled/rejected/returned/pending/unknown deltas and block payroll completion until policy permits.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Reconciliation](specs/transaction-ledger-reconciliation-and-repair.md), [workflow completion](specs/workflow-runtime.md).

- [ ] `GARN-001` **[CONFORMANCE][SOL_HIGH] Intake and version wage-attachment orders.**
  - **Depends:** `ARTIFACT-005`, `LEGAL-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.PAYROLL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_GARN_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_GARN_001`; `PROPERTY=TestTodo_GARN_001_Property`; `GOLDEN=TestTodo_GARN_001_Golden`; `SECURITY=TestTodo_GARN_001_Security`; `CONFORMANCE=TestTodo_GARN_001_Conformance`; `MUTATION=TestTodo_GARN_001_Mutation`.
  - **RED:** Unverified authority/jurisdiction/person/order/dates/balance/priority/remittance evidence cannot activate.
  - **GREEN:** Original document and extracted fields retain lineage.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Regulatory platform](specs/platform-architecture-catalog.md), [document pipeline](specs/platform-responsibility-boundaries.md).

- [ ] `GARN-002` **[PHASE_5][SOL_HIGH] Determine garnishment priority and concurrent composition.**
  - **Depends:** `GARN-001`, `RULE-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_GARN_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_GARN_002`; `RACE=TestTodo_GARN_002_Race`; `FAULT=TestTodo_GARN_002_Fault`; `SECURITY=TestTodo_GARN_002_Security`; `MUTATION=TestTodo_GARN_002_Mutation`.
  - **RED:** Ambiguous jurisdiction/order type/priority returns review.
  - **GREEN:** Exact legal/rule pack and competing orders determine sequence/concurrency.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Governance composition](specs/governance-decision-and-obligation-composition.md), [rules models](data/models/rules-and-decisions.md).

- [ ] `GARN-003` **[PHASE_5][SOL_HIGH] Calculate disposable earnings and legal limits.**
  - **Depends:** `GARN-002`, `PAYRUN-004`, `MODEL-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_GARN_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_GARN_003`; `RACE=TestTodo_GARN_003_Race`; `FAULT=TestTodo_GARN_003_Fault`; `SECURITY=TestTodo_GARN_003_Security`; `MUTATION=TestTodo_GARN_003_Mutation`.
  - **RED:** Exact earnings/deductions/jurisdiction/rules produce bounded withholding trace.
  - **GREEN:** Missing fact/rule is blocking, never zero/default.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Rewards/payroll models](data/models/rewards-payroll-workforce.md), [fixed decimal](data/models/wire-contract-primitives.md).

- [ ] `GARN-004` **[PHASE_5][SOL_HIGH] Track garnishment balance, arrears and changes.**
  - **Depends:** `GARN-003`, `BAL-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_GARN_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_GARN_004`; `PROPERTY=TestTodo_GARN_004_Property`; `RACE=TestTodo_GARN_004_Race`; `MUTATION=TestTodo_GARN_004_Mutation`.
  - **RED:** Payment/change/release/retro correction posts immutable entries and cannot over-withhold beyond order/rule cap.
  - **GREEN:** Balance explanation is complete.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Balance engine](#38-eligibility-business-cycle-and-balance-engines), [ledger](specs/transaction-ledger-reconciliation-and-repair.md).

- [x] `GARN-005` **[PHASE_5][SOL_HIGH] Generate and authorize garnishment remittance.**
  - **Evidence (2026-09-05):** `TestTodo_GARN_005` in `internal/domains/garnishment` (Wrong destination or changed batch invalidates approval; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/garnishment/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `GARN-003`, `SETTLE-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_GARN_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_GARN_005`; `MUTATION=TestTodo_GARN_005_Mutation`.
  - **RED:** Remittance totals reconcile to payroll withholding and payee/order.
  - **GREEN:** Wrong destination or changed batch invalidates approval.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Settlement engine](#43-deep-payroll-accounting-settlement-and-garnishment-engines), [AuthZ](specs/organization-scope-and-authz.md).

- [ ] `GARN-006` **[PHASE_5][SOL_HIGH] Handle order release, amendment and termination.**
  - **Depends:** `GARN-001`, `LEDGER-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_GARN_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_GARN_006`; `RACE=TestTodo_GARN_006_Race`; `MUTATION=TestTodo_GARN_006_Mutation`.
  - **RED:** Effective-dated amendment/release preserves prior order and prevents deductions outside validity.
  - **GREEN:** Late notice produces correction obligation.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Ledger corrections](specs/transaction-ledger-reconciliation-and-repair.md), [wire time](data/models/wire-contract-primitives.md).

- [ ] `GARN-007` **[PHASE_5][SOL_HIGH] Reconcile withheld and remitted garnishments.**
  - **Depends:** `GARN-004`, `GARN-005`, `TX-009`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_GARN_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_GARN_007`; `PROPERTY=TestTodo_GARN_007_Property`; `RACE=TestTodo_GARN_007_Race`.
  - **RED:** Compare order balance, payroll deductions, remittance and recipient observation by worker/order/period.
  - **GREEN:** Discrepancies create protected repair case.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Reconciliation](specs/transaction-ledger-reconciliation-and-repair.md), [case models](data/models/talent-experience-cases.md).

## 44. Benefits, time, attendance, scheduling and labor-cost engines

> **Disposition (2026-09-02):** DEFERRED; a separate product line with its own gate.

- [x] `BEN-001` **[CONFORMANCE][SOL_HIGH] Define Plan, PlanYear and immutable revisions.**
  - **Evidence (2026-09-05):** `TestTodo_BEN_001` in `internal/domains/benefits` (Model covers carrier, coverage tiers, rates, eligibility, enrollment windows, life events, dependents, deductions, continuation and authority without activating writes; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/benefits/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PROGRAM-CONF-001`, `CYCLE-001`, `CONF-010`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.REWARDS; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_BEN_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_BEN_001`; `PROPERTY=TestTodo_BEN_001_Property`; `CONFORMANCE=TestTodo_BEN_001_Conformance`; `MUTATION=TestTodo_BEN_001_Mutation`.
  - **RED:** Seeded defect: an overlapping plan window or out-of-window election is accepted for BEN-001; the primary test must return BEN_001_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Model covers carrier, coverage tiers, rates, eligibility, enrollment windows, life events, dependents, deductions, continuation and authority without activating writes.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Rewards/benefits models](data/models/rewards-payroll-workforce.md), [intent coverage](data/models/intent-coverage-matrix.md).

- [x] `BEN-002` **[PHASE_4][SOL_HIGH] Define and resolve enrollment windows.**
  - **Evidence:** Luna implementation, Sol refinement, and integration review on 2026-09-07: `TestTodo_BEN_002`, `TestTodo_BEN_002_Property`, `TestTodo_BEN_002_Mutation` and typed rejection/calendar/boundary regressions in `internal/domains/benefits`; `go test -count=1 -p 1 -cover ./internal/domains/benefits/ ./internal/domains/attendance/` PASS on windows/arm64 (benefits 78.3%); `go vet ./internal/domains/benefits/ ./internal/domains/attendance/` PASS. Enrollment resolution has no persistence, event, outbox, work-item or provider ports; zero effects are structural at this domain-support boundary. No deployed enrollment workflow is claimed.
  - **Depends:** `BEN-001`, `CYCLE-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_BEN_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_BEN_002`; `PROPERTY=TestTodo_BEN_002_Property`; `MUTATION=TestTodo_BEN_002_Mutation`.
  - **RED:** Seeded defect: an overlapping plan window or out-of-window election is accepted for BEN-002; the primary test must return BEN_002_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Open enrollment/new hire/life-event/correction windows resolve exact start/end/calendar/reason and return closed/conditional/unknown rather than ambient acceptance.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Business cycles](#38-eligibility-business-cycle-and-balance-engines), [wire time](data/models/wire-contract-primitives.md).

- [ ] `BEN-003` **[PHASE_4][SOL_HIGH] Evaluate benefit eligibility.**
  - **Depends:** `BEN-001`, `ELIG-008`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_BEN_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_BEN_003`; `PROPERTY=TestTodo_BEN_003_Property`; `SECURITY=TestTodo_BEN_003_Security`.
  - **RED:** Exact worker/plan/date/population/rules returns typed eligibility and evidence.
  - **GREEN:** Missing hours/classification/jurisdiction is conditional/unknown.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Eligibility engine](#38-eligibility-business-cycle-and-balance-engines), [rewards models](data/models/rewards-payroll-workforce.md).

- [ ] `BEN-004` **[PHASE_4][SOL_HIGH] Resolve coverage tiers and dependent qualification.**
  - **Depends:** `BEN-003`, `QUAL-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_BEN_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_BEN_004`; `PROPERTY=TestTodo_BEN_004_Property`; `FAULT=TestTodo_BEN_004_Fault`; `MUTATION=TestTodo_BEN_004_Mutation`.
  - **RED:** Tier/dependent relation/evidence/age/student/disability rules are exact and effective-dated.
  - **GREEN:** Unsupported/ambiguous dependent is not silently covered.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [People models](data/models/people-workforce.md), [qualification engine](#39-qualification-demand-matching-and-scenario-engines).

- [ ] `BEN-005` **[PHASE_4][SOL_HIGH] Record election revisions append-only.**
  - **Depends:** `BEN-002`–`BEN-004`, `TX-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_BEN_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_BEN_005`; `PROPERTY=TestTodo_BEN_005_Property`; `RECOVERY=TestTodo_BEN_005_Recovery`; `MUTATION=TestTodo_BEN_005_Mutation`.
  - **RED:** Stale window/plan/rate/dependent or changed replay fails.
  - **GREEN:** Election binds plan/tier/dependents/effective date/cost/waiver evidence and never overwrites prior revision.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Transaction coordinator](specs/transaction-plan-and-commit-coordinator.md), [ledger](specs/transaction-ledger-reconciliation-and-repair.md).

- [ ] `BEN-006` **[PHASE_4][SOL_HIGH] Process qualifying life events.**
  - **Depends:** `BEN-002`, `BEN-005`, `ARTIFACT-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_BEN_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_BEN_006`; `PROPERTY=TestTodo_BEN_006_Property`.
  - **RED:** Event type/date/evidence/window determines permitted election delta.
  - **GREEN:** Late/missing/uncertain evidence creates review and preserves existing coverage.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Workflow runtime](specs/workflow-runtime.md), [document pipeline](specs/platform-responsibility-boundaries.md).

- [ ] `BEN-007` **[PHASE_4][SOL_HIGH] Calculate payroll benefit deductions.**
  - **Depends:** `BEN-005`, `BAL-003`, `MODEL-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_BEN_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_BEN_007`; `PROPERTY=TestTodo_BEN_007_Property`; `RACE=TestTodo_BEN_007_Race`; `MUTATION=TestTodo_BEN_007_Mutation`.
  - **RED:** Plan/rate/frequency/coverage/arrears inputs yield exact employee/employer amounts and trace.
  - **GREEN:** Retro corrections create deltas, not overwritten totals.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Balance engine](#38-eligibility-business-cycle-and-balance-engines), [rewards models](data/models/rewards-payroll-workforce.md).

- [ ] `BEN-008` **[PHASE_4][SOL_HIGH] Reconcile carrier coverage and payroll deductions.**
  - **Depends:** `BEN-005`, `BEN-007`, `INTG-009`, `TX-009`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_BEN_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_BEN_008`; `PROPERTY=TestTodo_BEN_008_Property`; `MUTATION=TestTodo_BEN_008_Mutation`.
  - **RED:** Compare expected election/dependents/effective dates/rates/deductions with carrier and payroll observations.
  - **GREEN:** Partial/stale/unknown creates repair.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Integration platform](specs/integration-platform.md), [reconciliation](specs/transaction-ledger-reconciliation-and-repair.md).

- [x] `CLOCK-001` **[CONFORMANCE][SOL_HIGH] Register governed time devices and sources.**
  - **Evidence (2026-09-05):** `TestTodo_CLOCK_001` in `internal/domains/clock` (Device/source missing owner, location, clock trust, offline/replay/signature, firmware/cert and retention policy cannot submit authoritative observations; written by an earlier lane (Luna or Sonnet) and verified independently); `go test -count=1 ./internal/domains/clock/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CONF-011`, `AUTHN-006`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.WORKFORCE; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_CLOCK_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CLOCK_001`; `PROPERTY=TestTodo_CLOCK_001_Property`; `CONFORMANCE=TestTodo_CLOCK_001_Conformance`; `RECOVERY=TestTodo_CLOCK_001_Recovery`; `MUTATION=TestTodo_CLOCK_001_Mutation`.
  - **RED:** Seeded defect: a revoked source or duplicate signed punch posts for CLOCK-001; the primary test must return CLOCK_001_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Device/source missing owner, location, clock trust, offline/replay/signature, firmware/cert and retention policy cannot submit authoritative observations.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [People/workforce models](data/models/people-workforce.md), [security models](data/models/security-trust.md).

- [x] `CLOCK-002` **[PHASE_4][SOL_HIGH] Authenticate device and worker at capture.**
  - **Evidence (2026-09-05):** `TestTodo_CLOCK_002` in `internal/domains/clock` (Evidence separates device, principal and claimed worker; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/clock/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CLOCK-001`, `AUTHN-006`, `TRUST-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CLOCK_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CLOCK_002`; `PROPERTY=TestTodo_CLOCK_002_Property`.
  - **RED:** Shared/expired/revoked device proof, unresolved worker or prohibited location fails.
  - **GREEN:** Evidence separates device, principal and claimed worker.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Identity resolution](specs/identity-resolution-and-entity-linkage.md), [AuthZ](specs/organization-scope-and-authz.md).

- [ ] `CLOCK-003` **[PHASE_4][SOL_HIGH] Capture signed TimeObservation.**
  - **Depends:** `CLOCK-002`, `TIME-001`, `LEDGER-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CLOCK_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CLOCK_003`; `PROPERTY=TestTodo_CLOCK_003_Property`; `MUTATION=TestTodo_CLOCK_003_Mutation`.
  - **RED:** Observation binds event type, device/worker, occurred/recorded time, timezone/location evidence and signature.
  - **GREEN:** Caller cannot supply trusted recorded time.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Wire time](data/models/wire-contract-primitives.md), [ledger](specs/transaction-ledger-reconciliation-and-repair.md).

- [ ] `CLOCK-004` **[PHASE_4][SOL_HIGH] Handle offline store-and-forward.**
  - **Depends:** `CLOCK-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CLOCK_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CLOCK_004`; `PROPERTY=TestTodo_CLOCK_004_Property`; `RECOVERY=TestTodo_CLOCK_004_Recovery`.
  - **RED:** Lost power/reorder/replay/clock drift/tamper fixture preserves local sequence and signed occurred time.
  - **GREEN:** Sync marks confidence/skew and never rewrites receipt order.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Operations models](data/models/operations-production.md), [source authority](specs/source-authority-and-external-mastering.md).

- [ ] `CLOCK-005` **[PHASE_4][SOL_HIGH] Deduplicate and reconcile device/backend observations.**
  - **Depends:** `CLOCK-003`, `CLOCK-004`, `TX-009`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CLOCK_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CLOCK_005`; `PROPERTY=TestTodo_CLOCK_005_Property`; `RACE=TestTodo_CLOCK_005_Race`; `RECOVERY=TestTodo_CLOCK_005_Recovery`; `MUTATION=TestTodo_CLOCK_005_Mutation`.
  - **RED:** Seeded defect: a revoked source or duplicate signed punch posts for CLOCK-005; the primary test must return CLOCK_005_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Identical replay returns original receipt, changed replay is conflict and missing/extra/reordered device observations create exception without guessing.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Reconciliation](specs/transaction-ledger-reconciliation-and-repair.md), [data quality](specs/data-quality-and-invariant-evaluation.md).

- [ ] `CLOCK-006` **[PHASE_4][SOL_HIGH] Correct prior time punches append-only.**
  - **Depends:** `CLOCK-003`, `ATTEST-006`, `LEDGER-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CLOCK_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CLOCK_006`; `PROPERTY=TestTodo_CLOCK_006_Property`; `MUTATION=TestTodo_CLOCK_006_Mutation`.
  - **RED:** Correction binds original observation, reason, attestation/approval and recalculation impacts.
  - **GREEN:** Original signed observation remains immutable.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Attestation](#40-attestation-and-conformance-gated-program-engines), [ledger corrections](specs/transaction-ledger-reconciliation-and-repair.md).

- [x] `ATTEND-001` **[CONFORMANCE][SOL_HIGH] Define versioned attendance evaluation.**
  - **Evidence (2026-09-07):** `TestTodo_ATTEND_001`, `TestTodo_ATTEND_001_Conformance`, `TestTodo_ATTEND_001_Mutation`, `TestTodo_ATTEND_001_Property`, `TestTodo_ATTEND_001_Security` in `internal/domains/attendance` (written in 171cafc on 2026-09-03 and never ticked; the named tests prove the GREEN contract: missing evidence yields unknown/exception, not compliant); `go test -count=1 ./internal/domains/attendance/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CLOCK-003`, `RULE-003`, `CONF-011`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.WORKFORCE; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_ATTEND_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ATTEND_001`; `PROPERTY=TestTodo_ATTEND_001_Property`; `SECURITY=TestTodo_ATTEND_001_Security`; `CONFORMANCE=TestTodo_ATTEND_001_Conformance`; `MUTATION=TestTodo_ATTEND_001_Mutation`.
  - **RED:** Evaluation pins schedule, punches, breaks, jurisdiction/rules, tolerance and effective context.
  - **GREEN:** Missing evidence yields unknown/exception, not compliant.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [People/workforce models](data/models/people-workforce.md), [rules models](data/models/rules-and-decisions.md).

- [x] `ATTEND-002` **[PHASE_4][SOL_HIGH] Detect late, early, missing and unscheduled work.**
  - **Evidence:** Luna implementation, repeated Sol refinement, and integration review on 2026-09-07: `TestTodo_ATTEND_002`, `TestTodo_ATTEND_002_Property`, `TestTodo_ATTEND_002_Mutation`, permutation and typed-direction regressions in `internal/domains/attendance`; `go test -count=1 -p 1 -cover ./internal/domains/attendance/` PASS on windows/arm64 (87.3%); `go vet ./internal/domains/attendance/` completed with only the documented Windows executable-cleanup warning. Tests pin DST/split boundaries, exact rejection context, permutation-stable digests, IN/OUT sequencing, and zero-minute missing-side markers without double-counting known absence. The pure domain has no persistence/effect ports; production workflow routing remains separate.
  - **Depends:** `ATTEND-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ATTEND_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ATTEND_002`; `PROPERTY=TestTodo_ATTEND_002_Property`; `MUTATION=TestTodo_ATTEND_002_Mutation`.
  - **RED:** Seeded defect: a pinned schedule or DST boundary produces missing or duplicate exception for ATTEND-002; the primary test must return ATTEND_002_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Boundary/DST/overnight/split-shift fixtures return exact exception interval/minutes/reason and do not double count.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Wire time](data/models/wire-contract-primitives.md), [data quality](specs/data-quality-and-invariant-evaluation.md).

- [ ] `ATTEND-003` **[PHASE_4][SOL_HIGH] Detect meal, break and overtime exceptions.**
  - **Depends:** `ATTEND-001`, `LEGAL-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ATTEND_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ATTEND_003`; `PROPERTY=TestTodo_ATTEND_003_Property`; `GOLDEN=TestTodo_ATTEND_003_Golden`; `SECURITY=TestTodo_ATTEND_003_Security`; `MUTATION=TestTodo_ATTEND_003_Mutation`.
  - **RED:** Applicable legal/CBA/company composition and waiver/attestation evidence determine exception.
  - **GREEN:** Unknown jurisdiction/rule never reports compliant.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Governance composition](specs/governance-decision-and-obligation-composition.md), [attestation](#40-attestation-and-conformance-gated-program-engines).

- [ ] `ATTEND-004` **[PHASE_4][SOL_HIGH] Route and resolve attendance exceptions.**
  - **Depends:** `ATTEND-002`, `ATTEND-003`, `WORK-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ATTEND_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ATTEND_004`; `PROPERTY=TestTodo_ATTEND_004_Property`; `MUTATION=TestTodo_ATTEND_004_Mutation`.
  - **RED:** Exception creates scoped human work with evidence/deadline/SoD.
  - **GREEN:** Correction/resolution reevaluates affected interval and preserves prior result.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Human work](specs/human-work-forms-and-rules.md), [workflow runtime](specs/workflow-runtime.md).

- [ ] `ATTEND-005` **[PHASE_4][SOL_HIGH] Recalculate downstream time and payroll after resolution.**
  - **Depends:** `ATTEND-004`, `BAL-006`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ATTEND_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ATTEND_005`; `PROPERTY=TestTodo_ATTEND_005_Property`.
  - **RED:** Dependency graph identifies hours/overtime/balances/pay inputs.
  - **GREEN:** Recalculation posts deltas once and reconciliation verifies all consumers.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Balance engine](#38-eligibility-business-cycle-and-balance-engines), [repair](specs/transaction-ledger-reconciliation-and-repair.md).

- [x] `SCHED-OPT-001` **[CONFORMANCE][SOL_HIGH] Define bounded workforce optimization problem.**
  - **Evidence (2026-09-05):** `TestTodo_SCHED_OPT_001` in `internal/domains/schedopt` (bounded workforce optimization problem definition with a closed objective set, weighted hard and soft constraints, decision variables over a frozen candidate population and demand windows, declared size bounds, unbounded or unknown-constraint problems refused, canonical digest, feasibility pre-check reporting unmeetable hard constraints, no solver; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/schedopt/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DEMAND-001`, `MATCH-001`, `CONF-011`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.WORKFORCE; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_SCHED_OPT_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SCHED_OPT_001`; `PROPERTY=TestTodo_SCHED_OPT_001_Property`; `RACE=TestTodo_SCHED_OPT_001_Race`; `FAULT=TestTodo_SCHED_OPT_001_Fault`; `SECURITY=TestTodo_SCHED_OPT_001_Security`; `CONFORMANCE=TestTodo_SCHED_OPT_001_Conformance`; `MUTATION=TestTodo_SCHED_OPT_001_Mutation`.
  - **RED:** Problem pins demand, candidate population, qualification/legal/fatigue constraints, preferences, costs, objective weights, horizon, solver/version and limits.
  - **GREEN:** No hidden objective.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [People/workforce models](data/models/people-workforce.md), [rules models](data/models/rules-and-decisions.md).

- [x] `SCHED-OPT-002` **[PHASE_4][SOL_HIGH] Import frozen demand and candidate workforce.**
  - **Evidence (2026-09-05):** `TestTodo_SCHED_OPT_002` in `internal/domains/schedopt` (a problem definition bound to exactly one frozen demand aggregation digest and one frozen candidate population digest at a declared as-of into an immutable digested ProblemInstance with deterministically enumerated candidate by window variables filtered by availability, unfrozen or superseded inputs, an out-of-validity as-of or a variable count above the bound refused by name, order-independent digests by property, Explain listing digests and counts only; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/schedopt/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SCHED-OPT-001`, `DEMAND-002`, `MATCH-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SCHED_OPT_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SCHED_OPT_002`; `PROPERTY=TestTodo_SCHED_OPT_002_Property`; `GOLDEN=TestTodo_SCHED_OPT_002_Golden`; `RACE=TestTodo_SCHED_OPT_002_Race`; `FAULT=TestTodo_SCHED_OPT_002_Fault`; `MUTATION=TestTodo_SCHED_OPT_002_Mutation`.
  - **RED:** Seeded defect: a DST fold or stale frozen input produces duplicate or wrong scheduling state for SCHED-OPT-002; the primary test must return SCHED_OPT_002_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Mismatched/stale/partial snapshots block final optimization and remain explicit in candidate schedule.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Demand/matching](#39-qualification-demand-matching-and-scenario-engines), [population engine](#37-shared-transformation-and-population-engines).

- [ ] `SCHED-OPT-003` **[PHASE_4][SOL_HIGH] Apply qualification, legal and fatigue hard constraints.**
  - **Depends:** `SCHED-OPT-002`, `QUAL-004`, `LEGAL-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SCHED_OPT_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SCHED_OPT_003`; `PROPERTY=TestTodo_SCHED_OPT_003_Property`; `RACE=TestTodo_SCHED_OPT_003_Race`; `FAULT=TestTodo_SCHED_OPT_003_Fault`; `SECURITY=TestTodo_SCHED_OPT_003_Security`; `MUTATION=TestTodo_SCHED_OPT_003_Mutation`.
  - **RED:** Solver cannot violate hard constraint to improve score.
  - **GREEN:** Unsatisfiable problem returns exact conflict set/coverage gaps.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Qualification engine](#39-qualification-demand-matching-and-scenario-engines), [governance composition](specs/governance-decision-and-obligation-composition.md).

- [ ] `SCHED-OPT-004` **[PHASE_4][SOL_HIGH] Optimize preferences and cost deterministically.**
  - **Depends:** `SCHED-OPT-003`, `MODEL-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SCHED_OPT_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SCHED_OPT_004`; `PROPERTY=TestTodo_SCHED_OPT_004_Property`; `GOLDEN=TestTodo_SCHED_OPT_004_Golden`; `RACE=TestTodo_SCHED_OPT_004_Race`; `FAULT=TestTodo_SCHED_OPT_004_Fault`.
  - **RED:** Same problem/seed/solver returns identical schedule/score.
  - **GREEN:** Weights, tie-breaks, approximations and optimality gap are reported.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Fixed decimal](data/models/wire-contract-primitives.md), [assurance models](data/models/assurance-intelligence-platform.md).

- [ ] `SCHED-OPT-005` **[PHASE_4][SOL_HIGH] Explain candidate schedule assignments.**
  - **Depends:** `SCHED-OPT-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SCHED_OPT_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SCHED_OPT_005`; `PROPERTY=TestTodo_SCHED_OPT_005_Property`; `RACE=TestTodo_SCHED_OPT_005_Race`; `FAULT=TestTodo_SCHED_OPT_005_Fault`; `MUTATION=TestTodo_SCHED_OPT_005_Mutation`.
  - **RED:** Seeded defect: a DST fold or stale frozen input produces duplicate or wrong scheduling state for SCHED-OPT-005; the primary test must return SCHED_OPT_005_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Each assigned/unassigned demand and worker traces hard constraints, preferences, cost and alternatives without disclosing other workers' protected facts.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Provenance](specs/provenance-graph-and-lineage.md), [DLP](specs/data-classification-and-dlp.md).

- [ ] `SCHED-OPT-006` **[PHASE_4][SOL_HIGH] Human-review, modify and approve candidate schedules.**
  - **Depends:** `SCHED-OPT-005`, `WORK-001`, `APPROVAL-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SCHED_OPT_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SCHED_OPT_006`; `PROPERTY=TestTodo_SCHED_OPT_006_Property`; `RACE=TestTodo_SCHED_OPT_006_Race`; `FAULT=TestTodo_SCHED_OPT_006_Fault`; `MUTATION=TestTodo_SCHED_OPT_006_Mutation`.
  - **RED:** Manual change is revalidated against hard constraints and records reason/authority.
  - **GREEN:** Approval binds exact schedule revision.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Human work](specs/human-work-forms-and-rules.md), [workflow runtime](specs/workflow-runtime.md).

- [ ] `SCHED-OPT-007` **[PHASE_4][SOL_HIGH] Publish and reconcile schedules.**
  - **Depends:** `SCHED-OPT-006`, `TX-008`, `TX-009`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SCHED_OPT_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SCHED_OPT_007`; `PROPERTY=TestTodo_SCHED_OPT_007_Property`; `RACE=TestTodo_SCHED_OPT_007_Race`; `FAULT=TestTodo_SCHED_OPT_007_Fault`; `MUTATION=TestTodo_SCHED_OPT_007_Mutation`.
  - **RED:** Publication creates governed assignments/messages/integration effects once.
  - **GREEN:** Observed schedule/demand coverage and failures remain multidimensional and repairable.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Transaction coordinator](specs/transaction-plan-and-commit-coordinator.md), [messaging](specs/messaging-and-notification-plane.md).

- [x] `LABOR-001` **[CONFORMANCE][SOL_HIGH] Define labor-cost dimensions and rule versions.**
  - **Evidence (2026-09-05):** `TestTodo_LABOR_001` in `internal/domains/labor` (closed labor-cost dimension vocabulary with validation, effective-dated digested LaborRule versions superseded only by new versions, exact-decimal allocations summing to 100 percent, and Explain; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/labor/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-003`, `CONF-011`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.WORKFORCE; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_LABOR_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LABOR_001`; `PROPERTY=TestTodo_LABOR_001_Property`; `GOLDEN=TestTodo_LABOR_001_Golden`; `CONFORMANCE=TestTodo_LABOR_001_Conformance`; `MUTATION=TestTodo_LABOR_001_Mutation`.
  - **RED:** Seeded defect: currency version or exact component is omitted while totals pass for LABOR-001; the primary test must return LABOR_001_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Definition covers worker/time, entity, cost center/project/customer/location/job/earning, currency, rates, differentials, overtime and employer burden with effective intervals.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [People/workforce models](data/models/people-workforce.md), [rewards models](data/models/rewards-payroll-workforce.md).

- [ ] `LABOR-002` **[PHASE_4][SOL_HIGH] Allocate worked time exactly across dimensions.**
  - **Depends:** `LABOR-001`, `CLOCK-006`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LABOR_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LABOR_002`; `PROPERTY=TestTodo_LABOR_002_Property`.
  - **RED:** Allocations sum to source duration/amount under declared rounding and preserve residual.
  - **GREEN:** Stale/invalid dimension or overlap fails.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Fixed decimal](data/models/wire-contract-primitives.md), [organization relationships](specs/organization-and-relationship-domain.md).

- [ ] `LABOR-003` **[PHASE_4][SOL_HIGH] Calculate wages, overtime and differentials.**
  - **Depends:** `LABOR-002`, `ATTEND-003`, `LEGAL-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LABOR_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LABOR_003`; `PROPERTY=TestTodo_LABOR_003_Property`; `RACE=TestTodo_LABOR_003_Race`; `SECURITY=TestTodo_LABOR_003_Security`; `MUTATION=TestTodo_LABOR_003_Mutation`.
  - **RED:** Exact time/rate/rules return component trace.
  - **GREEN:** Unknown jurisdiction/rate/rule blocks authoritative cost.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Regulatory platform](specs/platform-architecture-catalog.md), [rules models](data/models/rules-and-decisions.md).

- [ ] `LABOR-004` **[PHASE_4][SOL_HIGH] Calculate employer burden and total labor cost.**
  - **Depends:** `LABOR-003`, `BAL-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LABOR_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LABOR_004`; `PROPERTY=TestTodo_LABOR_004_Property`; `MUTATION=TestTodo_LABOR_004_Mutation`.
  - **RED:** Seeded defect: currency version or exact component is omitted while totals pass for LABOR-004; the primary test must return LABOR_004_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Taxes/benefits/allowances/overheads identify basis/rate/cap/period and yield exact component totals with unknown ranges where inputs are incomplete.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Rewards models](data/models/rewards-payroll-workforce.md), [balance engine](#38-eligibility-business-cycle-and-balance-engines).

- [ ] `LABOR-005` **[PHASE_4][SOL_HIGH] Export and reconcile labor costing.**
  - **Depends:** `LABOR-004`, `PAYGL-003`, `INTG-009`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LABOR_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LABOR_005`; `PROPERTY=TestTodo_LABOR_005_Property`; `GOLDEN=TestTodo_LABOR_005_Golden`.
  - **RED:** Canonical allocations/costs map to Finance/Payroll without lost dimensions.
  - **GREEN:** External totals/lines/unknowns reconcile or create repair.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Integration platform](specs/integration-platform.md), [reconciliation](specs/transaction-ledger-reconciliation-and-repair.md).

## 45. Talent, learning, survey, recruiting and appointment engines

> **Disposition (2026-09-02):** DEFERRED, except `RESERVE-001` which P1B needs for the compensation-pool reservation.

- [x] `RESERVE-001` **[GATE_B][SOL_HIGH] Define the shared resource-reservation protocol.**
  - **Depends:** `MODEL-004`, `TRUST-006`, `TIME-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_RESERVE_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_RESERVE_001`; `PROPERTY=TestTodo_RESERVE_001_Property`; `GOLDEN=TestTodo_RESERVE_001_Golden`; `RACE=TestTodo_RESERVE_001_Race`; `BENCHMARK=BenchmarkTodo_RESERVE_001`; `MUTATION=TestTodo_RESERVE_001_Mutation`; `FAULT=TestTodo_RESERVE_001_Fault`.
  - **RED:** proposal/resource/version/quantity/interval/owner/priority/expiry is absent, two competing acquires win, stale fence consumes, changed proposal reuses hold, or release/expiry/consume happens twice.
  - **GREEN:** typed `Reservation` lifecycle binds exact proposal digest, authority and capacity; acquire is CAS/fenced, `HELD -> CONSUMED|RELEASED|EXPIRED` is append-only/idempotent, and ambiguity creates observation/reconciliation obligation.
  - **REFACTOR:** Position and Budget own domain validation while sharing lifecycle/fencing primitives; protocol does not invent remote ACID.
  - **Refs:** [Position reservation](specs/position-and-headcount-domain.md), [transaction coordinator](specs/transaction-plan-and-commit-coordinator.md).
  - **Evidence (2026-09-03):** `TestTodo_RESERVE_001`, Declared `RESERVE-001` test matrix and acceptance behavior in `internal/resource/reservation`; PASS; `go test -p 1 ./internal/resource/reservation` and `go vet ./internal/resource/reservation` on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `PERFORMANCE-001` **[CONFORMANCE][SOL_HIGH] Define PerformanceCycle and revisions.**
  - **Evidence (2026-09-05):** `TestTodo_PERFORMANCE_001` in `internal/domains/performance` (PerformanceCycle with an immutable revision lifecycle PLANNED/OPEN/CALIBRATING/CLOSED/LOCKED as new revisions, population and calendar binding refs, rating scale version and calibration evidence refs, out-of-order and lock-without-close refused, canonical digest and Explain; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/performance/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CYCLE-001`, `CONF-012`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.TALENT; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_PERFORMANCE_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERFORMANCE_001`; `PROPERTY=TestTodo_PERFORMANCE_001_Property`; `CONFORMANCE=TestTodo_PERFORMANCE_001_Conformance`; `MUTATION=TestTodo_PERFORMANCE_001_Mutation`.
  - **RED:** Seeded defect: a published cycle or rating history mutates in place for PERFORMANCE-001; the primary test must return PERFORMANCE_001_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Model covers population, reviewer graph, forms/goals/feedback, proposed/calibrated/final ratings, contest/correction, confidentiality and outcomes without Phase 1 authority.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Talent models](data/models/talent-experience-cases.md), [intent coverage](data/models/intent-coverage-matrix.md).

- [x] `PERFORMANCE-002` **[PHASE_4][SOL_HIGH] Freeze participant and reviewer graph.**
  - **Evidence (2026-09-05):** `TestTodo_PERFORMANCE_002` in `internal/domains/performance` (frozen participant and reviewer graph per cycle revision from a population binding ref with reviewers resolved per participant by relationship kind, self-review and reviewer-is-participant conflicts refused per the rules, digested and immutable after freeze, amendments only as a new frozen revision with lineage; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/performance/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PERFORMANCE-001`, `POP-005`, `MODEL-013`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PERFORMANCE_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERFORMANCE_002`; `PROPERTY=TestTodo_PERFORMANCE_002_Property`; `GOLDEN=TestTodo_PERFORMANCE_002_Golden`; `SECURITY=TestTodo_PERFORMANCE_002_Security`.
  - **RED:** Cycles/invalid reviewers/SoD/unauthorized scope or mutable graph fails.
  - **GREEN:** Snapshot supports explicit late/reassignment policy.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Relationship map](data/models/relationship-map.md), [population engine](#37-shared-transformation-and-population-engines).

- [x] `PERFORMANCE-003` **[PHASE_4][SOL_HIGH] Collect reviews and 360 feedback.**
  - **Evidence (2026-09-05):** `TestTodo_PERFORMANCE_003` in `internal/domains/performance` (review and 360 feedback collection accepted only from reviewers in the frozen graph for a participant, one review per reviewer/participant/cycle revision with resubmission as a new revision, narrative held as a digest, anonymous relationships compartmented from the participant, collection closed at the phase cutoff, Explain; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/performance/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PERFORMANCE-002`, `FORM-005`, `WORK-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PERFORMANCE_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERFORMANCE_003`; `MUTATION=TestTodo_PERFORMANCE_003_Mutation`.
  - **RED:** Submissions bind form/reviewer/subject/cycle revision, confidentiality and trusted time.
  - **GREEN:** Drafts/final/corrections remain distinct and append-only.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Human work/forms](specs/human-work-forms-and-rules.md), [talent models](data/models/talent-experience-cases.md).

- [x] `PERFORMANCE-004` **[PHASE_4][SOL_HIGH] Calculate proposed ratings transparently.**
  - **Evidence (2026-09-05):** `TestTodo_PERFORMANCE_004` in `internal/domains/performance` (proposed ratings per participant from collected reviews under a versioned rule with weights per relationship kind, minimum review count and closed outlier handling in exact decimal, Explain listing every contributing review by reference and weight, shortfalls refused by name, identical inputs yielding an identical digest; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/performance/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PERFORMANCE-003`, `RULE-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PERFORMANCE_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERFORMANCE_004`; `PROPERTY=TestTodo_PERFORMANCE_004_Property`.
  - **RED:** Weights/scales/missing inputs/rounding and overrides are explicit.
  - **GREEN:** No opaque agent output becomes rating and exact inputs reproduce result.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Rules models](data/models/rules-and-decisions.md), [agent/intelligence models](data/models/assurance-intelligence-platform.md).

- [x] `PERFORMANCE-005` **[PHASE_4][SOL_HIGH] Perform governed calibration.**
  - **Evidence (2026-09-05):** `TestTodo_PERFORMANCE_005` in `internal/domains/performance` (governed calibration sessions over a cohort with a distinct facilitator, adjustments as digested events naming participant, from and to rating, a closed reason code and the adjuster, separation of duties so a manager is never the sole adjuster of their report, a rule-bound magnitude cap, and calibrated ratings keeping the proposed rating and every adjustment in history; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/performance/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PERFORMANCE-004`, `WORK-001`, `TRUST-014`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PERFORMANCE_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERFORMANCE_005`; `SECURITY=TestTodo_PERFORMANCE_005_Security`; `MUTATION=TestTodo_PERFORMANCE_005_Mutation`.
  - **RED:** Calibration changes require authority, reason, comparison/privacy limits and immutable decision.
  - **GREEN:** Forced distribution cannot silently override policy/legal restrictions.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Governance composition](specs/governance-decision-and-obligation-composition.md), [DLP](specs/data-classification-and-dlp.md).

- [x] `PERFORMANCE-006` **[PHASE_4][SOL_HIGH] Contest, correct and finalize ratings.**
  - **Evidence (2026-09-05):** `TestTodo_PERFORMANCE_006` in `internal/domains/performance` (contests raised within a declared window with a closed reason code, corrections decided by a reviewer distinct from the manager and every calibration adjuster (uphold, correct or refer), finalization into an immutable digested FinalRating carrying the full proposed, calibrated, contested and corrected history, finalization refused while a contest is open or before the cutoff and a second contest refused after finalization, every transition a typed digested event; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/performance/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PERFORMANCE-005`, `LEDGER-005`, `APPROVAL-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PERFORMANCE_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERFORMANCE_006`; `RACE=TestTodo_PERFORMANCE_006_Race`; `MUTATION=TestTodo_PERFORMANCE_006_Mutation`.
  - **RED:** Contest/correction never rewrites submitted opinion.
  - **GREEN:** Finalization binds exact evidence/decisions and unresolved case/appeal policy.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Ledger corrections](specs/transaction-ledger-reconciliation-and-repair.md), [case models](data/models/talent-experience-cases.md).

- [x] `PERFORMANCE-007` **[PHASE_4][SOL_HIGH] Link outcomes without rewriting opinion history.**
  - **Evidence (2026-09-05):** `TestTodo_PERFORMANCE_007` in `internal/domains/performance` (append-only digested OutcomeLink records binding a FINAL rating by digest to downstream outcomes from a closed kind set with a linking principal and effective instant, non-final or superseded ratings refused, unlinking as a new UNLINK record with a reason, opinion records and outcome links returned as separate ordered streams, property that linking never changes any opinion digest; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/performance/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PERFORMANCE-006`, `MODEL-020`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PERFORMANCE_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERFORMANCE_007`; `MUTATION=TestTodo_PERFORMANCE_007_Mutation`.
  - **RED:** Seeded defect: a published cycle or rating history mutates in place for PERFORMANCE-007; the primary test must return PERFORMANCE_007_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Promotion/compensation/learning/outcome links remain separate transactions/observations and cannot retroactively make prior assessment factual.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Provenance](specs/provenance-graph-and-lineage.md), [outcome models](data/models/assurance-intelligence-platform.md).

- [ ] `LEARN-001` **[CONFORMANCE][SOL_HIGH] Define Course, CourseVersion and LearningPath.**
  - **Depends:** `PROGRAM-CONF-001`, `CONF-013`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.TALENT; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_LEARN_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEARN_001`; `INTEGRATION=TestTodo_LEARN_001_Integration`; `SECURITY=TestTodo_LEARN_001_Security`; `CONFORMANCE=TestTodo_LEARN_001_Conformance`; `BROWSER=TestTodo_LEARN_001_Browser`; `MUTATION=TestTodo_LEARN_001_Mutation`.
  - **RED:** Seeded defect: a mutable definition or stale LMS completion becomes credential truth for LEARN-001; the primary test must return LEARN_001_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Model covers provider/content, prerequisites, locale/accessibility, assignment/enrollment, assessment, completion, credential, expiry/renewal and external authority.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Talent models](data/models/talent-experience-cases.md), [intent coverage](data/models/intent-coverage-matrix.md).

- [ ] `LEARN-002` **[PHASE_4][SOL_HIGH] Resolve prerequisites and learning eligibility.**
  - **Depends:** `LEARN-001`, `ELIG-008`, `QUAL-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEARN_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEARN_002`; `PROPERTY=TestTodo_LEARN_002_Property`.
  - **RED:** Missing/expired/unknown prerequisite returns conditional/blocked/unknown with explanation.
  - **GREEN:** Equivalencies reference exact rule/evidence.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Eligibility engine](#38-eligibility-business-cycle-and-balance-engines), [qualification](#39-qualification-demand-matching-and-scenario-engines).

- [ ] `LEARN-003` **[PHASE_4][SOL_HIGH] Assign and enroll learners.**
  - **Depends:** `LEARN-002`, `PROGRAM-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEARN_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEARN_003`; `BENCHMARK=BenchmarkTodo_LEARN_003`.
  - **RED:** Duplicate/stale assignment or invalid capacity/window fails.
  - **GREEN:** Assignment records requirement source, due date, waiver/escalation and participation state.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Program engine](#40-attestation-and-conformance-gated-program-engines), [workflow runtime](specs/workflow-runtime.md).

- [ ] `LEARN-004` **[PHASE_4][SOL_HIGH] Receive authoritative learning completion.**
  - **Depends:** `LEARN-003`, `INTG-009`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEARN_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEARN_004`; `RACE=TestTodo_LEARN_004_Race`; `INTEGRATION=TestTodo_LEARN_004_Integration`; `MUTATION=TestTodo_LEARN_004_Mutation`.
  - **RED:** Provider event must resolve learner/course/version/assessment/time/source authority and dedupe.
  - **GREEN:** Acceptance is distinct from verified completion.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Integration observations](specs/integration-platform.md), [source authority](specs/source-authority-and-external-mastering.md).

- [ ] `LEARN-005` **[PHASE_4][SOL_HIGH] Validate assessments and issue credentials.**
  - **Depends:** `LEARN-004`, `QUAL-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEARN_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEARN_005`; `SECURITY=TestTodo_LEARN_005_Security`.
  - **RED:** Score/attempt/proctor/evidence and issuance rules yield pass/fail/unknown.
  - **GREEN:** Credential binds issuer, scope, dates, evidence and revocation status.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Talent models](data/models/talent-experience-cases.md), [qualification engine](#39-qualification-demand-matching-and-scenario-engines).

- [ ] `LEARN-006` **[PHASE_4][SOL_HIGH] Expire and renew credentials.**
  - **Depends:** `LEARN-005`, `SCHED-003`, `QUAL-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEARN_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEARN_006`; `SECURITY=TestTodo_LEARN_006_Security`.
  - **RED:** Trusted-time expiry emits warnings/qualification reevaluation once.
  - **GREEN:** Renewal creates new evidence/version and never extends old credential silently.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Scheduling triggers](#34-batch-scheduling-and-connector-execution-substrate), [ledger](specs/transaction-ledger-reconciliation-and-repair.md).

- [ ] `LEARN-007` **[PHASE_4][SOL_HIGH] Reconcile external LMS state.**
  - **Depends:** `LEARN-003`–`LEARN-006`, `TX-009`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TALENT; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_LEARN_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_LEARN_007`; `INTEGRATION=TestTodo_LEARN_007_Integration`; `SECURITY=TestTodo_LEARN_007_Security`; `MUTATION=TestTodo_LEARN_007_Mutation`.
  - **RED:** Seeded defect: a mutable definition or stale LMS completion becomes credential truth for LEARN-007; the primary test must return LEARN_007_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Expected enrollment/completion/credential versus provider observation classifies missing/extra/stale/conflicting/unknown and creates repair.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Integration platform](specs/integration-platform.md), [reconciliation](specs/transaction-ledger-reconciliation-and-repair.md).

- [x] `SURVEY-001` **[CONFORMANCE][SOL_HIGH] Define survey, question bank and campaign revisions.**
  - **Evidence (2026-09-05):** `TestTodo_SURVEY_001`, `TestTodo_SURVEY_001_Golden`, `TestTodo_SURVEY_001_Security`, `TestTodo_SURVEY_001_Mutation`, `TestTodo_SURVEY_001_Conformance` in `internal/domains/survey` (survey, question-bank and campaign revisions with canonical digests; free text requires classification; anonymity threshold below 5 refused); `go test -count=1 ./internal/domains/survey/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `FORM-002`, `ANON-001`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.EXPERIENCE,BI.CASES; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_SURVEY_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SURVEY_001`; `GOLDEN=TestTodo_SURVEY_001_Golden`; `SECURITY=TestTodo_SURVEY_001_Security`; `CONFORMANCE=TestTodo_SURVEY_001_Conformance`; `MUTATION=TestTodo_SURVEY_001_Mutation`.
  - **RED:** Seeded defect: privacy cohort or retention failure still launches or archives for SURVEY-001; the primary test must return SURVEY_001_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Definition covers purpose, questions/scales/branching, population/sample, disclosure mode, minimum cohort, communications, analysis, retention and withdrawal policy.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Talent/experience models](data/models/talent-experience-cases.md), [human work/forms](specs/human-work-forms-and-rules.md).

- [x] `SURVEY-002` **[PHASE_4][SOL_HIGH] Resolve and freeze campaign population/sample.**
  - **Evidence (2026-09-05):** `TestTodo_SURVEY_002`, `TestTodo_SURVEY_002_{Security,Mutation}` in `internal/domains/survey` (SamplingRule WHOLE/STRATIFIED/RANDOM-with-seed with a closed non-response policy, PopulationBindingRef cited by definition id/revision/digest, FreezeSample yields an immutable digested CampaignSample whose protected-membership form never carries a raw member list and whose disclosed form is sorted, de-duplicated and defensively copied); `go test -count=1 ./internal/domains/survey/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SURVEY-001`, `POP-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.EXPERIENCE,BI.CASES; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SURVEY_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SURVEY_002`; `SECURITY=TestTodo_SURVEY_002_Security`; `MUTATION=TestTodo_SURVEY_002_Mutation`.
  - **RED:** Sampling is deterministic/versioned, respects privacy/consent and cannot disclose member/count to unauthorized callers.
  - **GREEN:** Nonresponse policy is explicit.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Population engine](#37-shared-transformation-and-population-engines), [privacy models](data/models/assurance-intelligence-platform.md).

- [x] `SURVEY-003` **[PHASE_4][SOL_HIGH] Launch campaign and governed communications.**
  - **Evidence (2026-09-05):** `TestTodo_SURVEY_003` in `internal/domains/survey` (campaign launch binding a frozen sample to a template version and audience resolution into a digested LaunchRecord with a distinct approver, launch refused against an unfrozen or superseded sample, a template of the wrong purpose, or a delivery plan naming members outside the sample, protected membership never surfacing, idempotent repeat launch, bounded reminder follow-ups, Explain; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/survey/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SURVEY-002`, `MSG-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.EXPERIENCE,BI.CASES; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SURVEY_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SURVEY_003`; `SECURITY=TestTodo_SURVEY_003_Security`; `MUTATION=TestTodo_SURVEY_003_Mutation`.
  - **RED:** Messages reveal no other participant/response and honor channel/purpose/legal/preferences.
  - **GREEN:** Delivery identity cannot link anonymous response unless policy permits.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Messaging](specs/messaging-and-notification-plane.md), [confidential actors](#42-confidential-actors-abuse-detection-and-progressive-rollout).

- [x] `SURVEY-004` **[PHASE_4][SOL_HIGH] Record anonymous or pseudonymous responses.**
  - **Evidence (2026-09-05):** `TestTodo_SURVEY_004` in `internal/domains/survey` (pseudonymous and anonymous response recording with form-version validation, revision protection, campaign closure, cohort-safe aggregation and ExplainResponses; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/survey/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SURVEY-003`, `ANON-002`, `FORM-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.EXPERIENCE,BI.CASES; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SURVEY_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SURVEY_004`; `MUTATION=TestTodo_SURVEY_004_Mutation`.
  - **RED:** Response binds survey revision and scoped anti-duplicate proof while ordinary stores/logs/search cannot correlate actor.
  - **GREEN:** Withdrawal/correction semantics are explicit.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Confidential actors](#42-confidential-actors-abuse-detection-and-progressive-rollout), [forms](specs/human-work-forms-and-rules.md).

- [ ] `SURVEY-005` **[PHASE_4][SOL_HIGH] Enforce minimum cohort and re-identification defenses.**
  - **Depends:** `SURVEY-004`, `ANON-008`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.EXPERIENCE,BI.CASES; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SURVEY_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SURVEY_005`; `PROPERTY=TestTodo_SURVEY_005_Property`; `FUZZ=FuzzTodo_SURVEY_005`; `SECURITY=TestTodo_SURVEY_005_Security`; `MUTATION=TestTodo_SURVEY_005_Mutation`.
  - **RED:** Small cells, differencing, repeated filters/free text and rare attributes cannot reveal respondent.
  - **GREEN:** Unsafe aggregate is suppressed/generalized/reviewed.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [DLP](specs/data-classification-and-dlp.md), [privacy models](data/models/assurance-intelligence-platform.md).

- [ ] `SURVEY-006` **[PHASE_4][SOL_HIGH] Calculate aggregate survey results.**
  - **Depends:** `SURVEY-004`, `SURVEY-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.EXPERIENCE,BI.CASES; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SURVEY_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SURVEY_006`; `PROPERTY=TestTodo_SURVEY_006_Property`.
  - **RED:** Scoring/weight/nonresponse/rounding and uncertainty are versioned, exact and cohort-safe.
  - **GREEN:** Results never fabricate representativeness.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Rules models](data/models/rules-and-decisions.md), [intelligence models](data/models/assurance-intelligence-platform.md).

- [ ] `SURVEY-007` **[PHASE_4][SOL_HIGH] Close and archive survey campaign.**
  - **Depends:** `SURVEY-006`, `RECORDS-DISP-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.EXPERIENCE,BI.CASES; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_SURVEY_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_SURVEY_007`; `FAULT=TestTodo_SURVEY_007_Fault`; `MUTATION=TestTodo_SURVEY_007_Mutation`.
  - **RED:** Seeded defect: privacy cohort or retention failure still launches or archives for SURVEY-007; the primary test must return SURVEY_007_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Close locks new responses, freezes result/evidence, applies retention/hold/destruction and preserves minimum anonymous chronology without identity recovery.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Records](specs/records-management-and-disposition.md), [ledger](specs/transaction-ledger-reconciliation-and-repair.md).

- [x] `CRM-001` **[CONFORMANCE][SOL_HIGH] Define governed talent pools and memberships.**
  - **Evidence (2026-09-05):** `TestTodo_CRM_001` in `internal/domains/crm` (Candidate and worker roles remain nonexclusive; written by an earlier lane (Luna or Sonnet) and verified independently); `go test -count=1 ./internal/domains/crm/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `POP-001`, `CONF-002`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.RECRUITING; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_CRM_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CRM_001`; `CONFORMANCE=TestTodo_CRM_001_Conformance`; `MUTATION=TestTodo_CRM_001_Mutation`; `SECURITY=TestTodo_CRM_001_Security`.
  - **RED:** Pool/membership binds purpose, source attribution, consent/processing authority, scope, dates, owner and removal policy.
  - **GREEN:** Candidate and worker roles remain nonexclusive.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Talent models](data/models/talent-experience-cases.md), [identity resolution](specs/identity-resolution-and-entity-linkage.md).

- [x] `CRM-002` **[PHASE_4][SOL_HIGH] Manage prospect source attribution and consent.**
  - **Evidence:** Luna implementation, Sol refinement, and integration review on 2026-09-07: `TestTodo_CRM_002`, `TestTodo_CRM_002_Mutation`, `TestTodo_CRM_002_Security` and memory-store lifecycle/tenant/CAS regressions in `internal/domains/crm`; `go test -count=1 -p 1 -cover ./internal/domains/crm/` PASS on windows/arm64 (76.1%); `go vet ./internal/domains/crm/` PASS. Withdrawal/expiry stop outreach with explicit obligations; invalid authority stops outreach without authorizing deletion. This closes domain-support semantics, not a deployed recruiting workflow or durable prospect store.
  - **Depends:** `CRM-001`, `TRUST-024`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.RECRUITING; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CRM_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CRM_002`; `MUTATION=TestTodo_CRM_002_Mutation`; `SECURITY=TestTodo_CRM_002_Security`.
  - **RED:** Source/campaign/referral lineage and processing authority are exact.
  - **GREEN:** Withdrawal/expiry stops future outreach and triggers retention obligations.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Privacy lifecycle](specs/records-management-and-disposition.md), [talent models](data/models/talent-experience-cases.md).

- [ ] `CRM-003` **[PHASE_4][SOL_HIGH] Define recruiting campaigns and governed audiences.**
  - **Depends:** `CRM-001`, `POP-005`, `CYCLE-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.RECRUITING; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CRM_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CRM_003`; `INTEGRATION=TestTodo_CRM_003_Integration`; `SECURITY=TestTodo_CRM_003_Security`; `MUTATION=TestTodo_CRM_003_Mutation`.
  - **RED:** Campaign pins pool/population, purpose, content, channels, schedule, frequency/cost and suppression policy.
  - **GREEN:** Unauthorized/partial audience blocks send.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Messaging](specs/messaging-and-notification-plane.md), [population engine](#37-shared-transformation-and-population-engines).

- [ ] `CRM-004` **[PHASE_4][SOL_HIGH] Deliver campaigns and track permitted engagement.**
  - **Depends:** `CRM-003`, `MSG-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.RECRUITING; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CRM_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CRM_004`; `RACE=TestTodo_CRM_004_Race`; `INTEGRATION=TestTodo_CRM_004_Integration`; `FAULT=TestTodo_CRM_004_Fault`; `SECURITY=TestTodo_CRM_004_Security`; `MUTATION=TestTodo_CRM_004_Mutation`.
  - **RED:** Delivery/reply/click tracking observes only approved signals and cannot become covert surveillance.
  - **GREEN:** Preference/consent/bounce/fallback states reconcile.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Messaging](specs/messaging-and-notification-plane.md), [privacy models](data/models/assurance-intelligence-platform.md).

- [ ] `CRM-005` **[PHASE_4][SOL_HIGH] Convert prospect to candidate/application with lineage.**
  - **Depends:** `CRM-002`, `MODEL-022`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.RECRUITING; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_CRM_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_CRM_005`; `MUTATION=TestTodo_CRM_005_Mutation`; `SECURITY=TestTodo_CRM_005_Security`.
  - **RED:** Seeded defect: prospect converts without consent and source lineage for CRM-005; the primary test must return CRM_005_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Conversion resolves identity without destructive merge, preserves source/consent/campaign provenance and creates candidate/application role through governed intent.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Identity resolution](specs/identity-resolution-and-entity-linkage.md), [BusinessIntent](specs/business-intent-and-change-request.md).

- [x] `APPT-001` **[CONFORMANCE][SOL_HIGH] Define appointment requirements and resource types.**
  - **Evidence (2026-09-05):** `TestTodo_APPT_001` in `internal/domains/appointment` (versioned digested AppointmentRequirement (purpose, resource types with quantities, duration, participants, location class, lead time and cancellation rules) and ResourceType with closed capacity semantics, a feasibility check against declared availability windows yielding typed shortfalls, Explain; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/appointment/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-004`, `QUAL-001`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.RECRUITING; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_APPT_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_APPT_001`; `SECURITY=TestTodo_APPT_001_Security`; `CONFORMANCE=TestTodo_APPT_001_Conformance`; `MUTATION=TestTodo_APPT_001_Mutation`.
  - **RED:** Seeded defect: missing time or resource contract or competing slot reservation is accepted for APPT-001; the primary test must return APPT_001_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Requirement covers purpose, participants/roles, duration/window, location/channel, resource/qualification/privacy, cancellation/no-show and external-calendar policy.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Talent/experience models](data/models/talent-experience-cases.md), [relationship map](data/models/relationship-map.md).

- [ ] `APPT-002` **[PHASE_3][SOL_HIGH] Resolve eligible participants and availability.**
  - **Depends:** `APPT-001`, `ELIG-003`, `QUAL-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.RECRUITING; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_APPT_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_APPT_002`; `SECURITY=TestTodo_APPT_002_Security`; `MUTATION=TestTodo_APPT_002_Mutation`.
  - **RED:** Busy/leave/timezone/qualification/privacy unknowns remain explicit.
  - **GREEN:** No calendar detail of another person is disclosed.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Eligibility](#38-eligibility-business-cycle-and-balance-engines), [qualification](#39-qualification-demand-matching-and-scenario-engines).

- [ ] `APPT-003` **[PHASE_3][SOL_HIGH] Reserve appointment slot and resources atomically.**
  - **Depends:** `APPT-002`, `RESERVE-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.RECRUITING; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_APPT_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_APPT_003`; `RACE=TestTodo_APPT_003_Race`; `MUTATION=TestTodo_APPT_003_Mutation`.
  - **RED:** Seeded defect: missing time or resource contract or competing slot reservation is accepted for APPT-003; the primary test must return APPT_003_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Concurrent reservation yields one winner, expiry/release is fenced and all participant/resource holds commit together or none.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Transaction coordinator](specs/transaction-plan-and-commit-coordinator.md), [workflow runtime](specs/workflow-runtime.md).

- [ ] `APPT-004` **[PHASE_3][SOL_HIGH] Confirm, reschedule and cancel appointments.**
  - **Depends:** `APPT-003`, `MSG-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.RECRUITING; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_APPT_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_APPT_004`; `RACE=TestTodo_APPT_004_Race`.
  - **RED:** Changed availability/version or cancellation policy conflict fails.
  - **GREEN:** Transitions update reservations and send governed communications idempotently.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Messaging](specs/messaging-and-notification-plane.md), [human work](specs/human-work-forms-and-rules.md).

- [ ] `APPT-005` **[PHASE_3][SOL_HIGH] Handle attendance and no-show.**
  - **Depends:** `APPT-004`, `ATTEST-004`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.RECRUITING; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_APPT_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_APPT_005`; `PROPERTY=TestTodo_APPT_005_Property`; `INTEGRATION=TestTodo_APPT_005_Integration`; `FAULT=TestTodo_APPT_005_Fault`; `MUTATION=TestTodo_APPT_005_Mutation`.
  - **RED:** Attended/no-show/cancelled/unknown evidence is distinct, trusted and correctable.
  - **GREEN:** Downstream fee/case/workflow action follows policy, never assumption.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Attestation](#40-attestation-and-conformance-gated-program-engines), [workflow runtime](specs/workflow-runtime.md).

- [ ] `APPT-006` **[PHASE_3][SOL_HIGH] Reconcile external calendars.**
  - **Depends:** `APPT-004`, `INTG-009`, `TX-009`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.RECRUITING; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_APPT_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_APPT_006`; `INTEGRATION=TestTodo_APPT_006_Integration`; `MUTATION=TestTodo_APPT_006_Mutation`.
  - **RED:** Seeded defect: missing time or resource contract or competing slot reservation is accepted for APPT-006; the primary test must return APPT_006_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Expected slot/participants/status versus provider observation handles stale/partial/duplicate/deleted events and creates repair without exposing private calendar content.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Integration platform](specs/integration-platform.md), [reconciliation](specs/transaction-ledger-reconciliation-and-repair.md).

## 46. Knowledge and composable industry-pack engines

> **Disposition (2026-09-02):** DEFERRED.

- [x] `KNOW-001` **[PHASE_3][SOL_HIGH] Define versioned KnowledgeArticle.**
  - **Evidence (2026-09-05):** `TestTodo_KNOW_001`, `TestTodo_KNOW_001_{Property,Security,Mutation}` in `internal/domains/knowledge` (versioned ArticleRevision with audience scope, classification, jurisdiction, effective/known intervals, body digest by reference, review metadata; self-supersession refused); `go test -count=1 ./internal/domains/knowledge/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DOC-TEMPLATE-001`, `MODEL-017`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.CASES,BI.EXPERIENCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_KNOW_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_KNOW_001`; `PROPERTY=TestTodo_KNOW_001_Property`; `SECURITY=TestTodo_KNOW_001_Security`; `MUTATION=TestTodo_KNOW_001_Mutation`.
  - **RED:** Seeded defect: uncited stale injected or invalidated knowledge remains publishable for KNOW-001; the primary test must return KNOW_001_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Article missing owner/source authority/citations/audience/classification/jurisdiction/effective-known intervals/review/expiry/retirement fails publication.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Connectivity/content models](data/models/connectivity-access-content.md), [source authority](specs/source-authority-and-external-mastering.md).

- [x] `KNOW-002` **[PHASE_3][SOL_HIGH] Review, localize, publish and retire knowledge.**
  - **Evidence (2026-09-07):** `TestTodo_KNOW_002`, `TestTodo_KNOW_002_Mutation`, `TestTodo_KNOW_002_Security` in `internal/domains/knowledge` (written in d3c2a53 on 2026-09-06 and never ticked; the named tests prove the GREEN contract: exact locale/reviewer/source bundle activates and retirement preserves history); `go test -count=1 ./internal/domains/knowledge/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `KNOW-001`, `APPROVAL-002`, `CP-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.CASES,BI.EXPERIENCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_KNOW_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_KNOW_002`; `SECURITY=TestTodo_KNOW_002_Security`; `MUTATION=TestTodo_KNOW_002_Mutation`.
  - **RED:** Unapproved/stale/machine-only legal translation or unresolved citation cannot publish.
  - **GREEN:** Exact locale/reviewer/source bundle activates and retirement preserves history.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Configuration lifecycle](data/models/dataops-configuration.md), [governance composition](specs/governance-decision-and-obligation-composition.md).

- [ ] `KNOW-003` **[PHASE_3][SOL_HIGH] Resolve applicable knowledge by context.**
  - **Depends:** `KNOW-002`, `TRUST-010`, `LEGAL-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.CASES,BI.EXPERIENCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_KNOW_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_KNOW_003`; `GOLDEN=TestTodo_KNOW_003_Golden`; `SECURITY=TestTodo_KNOW_003_Security`; `MUTATION=TestTodo_KNOW_003_Mutation`.
  - **RED:** Seeded defect: uncited stale injected or invalidated knowledge remains publishable for KNOW-003; the primary test must return KNOW_003_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Resolver returns only authorized articles applicable to tenant/org/persona/jurisdiction/time/purpose and explicitly reports conflict/unknown/stale source.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [AuthZ](specs/organization-scope-and-authz.md), [regulatory platform](specs/platform-architecture-catalog.md).

- [ ] `KNOW-004` **[PHASE_3][SOL_HIGH] Publish safe, cited RAG derivatives.**
  - **Depends:** `KNOW-002`, `MODEL-023`, `TRUST-019`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.CASES,BI.EXPERIENCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_KNOW_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_KNOW_004`; `SECURITY=TestTodo_KNOW_004_Security`; `MUTATION=TestTodo_KNOW_004_Mutation`.
  - **RED:** Seeded defect: uncited stale injected or invalidated knowledge remains publishable for KNOW-004; the primary test must return KNOW_004_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Chunks preserve article/version/citations/classification/taint/effective scope, exclude hostile instructions and cannot broaden access beyond source.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [DLP](specs/data-classification-and-dlp.md), [security models](data/models/security-trust.md).

- [ ] `KNOW-005` **[PHASE_3][SOL_HIGH] Invalidate derived answers on content change.**
  - **Depends:** `KNOW-004`, `EVENT-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.CASES,BI.EXPERIENCE; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_KNOW_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_KNOW_005`; `RECOVERY=TestTodo_KNOW_005_Recovery`; `MUTATION=TestTodo_KNOW_005_Mutation`.
  - **RED:** Seeded defect: uncited stale injected or invalidated knowledge remains publishable for KNOW-005; the primary test must return KNOW_005_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Publish/retire/correction identifies affected chunks/answers/agents/workflows, marks stale before rebuild and preserves historical cited answer evidence.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Provenance](specs/provenance-graph-and-lineage.md), [intelligence models](data/models/assurance-intelligence-platform.md).

- [ ] `KNOW-006` **[CONFORMANCE][SOL_HIGH] Prove knowledge-answer freshness and citation safety.**
  - **Depends:** `KNOW-001`–`KNOW-005`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.CASES,BI.EXPERIENCE; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_KNOW_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_KNOW_006`; `CONFORMANCE=TestTodo_KNOW_006_Conformance`; `MUTATION=TestTodo_KNOW_006_Mutation`; `SECURITY=TestTodo_KNOW_006_Security`.
  - **RED:** Seeded defect: uncited stale injected or invalidated knowledge remains publishable for KNOW-006; the primary test must return KNOW_006_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Adversarial outdated/conflicting/hostile/unauthorized content yields cited current answer, explicit conflict/unknown or escalation—never uncited fabrication.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Adversarial audit](specs/adversarial-audit-32-reviewers-2026-08-14.md), [agent/intelligence models](data/models/assurance-intelligence-platform.md).

- [x] `PACK-001` **[CONFORMANCE][SOL_HIGH] Define IndustryPack manifest and composition contract.**
  - **Evidence (2026-09-05):** `TestTodo_PACK_001` in `internal/domains/industrypack` (IndustryPack manifest and composition contract; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/industrypack/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CP-002`, `MODEL-017`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.DATAOPS,BI.REGULATORY; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_PACK_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PACK_001`; `GOLDEN=TestTodo_PACK_001_Golden`; `CONFORMANCE=TestTodo_PACK_001_Conformance`.
  - **RED:** Pack declares owner, industry/version, dependencies, compatibility, reference/rule/workflow/form/skill/metric contents, scope and support.
  - **GREEN:** No executable arbitrary code.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Configuration models](data/models/dataops-configuration.md), [platform architecture](specs/platform-architecture-catalog.md).

- [x] `PACK-002` **[PHASE_4][SOL_HIGH] Bind pack reference data, rules and formulas.**
  - **Evidence (2026-09-07):** `TestTodo_PACK_002`, `TestTodo_PACK_002_Fault`, `TestTodo_PACK_002_Golden`, `TestTodo_PACK_002_Mutation`, `TestTodo_PACK_002_Property`, `TestTodo_PACK_002_Security` in `internal/domains/industrypack` (written in d3c2a53 on 2026-09-06 and never ticked; the named tests prove the GREEN contract: content is namespaced, effective-dated and dependency-complete); `go test -count=1 ./internal/domains/industrypack/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PACK-001`, `MODEL-018`, `RULE-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS,BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PACK_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PACK_002`; `PROPERTY=TestTodo_PACK_002_Property`; `GOLDEN=TestTodo_PACK_002_Golden`; `FAULT=TestTodo_PACK_002_Fault`; `SECURITY=TestTodo_PACK_002_Security`; `MUTATION=TestTodo_PACK_002_Mutation`.
  - **RED:** Unresolved ID/version, illegal override or ambiguous precedence fails.
  - **GREEN:** Content is namespaced, effective-dated and dependency-complete.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Rules models](data/models/rules-and-decisions.md), [registry contracts](data/models/registry-and-coverage-contracts.md).

- [ ] `PACK-003` **[PHASE_4][SOL_HIGH] Bind pack workflows, forms, skills and metrics.**
  - **Depends:** `PACK-001`, `WF-COMP-001`, `FORM-002`, `QUAL-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS,BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PACK_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PACK_003`; `GOLDEN=TestTodo_PACK_003_Golden`; `BROWSER=TestTodo_PACK_003_Browser`; `MUTATION=TestTodo_PACK_003_Mutation`.
  - **RED:** Seeded defect: unknown dependency or mandatory country rule override activates for PACK-003; the primary test must return PACK_003_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** All references compile/type-check, side effects/authority/locale/accessibility/metric definitions are explicit and deferred capabilities remain gated.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Workflow runtime](specs/workflow-runtime.md), [human work/forms](specs/human-work-forms-and-rules.md).

- [ ] `PACK-004` **[PHASE_4][SOL_HIGH] Validate pack compatibility and dependency impact.**
  - **Depends:** `PACK-002`, `PACK-003`, `CONFIG-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS,BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PACK_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PACK_004`; `GOLDEN=TestTodo_PACK_004_Golden`; `CONFORMANCE=TestTodo_PACK_004_Conformance`; `MUTATION=TestTodo_PACK_004_Mutation`.
  - **RED:** Seeded defect: unknown dependency or mandatory country rule override activates for PACK-004; the primary test must return PACK_004_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Incompatible platform/country/customer versions, missing dependency, active-workflow break or unresolved migration blocks publication with exact impacted objects.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [DataOps](specs/hris-admin-dataops.md), [configuration models](data/models/dataops-configuration.md).

- [ ] `PACK-005` **[PHASE_4][SOL_HIGH] Publish signed effective pack versions.**
  - **Depends:** `PACK-004`, `CP-003`, `ROLLOUT-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS,BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PACK_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PACK_005`; `GOLDEN=TestTodo_PACK_005_Golden`; `MUTATION=TestTodo_PACK_005_Mutation`.
  - **RED:** Unsigned/wrong-scope/stale/unapproved pack cannot activate.
  - **GREEN:** Receipt binds exact target, bundle, effective time and rollback version.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Control plane](#30-control-plane-publication-distribution-and-activation), [rollout](#42-confidential-actors-abuse-detection-and-progressive-rollout).

- [ ] `PACK-006` **[PHASE_4][SOL_HIGH] Compose platform, country, industry and customer packs.**
  - **Depends:** `PACK-005`, `LEGAL-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DATAOPS,BI.REGULATORY; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PACK_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PACK_006`; `GOLDEN=TestTodo_PACK_006_Golden`; `SECURITY=TestTodo_PACK_006_Security`; `MUTATION=TestTodo_PACK_006_Mutation`.
  - **RED:** Mandatory security/legal constraints cannot be weakened.
  - **GREEN:** Explicit family composition strategy resolves additive/precedence/restrictive/custom conflicts or returns blocking ambiguity.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Governance composition](specs/governance-decision-and-obligation-composition.md), [regulatory platform](specs/platform-architecture-catalog.md).

- [ ] `PACK-007` **[CONFORMANCE][SOL_HIGH] Prove pack composition with representative industries.**
  - **Depends:** `PACK-001`–`PACK-006`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.DATAOPS,BI.REGULATORY; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestTodo_PACK_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PACK_007`; `GOLDEN=TestTodo_PACK_007_Golden`; `CONFORMANCE=TestTodo_PACK_007_Conformance`; `MUTATION=TestTodo_PACK_007_Mutation`.
  - **RED:** Seeded defect: unknown dependency or mandatory country rule override activates for PACK-007; the primary test must return PACK_007_REJECTED with offending field/state/version and persist zero authoritative rows, business events, outbox entries, human work and provider requests.
  - **GREEN:** Healthcare, Retail and Manufacturing fixtures combine one country and customer pack, preserve mandatory rules and produce deterministic config/workflow/model digests.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Adversarial audit](specs/adversarial-audit-32-reviewers-2026-08-14.md), [configuration models](data/models/dataops-configuration.md).

- [x] `ENGINE-COVERAGE-001` **[P0][SOL_HIGH] Fail CI when an intent's reusable computation remains implicit.**
  - **Evidence (2026-09-05):** `TestTodo_ENGINE_COVERAGE_001` in `tools/policy/enginecoverage` (a declared table mapping each drafted intent's reusable computations to the internal/engines package that owns them, a scan proving each engine exists, exports Version and Explain and is imported by the intent's definition, app or conformance package, IMPLICIT computations reported against an owner allowlist, cmd exiting non-zero on new gaps; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/enginecoverage/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02. Wired during integration as `check:enginecoverage` in `npm run test:all` and a CI step; one allowlisted IMPLICIT row today (reject_proposal's snapshot consumer wiring).
  - **Depends:** `SUBSTRATE-COVERAGE-001`, `MSRC-009`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_ENGINE_COVERAGE_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ENGINE_COVERAGE_001`; `GOLDEN=TestTodo_ENGINE_COVERAGE_001_Golden`; `RACE=TestTodo_ENGINE_COVERAGE_001_Race`; `CONFORMANCE=TestTodo_ENGINE_COVERAGE_001_Conformance`; `MUTATION=TestTodo_ENGINE_COVERAGE_001_Mutation`.
  - **RED:** Every accepted-catalog intent declares workflow primitives plus invoked shared/domain/platform engines, typed inputs/results, authority, phase and conformance owner.
  - **GREEN:** Missing, circular or duplicate engine responsibility is named exactly and baseline/extension counts reconcile to the catalog release.
  - **REFACTOR:** Keep the tested contract behind its semantic owner, remove duplication and rerun the named unit, integration, conformance, race, fuzz, security and recovery suites that apply without changing observable behavior.
  - **Refs:** [Intent coverage](data/models/intent-coverage-matrix.md), [platform coverage](specs/platform-capability-coverage-matrix.md), [master plan](plan.md).

## 47. Workflow-research traceability and extracted-HR vertical conformance

> **Disposition (2026-09-02):** DEFERRED. The exploratory workflow corpus is read through the vocabulary mapping in its README; no registry over it is built.

- [ ] `WF-DISC-001` **[P0][TERRA] Register every workflow research artifact and its truthful discovery state.**
  - **Depends:** `GOV-022`, `MODEL-009`, `INTENT-025`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestWorkflowResearchRegistryMatchesCorpus`.
  - **TEST MATRIX:** `PRIMARY=TestWorkflowResearchRegistryMatchesCorpus`; `PROPERTY=TestTodo_WF_DISC_001_Property`; `GOLDEN=TestTodo_WF_DISC_001_Golden`; `CONFORMANCE=TestTodo_WF_DISC_001_Conformance`; `MUTATION=TestTodo_WF_DISC_001_Mutation`.
  - **RED:** adding, removing or renaming a workflow/catalog/sample without a unique workflow ID, intent IDs, domain, kernel family, source provenance, state, owner and exact file digest must return the orphan/stale entry; `EXTRACTED|REFERENCE|EXPLORED` may not be reported as `CONTRACTED|IMPLEMENTED` without typed contract and fresh evidence links.
  - **GREEN:** registry count and digest exactly match the workflow corpus; every artifact resolves accepted intents, archetype, dependencies, data register entries, step types, safe deferrals and next closure action while historical source behavior remains non-authoritative evidence.
  - **REFACTOR:** generate the workflow index and discovery-state tables from the registry so prose counts cannot drift.
  - **Refs:** [Workflow index](workflows/README.md), [discovery backlog](workflows/discovery-backlog.md), [exploration contract](workflows/_shared/exploration-contract.md).

- [ ] `WF-DISC-002` **[P0][SOL_HIGH] Prove every catalog-mapped HR workflow satisfies an explicit archetype contract.**
  - **Depends:** `WF-DISC-001`, `INTENT-CONF-001`, `WF-COMP-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestWorkflowCatalogRejectsImplicitArchetypeResponsibilities`.
  - **TEST MATRIX:** `PRIMARY=TestWorkflowCatalogRejectsImplicitArchetypeResponsibilities`; `PROPERTY=TestTodo_WF_DISC_002_Property`; `GOLDEN=TestTodo_WF_DISC_002_Golden`; `CONFORMANCE=TestTodo_WF_DISC_002_Conformance`; `MUTATION=TestTodo_WF_DISC_002_Mutation`.
  - **RED:** any mapped intent omits a required archetype phase, dependency/data class, authority/effective-time rule, failure route, correction/reconciliation path or explicit `NOT_APPLICABLE` reason; duplicate intent placement or an unknown step/entity also fails with exact catalog row.
  - **GREEN:** generated catalog report reconciles all six HR catalog counts and maps each accepted intent to one or more versioned archetypes plus its unique step/dependency/data delta, negative-state policy and deep-dive/conformance status.
  - **REFACTOR:** share one archetype schema across People, Workforce, Rewards, Lifecycle, Leave and HR Service catalogs without moving domain rules into the workflow kernel.
  - **Refs:** [Workflow archetypes](workflows/_shared/workflow-archetypes.md), [workflow catalogs](workflows/README.md), [discovery backlog](workflows/discovery-backlog.md).

- [ ] `WF-DISC-003` **[P0][SOL_HIGH] Turn every unresolved workflow-modeling decision into an owned safe boundary.**
  - **Depends:** `WF-DISC-001`, `GOV-003`, `GOV-010`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestWorkflowModelingDecisionRejectsImplicitOrUnsafeDefault`.
  - **TEST MATRIX:** `PRIMARY=TestWorkflowModelingDecisionRejectsImplicitOrUnsafeDefault`; `PROPERTY=TestTodo_WF_DISC_003_Property`; `GOLDEN=TestTodo_WF_DISC_003_Golden`; `SECURITY=TestTodo_WF_DISC_003_Security`; `CONFORMANCE=TestTodo_WF_DISC_003_Conformance`; `MUTATION=TestTodo_WF_DISC_003_Mutation`.
  - **RED:** an unresolved ownership, precedence, composition, boundary, readiness, timing or external-authority question lacks affected intents/workflows, accountable owner, decision deadline, safe `BLOCK|ROUTE_HUMAN|DEFER_CAPABILITY` default, deciding evidence and linked todo/test; prose or an implementation guess cannot close it.
  - **GREEN:** every register entry is `OPEN_OWNED|DECIDED|DEFERRED` with immutable decision revision, alternatives, consequences, authority, source evidence, invalidation trigger and executable negative fixture proving the safe default before the decision.
  - **REFACTOR:** resolved decisions update canonical model/intent/workflow registries and retain the superseded question for historical explanation.
  - **Refs:** [Workflow data register](workflows/hr-workflow-data-register.md#unresolved-modeling-decisions), [residual-unknown rule](workflows/_engine/workflow-context-adversarial-audit-2026-08-14.md#residual-unknown-rule).

- [ ] `WF-DISC-004` **[P0][TERRA] Gate promotion of exploratory workflows into contracted definitions.**
  - **Depends:** `WF-DISC-002`, `WF-DISC-003`, `WF-COMP-001`, `GOV-024`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestExploratoryWorkflowPromotionRequiresExactContractClosure`.
  - **TEST MATRIX:** `PRIMARY=TestExploratoryWorkflowPromotionRequiresExactContractClosure`; `GOLDEN=TestTodo_WF_DISC_004_Golden`; `CONFORMANCE=TestTodo_WF_DISC_004_Conformance`; `MUTATION=TestTodo_WF_DISC_004_Mutation`.
  - **RED:** promotion succeeds with unresolved schemas/capabilities/entities/properties/rules/classifications/owners/tests, untyped UNKNOWN behavior, an unsafe legacy shortcut or a missing parity/deviation record; it must fail with the complete closure set and zero published definition.
  - **GREEN:** one signed promotion receipt binds the exact exploratory digest to compiled schemas, capability versions, workflow graph, negative fixtures, deliberate legacy deviations, owner/review expiry and `CONTRACTED` state; implementation remains a separate evidence transition.
  - **REFACTOR:** the same maturity transition validator governs workflows, intents, rules and model bindings without conflating their evidence types.
  - **Refs:** [Exploration contract](workflows/_shared/exploration-contract.md), [legacy baseline](specs/legacy-implementation-baseline.md), [coverage contracts](data/models/registry-and-coverage-contracts.md).

- [x] `MODEL-031` **[CONFORMANCE][SOL_HIGH] Resolve ContactPoint and Address ownership without collapsing person and employment scope.**
  - **Evidence (2026-09-05):** `TestContactAndAddressOwnershipPreservesScopeAuthorityAndHistory` in `internal/intent/model` (reviewed aggregate/relationship policy resolves each fact to an owner and scoped role with explicit aliases, authority, classification, permitted derivations, correction/export/merge behavior and deterministic migration from legacy records; ambiguous scope returns UNKNOWN_SCOPE w; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/intent/model/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-012`, `MODEL-013`, `MODEL-021`, `WF-DISC-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestContactAndAddressOwnershipPreservesScopeAuthorityAndHistory`.
  - **TEST MATRIX:** `PRIMARY=TestContactAndAddressOwnershipPreservesScopeAuthorityAndHistory`; `PROPERTY=TestTodo_MODEL_031_Property`; `GOLDEN=TestTodo_MODEL_031_Golden`; `SECURITY=TestTodo_MODEL_031_Security`; `CONFORMANCE=TestTodo_MODEL_031_Conformance`; `MUTATION=TestTodo_MODEL_031_Mutation`.
  - **RED:** a fixture cannot independently represent personal/work/employment-specific endpoints, home/work/tax-residence addresses, multiple concurrent employments, country/customer variation, field authority, disclosure, corrections and effective/known history—or changing home address silently changes work location/tax residence.
  - **GREEN:** reviewed aggregate/relationship policy resolves each fact to an owner and scoped role with explicit aliases, authority, classification, permitted derivations, correction/export/merge behavior and deterministic migration from legacy records; ambiguous scope returns `UNKNOWN_SCOPE` with zero write.
  - **REFACTOR:** value objects remain reusable while ownership and authority stay on typed relationship/revision contracts rather than nullable foreign-key conventions.
  - **Refs:** [Workflow data register](workflows/hr-workflow-data-register.md#unresolved-modeling-decisions), [People models](data/models/people-workforce.md), [model conventions](data/models/modeling-conventions.md).

- [ ] `COMP-005` **[CONFORMANCE][SOL_HIGH] Prove compensation package changes compose parent and child intents without lost updates.**
  - **Depends:** `COMP-004`, `INTENT-015`, `INTENT-016`, `TX-001`, `WF-DISC-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.REWARDS; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestCompensationIntentCompositionPreservesOmittedComponentsAndAtomicMeaning`.
  - **TEST MATRIX:** `PRIMARY=TestCompensationIntentCompositionPreservesOmittedComponentsAndAtomicMeaning`; `PROPERTY=TestTodo_COMP_005_Property`; `GOLDEN=TestTodo_COMP_005_Golden`; `RACE=TestTodo_COMP_005_Race`; `FAULT=TestTodo_COMP_005_Fault`; `SECURITY=TestTodo_COMP_005_Security`; `CONFORMANCE=TestTodo_COMP_005_Conformance`; `MUTATION=TestTodo_COMP_005_Mutation`.
  - **RED:** base-pay-only input overwrites bonus/allowance, omitted/null/end semantics collapse, child approval differs from parent proposal, one child commits outside declared atomic boundary, correction/retro mode is lost, or a failed external effect reruns the package mutation.
  - **GREEN:** versioned composition policy deterministically selects one package proposal or parent plus bounded child intents, preserves presence semantics and exact component revisions, binds shared snapshot/effective date/approvals/transaction plan, returns per-child and aggregate completion, and appends one explainable correction chain.
  - **REFACTOR:** domain components retain independent semantics while the composition contract owns only proposal and transaction coordination.
  - **Refs:** [Compensation workflow](workflows/rewards/compensation-change.md), [compensation domain](specs/compensation-domain.md), [intent relationships](specs/business-intent-and-change-request.md).

- [x] `HEADCOUNT-001` **[CONFORMANCE][SOL_HIGH] Separate headcount authorization, position creation and recruiting requisition authority.**
  - **Evidence (2026-09-05):** `TestHeadcountPositionAndRequisitionRemainDistinctGovernedTransactions` in `internal/domains/headcount` (typed parent/child relationships preserve independent proposals, authority, approval, reservation, transaction and completion states for HeadcountRequest, Position and Requisition; capacity conservation and budget consumption hold under concurrency and each external creation reco; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/headcount/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `POSITION-002`, `BUDGET-001`, `INTENT-015`, `RESERVE-001`, `WF-DISC-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.WORKFORCE; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestHeadcountPositionAndRequisitionRemainDistinctGovernedTransactions`.
  - **TEST MATRIX:** `PRIMARY=TestHeadcountPositionAndRequisitionRemainDistinctGovernedTransactions`; `PROPERTY=TestTodo_HEADCOUNT_001_Property`; `GOLDEN=TestTodo_HEADCOUNT_001_Golden`; `RACE=TestTodo_HEADCOUNT_001_Race`; `FAULT=TestTodo_HEADCOUNT_001_Fault`; `SECURITY=TestTodo_HEADCOUNT_001_Security`; `CONFORMANCE=TestTodo_HEADCOUNT_001_Conformance`; `MUTATION=TestTodo_HEADCOUNT_001_Mutation`.
  - **RED:** approving capacity creates a position or requisition implicitly, a position consumes no approved plan/budget line, recruiting opens against unavailable/frozen capacity, requester-selected approvers bypass policy, concurrent requests oversubscribe capacity or external `REQUESTED` is treated as created.
  - **GREEN:** typed parent/child relationships preserve independent proposals, authority, approval, reservation, transaction and completion states for HeadcountRequest, Position and Requisition; capacity conservation and budget consumption hold under concurrency and each external creation reconciles separately.
  - **REFACTOR:** shared reservation/fencing mechanics do not erase the three domain lifecycles or their distinct owners.
  - **Refs:** [Headcount requisition workflow](workflows/workforce/headcount-requisition.md), [position domain](specs/position-and-headcount-domain.md), [budget authority](specs/workforce-budget-authority.md).

- [ ] `CONF-017` **[CONFORMANCE][SOL_HIGH] Prove verified contact-information update semantics.**
  - **Depends:** `CONF-001`, `MODEL-031`, `WF-STEP-010`, `WF-RUN-005`, `INTG-010`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestContactUpdateConformanceRejectsScopeLeakLostSignalAndFalseCompletion`.
  - **TEST MATRIX:** `PRIMARY=TestContactUpdateConformanceRejectsScopeLeakLostSignalAndFalseCompletion`; `PROPERTY=TestTodo_CONF_017_Property`; `GOLDEN=TestTodo_CONF_017_Golden`; `RACE=TestTodo_CONF_017_Race`; `INTEGRATION=TestTodo_CONF_017_Integration`; `FAULT=TestTodo_CONF_017_Fault`; `SECURITY=TestTodo_CONF_017_Security`; `CONFORMANCE=TestTodo_CONF_017_Conformance`; `MUTATION=TestTodo_CONF_017_Mutation`.
  - **RED:** nullable input ambiguously creates/removes data, proxy identity is lost, wrong-tenant/invalid/duplicate/late verification resumes, valid signal-before-subscription disappears, address changes work/tax facts, simulation mutates, outbox escapes failed commit or queued provider work closes external consistency.
  - **GREEN:** golden fixtures prove typed ADD/REVISE/REMOVE/CORRECT operations, subject-versus-actor evidence, normalization/version lineage, optional policy approval, durable verified signal correlation/dedupe, effective-date revalidation, one atomic revision/outbox, authority-aware observation and bounded repair with unrelated facts unchanged.
  - **REFACTOR:** verification, messaging, contact ownership and external reconciliation remain separately testable capabilities.
  - **Refs:** [Contact update](workflows/people/contact-information-update.md), [verified endpoint sample](workflows/samples/verified-contact-endpoint-change.md).

- [ ] `CONF-018` **[CONFORMANCE][SOL_HIGH] Prove emergency-contact membership, order and privacy semantics.**
  - **Depends:** `CONF-001`, `MODEL-031`, `RULE-003`, `INTG-010`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestEmergencyContactConformancePreservesRelationshipMeaningAndOrderedSet`.
  - **TEST MATRIX:** `PRIMARY=TestEmergencyContactConformancePreservesRelationshipMeaningAndOrderedSet`; `PROPERTY=TestTodo_CONF_018_Property`; `GOLDEN=TestTodo_CONF_018_Golden`; `RACE=TestTodo_CONF_018_Race`; `INTEGRATION=TestTodo_CONF_018_Integration`; `FAULT=TestTodo_CONF_018_Fault`; `SECURITY=TestTodo_CONF_018_Security`; `CONFORMANCE=TestTodo_CONF_018_Conformance`; `MUTATION=TestTodo_CONF_018_Mutation`.
  - **RED:** missing ID conflates add/replace, reorder loses members, concurrent priorities collide, supplied person becomes canonical without governed linkage, relationship label grants dependent/beneficiary/representative authority, inaccessible notes leak or provider acceptance closes reconciliation.
  - **GREEN:** ADD/REVISE/REMOVE/REORDER/CORRECT fixtures preserve exact effective membership, integer priority uniqueness, reachability, person-link provenance, minimum disclosure and classification; atomic local revision and external observation reconcile membership/order/values or create scoped repair.
  - **REFACTOR:** ordered-set invariants are domain-owned and transport/provider adapters consume only normalized revisions.
  - **Refs:** [Emergency contact workflow](workflows/people/emergency-contact-update.md), [People relationships](data/models/people-workforce.md).

- [ ] `CONF-019` **[CONFORMANCE][SOL_HIGH] Prove global legal-name change, evidence and downstream identity semantics.**
  - **Depends:** `CONF-001`, `DOC-MAL-001`, `DOC-INTAKE-001`, `LEGAL-001`, `MODEL-022`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestLegalNameChangeConformanceRejectsWesternNameAssumptionsAndImplicitEffects`.
  - **TEST MATRIX:** `PRIMARY=TestLegalNameChangeConformanceRejectsWesternNameAssumptionsAndImplicitEffects`; `PROPERTY=TestTodo_CONF_019_Property`; `GOLDEN=TestTodo_CONF_019_Golden`; `FUZZ=FuzzTodo_CONF_019`; `INTEGRATION=TestTodo_CONF_019_Integration`; `FAULT=TestTodo_CONF_019_Fault`; `SECURITY=TestTodo_CONF_019_Security`; `CONFORMANCE=TestTodo_CONF_019_Conformance`; `MUTATION=TestTodo_CONF_019_Mutation`.
  - **RED:** first/middle/last-only schema rejects valid scripts or corrupts ordering, Unicode confusables bypass validation, raw evidence enters workflow/telemetry, AI determines legal sufficiency, display/username/email changes implicitly, payroll cutoff or identity merge conflict is ignored, or downstream acceptance is called truth.
  - **GREEN:** multilingual structured-name fixtures preserve script/local/full/Latin representations and normalization profile; scanned/classified evidence, jurisdiction obligation, exact approval digest, effective-time revalidation and atomic legal-name revision produce separately authorized payroll/IAM/document/display effects whose observations reconcile without exposing evidence.
  - **REFACTOR:** name representation, evidence sufficiency and downstream naming policies remain independently versioned.
  - **Refs:** [Legal name workflow](workflows/people/legal-name-change.md), [document processing](#23-document-processing-and-signature-execution), [identity linkage](specs/identity-resolution-and-entity-linkage.md).

- [ ] `CONF-020` **[CONFORMANCE][SOL_HIGH] Prove standalone compensation-change simulation, reservation, commit and repair.**
  - **Depends:** `CONF-001`, `COMP-005`, `BUDGET-002`, `TX-004`, `INTG-016`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestCompensationChangeConformancePreservesExactMoneyPresenceAndAuthority`.
  - **TEST MATRIX:** `PRIMARY=TestCompensationChangeConformancePreservesExactMoneyPresenceAndAuthority`; `PROPERTY=TestTodo_CONF_020_Property`; `GOLDEN=TestTodo_CONF_020_Golden`; `RACE=TestTodo_CONF_020_Race`; `INTEGRATION=TestTodo_CONF_020_Integration`; `FAULT=TestTodo_CONF_020_Fault`; `SECURITY=TestTodo_CONF_020_Security`; `CONFORMANCE=TestTodo_CONF_020_Conformance`; `MUTATION=TestTodo_CONF_020_Mutation`.
  - **RED:** binary float, basis/frequency mismatch, duplicate effective times, omitted component overwrite, expired budget fence, stale band/payroll/legal context, unauthorized disclosure, partial local package write or external repair mutation reaches completion.
  - **GREEN:** golden salary/hourly/allowance/bonus/correction/retro fixtures return exact decimals, annualization/range/budget/payroll/legal impacts and zero-effect simulation; proposal-bound approvals/reservations precede one atomic package commit, external observations reconcile exact intervals/values and repair redrives only failed effects.
  - **REFACTOR:** this slice reuses Promotion substrates but does not require job/position/manager mutation.
  - **Refs:** [Compensation workflow](workflows/rewards/compensation-change.md), [compensation domain](specs/compensation-domain.md).

- [ ] `CONF-021` **[CONFORMANCE][SOL_HIGH] Prove position and headcount requisition with capacity conservation.**
  - **Depends:** `CONF-001`, `HEADCOUNT-001`, `APPROVAL-005`, `RESERVE-001`, `INTG-016`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestHeadcountRequisitionConformancePreventsApprovalCreationConflationAndOvercommit`.
  - **TEST MATRIX:** `PRIMARY=TestHeadcountRequisitionConformancePreventsApprovalCreationConflationAndOvercommit`; `PROPERTY=TestTodo_CONF_021_Property`; `GOLDEN=TestTodo_CONF_021_Golden`; `RACE=TestTodo_CONF_021_Race`; `INTEGRATION=TestTodo_CONF_021_Integration`; `FAULT=TestTodo_CONF_021_Fault`; `SECURITY=TestTodo_CONF_021_Security`; `CONFORMANCE=TestTodo_CONF_021_Conformance`; `MUTATION=TestTodo_CONF_021_Mutation`.
  - **RED:** float FTE/range, free-text canonical dimensions, requester-chosen approver, missing SoD/quorum, RFI without successor proposal, freeze/duplicate/open-capacity conflict, expired reservation, concurrent overcommit or external requested state is accepted as approved capacity/created position.
  - **GREEN:** fixtures separately approve headcount, optionally create position and later permit requisition; exact FTE/count/budget conservation holds under competing proposals, approvals bind immutable revisions, revalidation fences all scarce resources and external creation reconciles identifiers/capacity before closure.
  - **REFACTOR:** synchronous chain and asynchronous quorum use one approval protocol while domain transactions remain distinct.
  - **Refs:** [Headcount workflow](workflows/workforce/headcount-requisition.md), [position model](data/models/people-workforce.md).

- [ ] `CONF-022` **[CONFORMANCE][SOL_HIGH] Prove bounded bulk policy acknowledgement without false legal satisfaction.**
  - **Depends:** `CONF-001`, `POP-005`, `INTENT-019`, `MSG-013`, `ATTEST-008`, `WF-STEP-007`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestBulkPolicyAcknowledgementConformancePreservesRecipientObligations`.
  - **TEST MATRIX:** `PRIMARY=TestBulkPolicyAcknowledgementConformancePreservesRecipientObligations`; `PROPERTY=TestTodo_CONF_022_Property`; `GOLDEN=TestTodo_CONF_022_Golden`; `RACE=TestTodo_CONF_022_Race`; `INTEGRATION=TestTodo_CONF_022_Integration`; `FAULT=TestTodo_CONF_022_Fault`; `SECURITY=TestTodo_CONF_022_Security`; `CONFORMANCE=TestTodo_CONF_022_Conformance`; `BENCHMARK=BenchmarkTodo_CONF_022`; `MUTATION=TestTodo_CONF_022_Mutation`.
  - **RED:** audience changes after freeze, partition retry duplicates delivery, child broadens authority, JOIN timeout converts a mandatory missing acknowledgement to success, delivery substitutes for acknowledgement, inaccessible/localization-deficient content counts satisfied, aggregate leaks recipients or cancellation loses child state.
  - **GREEN:** versioned audience snapshot partitions within concurrency/cost limits, each child binds recipient/artifact/version/idempotency and verified signal, REQUIRED_SET join accounts for acknowledged/unreachable/excluded/degraded results, mandatory gaps create human work and terminal dimensions never overstate legal satisfaction.
  - **REFACTOR:** parent retains typed aggregates and child references rather than copying protected recipient payloads.
  - **Refs:** [Bulk acknowledgement sample](workflows/samples/bulk-policy-acknowledgement.md), [messaging requirements](specs/messaging-and-notification-plane.md).

- [ ] `CONF-023` **[CONFORMANCE][SOL_HIGH] Prove agent-assisted HR case triage cannot become an employment-decision path.**
  - **Depends:** `CONF-001`, `CONF-014`, `AGENT-001`–`AGENT-004`, `DOC-MAL-001`, `RULE-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestAgentCaseTriageConformanceContainsHostileContentAndAuthority`.
  - **TEST MATRIX:** `PRIMARY=TestAgentCaseTriageConformanceContainsHostileContentAndAuthority`; `PROPERTY=TestTodo_CONF_023_Property`; `GOLDEN=TestTodo_CONF_023_Golden`; `FUZZ=FuzzTodo_CONF_023`; `INTEGRATION=TestTodo_CONF_023_Integration`; `FAULT=TestTodo_CONF_023_Fault`; `SECURITY=TestTodo_CONF_023_Security`; `CONFORMANCE=TestTodo_CONF_023_Conformance`; `MUTATION=TestTodo_CONF_023_Mutation`.
  - **RED:** hostile attachment reaches prompt, client supplies system/tool messages, discovery leaks cases, model invokes a write, uncited/invalid taxonomy routes, protected keywords become an adverse decision, timeout/budget/provider/kill-switch failure drops the request, or prompt/output enters default telemetry.
  - **GREEN:** quarantine and minimum-necessary redaction precede read-only agent execution; typed cited TriageProposal validation plus deterministic rules routes high-risk/unknown to specialist and standard cases to governed creation, every failure takes deterministic/manual path and final evidence states the agent never made an employment decision.
  - **REFACTOR:** model/provider implementations remain replaceable behind identical taint, tool, output and fallback conformance.
  - **Refs:** [Agent case-triage sample](workflows/samples/agent-assisted-hr-case-triage.md), [HR case conformance](#21-future-domain-intent-conformance).

- [ ] `CONF-024` **[CONFORMANCE][SOL_HIGH] Execute the complete adversarial workflow-context edge matrix.**
  - **Depends:** `CONF-001`, `WF-DISC-004`, `WF-RUN-016`, `GOVERN-003`, `INTG-016`, `APPROVAL-006`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestWorkflowContextEdgeMatrixHasNoImplicitAuthorityOrCompletion`.
  - **TEST MATRIX:** `PRIMARY=TestWorkflowContextEdgeMatrixHasNoImplicitAuthorityOrCompletion`; `PROPERTY=TestTodo_CONF_024_Property`; `GOLDEN=TestTodo_CONF_024_Golden`; `RACE=TestTodo_CONF_024_Race`; `INTEGRATION=TestTodo_CONF_024_Integration`; `FAULT=TestTodo_CONF_024_Fault`; `SECURITY=TestTodo_CONF_024_Security`; `CONFORMANCE=TestTodo_CONF_024_Conformance`; `RECOVERY=TestTodo_CONF_024_Recovery`; `MUTATION=TestTodo_CONF_024_Mutation`.
  - **RED:** any required case—late/out-of-order event, queued authority handoff, legal change while waiting, assignee authority churn, delegation/SoD loop, one-subject bulk invalidation, shared/offline device, materially masked approval, provider accepted-not-applied, manual outage continuity or multi-step accessibility/localization—lacks a deterministic outcome, evidence chronology and zero-prohibited-effect assertion.
  - **GREEN:** version-pinned matrix executes every case through public capabilities and returns exact `BLOCK|REPLAN|REAPPROVE|ROUTE_HUMAN|DEGRADE|REPAIR_REQUIRED|COMPLETE` dimensions, authority/taint/evidence bindings and ledger/effect counts; no transport acceptance, stale decision or manual continuity path fabricates business completion.
  - **REFACTOR:** reusable fixtures compose across vertical workflows while each domain retains its own legal, authority and mutation semantics.
  - **Refs:** [Workflow context layers](workflows/_engine/workflow-context-layers.md#open-research-edges), [edge-case samples](workflows/samples/workflow-context-edge-cases.md), [adversarial audit](workflows/_engine/workflow-context-adversarial-audit-2026-08-14.md).

## 48. Feature-to-Intent semantic instruction-set closure

> **Disposition (2026-09-02):** DEFERRED. The feature intake has no funded consumer; `FEATURE-003` is rewritten to drop the catalog-release denominator.

- [x] `FEATURE-003` **[DESIGN][SOL_HIGH] Disposition every intake feature that is not one of the fourteen drafted definitions.**
  - **Evidence (2026-09-05):** `TestFeatureExtensionDispositionRejectsSilentAliasOrScopeExpansion` in `tools/planning/intentmanifests` (every intake feature outside the fourteen drafted definitions carries exactly one disposition (DEFERRED_TO_INTENT naming the future intent, NON_MATERIAL, MERGED_INTO naming the canonical feature, or REVIEW), counts reported per disposition, a feature without one fails the build; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/planning/intentmanifests/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `FEATURE-002`, `GOV-006`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestFeatureExtensionDispositionRejectsSilentAliasOrScopeExpansion`.
  - **TEST MATRIX:** `PRIMARY=TestFeatureExtensionDispositionRejectsSilentAliasOrScopeExpansion`; `GOLDEN=TestTodo_FEATURE_003_Golden`.
  - **RED:** a feature is implemented without a drafted definition, silently aliased to a near-name, or accepted without entity/property/authority/privacy/effect/evidence analysis.
  - **GREEN:** every non-drafted feature receives one of `ALIAS|TEMPLATE|COMPOSITE|CONSUMER|OBSERVER|NON_MATERIAL|NEW_DEFINITION_CANDIDATE|DEFERRED|REJECTED` with rationale and owner; a `NEW_DEFINITION_CANDIDATE` becomes work only when a funded domain writes its `DRAFT_CONTRACT`.
  - **REFACTOR:** the disposition list is a review artifact with no release denominator; there is no catalog count to reconcile against.
  - **Refs:** [Vocabulary list (non-normative)](specs/business-intent-catalog.md#vocabulary-list-non-normative), [scope exchange](execution-plan.md#scope-exchange-rule).

- [ ] `INTENT-026` **[PHASE_2][SOL_HIGH] Define immutable IntentCompositionPlan and IntentBundle semantics.**
  - **Depends:** `INTENT-015`, `INTENT-016`, `WF-COMP-002`, `TX-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestIntentCompositionPlanRejectsCyclesHiddenChildrenAndFalseAtomicity`.
  - **TEST MATRIX:** `PRIMARY=TestIntentCompositionPlanRejectsCyclesHiddenChildrenAndFalseAtomicity`; `PROPERTY=TestTodo_INTENT_026_Property`; `GOLDEN=TestTodo_INTENT_026_Golden`; `RACE=TestTodo_INTENT_026_Race`; `FAULT=TestTodo_INTENT_026_Fault`; `SECURITY=TestTodo_INTENT_026_Security`; `CONFORMANCE=TestTodo_INTENT_026_Conformance`; `MUTATION=TestTodo_INTENT_026_Mutation`.
  - **RED:** composer emits a cyclic/duplicate/ownerless child graph, hides a material child, broadens tenant/org/purpose/delegation, claims cross-system atomicity, leaves ordering/parallelism/completion/cancellation unspecified, or changes the bundle after approval; duplicate compilation creates different semantic identities.
  - **GREEN:** immutable plan binds parent definition/proposal, typed child templates and ordinals, dependency DAG, authority attenuation/approved expansion, consistency boundaries, wait/detach/failure/correction/cancellation policies, resource/cost limits and deterministic digest; bundle is packaging/inspection evidence and never an alternate execution gateway.
  - **REFACTOR:** workflow compilation may realize the plan, but intent relationships retain the business why and independent child governance/results.
  - **Refs:** [Intent relationships](specs/business-intent-and-change-request.md), [workflow compiler](specs/workflow-runtime.md), [transaction coordinator](specs/transaction-plan-and-commit-coordinator.md).

- [ ] `INTENT-027` **[GATE_B][SOL_HIGH] Compile one universal IntentPreflightPlan before proposal submission.**
  - **Depends:** `INTENT-005`, `SNAPSHOT-003`, `CONFLICT-003`, `GOVERN-002`, `WF-COMP-003`, `REPLAN-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestIntentPreflightPlanBindsSimulationRiskCostApprovalsObligationsAndEffects`.
  - **TEST MATRIX:** `PRIMARY=TestIntentPreflightPlanBindsSimulationRiskCostApprovalsObligationsAndEffects`; `PROPERTY=TestTodo_INTENT_027_Property`; `GOLDEN=TestTodo_INTENT_027_Golden`; `RACE=TestTodo_INTENT_027_Race`; `FAULT=TestTodo_INTENT_027_Fault`; `SECURITY=TestTodo_INTENT_027_Security`; `CONFORMANCE=TestTodo_INTENT_027_Conformance`; `MUTATION=TestTodo_INTENT_027_Mutation`.
  - **RED:** intent cost, risk, conflict, approval, obligation, write/effect, authority, revalidation or repair planning is absent, calculated from mismatched snapshots/control versions, hidden in workflow branches, treated as guaranteed external outcome or mutates domain/external/human state during preflight; a redacted material input produces an unsafe affirmative plan.
  - **GREEN:** side-effect-free plan binds exact request/snapshot/proposal candidate, assumptions/UNKNOWN states, engine/control versions, calculated cost/risk, conflicts/reservations, approvals/tasks, legal/privacy obligations, intended writes/effect DAG/observations, revalidation triggers, completion policy and repair expectations with canonical digest and redaction-safe explanation.
  - **REFACTOR:** domain simulations supply business meaning while the universal plan owns composition and comparable evidence, not domain calculations.
  - **Refs:** [Workflow simulation](specs/workflow-runtime.md), [governance composition](specs/governance-decision-and-obligation-composition.md), [transaction plan](specs/transaction-plan-and-commit-coordinator.md).

- [x] `INTENT-028` **[PHASE_2][SOL_HIGH] Govern live intent-definition evolution through compatibility or supersession, never in-place mutation.**
  - **Evidence (2026-09-05):** `TestLiveIntentVersionEvolutionRequiresCompatibleBindingOrSuccessorIntent` in `internal/intent/evolution`, `internal/intent` (compatibility verdicts between two versions of one intent definition (optional inputs added compatible; required input added or removed, type changed, effect class raised or approval requirement removed incompatible with the field named), an immutable digested supersession record with a reason, distinct approver, effective instant and live-instance policy, refusal of any in-place edit to a published version proven by digest, golden verdict table for a pair of Promotion definition versions; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/intent/evolution/ ./internal/intent/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `INTENT-001`, `INTENT-003`, `INTENT-015`, `WF-RUN-017`, `CONFIG-002`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestLiveIntentVersionEvolutionRequiresCompatibleBindingOrSuccessorIntent`.
  - **TEST MATRIX:** `PRIMARY=TestLiveIntentVersionEvolutionRequiresCompatibleBindingOrSuccessorIntent`; `PROPERTY=TestTodo_INTENT_028_Property`; `GOLDEN=TestTodo_INTENT_028_Golden`; `RACE=TestTodo_INTENT_028_Race`; `FAULT=TestTodo_INTENT_028_Fault`; `SECURITY=TestTodo_INTENT_028_Security`; `CONFORMANCE=TestTodo_INTENT_028_Conformance`; `RECOVERY=TestTodo_INTENT_028_Recovery`; `MUTATION=TestTodo_INTENT_028_Mutation`.
  - **RED:** publication changes a live instance's definition/family/input/result/governance/completion meaning, workflow migration silently changes business intent, incompatible payload/evidence is coerced, old approvals are retained after material migration, rollback rewrites history or concurrent migration/execution commits both meanings.
  - **GREEN:** compatibility preview compares schemas, semantics, controls, workflow bindings, outcomes and active states; compatible implementation/workflow migration preserves the original IntentDefinition contract, while material change creates proposal-bound successor/superseding intent with explicit state/evidence mapping, revalidation/reapproval and fenced cutover/rollback receipt.
  - **REFACTOR:** definition lifecycle, workflow runtime migration and configuration rollout share evidence/version primitives but remain separate authorities.
  - **Refs:** [Intent definition lifecycle](specs/business-intent-and-change-request.md), [workflow migration](specs/workflow-runtime.md), [configuration lifecycle](data/models/dataops-configuration.md).

- [ ] `INTENT-029` **[PHASE_2][SOL_HIGH] Link observed outcomes to intents without fabricating causation or rewriting results.**
  - **Depends:** `INTENT-007`, `INTENT-020`, `MODEL-020`, `REPRO-001`, `ANALYTICS-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=govern catalog, authority, lifecycle, evidence or backlog coverage for material HCM intents`.
  - **TEST:** `TestIntentOutcomeLinkPreservesObservationProvenanceAndCausalLimits`.
  - **TEST MATRIX:** `PRIMARY=TestIntentOutcomeLinkPreservesObservationProvenanceAndCausalLimits`; `PROPERTY=TestTodo_INTENT_029_Property`; `GOLDEN=TestTodo_INTENT_029_Golden`; `SECURITY=TestTodo_INTENT_029_Security`; `CONFORMANCE=TestTodo_INTENT_029_Conformance`; `MUTATION=TestTodo_INTENT_029_Mutation`.
  - **RED:** later metric/observation overwrites IntentResult, correlation is labeled caused-by, unauthorized cohort/outcome leaks through intent inspection, missing/confounded/stale outcomes default positive, model-generated assessment becomes domain fact or correction loses the original link/version.
  - **GREEN:** append-only OutcomeLink binds exact intent/result/proposal, outcome observation/metric definition, subject/cohort scope, effective/known/observed times, source/watermark, epistemic class, confidence/limitations and `OBSERVED_ASSOCIATION|DECLARED_CAUSAL|UNKNOWN` status; correction supersedes the link and analytical access remains purpose/disclosure governed.
  - **REFACTOR:** transactional completion and long-term outcome analysis remain independent dimensions connected by provenance rather than lifecycle mutation.
  - **Refs:** [Decision outcomes](data/models/assurance-intelligence-platform.md), [provenance](specs/provenance-graph-and-lineage.md), [risk 141](specs/risk-register.md).

- [x] `SEARCH-003` **[PHASE_2][SOL_HIGH] Convert authorized search results into frozen populations and bounded actions safely.**
  - **Evidence (2026-09-05):** `TestSearchToPopulationToBatchPreservesScopeSnapshotAndNonDisclosure` in `internal/engines/search` (conversion records authorized query/semantic plan/index and policy versions, minimum watermark, visible selection/exclusions and immutable PopulationSnapshot; subsequent AnalyticalRequest or BatchOperation uses fresh governance, bounded child identities and exact per-member resul; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/engines/search/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SEARCH-001`, `SEARCH-002`, `POP-005`, `INTENT-019`, `INTENT-020`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose governed intent creation, inspection or consumption without persistence or provider bypass`.
  - **TEST:** `TestSearchToPopulationToBatchPreservesScopeSnapshotAndNonDisclosure`.
  - **TEST MATRIX:** `PRIMARY=TestSearchToPopulationToBatchPreservesScopeSnapshotAndNonDisclosure`; `PROPERTY=TestTodo_SEARCH_003_Property`; `GOLDEN=TestTodo_SEARCH_003_Golden`; `RACE=TestTodo_SEARCH_003_Race`; `INTEGRATION=TestTodo_SEARCH_003_Integration`; `FAULT=TestTodo_SEARCH_003_Fault`; `SECURITY=TestTodo_SEARCH_003_Security`; `CONFORMANCE=TestTodo_SEARCH_003_Conformance`; `BENCHMARK=BenchmarkTodo_SEARCH_003`; `MUTATION=TestTodo_SEARCH_003_Mutation`.
  - **RED:** live ranked results become a batch loop, hidden/denied counts or similarity scores leak, membership changes between preview/approval/execution, stale index bypasses authority/freshness, selected result fields become child inputs without permission or retry duplicates children.
  - **GREEN:** conversion records authorized query/semantic plan/index and policy versions, minimum watermark, visible selection/exclusions and immutable PopulationSnapshot; subsequent AnalyticalRequest or BatchOperation uses fresh governance, bounded child identities and exact per-member results rather than search authority or live membership.
  - **REFACTOR:** search ranks candidates, Population owns frozen membership and BusinessIntent owns material action.
  - **Refs:** [Search-driven features](#25-search-metrics-and-outcome-intelligence), [population engine](#37-shared-transformation-and-population-engines), [batch intents](#6-businessintent-capabilities-governance-and-transaction-integrity).

- [ ] `FEATURE-CONF-001` **[CONFORMANCE][SOL_HIGH] Prove semantic parity across every actor, channel and feature-to-intent role.**
  - **Depends:** `FEATURE-003`, `INTENT-013`, `INTENT-024`, `UX-006`–`UX-008`, `API-001`, `APP-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestFeatureIntentRoleChannelMatrixHasNoSemanticOrAuthorityDrift`.
  - **TEST MATRIX:** `PRIMARY=TestFeatureIntentRoleChannelMatrixHasNoSemanticOrAuthorityDrift`; `PROPERTY=TestTodo_FEATURE_CONF_001_Property`; `GOLDEN=TestTodo_FEATURE_CONF_001_Golden`; `RACE=TestTodo_FEATURE_CONF_001_Race`; `INTEGRATION=TestTodo_FEATURE_CONF_001_Integration`; `FAULT=TestTodo_FEATURE_CONF_001_Fault`; `SECURITY=TestTodo_FEATURE_CONF_001_Security`; `CONFORMANCE=TestTodo_FEATURE_CONF_001_Conformance`; `BROWSER=TestTodo_FEATURE_CONF_001_Browser`; `MUTATION=TestTodo_FEATURE_CONF_001_Mutation`.
  - **RED:** equivalent feature invoked by employee, manager, HR admin, delegated actor, agent, partner app, schedule, event, operator, GWC, mobile/kiosk, gRPC, HTTP, CLI or bulk surface resolves a different semantic definition/schema/result, trusts caller context, weakens governance, hides child intents, bypasses simulation or produces divergent canonical request digest without an explicit channel policy.
  - **GREEN:** generated matrix covers all 49 source groups and every applicable `CREATE|CONSUME|EMIT_CHILD|OBSERVE|NON_MATERIAL` role; identical typed inputs resolve identical intent/capability semantics while trusted origin and current policy legitimately alter authorization/presentation only, and unavailable/deferred features cannot be invoked on any channel.
  - **REFACTOR:** one generated route/action manifest drives channel adapters and tests; channel-specific ergonomics never duplicate business semantics.
  - **Refs:** [Feature coverage](#6-businessintent-capabilities-governance-and-transaction-integrity), [capability parity](specs/capability-registry-and-lifecycle.md), [experience](specs/experience-ui-and-branding.md).

- [ ] `INTENT-CONF-002` **[CONFORMANCE][SOL_HIGH] Prove the higher-order BusinessIntent instruction set end to end.**
  - **Depends:** `INTENT-026`–`INTENT-029`, `SEARCH-003`, `FEATURE-CONF-001`, `CONF-001`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove accepted intent behavior or delivery evidence without creating production authority`.
  - **TEST:** `TestHigherOrderIntentInstructionSetConformance`.
  - **TEST MATRIX:** `PRIMARY=TestHigherOrderIntentInstructionSetConformance`; `PROPERTY=TestTodo_INTENT_CONF_002_Property`; `GOLDEN=TestTodo_INTENT_CONF_002_Golden`; `RACE=TestTodo_INTENT_CONF_002_Race`; `INTEGRATION=TestTodo_INTENT_CONF_002_Integration`; `FAULT=TestTodo_INTENT_CONF_002_Fault`; `SECURITY=TestTodo_INTENT_CONF_002_Security`; `CONFORMANCE=TestTodo_INTENT_CONF_002_Conformance`; `RECOVERY=TestTodo_INTENT_CONF_002_Recovery`; `MUTATION=TestTodo_INTENT_CONF_002_Mutation`.
  - **RED:** compose/template/bundle/dependency/preflight/simulate/compare/fork/schedule/subscribe/supersede/compensate/repair/replay/shadow/migrate/explain/evidence-export/outcome-link operation can mutate through a side door, lose proposal/version/authority/evidence lineage, duplicate effects under replay, broaden child scope or conflate historical analysis with a new authorized instruction.
  - **GREEN:** deterministic scenarios prove each higher-order operation is a typed creator/consumer/emitter/observer with exact relationship, lifecycle, governance, zero-effect or authorized-effect contract; crashes/replays recover without duplicated instruction/effect and complete evidence reconstructs why every child, successor, repair and outcome link exists.
  - **REFACTOR:** conformance templates share universal mechanics while fixtures retain domain-specific materiality, legality, financial and privacy oracles.
  - **Refs:** [BusinessIntent contract](specs/business-intent-and-change-request.md), [feature registry](#6-businessintent-capabilities-governance-and-transaction-integrity), [reference conformance](#19-reference-workflow-conformance-harness).

## 49. Exact initial BusinessIntent delivery closure

> **Disposition (2026-09-02):** Active as labelled: `PEOPLE-005`, `COMP-006`, `INTEL-001`, `REPAIR-003` are P1A; `APPROVAL-008` is P1B; `CONF-025` is CONFORMANCE.

- [ ] `CONF-025` **[CONFORMANCE][SOL_HIGH] Prove the standalone ChangeManager intent without broadening Gate B authority.**
  - **Depends:** `CONF-001`, `ORG-001`, `ORG-002`, `CONFLICT-003`, `GOVERN-003`, `WF-COMP-005`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.PEOPLE; INTENTS=hcmnext.people.change_manager/v1; FAMILY=CHANGE_REQUEST; DIRECT=none; WHY=cover accepted intent 28 at its declared design/conformance-only depth`.
  - **TEST:** `TestChangeManagerIntentConformancePreservesGraphAuthorityAndZeroProductionExposure`.
  - **TEST MATRIX:** `PRIMARY=TestChangeManagerIntentConformancePreservesGraphAuthorityAndZeroProductionExposure`; `PROPERTY=TestTodo_CONF_025_Property`; `GOLDEN=TestTodo_CONF_025_Golden`; `RACE=TestTodo_CONF_025_Race`; `SECURITY=TestTodo_CONF_025_Security`; `CONFORMANCE=TestTodo_CONF_025_Conformance`; `MUTATION=TestTodo_CONF_025_Mutation`.
  - **RED:** inactive worker/manager, self-management, graph cycle, stale future relationship, conflicting transfer/termination, unauthorized org scope, unbound approval, effective-date drift or external observation asserted as authority yields an executable standalone mutation or published production capability.
  - **GREEN:** fixture compiles the exact typed intent through snapshot, validation, AuthZ, conflict, simulation, approval, wait, revalidation and one relationship write plan; every negative case returns its exact block/replan state, and production registry exposure remains absent at conformance-only depth.
  - **REFACTOR:** reuse promotion relationship capabilities and workflow primitives without aliasing ChangeManager to PromoteWorker.
  - **Refs:** [Business Intent draft slice](specs/business-intent-catalog.md#initial-draft-contract-slice), [manager-change workflow](workflows/people/manager-change.md), [organization model](data/models/people-workforce.md).

- [x] `PEOPLE-005` **[GATE_A][SOL_HIGH] Implement the governed ExplainWorkerState analytical intent.**
  - **Depends:** `PEOPLE-001`–`PEOPLE-003`, `ORG-001`, `INTENT-013`, `CAP-002`, `TRUST-010`, `DATAOPS-007`.
  - **INTENT CONTEXT:** `ROLE=DIRECT; SETS=BI.PEOPLE; INTENTS=hcmnext.people.explain_worker_state/v1; FAMILY=ANALYTICAL_REQUEST; WHY=implement accepted intent 36 as the governed read/explanation entry point`.
  - **TEST:** `TestExplainWorkerStateReturnsAuthorizedBitemporalFactsAndProvenance`.
  - **TEST MATRIX:** `PRIMARY=TestExplainWorkerStateReturnsAuthorizedBitemporalFactsAndProvenance`; `PROPERTY=TestTodo_PEOPLE_005_Property`; `GOLDEN=TestTodo_PEOPLE_005_Golden`; `INTEGRATION=TestTodo_PEOPLE_005_Integration`; `SECURITY=TestTodo_PEOPLE_005_Security`; `CONFORMANCE=TestTodo_PEOPLE_005_Conformance`; `MUTATION=TestTodo_PEOPLE_005_Mutation`.
  - **RED:** explanation trusts caller-supplied current facts, omits effective-at/known-at/source authority, leaks denied fields/existence, collapses redacted/unknown/partial values, rewrites corrections or emits a domain/external/human effect.
  - **GREEN:** one governed invocation returns typed authorized worker/employment/assignment/relationship facts with presence, revision, effective/known time, source authority, provenance and bounded explanation; denied and incomplete cases return typed non-disclosing states and all write/effect/outbox counts remain zero.
  - **REFACTOR:** compose owned read capabilities and history projection; do not create a second worker query model inside Intent.
  - **Refs:** [Initial draft intents](specs/business-intent-catalog.md#initial-draft-contract-slice), [People domain](specs/people-employment-assignment-domain.md), [source authority](specs/source-authority-and-external-mastering.md).
  - **Evidence (2026-09-03):** `TestExplainWorkerStateReturnsAuthorizedBitemporalFactsAndProvenance`, `TestTodo_PEOPLE_005_Security/_Property/_Golden` in `internal/domains/people`; `go test -count=1 ./internal/domains/...` PASS on windows/arm64 (Go 1.26.3); authorization is an input decision, unruled fields refused, withheld subjects return no presence; branch plan-revision-2026-09-02.

- [x] `COMP-006` **[GATE_A][SOL_HIGH] Implement the standalone SimulateCompensation calculation intent.**
  - **Depends:** `COMP-001`–`COMP-003`, `BUDGET-001`, `SNAPSHOT-003`, `INTENT-023`, `CAP-002`, `GOVERN-002`.
  - **INTENT CONTEXT:** `ROLE=DIRECT; SETS=BI.REWARDS; INTENTS=hcmnext.rewards.simulate_compensation/v1; FAMILY=CALCULATION_REQUEST; WHY=implement accepted intent 81 as a pure version-pinned compensation simulation`.
  - **TEST:** `TestSimulateCompensationReturnsExactVersionedResultWithZeroEffects`.
  - **TEST MATRIX:** `PRIMARY=TestSimulateCompensationReturnsExactVersionedResultWithZeroEffects`; `PROPERTY=TestTodo_COMP_006_Property`; `GOLDEN=TestTodo_COMP_006_Golden`; `FUZZ=FuzzTodo_COMP_006`; `INTEGRATION=TestTodo_COMP_006_Integration`; `SECURITY=TestTodo_COMP_006_Security`; `CONFORMANCE=TestTodo_COMP_006_Conformance`; `MUTATION=TestTodo_COMP_006_Mutation`.
  - **RED:** float/ambient rounding, mismatched currency/basis, stale or partial snapshot, redacted material input, unpinned band/formula/calendar/authority, budget observation presented as guarantee, or simulation creates proposal/approval/work/domain/external state.
  - **GREEN:** result returns exact current/proposed components, annualization/proration, deltas, band result, assumptions/UNKNOWN values, authority/provenance and formula/control versions with canonical digest; all authoritative writes, reservations, work, messages, outbox and provider calls equal zero.
  - **REFACTOR:** the intent composes compensation calculation capabilities; workflow and UI consume the same typed result.
  - **Refs:** [Compensation domain](specs/compensation-domain.md), [simulation contract](specs/workflow-runtime.md), [Business Intent draft slice](specs/business-intent-catalog.md#initial-draft-contract-slice).
  - **Evidence (2026-09-03):** `TestSimulateCompensationReturnsExactVersionedResultWithZeroEffects`, `TestTodo_COMP_006_Security/_Property/_Golden`, `FuzzTodo_COMP_006` in `internal/domains/rewards` plus `internal/engines/payband` (`TestEvaluatePayBandPosition*`, `FuzzEvaluatePayBandPosition`); `go test -count=1 ./internal/domains/... ./internal/engines/...` PASS; exact apd decimals, byte-identical result for identical inputs, zero effect counters; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `APPROVAL-008` **[GATE_B][SOL_HIGH] Implement governed ApproveProposal and RejectProposal intent entry points.**
  - **Evidence (2026-09-05):** `TestProposalDecisionIntentsReturnBoundDecisionAndCompleteExactlyOneWorkItem` in `internal/intent/app` (ApproveProposal and RejectProposal in internal/intent/app enforce separation of duties (the proposer may not decide) and delegate the journey Decide path to the routed approver principal, recording the decision on the work item and the intent in one transaction; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/intent/app/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `APPROVAL-001`–`APPROVAL-007`, `INTENT-013`, `WORK-005`, `TRUST-013`, `TRUST-014`.
  - **INTENT CONTEXT:** `ROLE=DIRECT; SETS=BI.WORK; INTENTS=hcmnext.work.approve_proposal/v1,hcmnext.work.reject_proposal/v1; FAMILY=CHANGE_REQUEST; WHY=implement accepted intents 366 and 367 through the universal gateway`.
  - **TEST:** `TestProposalDecisionIntentsReturnBoundDecisionAndCompleteExactlyOneWorkItem`.
  - **TEST MATRIX:** `PRIMARY=TestProposalDecisionIntentsReturnBoundDecisionAndCompleteExactlyOneWorkItem`; `PROPERTY=TestTodo_APPROVAL_008_Property`; `GOLDEN=TestTodo_APPROVAL_008_Golden`; `RACE=TestTodo_APPROVAL_008_Race`; `INTEGRATION=TestTodo_APPROVAL_008_Integration`; `FAULT=TestTodo_APPROVAL_008_Fault`; `SECURITY=TestTodo_APPROVAL_008_Security`; `CONFORMANCE=TestTodo_APPROVAL_008_Conformance`; `MUTATION=TestTodo_APPROVAL_008_Mutation`.
  - **RED:** caller chooses trusted actor/scope, stale proposal/control/authority, wrong assignee, delegation/SoD violation, duplicate/opposite concurrent decision, inaccessible material context or crash between decision and work completion produces an accepted decision, double completion or downstream execution.
  - **GREEN:** exact intent resolves current requirement and server-rendered receipt, records one proposal/control/authority-bound `APPROVED|REJECTED` decision and atomically completes one WorkItem; replay returns the same decision, conflict returns typed error, and the decision alone never executes the parent proposal.
  - **REFACTOR:** approval and rejection share mechanics and retain distinct immutable semantic identities/results.
  - **Refs:** [Approval safety](specs/human-work-forms-and-rules.md), [proposal binding](plan.md#53-approval-must-bind-to-an-immutable-proposal), [draft intent slice](specs/business-intent-catalog.md#initial-draft-contract-slice).

- [x] `INTEL-001` **[GATE_A][SOL_HIGH] Implement the governed ExplainTransaction analytical intent.**
  - **Depends:** `TX-006`, `LEDGER-003`, `LEDGER-005`, `DATAOPS-007`, `INTENT-013`, `CAP-002`, `TRUST-010`.
  - **INTENT CONTEXT:** `ROLE=DIRECT; SETS=BI.INTELLIGENCE; INTENTS=hcmnext.intelligence.explain_transaction/v1; FAMILY=ANALYTICAL_REQUEST; WHY=implement accepted intent 457 from immutable transaction, ledger and provenance evidence`.
  - **TEST:** `TestExplainTransactionReconstructsAuthorizedChronologyWithoutCausalFabrication`.
  - **TEST MATRIX:** `PRIMARY=TestExplainTransactionReconstructsAuthorizedChronologyWithoutCausalFabrication`; `PROPERTY=TestTodo_INTEL_001_Property`; `GOLDEN=TestTodo_INTEL_001_Golden`; `INTEGRATION=TestTodo_INTEL_001_Integration`; `SECURITY=TestTodo_INTEL_001_Security`; `CONFORMANCE=TestTodo_INTEL_001_Conformance`; `MUTATION=TestTodo_INTEL_001_Mutation`.
  - **RED:** missing/unauthorized transaction leaks existence, chronology omits proposal/approvals/write set/events/outbox/observations/repairs, external observation becomes domain fact, correlation becomes causation, stale projection overrides ledger evidence, or explanation triggers replay/repair/effect.
  - **GREEN:** typed response reconstructs authorized intent/proposal/governance/transaction/event/effect/observation/reconciliation/repair links with authority, effective/known/recorded times, completeness and epistemic labels; redacted/partial/missing cases remain explicit and all mutation/effect counts are zero.
  - **REFACTOR:** explanation queries owned evidence/provenance contracts and remains independent of model-generated narration.
  - **Refs:** [Ledger and repair](specs/transaction-ledger-reconciliation-and-repair.md), [provenance](specs/provenance-graph-and-lineage.md), [initial draft intents](specs/business-intent-catalog.md#initial-draft-contract-slice).
  - **Evidence (2026-09-03):** `TestExplainTransactionReconstructsAuthorizedChronologyWithoutCausalFabrication`, `TestTodo_INTEL_001_{Property,Golden,Integration,Security,Conformance,Mutation}` in `internal/domains/intelligence` (`ExplainTransaction` over a `TransactionHistory` port with authorization-input redaction, deterministic); PASS; branch plan-revision-2026-09-02; `go test -count=1 ./internal/domains/intelligence/...` on windows/arm64 (Go 1.26.3).

- [x] `REPAIR-003` **[GATE_A][SOL_HIGH] Implement the pure SimulateRepair calculation intent.**
  - **Depends:** `REPAIR-001`, `INTENT-023`, `GOVERN-002`, `TX-001`, `CAP-002`.
  - **INTENT CONTEXT:** `ROLE=DIRECT; SETS=BI.OPERATIONS; INTENTS=hcmnext.operations.simulate_repair/v1; FAMILY=CALCULATION_REQUEST; WHY=implement accepted intent 464 without granting repair execution authority`.
  - **TEST:** `TestSimulateRepairReturnsBoundImpactAndZeroCorrectiveEffects`.
  - **TEST MATRIX:** `PRIMARY=TestSimulateRepairReturnsBoundImpactAndZeroCorrectiveEffects`; `PROPERTY=TestTodo_REPAIR_003_Property`; `GOLDEN=TestTodo_REPAIR_003_Golden`; `FUZZ=FuzzTodo_REPAIR_003`; `INTEGRATION=TestTodo_REPAIR_003_Integration`; `FAULT=TestTodo_REPAIR_003_Fault`; `SECURITY=TestTodo_REPAIR_003_Security`; `CONFORMANCE=TestTodo_REPAIR_003_Conformance`; `MUTATION=TestTodo_REPAIR_003_Mutation`.
  - **RED:** simulation uses stale finding/observation/control versions, hides sibling effects or irreversible/ambiguous risk, claims guaranteed correction, broadens target/authority, or creates reservation/approval/work/domain/external/connector state.
  - **GREEN:** result binds exact RepairPlan and current evidence, returns proposed writes/effects, risks, approvals/SoD, cost, observation/success/rollback expectations and `VALID|REPLAN_REQUIRED|NO_LONGER_REQUIRED|BLOCKED|UNKNOWN`; every corrective, provider, outbox and human-work count remains zero.
  - **REFACTOR:** reuse transaction/effect simulation contracts while keeping repair diagnosis, simulation and execution separate authorities.
  - **Refs:** [Repair architecture](specs/transaction-ledger-reconciliation-and-repair.md), [workflow simulation](specs/workflow-runtime.md), [initial draft intents](specs/business-intent-catalog.md#initial-draft-contract-slice).
  - **Evidence (2026-09-03):** `TestSimulateRepairReturnsBoundImpactAndZeroCorrectiveEffects`, `TestTodo_REPAIR_003_{Property,Golden,Integration,Fault,Security,Conformance,Mutation}`, `FuzzTodo_REPAIR_003` in `internal/domains/repair` (pure simulation, residual diffs, zero-effect receipt); PASS; branch plan-revision-2026-09-02; `go test -count=1 ./internal/domains/repair/...` on windows/arm64 (Go 1.26.3).

## 50. Critical HCM domain-engine ownership

> **Disposition (2026-09-02):** DEFERRED.

- [ ] `RECRUIT-001` **[PHASE_3][SOL_HIGH] Define authoritative requisition, posting, application and candidacy lifecycles.**
  - **Depends:** `MODEL-016`, `INTENT-CONF-001`, `JOB-001`, `POSITION-001`, `CRM-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.RECRUITING; DIRECT=none; WHY=own native ATS transaction semantics rather than treating CRM or an external connector as the recruiting domain`.
  - **TEST:** `TestRecruitingAggregateLifecyclesRejectMissingIdentityAndIllegalTransitions`.
  - **TEST MATRIX:** `PRIMARY=TestRecruitingAggregateLifecyclesRejectMissingIdentityAndIllegalTransitions`; `PROPERTY=TestTodo_RECRUIT_001_Property`; `GOLDEN=TestTodo_RECRUIT_001_Golden`; `SECURITY=TestTodo_RECRUIT_001_Security`; `CONFORMANCE=TestTodo_RECRUIT_001_Conformance`; `MUTATION=TestTodo_RECRUIT_001_Mutation`.
  - **RED:** application without candidate/requisition/posting revisions, duplicate application under declared uniqueness policy, posting after requisition closure, candidacy without consent/purpose, or transition from terminal `WITHDRAWN|REJECTED|HIRED` is accepted and appends a recruiting event.
  - **GREEN:** immutable aggregate revisions enforce exact IDs, source/consent, effective/known times and allowed state machines; invalid commands return `MISSING_PARENT|DUPLICATE_APPLICATION|REQUISITION_CLOSED|INVALID_CANDIDACY_TRANSITION` with zero events/outbox.
  - **REFACTOR:** Person/prospect identity, job/position truth and CRM membership remain referenced owners rather than copied ATS facts.
  - **Refs:** [Recruiting intents](specs/business-intent-catalog.md#vocabulary-list-non-normative), [talent models](data/models/talent-experience-cases.md), [recruit workflow](workflows/lifecycle/recruit-hire-onboard.md).

- [ ] `RECRUIT-002` **[PHASE_3][SOL_HIGH] Govern candidacy stages, screening, assessments, interviews and evidence.**
  - **Depends:** `RECRUIT-001`, `APPT-004`, `ARTIFACT-005`, `WORK-003`, `GOVERN-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.RECRUITING; DIRECT=none; WHY=own stage and evaluation chronology used by recruiting intents`.
  - **TEST:** `TestCandidacyStageTransitionBindsRequirementsEvidenceAndDecisionAuthority`.
  - **TEST MATRIX:** `PRIMARY=TestCandidacyStageTransitionBindsRequirementsEvidenceAndDecisionAuthority`; `PROPERTY=TestTodo_RECRUIT_002_Property`; `GOLDEN=TestTodo_RECRUIT_002_Golden`; `RACE=TestTodo_RECRUIT_002_Race`; `FAULT=TestTodo_RECRUIT_002_Fault`; `SECURITY=TestTodo_RECRUIT_002_Security`; `MUTATION=TestTodo_RECRUIT_002_Mutation`.
  - **RED:** requester skips a mandatory stage, changes a submitted score, exposes protected screening evidence, advances on provider acceptance instead of observed result, or concurrent interview decisions create two current stages.
  - **GREEN:** transition CAS binds stage/requisition/candidacy revisions, required evidence and authorized decision receipt; provider results remain observations, correction appends a successor, and exactly one stage event/frontier is current.
  - **REFACTOR:** assessment and appointment providers remain adapters; ATS owns their business interpretation and chronology.
  - **Refs:** [Recruiting workflow](workflows/lifecycle/recruit-hire-onboard.md), [human work](specs/human-work-forms-and-rules.md), [external observations](specs/source-authority-and-external-mastering.md).

- [ ] `RECRUIT-003` **[PHASE_3][SOL_HIGH] Create, approve, sign and accept immutable OfferRevisions.**
  - **Depends:** `RECRUIT-002`, `COMP-003`, `APPROVAL-008`, `DOC-TEMPLATE-001`, `DOC-SIGN-001`, `INTENT-015`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.RECRUITING,BI.LIFECYCLE; DIRECT=none; WHY=own offer truth and the explicit accepted-offer to Hire intent boundary`.
  - **TEST:** `TestOfferRevisionAcceptanceRequiresExactApprovalSignatureAndCurrentCandidacy`.
  - **TEST MATRIX:** `PRIMARY=TestOfferRevisionAcceptanceRequiresExactApprovalSignatureAndCurrentCandidacy`; `PROPERTY=TestTodo_RECRUIT_003_Property`; `GOLDEN=TestTodo_RECRUIT_003_Golden`; `RACE=TestTodo_RECRUIT_003_Race`; `INTEGRATION=TestTodo_RECRUIT_003_Integration`; `FAULT=TestTodo_RECRUIT_003_Fault`; `SECURITY=TestTodo_RECRUIT_003_Security`; `MUTATION=TestTodo_RECRUIT_003_Mutation`.
  - **RED:** salary/position/start date changes after approval, signature applies to a different digest, expired/rescinded offer is accepted, two offers win concurrently, or document-provider acceptance directly creates a Worker.
  - **GREEN:** acceptance CAS binds current OfferRevision, proposal/approval/signature digests, candidate/candidacy and expiry; one `OfferAccepted` event emits one deterministic Hire child intent while worker creation remains separately governed.
  - **REFACTOR:** offer document rendering/signature evidence is delegated to Documents without moving offer authority there.
  - **Refs:** [Offer and hire workflow](workflows/lifecycle/recruit-hire-onboard.md), [proposal integrity](plan.md#53-approval-must-bind-to-an-immutable-proposal), [documents](specs/platform-responsibility-boundaries.md).

- [ ] `RECRUIT-004` **[CONFORMANCE][SOL_HIGH] Prove ATS withdrawal, rejection, rescission, correction and external reconciliation semantics.**
  - **Depends:** `RECRUIT-001`–`RECRUIT-003`, `INTG-010`, `REPAIR-001`, `CONF-011`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.RECRUITING; DIRECT=none; WHY=prove the authoritative ATS owner remains correct across terminal changes and external coexistence`.
  - **TEST:** `TestATSConformancePreservesTerminalHistoryAndRepairsExternalDrift`.
  - **TEST MATRIX:** `PRIMARY=TestATSConformancePreservesTerminalHistoryAndRepairsExternalDrift`; `PROPERTY=TestTodo_RECRUIT_004_Property`; `GOLDEN=TestTodo_RECRUIT_004_Golden`; `RACE=TestTodo_RECRUIT_004_Race`; `INTEGRATION=TestTodo_RECRUIT_004_Integration`; `FAULT=TestTodo_RECRUIT_004_Fault`; `SECURITY=TestTodo_RECRUIT_004_Security`; `CONFORMANCE=TestTodo_RECRUIT_004_Conformance`; `MUTATION=TestTodo_RECRUIT_004_Mutation`.
  - **RED:** withdrawal deletes history, rejection reason leaks outside purpose, rescission rewrites acceptance, late provider event reopens candidacy, duplicate external ID merges candidates, or mismatch triggers blind overwrite.
  - **GREEN:** append-only fixtures return exact terminal/successor states, preserve restricted reasons/evidence, quarantine late/ambiguous identity, and create a scoped RepairPlan without duplicating Hire or recruiting events.
  - **REFACTOR:** use the shared observation/reconciliation protocol with ATS-specific expected-state comparison.
  - **Refs:** [Recruiting conformance](#21-future-domain-intent-conformance), [repair](specs/transaction-ledger-reconciliation-and-repair.md).

- [x] `ACCESS-001` **[PHASE_3][SOL_HIGH] Define the authoritative workforce identity and expected-access graph.**
  - **Evidence (2026-09-05):** `TestWorkforceAccessGraphRejectsUnownedAccountEntitlementAndSubjectLinks`, `TestTodo_ACCESS_001_{Property,Golden,Security,Conformance,Mutation}` in `internal/domains/access` (revisioned WorkforceIdentity, AccountLink, EntitlementDefinition, ExpectedEntitlement and ExternalAccessObservation records carrying tenant, subject, system, authority class, effective/known time, provenance and lifecycle; the graph refuses unowned account, entitlement and subject links, tenant mismatches, an expected edge without an employment/position/policy basis and any observation presented as authoritative, with typed errors naming the field, a canonical digest and Explain; platform Principal stays separate; written by a codex lane and verified independently); `go test -count=1 ./internal/domains/access/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-016`, `TRUST-006`, `CONN-RT-001`, `INTENT-CONF-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ACCESS; DIRECT=none; WHY=establish Access as an HCM domain rather than a collection of IAM connector calls`.
  - **TEST:** `TestWorkforceAccessGraphRejectsUnownedAccountEntitlementAndSubjectLinks`.
  - **TEST MATRIX:** `PRIMARY=TestWorkforceAccessGraphRejectsUnownedAccountEntitlementAndSubjectLinks`; `PROPERTY=TestTodo_ACCESS_001_Property`; `GOLDEN=TestTodo_ACCESS_001_Golden`; `SECURITY=TestTodo_ACCESS_001_Security`; `CONFORMANCE=TestTodo_ACCESS_001_Conformance`; `MUTATION=TestTodo_ACCESS_001_Mutation`.
  - **RED:** account lacks WorkforceIdentity/system/source link, entitlement lacks version/risk/owner, expected edge has no employment/position/policy basis, or external account observation becomes authoritative graph state.
  - **GREEN:** revisions distinguish WorkforceIdentity, AccountLink, EntitlementDefinition, ExpectedEntitlement and ExternalAccessObservation with tenant/subject/system, authority, effective/known time, provenance and lifecycle invariants.
  - **REFACTOR:** platform Principal/workload identity remains separate from workforce application identity.
  - **Refs:** [Access models](data/models/connectivity-access-content.md), [Access pillar](plan.md), [authority](specs/source-authority-and-external-mastering.md).

- [x] `ACCESS-002` **[PHASE_3][SOL_HIGH] Calculate expected entitlements deterministically from governed workforce facts.**
  - **Evidence (2026-09-05):** `TestExpectedEntitlementCalculationReturnsExplainableUnknownSafeGraph` in `internal/domains/access` (deterministic derivation of expected entitlements from governed employment, position, org and access-policy facts at an as-of, every derived edge carrying its basis refs, identical inputs yielding an identical digested set across goroutines, observations refused as facts, typed grant/revoke deltas against the current expected set, Explain; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/access/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ACCESS-001`, `POP-003`, `ELIG-003`, `RULE-003`, `SNAPSHOT-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ACCESS; DIRECT=none; WHY=own expected-access computation consumed by provision, certification and drift intents`.
  - **TEST:** `TestExpectedEntitlementCalculationReturnsExplainableUnknownSafeGraph`.
  - **TEST MATRIX:** `PRIMARY=TestExpectedEntitlementCalculationReturnsExplainableUnknownSafeGraph`; `PROPERTY=TestTodo_ACCESS_002_Property`; `GOLDEN=TestTodo_ACCESS_002_Golden`; `FUZZ=FuzzTodo_ACCESS_002`; `SECURITY=TestTodo_ACCESS_002_Security`; `MUTATION=TestTodo_ACCESS_002_Mutation`.
  - **RED:** missing manager/position/employment/policy fact defaults to grant, deny rule loses to allow, terminated/future worker receives current access, inherited entitlement cycles, or hidden sensitive basis appears in explanation.
  - **GREEN:** pinned facts/rules produce `EXPECTED|NOT_EXPECTED|CONDITIONAL|UNKNOWN` edges with deny dominance, effective interval, basis and redaction-safe trace; pure calculation writes no account, entitlement, work or outbox state.
  - **REFACTOR:** reuse Population/Eligibility/Rules mechanics while Access owns entitlement meaning and deny behavior.
  - **Refs:** [Authorization and access](specs/platform-responsibility-boundaries.md), [rules](data/models/rules-and-decisions.md).

- [x] `ACCESS-003` **[PHASE_3][SOL_HIGH] Govern access request, approval, grant, revoke and certification lifecycles.**
  - **Evidence (2026-09-05):** `TestAccessDecisionLifecycleRequiresCurrentRiskApprovalAndSeparationOfDuties` in `internal/domains/access` (one fenced decision binds exact subject/account/entitlement/scope/risk/proposal/approvals and appends intended grant/revoke/certification state plus idempotent external effect; stale/conflict returns typed replan/reapproval with zero effect; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/access/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ACCESS-002`, `APPROVAL-008`, `WORK-005`, `TX-004`, `INTG-012`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ACCESS,BI.WORK; DIRECT=none; WHY=own access decisions and intended state before external IAM effects`.
  - **TEST:** `TestAccessDecisionLifecycleRequiresCurrentRiskApprovalAndSeparationOfDuties`.
  - **TEST MATRIX:** `PRIMARY=TestAccessDecisionLifecycleRequiresCurrentRiskApprovalAndSeparationOfDuties`; `PROPERTY=TestTodo_ACCESS_003_Property`; `GOLDEN=TestTodo_ACCESS_003_Golden`; `RACE=TestTodo_ACCESS_003_Race`; `INTEGRATION=TestTodo_ACCESS_003_Integration`; `FAULT=TestTodo_ACCESS_003_Fault`; `SECURITY=TestTodo_ACCESS_003_Security`; `MUTATION=TestTodo_ACCESS_003_Mutation`.
  - **RED:** requester self-approves privileged access, stale risk/manager/entitlement revision is consumed, revoke loses to concurrent grant, certification attests an unseen scope, or provider success substitutes for authoritative decision.
  - **GREEN:** one fenced decision binds exact subject/account/entitlement/scope/risk/proposal/approvals and appends intended grant/revoke/certification state plus idempotent external effect; stale/conflict returns typed replan/reapproval with zero effect.
  - **REFACTOR:** Human Work owns tasks; Access owns decision semantics and intended graph.
  - **Refs:** [Approval safety](specs/human-work-forms-and-rules.md), [integration effects](specs/integration-platform.md).

- [x] `ACCESS-004` **[PHASE_3][SOL_HIGH] Reconcile accounts, entitlements, devices and physical access and repair drift.**
  - **Evidence (2026-09-05):** `TestAccessReconciliationClassifiesAndRepairsPartialLogicalDeviceAndBadgeDrift` in `internal/operations/accessdrift` (comparison returns per-resource MATCH|MISSING|EXCESS|PARTIAL|STALE|UNKNOWN, elevates privileged excess/revoke gaps, creates separately governed RepairPlans and reaches consistency only after fresh provider observations; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/operations/accessdrift/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ACCESS-003`, `INTG-010`, `RECON-002`, `REPAIR-002`, `ARTIFACT-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ACCESS,BI.OPERATIONS; DIRECT=none; WHY=own expected-versus-observed access consistency across logical and physical resources`.
  - **TEST:** `TestAccessReconciliationClassifiesAndRepairsPartialLogicalDeviceAndBadgeDrift`.
  - **TEST MATRIX:** `PRIMARY=TestAccessReconciliationClassifiesAndRepairsPartialLogicalDeviceAndBadgeDrift`; `PROPERTY=TestTodo_ACCESS_004_Property`; `GOLDEN=TestTodo_ACCESS_004_Golden`; `RACE=TestTodo_ACCESS_004_Race`; `INTEGRATION=TestTodo_ACCESS_004_Integration`; `FAULT=TestTodo_ACCESS_004_Fault`; `SECURITY=TestTodo_ACCESS_004_Security`; `CONFORMANCE=TestTodo_ACCESS_004_Conformance`; `MUTATION=TestTodo_ACCESS_004_Mutation`.
  - **RED:** stale/unavailable observation reports consistent, missing privileged revoke is downgraded, device/badge custody is inferred from account state, ambiguous deprovision is blindly retried, or repair reruns the parent employment transaction.
  - **GREEN:** comparison returns per-resource `MATCH|MISSING|EXCESS|PARTIAL|STALE|UNKNOWN`, elevates privileged excess/revoke gaps, creates separately governed RepairPlans and reaches consistency only after fresh provider observations.
  - **REFACTOR:** logical accounts, device custody and physical badge adapters share reconciliation mechanics but retain distinct authority and risk policy.
  - **Refs:** [Promotion repair](reference-workflows/promote-into-management.md), [termination conformance](workflows/lifecycle/termination-offboarding.md), [repair](specs/transaction-ledger-reconciliation-and-repair.md).

- [x] `CASE-001` **[PHASE_3][SOL_HIGH] Define authoritative HR request and CaseDefinition lifecycle semantics.**
  - **Evidence (2026-09-05):** `TestHRCaseLifecycleRejectsMissingTypeSubjectPurposeAndIllegalTransition` in `internal/domains/hrcase` (immutable CaseRevision enforces DRAFT|OPEN|WAITING|PAUSED|RESOLVED|CLOSED|REOPENED|APPEALED, exact participants/purpose/classification/retention and append-only successor transitions; invalid command emits zero events/work/outbox; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/hrcase/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-016`, `WORK-001`, `RECORDS-COPY-001`, `INTENT-CONF-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.CASES; DIRECT=none; WHY=own HR case truth rather than leaving it inside workflow and human-task records`.
  - **TEST:** `TestHRCaseLifecycleRejectsMissingTypeSubjectPurposeAndIllegalTransition`.
  - **TEST MATRIX:** `PRIMARY=TestHRCaseLifecycleRejectsMissingTypeSubjectPurposeAndIllegalTransition`; `PROPERTY=TestTodo_CASE_001_Property`; `GOLDEN=TestTodo_CASE_001_Golden`; `SECURITY=TestTodo_CASE_001_Security`; `CONFORMANCE=TestTodo_CASE_001_Conformance`; `MUTATION=TestTodo_CASE_001_Mutation`.
  - **RED:** case lacks versioned type/service, requester/subject/purpose/classification, terminal case is edited, pause closes SLA silently, or reopening overwrites prior closure/disposition.
  - **GREEN:** immutable CaseRevision enforces `DRAFT|OPEN|WAITING|PAUSED|RESOLVED|CLOSED|REOPENED|APPEALED`, exact participants/purpose/classification/retention and append-only successor transitions; invalid command emits zero events/work/outbox.
  - **REFACTOR:** generic workflow coordinates a case but never owns case lifecycle or disposition.
  - **Refs:** [Case models](data/models/talent-experience-cases.md), [HR service workflows](workflows/hr-service/catalog.md), [records](specs/records-management-and-disposition.md).

- [ ] `CASE-002` **[PHASE_3][SOL_HIGH] Enforce case participants, compartments, confidential notes and evidence custody.**
  - **Depends:** `CASE-001`, `ANON-004`, `ARTIFACT-005`, `TRUST-010`, `RECORDS-COPY-001`, `RECORDS-HOLD-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.CASES,BI.PRIVACY; DIRECT=none; WHY=own relationship- and purpose-scoped visibility for sensitive HR matters`.
  - **TEST:** `TestCaseCompartmentAuthorizationPreventsParticipantNoteAndEvidenceLeakage`.
  - **TEST MATRIX:** `PRIMARY=TestCaseCompartmentAuthorizationPreventsParticipantNoteAndEvidenceLeakage`; `PROPERTY=TestTodo_CASE_002_Property`; `GOLDEN=TestTodo_CASE_002_Golden`; `FUZZ=FuzzTodo_CASE_002`; `SECURITY=TestTodo_CASE_002_Security`; `MUTATION=TestTodo_CASE_002_Mutation`.
  - **RED:** manager sees medical/investigation artifact, subject infers confidential witness, search/count reveals hidden case, note classification is lowered by caller, escrowed reporter is correlated, or removed participant retains cached access.
  - **GREEN:** every participant/role/note/evidence edge binds compartment, purpose, fields, assurance, effective interval and disclosure receipt; denied reads return non-disclosing result and revocation invalidates derived search/cache views.
  - **REFACTOR:** artifact bytes remain in Documents/object custody; Case owns business visibility and evidence relationships.
  - **Refs:** [Case conformance](#21-future-domain-intent-conformance), [confidential actors](data/models/talent-experience-cases.md), [DLP](specs/data-classification-and-dlp.md).

- [ ] `CASE-003` **[PHASE_3][SOL_HIGH] Implement case assignment, SLA, evidence review, finding and disposition.**
  - **Depends:** `CASE-002`, `WORK-003`, `CYCLE-003`, `LEGAL-003`, `APPROVAL-005`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.CASES,BI.WORK; DIRECT=none; WHY=own accountable human-case progress and business resolution`.
  - **TEST:** `TestCaseResolutionRequiresEligibleAssigneeCompleteObligationsAndBoundFinding`.
  - **TEST MATRIX:** `PRIMARY=TestCaseResolutionRequiresEligibleAssigneeCompleteObligationsAndBoundFinding`; `PROPERTY=TestTodo_CASE_003_Property`; `GOLDEN=TestTodo_CASE_003_Golden`; `RACE=TestTodo_CASE_003_Race`; `FAULT=TestTodo_CASE_003_Fault`; `SECURITY=TestTodo_CASE_003_Security`; `MUTATION=TestTodo_CASE_003_Mutation`.
  - **RED:** conflicted investigator is assigned, SLA timezone/calendar is ambient, evidence review disappears on reassignment, finding changes after disposition approval, mandatory legal task remains open, or concurrent closers create two dispositions.
  - **GREEN:** assignment binds eligibility/recusal/delegation, SLA records calendar/version/pause reasons, finding/disposition bind exact evidence and approvals, and close CAS succeeds once only when required obligations are satisfied.
  - **REFACTOR:** Work owns claims/leases/tasks; Case owns accountable assignment history, findings and disposition.
  - **Refs:** [Human work](specs/human-work-forms-and-rules.md), [legal obligations](specs/governance-decision-and-obligation-composition.md).

- [ ] `CASE-004` **[PHASE_3][SOL_HIGH] Implement case reopen, appeal, relationship graph and evidence-package export.**
  - **Depends:** `CASE-003`, `EVIDENCE-001`, `RECORDS-HOLD-001`, `INTENT-015`, `DOC-EVIDENCE-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.CASES,BI.DOCUMENTS; DIRECT=none; WHY=preserve reviewable case chronology across successor proceedings and exports`.
  - **TEST:** `TestCaseAppealCreatesSuccessorProceedingAndVerifiableScopedEvidencePackage`.
  - **TEST MATRIX:** `PRIMARY=TestCaseAppealCreatesSuccessorProceedingAndVerifiableScopedEvidencePackage`; `PROPERTY=TestTodo_CASE_004_Property`; `GOLDEN=TestTodo_CASE_004_Golden`; `INTEGRATION=TestTodo_CASE_004_Integration`; `FAULT=TestTodo_CASE_004_Fault`; `SECURITY=TestTodo_CASE_004_Security`; `CONFORMANCE=TestTodo_CASE_004_Conformance`; `MUTATION=TestTodo_CASE_004_Mutation`.
  - **RED:** reopen erases closure, appeal mutates original finding, related cases leak compartment membership, export omits correction/hold/decision lineage, includes unauthorized artifacts, or repeated export produces different semantic digest.
  - **GREEN:** successor CaseRevision/related Case creates explicit `REOPEN_OF|APPEAL_OF|DUPLICATE_OF|RELATED_TO` edge; scoped evidence package has complete ordered chronology, redaction/hold/retention decisions and verifiable canonical digest.
  - **REFACTOR:** package generation consumes ledger/provenance rather than reconstructing truth from mutable projections.
  - **Refs:** [Evidence package](specs/provenance-graph-and-lineage.md), [case model](data/models/talent-experience-cases.md).

- [ ] `WORKER-LIFE-001` **[PHASE_3][SOL_HIGH] Define immutable WorkerLifecyclePlan and requirement semantics.**
  - **Depends:** `MODEL-016`, `INTENT-015`, `READINESS-001`, `CYCLE-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.LIFECYCLE; DIRECT=none; WHY=own employee onboarding/offboarding plans separately from tenant onboarding`.
  - **TEST:** `TestWorkerLifecyclePlanRejectsUnownedCircularAndUnverifiableRequirements`.
  - **TEST MATRIX:** `PRIMARY=TestWorkerLifecyclePlanRejectsUnownedCircularAndUnverifiableRequirements`; `PROPERTY=TestTodo_WORKER_LIFE_001_Property`; `GOLDEN=TestTodo_WORKER_LIFE_001_Golden`; `SECURITY=TestTodo_WORKER_LIFE_001_Security`; `CONFORMANCE=TestTodo_WORKER_LIFE_001_Conformance`; `MUTATION=TestTodo_WORKER_LIFE_001_Mutation`.
  - **RED:** plan lacks worker/employment/start-or-end event, requirement lacks owner/due rule/evidence/completion policy, child graph cycles, external effect is called requirement satisfaction, or plan changes after approval.
  - **GREEN:** immutable plan binds event/proposal, ordered typed requirements, child intent templates, owners, calendars, evidence, dependencies and completion policy with deterministic digest; invalid graph emits no tasks/intents.
  - **REFACTOR:** Readiness calculates state and Workflow coordinates; Worker Lifecycle owns requirement meaning and plan completion.
  - **Refs:** [Onboarding workflow](workflows/lifecycle/recruit-hire-onboard.md), [offboarding workflow](workflows/lifecycle/termination-offboarding.md).

- [ ] `WORKER-LIFE-002` **[PHASE_3][SOL_HIGH] Resolve onboarding requirements and readiness from worker context.**
  - **Depends:** `WORKER-LIFE-001`, `ELIG-003`, `LEGAL-003`, `ACCESS-002`, `LEARN-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.LIFECYCLE; DIRECT=none; WHY=own context-specific onboarding requirements before child intent emission`.
  - **TEST:** `TestOnboardingRequirementResolutionReturnsReadyConditionalBlockedOrUnknown`.
  - **TEST MATRIX:** `PRIMARY=TestOnboardingRequirementResolutionReturnsReadyConditionalBlockedOrUnknown`; `PROPERTY=TestTodo_WORKER_LIFE_002_Property`; `GOLDEN=TestTodo_WORKER_LIFE_002_Golden`; `SECURITY=TestTodo_WORKER_LIFE_002_Security`; `MUTATION=TestTodo_WORKER_LIFE_002_Mutation`.
  - **RED:** missing work authorization/payroll/access/equipment/training fact defaults ready, optional task blocks start, legal requirement is waived by manager, stale evidence counts satisfied, or hidden medical/identity evidence leaks into manager view.
  - **GREEN:** pinned context returns per-requirement and aggregate `READY|CONDITIONAL|BLOCKED|UNKNOWN`, exact blockers/owners/dates and redaction-safe explanation; resolution is pure and creates no child intent/task/effect.
  - **REFACTOR:** domain engines evaluate individual requirements; lifecycle plan composes their typed results.
  - **Refs:** [Readiness engine](#40-readiness-and-leave-entitlement-engines), [recruit-to-onboard](workflows/lifecycle/recruit-hire-onboard.md).

- [ ] `WORKER-LIFE-003` **[PHASE_3][SOL_HIGH] Track onboarding requirement satisfaction and emit bounded child intents.**
  - **Depends:** `WORKER-LIFE-002`, `INTENT-016`, `WF-RUN-025`, `WORK-005`, `MSG-011`.
  - **INTENT CONTEXT:** `ROLE=COMPOSITE; SETS=BI.LIFECYCLE,BI.ACCESS,BI.TALENT; ROOT_SET=BI.LIFECYCLE; FAMILY=CHANGE_REQUEST (child-bound process); CHILDREN=typed onboarding requirement intents from the accepted catalog; WHY=coordinate employee onboarding without hiding child authority or outcome`.
  - **TEST:** `TestOnboardingPlanEmitsEachBoundedChildOnceAndCompletesOnlyWhenReady`.
  - **TEST MATRIX:** `PRIMARY=TestOnboardingPlanEmitsEachBoundedChildOnceAndCompletesOnlyWhenReady`; `PROPERTY=TestTodo_WORKER_LIFE_003_Property`; `GOLDEN=TestTodo_WORKER_LIFE_003_Golden`; `RACE=TestTodo_WORKER_LIFE_003_Race`; `FAULT=TestTodo_WORKER_LIFE_003_Fault`; `SECURITY=TestTodo_WORKER_LIFE_003_Security`; `CONFORMANCE=TestTodo_WORKER_LIFE_003_Conformance`; `MUTATION=TestTodo_WORKER_LIFE_003_Mutation`.
  - **RED:** retry duplicates payroll/access/learning/equipment child, child broadens scope, task completion substitutes for required observation, late start-date change preserves stale children, or aggregate completes with mandatory blocker/UNKNOWN.
  - **GREEN:** deterministic child identities and dependency graph emit once, each result/evidence remains independently governed, material change replans affected requirements, and lifecycle closes only under its exact readiness/completion policy.
  - **REFACTOR:** parent stores child references and typed aggregates, never copies protected payloads or directly calls providers.
  - **Refs:** [Intent composition](specs/business-intent-and-change-request.md), [workflow atomic frontier](specs/workflow-runtime.md).

- [ ] `WORKER-LIFE-004` **[PHASE_3][SOL_HIGH] Generate and reconcile offboarding closure across access, assets, payroll and benefits.**
  - **Depends:** `WORKER-LIFE-001`, `ACCESS-004`, `RECON-002`, `REPAIR-002`, `CONF-012`.
  - **INTENT CONTEXT:** `ROLE=COMPOSITE; SETS=BI.LIFECYCLE,BI.ACCESS,BI.REWARDS,BI.PAYROLL,BI.OPERATIONS; ROOT_SET=BI.LIFECYCLE; FAMILY=CHANGE_REQUEST (child-bound process); CHILDREN=typed offboarding closure intents from the accepted catalog; WHY=own worker offboarding completion without pretending downstream atomicity`.
  - **TEST:** `TestOffboardingPlanSeparatesEmploymentCompletionFromExternalClosureAndRepair`.
  - **TEST MATRIX:** `PRIMARY=TestOffboardingPlanSeparatesEmploymentCompletionFromExternalClosureAndRepair`; `PROPERTY=TestTodo_WORKER_LIFE_004_Property`; `GOLDEN=TestTodo_WORKER_LIFE_004_Golden`; `RACE=TestTodo_WORKER_LIFE_004_Race`; `INTEGRATION=TestTodo_WORKER_LIFE_004_Integration`; `FAULT=TestTodo_WORKER_LIFE_004_Fault`; `SECURITY=TestTodo_WORKER_LIFE_004_Security`; `CONFORMANCE=TestTodo_WORKER_LIFE_004_Conformance`; `MUTATION=TestTodo_WORKER_LIFE_004_Mutation`.
  - **RED:** revoked approval/final-pay deadline/benefit/access/device/equipment/records obligation is omitted, provider timeout triggers blind retry, missing privileged revoke is called complete, or partial external failure rolls employment history back.
  - **GREEN:** plan records independent Business, Consistency and Obligation dimensions; expected closure is observed per domain, gaps create scoped repair work and final close requires declared policy without rerunning termination.
  - **REFACTOR:** domain owners define closure evidence; Worker Lifecycle composes and reports it.
  - **Refs:** [Termination/offboarding](workflows/lifecycle/termination-offboarding.md), [multidimensional completion](specs/transaction-ledger-reconciliation-and-repair.md).

## 51. HCM semantic engine ownership expansion

> **Disposition (2026-09-02):** DEFERRED.

- [x] `FX-001` **[PHASE_2][SOL_HIGH] Define versioned exchange-rate sources, quotes and conversion profiles.**
  - **Evidence (2026-09-05):** `TestFXQuoteResolutionRequiresSourceEffectiveKnownTimeAndPair` in `internal/domains/fx` (versioned exchange-rate sources, quotes and conversion profiles; resolution requires source, effective and known-at instants and a currency pair; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/fx/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-016`, `INTENT-CONF-001`, `ENGINE-COVERAGE-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS,BI.PAYROLL,BI.ANALYTICS; DIRECT=none; WHY=provide an explicit semantic engine owner for this accepted BusinessIntent partition`.
  - **TEST:** `TestFXQuoteResolutionRequiresSourceEffectiveKnownTimeAndPair`.
  - **TEST MATRIX:** `PRIMARY=TestFXQuoteResolutionRequiresSourceEffectiveKnownTimeAndPair`; `PROPERTY=TestTodo_FX_001_Property`; `GOLDEN=TestTodo_FX_001_Golden`; `RACE=TestTodo_FX_001_Race`; `FAULT=TestTodo_FX_001_Fault`; `SECURITY=TestTodo_FX_001_Security`; `CONFORMANCE=TestTodo_FX_001_Conformance`; `MUTATION=TestTodo_FX_001_Mutation`.
  - **RED:** quote without source/revision/base/quote currency, effective-at, known-at, market convention or freshness is selected; duplicate providers silently override each other.
  - **GREEN:** resolver returns one `FXQuoteRevision|UNKNOWN|STALE|CONFLICT` with source priority, observed/effective/known time, confidence and canonical digest; no ambient current rate is available.
  - **REFACTOR:** keep shared mechanics in kernel/engines and this package as the sole owner of the stated HCM meaning, lifecycle and correction semantics.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [model coverage](data/models/intent-coverage-matrix.md), [engine ownership](#businessintent-context-required-by-every-todo).

### Conformance-first future domain owners

- [x] `PROOF-001` **[PHASE_3][SOL_HIGH] Define identity-proofing and work-authorization evidence sessions.**
  - **Evidence (2026-09-05):** `TestIdentityProofAndWorkAuthorizationRequireScopedEvidenceAssuranceAndExpiry` in `internal/domains/proofing` (identity-proofing and work-authorization evidence sessions with scoped evidence, assurance gating, effective-dated authorization results and expiry; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/proofing/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-016`, `INTENT-CONF-001`, `ENGINE-COVERAGE-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.LIFECYCLE,BI.MOBILITY,BI.SECURITY; DIRECT=none; WHY=establish explicit domain ownership while retaining evidence-gated authority expansion`.
  - **TEST:** `TestIdentityProofAndWorkAuthorizationRequireScopedEvidenceAssuranceAndExpiry`.
  - **TEST MATRIX:** `PRIMARY=TestIdentityProofAndWorkAuthorizationRequireScopedEvidenceAssuranceAndExpiry`; `PROPERTY=TestTodo_PROOF_001_Property`; `GOLDEN=TestTodo_PROOF_001_Golden`; `RACE=TestTodo_PROOF_001_Race`; `FAULT=TestTodo_PROOF_001_Fault`; `SECURITY=TestTodo_PROOF_001_Security`; `CONFORMANCE=TestTodo_PROOF_001_Conformance`; `MUTATION=TestTodo_PROOF_001_Mutation`.
  - **RED:** provider assertion without subject/document/source/assurance/purpose is trusted, work authorization lacks jurisdiction/category/validity or raw identity evidence enters ordinary projections.
  - **GREEN:** typed verification session returns `VERIFIED|REVIEW_REQUIRED|REJECTED|EXPIRED|UNKNOWN`, binds protected artifacts/provider observations and creates a separate effective-dated authorization result.
  - **REFACTOR:** reuse shared engines for mechanics while this domain retains authoritative meaning, lifecycle, correction and evidence policy.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [domain model coverage](data/models/intent-coverage-matrix.md), [authority expansion](specs/competitive-positioning-and-authority-expansion.md#authority-absorption-gate).

- [x] `PROOF-002` **[CONFORMANCE][SOL_HIGH] Prove review, expiration, renewal, correction and provider ambiguity semantics.**
  - **Evidence (2026-09-07):** `TestWorkAuthorizationConformanceBlocksExpiredUnknownAndAmbiguousProviderState`, `TestTodo_PROOF_002_*`, effective-date boundary and historical-correction regressions; `go test -count=1 -cover ./internal/domains/proofing/` PASS on windows/arm64 (88.3%); scoped vet PASS. Luna implementation, Sol refinement and integration review prove the domain conformance fixtures, not a deployed scheduling authorization adapter. The named race test uses goroutines; detector-backed execution remains CI-only.
  - **Depends:** `PROOF-001`, `CONF-001`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.LIFECYCLE,BI.MOBILITY,BI.SECURITY; DIRECT=none; WHY=establish explicit domain ownership while retaining evidence-gated authority expansion`.
  - **TEST:** `TestWorkAuthorizationConformanceBlocksExpiredUnknownAndAmbiguousProviderState`.
  - **TEST MATRIX:** `PRIMARY=TestWorkAuthorizationConformanceBlocksExpiredUnknownAndAmbiguousProviderState`; `PROPERTY=TestTodo_PROOF_002_Property`; `GOLDEN=TestTodo_PROOF_002_Golden`; `RACE=TestTodo_PROOF_002_Race`; `FAULT=TestTodo_PROOF_002_Fault`; `SECURITY=TestTodo_PROOF_002_Security`; `CONFORMANCE=TestTodo_PROOF_002_Conformance`; `MUTATION=TestTodo_PROOF_002_Mutation`.
  - **RED:** expired/unknown authorization permits work, provider timeout is retried as success, renewal overwrites prior evidence, reviewer conflict is ignored or correction changes historical eligibility.
  - **GREEN:** fixtures create dated review/renewal intents, preserve prior evidence, block unsafe employment/scheduling, quarantine ambiguity and reconcile fresh provider state without identity leakage.
  - **REFACTOR:** reuse shared engines for mechanics while this domain retains authoritative meaning, lifecycle, correction and evidence policy.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [domain model coverage](data/models/intent-coverage-matrix.md), [authority expansion](specs/competitive-positioning-and-authority-expansion.md#authority-absorption-gate).

- [x] `PROOF-003` **[PHASE_4][SOL_HIGH] Persist proofing revisions behind a typed Repository port with per-tenant CAS.**
  - **Evidence (2026-09-06):** `TestMemoryRepositoryUsesTypedRevisionErrors` in `internal/domains/proofing` (a tenant-aware `Repository` port over sessions and work-authorization evidence, a closed `StoreErrorCode` taxonomy (`INVALID`, `NOT_FOUND`, `DUPLICATE_REVISION`, `STALE_CAS`) and a kernel-pure `MemoryRepository` enforcing per-tenant append-only revisioning and expected-revision compare-and-swap; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/proofing/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PROOF-001`, `PERSIST-IDENTITYPRIVACY-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.LIFECYCLE,BI.MOBILITY,BI.SECURITY; DIRECT=none; WHY=establish explicit domain ownership while retaining evidence-gated authority expansion`.
  - **TEST:** `TestMemoryRepositoryUsesTypedRevisionErrors`.
  - **TEST MATRIX:** `PRIMARY=TestMemoryRepositoryUsesTypedRevisionErrors`.
  - **RED:** a session or work-authorization revision saves without a matching expected revision, a duplicate revision number is accepted, a failed compare-and-swap reports an untyped error a caller cannot branch on, or two tenants' revisions are addressable through the same key.
  - **GREEN:** `Repository.SaveSession`/`SaveAuthorization` require the caller's expected current revision, `MemoryRepository` refuses a duplicate revision with `StoreDuplicateCode` and a revision mismatch with `StoreStaleCASCode` carrying typed `Expected`/`Actual` revision numbers, and every lookup is scoped by the caller-supplied tenant id.
  - **REFACTOR:** `MemoryRepository` mirrors the PostgreSQL adapter's CAS rules without opening a database, so domain and conformance tests can exercise the same typed error taxonomy the production `Repository` implementation returns.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [domain model coverage](data/models/intent-coverage-matrix.md), [authority expansion](specs/competitive-positioning-and-authority-expansion.md#authority-absorption-gate).

- [x] `ER-001` **[PHASE_4][SOL_HIGH] Define allegation, investigation, interview, finding, discipline and grievance semantics.**
  - **Evidence (2026-09-05):** `TestEmployeeRelationsDomainRequiresAllegationAuthorityRecusalEvidenceAndDisposition` in `internal/domains/employeerelations` (allegation, investigation, interview, finding, discipline and grievance semantics requiring authority, recusal, evidence and a disposition; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/employeerelations/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-016`, `INTENT-CONF-001`, `ENGINE-COVERAGE-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.CASES,BI.WORK,BI.PRIVACY; DIRECT=none; WHY=establish explicit domain ownership while retaining evidence-gated authority expansion`.
  - **TEST:** `TestEmployeeRelationsDomainRequiresAllegationAuthorityRecusalEvidenceAndDisposition`.
  - **TEST MATRIX:** `PRIMARY=TestEmployeeRelationsDomainRequiresAllegationAuthorityRecusalEvidenceAndDisposition`; `PROPERTY=TestTodo_ER_001_Property`; `GOLDEN=TestTodo_ER_001_Golden`; `RACE=TestTodo_ER_001_Race`; `FAULT=TestTodo_ER_001_Fault`; `SECURITY=TestTodo_ER_001_Security`; `CONFORMANCE=TestTodo_ER_001_Conformance`; `MUTATION=TestTodo_ER_001_Mutation`.
  - **RED:** investigation has no allegation/participants/purpose, conflicted investigator acts, interview statement is edited, finding lacks evidence standard or discipline bypasses legal/representation review.
  - **GREEN:** ER revisions extend Case with protected allegation, recusal, interview/statement, evidence-weight, finding, discipline/grievance and retaliation-safeguard contracts.
  - **REFACTOR:** reuse shared engines for mechanics while this domain retains authoritative meaning, lifecycle, correction and evidence policy.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [domain model coverage](data/models/intent-coverage-matrix.md), [authority expansion](specs/competitive-positioning-and-authority-expansion.md#authority-absorption-gate).

- [ ] `ER-002` **[CONFORMANCE][SOL_HIGH] Prove appeal, grievance, settlement, retaliation and correction chronology.**
  - **Depends:** `ER-001`, `CONF-001`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.CASES,BI.WORK,BI.PRIVACY; DIRECT=none; WHY=establish explicit domain ownership while retaining evidence-gated authority expansion`.
  - **TEST:** `TestEmployeeRelationsConformancePreservesAppealSettlementAndRetaliationSafeguards`.
  - **TEST MATRIX:** `PRIMARY=TestEmployeeRelationsConformancePreservesAppealSettlementAndRetaliationSafeguards`; `PROPERTY=TestTodo_ER_002_Property`; `GOLDEN=TestTodo_ER_002_Golden`; `RACE=TestTodo_ER_002_Race`; `FAULT=TestTodo_ER_002_Fault`; `SECURITY=TestTodo_ER_002_Security`; `CONFORMANCE=TestTodo_ER_002_Conformance`; `MUTATION=TestTodo_ER_002_Mutation`.
  - **RED:** appeal overwrites finding, settlement erases allegation, retaliation signal is exposed to subject manager, grievance deadline disappears or correction removes original discipline.
  - **GREEN:** successor proceedings preserve compartments and chronology, deadlines/holds/representation remain explicit and evidence package returns exact redacted history without causal overclaim.
  - **REFACTOR:** reuse shared engines for mechanics while this domain retains authoritative meaning, lifecycle, correction and evidence policy.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [domain model coverage](data/models/intent-coverage-matrix.md), [authority expansion](specs/competitive-positioning-and-authority-expansion.md#authority-absorption-gate).

- [x] `MOBILITY-001` **[PHASE_4][SOL_HIGH] Define home/host assignment, relocation and immigration milestone revisions.**
  - **Evidence (2026-09-05):** `TestMobilityAssignmentRequiresHomeHostAuthorityDatesAndAuthorizationMilestones` in `internal/domains/mobility` (home/host assignment, relocation and immigration milestone revisions requiring home and host authority, dates and authorization milestones; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/mobility/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-016`, `INTENT-CONF-001`, `ENGINE-COVERAGE-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.MOBILITY,BI.REGULATORY,BI.PAYROLL,BI.PRIVACY; DIRECT=none; WHY=establish explicit domain ownership while retaining evidence-gated authority expansion`.
  - **TEST:** `TestMobilityAssignmentRequiresHomeHostAuthorityDatesAndAuthorizationMilestones`.
  - **TEST MATRIX:** `PRIMARY=TestMobilityAssignmentRequiresHomeHostAuthorityDatesAndAuthorizationMilestones`; `PROPERTY=TestTodo_MOBILITY_001_Property`; `GOLDEN=TestTodo_MOBILITY_001_Golden`; `RACE=TestTodo_MOBILITY_001_Race`; `FAULT=TestTodo_MOBILITY_001_Fault`; `SECURITY=TestTodo_MOBILITY_001_Security`; `CONFORMANCE=TestTodo_MOBILITY_001_Conformance`; `MUTATION=TestTodo_MOBILITY_001_Mutation`.
  - **RED:** international assignment lacks home/host employment/location/payroll, visa milestone/source/expiry, relocation owner or privacy-transfer basis yet becomes active.
  - **GREEN:** immutable mobility plan binds home/host facts, legs, authorization milestones, relocation package, payroll/tax/PE/privacy obligations and returns `READY|CONDITIONAL|BLOCKED|UNKNOWN`.
  - **REFACTOR:** reuse shared engines for mechanics while this domain retains authoritative meaning, lifecycle, correction and evidence policy.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [domain model coverage](data/models/intent-coverage-matrix.md), [authority expansion](specs/competitive-positioning-and-authority-expansion.md#authority-absorption-gate).

- [x] `MOBILITY-002` **[CONFORMANCE][SOL_HIGH] Prove extension, country change, return, vendor reconciliation and retro correction.**
  - **Evidence (2026-09-08):** `TestMobilityConformanceReplansJurisdictionPayrollTaxAndPrivacyOnMaterialChange` and `TestTodo_MOBILITY_002_Property`, `_Golden`, `_Race`, `_Fault`, `_Security`, `_Conformance`, `_Mutation` prove immutable successor changes, exact host/obligation targeting, non-authoritative vendor observations, independent host-effect closure and append-only retro history. Generated properties, fixed golden digests, concurrent assertions and multi-host parent-preservation regressions were refined by Sol/Astra after Luna implementation. Root `go test -count=1 -cover ./internal/domains/mobility/` PASS (71.6%); `go vet ./internal/domains/mobility/` PASS. Domain conformance only; no live vendor or jurisdiction approval is inferred.
  - **Depends:** `MOBILITY-001`, `CONF-001`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.MOBILITY,BI.REGULATORY,BI.PAYROLL,BI.PRIVACY; DIRECT=none; WHY=establish explicit domain ownership while retaining evidence-gated authority expansion`.
  - **TEST:** `TestMobilityConformanceReplansJurisdictionPayrollTaxAndPrivacyOnMaterialChange`.
  - **TEST MATRIX:** `PRIMARY=TestMobilityConformanceReplansJurisdictionPayrollTaxAndPrivacyOnMaterialChange`; `PROPERTY=TestTodo_MOBILITY_002_Property`; `GOLDEN=TestTodo_MOBILITY_002_Golden`; `RACE=TestTodo_MOBILITY_002_Race`; `FAULT=TestTodo_MOBILITY_002_Fault`; `SECURITY=TestTodo_MOBILITY_002_Security`; `CONFORMANCE=TestTodo_MOBILITY_002_Conformance`; `MUTATION=TestTodo_MOBILITY_002_Mutation`.
  - **RED:** host-country/date change preserves stale visa/tax/payroll/privacy approval, vendor acceptance counts complete, return leaves host effects active or retro correction rewrites mobility history.
  - **GREEN:** material change creates successor plan and scoped reevaluation, provider observations reconcile per obligation, return closes each host effect independently and corrections preserve dated prior truth.
  - **REFACTOR:** reuse shared engines for mechanics while this domain retains authoritative meaning, lifecycle, correction and evidence policy.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [domain model coverage](data/models/intent-coverage-matrix.md), [authority expansion](specs/competitive-positioning-and-authority-expansion.md#authority-absorption-gate).

- [x] `SAFETY-001` **[PHASE_4][SOL_HIGH] Define incident, injury, reportability, claim, restriction and corrective-action semantics.**
  - **Evidence (2026-09-05):** `TestSafetyDomainSeparatesOperationalMedicalClaimAndRegulatoryAuthority` in `internal/domains/safety` (incident, injury, reportability, claim, restriction and corrective-action semantics separating operational, medical, claim and regulatory authority; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/safety/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-016`, `INTENT-CONF-001`, `ENGINE-COVERAGE-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE,BI.CASES,BI.REGULATORY; DIRECT=none; WHY=establish explicit domain ownership while retaining evidence-gated authority expansion`.
  - **TEST:** `TestSafetyDomainSeparatesOperationalMedicalClaimAndRegulatoryAuthority`.
  - **TEST MATRIX:** `PRIMARY=TestSafetyDomainSeparatesOperationalMedicalClaimAndRegulatoryAuthority`; `PROPERTY=TestTodo_SAFETY_001_Property`; `GOLDEN=TestTodo_SAFETY_001_Golden`; `RACE=TestTodo_SAFETY_001_Race`; `FAULT=TestTodo_SAFETY_001_Fault`; `SECURITY=TestTodo_SAFETY_001_Security`; `CONFORMANCE=TestTodo_SAFETY_001_Conformance`; `MUTATION=TestTodo_SAFETY_001_Mutation`.
  - **RED:** incident/injury facts share unrestricted record, reportability is guessed, claim/payment lacks authority, restriction is free text or corrective action closes without verification.
  - **GREEN:** typed revisions separate operational/medical/claim/regulatory compartments, return deterministic reportability and work-restriction states, and bind corrective actions to evidence/owner/due rule.
  - **REFACTOR:** reuse shared engines for mechanics while this domain retains authoritative meaning, lifecycle, correction and evidence policy.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [domain model coverage](data/models/intent-coverage-matrix.md), [authority expansion](specs/competitive-positioning-and-authority-expansion.md#authority-absorption-gate).

- [x] `SAFETY-002` **[CONFORMANCE][SOL_HIGH] Prove filing, workers-comp payment, restriction clearance and correction reconciliation.**
  - **Evidence (2026-09-07):** `TestSafetyConformancePreservesFilingPaymentRestrictionAndCorrectionHistory`, `TestTodo_SAFETY_002_*`, and `TestResubmitFilingRequiresFreshObservedRejection`; `go test -count=1 -cover ./internal/domains/safety/` PASS on windows/arm64 (58.5%, existing exact exception expires 2026-12-31); scoped vet PASS. Luna implementation, Sol refinement and integration review verify append-only fixture lineage, exact payment reversal, observed-rejection-only resubmission, and fresh-observation closure. No medical/legal determination, live filing, or ledger payment execution is claimed; detector-backed races remain CI-only.
  - **Depends:** `SAFETY-001`, `CONF-001`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.WORKFORCE,BI.CASES,BI.REGULATORY; DIRECT=none; WHY=establish explicit domain ownership while retaining evidence-gated authority expansion`.
  - **TEST:** `TestSafetyConformancePreservesFilingPaymentRestrictionAndCorrectionHistory`.
  - **TEST MATRIX:** `PRIMARY=TestSafetyConformancePreservesFilingPaymentRestrictionAndCorrectionHistory`; `PROPERTY=TestTodo_SAFETY_002_Property`; `GOLDEN=TestTodo_SAFETY_002_Golden`; `RACE=TestTodo_SAFETY_002_Race`; `FAULT=TestTodo_SAFETY_002_Fault`; `SECURITY=TestTodo_SAFETY_002_Security`; `CONFORMANCE=TestTodo_SAFETY_002_Conformance`; `MUTATION=TestTodo_SAFETY_002_Mutation`.
  - **RED:** provider claim acceptance equals paid, filing timeout blindly resubmits, clearance deletes restriction, payment reversal rewrites ledger or retro incident change omits amended filing.
  - **GREEN:** fixtures separate submission/acceptance/payment/settlement, append clearance/reversal/correction, generate amendments/repairs and close only after fresh observations and obligations.
  - **REFACTOR:** reuse shared engines for mechanics while this domain retains authoritative meaning, lifecycle, correction and evidence policy.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [domain model coverage](data/models/intent-coverage-matrix.md), [authority expansion](specs/competitive-positioning-and-authority-expansion.md#authority-absorption-gate).

- [x] `SKILL-001` **[PHASE_3][SOL_HIGH] Define skill ontology, proficiency, evidence, equivalence and expiration.**
  - **Evidence (2026-09-05):** `TestSkillOntologyRejectsCyclesUnverifiedProficiencyAndUnsafeEquivalence` in `internal/domains/skill` (skill ontology with proficiency, evidence expiry and status, equivalence resolution refusing cycles, unverified proficiency and unsafe equivalence, immutable digests and audit-safe Explain; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/skill/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-016`, `INTENT-CONF-001`, `ENGINE-COVERAGE-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TALENT,BI.WORKFORCE,BI.RECRUITING; DIRECT=none; WHY=establish explicit domain ownership while retaining evidence-gated authority expansion`.
  - **TEST:** `TestSkillOntologyRejectsCyclesUnverifiedProficiencyAndUnsafeEquivalence`.
  - **TEST MATRIX:** `PRIMARY=TestSkillOntologyRejectsCyclesUnverifiedProficiencyAndUnsafeEquivalence`; `PROPERTY=TestTodo_SKILL_001_Property`; `GOLDEN=TestTodo_SKILL_001_Golden`; `RACE=TestTodo_SKILL_001_Race`; `FAULT=TestTodo_SKILL_001_Fault`; `SECURITY=TestTodo_SKILL_001_Security`; `CONFORMANCE=TestTodo_SKILL_001_Conformance`; `MUTATION=TestTodo_SKILL_001_Mutation`.
  - **RED:** skill hierarchy/equivalence cycles, self-asserted proficiency becomes verified, evidence expiry is ignored or external taxonomy overwrites canonical skill.
  - **GREEN:** versioned SkillDefinition graph and WorkerSkillEvidence return `VERIFIED|ASSERTED|EXPIRED|DISPUTED|UNKNOWN`, preserve source/proficiency/effective time and reject unsafe equivalence.
  - **REFACTOR:** reuse shared engines for mechanics while this domain retains authoritative meaning, lifecycle, correction and evidence policy.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [domain model coverage](data/models/intent-coverage-matrix.md), [authority expansion](specs/competitive-positioning-and-authority-expansion.md#authority-absorption-gate).

- [ ] `SKILL-002` **[CONFORMANCE][SOL_HIGH] Prove correction and parity across qualification, recruiting, learning and planning.**
  - **Depends:** `SKILL-001`, `CONF-001`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.TALENT,BI.WORKFORCE,BI.RECRUITING; DIRECT=none; WHY=establish explicit domain ownership while retaining evidence-gated authority expansion`.
  - **TEST:** `TestSkillConformanceUsesOnePinnedEvidenceRevisionAcrossConsumers`.
  - **TEST MATRIX:** `PRIMARY=TestSkillConformanceUsesOnePinnedEvidenceRevisionAcrossConsumers`; `PROPERTY=TestTodo_SKILL_002_Property`; `GOLDEN=TestTodo_SKILL_002_Golden`; `RACE=TestTodo_SKILL_002_Race`; `FAULT=TestTodo_SKILL_002_Fault`; `SECURITY=TestTodo_SKILL_002_Security`; `CONFORMANCE=TestTodo_SKILL_002_Conformance`; `MUTATION=TestTodo_SKILL_002_Mutation`.
  - **RED:** consumers use different proficiency/equivalence revisions, correction deletes old assessment, expired credential remains qualified or hidden evidence leaks in candidate ranking.
  - **GREEN:** all consumers resolve one pinned skill/evidence digest with purpose-safe explanation; successor evidence triggers scoped reevaluation and historical decisions retain original basis.
  - **REFACTOR:** reuse shared engines for mechanics while this domain retains authoritative meaning, lifecycle, correction and evidence policy.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [domain model coverage](data/models/intent-coverage-matrix.md), [authority expansion](specs/competitive-positioning-and-authority-expansion.md#authority-absorption-gate).

- [x] `CAREER-001` **[PHASE_4][SOL_HIGH] Define career preferences, target roles and development objectives.**
  - **Evidence (2026-09-05):** `TestCareerProfileSeparatesWorkerPreferenceFromManagerAssessmentAndPrediction` in `internal/domains/career` (career preferences, target roles and development objectives separating worker preference from manager assessment and prediction, with readiness gaps, objective lineage and completion evidence; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/career/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-016`, `INTENT-CONF-001`, `ENGINE-COVERAGE-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TALENT,BI.RECRUITING,BI.ANALYTICS; DIRECT=none; WHY=establish explicit domain ownership while retaining evidence-gated authority expansion`.
  - **TEST:** `TestCareerProfileSeparatesWorkerPreferenceFromManagerAssessmentAndPrediction`.
  - **TEST MATRIX:** `PRIMARY=TestCareerProfileSeparatesWorkerPreferenceFromManagerAssessmentAndPrediction`; `PROPERTY=TestTodo_CAREER_001_Property`; `GOLDEN=TestTodo_CAREER_001_Golden`; `RACE=TestTodo_CAREER_001_Race`; `FAULT=TestTodo_CAREER_001_Fault`; `SECURITY=TestTodo_CAREER_001_Security`; `CONFORMANCE=TestTodo_CAREER_001_Conformance`; `MUTATION=TestTodo_CAREER_001_Mutation`.
  - **RED:** manager/model overwrites worker preference, target role lacks job revision, sensitive mobility preference leaks, or predicted fit becomes domain fact.
  - **GREEN:** immutable worker-authored preference and separately sourced assessment/recommendation revisions bind visibility, target JobProfile, effective time and epistemic class.
  - **REFACTOR:** reuse shared engines for mechanics while this domain retains authoritative meaning, lifecycle, correction and evidence policy.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [domain model coverage](data/models/intent-coverage-matrix.md), [authority expansion](specs/competitive-positioning-and-authority-expansion.md#authority-absorption-gate).

- [x] `CAREER-002` **[CONFORMANCE][SOL_HIGH] Prove internal-mobility recommendation, correction and consent semantics.**
  - **Evidence (2026-09-08):** `TestCareerMobilityConformanceRequiresConsentEligibilityAndExplainableCandidateSet` and `TestTodo_CAREER_002_*`; `go test -count=1 -cover ./internal/domains/career/` PASS on windows/arm64 (45.5%, existing exact exception expires 2026-12-31); scoped vet PASS. Luna implementation, Sol refinement and integration review verify current consent/preference/population/role evidence through injected ports and reverify the exact assessment/action basis before a separate application intent. Forged assessment, stale preference, withdrawn role and post-assessment consent withdrawal regressions pass. No live recruiting adapter is claimed; detector-backed races remain CI-only.
  - **Depends:** `CAREER-001`, `CONF-001`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.TALENT,BI.RECRUITING,BI.ANALYTICS; DIRECT=none; WHY=establish explicit domain ownership while retaining evidence-gated authority expansion`.
  - **TEST:** `TestCareerMobilityConformanceRequiresConsentEligibilityAndExplainableCandidateSet`.
  - **TEST MATRIX:** `PRIMARY=TestCareerMobilityConformanceRequiresConsentEligibilityAndExplainableCandidateSet`; `PROPERTY=TestTodo_CAREER_002_Property`; `GOLDEN=TestTodo_CAREER_002_Golden`; `RACE=TestTodo_CAREER_002_Race`; `FAULT=TestTodo_CAREER_002_Fault`; `SECURITY=TestTodo_CAREER_002_Security`; `CONFORMANCE=TestTodo_CAREER_002_Conformance`; `MUTATION=TestTodo_CAREER_002_Mutation`.
  - **RED:** worker is recommended/applied without consent, denied role leaks, ranking uses protected attribute, stale preference persists or correction rewrites prior recommendation.
  - **GREEN:** authorized population/eligibility/matching returns explained candidates, action creates separate application intent, withdrawal stops future use and corrections preserve provenance.
  - **REFACTOR:** reuse shared engines for mechanics while this domain retains authoritative meaning, lifecycle, correction and evidence policy.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [domain model coverage](data/models/intent-coverage-matrix.md), [authority expansion](specs/competitive-positioning-and-authority-expansion.md#authority-absorption-gate).

- [x] `SUCCESSION-001` **[PHASE_4][SOL_HIGH] Define critical-role, succession-slate, successor readiness and risk revisions.**
  - **Evidence (2026-09-05):** `TestSuccessionSlateRequiresCriticalRoleAuthorizedNominationAndDatedReadiness` in `internal/domains/succession` (critical-role, succession-slate, successor readiness and risk revisions requiring an authorized nomination and dated readiness, with disclosure controls; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/succession/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-016`, `INTENT-CONF-001`, `ENGINE-COVERAGE-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.TALENT,BI.WORKFORCE,BI.PRIVACY; DIRECT=none; WHY=establish explicit domain ownership while retaining evidence-gated authority expansion`.
  - **TEST:** `TestSuccessionSlateRequiresCriticalRoleAuthorizedNominationAndDatedReadiness`.
  - **TEST MATRIX:** `PRIMARY=TestSuccessionSlateRequiresCriticalRoleAuthorizedNominationAndDatedReadiness`; `PROPERTY=TestTodo_SUCCESSION_001_Property`; `GOLDEN=TestTodo_SUCCESSION_001_Golden`; `RACE=TestTodo_SUCCESSION_001_Race`; `FAULT=TestTodo_SUCCESSION_001_Fault`; `SECURITY=TestTodo_SUCCESSION_001_Security`; `CONFORMANCE=TestTodo_SUCCESSION_001_Conformance`; `MUTATION=TestTodo_SUCCESSION_001_Mutation`.
  - **RED:** slate lacks position/job revision, unauthorized manager nominates, readiness/risk is unlabeled opinion, hidden nominee leaks or one current slate has conflicting revisions.
  - **GREEN:** immutable slate binds role, candidates, nominator/assessment sources, readiness `READY_NOW|READY_LATER|NOT_READY|UNKNOWN`, risk, visibility and effective/known times.
  - **REFACTOR:** reuse shared engines for mechanics while this domain retains authoritative meaning, lifecycle, correction and evidence policy.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [domain model coverage](data/models/intent-coverage-matrix.md), [authority expansion](specs/competitive-positioning-and-authority-expansion.md#authority-absorption-gate).

- [x] `SUCCESSION-002` **[CONFORMANCE][SOL_HIGH] Prove calibration, correction, vacancy use and non-guarantee semantics.**
  - **Evidence (2026-09-07):** `TestSuccessionConformanceNeverTurnsSlateIntoPromotionOrEmploymentPromise` and `TestTodo_SUCCESSION_002_*`; `go test -count=1 -cover ./internal/domains/succession/` PASS on windows/arm64 (52.6%, existing exact exception expires 2026-12-31); scoped vet PASS. Luna implementation, Sol refinement and integration review verify injected authority/consent/privacy/current-readiness checks, exact correction-source tuple binding, withheld candidate/count metadata and separate non-executing vacancy intent. Live authority adapters are not claimed; detector-backed races remain CI-only.
  - **Depends:** `SUCCESSION-001`, `CONF-001`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.TALENT,BI.WORKFORCE,BI.PRIVACY; DIRECT=none; WHY=establish explicit domain ownership while retaining evidence-gated authority expansion`.
  - **TEST:** `TestSuccessionConformanceNeverTurnsSlateIntoPromotionOrEmploymentPromise`.
  - **TEST MATRIX:** `PRIMARY=TestSuccessionConformanceNeverTurnsSlateIntoPromotionOrEmploymentPromise`; `PROPERTY=TestTodo_SUCCESSION_002_Property`; `GOLDEN=TestTodo_SUCCESSION_002_Golden`; `RACE=TestTodo_SUCCESSION_002_Race`; `FAULT=TestTodo_SUCCESSION_002_Fault`; `SECURITY=TestTodo_SUCCESSION_002_Security`; `CONFORMANCE=TestTodo_SUCCESSION_002_Conformance`; `MUTATION=TestTodo_SUCCESSION_002_Mutation`.
  - **RED:** calibration overwrites assessments, slate order directly fills vacancy/promotes, stale readiness is used, candidate views confidential rank or correction erases original.
  - **GREEN:** calibration appends decisions, vacancy creates separately governed selection intent, explanations state non-guarantee, restricted visibility holds and successor revisions preserve history.
  - **REFACTOR:** reuse shared engines for mechanics while this domain retains authoritative meaning, lifecycle, correction and evidence policy.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [domain model coverage](data/models/intent-coverage-matrix.md), [authority expansion](specs/competitive-positioning-and-authority-expansion.md#authority-absorption-gate).

- [x] `MERIT-001` **[PHASE_4][SOL_HIGH] Define merit cycle, population, budget, recommendation and calibration semantics.**
  - **Evidence (2026-09-05):** `TestMeritCycleRequiresFrozenPopulationBudgetAndExplainableRecommendation` in `internal/domains/merit` (merit cycle with frozen population, exact-decimal budget, guideline matrix, explainable recommendations, append-only calibration and manager separation; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/merit/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-016`, `INTENT-CONF-001`, `ENGINE-COVERAGE-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS,BI.ANALYTICS; DIRECT=none; WHY=establish explicit domain ownership while retaining evidence-gated authority expansion`.
  - **TEST:** `TestMeritCycleRequiresFrozenPopulationBudgetAndExplainableRecommendation`.
  - **TEST MATRIX:** `PRIMARY=TestMeritCycleRequiresFrozenPopulationBudgetAndExplainableRecommendation`; `PROPERTY=TestTodo_MERIT_001_Property`; `GOLDEN=TestTodo_MERIT_001_Golden`; `RACE=TestTodo_MERIT_001_Race`; `FAULT=TestTodo_MERIT_001_Fault`; `SECURITY=TestTodo_MERIT_001_Security`; `CONFORMANCE=TestTodo_MERIT_001_Conformance`; `MUTATION=TestTodo_MERIT_001_Mutation`.
  - **RED:** live population changes after launch, manager exceeds budget/range, recommendation uses stale salary/performance, protected feature drives outcome or calibration edits proposal silently.
  - **GREEN:** cycle revision binds population/snapshot/budget/rules, exact recommendations and calibration decisions with `PROPOSED|ADJUSTED|APPROVED|FINALIZED` and deterministic totals.
  - **REFACTOR:** reuse shared engines for mechanics while this domain retains authoritative meaning, lifecycle, correction and evidence policy.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [domain model coverage](data/models/intent-coverage-matrix.md), [authority expansion](specs/competitive-positioning-and-authority-expansion.md#authority-absorption-gate).

- [x] `MERIT-002` **[CONFORMANCE][SOL_HIGH] Prove concurrent budgeting, correction, finalization and compensation-child intents.**
  - **Evidence (2026-09-08):** `TestMeritConformanceConservesBudgetAndEmitsEachCompensationChangeOnce` and `TestTodo_MERIT_002_Property`, `_Golden`, `_Race`, `_Fault`, `_Security`, `_Conformance`, `_Mutation` cover exact conservation, explicit approved/rejected outcomes and immutable corrections. `TestMeritDecisionEvidenceRoundTrip` proves tenant-bound finalized-source verification, caller-transaction rollback, concurrent once-only child persistence, restart retrieval and synchronized post-insert conflicting-payload refusal. Root `go test -count=1 -cover ./internal/domains/merit/` PASS (70.1%); `go test -count=1 -cover ./internal/data/meritstore/` PASS (76.2%); final `go test -count=1 -run '^TestMeritDecisionEvidenceRoundTrip$' ./internal/data/meritstore/` PASS; `go vet ./internal/domains/merit/ ./internal/data/meritstore/` PASS. Migrations 00268/00270 retain exact timestamps and durable emission records; no compensation execution authority is inferred.
  - **Depends:** `MERIT-001`, `CONF-001`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.REWARDS,BI.ANALYTICS; DIRECT=none; WHY=establish explicit domain ownership while retaining evidence-gated authority expansion`.
  - **TEST:** `TestMeritConformanceConservesBudgetAndEmitsEachCompensationChangeOnce`.
  - **TEST MATRIX:** `PRIMARY=TestMeritConformanceConservesBudgetAndEmitsEachCompensationChangeOnce`; `PROPERTY=TestTodo_MERIT_002_Property`; `GOLDEN=TestTodo_MERIT_002_Golden`; `RACE=TestTodo_MERIT_002_Race`; `FAULT=TestTodo_MERIT_002_Fault`; `SECURITY=TestTodo_MERIT_002_Security`; `CONFORMANCE=TestTodo_MERIT_002_Conformance`; `MUTATION=TestTodo_MERIT_002_Mutation`.
  - **RED:** concurrent recommendations overspend, rounding loses money, finalization hides rejected worker, correction rewrites award or retry duplicates base-pay child intent.
  - **GREEN:** exact decimal conservation holds, per-worker outcomes remain explicit, successor correction is append-only and deterministic child ChangeBasePay intents emit once after finalization.
  - **REFACTOR:** reuse shared engines for mechanics while this domain retains authoritative meaning, lifecycle, correction and evidence policy.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [domain model coverage](data/models/intent-coverage-matrix.md), [authority expansion](specs/competitive-positioning-and-authority-expansion.md#authority-absorption-gate).

- [x] `INCENTIVE-001` **[PHASE_4][SOL_HIGH] Define incentive/commission plans, measures, attainment and award revisions.**
  - **Evidence (2026-09-05):** `TestIncentiveAwardRequiresPinnedPlanMeasuresAttainmentAndEligibility` in `internal/domains/incentive` (incentive and commission plans, measures, attainment and award revisions; an award requires a pinned plan, measures, attainment and eligibility; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/incentive/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-016`, `INTENT-CONF-001`, `ENGINE-COVERAGE-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS,BI.PAYROLL; DIRECT=none; WHY=establish explicit domain ownership while retaining evidence-gated authority expansion`.
  - **TEST:** `TestIncentiveAwardRequiresPinnedPlanMeasuresAttainmentAndEligibility`.
  - **TEST MATRIX:** `PRIMARY=TestIncentiveAwardRequiresPinnedPlanMeasuresAttainmentAndEligibility`; `PROPERTY=TestTodo_INCENTIVE_001_Property`; `GOLDEN=TestTodo_INCENTIVE_001_Golden`; `RACE=TestTodo_INCENTIVE_001_Race`; `FAULT=TestTodo_INCENTIVE_001_Fault`; `SECURITY=TestTodo_INCENTIVE_001_Security`; `CONFORMANCE=TestTodo_INCENTIVE_001_Conformance`; `MUTATION=TestTodo_INCENTIVE_001_Mutation`.
  - **RED:** award lacks plan/period/measure source, attainment changes after approval, negative/threshold/cap semantics are implicit or ineligible worker receives payout.
  - **GREEN:** versioned plan and award calculation bind eligibility, measures/watermarks, formula, thresholds/caps, exact amount/currency, approval and `CALCULATED|APPROVED|FINALIZED`.
  - **REFACTOR:** reuse shared engines for mechanics while this domain retains authoritative meaning, lifecycle, correction and evidence policy.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [domain model coverage](data/models/intent-coverage-matrix.md), [authority expansion](specs/competitive-positioning-and-authority-expansion.md#authority-absorption-gate).

- [ ] `INCENTIVE-002` **[CONFORMANCE][SOL_HIGH] Prove restatement, clawback, payroll-input and external-measure reconciliation.**
  - **Depends:** `INCENTIVE-001`, `CONF-001`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.REWARDS,BI.PAYROLL; DIRECT=none; WHY=establish explicit domain ownership while retaining evidence-gated authority expansion`.
  - **TEST:** `TestIncentiveConformanceHandlesRestatementClawbackAndPayrollWithoutRewrite`.
  - **TEST MATRIX:** `PRIMARY=TestIncentiveConformanceHandlesRestatementClawbackAndPayrollWithoutRewrite`; `PROPERTY=TestTodo_INCENTIVE_002_Property`; `GOLDEN=TestTodo_INCENTIVE_002_Golden`; `RACE=TestTodo_INCENTIVE_002_Race`; `FAULT=TestTodo_INCENTIVE_002_Fault`; `SECURITY=TestTodo_INCENTIVE_002_Security`; `CONFORMANCE=TestTodo_INCENTIVE_002_Conformance`; `MUTATION=TestTodo_INCENTIVE_002_Mutation`.
  - **RED:** restated sales measure overwrites award, clawback becomes negative history deletion, provider timeout duplicates payout or payroll uses unapproved award.
  - **GREEN:** successor calculation identifies delta, governed correction/clawback and one payroll input are emitted, external measures remain observations and settlement reconciles independently.
  - **REFACTOR:** reuse shared engines for mechanics while this domain retains authoritative meaning, lifecycle, correction and evidence policy.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [domain model coverage](data/models/intent-coverage-matrix.md), [authority expansion](specs/competitive-positioning-and-authority-expansion.md#authority-absorption-gate).

- [x] `EQUITY-001` **[PHASE_4][SOL_HIGH] Define equity plan, grant, vesting schedule and acceptance revisions.**
  - **Evidence (2026-09-05):** `TestEquityGrantRequiresPlanPoolInstrumentVestingApprovalAndAcceptance` in `internal/domains/equity` (equity plan, grant, vesting schedule and acceptance revisions; a grant requires plan, pool, instrument, vesting, approval and acceptance; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/equity/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-016`, `INTENT-CONF-001`, `ENGINE-COVERAGE-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS,BI.DOCUMENTS,BI.PAYROLL; DIRECT=none; WHY=establish explicit domain ownership while retaining evidence-gated authority expansion`.
  - **TEST:** `TestEquityGrantRequiresPlanPoolInstrumentVestingApprovalAndAcceptance`.
  - **TEST MATRIX:** `PRIMARY=TestEquityGrantRequiresPlanPoolInstrumentVestingApprovalAndAcceptance`; `PROPERTY=TestTodo_EQUITY_001_Property`; `GOLDEN=TestTodo_EQUITY_001_Golden`; `RACE=TestTodo_EQUITY_001_Race`; `FAULT=TestTodo_EQUITY_001_Fault`; `SECURITY=TestTodo_EQUITY_001_Security`; `CONFORMANCE=TestTodo_EQUITY_001_Conformance`; `MUTATION=TestTodo_EQUITY_001_Mutation`.
  - **RED:** grant lacks plan/pool/instrument/currency/quantity, vesting dates overlap, approval/acceptance digest differs or grant exceeds authorized pool.
  - **GREEN:** immutable grant binds exact plan/pool, units, price/currency, vesting schedule, approvals/documents and lifecycle `PROPOSED|ACCEPTED|ACTIVE|CANCELLED|SETTLED`.
  - **REFACTOR:** reuse shared engines for mechanics while this domain retains authoritative meaning, lifecycle, correction and evidence policy.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [domain model coverage](data/models/intent-coverage-matrix.md), [authority expansion](specs/competitive-positioning-and-authority-expansion.md#authority-absorption-gate).

- [x] `EQUITY-002` **[CONFORMANCE][SOL_HIGH] Prove vesting, forfeiture, correction, tax/payroll effect and provider reconciliation.**
  - **Evidence (2026-09-07):** `TestEquityConformancePreservesVestingLotsAndReconcilesProviderTaxEffects`, `TestTodo_EQUITY_002_*` and historical-forfeiture, correction-worker and effect-tampering regressions; `go test -count=1 -cover ./internal/domains/equity/` PASS on windows/arm64 (73.7%); scoped vet PASS. Luna implementation, Sol refinement and integration review prove exact domain lot/effect conformance; provider observations remain non-authoritative and no live payroll posting is claimed. Detector-backed race execution remains CI-only.
  - **Depends:** `EQUITY-001`, `CONF-001`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.REWARDS,BI.DOCUMENTS,BI.PAYROLL; DIRECT=none; WHY=establish explicit domain ownership while retaining evidence-gated authority expansion`.
  - **TEST:** `TestEquityConformancePreservesVestingLotsAndReconcilesProviderTaxEffects`.
  - **TEST MATRIX:** `PRIMARY=TestEquityConformancePreservesVestingLotsAndReconcilesProviderTaxEffects`; `PROPERTY=TestTodo_EQUITY_002_Property`; `GOLDEN=TestTodo_EQUITY_002_Golden`; `RACE=TestTodo_EQUITY_002_Race`; `FAULT=TestTodo_EQUITY_002_Fault`; `SECURITY=TestTodo_EQUITY_002_Security`; `CONFORMANCE=TestTodo_EQUITY_002_Conformance`; `MUTATION=TestTodo_EQUITY_002_Mutation`.
  - **RED:** termination erases vested lot, forfeiture affects vested units, correction rewrites grant, provider statement counts authoritative or tax/payroll effect is omitted.
  - **GREEN:** append-only lot events calculate vested/unvested/forfeited exactly, corrections create successor grant/effects and provider/tax/payroll outcomes reconcile with explicit unknown/repair states.
  - **REFACTOR:** reuse shared engines for mechanics while this domain retains authoritative meaning, lifecycle, correction and evidence policy.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [domain model coverage](data/models/intent-coverage-matrix.md), [authority expansion](specs/competitive-positioning-and-authority-expansion.md#authority-absorption-gate).

- [x] `ASSET-001` **[PHASE_3][SOL_HIGH] Define equipment inventory, custody, assignment and return revisions.**
  - **Evidence (2026-09-05):** `TestAssetCustodyRejectsDoubleAssignmentUnknownInventoryAndUnverifiedReturn` in `internal/domains/asset` (immutable digested equipment inventory, custody, assignment and return revisions with double-assignment, unknown-inventory and unverified-return refusals, in-memory store and audit-safe Explain; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/asset/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-016`, `INTENT-CONF-001`, `ENGINE-COVERAGE-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.LIFECYCLE,BI.ACCESS,BI.OPERATIONS; DIRECT=none; WHY=establish explicit domain ownership while retaining evidence-gated authority expansion`.
  - **TEST:** `TestAssetCustodyRejectsDoubleAssignmentUnknownInventoryAndUnverifiedReturn`.
  - **TEST MATRIX:** `PRIMARY=TestAssetCustodyRejectsDoubleAssignmentUnknownInventoryAndUnverifiedReturn`; `PROPERTY=TestTodo_ASSET_001_Property`; `GOLDEN=TestTodo_ASSET_001_Golden`; `RACE=TestTodo_ASSET_001_Race`; `FAULT=TestTodo_ASSET_001_Fault`; `SECURITY=TestTodo_ASSET_001_Security`; `CONFORMANCE=TestTodo_ASSET_001_Conformance`; `MUTATION=TestTodo_ASSET_001_Mutation`.
  - **RED:** asset lacks inventory identity/owner/classification, is assigned to two workers, custody transfer lacks receipt or reported return closes on employee assertion alone.
  - **GREEN:** CAS-fenced custody revisions bind asset, worker/location, condition, assignee/issuer receipts, effective time and `AVAILABLE|ASSIGNED|LOST|RETURN_PENDING|RETURNED|RETIRED`.
  - **REFACTOR:** reuse shared engines for mechanics while this domain retains authoritative meaning, lifecycle, correction and evidence policy.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [domain model coverage](data/models/intent-coverage-matrix.md), [authority expansion](specs/competitive-positioning-and-authority-expansion.md#authority-absorption-gate).

- [x] `ASSET-002` **[CONFORMANCE][SOL_HIGH] Prove loss, recovery, offboarding and external inventory reconciliation.**
  - **Evidence (2026-09-07):** `TestAssetConformanceTracksLossOffboardingRecoveryAndProviderDrift`, `TestTodo_ASSET_002_*`, and `TestAssetOffboardingAndCustodyAttributionResistCallerMutation`; `go test -count=1 -cover ./internal/domains/asset/` PASS on windows/arm64 (75.0%); `go vet ./internal/domains/asset/` PASS. Luna implementation, Sol refinement and integration review verify atomic in-memory recovery replay, immutable custody history, detached obligations and worker attribution. Provider observations cannot change physical custody. No external inventory deployment or durable crash recovery is claimed; detector-backed races remain CI-only.
  - **Depends:** `ASSET-001`, `CONF-001`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.LIFECYCLE,BI.ACCESS,BI.OPERATIONS; DIRECT=none; WHY=establish explicit domain ownership while retaining evidence-gated authority expansion`.
  - **TEST:** `TestAssetConformanceTracksLossOffboardingRecoveryAndProviderDrift`.
  - **TEST MATRIX:** `PRIMARY=TestAssetConformanceTracksLossOffboardingRecoveryAndProviderDrift`; `PROPERTY=TestTodo_ASSET_002_Property`; `GOLDEN=TestTodo_ASSET_002_Golden`; `RACE=TestTodo_ASSET_002_Race`; `FAULT=TestTodo_ASSET_002_Fault`; `SECURITY=TestTodo_ASSET_002_Security`; `CONFORMANCE=TestTodo_ASSET_002_Conformance`; `MUTATION=TestTodo_ASSET_002_Mutation`.
  - **RED:** lost asset disappears, offboarding closes with mandatory return open, MDM observation equals physical custody, timeout duplicates wipe/recovery or correction overwrites chain.
  - **GREEN:** offboarding obligation remains open by policy, logical/physical observations are distinct, scoped recovery/repair executes once and append-only custody chain verifies every handoff.
  - **REFACTOR:** reuse shared engines for mechanics while this domain retains authoritative meaning, lifecycle, correction and evidence policy.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [domain model coverage](data/models/intent-coverage-matrix.md), [authority expansion](specs/competitive-positioning-and-authority-expansion.md#authority-absorption-gate).

- [x] `CONTACT-001` **[PHASE_2][SOL_HIGH] Define normalized contact endpoint revision and verification challenge lifecycle.**
  - **Evidence (2026-09-05):** `TestContactEndpointAndChallengeRejectAmbiguousNormalizationReplayAndScopeMixing` in `internal/domains/contact` (normalized contact endpoint revisions and verification challenge lifecycle with digest-only endpoints, attempt budgets, terminal events and primary-selection protection; ambiguous normalization, replay and scope mixing refused; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/contact/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-016`, `INTENT-CONF-001`, `ENGINE-COVERAGE-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PEOPLE,BI.EXPERIENCE,BI.SECURITY; DIRECT=none; WHY=establish explicit domain ownership while retaining evidence-gated authority expansion`.
  - **TEST:** `TestContactEndpointAndChallengeRejectAmbiguousNormalizationReplayAndScopeMixing`.
  - **TEST MATRIX:** `PRIMARY=TestContactEndpointAndChallengeRejectAmbiguousNormalizationReplayAndScopeMixing`; `PROPERTY=TestTodo_CONTACT_001_Property`; `GOLDEN=TestTodo_CONTACT_001_Golden`; `RACE=TestTodo_CONTACT_001_Race`; `FAULT=TestTodo_CONTACT_001_Fault`; `SECURITY=TestTodo_CONTACT_001_Security`; `CONFORMANCE=TestTodo_CONTACT_001_Conformance`; `MUTATION=TestTodo_CONTACT_001_Mutation`.
  - **RED:** email/phone normalizes to ambiguous value, duplicate endpoint links identities, challenge token is stored plaintext/replayed/cross-purpose or unverified endpoint becomes primary.
  - **GREEN:** endpoint revision binds normalized/masked value, type/purpose/priority/source; hashed one-time challenge binds subject/endpoint/purpose/expiry and returns `VERIFIED|EXPIRED|CONSUMED|INVALID`.
  - **REFACTOR:** reuse shared engines for mechanics while this domain retains authoritative meaning, lifecycle, correction and evidence policy.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [domain model coverage](data/models/intent-coverage-matrix.md), [authority expansion](specs/competitive-positioning-and-authority-expansion.md#authority-absorption-gate).

- [x] `CONTACT-002` **[CONFORMANCE][SOL_HIGH] Prove reissue, primary selection, correction and external-contact reconciliation.**
  - **Evidence (2026-09-07):** `TestContactConformancePreservesVerificationAndPurposeAcrossCorrectionAndSync` and `TestTodo_CONTACT_002_*`; `go test -count=1 -cover ./internal/domains/contact/` PASS on windows/arm64 (54.4%, existing exact coverage exception expires 2026-12-31); `go vet ./internal/domains/contact/` PASS. Luna implementation, Sol refinement and integration review prove reference-store atomic reissue and exact retry receipts, minimum-priority permutation invariance, equivocation refusal and purpose-bound correction/repair. This is the digest-backed reference store, not a claim of PostgreSQL crash atomicity or message delivery; detector-backed races remain CI-only.
  - **Depends:** `CONTACT-001`, `CONF-001`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.PEOPLE,BI.EXPERIENCE,BI.SECURITY; DIRECT=none; WHY=establish explicit domain ownership while retaining evidence-gated authority expansion`.
  - **TEST:** `TestContactConformancePreservesVerificationAndPurposeAcrossCorrectionAndSync`.
  - **TEST MATRIX:** `PRIMARY=TestContactConformancePreservesVerificationAndPurposeAcrossCorrectionAndSync`; `PROPERTY=TestTodo_CONTACT_002_Property`; `GOLDEN=TestTodo_CONTACT_002_Golden`; `RACE=TestTodo_CONTACT_002_Race`; `FAULT=TestTodo_CONTACT_002_Fault`; `SECURITY=TestTodo_CONTACT_002_Security`; `CONFORMANCE=TestTodo_CONTACT_002_Conformance`; `MUTATION=TestTodo_CONTACT_002_Mutation`.
  - **RED:** reissue leaves old challenge valid, work-verified endpoint becomes personal recovery, removal deletes history, provider observation overwrites Person truth or retry sends duplicate challenge.
  - **GREEN:** old challenge revokes atomically, purpose-specific primary selection is deterministic, successor correction preserves evidence and external mismatches create bounded repair without Messaging owning contact truth.
  - **REFACTOR:** reuse shared engines for mechanics while this domain retains authoritative meaning, lifecycle, correction and evidence policy.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [domain model coverage](data/models/intent-coverage-matrix.md), [authority expansion](specs/competitive-positioning-and-authority-expansion.md#authority-absorption-gate).

- [x] `CONTACT-003` **[PHASE_3][SOL_HIGH] Persist contact endpoints and challenges behind a typed Store port with per-tenant CAS.**
  - **Evidence (2026-09-06):** `TestStorePortMemoryStorePreservesCASAndDigestOnlyHistory` in `internal/domains/contact` (a tenant-aware `Store` port over the digest-backed contact generation, a closed `StoreErrorCode` taxonomy (`INVALID`, `NOT_FOUND`, `DUPLICATE_REVISION`, `DUPLICATE_EVENT`, `STALE_CAS`, `DATABASE`) and a kernel-pure `MemoryStore` enforcing sequential endpoint revisions, compare-and-swap on endpoint revisions and challenges, and append-only challenge event history; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/contact/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CONTACT-001`, `PERSIST-CONTACT-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PEOPLE,BI.EXPERIENCE,BI.SECURITY; DIRECT=none; WHY=establish explicit domain ownership while retaining evidence-gated authority expansion`.
  - **TEST:** `TestStorePortMemoryStorePreservesCASAndDigestOnlyHistory`.
  - **TEST MATRIX:** `PRIMARY=TestStorePortMemoryStorePreservesCASAndDigestOnlyHistory`.
  - **RED:** an endpoint revision is admitted out of sequence or without an expected prior revision, a duplicate revision or challenge event is silently accepted, a challenge write against a changed canonical digest succeeds instead of returning a stale-CAS code, or a challenge's event history can shrink or be rewritten.
  - **GREEN:** `MemoryStore.PutEndpointRevision` enforces a strictly sequential `SupersedesRevision` chain and returns `StoreDuplicateCode` for a repeated revision or `StoreStaleCASCode` for a mismatched expected revision; `PutChallenge` requires the caller's expected `CanonicalDigest` for every update after the first write and refuses any update whose event history is shorter than or diverges from the stored one; `CodeOf` exposes the stable `StoreErrorCode` for every refusal.
  - **REFACTOR:** `MemoryStore` stays a kernel-pure reference implementation of `Store`, distinct from the older `EndpointRevision`/`VerificationChallenge` vocabulary `PERSIST-CONTACT-001` also persists, so both can evolve independently until one is retired.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [domain model coverage](data/models/intent-coverage-matrix.md), [authority expansion](specs/competitive-positioning-and-authority-expansion.md#authority-absorption-gate).

### Shared and high-priority engine continuation

- [x] `FX-002` **[PHASE_2][SOL_HIGH] Convert Money exactly with direct, inverse and triangulated quotes.**
  - **Evidence (2026-09-08):** `TestFXConversionReturnsExactAmountRoundingAndTrace`, `TestTodo_FX_002_Property`, `_Golden`, `_Race`, `_Fault`, `_Security`, `_Conformance`, `_Mutation`, and `TestDecimalDivisionNearMaxPrecisionHalfBoundary` verify direct/inverse/approved triangulation, exact policy rounding and refusal paths. Independent rational-oracle cases cover signed maximum-precision near-ties as well as 2,304 bounded combinations; kernel division rounds once from the exact quotient/remainder. Root `go test -count=1 -cover ./internal/kernel/values/ ./internal/domains/fx/` PASS (71.2% and 71.7%); `go vet ./internal/kernel/values/ ./internal/domains/fx/` PASS. Luna implementation and Sol/Astra refinement; no live rate-provider or financial-write authority claimed.
  - **Depends:** `FX-001`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REWARDS,BI.PAYROLL,BI.ANALYTICS; DIRECT=none; WHY=provide an explicit semantic engine owner for this accepted BusinessIntent partition`.
  - **TEST:** `TestFXConversionReturnsExactAmountRoundingAndTrace`.
  - **TEST MATRIX:** `PRIMARY=TestFXConversionReturnsExactAmountRoundingAndTrace`; `PROPERTY=TestTodo_FX_002_Property`; `GOLDEN=TestTodo_FX_002_Golden`; `RACE=TestTodo_FX_002_Race`; `FAULT=TestTodo_FX_002_Fault`; `SECURITY=TestTodo_FX_002_Security`; `CONFORMANCE=TestTodo_FX_002_Conformance`; `MUTATION=TestTodo_FX_002_Mutation`.
  - **RED:** conversion uses float, reverses a zero/invalid quote, triangulates through an unapproved currency, mixes quote times or omits currency-specific rounding.
  - **GREEN:** result returns source amount, exact decimal operations, every quote revision/path, rounding profile, converted amount and remainder; invalid inputs return typed error and no financial state.
  - **REFACTOR:** keep shared mechanics in kernel/engines and this package as the sole owner of the stated HCM meaning, lifecycle and correction semantics.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [model coverage](data/models/intent-coverage-matrix.md), [engine ownership](#businessintent-context-required-by-every-todo).

- [ ] `FX-003` **[CONFORMANCE][SOL_HIGH] Correct and reconcile FX rates across compensation, budget, payroll and analytics.**
  - **Depends:** `FX-002`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.REWARDS,BI.PAYROLL,BI.ANALYTICS; DIRECT=none; WHY=provide an explicit semantic engine owner for this accepted BusinessIntent partition`.
  - **TEST:** `TestFXCorrectionAppendsSuccessorAndRecomputesOnlyAffectedResults`.
  - **TEST MATRIX:** `PRIMARY=TestFXCorrectionAppendsSuccessorAndRecomputesOnlyAffectedResults`; `PROPERTY=TestTodo_FX_003_Property`; `GOLDEN=TestTodo_FX_003_Golden`; `RACE=TestTodo_FX_003_Race`; `FAULT=TestTodo_FX_003_Fault`; `SECURITY=TestTodo_FX_003_Security`; `CONFORMANCE=TestTodo_FX_003_Conformance`; `MUTATION=TestTodo_FX_003_Mutation`.
  - **RED:** provider restatement overwrites a quote, silently changes approved proposal, recomputes unrelated currencies or makes historical report use today's rate.
  - **GREEN:** correction appends successor quote, impact graph identifies affected derived results/intents, approved artifacts require explicit replan and cross-domain golden vectors produce identical conversion digest.
  - **REFACTOR:** keep shared mechanics in kernel/engines and this package as the sole owner of the stated HCM meaning, lifecycle and correction semantics.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [model coverage](data/models/intent-coverage-matrix.md), [engine ownership](#businessintent-context-required-by-every-todo).

- [x] `I18N-001` **[PHASE_2][SOL_HIGH] Define LocaleContext and immutable translation catalog revisions.**
  - **Evidence (2026-09-06):** `TestLocaleAndTranslationCatalogRejectInvalidFallbackAndUnreviewedLegalContent` in `internal/experience/i18n`; `go test -count=1 ./internal/experience/i18n/` PASS on windows/arm64 (Go 1.26.3); evidence recorded by the orchestrator after the front-end session ticked the entry without one.
  - **Depends:** `MODEL-016`, `INTENT-CONF-001`, `ENGINE-COVERAGE-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.EXPERIENCE,BI.DOCUMENTS,BI.WORK,BI.CASES; DIRECT=none; WHY=provide an explicit semantic engine owner for this accepted BusinessIntent partition`.
  - **TEST:** `TestLocaleAndTranslationCatalogRejectInvalidFallbackAndUnreviewedLegalContent`.
  - **TEST MATRIX:** `PRIMARY=TestLocaleAndTranslationCatalogRejectInvalidFallbackAndUnreviewedLegalContent`; `PROPERTY=TestTodo_I18N_001_Property`; `GOLDEN=TestTodo_I18N_001_Golden`; `RACE=TestTodo_I18N_001_Race`; `FAULT=TestTodo_I18N_001_Fault`; `SECURITY=TestTodo_I18N_001_Security`; `CONFORMANCE=TestTodo_I18N_001_Conformance`; `MUTATION=TestTodo_I18N_001_Mutation`.
  - **RED:** caller chooses unsupported locale, fallback cycles, translation key changes meaning in place, or legal text is published without reviewer/source/effective interval.
  - **GREEN:** catalog revision binds BCP-47 locale, fallback DAG, key/source/classification/reviewer/effective interval and legal-review status; invalid publication creates no active catalog.
  - **REFACTOR:** keep shared mechanics in kernel/engines and this package as the sole owner of the stated HCM meaning, lifecycle and correction semantics.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [model coverage](data/models/intent-coverage-matrix.md), [engine ownership](#businessintent-context-required-by-every-todo).

- [x] `I18N-002` **[PHASE_2][SOL_HIGH] Resolve translations and format names, dates, numbers and money deterministically.**
  - **Evidence (2026-09-06):** `TestLocalizationResolutionReturnsVersionedTextFormattingAndDirection` in `internal/experience/localize`; `go test -count=1 ./internal/experience/localize/` PASS on windows/arm64 (Go 1.26.3); evidence recorded by the orchestrator after the front-end session ticked the entry without one.
  - **Depends:** `I18N-001`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.EXPERIENCE,BI.DOCUMENTS,BI.WORK,BI.CASES; DIRECT=none; WHY=provide an explicit semantic engine owner for this accepted BusinessIntent partition`.
  - **TEST:** `TestLocalizationResolutionReturnsVersionedTextFormattingAndDirection`.
  - **TEST MATRIX:** `PRIMARY=TestLocalizationResolutionReturnsVersionedTextFormattingAndDirection`; `PROPERTY=TestTodo_I18N_002_Property`; `GOLDEN=TestTodo_I18N_002_Golden`; `RACE=TestTodo_I18N_002_Race`; `FAULT=TestTodo_I18N_002_Fault`; `SECURITY=TestTodo_I18N_002_Security`; `CONFORMANCE=TestTodo_I18N_002_Conformance`; `MUTATION=TestTodo_I18N_002_Mutation`.
  - **RED:** missing key falls back nondeterministically, plural/gender branch is guessed, RTL direction is omitted, or formatting depends on host locale/tzdb.
  - **GREEN:** result binds locale/catalog/CLDR-tzdb-format profile, selected fallback path, plural/gender case, direction and exact rendered value; unknown remains explicit.
  - **REFACTOR:** keep shared mechanics in kernel/engines and this package as the sole owner of the stated HCM meaning, lifecycle and correction semantics.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [model coverage](data/models/intent-coverage-matrix.md), [engine ownership](#businessintent-context-required-by-every-todo).

- [x] `I18N-003` **[CONFORMANCE][SOL_HIGH] Invalidate localized derivatives and prove channel parity.**
  - **Evidence (2026-09-06):** `TestLegalTranslationChangeInvalidatesRenderedDerivativesAcrossChannels` in `internal/experience/i18nparity`; `go test -count=1 ./internal/experience/i18nparity/` PASS on windows/arm64 (Go 1.26.3); evidence recorded by the orchestrator after the front-end session ticked the entry without one.
  - **Depends:** `I18N-002`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.EXPERIENCE,BI.DOCUMENTS,BI.WORK,BI.CASES; DIRECT=none; WHY=provide an explicit semantic engine owner for this accepted BusinessIntent partition`.
  - **TEST:** `TestLegalTranslationChangeInvalidatesRenderedDerivativesAcrossChannels`.
  - **TEST MATRIX:** `PRIMARY=TestLegalTranslationChangeInvalidatesRenderedDerivativesAcrossChannels`; `PROPERTY=TestTodo_I18N_003_Property`; `GOLDEN=TestTodo_I18N_003_Golden`; `RACE=TestTodo_I18N_003_Race`; `FAULT=TestTodo_I18N_003_Fault`; `SECURITY=TestTodo_I18N_003_Security`; `CONFORMANCE=TestTodo_I18N_003_Conformance`; `MUTATION=TestTodo_I18N_003_Mutation`.
  - **RED:** updated legal translation leaves old document/message/form/knowledge/GWC derivative active, or channels render different semantic content without policy.
  - **GREEN:** dependency index invalidates exact derivatives, regeneration preserves source/version/reviewer lineage and conformance fixtures match meaning, direction, accessibility and acknowledgement digest across channels.
  - **REFACTOR:** keep shared mechanics in kernel/engines and this package as the sole owner of the stated HCM meaning, lifecycle and correction semantics.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [model coverage](data/models/intent-coverage-matrix.md), [engine ownership](#businessintent-context-required-by-every-todo).

- [x] `LOCATION-001` **[PHASE_2][SOL_HIGH] Define canonical Address, WorkLocation and Worksite revision identities.**
  - **Evidence (2026-09-05):** `TestLocationModelSeparatesHomeAddressWorksiteTaxResidenceAndMailing` in `internal/domains/location` (canonical Address, WorkLocation and Worksite revision identities separating home address, worksite, tax residence and mailing, with overlap refusals and jurisdiction resolution; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/location/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-016`, `INTENT-CONF-001`, `ENGINE-COVERAGE-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PEOPLE,BI.REGULATORY,BI.WORKFORCE,BI.MOBILITY; DIRECT=none; WHY=provide an explicit semantic engine owner for this accepted BusinessIntent partition`.
  - **TEST:** `TestLocationModelSeparatesHomeAddressWorksiteTaxResidenceAndMailing`.
  - **TEST MATRIX:** `PRIMARY=TestLocationModelSeparatesHomeAddressWorksiteTaxResidenceAndMailing`; `PROPERTY=TestTodo_LOCATION_001_Property`; `GOLDEN=TestTodo_LOCATION_001_Golden`; `RACE=TestTodo_LOCATION_001_Race`; `FAULT=TestTodo_LOCATION_001_Fault`; `SECURITY=TestTodo_LOCATION_001_Security`; `CONFORMANCE=TestTodo_LOCATION_001_Conformance`; `MUTATION=TestTodo_LOCATION_001_Mutation`.
  - **RED:** home address, worksite, tax residence and mailing endpoint share one mutable record, or revision lacks source/effective/known time and confidence.
  - **GREEN:** distinct typed revisions preserve normalized components, locality/timezone candidates, source authority, confidence, effective/known interval and correction lineage.
  - **REFACTOR:** keep shared mechanics in kernel/engines and this package as the sole owner of the stated HCM meaning, lifecycle and correction semantics.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [model coverage](data/models/intent-coverage-matrix.md), [engine ownership](#businessintent-context-required-by-every-todo).

- [x] `LOCATION-002` **[PHASE_2][SOL_HIGH] Normalize addresses and resolve timezone, locality and jurisdiction candidates.**
  - **Evidence (2026-09-05):** `TestLocationResolutionReturnsCandidatesConfidenceAndUnknownWithoutInventingJurisdiction` in `internal/domains/location` (resolver returns normalized address plus ranked locality/timezone/jurisdiction candidates, confidence, source/release and RESOLVED|AMBIGUOUS|PARTIAL|UNKNOWN; no legal decision is made; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/location/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LOCATION-001`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PEOPLE,BI.REGULATORY,BI.WORKFORCE,BI.MOBILITY; DIRECT=none; WHY=provide an explicit semantic engine owner for this accepted BusinessIntent partition`.
  - **TEST:** `TestLocationResolutionReturnsCandidatesConfidenceAndUnknownWithoutInventingJurisdiction`.
  - **TEST MATRIX:** `PRIMARY=TestLocationResolutionReturnsCandidatesConfidenceAndUnknownWithoutInventingJurisdiction`; `PROPERTY=TestTodo_LOCATION_002_Property`; `GOLDEN=TestTodo_LOCATION_002_Golden`; `RACE=TestTodo_LOCATION_002_Race`; `FAULT=TestTodo_LOCATION_002_Fault`; `SECURITY=TestTodo_LOCATION_002_Security`; `CONFORMANCE=TestTodo_LOCATION_002_Conformance`; `MUTATION=TestTodo_LOCATION_002_Mutation`.
  - **RED:** partial/ambiguous address yields one certain jurisdiction, geocoder/provider acceptance becomes fact, postal normalization loses original evidence or timezone is inferred from tenant default.
  - **GREEN:** resolver returns normalized address plus ranked locality/timezone/jurisdiction candidates, confidence, source/release and `RESOLVED|AMBIGUOUS|PARTIAL|UNKNOWN`; no legal decision is made.
  - **REFACTOR:** keep shared mechanics in kernel/engines and this package as the sole owner of the stated HCM meaning, lifecycle and correction semantics.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [model coverage](data/models/intent-coverage-matrix.md), [engine ownership](#businessintent-context-required-by-every-todo).

- [x] `LOCATION-003` **[CONFORMANCE][SOL_HIGH] Correct location revisions and emit governed downstream impact.**
  - **Evidence (2026-09-05):** `TestMaterialLocationChangeEmitsExactLegalTaxPayrollLeaveAndScheduleImpacts` in `internal/domains/location` (successor revision and impact plan identify exact subjects/intervals/rule releases and emit bounded reevaluation intents; unaffected periods remain unchanged and downstream outcomes reconcile independently; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/location/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LOCATION-002`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.PEOPLE,BI.REGULATORY,BI.WORKFORCE,BI.MOBILITY; DIRECT=none; WHY=provide an explicit semantic engine owner for this accepted BusinessIntent partition`.
  - **TEST:** `TestMaterialLocationChangeEmitsExactLegalTaxPayrollLeaveAndScheduleImpacts`.
  - **TEST MATRIX:** `PRIMARY=TestMaterialLocationChangeEmitsExactLegalTaxPayrollLeaveAndScheduleImpacts`; `PROPERTY=TestTodo_LOCATION_003_Property`; `GOLDEN=TestTodo_LOCATION_003_Golden`; `RACE=TestTodo_LOCATION_003_Race`; `FAULT=TestTodo_LOCATION_003_Fault`; `SECURITY=TestTodo_LOCATION_003_Security`; `CONFORMANCE=TestTodo_LOCATION_003_Conformance`; `MUTATION=TestTodo_LOCATION_003_Mutation`.
  - **RED:** retroactive location correction overwrites history, misses affected legal/tax/payroll/leave/schedule periods or directly mutates downstream domains.
  - **GREEN:** successor revision and impact plan identify exact subjects/intervals/rule releases and emit bounded reevaluation intents; unaffected periods remain unchanged and downstream outcomes reconcile independently.
  - **REFACTOR:** keep shared mechanics in kernel/engines and this package as the sole owner of the stated HCM meaning, lifecycle and correction semantics.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [model coverage](data/models/intent-coverage-matrix.md), [engine ownership](#businessintent-context-required-by-every-todo).

- [x] `SERVICE-001` **[PHASE_3][SOL_HIGH] Define service periods, credit sources and seniority dimensions.**
  - **Evidence (2026-09-05):** `TestServiceModelRejectsOverlappingUnattributedPeriodsAndImplicitSeniority` in `internal/domains/service` (service periods with credit sources and seniority dimensions; overlapping and unattributed periods refused, seniority never implied without an attributed credit source; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/service/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-016`, `INTENT-CONF-001`, `ENGINE-COVERAGE-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PEOPLE,BI.REWARDS,BI.WORKFORCE; DIRECT=none; WHY=provide an explicit semantic engine owner for this accepted BusinessIntent partition`.
  - **TEST:** `TestServiceModelRejectsOverlappingUnattributedPeriodsAndImplicitSeniority`.
  - **TEST MATRIX:** `PRIMARY=TestServiceModelRejectsOverlappingUnattributedPeriodsAndImplicitSeniority`; `PROPERTY=TestTodo_SERVICE_001_Property`; `GOLDEN=TestTodo_SERVICE_001_Golden`; `RACE=TestTodo_SERVICE_001_Race`; `FAULT=TestTodo_SERVICE_001_Fault`; `SECURITY=TestTodo_SERVICE_001_Security`; `CONFORMANCE=TestTodo_SERVICE_001_Conformance`; `MUTATION=TestTodo_SERVICE_001_Mutation`.
  - **RED:** service period overlaps without policy, acquisition/union credit lacks authority, leave/termination is treated uniformly across dimensions or one service date serves every purpose.
  - **GREEN:** typed periods and credit revisions bind employment/source, break type, applicable service dimensions, rules and effective/known times; invalid chronology returns exact conflict.
  - **REFACTOR:** keep shared mechanics in kernel/engines and this package as the sole owner of the stated HCM meaning, lifecycle and correction semantics.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [model coverage](data/models/intent-coverage-matrix.md), [engine ownership](#businessintent-context-required-by-every-todo).

- [x] `SERVICE-002` **[PHASE_3][SOL_HIGH] Calculate continuous service, adjusted dates and seniority with bridging rules.**
  - **Evidence (2026-09-07):** `TestServiceCalculationExplainsBreakBridgeCreditAndDimensionSpecificOrder`, `TestTodo_SERVICE_002_*` and `TestTodo_SERVICE_002_ContinuousRunResetsAtUnbridgedAndTrailingBreaks`; `go test -count=1 -cover ./internal/domains/service/` PASS on windows/arm64 (77.4%); scoped vet PASS. Luna implementation, Sol refinement and integration review prove interval union, explicit bridge credit, separate total/continuous duration, adjusted dates and pinned-cohort ranking with unknown-peer refusal. New evaluation canonical format is v2 while legacy unpinned model bytes remain v1. No live eligibility adapter is claimed; detector-backed races remain CI-only.
  - **Depends:** `SERVICE-001`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PEOPLE,BI.REWARDS,BI.WORKFORCE; DIRECT=none; WHY=provide an explicit semantic engine owner for this accepted BusinessIntent partition`.
  - **TEST:** `TestServiceCalculationExplainsBreakBridgeCreditAndDimensionSpecificOrder`.
  - **TEST MATRIX:** `PRIMARY=TestServiceCalculationExplainsBreakBridgeCreditAndDimensionSpecificOrder`; `PROPERTY=TestTodo_SERVICE_002_Property`; `GOLDEN=TestTodo_SERVICE_002_Golden`; `RACE=TestTodo_SERVICE_002_Race`; `FAULT=TestTodo_SERVICE_002_Fault`; `SECURITY=TestTodo_SERVICE_002_Security`; `CONFORMANCE=TestTodo_SERVICE_002_Conformance`; `MUTATION=TestTodo_SERVICE_002_Mutation`.
  - **RED:** calculation ignores break threshold, bridges without rule, double-counts concurrent employment, uses ambient date or produces one ordering for company/job/unit seniority.
  - **GREEN:** pinned history/rules return exact credited duration, adjusted date and dimension-specific seniority rank with operation trace and `KNOWN|PARTIAL|UNKNOWN`.
  - **REFACTOR:** keep shared mechanics in kernel/engines and this package as the sole owner of the stated HCM meaning, lifecycle and correction semantics.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [model coverage](data/models/intent-coverage-matrix.md), [engine ownership](#businessintent-context-required-by-every-todo).

- [x] `SERVICE-003` **[CONFORMANCE][SOL_HIGH] Correct service history and trigger affected eligibility safely.**
  - **Evidence (2026-09-08):** `TestServiceCorrectionAppendsHistoryAndReevaluatesOnlyDependentEligibility` and independent `TestTodo_SERVICE_003_Property`, `_Golden`, `_Race`, `_Fault`, `_Security`, `_Conformance`, `_Mutation`, and removed-scope/rule-change regressions; `go test -count=1 -cover ./internal/domains/service/` PASS (78.4%); `go vet ./internal/domains/service/` PASS. Exact successor/current/result pins retain prior explanation; old and new interval/dimension scope and rule-only changes emit bounded dependent drafts, excluding unrelated workers. Protected outcomes require governed correction. Sol/root refinement replaced matrix aliases with exact golden, permutation, shared-input concurrency and five eligibility-domain assertions. This semantic compiler emits non-executable drafts; no live dispatcher or governance authority is claimed.
  - **Depends:** `SERVICE-002`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.PEOPLE,BI.REWARDS,BI.WORKFORCE; DIRECT=none; WHY=provide an explicit semantic engine owner for this accepted BusinessIntent partition`.
  - **TEST:** `TestServiceCorrectionAppendsHistoryAndReevaluatesOnlyDependentEligibility`.
  - **TEST MATRIX:** `PRIMARY=TestServiceCorrectionAppendsHistoryAndReevaluatesOnlyDependentEligibility`; `PROPERTY=TestTodo_SERVICE_003_Property`; `GOLDEN=TestTodo_SERVICE_003_Golden`; `RACE=TestTodo_SERVICE_003_Race`; `FAULT=TestTodo_SERVICE_003_Fault`; `SECURITY=TestTodo_SERVICE_003_Security`; `CONFORMANCE=TestTodo_SERVICE_003_Conformance`; `MUTATION=TestTodo_SERVICE_003_Mutation`.
  - **RED:** retro correction overwrites prior result, silently revokes vested outcome, fails to reevaluate leave/benefit/PTO/severance/promotion dependencies or recalculates unrelated workers.
  - **GREEN:** successor result preserves old explanation, dependency graph emits scoped reevaluation/replan intents and material approved outcomes require governed correction rather than mutation.
  - **REFACTOR:** keep shared mechanics in kernel/engines and this package as the sole owner of the stated HCM meaning, lifecycle and correction semantics.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [model coverage](data/models/intent-coverage-matrix.md), [engine ownership](#businessintent-context-required-by-every-todo).

- [x] `CBA-001` **[PHASE_4][SOL_HIGH] Define agreement revisions, bargaining units and applicability.**
  - **Evidence (2026-09-05):** `TestCBAApplicabilityRequiresAgreementUnitWorkerAndEffectiveRuleRevision` in `internal/domains/cba` (agreement revisions, bargaining units and port-backed applicability requiring agreement, unit, worker and an effective rule revision, with lineage, canonical digests and safe Explain; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/cba/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-016`, `INTENT-CONF-001`, `ENGINE-COVERAGE-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE,BI.REWARDS,BI.CASES,BI.REGULATORY; DIRECT=none; WHY=provide an explicit semantic engine owner for this accepted BusinessIntent partition`.
  - **TEST:** `TestCBAApplicabilityRequiresAgreementUnitWorkerAndEffectiveRuleRevision`.
  - **TEST MATRIX:** `PRIMARY=TestCBAApplicabilityRequiresAgreementUnitWorkerAndEffectiveRuleRevision`; `PROPERTY=TestTodo_CBA_001_Property`; `GOLDEN=TestTodo_CBA_001_Golden`; `RACE=TestTodo_CBA_001_Race`; `FAULT=TestTodo_CBA_001_Fault`; `SECURITY=TestTodo_CBA_001_Security`; `CONFORMANCE=TestTodo_CBA_001_Conformance`; `MUTATION=TestTodo_CBA_001_Mutation`.
  - **RED:** worker is placed in unit by free text, expired agreement applies, overlapping agreements resolve silently or membership lacks representation/source/effective evidence.
  - **GREEN:** immutable agreement/unit/membership revisions return `APPLICABLE|NOT_APPLICABLE|CONFLICT|UNKNOWN` with precedence basis, representative and effective/known interval.
  - **REFACTOR:** keep shared mechanics in kernel/engines and this package as the sole owner of the stated HCM meaning, lifecycle and correction semantics.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [model coverage](data/models/intent-coverage-matrix.md), [engine ownership](#businessintent-context-required-by-every-todo).

- [x] `CBA-002` **[PHASE_4][SOL_HIGH] Compose CBA wage, schedule, leave, seniority and discipline constraints.**
  - **Evidence (2026-09-07):** `TestCBACompositionPreservesMandatoryStatutoryAndAgreementConstraints` and `TestTodo_CBA_002_*`; `go test -count=1 -cover ./internal/domains/cba/` PASS on windows/arm64 (52.9%, existing exact coverage exception expires 2026-12-31); `go vet ./internal/domains/cba/` PASS. Luna implementation, Sol refinement and integration review verify explicit policy, mandatory intersections, zero-bound presence, separate per-bound provenance, permutation stability and stale/unpinned seniority refusal. Pure composition consumes supplied release/service pins; it does not certify current law or authenticate a live policy source. Detector-backed race execution remains CI-only.
  - **Depends:** `CBA-001`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE,BI.REWARDS,BI.CASES,BI.REGULATORY; DIRECT=none; WHY=provide an explicit semantic engine owner for this accepted BusinessIntent partition`.
  - **TEST:** `TestCBACompositionPreservesMandatoryStatutoryAndAgreementConstraints`.
  - **TEST MATRIX:** `PRIMARY=TestCBACompositionPreservesMandatoryStatutoryAndAgreementConstraints`; `PROPERTY=TestTodo_CBA_002_Property`; `GOLDEN=TestTodo_CBA_002_Golden`; `RACE=TestTodo_CBA_002_Race`; `FAULT=TestTodo_CBA_002_Fault`; `SECURITY=TestTodo_CBA_002_Security`; `CONFORMANCE=TestTodo_CBA_002_Conformance`; `MUTATION=TestTodo_CBA_002_Mutation`.
  - **RED:** company rule overrides mandatory statute/agreement, most-favorable rule is assumed without policy, unknown applicability permits action or seniority list uses stale service result.
  - **GREEN:** typed composition returns obligations/prohibitions/calculations with source clause/release and `ALLOW|ALLOW_WITH_OBLIGATIONS|BLOCK|UNKNOWN`; mandatory restrictions survive.
  - **REFACTOR:** keep shared mechanics in kernel/engines and this package as the sole owner of the stated HCM meaning, lifecycle and correction semantics.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [model coverage](data/models/intent-coverage-matrix.md), [engine ownership](#businessintent-context-required-by-every-todo).

- [ ] `CBA-003` **[CONFORMANCE][SOL_HIGH] Generate representation/grievance obligations and analyze agreement changes.**
  - **Depends:** `CBA-002`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.WORKFORCE,BI.REWARDS,BI.CASES,BI.REGULATORY; DIRECT=none; WHY=provide an explicit semantic engine owner for this accepted BusinessIntent partition`.
  - **TEST:** `TestCBARevisionImpactCreatesRepresentationAndGrievanceWorkWithoutRewritingHistory`.
  - **TEST MATRIX:** `PRIMARY=TestCBARevisionImpactCreatesRepresentationAndGrievanceWorkWithoutRewritingHistory`; `PROPERTY=TestTodo_CBA_003_Property`; `GOLDEN=TestTodo_CBA_003_Golden`; `RACE=TestTodo_CBA_003_Race`; `FAULT=TestTodo_CBA_003_Fault`; `SECURITY=TestTodo_CBA_003_Security`; `CONFORMANCE=TestTodo_CBA_003_Conformance`; `MUTATION=TestTodo_CBA_003_Mutation`.
  - **RED:** agreement change silently alters approved schedules/pay/leave/discipline, misses represented population or grievance/arbitration deadline, or manager waives representation.
  - **GREEN:** frozen affected population and impact graph create typed consultation/notice/grievance/reevaluation intents with calendars/evidence; prior decisions remain historically explainable.
  - **REFACTOR:** keep shared mechanics in kernel/engines and this package as the sole owner of the stated HCM meaning, lifecycle and correction semantics.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [model coverage](data/models/intent-coverage-matrix.md), [engine ownership](#businessintent-context-required-by-every-todo).

- [x] `REPORT-001` **[PHASE_2][SOL_HIGH] Define immutable report and dashboard definitions.**
  - **Evidence (2026-09-06):** `TestReportDefinitionRejectsUnknownFieldsMetricsParametersAndUnsafeDefaults` in `internal/experience/reporting`; `go test -count=1 ./internal/experience/reporting/` PASS on windows/arm64 (Go 1.26.3); evidence recorded by the orchestrator after the front-end session ticked the entry without one.
  - **Depends:** `MODEL-016`, `INTENT-CONF-001`, `ENGINE-COVERAGE-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ANALYTICS,BI.INTELLIGENCE,BI.EXPERIENCE; DIRECT=none; WHY=provide an explicit semantic engine owner for this accepted BusinessIntent partition`.
  - **TEST:** `TestReportDefinitionRejectsUnknownFieldsMetricsParametersAndUnsafeDefaults`.
  - **TEST MATRIX:** `PRIMARY=TestReportDefinitionRejectsUnknownFieldsMetricsParametersAndUnsafeDefaults`; `PROPERTY=TestTodo_REPORT_001_Property`; `GOLDEN=TestTodo_REPORT_001_Golden`; `RACE=TestTodo_REPORT_001_Race`; `FAULT=TestTodo_REPORT_001_Fault`; `SECURITY=TestTodo_REPORT_001_Security`; `CONFORMANCE=TestTodo_REPORT_001_Conformance`; `MUTATION=TestTodo_REPORT_001_Mutation`.
  - **RED:** report references unknown field/metric/population, parameter default broadens scope, dashboard embeds mutable query or definition changes in place.
  - **GREEN:** versioned definition binds typed parameters, fields/metrics/population, sorting/aggregation, disclosure policy, render/export formats, owner and compatibility digest.
  - **REFACTOR:** keep shared mechanics in kernel/engines and this package as the sole owner of the stated HCM meaning, lifecycle and correction semantics.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [model coverage](data/models/intent-coverage-matrix.md), [engine ownership](#businessintent-context-required-by-every-todo).

- [x] `REPORT-002` **[PHASE_2][SOL_HIGH] Execute, render and export reports with authorization and watermarks.**
  - **Evidence (2026-09-06):** `TestReportExecutionBindsAuthorizationPopulationWatermarkAndExactOutput` in `internal/experience/reportrender`; `go test -count=1 ./internal/experience/reportrender/` PASS on windows/arm64 (Go 1.26.3); evidence recorded by the orchestrator after the front-end session ticked the entry without one.
  - **Depends:** `REPORT-001`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ANALYTICS,BI.INTELLIGENCE,BI.EXPERIENCE; DIRECT=none; WHY=provide an explicit semantic engine owner for this accepted BusinessIntent partition`.
  - **TEST:** `TestReportExecutionBindsAuthorizationPopulationWatermarkAndExactOutput`.
  - **TEST MATRIX:** `PRIMARY=TestReportExecutionBindsAuthorizationPopulationWatermarkAndExactOutput`; `PROPERTY=TestTodo_REPORT_002_Property`; `GOLDEN=TestTodo_REPORT_002_Golden`; `RACE=TestTodo_REPORT_002_Race`; `FAULT=TestTodo_REPORT_002_Fault`; `SECURITY=TestTodo_REPORT_002_Security`; `CONFORMANCE=TestTodo_REPORT_002_Conformance`; `MUTATION=TestTodo_REPORT_002_Mutation`.
  - **RED:** cached/admin execution leaks denied rows/counts, mixed watermarks claim one as-of state, export differs from viewed result or provider timeout returns partial as complete.
  - **GREEN:** execution plan binds principal/purpose/field/population scope and source watermarks; result returns `COMPLETE|PARTIAL|STALE|UNKNOWN`, exact row/aggregate digest and identical authorized render/export semantics.
  - **REFACTOR:** keep shared mechanics in kernel/engines and this package as the sole owner of the stated HCM meaning, lifecycle and correction semantics.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [model coverage](data/models/intent-coverage-matrix.md), [engine ownership](#businessintent-context-required-by-every-todo).

- [x] `REPORT-003` **[CONFORMANCE][SOL_HIGH] Schedule dashboards and reproduce historical report results.**
  - **Evidence (2026-09-06):** `TestScheduledReportAndDashboardRefreshRemainReproducibleAndNonDisclosing` in `internal/experience/reportschedule`; `go test -count=1 ./internal/experience/reportschedule/` PASS on windows/arm64 (Go 1.26.3); evidence recorded by the orchestrator after the front-end session ticked the entry without one.
  - **Depends:** `REPORT-002`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ANALYTICS,BI.INTELLIGENCE,BI.EXPERIENCE; DIRECT=none; WHY=provide an explicit semantic engine owner for this accepted BusinessIntent partition`.
  - **TEST:** `TestScheduledReportAndDashboardRefreshRemainReproducibleAndNonDisclosing`.
  - **TEST MATRIX:** `PRIMARY=TestScheduledReportAndDashboardRefreshRemainReproducibleAndNonDisclosing`; `PROPERTY=TestTodo_REPORT_003_Property`; `GOLDEN=TestTodo_REPORT_003_Golden`; `RACE=TestTodo_REPORT_003_Race`; `FAULT=TestTodo_REPORT_003_Fault`; `SECURITY=TestTodo_REPORT_003_Security`; `CONFORMANCE=TestTodo_REPORT_003_Conformance`; `MUTATION=TestTodo_REPORT_003_Mutation`.
  - **RED:** schedule runs under creator's expired authority, dashboard refresh mixes versions, delivery leaks attachment/link, or historical rerun uses current definition/data without disclosure.
  - **GREEN:** each run reauthorizes, pins definition/data/control versions, stores evidence and secure delivery intent; historical reproduction states exact/recomputed/unavailable and never fabricates equality.
  - **REFACTOR:** keep shared mechanics in kernel/engines and this package as the sole owner of the stated HCM meaning, lifecycle and correction semantics.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [model coverage](data/models/intent-coverage-matrix.md), [engine ownership](#businessintent-context-required-by-every-todo).

- [x] `TAXPROFILE-001` **[PHASE_4][SOL_HIGH] Define worker tax profile, registration, election and exemption revisions.**
  - **Evidence (2026-09-05):** `TestTaxProfileRejectsUnregisteredJurisdictionAndUnverifiedElection` in `internal/domains/taxprofile` (worker tax profile, registration, election and exemption revisions refusing unregistered jurisdictions and unverified elections; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/taxprofile/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-016`, `INTENT-CONF-001`, `ENGINE-COVERAGE-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL,BI.REGULATORY,BI.PEOPLE; DIRECT=none; WHY=provide an explicit semantic engine owner for this accepted BusinessIntent partition`.
  - **TEST:** `TestTaxProfileRejectsUnregisteredJurisdictionAndUnverifiedElection`.
  - **TEST MATRIX:** `PRIMARY=TestTaxProfileRejectsUnregisteredJurisdictionAndUnverifiedElection`; `PROPERTY=TestTodo_TAXPROFILE_001_Property`; `GOLDEN=TestTodo_TAXPROFILE_001_Golden`; `RACE=TestTodo_TAXPROFILE_001_Race`; `FAULT=TestTodo_TAXPROFILE_001_Fault`; `SECURITY=TestTodo_TAXPROFILE_001_Security`; `CONFORMANCE=TestTodo_TAXPROFILE_001_Conformance`; `MUTATION=TestTodo_TAXPROFILE_001_Mutation`.
  - **RED:** election lacks worker/jurisdiction/form/version/evidence, exemption has no expiry, registration overlaps inconsistently or bank/HR caller lowers tax classification.
  - **GREEN:** immutable profile revisions preserve jurisdiction registration, filing status/elections/exemptions, evidence authority, effective/known intervals and restricted classification.
  - **REFACTOR:** keep shared mechanics in kernel/engines and this package as the sole owner of the stated HCM meaning, lifecycle and correction semantics.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [model coverage](data/models/intent-coverage-matrix.md), [engine ownership](#businessintent-context-required-by-every-todo).

- [x] `TAXPROFILE-002` **[PHASE_4][SOL_HIGH] Validate, effective-date and correct worker tax elections.**
  - **Evidence (2026-09-08):** `TestTaxElectionChangeRequiresEvidenceEffectiveDateAndAppendOnlyCorrection` and independent `TestTodo_TAXPROFILE_002` property/golden/race/fault/security/conformance/mutation matrix; `go test -count=1 -cover ./internal/domains/taxprofile/` PASS (71.3%); `go vet ./internal/domains/taxprofile/` PASS. Sol's full `go test -count=1 -cover ./internal/data/taxprofilestore/` PASS (73.8%); root independently ran `go test -count=1 -run '^TestTodo_TAXPROFILE_002_PostgresSubMicrosecondEffectiveBounds$' ./internal/data/taxprofilestore/` PASS. Migration 00263 and real PostgreSQL tests cover tenant-scoped successor CAS, populated append-only upgrade, exact scale/nanosecond round trips and reversed interval refusal. Closed-payroll inputs produce correction drafts, never payroll rewrites; domain tests pin exact golden bytes and generated boundary/permutation invariants. No tax-rate or payroll-execution authority is inferred.
  - **Depends:** `TAXPROFILE-001`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL,BI.REGULATORY,BI.PEOPLE; DIRECT=none; WHY=provide an explicit semantic engine owner for this accepted BusinessIntent partition`.
  - **TEST:** `TestTaxElectionChangeRequiresEvidenceEffectiveDateAndAppendOnlyCorrection`.
  - **TEST MATRIX:** `PRIMARY=TestTaxElectionChangeRequiresEvidenceEffectiveDateAndAppendOnlyCorrection`; `PROPERTY=TestTodo_TAXPROFILE_002_Property`; `GOLDEN=TestTodo_TAXPROFILE_002_Golden`; `RACE=TestTodo_TAXPROFILE_002_Race`; `FAULT=TestTodo_TAXPROFILE_002_Fault`; `SECURITY=TestTodo_TAXPROFILE_002_Security`; `CONFORMANCE=TestTodo_TAXPROFILE_002_Conformance`; `MUTATION=TestTodo_TAXPROFILE_002_Mutation`.
  - **RED:** missing/expired form is accepted, late election rewrites closed payroll, correction deletes original or same effective date has two winners.
  - **GREEN:** validation returns `VALID|INCOMPLETE|INVALID|EXPIRED|UNKNOWN`; accepted successor is CAS-fenced, closed periods create retro/correction intent and current resolution is deterministic.
  - **REFACTOR:** keep shared mechanics in kernel/engines and this package as the sole owner of the stated HCM meaning, lifecycle and correction semantics.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [model coverage](data/models/intent-coverage-matrix.md), [engine ownership](#businessintent-context-required-by-every-todo).

- [ ] `TAXPROFILE-003` **[CONFORMANCE][SOL_HIGH] Produce version-pinned tax input and prove payroll/tax parity.**
  - **Implementation evidence (2026-09-08; commit gates pending):** Snapshot matrix tests and `TestTodo_TAXPROFILE_003_CrossPackagePayrollBinding` exercise sealed version pins, all 27 presence-state combinations, exact calculation output, frozen membership and rejection of foreign run/population/pay-group substitution. Review removed digest-string authority minting and closed the typed-population transplant path. `go test -count=1 ./internal/domains/taxprofile/ ./internal/domains/payroll/calcpolicy/ ./internal/domains/payroll/` PASS; subsequent independent `go test -count=1 -cover ./internal/domains/payroll/` PASS at 64.2%, with the existing exact payroll coverage exception through 2026-12-31. No tax-rate provider or payroll execution deployment is claimed.
  - **Depends:** `TAXPROFILE-002`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.PAYROLL,BI.REGULATORY,BI.PEOPLE; DIRECT=none; WHY=provide an explicit semantic engine owner for this accepted BusinessIntent partition`.
  - **TEST:** `TestTaxProfileSnapshotFeedsPinnedTaxCalculationAndPayrollRun`.
  - **TEST MATRIX:** `PRIMARY=TestTaxProfileSnapshotFeedsPinnedTaxCalculationAndPayrollRun`; `PROPERTY=TestTodo_TAXPROFILE_003_Property`; `GOLDEN=TestTodo_TAXPROFILE_003_Golden`; `RACE=TestTodo_TAXPROFILE_003_Race`; `FAULT=TestTodo_TAXPROFILE_003_Fault`; `SECURITY=TestTodo_TAXPROFILE_003_Security`; `CONFORMANCE=TestTodo_TAXPROFILE_003_Conformance`; `MUTATION=TestTodo_TAXPROFILE_003_Mutation`.
  - **RED:** tax calculation/payroll use different profile revisions, unknown registration defaults resident, redacted election becomes zero withholding or snapshot changes after payroll freeze.
  - **GREEN:** immutable snapshot binds profile/registration/election/form/rule releases and presence states; Tax and Payroll conformance consume identical digest and block unsafe unknowns.
  - **REFACTOR:** keep shared mechanics in kernel/engines and this package as the sole owner of the stated HCM meaning, lifecycle and correction semantics.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [model coverage](data/models/intent-coverage-matrix.md), [engine ownership](#businessintent-context-required-by-every-todo).

- [x] `PAYINPUT-001` **[PHASE_5][SOL_HIGH] Define earning and deduction definitions plus worker assignments.**
  - **Evidence (2026-09-05):** `TestPayInputDefinitionsRejectUnknownTaxabilityRecurrenceAndOverlappingAssignments` in `internal/domains/payinput` (earning and deduction definitions plus worker assignments refusing unknown taxability, unknown recurrence and overlapping assignments; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/payinput/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-016`, `INTENT-CONF-001`, `ENGINE-COVERAGE-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL,BI.REWARDS; DIRECT=none; WHY=provide an explicit semantic engine owner for this accepted BusinessIntent partition`.
  - **TEST:** `TestPayInputDefinitionsRejectUnknownTaxabilityRecurrenceAndOverlappingAssignments`.
  - **TEST MATRIX:** `PRIMARY=TestPayInputDefinitionsRejectUnknownTaxabilityRecurrenceAndOverlappingAssignments`; `PROPERTY=TestTodo_PAYINPUT_001_Property`; `GOLDEN=TestTodo_PAYINPUT_001_Golden`; `RACE=TestTodo_PAYINPUT_001_Race`; `FAULT=TestTodo_PAYINPUT_001_Fault`; `SECURITY=TestTodo_PAYINPUT_001_Security`; `CONFORMANCE=TestTodo_PAYINPUT_001_Conformance`; `MUTATION=TestTodo_PAYINPUT_001_Mutation`.
  - **RED:** earning/deduction lacks code/type/currency/taxability/accounting/owner, recurrence has no period rule or overlapping worker assignments resolve silently.
  - **GREEN:** immutable definitions and assignments bind calculation/tax/accounting semantics, amount/rate/formula, limits, recurrence and effective/known intervals; conflicts reject with zero payroll input.
  - **REFACTOR:** keep shared mechanics in kernel/engines and this package as the sole owner of the stated HCM meaning, lifecycle and correction semantics.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [model coverage](data/models/intent-coverage-matrix.md), [engine ownership](#businessintent-context-required-by-every-todo).

- [ ] `PAYINPUT-002` **[PHASE_5][SOL_HIGH] Calculate recurring and one-time payroll inputs with limits and arrears.**
  - **Depends:** `PAYINPUT-001`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL,BI.REWARDS; DIRECT=none; WHY=provide an explicit semantic engine owner for this accepted BusinessIntent partition`.
  - **TEST:** `TestPayInputCalculationAppliesTaxabilityLimitsArrearsAndExactDecimalRules`.
  - **TEST MATRIX:** `PRIMARY=TestPayInputCalculationAppliesTaxabilityLimitsArrearsAndExactDecimalRules`; `PROPERTY=TestTodo_PAYINPUT_002_Property`; `GOLDEN=TestTodo_PAYINPUT_002_Golden`; `RACE=TestTodo_PAYINPUT_002_Race`; `FAULT=TestTodo_PAYINPUT_002_Fault`; `SECURITY=TestTodo_PAYINPUT_002_Security`; `CONFORMANCE=TestTodo_PAYINPUT_002_Conformance`; `MUTATION=TestTodo_PAYINPUT_002_Mutation`.
  - **RED:** float arithmetic, pre/post-tax ambiguity, cap/floor order drift, one-time duplicate, deduction below protected floor or arrears disappear.
  - **GREEN:** pinned definitions/assignments/period produce exact typed earning/deduction lines, taxable treatment, applied/deferred amount, remaining limit/arrears and trace; replay digest is stable.
  - **REFACTOR:** keep shared mechanics in kernel/engines and this package as the sole owner of the stated HCM meaning, lifecycle and correction semantics.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [model coverage](data/models/intent-coverage-matrix.md), [engine ownership](#businessintent-context-required-by-every-todo).

- [ ] `PAYINPUT-003` **[CONFORMANCE][SOL_HIGH] Correct assignments and freeze a complete payroll-input snapshot.**
  - **Depends:** `PAYINPUT-002`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.PAYROLL,BI.REWARDS; DIRECT=none; WHY=provide an explicit semantic engine owner for this accepted BusinessIntent partition`.
  - **TEST:** `TestPayrollInputCorrectionPreservesClosedRunsAndFreezesCompleteManifest`.
  - **TEST MATRIX:** `PRIMARY=TestPayrollInputCorrectionPreservesClosedRunsAndFreezesCompleteManifest`; `PROPERTY=TestTodo_PAYINPUT_003_Property`; `GOLDEN=TestTodo_PAYINPUT_003_Golden`; `RACE=TestTodo_PAYINPUT_003_Race`; `FAULT=TestTodo_PAYINPUT_003_Fault`; `SECURITY=TestTodo_PAYINPUT_003_Security`; `CONFORMANCE=TestTodo_PAYINPUT_003_Conformance`; `MUTATION=TestTodo_PAYINPUT_003_Mutation`.
  - **RED:** retro assignment mutates finalized run, correction omits dependent periods, snapshot misses time/benefit/tax/garnishment source or changes after cutoff.
  - **GREEN:** append-only correction emits affected-run plan; immutable snapshot records every input revision/presence/watermark and finalized runs require explicit payroll correction, never in-place rewrite.
  - **REFACTOR:** keep shared mechanics in kernel/engines and this package as the sole owner of the stated HCM meaning, lifecycle and correction semantics.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [model coverage](data/models/intent-coverage-matrix.md), [engine ownership](#businessintent-context-required-by-every-todo).

- [x] `PAYMETHOD-001` **[PHASE_4][SOL_HIGH] Define classified payment destinations, verification and split priority.**
  - **Evidence (2026-09-05):** `TestPaymentDestinationModelRejectsPlaintextUnverifiedAndInvalidSplit` in `internal/domains/paymethod` (classified payment destinations with verification and split priority; plaintext, unverified and invalid-split destinations refused; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/paymethod/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-016`, `INTENT-CONF-001`, `ENGINE-COVERAGE-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL,BI.PEOPLE,BI.SECURITY; DIRECT=none; WHY=provide an explicit semantic engine owner for this accepted BusinessIntent partition`.
  - **TEST:** `TestPaymentDestinationModelRejectsPlaintextUnverifiedAndInvalidSplit`.
  - **TEST MATRIX:** `PRIMARY=TestPaymentDestinationModelRejectsPlaintextUnverifiedAndInvalidSplit`; `PROPERTY=TestTodo_PAYMETHOD_001_Property`; `GOLDEN=TestTodo_PAYMETHOD_001_Golden`; `RACE=TestTodo_PAYMETHOD_001_Race`; `FAULT=TestTodo_PAYMETHOD_001_Fault`; `SECURITY=TestTodo_PAYMETHOD_001_Security`; `CONFORMANCE=TestTodo_PAYMETHOD_001_Conformance`; `MUTATION=TestTodo_PAYMETHOD_001_Mutation`.
  - **RED:** raw bank data enters ordinary worker projection/log, destination lacks token/provider/owner/verification, split exceeds 100 percent or priority cycles.
  - **GREEN:** restricted destination revision stores governed token/reference, masked display, verification state, currency/country, exact split/priority and effective interval; invalid input emits no destination event.
  - **REFACTOR:** keep shared mechanics in kernel/engines and this package as the sole owner of the stated HCM meaning, lifecycle and correction semantics.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [model coverage](data/models/intent-coverage-matrix.md), [engine ownership](#businessintent-context-required-by-every-todo).

- [x] `PAYMETHOD-002` **[PHASE_4][SOL_HIGH] Govern direct-deposit changes with step-up, fraud and cooling controls.**
  - **Evidence (2026-09-05):** `TestPayMethodChangeRequiresStepUpRiskDecisionAndCurrentProposal` in `internal/domains/paymethod` (change binds verified destination, principal/session assurance, risk/fraud decision, proposal/approval and activation policy; unsafe cases return STEP_UP_REQUIRED|REVIEW_REQUIRED|REPLAN_REQUIRED|BLOCKED with zero activation; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/paymethod/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PAYMETHOD-001`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.PAYROLL,BI.PEOPLE,BI.SECURITY; DIRECT=none; WHY=provide an explicit semantic engine owner for this accepted BusinessIntent partition`.
  - **TEST:** `TestPayMethodChangeRequiresStepUpRiskDecisionAndCurrentProposal`.
  - **TEST MATRIX:** `PRIMARY=TestPayMethodChangeRequiresStepUpRiskDecisionAndCurrentProposal`; `PROPERTY=TestTodo_PAYMETHOD_002_Property`; `GOLDEN=TestTodo_PAYMETHOD_002_Golden`; `RACE=TestTodo_PAYMETHOD_002_Race`; `FAULT=TestTodo_PAYMETHOD_002_Fault`; `SECURITY=TestTodo_PAYMETHOD_002_Security`; `CONFORMANCE=TestTodo_PAYMETHOD_002_Conformance`; `MUTATION=TestTodo_PAYMETHOD_002_Mutation`.
  - **RED:** caller-selected account, weak session, anomalous bulk change, stale verification, self-approval or change inside protected payroll window commits.
  - **GREEN:** change binds verified destination, principal/session assurance, risk/fraud decision, proposal/approval and activation policy; unsafe cases return `STEP_UP_REQUIRED|REVIEW_REQUIRED|REPLAN_REQUIRED|BLOCKED` with zero activation.
  - **REFACTOR:** keep shared mechanics in kernel/engines and this package as the sole owner of the stated HCM meaning, lifecycle and correction semantics.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [model coverage](data/models/intent-coverage-matrix.md), [engine ownership](#businessintent-context-required-by-every-todo).

- [ ] `PAYMETHOD-003` **[CONFORMANCE][SOL_HIGH] Observe prenote and reconcile payroll settlement destinations.**
  - **Depends:** `PAYMETHOD-002`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.PAYROLL,BI.PEOPLE,BI.SECURITY; DIRECT=none; WHY=provide an explicit semantic engine owner for this accepted BusinessIntent partition`.
  - **TEST:** `TestPayMethodReconciliationDistinguishesVerificationAcceptanceAndSettlement`.
  - **TEST MATRIX:** `PRIMARY=TestPayMethodReconciliationDistinguishesVerificationAcceptanceAndSettlement`; `PROPERTY=TestTodo_PAYMETHOD_003_Property`; `GOLDEN=TestTodo_PAYMETHOD_003_Golden`; `RACE=TestTodo_PAYMETHOD_003_Race`; `FAULT=TestTodo_PAYMETHOD_003_Fault`; `SECURITY=TestTodo_PAYMETHOD_003_Security`; `CONFORMANCE=TestTodo_PAYMETHOD_003_Conformance`; `MUTATION=TestTodo_PAYMETHOD_003_Mutation`.
  - **RED:** prenote submission counts verified, provider timeout triggers duplicate, payroll uses superseded destination, returned payment marks worker paid or mismatch overwrites intent.
  - **GREEN:** observations remain external and fresh; expected/used/settled destination comparison returns exact state, quarantines ambiguity and creates separate repair/payment-return intent without rewriting payroll.
  - **REFACTOR:** keep shared mechanics in kernel/engines and this package as the sole owner of the stated HCM meaning, lifecycle and correction semantics.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [model coverage](data/models/intent-coverage-matrix.md), [engine ownership](#businessintent-context-required-by-every-todo).

- [x] `JOBARCH-001` **[PHASE_3][SOL_HIGH] Define job profiles, families, levels, grades and immutable lifecycle.**
  - **Evidence (2026-09-05):** `TestJobArchitectureGraphRejectsCyclesMissingLevelsAndInPlaceMeaningChange` in `internal/domains/jobarch` (job profiles, families, levels and grades as an immutable graph refusing cycles, missing levels and in-place meaning change, with pay-band compatibility and retirement protection; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/jobarch/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-016`, `INTENT-CONF-001`, `ENGINE-COVERAGE-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE,BI.REWARDS,BI.TALENT,BI.RECRUITING; DIRECT=none; WHY=provide an explicit semantic engine owner for this accepted BusinessIntent partition`.
  - **TEST:** `TestJobArchitectureGraphRejectsCyclesMissingLevelsAndInPlaceMeaningChange`.
  - **TEST MATRIX:** `PRIMARY=TestJobArchitectureGraphRejectsCyclesMissingLevelsAndInPlaceMeaningChange`; `PROPERTY=TestTodo_JOBARCH_001_Property`; `GOLDEN=TestTodo_JOBARCH_001_Golden`; `RACE=TestTodo_JOBARCH_001_Race`; `FAULT=TestTodo_JOBARCH_001_Fault`; `SECURITY=TestTodo_JOBARCH_001_Security`; `CONFORMANCE=TestTodo_JOBARCH_001_Conformance`; `MUTATION=TestTodo_JOBARCH_001_Mutation`.
  - **RED:** job has no family/level, family hierarchy cycles, grade/career level conflict, published profile mutates or retired job accepts new position without policy.
  - **GREEN:** versioned graph enforces stable IDs, hierarchy DAG, lifecycle `DRAFT|PUBLISHED|RETIRED|SUPERSEDED`, effective/known time and compatibility diagnostics.
  - **REFACTOR:** keep shared mechanics in kernel/engines and this package as the sole owner of the stated HCM meaning, lifecycle and correction semantics.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [model coverage](data/models/intent-coverage-matrix.md), [engine ownership](#businessintent-context-required-by-every-todo).

- [x] `JOBARCH-002` **[PHASE_3][SOL_HIGH] Assign classifications, skills, credentials and compensation relationships.**
  - **Evidence (2026-09-05):** `TestJobProfileRequirementsBindClassificationQualificationAndRewardReferences` in `internal/domains/jobarch` (job revision references versioned classification, Qualification requirements, skill/credential and grade/band definitions with authority and effective interval; unresolved reference blocks publication; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/jobarch/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `JOBARCH-001`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE,BI.REWARDS,BI.TALENT,BI.RECRUITING; DIRECT=none; WHY=provide an explicit semantic engine owner for this accepted BusinessIntent partition`.
  - **TEST:** `TestJobProfileRequirementsBindClassificationQualificationAndRewardReferences`.
  - **TEST MATRIX:** `PRIMARY=TestJobProfileRequirementsBindClassificationQualificationAndRewardReferences`; `PROPERTY=TestTodo_JOBARCH_002_Property`; `GOLDEN=TestTodo_JOBARCH_002_Golden`; `RACE=TestTodo_JOBARCH_002_Race`; `FAULT=TestTodo_JOBARCH_002_Fault`; `SECURITY=TestTodo_JOBARCH_002_Security`; `CONFORMANCE=TestTodo_JOBARCH_002_Conformance`; `MUTATION=TestTodo_JOBARCH_002_Mutation`.
  - **RED:** exemption/classification is free text, required skill/license lacks proficiency/validity, pay grade currency mismatches or generic Job engine owns skill/compensation truth.
  - **GREEN:** job revision references versioned classification, Qualification requirements, skill/credential and grade/band definitions with authority and effective interval; unresolved reference blocks publication.
  - **REFACTOR:** keep shared mechanics in kernel/engines and this package as the sole owner of the stated HCM meaning, lifecycle and correction semantics.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [model coverage](data/models/intent-coverage-matrix.md), [engine ownership](#businessintent-context-required-by-every-todo).

- [x] `JOBARCH-003` **[CONFORMANCE][SOL_HIGH] Publish, retire and impact-analyze job revisions.**
  - **Evidence (2026-09-05):** `TestJobRevisionPublicationAndRetirementIdentifyAffectedPositionsWorkersAndIntents` in `internal/domains/jobarch` (publication binds configuration approval and compatibility report; frozen impact set emits scoped review/migration intents while existing assignments retain historical job revision until governed change; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/domains/jobarch/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `JOBARCH-002`, `GOVERN-002`, `LEDGER-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.WORKFORCE,BI.REWARDS,BI.TALENT,BI.RECRUITING; DIRECT=none; WHY=provide an explicit semantic engine owner for this accepted BusinessIntent partition`.
  - **TEST:** `TestJobRevisionPublicationAndRetirementIdentifyAffectedPositionsWorkersAndIntents`.
  - **TEST MATRIX:** `PRIMARY=TestJobRevisionPublicationAndRetirementIdentifyAffectedPositionsWorkersAndIntents`; `PROPERTY=TestTodo_JOBARCH_003_Property`; `GOLDEN=TestTodo_JOBARCH_003_Golden`; `RACE=TestTodo_JOBARCH_003_Race`; `FAULT=TestTodo_JOBARCH_003_Fault`; `SECURITY=TestTodo_JOBARCH_003_Security`; `CONFORMANCE=TestTodo_JOBARCH_003_Conformance`; `MUTATION=TestTodo_JOBARCH_003_Mutation`.
  - **RED:** breaking revision activates without approval, retirement or supersession strands positions/requisitions/workers, impact omits compensation/qualification/access/talent dependencies or silently updates them.
  - **GREEN:** publication binds configuration approval and compatibility report; frozen impact set emits scoped review/migration intents while existing assignments retain historical job revision until governed change.
  - **REFACTOR:** keep shared mechanics in kernel/engines and this package as the sole owner of the stated HCM meaning, lifecycle and correction semantics.
  - **Refs:** [BusinessIntent partitions](specs/business-intent-catalog.md#vocabulary-list-non-normative), [model coverage](data/models/intent-coverage-matrix.md), [engine ownership](#businessintent-context-required-by-every-todo).

- [x] `JOBARCH-004` **[PHASE_3][SOL_HIGH] Define immutable career-ladder promotion paths and exact reward guardrails.**
  - **Evidence (2026-09-06):** `TestPromotionPathValidatesAgainstRankedArchitecture` with `PromotionPathRevision`, `ProfileRevisionRef`, `ValidateAgainst`, `AllowsBaseIncrease` and promotion-path tests in `internal/domains/jobarch` define versioned source/target profile revisions, upward/lateral/cross-family graph rules, effective/known time, exact `values.Percentage` base-increase bounds, compensation-policy and benefit-eligibility rule references, and a deterministic digest with no float arithmetic or direct benefit-election mutation. `internal/domains/fixtures/testdata/job-architecture.json` publishes the prototype's People Operations, Engineering and Clinical families, ranked profiles and two governed ladder edges. `WorkforceOptions.promotion_paths` exposes only exact next-role tuples and their pay/benefit rule references; the promotion client filters its role selector to those edges and the server independently refuses an unrelated role or an increase outside the exact bound. A live Codex-browser run exercised OPS-HRBP2/P2 → OPS-HRBP3/P3 at +5.4%, then completed its durable approvals, effective-date WAIT and single governed ledger write. `go test ./internal/domains/jobarch ./internal/domains/fixtures ./internal/intent/app ./internal/transport/journey ./tools/uxqual/journeyclient` and `npm run check:driftgate` PASS on Windows (Go 1.26.3).
  - **Depends:** `JOBARCH-001`, `JOBARCH-002`, `COMP-003`, `GOVERN-002`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE,BI.REWARDS,BI.TALENT; DIRECT=none; WHY=make promotion targets governed architecture edges instead of arbitrary job-code and grade text`.
  - **TEST:** `TestPromotionPathValidatesAgainstRankedArchitecture`.
  - **TEST MATRIX:** `PRIMARY=TestPromotionPathValidatesAgainstRankedArchitecture`; `PROPERTY=TestTodo_JOBARCH_004_Property`; `GOLDEN=TestTodo_JOBARCH_004_Golden`; `SECURITY=TestTodo_JOBARCH_004_Security`; `MUTATION=TestTodo_JOBARCH_004_Mutation`.
  - **RED:** a promotion path targets an unknown or mutable profile revision, calls a lower/equal rank an upward move, crosses families without an explicit exception, uses floating-point salary rules, or directly changes benefit elections.
  - **GREEN:** one immutable path pins source and target profiles, validates its ladder shape, carries exact minimum/maximum base-increase guardrails, references versioned compensation and benefit-eligibility rules, and produces a deterministic digest.
  - **REFACTOR:** promotion approval thresholds remain owned by the versioned promotion rule pack; benefits reevaluate eligibility through `BEN-003` and never become job-architecture-owned election state.
  - **Refs:** [job architecture](specs/job-architecture.md), [compensation](specs/compensation.md), [Phase 1 depth matrix](plan.md#phase-1-implementation-depth-matrix).

- [ ] `PERSIST-JOBARCH-002` **[PHASE_3][SOL_HIGH] Persist promotion paths and pin assignment job-profile revisions.**
  - **Depends:** `JOBARCH-004`, `PERSIST-JOBARCH-001`, `STORE-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.WORKFORCE,BI.REWARDS; DIRECT=none; WHY=make a path and both assignment profiles durable evidence for proposal, simulation and terminal commit`.
  - **TEST:** `TestPromotionPathStorePinsProfileRevisionsAndRefusesOverlappingPublishedEdges`.
  - **TEST MATRIX:** `PRIMARY=TestPromotionPathStorePinsProfileRevisionsAndRefusesOverlappingPublishedEdges`; `PROPERTY=TestTodo_PERSIST_JOBARCH_002_Property`; `GOLDEN=TestTodo_PERSIST_JOBARCH_002_Golden`; `INTEGRATION=TestTodo_PERSIST_JOBARCH_002_Integration`; `FAULT=TestTodo_PERSIST_JOBARCH_002_Fault`; `SECURITY=TestTodo_PERSIST_JOBARCH_002_Security`; `MUTATION=TestTodo_PERSIST_JOBARCH_002_Mutation`.
  - **RED:** assignment stores only free-text job code/grade, a path is edited in place, overlapping published edges are ambiguous, or a proposal can change which profile revision it means between simulation and commit.
  - **GREEN:** tenant-scoped append-only path rows carry effective/known coordinates and RLS; assignment and proposal snapshots pin source/target job-profile revisions while denormalized job code/grade are checked projections.
  - **REFACTOR:** keep path and pin persistence in one tenant-scoped store package under internal/data with RLS and append-only rows; the domain package stays the sole owner of path meaning and validation.
  - **Refs:** [job architecture](specs/job-architecture.md), [storage disposition](../definitions/storage/storage-disposition.yaml), [Phase 1 depth matrix](plan.md#phase-1-implementation-depth-matrix).

- [ ] `UX-JOBARCH-001` **[PHASE_4][TERRA] Add an authorized Job Architecture admin workspace.**
  - **Depends:** `PERSIST-JOBARCH-002`, `ALIGN-022`, `I18N-002`, `A11Y-001`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.WORKFORCE,BI.REWARDS,BI.TALENT; DIRECT=none; WHY=let authorized customers manage job codes, families, levels, grades, ladders and rule references without bypassing publication governance`.
  - **TEST:** `TestJobArchitectureWorkspaceShowsLadderRulesAndPublishesOnlyThroughWorkflow`.
  - **TEST MATRIX:** `PRIMARY=TestJobArchitectureWorkspaceShowsLadderRulesAndPublishesOnlyThroughWorkflow`; `GOLDEN=TestTodo_UX_JOBARCH_001_Golden`; `SECURITY=TestTodo_UX_JOBARCH_001_Security`; `CONFORMANCE=TestTodo_UX_JOBARCH_001_Conformance`.
  - **RED:** admin UI edits published architecture in place, offers unsupported job/grade combinations, exposes salary or benefit rules without authority, or saves browser-only configuration.
  - **GREEN:** responsive reusable components show the family/level/profile graph, exact pay-band and increase rules, benefit impacts and publish status; all writes use server-side governed workflows and role/page/action authorization.
  - **REFACTOR:** the workspace renders only authorized, server-resolved architecture state through the journey transport; every publish goes through the governed workflow, never a direct store write.
  - **Refs:** [job architecture](specs/job-architecture.md), [product UI](specs/product-ui.md), [Phase 1 depth matrix](plan.md#phase-1-implementation-depth-matrix).

## 58. BusinessIntent-to-workflow design convergence

> **Disposition (2026-09-02):** DEFERRED.

- [ ] `WF-DISC-005` **[P0][SOL_HIGH] Define the machine-readable WorkflowDesignRecord contract.**
  - **Depends:** `WF-DISC-001`, `INTENT-CONF-001`, `MODEL-009`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=require every accepted BusinessIntent to have an explicit executable high-level design or an honest direct-capability disposition`.
  - **TEST:** `TestWorkflowDesignRecordRejectsImplicitExecutionResponsibilities`.
  - **TEST MATRIX:** `PRIMARY=TestWorkflowDesignRecordRejectsImplicitExecutionResponsibilities`; `PROPERTY=TestTodo_WF_DISC_005_Property`; `GOLDEN=TestTodo_WF_DISC_005_Golden`; `SECURITY=TestTodo_WF_DISC_005_Security`; `CONFORMANCE=TestTodo_WF_DISC_005_Conformance`; `MUTATION=TestTodo_WF_DISC_005_Mutation`.
  - **RED:** a record validates while missing execution disposition, archetype, domain profile, trusted input boundary, snapshot policy, engines/capabilities, human work, writes/effects, waits/signals, invalidators, reconciliation, correction/repair or multidimensional completion.
  - **GREEN:** the typed SchemaFlux contract requires every field, accepts explicit `NOT_APPLICABLE` only with a reason code, emits stable Go/Protobuf registries and produces the same canonical digest across repeated compilation.
  - **REFACTOR:** generate Markdown tables and indexes from the contract; do not maintain a second handwritten runtime registry.
  - **Refs:** [workflow design contract](workflows/business-intent-workflow-registry.md#complete-high-level-design-contract), [BusinessIntent required fields](specs/business-intent-catalog.md#required-definition-fields), [workflow context](workflows/_engine/workflow-context-contract.md).

- [ ] `WF-DISC-006` **[DESIGN][SOL_HIGH] Join each drafted definition to exactly one workflow design disposition.**
  - **Depends:** `MODEL-010`, `WF-DISC-005`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.PEOPLE,BI.REWARDS,BI.WORK,BI.INTELLIGENCE,BI.OPERATIONS; DIRECT=none; WHY=give each of the fourteen drafted definitions one workflow-design or direct-capability disposition`.
  - **TEST:** `TestAcceptedIntentWorkflowDesignJoinIsExactAndTotal`.
  - **TEST MATRIX:** `PRIMARY=TestAcceptedIntentWorkflowDesignJoinIsExactAndTotal`; `PROPERTY=TestTodo_WF_DISC_006_Property`; `GOLDEN=TestTodo_WF_DISC_006_Golden`; `SECURITY=TestTodo_WF_DISC_006_Security`; `CONFORMANCE=TestTodo_WF_DISC_006_Conformance`; `MUTATION=TestTodo_WF_DISC_006_Mutation`.
  - **RED:** one of the fourteen definitions has zero or multiple design records, or a presentation alias creates another design.
  - **GREEN:** a one-to-one join over the fourteen definitions returns `14/14` with zero missing, duplicate or alias-only records; there is no other denominator.
  - **REFACTOR:** use stable semantic identity and version; display names must never be join keys.
  - **Refs:** [source-manifest convergence](workflows/business-intent-workflow-registry.md#source-manifest-convergence), [catalog identity](specs/business-intent-catalog.md#catalog-identity), [catalog ingestion](#businessintent-context-required-by-every-todo).

- [ ] `WF-DISC-007` **[P0][SOL_HIGH] Compile archetype plus domain profile plus intent delta into a complete high-level graph.**
  - **Depends:** `WF-DISC-005`, `WF-DISC-006`, `WF-COMP-001`, `WF-COMP-002`.
  - **INTENT CONTEXT:** `ROLE=ORCHESTRATION; SETS=BI.ALL; DIRECT=none; WHY=make compact per-intent workflow designs expand into reviewable complete execution responsibilities rather than vague labels`.
  - **TEST:** `TestWorkflowDesignExpansionProducesCompleteOrderedResponsibilities`.
  - **TEST MATRIX:** `PRIMARY=TestWorkflowDesignExpansionProducesCompleteOrderedResponsibilities`; `PROPERTY=TestTodo_WF_DISC_007_Property`; `GOLDEN=TestTodo_WF_DISC_007_Golden`; `FAULT=TestTodo_WF_DISC_007_Fault`; `SECURITY=TestTodo_WF_DISC_007_Security`; `CONFORMANCE=TestTodo_WF_DISC_007_Conformance`; `MUTATION=TestTodo_WF_DISC_007_Mutation`.
  - **RED:** expansion loses an archetype phase, lets a delta replace a mandatory governance/revalidation/reconciliation responsibility, leaves an external effect unordered or produces a graph with an unreachable closure/repair branch.
  - **GREEN:** compiler deterministically expands the recipe/profile/delta, reports every inherited/added/justifiably omitted responsibility, emits typed nodes/edges/effects/invalidators/completion policy and produces a stable graph digest.
  - **REFACTOR:** keep archetype expansion separate from production workflow compilation so exploratory designs cannot be published accidentally.
  - **Refs:** [workflow archetypes](workflows/_shared/workflow-archetypes.md), [domain profiles](workflows/business-intent-workflow-registry.md#domain-profiles), [workflow compiler](specs/workflow-runtime.md).

- [ ] `WF-DISC-008` **[P0][SOL_HIGH] Prove direct-capability dispositions do not invent fake durable workflows or bypass governance.**
  - **Depends:** `WF-DISC-007`, `CAP-002`, `INTENT-013`, `WF-RUN-012`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ANALYTICS,BI.INTELLIGENCE,BI.REGULATORY,BI.ALL; DIRECT=none; WHY=keep pure reads and calculations simple while preserving authorization, evidence and separate action authority`.
  - **TEST:** `TestDirectCapabilityDispositionIsPureGovernedAndPromotesActionsAsNewIntents`.
  - **TEST MATRIX:** `PRIMARY=TestDirectCapabilityDispositionIsPureGovernedAndPromotesActionsAsNewIntents`; `PROPERTY=TestTodo_WF_DISC_008_Property`; `GOLDEN=TestTodo_WF_DISC_008_Golden`; `FAULT=TestTodo_WF_DISC_008_Fault`; `SECURITY=TestTodo_WF_DISC_008_Security`; `CONFORMANCE=TestTodo_WF_DISC_008_Conformance`; `MUTATION=TestTodo_WF_DISC_008_Mutation`.
  - **RED:** a `DIRECT_CAPABILITY` creates domain/outbox/work/timer rows, skips purpose/field/population authorization, returns incomplete data as complete, or an analytical recommendation mutates state without a separately governed BusinessIntent.
  - **GREEN:** direct execution pins inputs/rules/watermarks, applies governance/DLP, returns typed completeness/uncertainty/trace, records evidence and emits any proposed action only as a new unexecuted intent.
  - **REFACTOR:** share the same capability gateway and governance contracts with workflow steps; only durability mechanics differ.
  - **Refs:** [D1 governed direct capability](workflows/_shared/workflow-archetypes.md#d1-governed-direct-capability-no-durable-workflow), [intent boundaries](specs/business-intent-catalog.md#intent-capability-workflow-and-transaction-boundaries), [analysis-to-action](#businessintent-context-required-by-every-todo).

- [ ] `WF-DISC-009` **[P0][SOL_HIGH] Validate every workflow design against model, engine, capability and authority ownership.**
  - **Depends:** `WF-DISC-007`, `ENGINE-COVERAGE-001`, `DB-COVERAGE-001`, `CAP-002`, `MODEL-021`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=prevent a designed workflow from hiding undefined data, reusable computation, semantic operations or source authority`.
  - **TEST:** `TestWorkflowDesignReferencesResolveToSingleSemanticOwners`.
  - **TEST MATRIX:** `PRIMARY=TestWorkflowDesignReferencesResolveToSingleSemanticOwners`; `PROPERTY=TestTodo_WF_DISC_009_Property`; `GOLDEN=TestTodo_WF_DISC_009_Golden`; `SECURITY=TestTodo_WF_DISC_009_Security`; `CONFORMANCE=TestTodo_WF_DISC_009_Conformance`; `MUTATION=TestTodo_WF_DISC_009_Mutation`.
  - **RED:** a design reads/writes an unowned entity/property, embeds reusable calculation in workflow, invokes an unregistered capability, assumes a physical system owner, or calls domain persistence/provider adapters directly.
  - **GREEN:** every data/engine/capability/authority reference resolves to one versioned semantic owner; missing contracts emit atomic todo candidates and block `CONTRACTED` maturity.
  - **REFACTOR:** generate reverse indexes from owner to consuming intents and fail dependency cycles instead of duplicating ownership prose.
  - **Refs:** [domain profiles](workflows/business-intent-workflow-registry.md#domain-profiles), [model coverage](data/models/intent-coverage-matrix.md), [platform responsibility boundaries](specs/platform-responsibility-boundaries.md).

- [ ] `WF-DISC-010` **[P0][SOL_HIGH] Generate adversarial scenario matrices from every workflow design.**
  - **Depends:** `WF-DISC-007`, `WF-DISC-009`, `GOV-018`, `GOV-028`, `TOOL-014`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=turn each high-level BusinessIntent design into exact positive, negative, temporal, concurrency, failure, security and repair obligations`.
  - **TEST:** `TestWorkflowDesignScenarioGeneratorCoversEveryDeclaredBoundary`.
  - **TEST MATRIX:** `PRIMARY=TestWorkflowDesignScenarioGeneratorCoversEveryDeclaredBoundary`; `PROPERTY=TestTodo_WF_DISC_010_Property`; `GOLDEN=TestTodo_WF_DISC_010_Golden`; `RACE=TestTodo_WF_DISC_010_Race`; `FAULT=TestTodo_WF_DISC_010_Fault`; `SECURITY=TestTodo_WF_DISC_010_Security`; `CONFORMANCE=TestTodo_WF_DISC_010_Conformance`; `MUTATION=TestTodo_WF_DISC_010_Mutation`.
  - **RED:** generated scenarios omit stale approval, authority drift, partial snapshot, conflicting future change, duplicate signal/effect, worker crash, provider ambiguity, confidential-field leak, cancellation boundary, correction, partial external success or failed repair.
  - **GREEN:** each declared read/write/effect/wait/human decision/compartment/invalidator/completion dimension produces exact fixtures and prohibited/expected states; inapplicable classes require typed justification.
  - **REFACTOR:** deduplicate mechanics through reusable scenario templates while keeping domain-specific semantic oracles explicit.
  - **Refs:** [completion policy](workflows/business-intent-workflow-registry.md#completion-policy-common-to-all-mappings), [workflow context edge cases](workflows/samples/workflow-context-edge-cases.md), [test doctrine](#test-doctrine).

- [ ] `WF-DISC-011` **[P0][SOL_HIGH] Prove parent, child, batch and trigger workflow designs preserve independent intent truth.**
  - **Depends:** `WF-DISC-007`, `INTENT-015`, `INTENT-016`, `INTENT-019`, `INTENT-018`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL,BI.TRIGGERS; DIRECT=none; WHY=ensure composite workflows converge without flattening child authorization, idempotency, failure, evidence or completion`.
  - **TEST:** `TestWorkflowDesignCompositionNeverCollapsesChildOrTriggerOutcome`.
  - **TEST MATRIX:** `PRIMARY=TestWorkflowDesignCompositionNeverCollapsesChildOrTriggerOutcome`; `PROPERTY=TestTodo_WF_DISC_011_Property`; `GOLDEN=TestTodo_WF_DISC_011_Golden`; `RACE=TestTodo_WF_DISC_011_Race`; `FAULT=TestTodo_WF_DISC_011_Fault`; `SECURITY=TestTodo_WF_DISC_011_Security`; `CONFORMANCE=TestTodo_WF_DISC_011_Conformance`; `MUTATION=TestTodo_WF_DISC_011_Mutation`.
  - **RED:** parent completion overwrites a failed child, batch retry reruns successful items, trigger retry duplicates target intent, child inherits broader authority/classification, or cancellation silently crosses an irreversible child boundary.
  - **GREEN:** composition graph binds typed relationship/policy, each child/firing retains lifecycle/evidence/idempotency/governance, aggregate status is derived without loss and repair targets only affected nodes.
  - **REFACTOR:** reuse IntentRelationship and IntentCompositionPlan; workflows must not invent a parallel parent-child model.
  - **Refs:** [composition rules](specs/business-intent-catalog.md#composition-rules), [system trigger designs](workflows/business-intent-workflow-registry.md#system-and-trigger-driven-intents-trigger), [higher-order workflows](workflows/business-intent-workflow-registry.md#higher-order-businessintent-workflows).

- [ ] `WF-DISC-012` **[P0][SOL_HIGH] Gate workflow contraction and implementation on complete design evidence.**
  - **Depends:** `WF-DISC-004`, `WF-DISC-006`, `WF-DISC-007`, `WF-DISC-008`, `WF-DISC-009`, `WF-DISC-010`, `WF-DISC-011`, `GOV-003`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=make workflow research feed production todos and prevent shallow catalog names from masquerading as contracted or implemented behavior`.
  - **TEST:** `TestWorkflowMaturityRequiresDesignCoverageCompilationScenariosAndOwnedTodos`.
  - **TEST MATRIX:** `PRIMARY=TestWorkflowMaturityRequiresDesignCoverageCompilationScenariosAndOwnedTodos`; `PROPERTY=TestTodo_WF_DISC_012_Property`; `GOLDEN=TestTodo_WF_DISC_012_Golden`; `SECURITY=TestTodo_WF_DISC_012_Security`; `CONFORMANCE=TestTodo_WF_DISC_012_Conformance`; `MUTATION=TestTodo_WF_DISC_012_Mutation`.
  - **RED:** an intent advances to `CONTRACTED`, `IMPLEMENTED` or `VERIFIED` with an unbound design, unresolved reference, unjustified omission, missing adversarial scenario, dangling todo/test/evidence edge or only a catalog name/archetype label.
  - **GREEN:** maturity gate requires exact manifest join, expanded graph digest, resolved owners/contracts, scenario suite, atomic todo/test traceability and current evidence; report names every blocker and never inflates aggregate coverage.
  - **REFACTOR:** make the delivery manifest consume generated coverage evidence rather than manually maintained status prose.
  - **Refs:** [workflow design registry](workflows/business-intent-workflow-registry.md), [workflow discovery states](workflows/README.md), [traceability](#backlog-governance-and-traceability).

---

## 59. Adversarial infrastructure edges and third-party tool qualification

> **Disposition (2026-09-02):** P1A only for the `LIB-*` items M2 imports; the rest are qualified when first needed.

This section closes infrastructure-mechanics gaps found after the intent and
workflow convergence pass. It does not add HCM semantics to any dependency.
Candidates remain unapproved until their qualification todo passes; rejection
is an acceptable result.

- [x] `LIB-016` **[P0][TERRA] Evaluate Rapid as a test-only property and state-machine generator.**
  - **Depends:** `LIB-001`, `TOOL-013`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=stress owned value, lifecycle and workflow invariants with generated state sequences without adding a production dependency`.
  - **TEST:** `TestRapidQualificationShrinksOwnedStateMachineFailure`.
  - **TEST MATRIX:** `PRIMARY=TestRapidQualificationShrinksOwnedStateMachineFailure`; `PROPERTY=TestTodo_LIB_016_Property`; `GOLDEN=TestTodo_LIB_016_Golden`; `FUZZ=FuzzTodo_LIB_016`; `CONFORMANCE=TestTodo_LIB_016_Conformance`.
  - **RED:** an injected balance underflow, illegal workflow transition and temporal-overlap defect cannot be found reproducibly, does not shrink to a stable minimal sequence, or requires Rapid in the production module graph.
  - **GREEN:** a pinned test-only Rapid adapter finds and shrinks every seeded defect, persists deterministic regression seeds, records license/maintenance/replacement evidence and leaves release/runtime dependency graphs unchanged; otherwise the decision is `REJECT` with standard-library alternative.
  - **REFACTOR:** expose owned generators and invariants so the backend can be removed without changing test meaning.
  - **Refs:** [edge/tooling audit](specs/adversarial-edge-and-tooling-audit-2026-08-14.md), [Rapid](https://github.com/flyingmutant/rapid), [test doctrine](#secondary-test-taxonomy-and-required-return-contracts).
  - **Evidence (2026-09-03):** decision `REJECT` in `definitions/architecture/rapid-qualification.yaml`; `TestRapidQualificationShrinksOwnedStateMachineFailure`, `TestTodo_LIB_016_Property`, `TestTodo_LIB_016_Golden`, `FuzzTodo_LIB_016`, `TestTodo_LIB_016_Conformance` in `tools/quality/rapidkit` prove deterministic standard-library generation/shrinking for balance underflow, illegal workflow transition and temporal overlap, pin regression seeds, and verify `pgregory.net/rapid` is absent from `go.mod`, `go.sum` and all imports. `go test -count=1 ./tools/quality/rapidkit`, `go test -fuzz=FuzzTodo_LIB_016 -fuzztime=2s ./tools/quality/rapidkit`, and `go vet ./tools/quality/rapidkit` PASS on windows/arm64 (Go 1.26.3); refactored; branch plan-revision-2026-09-02.

- [x] `LIB-017` **[P0][SOL_LOW] Evaluate sqlc for typed PostgreSQL adapter plumbing without semantic capture.**
  - **Depends:** `LIB-001`, `LIB-004`, `TOOL-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=reduce database scan/bind drift while preserving authored SQL, semantic repository ownership and explicit transaction boundaries`.
  - **TEST:** `TestSQLCQualificationPreservesOwnedRepositoryAndTransactionBoundaries`.
  - **TEST MATRIX:** `PRIMARY=TestSQLCQualificationPreservesOwnedRepositoryAndTransactionBoundaries`; `GOLDEN=TestTodo_LIB_017_Golden`; `INTEGRATION=TestTodo_LIB_017_Integration`; `CONFORMANCE=TestTodo_LIB_017_Conformance`; `MUTATION=TestTodo_LIB_017_Mutation`.
  - **RED:** generated code exposes sqlc/pgx types through an owner port, invents repository/domain semantics, hides a temporal predicate/lock/consistency boundary, accepts schema drift or changes bytes across clean generation.
  - **GREEN:** pinned offline generation produces byte-stable concrete PostgreSQL adapter code from reviewed SQL, strict schema/type checks reject drift, owned repository tests return identical typed results and the manifest records adopt/reject plus removal path.
  - **REFACTOR:** generate only repetitive adapter mechanics; correctness-bearing SQL and unit-of-work composition remain explicit and human-reviewable.
  - **Refs:** [edge/tooling audit](specs/adversarial-edge-and-tooling-audit-2026-08-14.md), [sqlc](https://github.com/sqlc-dev/sqlc), [PostgreSQL materialization](#26-postgresql-database-and-model-materialization).
  - **Evidence (2026-09-03):** decision `REJECT` in `definitions/architecture/sqlc-qualification.yaml` because no pinned offline sqlc binary/configuration pair exists; `TestSQLCQualificationPreservesOwnedRepositoryAndTransactionBoundaries`, `TestTodo_LIB_017_Golden`, `TestTodo_LIB_017_Integration`, `TestTodo_LIB_017_Conformance`, `TestTodo_LIB_017_Mutation` in `tools/quality/sqlckit` prove the authored-SQL/standard-library alternative is byte-stable, preserves temporal predicates, locks and explicit commit/rollback, rejects schema/query drift, returns typed rows and leaks no sqlc/pgx type through an owned port. `go test -count=1 ./tools/quality/sqlckit` and `go vet ./tools/quality/sqlckit` PASS on windows/arm64 (Go 1.26.3); production/runtime module graph unchanged; refactored; branch plan-revision-2026-09-02.

- [x] `LIB-018` **[GATE_A][SOL_HIGH] Qualify AWS SDK for Go v2 behind the provider-neutral object-store adapter.**
  - **Evidence (2026-09-05):** `TestAWSSDKAdapterPassesObjectStoreConformanceWithoutTypeLeakage` in `internal/store/object/aws` (the pinned adapter maps exact provider version/checksum/precondition/retention/hold results into owned types, passes the same fake/alternate-adapter suite and records endpoint, credential, retry and removal policy; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/store/object/aws/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LIB-001`, `ARTIFACT-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.DOCUMENTS,BI.PRIVACY,BI.DATAOPS; DIRECT=none; WHY=provide S3-compatible transport mechanics without making provider keys, versions or retention APIs the artifact domain model`.
  - **TEST:** `TestAWSSDKAdapterPassesObjectStoreConformanceWithoutTypeLeakage`.
  - **TEST MATRIX:** `PRIMARY=TestAWSSDKAdapterPassesObjectStoreConformanceWithoutTypeLeakage`; `GOLDEN=TestTodo_LIB_018_Golden`; `RACE=TestTodo_LIB_018_Race`; `INTEGRATION=TestTodo_LIB_018_Integration`; `FAULT=TestTodo_LIB_018_Fault`; `SECURITY=TestTodo_LIB_018_Security`; `CONFORMANCE=TestTodo_LIB_018_Conformance`.
  - **RED:** an AWS type enters an owned contract, key name becomes artifact identity, retry hides ambiguous completion, checksum/version/hold is dropped or the adapter requires a cloud-specific HCM behavior.
  - **GREEN:** the pinned adapter maps exact provider version/checksum/precondition/retention/hold results into owned types, passes the same fake/alternate-adapter suite and records endpoint, credential, retry and removal policy.
  - **REFACTOR:** isolate SDK configuration and error translation in `store/object` provider code; no domain/workflow package imports it.
  - **Refs:** [edge/tooling audit](specs/adversarial-edge-and-tooling-audit-2026-08-14.md), [AWS SDK for Go v2](https://github.com/aws/aws-sdk-go-v2), [artifact contracts](#35-artifact-bytes-and-customer-data-onboarding).

- [x] `LIB-019` **[P0][SOL_HIGH] Qualify Buf and Protovalidate for schema linting and structural validation only.**
  - **Evidence (2026-09-05):** `TestBufProtovalidateQualificationCannotBecomeBusinessAuthority`, `TestTodo_LIB_019_{Property,Golden,Integration,Security,Conformance,Race}` in `tools/policy/libqualification`: buf ADOPTED (buf.yaml v2, lint STANDARD, breaking FILE; `buf lint schema/proto` clean), the protovalidate runtime library stays out (tools/quality/bufprotovalidatekit owns descriptor validation and asserts its absence); `go test -count=1 ./tools/policy/libqualification/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LIB-001`, `LIB-003`, `TOOL-006`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide deterministic Protobuf lint, compatibility and request-shape mechanics while HCM Next retains all business/governance semantics`.
  - **TEST:** `TestBufProtovalidateQualificationCannotBecomeBusinessAuthority`.
  - **TEST MATRIX:** `PRIMARY=TestBufProtovalidateQualificationCannotBecomeBusinessAuthority`; `PROPERTY=TestTodo_LIB_019_Property`; `GOLDEN=TestTodo_LIB_019_Golden`; `FUZZ=FuzzTodo_LIB_019`; `SECURITY=TestTodo_LIB_019_Security`; `CONFORMANCE=TestTodo_LIB_019_Conformance`.
  - **RED:** unpinned remote modules/plugins are required, a validation rule mutates state or decides AuthZ/legal/eligibility, invalid CEL constraints publish, unrestricted `Any` passes or runtime validation disagrees across gRPC/grpcbridge.
  - **GREEN:** pinned offline Buf checks and Protovalidate interceptors return owned typed field violations identically across transports, restrict validation to structural/local invariants and remain replaceable behind generated contracts.
  - **REFACTOR:** SchemaFlux owns definition compilation and HCM semantics; Buf/Protovalidate supply only declared Protobuf mechanics.
  - **Refs:** [Buf lint rules](https://buf.build/docs/lint/rules/), [Protovalidate Any rules](https://buf.build/docs/reference/protovalidate/rules/any_rules/), [edge/tooling audit](specs/adversarial-edge-and-tooling-audit-2026-08-14.md).
  - **Evidence (partial, 2026-09-03):** `TestBufProtovalidateQualificationCannotBecomeBusinessAuthority`, `TestTodo_LIB_019_Property`, `TestTodo_LIB_019_Golden`, `FuzzTodo_LIB_019`, `TestTodo_LIB_019_Security`, and `TestTodo_LIB_019_Conformance` in `tools/quality/bufprotovalidatekit` plus `definitions/architecture/buf-protovalidate-qualification.yaml`; Buf is pinned/adopted as developer-only while absent Protovalidate is rejected behind an owned descriptor/standard-library fallback that fails closed on business/AuthZ/legal/eligibility/mutation/CEL/unregistered-Any scope. Focused tests, `go vet`, a three-second fuzz run (177,894 executions), and live `buf lint schema/proto` PASS, but no Protovalidate module, annotations, or interceptor evidence exists, so this TODO remains open; on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `LIB-020` **[P0][SOL_HIGH] Qualify golang.org/x/text behind owned Unicode and locale policies.**
  - **Depends:** `LIB-001`, `TOOL-013`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.PEOPLE,BI.RECRUITING,BI.EXPERIENCE,BI.DATAOPS,BI.PRIVACY; DIRECT=none; WHY=provide Unicode normalization and language mechanics without making normalization or confusable detection an identity decision`.
  - **TEST:** `TestXTextQualificationPinsDataAndPreservesOwnedFieldSemantics`.
  - **TEST MATRIX:** `PRIMARY=TestXTextQualificationPinsDataAndPreservesOwnedFieldSemantics`; `PROPERTY=TestTodo_LIB_020_Property`; `GOLDEN=TestTodo_LIB_020_Golden`; `FUZZ=FuzzTodo_LIB_020`; `SECURITY=TestTodo_LIB_020_Security`; `CONFORMANCE=TestTodo_LIB_020_Conformance`.
  - **RED:** canonically equivalent input changes digest unpredictably, original text is destroyed, normalization broadens an identifier match, a confusable skeleton auto-merges people or an unpinned Unicode-data update silently changes search/auth behavior.
  - **GREEN:** owned per-field profiles pin Unicode data/operation, preserve original plus normalized/search forms where authorized, treat confusables as review signals only and emit impact/reindex evidence on version change.
  - **REFACTOR:** centralize Unicode mechanics in kernel/I18N adapters; domains select owned field policy rather than calling `x/text` directly.
  - **Refs:** [Go normalization](https://go.dev/blog/normalization), [Unicode UTS #39](https://www.unicode.org/reports/tr39/), [edge/tooling audit](specs/adversarial-edge-and-tooling-audit-2026-08-14.md).
  - **Evidence (2026-09-03):** decision `ADOPT` for `golang.org/x/text v0.41.0` in `definitions/architecture/xtext-qualification.yaml`; `TestXTextQualificationPinsDataAndPreservesOwnedFieldSemantics`, `TestTodo_LIB_020_Property`, `TestTodo_LIB_020_Golden`, `FuzzTodo_LIB_020`, `TestTodo_LIB_020_Security`, `TestTodo_LIB_020_Conformance` in `tools/quality/xtextkit` prove deterministic normalization, original/normalized/search preservation, restricted locale profiles, confusable review without auto-merge, update impact/reindex evidence, three approved import roots and zero exported x/text leakage. `go test -count=1 ./tools/quality/xtextkit`, `go test -fuzz=FuzzTodo_LIB_020 -fuzztime=3s ./tools/quality/xtextkit` (193,070 executions), and `go vet ./tools/quality/xtextkit` PASS on windows/arm64 (Go 1.26.3); production/runtime module graph unchanged; refactored; branch plan-revision-2026-09-02.

- [x] `TOOL-021` **[P0][TERRA] Qualify testing/synctest for deterministic in-process time and concurrency tests.**
  - **Depends:** `TOOL-001`, `TIME-001`, `WF-RUN-002`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove timers, retries and goroutine quiescence quickly without pretending virtual time covers database, process or network behavior`.
  - **TEST:** `TestSynctestQualificationAdvancesTimersAndDetectsQuiescenceWithoutSleep`.
  - **TEST MATRIX:** `PRIMARY=TestSynctestQualificationAdvancesTimersAndDetectsQuiescenceWithoutSleep`; `PROPERTY=TestTodo_TOOL_021_Property`; `RACE=TestTodo_TOOL_021_Race`; `FAULT=TestTodo_TOOL_021_Fault`; `CONFORMANCE=TestTodo_TOOL_021_Conformance`.
  - **RED:** timer/retry tests require wall-clock sleep, leak goroutines, pass before asynchronous assertions settle or claim to validate PostgreSQL/network/process failure using only virtual time.
  - **GREEN:** pinned Go toolchain runs deterministic fake-time/quiescence fixtures for in-process logic with zero sleeps; test metadata explicitly routes real I/O and crash cases to integration/fault harnesses.
  - **REFACTOR:** inject owned clocks/schedulers; production packages never depend on test bubbles.
  - **Refs:** [Go Testing Time](https://go.dev/blog/testing-time), [edge/tooling audit](specs/adversarial-edge-and-tooling-audit-2026-08-14.md), [workflow runtime](specs/workflow-runtime.md).
  - **Evidence (2026-09-03):** `TestSynctestQualificationAdvancesTimersAndDetectsQuiescenceWithoutSleep`, `TestTodo_TOOL_021_{Property,Race,Fault,Conformance}` in `tools/quality/synctestkit` (real testing/synctest: virtual time, quiescence, lease-loop fixture 90s virtual in <5s wall); verdict QUALIFIED in `definitions/toolchain/synctest-qualification.yaml`; PASS; branch plan-revision-2026-09-02; `go test -count=1 ./tools/quality/synctestkit/...` on windows/arm64 (Go 1.26.3).

- [x] `TOOL-022` **[GATE_A][SOL_LOW] Qualify Toxiproxy as a test-only network failure injector.**
  - **Evidence (2026-09-05):** `TestToxiproxyQualificationReproducesDeclaredTransportFaultSchedule`, `TestTodo_TOOL_022_{Golden,Race,Integration,Fault,Conformance}` in `tools/policy/libqualification` (decision REJECT for this host: no Docker, fault injection stays in-process through the existing helpers, the declared transport fault schedule is reproduced in-process and go.mod carries no toxiproxy module); `go test -count=1 ./tools/policy/libqualification/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TOOL-014`, `LIB-009`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=exercise connector, database and RPC transport failures against exact durable-state and duplicate-effect oracles`.
  - **TEST:** `TestToxiproxyQualificationReproducesDeclaredTransportFaultSchedule`.
  - **TEST MATRIX:** `PRIMARY=TestToxiproxyQualificationReproducesDeclaredTransportFaultSchedule`; `GOLDEN=TestTodo_TOOL_022_Golden`; `RACE=TestTodo_TOOL_022_Race`; `INTEGRATION=TestTodo_TOOL_022_Integration`; `FAULT=TestTodo_TOOL_022_Fault`; `CONFORMANCE=TestTodo_TOOL_022_Conformance`.
  - **RED:** latency, timeout, reset, half-close, truncation or bandwidth faults are nondeterministic/unrecorded, poison production topology, or tests assert only an RPC error instead of durable workflow/transaction/effect state.
  - **GREEN:** digest-pinned test containers execute seeded toxic schedules, record proxy/version/timeline, and each scenario returns an allowed typed state with zero lost/duplicate accepted work; release graph contains no proxy dependency.
  - **REFACTOR:** keep provider-independent fault schedules and allow an alternate injector to satisfy the same harness.
  - **Refs:** [Toxiproxy](https://github.com/shopify/toxiproxy), [edge/tooling audit](specs/adversarial-edge-and-tooling-audit-2026-08-14.md), [integration harness](#19-reference-workflow-conformance-harness).

- [x] `TOOL-023` **[GATE_A][SOL_HIGH] Pin Cosign/Sigstore verification mechanics for release admission.**
  - **Evidence (2026-09-05):** `TestCosignAdmissionRejectsWrongSubjectIdentityIssuerOrSBOM` in `tools/policy/releaseadmission` (release admission verifying a provenance statement's ed25519 signature against a pinned key set, requiring the SBOM digest match and an allowlisted builder id, recording a decision; Cosign/Sigstore qualification DEFER with go.mod pinned free of sigstore modules and the adoption trigger documented; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/releaseadmission/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TOOL-017`, `TOOL-018`, `LIB-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=verify the exact production artifact/toolchain/SBOM provenance before any intent-serving workload is admitted`.
  - **TEST:** `TestCosignAdmissionRejectsWrongSubjectIdentityIssuerOrSBOM`.
  - **TEST MATRIX:** `PRIMARY=TestCosignAdmissionRejectsWrongSubjectIdentityIssuerOrSBOM`; `GOLDEN=TestTodo_TOOL_023_Golden`; `FAULT=TestTodo_TOOL_023_Fault`; `SECURITY=TestTodo_TOOL_023_Security`; `CONFORMANCE=TestTodo_TOOL_023_Conformance`.
  - **RED:** unsigned, wrong-digest, untrusted-builder, wrong-issuer, stale-policy, detached-SBOM, expired/bad transparency proof or replayed attestation reaches deployment admission.
  - **GREEN:** pinned verification tooling checks subject digest, signer/workflow identity, issuer, source/build predicate, SBOM linkage and policy; retained bundles support the declared outage/offline mode and no runtime HCM behavior depends on Sigstore availability.
  - **REFACTOR:** keep the admission policy owned and tool-neutral; Cosign is a CLI/mechanics implementation, not the trust model.
  - **Refs:** [Cosign](https://github.com/sigstore/cosign), [GitHub artifact attestations](https://docs.github.com/en/actions/concepts/security/artifact-attestations), [edge/tooling audit](specs/adversarial-edge-and-tooling-audit-2026-08-14.md).

- [x] `TOOL-024` **[GATE_A][TERRA] Add symbol-aware govulncheck evidence to dependency admission.**
  - **Depends:** `TOOL-001`, `TOOL-019`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=identify reachable vulnerable Go symbols while preserving owned exploitability, remediation and exception decisions`.
  - **TEST:** `TestGovulncheckAdmissionClassifiesReachableAndUnreachableFindings`.
  - **TEST MATRIX:** `PRIMARY=TestGovulncheckAdmissionClassifiesReachableAndUnreachableFindings`; `GOLDEN=TestTodo_TOOL_024_Golden`; `SECURITY=TestTodo_TOOL_024_Security`; `CONFORMANCE=TestTodo_TOOL_024_Conformance`.
  - **RED:** seeded reachable vulnerable call passes, an unreachable module finding is silently ignored, scan output lacks tool/database/module-graph digests or an exception has no owner/expiry/compensating control.
  - **GREEN:** pinned `govulncheck` produces reproducible symbol/call evidence joined to the SBOM; policy blocks reachable prohibited findings and records governed disposition for every remaining finding.
  - **REFACTOR:** scanning informs but never replaces dependency inventory, patch SLA, threat analysis or runtime control evidence.
  - **Refs:** [Go vulnerability management](https://go.dev/doc/security/vuln/), [dependency policy](#2b-external-go-dependency-qualification-and-semantic-ownership), [edge/tooling audit](specs/adversarial-edge-and-tooling-audit-2026-08-14.md).
  - **Evidence (2026-09-03):** `TestGovulncheckAdmissionClassifiesReachableAndUnreachableFindings`, `TestTodo_TOOL_024_{Golden,Security,Conformance}` in `tools/policy/depadmission` (parses govulncheck JSON, reachable vs module-only findings, digest-bound exceptions with owner/expiry, absent scanner = SKIPPED_WITH_REASON never PASS); PASS; branch plan-revision-2026-09-02; `go test -count=1 ./tools/policy/depadmission/...` on windows/arm64 (Go 1.26.3).

- [x] `DB-EDGE-001` **[GATE_A][SOL_HIGH] Make PostgreSQL LISTEN/NOTIFY a lossy wake-up hint over durable work tables.**
  - **Evidence (2026-09-05):** `TestPostgresNotificationLossAndStartupRaceCannotLoseDurableWork` in `internal/data/wakeup` (NOTIFY sent after commit on a per-tenant channel as a lossy hint, a Listener that re-reads the durable work tables on every hint and trusts nothing beyond the channel name, dropped notifications losing no work under a durable catch-up scan, forged notifications causing no action beyond one durable read, re-LISTEN with catch-up after reconnect, caller-driven Wait with no scheduler loop; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/wakeup/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DB-001`, `DB-018`, `DATA-006`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=wake durable workflow, outbox and projection workers without treating an ephemeral database notification as accepted work or authority`.
  - **TEST:** `TestPostgresNotificationLossAndStartupRaceCannotLoseDurableWork`.
  - **TEST MATRIX:** `PRIMARY=TestPostgresNotificationLossAndStartupRaceCannotLoseDurableWork`; `PROPERTY=TestTodo_DB_EDGE_001_Property`; `RACE=TestTodo_DB_EDGE_001_Race`; `INTEGRATION=TestTodo_DB_EDGE_001_Integration`; `FAULT=TestTodo_DB_EDGE_001_Fault`; `RECOVERY=TestTodo_DB_EDGE_001_Recovery`.
  - **RED:** dropping every notification loses an outbox/timer/projection row, listener startup during concurrent commit misses work, duplicate hints duplicate a claim, payload becomes the durable record or queue exhaustion makes accepted work unreachable.
  - **GREEN:** worker commits `LISTEN`, scans the authoritative claim table, then consumes hint-only notifications plus bounded periodic recovery; loss/reorder/duplicate/reconnect fixtures process each logical row under its normal lease/idempotency contract.
  - **REFACTOR:** share one hint adapter and keep authoritative payload/state in governed tables; PostgreSQL notification channels carry no sensitive business data.
  - **Refs:** [PostgreSQL LISTEN](https://www.postgresql.org/docs/17/sql-listen.html), [PostgreSQL NOTIFY](https://www.postgresql.org/docs/17/sql-notify.html), [edge/tooling audit](specs/adversarial-edge-and-tooling-audit-2026-08-14.md).

- [ ] `DB-EDGE-002` **[GATE_B][SOL_HIGH] Govern PostgreSQL advisory-lock scope, namespace and exhaustion.**
  - **Depends:** `DB-001`, `DB-018`, `ADMISSION-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=coordinate bounded database mechanics without session-lock leakage, tenant collision or unbounded shared-memory consumption`.
  - **TEST:** `TestPostgresAdvisoryLocksAreTransactionScopedNamespacedAndBudgeted`.
  - **TEST MATRIX:** `PRIMARY=TestPostgresAdvisoryLocksAreTransactionScopedNamespacedAndBudgeted`; `PROPERTY=TestTodo_DB_EDGE_002_Property`; `RACE=TestTodo_DB_EDGE_002_Race`; `INTEGRATION=TestTodo_DB_EDGE_002_Integration`; `FAULT=TestTodo_DB_EDGE_002_Fault`; `SECURITY=TestTodo_DB_EDGE_002_Security`; `BENCHMARK=BenchmarkTodo_DB_EDGE_002`.
  - **RED:** rollback/pool return leaves a session lock, two tenant/resource keys collide, SQL evaluation acquires more locks than the limited result set, reentrancy hides imbalance or lock pressure exhausts shared memory without admission response.
  - **GREEN:** approved use defaults to transaction-scoped locks with versioned collision-tested key derivation, explicit acquisition order/count budget and typed saturation result; session locks require a separately reviewed owner and cleanup proof.
  - **REFACTOR:** prefer row/CAS/fence contracts where they express ownership better; advisory locks never become business truth.
  - **Refs:** [PostgreSQL explicit locking](https://www.postgresql.org/docs/16/explicit-locking.html), [edge/tooling audit](specs/adversarial-edge-and-tooling-audit-2026-08-14.md), [transaction coordinator](specs/transaction-plan-and-commit-coordinator.md).

- [ ] `DB-EDGE-003` **[GATE_B][SOL_HIGH] Retry serializable transactions only as complete bounded closures.**
  - **Depends:** `DB-018`, `TX-005`, `ADMISSION-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=preserve exact intent revalidation and commit semantics under PostgreSQL serialization/deadlock failures without duplicating effects`.
  - **TEST:** `TestSerializableRetryRestartsWholeClosureAndNeverRetriesAmbiguousCommit`.
  - **TEST MATRIX:** `PRIMARY=TestSerializableRetryRestartsWholeClosureAndNeverRetriesAmbiguousCommit`; `PROPERTY=TestTodo_DB_EDGE_003_Property`; `RACE=TestTodo_DB_EDGE_003_Race`; `INTEGRATION=TestTodo_DB_EDGE_003_Integration`; `FAULT=TestTodo_DB_EDGE_003_Fault`; `MUTATION=TestTodo_DB_EDGE_003_Mutation`.
  - **RED:** retry reuses a stale read/result, retries only the failed statement, performs external I/O inside the closure, maps a changed unique constraint to success, loops without budget or retries after connection loss makes commit outcome unknown.
  - **GREEN:** only classified `40001|40P01` restart a pure complete closure from a fresh transaction/snapshot with bounded jitter/admission; changed facts revalidate/recompute, unique conflicts return owned meaning and ambiguous commit enters idempotent resolution rather than retry.
  - **REFACTOR:** centralize retry classification/telemetry while each semantic owner supplies recomputation and typed conflict mapping.
  - **Refs:** [PostgreSQL transaction isolation](https://www.postgresql.org/docs/15/transaction-iso.html), [edge/tooling audit](specs/adversarial-edge-and-tooling-audit-2026-08-14.md), [transaction plan](specs/transaction-plan-and-commit-coordinator.md).

- [x] `DB-EDGE-004` **[GATE_A][SOL_HIGH] Prove PostgreSQL pool session hygiene and tenant-state reset.**
  - **Evidence (2026-09-05):** `TestPooledConnectionCannotLeakTenantRoleLocksOrSessionState`, `TestTodo_DB_EDGE_004_{Fault,Property,Race,Security}`, `TestPoolReusesAConnectionAcrossItsOwnHygiene` in `internal/data/pgxadapter` (BeforeAcquire hygiene: RESET ROLE, advisory unlock, DISCARD ALL, runtime params re-applied through the simple protocol; QueryExecModeCacheDescribe so the hygiene never invalidates the statement cache; zero hygiene failures and one reused connection); `TestTodo_DB_EDGE_004_Integration` runs only against an external server via HCMNEXT_TEST_DATABASE_URL; `go test -count=1 ./internal/data/pgxadapter/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LIB-004`, `DB-001`, `DB-017`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=prevent pooled PostgreSQL session state from crossing tenant, purpose, role or request boundaries`.
  - **TEST:** `TestPooledConnectionCannotLeakTenantRoleLocksOrSessionState`.
  - **TEST MATRIX:** `PRIMARY=TestPooledConnectionCannotLeakTenantRoleLocksOrSessionState`; `PROPERTY=TestTodo_DB_EDGE_004_Property`; `RACE=TestTodo_DB_EDGE_004_Race`; `INTEGRATION=TestTodo_DB_EDGE_004_Integration`; `FAULT=TestTodo_DB_EDGE_004_Fault`; `SECURITY=TestTodo_DB_EDGE_004_Security`.
  - **RED:** a connection returned after `SET ROLE`, tenant/purpose context, advisory lock, temp object, prepared statement or failed transaction exposes that state to the next borrower; cancellation returns an unusable/unknown session as healthy.
  - **GREEN:** checkout establishes trusted transaction-local context, checkin proves idle/clean state or destroys the connection, tenant/RLS fixtures never cross-contaminate and pool metrics expose discard/reset/failure counts without sensitive labels.
  - **REFACTOR:** use transaction-local settings and narrow adapter hooks; application packages cannot mutate ambient session state directly.
  - **Refs:** [pgx qualification](#2b-external-go-dependency-qualification-and-semantic-ownership), [tenant isolation](#26-postgresql-database-and-model-materialization), [edge/tooling audit](specs/adversarial-edge-and-tooling-audit-2026-08-14.md).

- [x] `RPC-EDGE-001` **[GATE_A][SOL_HIGH] Classify gRPC deadline, retry and hedging behavior per capability method.**
  - **Evidence (2026-09-05):** `TestGRPCRetryPolicyCannotReplayNonIdempotentCapabilityEffect` in `internal/transport/rpcpolicy` (generated method manifest declares deadline, wait-for-ready, idempotency, retryable statuses/attempt budget and hedging prohibition/allowance; transparent/configured replay fixtures return one logical result/effect and preserve unknown/committed outcomes accurately; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/transport/rpcpolicy/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `LIB-003`, `PROTO-006`, `TX-005`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=prevent transport replay or unbounded waits from duplicating governed intent, transaction or external-effect behavior`.
  - **TEST:** `TestGRPCRetryPolicyCannotReplayNonIdempotentCapabilityEffect`.
  - **TEST MATRIX:** `PRIMARY=TestGRPCRetryPolicyCannotReplayNonIdempotentCapabilityEffect`; `PROPERTY=TestTodo_RPC_EDGE_001_Property`; `RACE=TestTodo_RPC_EDGE_001_Race`; `INTEGRATION=TestTodo_RPC_EDGE_001_Integration`; `FAULT=TestTodo_RPC_EDGE_001_Fault`; `SECURITY=TestTodo_RPC_EDGE_001_Security`; `CONFORMANCE=TestTodo_RPC_EDGE_001_Conformance`.
  - **RED:** a public call has no deadline policy, retries/hedges an effect-bearing method without owned idempotency, `UNAVAILABLE` duplicates intent/effect rows, client cancellation is reported as business rollback or retry amplification defeats admission control.
  - **GREEN:** generated method manifest declares deadline, wait-for-ready, idempotency, retryable statuses/attempt budget and hedging prohibition/allowance; transparent/configured replay fixtures return one logical result/effect and preserve unknown/committed outcomes accurately.
  - **REFACTOR:** generate service config and conformance cases from capability effect metadata rather than handwritten client defaults.
  - **Refs:** [gRPC retry](https://grpc.io/docs/guides/retry/), [gRPC deadlines](https://grpc.io/docs/guides/deadlines/), [edge/tooling audit](specs/adversarial-edge-and-tooling-audit-2026-08-14.md).

- [x] `PROTO-009` **[P0][SOL_HIGH] Preserve or explicitly reject Protobuf unknown fields and restrict dynamic types.**
  - **Depends:** `PROTO-001`, `PROTO-008`, `LIB-019`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=prevent mixed-version relays and dynamic envelopes from silently weakening an intent, approval, governance or effect contract`.
  - **TEST:** `TestMixedVersionProtoRelayNeverSilentlyDropsMaterialUnknownFields`.
  - **TEST MATRIX:** `PRIMARY=TestMixedVersionProtoRelayNeverSilentlyDropsMaterialUnknownFields`; `PROPERTY=TestTodo_PROTO_009_Property`; `GOLDEN=TestTodo_PROTO_009_Golden`; `FUZZ=FuzzTodo_PROTO_009`; `SECURITY=TestTodo_PROTO_009_Security`; `CONFORMANCE=TestTodo_PROTO_009_Conformance`; `MUTATION=TestTodo_PROTO_009_Mutation`.
  - **RED:** old relay/JSON bridge/transformation drops a new material field and still returns valid approval/execution, unknown enum becomes a default authorization, unregistered `Any` type URL resolves or canonical digest changes without compatibility classification.
  - **GREEN:** each boundary declares preserve/reject/ignore policy by message/use, material unknowns block semantic execution with typed compatibility error, allowed relays preserve golden bytes and `Any`/extension types resolve only through the signed registry allowlist.
  - **REFACTOR:** generate unknown-field and dynamic-type policy from descriptors/registry; domain code never performs ad hoc reflection.
  - **Refs:** [Buf reflection](https://buf.build/docs/bsr/reflection/), [Protovalidate Any rules](https://buf.build/docs/reference/protovalidate/rules/any_rules/), [edge/tooling audit](specs/adversarial-edge-and-tooling-audit-2026-08-14.md).
  - **Evidence (2026-09-03):** `TestMixedVersionProtoRelayNeverSilentlyDropsMaterialUnknownFields` in `internal/transport/manifest` (59 reachable messages walked; `TypedPayload` is the single dynamically-typed message with a catalog-derived allowlist; unknown-field injection test); PASS; branch plan-revision-2026-09-02; `go test -count=1 ./internal/transport/manifest/...` on windows/arm64 (Go 1.26.3).

- [ ] `ARTIFACT-007` **[GATE_B][SOL_HIGH] Bind artifact identity, multipart completion, retention and legal hold to immutable object versions.**
  - **Depends:** `ARTIFACT-002`, `ARTIFACT-006`, `LIB-018`, `RECORDS-HOLD-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.DOCUMENTS,BI.PRIVACY,BI.CASES,BI.DATAOPS; DIRECT=none; WHY=keep evidence/artifact truth stable across concurrent upload, provider versioning, retention, hold and repair`.
  - **TEST:** `TestConcurrentMultipartCompletionBindsOnlyAuthorizedVersionChecksumAndHold`.
  - **TEST MATRIX:** `PRIMARY=TestConcurrentMultipartCompletionBindsOnlyAuthorizedVersionChecksumAndHold`; `PROPERTY=TestTodo_ARTIFACT_007_Property`; `RACE=TestTodo_ARTIFACT_007_Race`; `INTEGRATION=TestTodo_ARTIFACT_007_Integration`; `FAULT=TestTodo_ARTIFACT_007_Fault`; `SECURITY=TestTodo_ARTIFACT_007_Security`; `RECOVERY=TestTodo_ARTIFACT_007_Recovery`; `MUTATION=TestTodo_ARTIFACT_007_Mutation`.
  - **RED:** two uploads overwrite by key, completion after cancel binds unknown bytes, returned version/checksum is ignored, orphan parts remain unbounded, legal hold attaches only to `latest` or governance-mode bypass is reported as compliant deletion.
  - **GREEN:** conditional create/complete binds provider version ID plus verified checksum to immutable ArtifactRevision, losing/ambiguous uploads reconcile then abort/collect safely, and retention/legal hold/delete operate and verify per exact version with truthful evidence.
  - **REFACTOR:** logical names point to immutable revisions; provider key/version APIs stay adapter details.
  - **Refs:** [S3 multipart upload](https://docs.aws.amazon.com/AmazonS3/latest/userguide/mpuoverview.html), [S3 Object Lock](https://docs.aws.amazon.com/AmazonS3/latest/userguide/object-lock.html), [edge/tooling audit](specs/adversarial-edge-and-tooling-audit-2026-08-14.md).

- [x] `OBS-022` **[GATE_A][SOL_HIGH] Prove third-party telemetry egress strips sensitive and authority-bearing baggage.**
  - **Evidence (2026-09-05):** `TestTelemetryBaggageAllowlistStripsSensitiveUntrustedAndThirdPartyFields` in `internal/platform/telemetry` (third-party telemetry egress strips sensitive and authority-bearing baggage; the export path test proves no tenant id, subject or token attribute leaves the process; written by a subagent and verified independently); `go test -count=1 ./internal/platform/telemetry/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `OBS-004`, `OBS-011`, `LIB-007`, `EDGE-005`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=retain operational correlation without leaking HCM data or allowing untrusted propagated metadata to influence authorization`.
  - **TEST:** `TestTelemetryBaggageAllowlistStripsSensitiveUntrustedAndThirdPartyFields`.
  - **TEST MATRIX:** `PRIMARY=TestTelemetryBaggageAllowlistStripsSensitiveUntrustedAndThirdPartyFields`; `PROPERTY=TestTodo_OBS_022_Property`; `FUZZ=FuzzTodo_OBS_022`; `INTEGRATION=TestTodo_OBS_022_Integration`; `SECURITY=TestTodo_OBS_022_Security`; `CONFORMANCE=TestTodo_OBS_022_Conformance`; `BENCHMARK=BenchmarkTodo_OBS_022`.
  - **RED:** worker/person/email/medical/pay/bank/free-text/attacker key reaches baggage, log, span, metric label or third-party request; a baggage value changes AuthZ/routing/tenant context; high-cardinality values exhaust the pipeline or redaction failure is silent.
  - **GREEN:** trusted transport reconstructs an allowlisted bounded correlation context, baggage is never an authority input, third-party egress strips it, collector redaction is defense-in-depth and leak/cardinality fixtures emit a bounded security signal without payload.
  - **REFACTOR:** generate propagation and telemetry-label policy from classification metadata; domains emit owned semantic events, not raw attributes.
  - **Refs:** [OpenTelemetry baggage](https://opentelemetry.io/docs/concepts/signals/baggage/), [telemetry transformation](https://opentelemetry.io/docs/collector/transforming-telemetry/), [edge/tooling audit](specs/adversarial-edge-and-tooling-audit-2026-08-14.md).
- [x] `OBS-023` **[GATE_B][SOL_HIGH] Instrument the workflow engine with trace ids, spans and structured logs per advancement.**
  - **Evidence (2026-09-05):** `TestTodo_OBS_023` in `internal/platform/execution`, `internal/workflow/execute`, `test/workflow` (the workflow engine emits a trace id, one span per node advancement and a structured log per transition, proven on the promotion plan end to end; written by a subagent and verified independently); `go test -count=1 ./internal/platform/execution/ ./internal/workflow/execute/ ./test/workflow/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `OBS-002`, `OBS-009`, `OBS-011`, `OBS-013`, `WF-RUN-025`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=an operator must be able to pivot from a node execution to the span and log that produced it and back`.
  - **TEST:** `TestTodo_OBS_023`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_OBS_023`; `GOLDEN=TestTodo_OBS_023_Golden`; `INTEGRATION=TestTodo_OBS_023_Integration`; `SECURITY=TestTodo_OBS_023_Security`.
  - **RED:** node executions carry an empty trace id; Execute, Resume, each advancement and the terminal write emit no span, no structured log and no duration; or a span or log carries payload, approver identity or authority-bearing baggage.
  - **GREEN:** the driver propagates the transport span context into `StepRequest` and `AdvanceRequest.TraceID`, opens one child span per node run, advancement and terminal write with instance, node, attempt and terminal code attributes, emits one `log/slog` envelope line per advancement and terminal write, and records node durations; the in-memory exporter (OBS-015) proves the chain in tests.
  - **REFACTOR:** span and log names follow the OBS-012 topology; no engine package imports an exporter.
  - **Refs:** [telemetry contract](specs/structured-logging-and-opentelemetry.md), [review findings](devlog/2026-09-03-executable-prototype.md#9-review-findings-and-the-hardening-plan).

- [x] `OBS-024` **[GATE_B][SOL_HIGH] Record execution evidence for authority-gate refusals, approvals, submissions and terminal writes.**
  - **Evidence (2026-09-05):** `TestTodo_OBS_024` in `internal/platform/execution`, `internal/workflow/execute`, `test/bootstrap`, `test/workflow` (execution evidence rows are recorded for authority-gate refusals, approvals, submissions and terminals, proven on the promotion plan; written by a subagent and verified independently); `go test -count=1 ./internal/platform/execution/ ./internal/workflow/execute/ ./test/bootstrap/ ./test/workflow/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `OBS-023`, `WF-RUN-030`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=ExecuteIntent must leave the same inspectable evidence trail SimulateIntent leaves, including what it refused`.
  - **TEST:** `TestTodo_OBS_024`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_OBS_024`; `GOLDEN=TestTodo_OBS_024_Golden`; `INTEGRATION=TestTodo_OBS_024_Integration`; `SECURITY=TestTodo_OBS_024_Security`.
  - **RED:** an authority-gate refusal, an approval completion, a task submission or a terminal write produces no evidence record; or evidence ids are not returned on the receipt; or the evidence sink loses records on restart.
  - **GREEN:** each of those events records an evidence entry (kind, instance, node, work item or decision id, digest, evidence id) through the capability evidence sink backed by the durable evidence store, the receipt returns the evidence ids, and the harness reads them back over both transports.
  - **REFACTOR:** evidence kinds are an enumerated vocabulary shared with the inspector.
  - **Refs:** [capability evidence](specs/platform-foundation-gap-closure.md), [review findings](devlog/2026-09-03-executable-prototype.md#9-review-findings-and-the-hardening-plan).

- [x] `EXPORT-001` **[GATE_A][SOL_HIGH] Separate safe human-spreadsheet export from exact machine-data export.**
  - **Evidence (2026-09-05):** `TestSpreadsheetExportNeutralizesFormulaCellsWhileMachineExportPreservesExactValues` in `internal/operations/export` (explicit HUMAN_SPREADSHEET profile produces inert golden cells under declared applications/locales and records a transformation warning, while MACHINE_DATA uses a non-executable typed format/profile preserving exact values; both bind scope, purpose, schema, digest, classification; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/operations/export/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ADMIN-007`, `TRUST-018`, `ARTIFACT-005`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ANALYTICS,BI.DATAOPS,BI.PRIVACY,BI.OPERATIONS; DIRECT=none; WHY=deliver authorized report/evidence data without turning untrusted HCM text into executable spreadsheet formulas or corrupting machine round trips`.
  - **TEST:** `TestSpreadsheetExportNeutralizesFormulaCellsWhileMachineExportPreservesExactValues`.
  - **TEST MATRIX:** `PRIMARY=TestSpreadsheetExportNeutralizesFormulaCellsWhileMachineExportPreservesExactValues`; `PROPERTY=TestTodo_EXPORT_001_Property`; `GOLDEN=TestTodo_EXPORT_001_Golden`; `FUZZ=FuzzTodo_EXPORT_001`; `INTEGRATION=TestTodo_EXPORT_001_Integration`; `SECURITY=TestTodo_EXPORT_001_Security`; `CONFORMANCE=TestTodo_EXPORT_001_Conformance`.
  - **RED:** ASCII/full-width `=+-@`, tab/CR/LF, delimiter/quote breakout or locale delimiter becomes a spreadsheet formula; the same escaping silently changes a machine import value; classification/DLP/manifest omits the transformation profile.
  - **GREEN:** explicit `HUMAN_SPREADSHEET` profile produces inert golden cells under declared applications/locales and records a transformation warning, while `MACHINE_DATA` uses a non-executable typed format/profile preserving exact values; both bind scope, purpose, schema, digest, classification and expiry.
  - **REFACTOR:** one export planner chooses typed renderer profiles; never apply presentation escaping to canonical stored data.
  - **Refs:** [OWASP CSV Injection](https://owasp.org/www-community/attacks/CSV_Injection), [edge/tooling audit](specs/adversarial-edge-and-tooling-audit-2026-08-14.md), [DLP](specs/data-classification-and-dlp.md).

---

## 60. Canonical gRPC services, grpcbridge HTTP endpoints and endpoint TDD

> **Disposition (2026-09-02):** P1A for `RegistryService`, `IntentService.Create/Get/Simulate/Submit`, `ExplainIntent` and health through the qualified edge. P1B for `WorkService`, `WorkflowService` and `PromotionService`. Evidence export and long-running operations are Gate C.

Protobuf/gRPC is canonical and grpcbridge is the sole public HTTP projection.
These todos add endpoint contracts, not HTTP-owned business behavior. Every
material method resolves to the same registered capability and BusinessIntent
path regardless of transport.

- [x] `ENDPOINT-001` **[P0][SOL_HIGH] Define and generate the canonical EndpointDefinition manifest.**
  - **Depends:** `API-001`, `PROTO-005`, `MSRC-009`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=bind every public gRPC/HTTP method to one capability, intent behavior and owned transport/security contract`.
  - **TEST:** `TestEndpointManifestRejectsUnownedUnboundOrHandwrittenRoute`.
  - **TEST MATRIX:** `PRIMARY=TestEndpointManifestRejectsUnownedUnboundOrHandwrittenRoute`; `PROPERTY=TestTodo_ENDPOINT_001_Property`; `GOLDEN=TestTodo_ENDPOINT_001_Golden`; `SECURITY=TestTodo_ENDPOINT_001_Security`; `CONFORMANCE=TestTodo_ENDPOINT_001_Conformance`; `MUTATION=TestTodo_ENDPOINT_001_Mutation`.
  - **RED:** a public method validates without service/method, request/response/error descriptors, capability/version, `CREATES|CONSUMES|EMITS|OBSERVES|NON_MATERIAL` intent disposition, authn/AuthZ/purpose, HTTP binding, idempotency, revision, deadline/retry, budgets, evidence, owner/phase or compatibility policy; handwritten route not in the manifest is reachable.
  - **GREEN:** SchemaFlux compiles one immutable manifest from reviewed Protobuf/capability/intent sources, grpcbridge and discovery consume its canonical digest, every route has one semantic owner and production startup rejects unregistered handlers.
  - **REFACTOR:** generate documentation, client metadata, edge policy and test cases from the manifest; do not maintain parallel route registries.
  - **Refs:** [endpoint contract](specs/http-grpc-endpoint-contract.md), [capability registry](specs/capability-registry-and-lifecycle.md), [API discovery](#17-experience-api-and-accessibility).
  - **Evidence (2026-09-03):** `TestEndpointManifestRejectsUnownedUnboundOrHandwrittenRoute` plus `TestTodo_ENDPOINT_001_Golden` in `internal/transport/manifest` (13 RPCs discovered from protoregistry, reviewed rule table, 10 SERVED / 3 REFUSED_P1A, generated `definitions/api/endpoint-manifest.json` via `tools/policy/endpointmanifest`, presence table reproduces `internal/transport/validate.go` with zero diff); `go test -count=1 ./internal/transport/manifest/... ./tools/policy/endpointmanifest/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `ENDPOINT-002` **[GATE_A][SOL_HIGH] Construct trusted request context identically for gRPC and HTTP.**
  - **Depends:** `ENDPOINT-001`, `TRUST-001`, `CAP-002`, `EDGE-001`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=ensure public callers can supply desired business input but cannot select tenant, principal, purpose, assurance, authority or placement`.
  - **TEST:** `TestEndpointTrustedContextRejectsCallerSelectedAuthorityAcrossTransports`.
  - **TEST MATRIX:** `PRIMARY=TestEndpointTrustedContextRejectsCallerSelectedAuthorityAcrossTransports`; `PROPERTY=TestTodo_ENDPOINT_002_Property`; `FUZZ=FuzzTodo_ENDPOINT_002`; `INTEGRATION=TestTodo_ENDPOINT_002_Integration`; `SECURITY=TestTodo_ENDPOINT_002_Security`; `CONFORMANCE=TestTodo_ENDPOINT_002_Conformance`; `MUTATION=TestTodo_ENDPOINT_002_Mutation`.
  - **RED:** body/query/header/gRPC metadata can override `PrincipalContext`, tenant, organization scope, purpose, session assurance, legal/policy, authority or processing placement; equivalent gRPC/HTTP requests resolve different trusted context.
  - **GREEN:** authenticated interceptors construct trusted context server-side, reject conflicting reserved fields, pass one immutable invocation context to CapabilityGateway and golden vectors produce the same safe decision/evidence references on both transports.
  - **REFACTOR:** one interceptor chain owns context construction; handlers and domain services never parse ambient headers.
  - **Refs:** [trusted boundary](specs/http-grpc-endpoint-contract.md#trusted-request-boundary), [organization AuthZ](specs/organization-scope-and-authz.md), [edge admission](#32-production-identity-key-custody-and-edge-enforcement).
  - **Evidence (2026-09-03):** `TestEndpointTrustedContextRejectsCallerSelectedAuthorityAcrossTransports`, `TestTodo_ENDPOINT_002_{Property,Integration,Security,Conformance,Mutation}`, `FuzzTodo_ENDPOINT_002` in `internal/transport/edge`; `go test -count=1 ./internal/transport/...` PASS; both transports share one `transport.Config` and overwrite request `scope`/`initiator` server-side; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [x] `ENDPOINT-003` **[P0][SOL_HIGH] Enforce strict request decoding, structural validation and owned error projection.**
  - **Depends:** `ENDPOINT-001`, `CAP-003`, `LIB-019`, `PROTO-009`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=reject malformed or incompatible public input consistently without confusing transport validation with HCM business authority`.
  - **TEST:** `TestEndpointValidationAndErrorProjectionParity`.
  - **TEST MATRIX:** `PRIMARY=TestEndpointValidationAndErrorProjectionParity`; `PROPERTY=TestTodo_ENDPOINT_003_Property`; `GOLDEN=TestTodo_ENDPOINT_003_Golden`; `FUZZ=FuzzTodo_ENDPOINT_003`; `INTEGRATION=TestTodo_ENDPOINT_003_Integration`; `SECURITY=TestTodo_ENDPOINT_003_Security`; `CONFORMANCE=TestTodo_ENDPOINT_003_Conformance`.
  - **RED:** unknown JSON field, duplicate key, invalid UTF-8, ambiguous numeric value, unknown enum, material unknown Protobuf field or invalid local constraint is accepted; HTTP/gRPC disagree on owned code, field path, retryability or zero-effect result; error leaks stack/SQL/policy/secret/restricted value.
  - **GREEN:** strict bounded decoding plus structural validation returns one owned typed error projected through the declared gRPC status/HTTP status and safe details; every rejected vector persists zero intent/domain/event/outbox/work/effect rows.
  - **REFACTOR:** validation adapters emit owned violations; business, legal, temporal and cross-aggregate decisions remain capabilities/engines.
  - **Refs:** [error projection](specs/http-grpc-endpoint-contract.md#canonical-error-projection), [Protobuf compatibility](#29-machine-readable-model-sources-and-public-api-contracts), [capability errors](#6-businessintent-capabilities-governance-and-transaction-integrity).
  - **Evidence (2026-09-03):** `TestEndpointValidationAndErrorProjectionParity`, `TestTodo_ENDPOINT_003_{Golden,Property,Integration,Security,Conformance}`, `FuzzTodo_ENDPOINT_003` in `internal/transport/edge`; `go test -count=1 ./internal/transport/...` PASS; strict JSON decoding (unknown fields rejected), presence validation table in `internal/transport/validate.go` pending replacement by the ENDPOINT-001 manifest; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [ ] `ENDPOINT-004` **[GATE_B][SOL_HIGH] Unify endpoint idempotency, expected revision, ETag and conflict semantics.**
  - **Depends:** `ENDPOINT-001`, `CAP-002`, `TX-005`, `INTENT-005`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=make client retry and optimistic concurrency safe for intent, decision, workflow and effect-bearing methods`.
  - **TEST:** `TestEndpointIdempotencyAndRevisionParityUnderConcurrentReplay`.
  - **TEST MATRIX:** `PRIMARY=TestEndpointIdempotencyAndRevisionParityUnderConcurrentReplay`; `PROPERTY=TestTodo_ENDPOINT_004_Property`; `RACE=TestTodo_ENDPOINT_004_Race`; `INTEGRATION=TestTodo_ENDPOINT_004_Integration`; `FAULT=TestTodo_ENDPOINT_004_Fault`; `SECURITY=TestTodo_ENDPOINT_004_Security`; `CONFORMANCE=TestTodo_ENDPOINT_004_Conformance`; `MUTATION=TestTodo_ENDPOINT_004_Mutation`.
  - **RED:** mismatched header/message idempotency keys pass, same key with changed payload reuses success, concurrent transports create two logical intents/effects, stale `If-Match`/expected revision commits or retry after ambiguous response duplicates work.
  - **GREEN:** one canonical request digest and principal/tenant/capability scope maps header plus message key; exact replay returns the original result/evidence, mismatch returns conflict, stale revision returns current-safe precondition evidence and concurrency/ambiguity creates one logical operation/effect.
  - **REFACTOR:** centralize mechanics in gateway/transaction adapters; semantic owners define what revision and material payload mean.
  - **Refs:** [endpoint common behavior](specs/http-grpc-endpoint-contract.md#common-wire-and-http-behavior), [transaction coordinator](specs/transaction-plan-and-commit-coordinator.md), [RPC retry edge](#59-adversarial-infrastructure-edges-and-third-party-tool-qualification).

- [x] `ENDPOINT-005` **[GATE_A][SOL_HIGH] Implement authorization-safe list, cursor, field-mask and resource-name semantics.**
  - **Evidence (2026-09-05):** `TestEndpointListCursorAndFieldMaskCannotBroadenScopeOrRevealExistence`, `TestTodo_ENDPOINT_005_{Property,Golden,Integration,Security,Conformance}`, `FuzzTodo_ENDPOINT_005`, `BenchmarkTodo_ENDPOINT_005` in `internal/transport/list` (opaque resource names, HMAC-signed expiring cursors bound to principal/tenant/filter/watermark, bounded pages, post-authorization field masks, non-disclosing errors); `go test -count=1 ./internal/transport/list/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ENDPOINT-002`, `TRUST-006`, `DATA-005`, `PROTO-001`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=expose bounded resource inspection without count, existence, field or cross-tenant leakage`.
  - **TEST:** `TestEndpointListCursorAndFieldMaskCannotBroadenScopeOrRevealExistence`.
  - **TEST MATRIX:** `PRIMARY=TestEndpointListCursorAndFieldMaskCannotBroadenScopeOrRevealExistence`; `PROPERTY=TestTodo_ENDPOINT_005_Property`; `GOLDEN=TestTodo_ENDPOINT_005_Golden`; `FUZZ=FuzzTodo_ENDPOINT_005`; `INTEGRATION=TestTodo_ENDPOINT_005_Integration`; `SECURITY=TestTodo_ENDPOINT_005_Security`; `CONFORMANCE=TestTodo_ENDPOINT_005_Conformance`; `BENCHMARK=BenchmarkTodo_ENDPOINT_005`.
  - **RED:** display name becomes identity, cursor can be edited/replayed by another principal/tenant/filter, page drift duplicates/skips entries, page size is unbounded, field mask grants a restricted field or forbidden/not-found responses leak existence/count/timing materially.
  - **GREEN:** opaque IDs, stable ordering and signed expiring cursors bind principal/tenant/scope/filter/watermark; bounded pages and post-authorization field masks return exact completeness/redaction metadata with uniform non-disclosing errors.
  - **REFACTOR:** reuse generated list/read contracts; owner repositories supply stable keys/watermarks rather than transport offsets.
  - **Refs:** [endpoint common behavior](specs/http-grpc-endpoint-contract.md#common-wire-and-http-behavior), [AuthZ](specs/organization-scope-and-authz.md), [data plane](#11-authoritative-data-plane-ledger-projections-and-artifacts).

- [x] `ENDPOINT-006` **[GATE_A][SOL_HIGH] Enforce HTTP media, body, cache, origin and response-header policy.**
  - **Evidence (2026-09-05):** `TestHTTPEndpointMediaCacheOriginAndBodyPolicy` in `internal/transport/endpoint` (generated route policy enforces method/content type/encoding/body/decompression limits, no-store/private cache rules, canonical security headers, origin/CSRF/host controls and safe 405|406|413|415 responses before business invocation; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/transport/endpoint/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ENDPOINT-001`, `EDGE-001`, `EDGE-002`, `EDGE-004`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=make grpcbridge HTTP behavior safe and deterministic without adding an independent REST implementation`.
  - **TEST:** `TestHTTPEndpointMediaCacheOriginAndBodyPolicy`.
  - **TEST MATRIX:** `PRIMARY=TestHTTPEndpointMediaCacheOriginAndBodyPolicy`; `PROPERTY=TestTodo_ENDPOINT_006_Property`; `GOLDEN=TestTodo_ENDPOINT_006_Golden`; `FUZZ=FuzzTodo_ENDPOINT_006`; `INTEGRATION=TestTodo_ENDPOINT_006_Integration`; `SECURITY=TestTodo_ENDPOINT_006_Security`; `BROWSER=TestTodo_ENDPOINT_006_Browser`; `CONFORMANCE=TestTodo_ENDPOINT_006_Conformance`.
  - **RED:** wrong content type/encoding or GET body is accepted, oversized/compression-bomb body reaches decoding, sensitive response is cacheable, Host/Origin/CSRF/redirect is bypassed, unsafe method changes state via prefetch or security headers vary by handler.
  - **GREEN:** generated route policy enforces method/content type/encoding/body/decompression limits, no-store/private cache rules, canonical security headers, origin/CSRF/host controls and safe `405|406|413|415` responses before business invocation.
  - **REFACTOR:** grpcbridge middleware consumes endpoint metadata; route handlers contain no custom edge policy.
  - **Refs:** [endpoint contract](specs/http-grpc-endpoint-contract.md), [edge controls](#32-production-identity-key-custody-and-edge-enforcement), [DLP](specs/data-classification-and-dlp.md).

- [x] `ENDPOINT-007` **[GATE_A][SOL_HIGH] Enforce per-method deadlines, cancellation, retry, hedging and resource budgets.**
  - **Evidence (2026-09-05):** `TestEndpointMethodPolicyBoundsDeadlineRetryHedgingAndWork` in `internal/transport/endpoint` (generated method policy caps deadline, payload/work/concurrency, propagates cancellation only to declared safe boundary, permits retries/hedging solely by effect/idempotency class and returns identical typed committed/unknown/cancelled outcomes across transports; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/transport/endpoint/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ENDPOINT-001`, `RPC-EDGE-001`, `ADMISSION-001`, `EDGE-007`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=bound public work and transport replay according to capability effects and idempotency`.
  - **TEST:** `TestEndpointMethodPolicyBoundsDeadlineRetryHedgingAndWork`.
  - **TEST MATRIX:** `PRIMARY=TestEndpointMethodPolicyBoundsDeadlineRetryHedgingAndWork`; `PROPERTY=TestTodo_ENDPOINT_007_Property`; `RACE=TestTodo_ENDPOINT_007_Race`; `INTEGRATION=TestTodo_ENDPOINT_007_Integration`; `FAULT=TestTodo_ENDPOINT_007_Fault`; `SECURITY=TestTodo_ENDPOINT_007_Security`; `CONFORMANCE=TestTodo_ENDPOINT_007_Conformance`; `BENCHMARK=BenchmarkTodo_ENDPOINT_007`.
  - **RED:** method waits without cap, client expands server deadline/budget, cancellation is reported as rollback after commit, write/effect method is hedged, retry storm defeats tenant fairness or gRPC/HTTP use different timeout/retry classification.
  - **GREEN:** generated method policy caps deadline, payload/work/concurrency, propagates cancellation only to declared safe boundary, permits retries/hedging solely by effect/idempotency class and returns identical typed committed/unknown/cancelled outcomes across transports.
  - **REFACTOR:** one policy compiler emits gRPC service config, HTTP middleware and test vectors from capability metadata.
  - **Refs:** [endpoint manifest](specs/http-grpc-endpoint-contract.md#endpoint-manifest), [RPC edge](#59-adversarial-infrastructure-edges-and-third-party-tool-qualification), [admission](#34-batch-scheduling-and-connector-execution-substrate).

- [x] `ENDPOINT-008` **[GATE_A][SOL_HIGH] Build the reusable endpoint TDD and transport-parity harness.**
  - **Evidence (2026-09-05):** `TestEndpointHarnessDetectsTransportSemanticDivergence` in `internal/transport/endpoint` (one versioned vector runs direct capability/application, in-memory gRPC with production interceptors and real grpcbridge HTTP; it compares canonical request/result/error/evidence digests plus exact durable side-effect counts and kills each seeded transport mutant; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/transport/endpoint/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ENDPOINT-002`–`ENDPOINT-007`, `PROTO-006`, `TOOL-014`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove every public method through direct owner, real interceptor gRPC and grpcbridge HTTP paths with identical semantic outcomes`.
  - **TEST:** `TestEndpointHarnessDetectsTransportSemanticDivergence`.
  - **TEST MATRIX:** `PRIMARY=TestEndpointHarnessDetectsTransportSemanticDivergence`; `PROPERTY=TestTodo_ENDPOINT_008_Property`; `GOLDEN=TestTodo_ENDPOINT_008_Golden`; `FUZZ=FuzzTodo_ENDPOINT_008`; `RACE=TestTodo_ENDPOINT_008_Race`; `INTEGRATION=TestTodo_ENDPOINT_008_Integration`; `FAULT=TestTodo_ENDPOINT_008_Fault`; `SECURITY=TestTodo_ENDPOINT_008_Security`; `CONFORMANCE=TestTodo_ENDPOINT_008_Conformance`; `MUTATION=TestTodo_ENDPOINT_008_Mutation`.
  - **RED:** seeded divergence in presence, trusted context, validation, status/details, idempotency, persisted rows/events/outbox/work/effects, evidence, redaction, deadline or cancellation is missed because tests compare only HTTP/gRPC status.
  - **GREEN:** one versioned vector runs direct capability/application, in-memory gRPC with production interceptors and real grpcbridge HTTP; it compares canonical request/result/error/evidence digests plus exact durable side-effect counts and kills each seeded transport mutant.
  - **REFACTOR:** endpoint-specific tests provide fixtures and semantic oracles; harness owns transport mechanics and diagnostics.
  - **Refs:** [required endpoint tests](specs/http-grpc-endpoint-contract.md#required-test-layers-for-every-endpoint), [grpcbridge parity](#29-machine-readable-model-sources-and-public-api-contracts), [test doctrine](#secondary-test-taxonomy-and-required-return-contracts).

- [x] `ENDPOINT-009` **[P0][SOL_HIGH] Gate every contracted intent/capability on an explicit endpoint disposition.**
  - **Depends:** `ENDPOINT-001`, `ENDPOINT-008`, `INTENT-010`, `WF-DISC-012`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=prevent implemented behavior from acquiring an accidental route or remaining unreachable without a reviewed channel decision`.
  - **TEST:** `TestContractedIntentAndCapabilityEndpointDispositionIsTotal`.
  - **TEST MATRIX:** `PRIMARY=TestContractedIntentAndCapabilityEndpointDispositionIsTotal`; `PROPERTY=TestTodo_ENDPOINT_009_Property`; `GOLDEN=TestTodo_ENDPOINT_009_Golden`; `SECURITY=TestTodo_ENDPOINT_009_Security`; `CONFORMANCE=TestTodo_ENDPOINT_009_Conformance`; `MUTATION=TestTodo_ENDPOINT_009_Mutation`.
  - **RED:** `CONTRACTED|IMPLEMENTED|VERIFIED` feature has zero/multiple unjustified public routes, endpoint lacks feature-intent coverage, database entity creates CRUD route, `NO_ENDPOINT` has no reason or internal/event-only capability is publicly reachable.
  - **GREEN:** exact join assigns `TYPED_PUBLIC_METHOD|GENERIC_INTENT_LIFECYCLE_ONLY|INTERNAL_CAPABILITY_ONLY|EVENT_OR_SCHEDULE_ONLY|NO_ENDPOINT_WITH_JUSTIFICATION`, reports baseline/extensions separately and blocks maturity on every missing, duplicate or incompatible disposition.
  - **REFACTOR:** generate reverse indexes from intent/capability/feature to methods and remove manually counted endpoint inventories.
  - **Refs:** [endpoint expansion rule](specs/http-grpc-endpoint-contract.md#expansion-rule), [feature-intent coverage](#6-businessintent-capabilities-governance-and-transaction-integrity), [workflow convergence](#58-businessintent-to-workflow-design-convergence).
  - **Evidence (2026-09-03):** `TestContractedIntentAndCapabilityEndpointDispositionIsTotal` in `internal/transport/manifest` (14 intents + 10 capabilities each classified into one of five disposition categories; missing = RED); PASS; branch plan-revision-2026-09-02; `go test -count=1 ./internal/transport/manifest/...` on windows/arm64 (Go 1.26.3).

- [x] `EP-REG-001` **[GATE_A][SOL_HIGH] Implement authorized RegistryService intent-definition and capability discovery endpoints.**
  - **Evidence (2026-09-05):** `TestRegistryEndpointsReturnAuthorizedVersionedDiscoveryParity` in `internal/transport/endpoint` (four methods return authorized phase/tenant/subject-filtered immutable descriptors with stable cursor, schema/capability/intent versions, effect/risk/simulation metadata and identical canonical digest across transports; hidden and absent resources are non-disclosing; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/transport/endpoint/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ENDPOINT-008`, `API-001`, `INTENT-001`, `CAP-001`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=let authorized humans, UIs, agents and applications discover only currently available semantic actions and immutable definitions`.
  - **TEST:** `TestRegistryEndpointsReturnAuthorizedVersionedDiscoveryParity`.
  - **TEST MATRIX:** `PRIMARY=TestRegistryEndpointsReturnAuthorizedVersionedDiscoveryParity`; `PROPERTY=TestTodo_EP_REG_001_Property`; `GOLDEN=TestTodo_EP_REG_001_Golden`; `FUZZ=FuzzTodo_EP_REG_001`; `INTEGRATION=TestTodo_EP_REG_001_Integration`; `SECURITY=TestTodo_EP_REG_001_Security`; `CONFORMANCE=TestTodo_EP_REG_001_Conformance`; `MUTATION=TestTodo_EP_REG_001_Mutation`.
  - **RED:** `List/GetIntentDefinitions` or `List/GetCapabilities` reveals unavailable tenant feature, unauthorized subject/action, draft/quarantined version, hidden schema/policy, unstable ordering or a different descriptor through HTTP and gRPC.
  - **GREEN:** four methods return authorized phase/tenant/subject-filtered immutable descriptors with stable cursor, schema/capability/intent versions, effect/risk/simulation metadata and identical canonical digest across transports; hidden and absent resources are non-disclosing.
  - **REFACTOR:** global action/search/SDK discovery consume this service rather than separate registries.
  - **Refs:** [initial registry endpoints](specs/http-grpc-endpoint-contract.md#registry-and-discovery), [capability registry](specs/capability-registry-and-lifecycle.md), [action discovery](#17-experience-api-and-accessibility).

- [x] `EP-INTENT-001` **[GATE_A][SOL_HIGH] Implement CreateIntent, GetIntent and ListIntents endpoints.**
  - **Evidence (2026-09-05):** `TestIntentCreateGetListEndpointsPreserveTypedOriginLifecycleAndParity` in `internal/transport/endpoint` (create stores one typed CREATED|DRAFT IntentInstance with trusted origin and zero domain/effect mutation; exact replay returns it; get/list return authorized multidimensional state, versions, relationships and stable cursor with identical gRPC/HTTP result/evidence; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/transport/endpoint/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ENDPOINT-008`, `INTENT-001`–`INTENT-003`, `INTENT-012`, `PROTO-002`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=create and inspect typed root/child intents without accepting caller-derived truth or leaking lifecycle state`.
  - **TEST:** `TestIntentCreateGetListEndpointsPreserveTypedOriginLifecycleAndParity`.
  - **TEST MATRIX:** `PRIMARY=TestIntentCreateGetListEndpointsPreserveTypedOriginLifecycleAndParity`; `PROPERTY=TestTodo_EP_INTENT_001_Property`; `GOLDEN=TestTodo_EP_INTENT_001_Golden`; `FUZZ=FuzzTodo_EP_INTENT_001`; `RACE=TestTodo_EP_INTENT_001_Race`; `INTEGRATION=TestTodo_EP_INTENT_001_Integration`; `FAULT=TestTodo_EP_INTENT_001_Fault`; `SECURITY=TestTodo_EP_INTENT_001_Security`; `CONFORMANCE=TestTodo_EP_INTENT_001_Conformance`.
  - **RED:** create accepts unknown/unavailable definition, unregistered payload type, client principal/tenant/current facts, invalid parent authority or duplicate request; read/list conflates lifecycle dimensions, leaks hidden intent, changes ordering or returns different revision/digest by transport.
  - **GREEN:** create stores one typed `CREATED|DRAFT` IntentInstance with trusted origin and zero domain/effect mutation; exact replay returns it; get/list return authorized multidimensional state, versions, relationships and stable cursor with identical gRPC/HTTP result/evidence.
  - **REFACTOR:** typed domain façades call the same create application service; list/read use owned projections, never workflow tables directly.
  - **Refs:** [intent endpoints](specs/http-grpc-endpoint-contract.md#businessintent-lifecycle), [BusinessIntent](specs/business-intent-and-change-request.md), [intent creation](#6-businessintent-capabilities-governance-and-transaction-integrity).

- [x] `EP-INTENT-002` **[GATE_A][SOL_HIGH] Implement the no-effect SimulateIntent endpoint.**
  - **Evidence (2026-09-05):** `TestSimulateIntentEndpointReturnsCompleteContractWithZeroEffects` in `internal/transport/endpoint` (endpoint returns immutable WorkflowSimulationContract with exact source/config/rule/authority versions and typed completeness/risk/result; database/provider assertions prove zero authoritative/effect rows and equivalent transport requests return the same digest; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/transport/endpoint/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `EP-INTENT-001`, `INTENT-023`, `WF-RUN-012`, `PROMO-004`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=return a version-pinned proposed outcome, reads/writes/effects/approvals/obligations/cost/repair plan without execution authority`.
  - **TEST:** `TestSimulateIntentEndpointReturnsCompleteContractWithZeroEffects`.
  - **TEST MATRIX:** `PRIMARY=TestSimulateIntentEndpointReturnsCompleteContractWithZeroEffects`; `PROPERTY=TestTodo_EP_INTENT_002_Property`; `GOLDEN=TestTodo_EP_INTENT_002_Golden`; `FUZZ=FuzzTodo_EP_INTENT_002`; `RACE=TestTodo_EP_INTENT_002_Race`; `INTEGRATION=TestTodo_EP_INTENT_002_Integration`; `FAULT=TestTodo_EP_INTENT_002_Fault`; `SECURITY=TestTodo_EP_INTENT_002_Security`; `CONFORMANCE=TestTodo_EP_INTENT_002_Conformance`; `MUTATION=TestTodo_EP_INTENT_002_Mutation`.
  - **RED:** simulation performs domain write, external request, message, WorkItem, timer or committed outbox; omits partial/stale/unknown input, authority, conflicts, costs, obligations, invalidators or repair; replay returns changed result without new pinned snapshot/digest.
  - **GREEN:** endpoint returns immutable `WorkflowSimulationContract` with exact source/config/rule/authority versions and typed completeness/risk/result; database/provider assertions prove zero authoritative/effect rows and equivalent transport requests return the same digest.
  - **REFACTOR:** endpoint delegates to execution-mode-aware capability/workflow simulation; it contains no domain-specific calculator.
  - **Refs:** [simulation endpoint](specs/http-grpc-endpoint-contract.md#businessintent-lifecycle), [workflow simulation](specs/workflow-runtime.md), [Promotion simulation](reference-workflows/promote-into-management.md).

- [ ] `EP-INTENT-003` **[GATE_B][SOL_HIGH] Implement SubmitIntent, CancelIntent and SupersedeIntent lifecycle endpoints.**
  - **Depends:** `EP-INTENT-001`, `INTENT-005`, `INTENT-015`, `INTENT-021`, `WF-RUN-010`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=bind an immutable proposal for execution or create governed cancellation/successor instructions without rewriting history`.
  - **TEST:** `TestIntentSubmitCancelSupersedeEndpointsRespectRevisionAuthorityAndIrreversibility`.
  - **TEST MATRIX:** `PRIMARY=TestIntentSubmitCancelSupersedeEndpointsRespectRevisionAuthorityAndIrreversibility`; `PROPERTY=TestTodo_EP_INTENT_003_Property`; `GOLDEN=TestTodo_EP_INTENT_003_Golden`; `RACE=TestTodo_EP_INTENT_003_Race`; `INTEGRATION=TestTodo_EP_INTENT_003_Integration`; `FAULT=TestTodo_EP_INTENT_003_Fault`; `SECURITY=TestTodo_EP_INTENT_003_Security`; `CONFORMANCE=TestTodo_EP_INTENT_003_Conformance`; `MUTATION=TestTodo_EP_INTENT_003_Mutation`.
  - **RED:** submit accepts stale/unapproved proposal or starts twice; cancel implies rollback after irreversible effect, deletes history or races commit incorrectly; supersede mutates original, broadens authority or loses child/effect lineage.
  - **GREEN:** submit binds exact proposal/governance/approval revision and starts once; cancel returns `CANCELLED|CANCELLATION_PENDING|TOO_LATE|REPAIR_REQUIRED` truthfully; supersede creates an authorized successor relationship and preserves every prior state/evidence; HTTP/gRPC agree exactly.
  - **REFACTOR:** all three call intent lifecycle capabilities and workflow safe-point contracts; transport owns no state transition.
  - **Refs:** [intent lifecycle endpoints](specs/http-grpc-endpoint-contract.md#businessintent-lifecycle), [intent lifecycle](specs/business-intent-and-change-request.md), [workflow cancellation](specs/workflow-runtime.md).

- [x] `EP-INTENT-004` **[GATE_A][SOL_HIGH] Implement ExplainIntent and ListIntentTimeline endpoints.**
  - **Evidence (2026-09-05):** `TestIntentExplanationAndTimelineEndpointsPreserveAuthorityProvenanceAndRedaction` in `internal/transport/endpoint` (purpose/field-authorized output pins effective/known time, facts/observations/claims/inferences, decision traces and redactions; timeline preserves append-only business chronology and independent completion dimensions with stable cursor/digest across transports; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/transport/endpoint/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `EP-INTENT-001`, `LEDGER-006`, `MODEL-020`, `TRUST-010`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=explain governed decisions and chronology without fabricating causality or exposing restricted evidence`.
  - **TEST:** `TestIntentExplanationAndTimelineEndpointsPreserveAuthorityProvenanceAndRedaction`.
  - **TEST MATRIX:** `PRIMARY=TestIntentExplanationAndTimelineEndpointsPreserveAuthorityProvenanceAndRedaction`; `PROPERTY=TestTodo_EP_INTENT_004_Property`; `GOLDEN=TestTodo_EP_INTENT_004_Golden`; `INTEGRATION=TestTodo_EP_INTENT_004_Integration`; `SECURITY=TestTodo_EP_INTENT_004_Security`; `CONFORMANCE=TestTodo_EP_INTENT_004_Conformance`; `MUTATION=TestTodo_EP_INTENT_004_Mutation`.
  - **RED:** explanation states inference/observation as fact, omits rule/source/version/unknown, exposes medical/compensation/confidential actor data or changes with current policy; timeline orders by ingestion only, overwrites correction or labels external consistency as business failure.
  - **GREEN:** purpose/field-authorized output pins effective/known time, facts/observations/claims/inferences, decision traces and redactions; timeline preserves append-only business chronology and independent completion dimensions with stable cursor/digest across transports.
  - **REFACTOR:** compose ledger/provenance/governance projections; endpoint does not reconstruct truth ad hoc.
  - **Refs:** [intent inspection endpoints](specs/http-grpc-endpoint-contract.md#businessintent-lifecycle), [provenance](specs/provenance-graph-and-lineage.md), [ledger](specs/transaction-ledger-reconciliation-and-repair.md).

- [x] `EP-PROMO-001` **[GATE_A][SOL_HIGH] Implement the no-effect typed ProposeIntoManagement gRPC and HTTP endpoint.**
  - **Evidence (2026-09-05):** `TestProposeIntoManagementEndpointAcceptsOnlyIntentAndResolvesServerTruth` in `internal/transport/journey` (typed request accepts worker, desired job/position/org/manager/compensation/effective date/reason/expected revision/client id; server resolves facts and creates exactly one PromoteWorker intent/change request plus immutable snapshot/simulation/proposal candidates with zero author; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/transport/journey/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ENDPOINT-008`, `PROMO-007`, `PROMO-001`–`PROMO-004`, `INTENT-004`.
  - **INTENT CONTEXT:** `ROLE=DIRECT; SETS=BI.PEOPLE,BI.REWARDS; INTENTS=PromoteWorker; KERNEL=CHANGE_REQUEST; WHY=provide the first typed semantic endpoint that creates a governed management-promotion intent from desired outcome only`.
  - **TEST:** `TestProposeIntoManagementEndpointAcceptsOnlyIntentAndResolvesServerTruth`.
  - **TEST MATRIX:** `PRIMARY=TestProposeIntoManagementEndpointAcceptsOnlyIntentAndResolvesServerTruth`; `PROPERTY=TestTodo_EP_PROMO_001_Property`; `GOLDEN=TestTodo_EP_PROMO_001_Golden`; `FUZZ=FuzzTodo_EP_PROMO_001`; `RACE=TestTodo_EP_PROMO_001_Race`; `INTEGRATION=TestTodo_EP_PROMO_001_Integration`; `FAULT=TestTodo_EP_PROMO_001_Fault`; `SECURITY=TestTodo_EP_PROMO_001_Security`; `CONFORMANCE=TestTodo_EP_PROMO_001_Conformance`; `MUTATION=TestTodo_EP_PROMO_001_Mutation`.
  - **RED:** request can supply current salary/manager, vacancy, budget, authority, principal or tenant; stale worker revision is ignored; endpoint mutates domains, bypasses capability gateway, creates multiple intents on replay or differs from generic IntentService lifecycle semantics.
  - **GREEN:** typed request accepts worker, desired job/position/org/manager/compensation/effective date/reason/expected revision/client id; server resolves facts and creates exactly one `PromoteWorker` intent/change request plus immutable snapshot/simulation/proposal candidates with zero authoritative mutation and identical HTTP/gRPC digest.
  - **REFACTOR:** promotion façade only maps typed input to `promotion.propose`; generic lifecycle endpoints own subsequent simulate/submit/inspect operations.
  - **Refs:** [Promotion endpoint](specs/http-grpc-endpoint-contract.md#gate-b-promotion-slice), [Promotion workflow](reference-workflows/promote-into-management.md), [intent-only request](#5-phase-1-people-organization-position-compensation-and-budget-domains).

- [ ] `EP-WORK-001` **[GATE_B][SOL_HIGH] Implement ListWorkItems and GetWorkItem endpoints.**
  - **Depends:** `ENDPOINT-008`, `WORK-001`, `WORK-002`, `WORK-007`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.WORK,BI.ALL; DIRECT=none; WHY=let authorized assignees and delegates inspect bounded human responsibility without evidence-compartment or queue leakage`.
  - **TEST:** `TestWorkItemReadEndpointsEnforceAssignmentQueueAndEvidenceVisibility`.
  - **TEST MATRIX:** `PRIMARY=TestWorkItemReadEndpointsEnforceAssignmentQueueAndEvidenceVisibility`; `PROPERTY=TestTodo_EP_WORK_001_Property`; `GOLDEN=TestTodo_EP_WORK_001_Golden`; `RACE=TestTodo_EP_WORK_001_Race`; `INTEGRATION=TestTodo_EP_WORK_001_Integration`; `SECURITY=TestTodo_EP_WORK_001_Security`; `CONFORMANCE=TestTodo_EP_WORK_001_Conformance`; `MUTATION=TestTodo_EP_WORK_001_Mutation`.
  - **RED:** queue membership leaks other tenants/tasks/counts, stale/delegated/recused principal sees item, manager receives restricted evidence, list ordering/deadline is unstable or get/list show different revision/action set by transport.
  - **GREEN:** authorized queue snapshot and item detail return current version/claim/deadline/proposal digest, classification-filtered context and server-computed permitted actions using signed stable cursors; prohibited/absent remains non-disclosing.
  - **REFACTOR:** use Human Work projections and evidence views; endpoints never query workflow/domain tables directly.
  - **Refs:** [work endpoints](specs/http-grpc-endpoint-contract.md#human-work-and-approvals), [human work](specs/human-work-forms-and-rules.md), [work visibility](#24-human-decision-safety-and-continuity).

- [ ] `EP-WORK-002` **[GATE_B][SOL_HIGH] Implement ClaimWorkItem and ReleaseWorkItem endpoints.**
  - **Depends:** `EP-WORK-001`, `WORK-003`, `WORK-004`, `ENDPOINT-004`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.WORK; DIRECT=none; WHY=expose exclusive human-work ownership changes under version, lease, delegation and current authorization`.
  - **TEST:** `TestWorkItemClaimReleaseEndpointsAreExclusiveVersionBoundAndIdempotent`.
  - **TEST MATRIX:** `PRIMARY=TestWorkItemClaimReleaseEndpointsAreExclusiveVersionBoundAndIdempotent`; `PROPERTY=TestTodo_EP_WORK_002_Property`; `RACE=TestTodo_EP_WORK_002_Race`; `INTEGRATION=TestTodo_EP_WORK_002_Integration`; `FAULT=TestTodo_EP_WORK_002_Fault`; `SECURITY=TestTodo_EP_WORK_002_Security`; `CONFORMANCE=TestTodo_EP_WORK_002_Conformance`; `MUTATION=TestTodo_EP_WORK_002_Mutation`.
  - **RED:** two principals/transports claim exclusive item, stale revision steals/releases claim, unauthorized delegate claims, expired lease completes release as current or ambiguous response produces duplicate assignment history.
  - **GREEN:** atomic CAS creates/releases one versioned claim/lease for current authorized principal/delegate, exact replay returns original result, losers receive safe conflict/current-action data and assignment evidence remains append-only.
  - **REFACTOR:** endpoint delegates to Human Work claim service and shared idempotency; no transport mutex or session ownership.
  - **Refs:** [work endpoints](specs/http-grpc-endpoint-contract.md#human-work-and-approvals), [claim semantics](specs/human-work-forms-and-rules.md#claim-reassignment-delegation-and-escalation), [endpoint concurrency](specs/http-grpc-endpoint-contract.md#common-wire-and-http-behavior).

- [ ] `EP-WORK-003` **[GATE_B][SOL_HIGH] Implement CompleteWorkItem and DecideApproval endpoints.**
  - **Depends:** `EP-WORK-002`, `WORK-006`, `APPROVAL-001`–`APPROVAL-006`, `INTENT-006`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.WORK,BI.ALL; DIRECT=none; WHY=record authorized human task output or approval decision against the exact work/proposal/evidence revision`.
  - **TEST:** `TestWorkCompletionAndApprovalEndpointsRejectStaleProposalAuthorityAndDuplicateDecision`.
  - **TEST MATRIX:** `PRIMARY=TestWorkCompletionAndApprovalEndpointsRejectStaleProposalAuthorityAndDuplicateDecision`; `PROPERTY=TestTodo_EP_WORK_003_Property`; `GOLDEN=TestTodo_EP_WORK_003_Golden`; `RACE=TestTodo_EP_WORK_003_Race`; `INTEGRATION=TestTodo_EP_WORK_003_Integration`; `FAULT=TestTodo_EP_WORK_003_Fault`; `SECURITY=TestTodo_EP_WORK_003_Security`; `CONFORMANCE=TestTodo_EP_WORK_003_Conformance`; `MUTATION=TestTodo_EP_WORK_003_Mutation`.
  - **RED:** stale task/proposal digest, expired session/claim/delegation, changed manager/SoD, missing required evidence or duplicate/mutated decision completes; restricted evidence appears in response; approval itself executes the business change.
  - **GREEN:** method rechecks identity/assurance/AuthZ/claim/proposal/decision receipt/SoD, appends exactly one typed completion/approval result and signal, returns current work/intent state/evidence and performs zero domain mutation until workflow/transaction execution.
  - **REFACTOR:** approval is a specialized WorkItem result; both methods share current-authority and signal publication mechanics.
  - **Refs:** [approval endpoint](specs/http-grpc-endpoint-contract.md#human-work-and-approvals), [human decisions](specs/human-work-forms-and-rules.md), [approval safety](#24-human-decision-safety-and-continuity).

- [x] `EP-WF-001` **[GATE_A][SOL_HIGH] Implement GetWorkflow and ListNodeExecutions inspection endpoints.**
  - **Evidence (2026-09-05):** `TestWorkflowInspectionEndpointsReturnAuthorizedConsistentExecutionView` in `internal/transport/cell`, `internal/transport/workflow` (authorized projection pins instance/definition/proposal versions and watermark, returns redacted frontier/nodes/attempts/waits/signals/errors/repairs/evidence with stable pagination and identical transport digest; unavailable projection is explicit; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/transport/cell/ ./internal/transport/workflow/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ENDPOINT-008`, `WF-RUN-019`, `ADMIN-002`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.OPERATIONS,BI.DATAOPS; DIRECT=none; WHY=inspect durable execution truth and multidimensional completion without exposing raw runtime persistence`.
  - **TEST:** `TestWorkflowInspectionEndpointsReturnAuthorizedConsistentExecutionView`.
  - **TEST MATRIX:** `PRIMARY=TestWorkflowInspectionEndpointsReturnAuthorizedConsistentExecutionView`; `PROPERTY=TestTodo_EP_WF_001_Property`; `GOLDEN=TestTodo_EP_WF_001_Golden`; `RACE=TestTodo_EP_WF_001_Race`; `INTEGRATION=TestTodo_EP_WF_001_Integration`; `SECURITY=TestTodo_EP_WF_001_Security`; `CONFORMANCE=TestTodo_EP_WF_001_Conformance`; `MUTATION=TestTodo_EP_WF_001_Mutation`.
  - **RED:** response conflates runtime/business/external/reconciliation/obligation/operational dimensions, exposes raw payload/secret/restricted task, reads inconsistent instance/node frontier or permits tenant/support enumeration.
  - **GREEN:** authorized projection pins instance/definition/proposal versions and watermark, returns redacted frontier/nodes/attempts/waits/signals/errors/repairs/evidence with stable pagination and identical transport digest; unavailable projection is explicit.
  - **REFACTOR:** inspector projection remains rebuildable and non-authoritative; endpoint never reads mutable tables into a fabricated snapshot.
  - **Refs:** [workflow endpoints](specs/http-grpc-endpoint-contract.md#workflow-operations), [runtime inspector](specs/workflow-runtime.md), [operator surface](#36-operator-surfaces-ownership-and-measurable-production-limits).

- [ ] `EP-WF-002` **[GATE_B][SOL_HIGH] Implement governed Pause, Resume, Cancel and RetryNode workflow endpoints.**
  - **Depends:** `EP-WF-001`, `WF-RUN-008`, `WF-RUN-010`, `WF-RUN-015`, `INTENT-022`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.OPERATIONS; DIRECT=none; WHY=turn operator workflow interventions into governed capabilities/intents rather than direct runtime-row edits`.
  - **TEST:** `TestWorkflowControlEndpointsRespectSafePointAuthorityIdempotencyAndEffectBoundary`.
  - **TEST MATRIX:** `PRIMARY=TestWorkflowControlEndpointsRespectSafePointAuthorityIdempotencyAndEffectBoundary`; `PROPERTY=TestTodo_EP_WF_002_Property`; `RACE=TestTodo_EP_WF_002_Race`; `INTEGRATION=TestTodo_EP_WF_002_Integration`; `FAULT=TestTodo_EP_WF_002_Fault`; `SECURITY=TestTodo_EP_WF_002_Security`; `CONFORMANCE=TestTodo_EP_WF_002_Conformance`; `RECOVERY=TestTodo_EP_WF_002_Recovery`; `MUTATION=TestTodo_EP_WF_002_Mutation`.
  - **RED:** endpoint directly edits status, pause crosses unsafe commit, resume skips revalidation, cancel claims reversal after irreversible effect, RetryNode targets successful/currently leased/non-idempotent node or repeated call creates duplicate execution/effect.
  - **GREEN:** each method creates/evaluates the registered operational action, checks support/JIT/SoD/expected version/safe point/current authority, returns `APPLIED|PENDING_SAFE_POINT|DENIED|TOO_LATE|REPAIR_REQUIRED` and appends one intervention/evidence trail with exact runtime transition.
  - **REFACTOR:** endpoint maps to workflow intervention contracts; operators cannot submit arbitrary target state.
  - **Refs:** [workflow operations](specs/http-grpc-endpoint-contract.md#workflow-operations), [workflow runtime](specs/workflow-runtime.md), [operator intents](#6-businessintent-capabilities-governance-and-transaction-integrity).

- [ ] `EP-EVID-001` **[GATE_B][SOL_HIGH] Implement GetExecutionReceipt and ExportIntentEvidence endpoints.**
  - **Depends:** `ENDPOINT-008`, `EVIDENCE-001`, `LEDGER-012`, `ADMIN-007`, `EXPORT-001`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.DATAOPS,BI.PRIVACY,BI.OPERATIONS; DIRECT=none; WHY=retrieve or generate minimum-necessary verifiable intent execution evidence without ad hoc database export`.
  - **TEST:** `TestEvidenceEndpointsEnforcePurposeRedactionManifestAndOperationSemantics`.
  - **TEST MATRIX:** `PRIMARY=TestEvidenceEndpointsEnforcePurposeRedactionManifestAndOperationSemantics`; `PROPERTY=TestTodo_EP_EVID_001_Property`; `GOLDEN=TestTodo_EP_EVID_001_Golden`; `RACE=TestTodo_EP_EVID_001_Race`; `INTEGRATION=TestTodo_EP_EVID_001_Integration`; `FAULT=TestTodo_EP_EVID_001_Fault`; `SECURITY=TestTodo_EP_EVID_001_Security`; `CONFORMANCE=TestTodo_EP_EVID_001_Conformance`; `MUTATION=TestTodo_EP_EVID_001_Mutation`.
  - **RED:** receipt/export reveals unauthorized fields/existence, omits intent→proposal→approval→transaction→observation→reconciliation lineage, uses ambient current data, creates unbounded synchronous archive, formula-injects spreadsheet output or unsigned/tampered package verifies.
  - **GREEN:** receipt returns authorized immutable summary/digest; export creates one idempotent long-running operation binding purpose/scope/fields/watermarks/redaction/format/expiry and produces encrypted signed offline-verifiable package with exact exclusions and zero source mutation.
  - **REFACTOR:** evidence owner composes registered sources; transport streams only authorized artifact/operation references.
  - **Refs:** [evidence endpoints](specs/http-grpc-endpoint-contract.md#evidence-and-long-running-operations), [ledger evidence](specs/transaction-ledger-reconciliation-and-repair.md), [evidence export](specs/platform-responsibility-boundaries.md#audit-evidence-packaging).

- [x] `EP-OPS-001` **[GATE_A][SOL_HIGH] Implement GetOperation and CancelOperation endpoints for long-running work.**
  - **Evidence (2026-09-05):** `TestLongRunningOperationEndpointsPreserveStateResultErrorAndCancellationTruth` in `internal/transport/operations` (operation resource returns immutable owner/request/type/create/update, PENDING|RUNNING|SUCCEEDED|FAILED|CANCELLATION_REQUESTED|CANCELLED, progress/completeness, typed result/error/evidence; cancellation is idempotent, policy/safe-boundary aware and recovery preserves state; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/transport/operations/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ENDPOINT-008`, `PROTO-007`, `SVC-011`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=inspect and request cancellation of resumable long-running endpoint work without equating transport lifetime with business completion`.
  - **TEST:** `TestLongRunningOperationEndpointsPreserveStateResultErrorAndCancellationTruth`.
  - **TEST MATRIX:** `PRIMARY=TestLongRunningOperationEndpointsPreserveStateResultErrorAndCancellationTruth`; `PROPERTY=TestTodo_EP_OPS_001_Property`; `GOLDEN=TestTodo_EP_OPS_001_Golden`; `RACE=TestTodo_EP_OPS_001_Race`; `INTEGRATION=TestTodo_EP_OPS_001_Integration`; `FAULT=TestTodo_EP_OPS_001_Fault`; `SECURITY=TestTodo_EP_OPS_001_Security`; `CONFORMANCE=TestTodo_EP_OPS_001_Conformance`; `RECOVERY=TestTodo_EP_OPS_001_Recovery`.
  - **RED:** losing HTTP/gRPC connection cancels durable accepted work, polling reveals another principal's operation, cursor/result changes after completion, CancelOperation deletes evidence or reports cancelled after irreversible completion and duplicate cancel creates multiple actions.
  - **GREEN:** operation resource returns immutable owner/request/type/create/update, `PENDING|RUNNING|SUCCEEDED|FAILED|CANCELLATION_REQUESTED|CANCELLED`, progress/completeness, typed result/error/evidence; cancellation is idempotent, policy/safe-boundary aware and recovery preserves state.
  - **REFACTOR:** all long-running endpoint families reuse one owned Operation contract; it does not replace BusinessIntent or Workflow state.
  - **Refs:** [operation endpoints](specs/http-grpc-endpoint-contract.md#evidence-and-long-running-operations), [streaming contract](#29-machine-readable-model-sources-and-public-api-contracts), [workflow truth](specs/workflow-runtime.md).

- [x] `EP-HEALTH-001` **[GATE_A][TERRA] Implement non-disclosing gRPC health, `/healthz` and `/readyz` endpoints per process role.**
  - **Evidence (2026-09-05):** `TestHealthAndReadinessEndpointsDistinguishProcessLifeFromAdmissionReadiness` in `internal/transport/health` (standard gRPC Health plus HTTP /healthz report process-loop life; /readyz reports role-specific admission state from bounded cached dependency/control checks, public output is minimal, internal reasons are authorized telemetry and transitions meet latency/load budgets; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/transport/health/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ENDPOINT-006`, `SVC-002`, `OPS-001`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=provide non-material liveness/readiness signals for safe workload routing without exposing tenant or dependency internals`.
  - **TEST:** `TestHealthAndReadinessEndpointsDistinguishProcessLifeFromAdmissionReadiness`.
  - **TEST MATRIX:** `PRIMARY=TestHealthAndReadinessEndpointsDistinguishProcessLifeFromAdmissionReadiness`; `PROPERTY=TestTodo_EP_HEALTH_001_Property`; `GOLDEN=TestTodo_EP_HEALTH_001_Golden`; `RACE=TestTodo_EP_HEALTH_001_Race`; `INTEGRATION=TestTodo_EP_HEALTH_001_Integration`; `FAULT=TestTodo_EP_HEALTH_001_Fault`; `SECURITY=TestTodo_EP_HEALTH_001_Security`; `CONFORMANCE=TestTodo_EP_HEALTH_001_Conformance`; `BENCHMARK=BenchmarkTodo_EP_HEALTH_001`.
  - **RED:** liveness fails for downstream outage and causes restart loop, readiness reports serving before config/key/schema/role dependencies are usable, public response names tenant/database/provider/secret, check performs business write or health traffic exhausts dependencies.
  - **GREEN:** standard gRPC Health plus HTTP `/healthz` report process-loop life; `/readyz` reports role-specific admission state from bounded cached dependency/control checks, public output is minimal, internal reasons are authorized telemetry and transitions meet latency/load budgets.
  - **REFACTOR:** commands register role-owned readiness predicates; endpoint framework owns protocol and disclosure policy.
  - **Refs:** [non-material endpoints](specs/http-grpc-endpoint-contract.md#non-material-service-endpoints), [process roles](#28-modular-monolith-process-roles-and-go-command-binaries), [operations](#16-operations-assurance-overload-and-recovery).

---

## 61. Every-BusinessIntent maximal vertical-slice convergence

> **Disposition (2026-09-02):** RETIRED as a whole. The 530-slot baseline, 807-name vocabulary and MAX-v1 configuration program have no consumer; the catalog is the fourteen drafted definitions.

These todos were written to turn a numbered 530-slot baseline and an 807-name
vocabulary into vertical-slice contracts. That program was withdrawn on
2026-09-02: the catalog is the fourteen drafted definitions and the intake list
is non-normative vocabulary. Every item below is `[RETIRED]` and kept only for
traceability.

- [ ] `SLICE-001` **[RETIRED][SOL_HIGH] RETIRED 2026-09-02: Define the machine-readable VerticalSliceRecord and expansion contract.**
  - **Disposition:** RETIRED 2026-09-02. The vertical-slice program over the 530-slot baseline and 807-name vocabulary has no consumer. Dependencies on this ID resolve as satisfied; its tests are not written.
  - **Depends:** `WF-DISC-005`, `INTENT-025`, `MODEL-009`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=require every accepted intent to expose its complete request-to-outcome execution and evidence obligations`.
  - **TEST:** `TestVerticalSliceRecordRejectsMissingExecutionConfigurationOrEvidenceDimension`.
  - **TEST MATRIX:** `PRIMARY=TestVerticalSliceRecordRejectsMissingExecutionConfigurationOrEvidenceDimension`; `PROPERTY=TestTodo_SLICE_001_Property`; `GOLDEN=TestTodo_SLICE_001_Golden`; `SECURITY=TestTodo_SLICE_001_Security`; `CONFORMANCE=TestTodo_SLICE_001_Conformance`; `MUTATION=TestTodo_SLICE_001_Mutation`.
  - **RED:** record validates without stable intent/source, owner/family/archetype, MAX profile, initiator/endpoint, trusted boundary, snapshot/data, governance, engines/capabilities, human work, transaction/effects, observation/reconciliation/repair, lifecycle/completion, scenario/tests/todos or phase/evidence; empty value is accepted as not applicable.
  - **GREEN:** SchemaFlux contract requires every dimension, permits only typed `SUPPORTED|PROHIBITED|UNKNOWN|NOT_APPLICABLE(reason,owner)` applicability, emits stable Go/Protobuf/docs and canonical digest, and unknown blocks contracted maturity.
  - **REFACTOR:** generate compact registers and expanded slice documents from one source; prose is never the runtime registry.
  - **Refs:** [vertical-slice program](workflows/vertical-slices/README.md), [workflow design record](workflows/business-intent-workflow-registry.md#complete-high-level-design-contract), [intent definitions](specs/business-intent-catalog.md#required-definition-fields).

- [ ] `SLICE-002` **[RETIRED][SOL_HIGH] RETIRED 2026-09-02: Reconcile the exact 530-slot baseline register to the immutable source manifest.**
  - **Disposition:** RETIRED 2026-09-02. The vertical-slice program over the 530-slot baseline and 807-name vocabulary has no consumer. Dependencies on this ID resolve as satisfied; its tests are not written.
  - **Depends:** `SLICE-001`, `MODEL-008`, `INTENT-025`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=replace 516 source-unbound baseline slots with exact stable identities and provenance without fabricating names`.
  - **TEST:** `TestBaselineVerticalSliceRegisterIsExactGaplessAndSourceBound`.
  - **TEST MATRIX:** `PRIMARY=TestBaselineVerticalSliceRegisterIsExactGaplessAndSourceBound`; `PROPERTY=TestTodo_SLICE_002_Property`; `GOLDEN=TestTodo_SLICE_002_Golden`; `SECURITY=TestTodo_SLICE_002_Security`; `CONFORMANCE=TestTodo_SLICE_002_Conformance`; `MUTATION=TestTodo_SLICE_002_Mutation`.
  - **RED:** register differs from exactly `1..530`, source digest/provenance is absent, known draft number/name changes, one number maps twice, one identity spans numbers without alias decision or `UNBOUND_SOURCE` advances maturity.
  - **GREEN:** generated join has 530 unique stable definition identities with signed source locator/digest, preserves catalog numbering/history, reconciles the 14 current drafts and reports zero unbound/duplicate/unstable rows before slice completeness claims.
  - **REFACTOR:** baseline register is generated from source manifest plus accepted definition registry; do not edit its rows manually.
  - **Refs:** [baseline slice register](workflows/vertical-slices/baseline-vertical-slice-register.md), [catalog truth boundary](specs/business-intent-catalog.md#required-definition-fields), [convergence plan](workflows/vertical-slices/plan.md#stage-0--recover-source-truth).

- [ ] `SLICE-003` **[RETIRED][SOL_HIGH] RETIRED 2026-09-02: Reconcile all repository vocabulary candidates without promoting display names to identity.**
  - **Disposition:** RETIRED 2026-09-02. The vertical-slice program over the 530-slot baseline and 807-name vocabulary has no consumer. Dependencies on this ID resolve as satisfied; its tests are not written.
  - **Depends:** `SLICE-001`, `WF-DISC-006`, `INTENT-025`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=classify every repository-enumerable material action as baseline, alias, extension, event/capability-only or rejected`.
  - **TEST:** `TestVocabularyVerticalSliceRegisterReconcilesEveryCandidateAndOverlap`.
  - **TEST MATRIX:** `PRIMARY=TestVocabularyVerticalSliceRegisterReconcilesEveryCandidateAndOverlap`; `PROPERTY=TestTodo_SLICE_003_Property`; `GOLDEN=TestTodo_SLICE_003_Golden`; `SECURITY=TestTodo_SLICE_003_Security`; `CONFORMANCE=TestTodo_SLICE_003_Conformance`; `MUTATION=TestTodo_SLICE_003_Mutation`.
  - **RED:** one of 807 current names disappears, new catalog/registry name is not detected, the five cross-source overlaps auto-merge, profile/recipe/source locator is absent or a candidate is counted in the 530 baseline without stable definition proof.
  - **GREEN:** generated register exactly joins current detailed/compact sources, preserves all overlaps, assigns reviewed `BASELINE|ALIAS|EXTENSION|EVENT_OR_CAPABILITY|REJECTED` disposition and reports baseline and extension coverage separately.
  - **REFACTOR:** source catalogs own deltas; this register is a generated reverse index rather than another handwritten vocabulary.
  - **Refs:** [vocabulary slice register](workflows/vertical-slices/vocabulary-vertical-slice-register.md), [gap register](workflows/vertical-slices/gap-register.md), [workflow registry](workflows/business-intent-workflow-registry.md).

- [ ] `SLICE-004` **[RETIRED][SOL_HIGH] RETIRED 2026-09-02: Resolve catalog aliases, collisions and cross-domain slice composition explicitly.**
  - **Disposition:** RETIRED 2026-09-02. The vertical-slice program over the 530-slot baseline and 807-name vocabulary has no consumer. Dependencies on this ID resolve as satisfied; its tests are not written.
  - **Depends:** `SLICE-002`, `SLICE-003`, `INTENT-015`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=prevent same-looking names from merging distinct semantics or duplicating one cross-domain intent`.
  - **TEST:** `TestVerticalSliceIdentityResolutionRequiresQualifiedReviewedDisposition`.
  - **TEST MATRIX:** `PRIMARY=TestVerticalSliceIdentityResolutionRequiresQualifiedReviewedDisposition`; `PROPERTY=TestTodo_SLICE_004_Property`; `GOLDEN=TestTodo_SLICE_004_Golden`; `SECURITY=TestTodo_SLICE_004_Security`; `CONFORMANCE=TestTodo_SLICE_004_Conformance`; `MUTATION=TestTodo_SLICE_004_Mutation`.
  - **RED:** display/name similarity merges access/commercial entitlements, PIP/case/performance semantics, legal holds, pay equity or compensation correction; alias changes kernel/input/output/effects, or composite profile loses one owner's data/governance/evidence.
  - **GREEN:** every overlap records stable qualified identities or one canonical definition with alias/profile composition, compatibility/evidence owner and exact rationale; semantic collisions remain separate and no lifecycle history is rewritten.
  - **REFACTOR:** use catalog identity/relationship objects; do not encode collision exceptions in generators.
  - **Refs:** [catalog identity](specs/business-intent-catalog.md#catalog-identity), [known overlaps](workflows/vertical-slices/gap-register.md#cross-catalog-overlaps-requiring-one-stable-identity-decision), [composition](specs/business-intent-catalog.md#composition-rules).

- [ ] `SLICE-005` **[RETIRED][SOL_HIGH] RETIRED 2026-09-02: Compile MAX-v1 applicability for every vertical slice.**
  - **Disposition:** RETIRED 2026-09-02. The vertical-slice program over the 530-slot baseline and 807-name vocabulary has no consumer. Dependencies on this ID resolve as satisfied; its tests are not written.
  - **Depends:** `SLICE-001`, `GOV-018`, `TIME-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=force every intent to consider all supported initiator, authority, legal, privacy, temporal, failure, lifecycle, evidence, experience and scale configurations`.
  - **TEST:** `TestMaximalConfigurationCompilerCoversEveryAxisBoundaryAndMandatoryInteraction`.
  - **TEST MATRIX:** `PRIMARY=TestMaximalConfigurationCompilerCoversEveryAxisBoundaryAndMandatoryInteraction`; `PROPERTY=TestTodo_SLICE_005_Property`; `GOLDEN=TestTodo_SLICE_005_Golden`; `FUZZ=FuzzTodo_SLICE_005`; `SECURITY=TestTodo_SLICE_005_Security`; `CONFORMANCE=TestTodo_SLICE_005_Conformance`; `MUTATION=TestTodo_SLICE_005_Mutation`.
  - **RED:** slice omits an axis/value/boundary, treats ambient default as supported, silently marks a value inapplicable, applies pairwise coverage to a mandatory high-risk triple/quadruple or lacks phase/owner for deferred configuration.
  - **GREEN:** compiler returns supported/prohibited/unknown/N-A disposition for every MAX-v1 value, emits positive/boundary/forbidden/pairwise plus all applicable mandatory interactions, records equivalence proof for excluded combinations and stable applicability digest.
  - **REFACTOR:** domain profiles supply reviewed applicability defaults but each intent delta can narrow/extend only with explicit reason and compatibility impact.
  - **Refs:** [MAX-v1](workflows/vertical-slices/maximal-configuration-profile.md), [test taxonomy](#secondary-test-taxonomy-and-required-return-contracts), [phase depth](execution-plan.md).

- [ ] `SLICE-006` **[RETIRED][SOL_HIGH] RETIRED 2026-09-02: Expand definition, MAX-v1, domain profile, archetype and intent delta into one complete graph.**
  - **Disposition:** RETIRED 2026-09-02. The vertical-slice program over the 530-slot baseline and 807-name vocabulary has no consumer. Dependencies on this ID resolve as satisfied; its tests are not written.
  - **Depends:** `SLICE-004`, `SLICE-005`, `WF-DISC-007`.
  - **INTENT CONTEXT:** `ROLE=ORCHESTRATION; SETS=BI.ALL; DIRECT=none; WHY=produce the full request-to-outcome vertical slice for every accepted definition without handwritten omission`.
  - **TEST:** `TestVerticalSliceExpansionProducesCompleteOrderedRequestToOutcomeGraph`.
  - **TEST MATRIX:** `PRIMARY=TestVerticalSliceExpansionProducesCompleteOrderedRequestToOutcomeGraph`; `PROPERTY=TestTodo_SLICE_006_Property`; `GOLDEN=TestTodo_SLICE_006_Golden`; `FAULT=TestTodo_SLICE_006_Fault`; `SECURITY=TestTodo_SLICE_006_Security`; `CONFORMANCE=TestTodo_SLICE_006_Conformance`; `MUTATION=TestTodo_SLICE_006_Mutation`.
  - **RED:** expansion loses mandatory phase/configuration, delta deletes governance/revalidation/evidence/repair, direct capability gains workflow/effects, child/case/batch truth collapses, graph has unreachable failure/closure route or repeated generation changes ordering/digest.
  - **GREEN:** deterministic graph contains every slice dimension, inherited/added/omitted responsibility provenance, typed nodes/edges/data/effects/invalidators/completion/configuration and stable digest; unknown owner/reference blocks `SLICE_CONTRACTED`.
  - **REFACTOR:** keep exploratory slice expansion distinct from executable workflow compilation and publication.
  - **Refs:** [slice expansion](workflows/vertical-slices/README.md#slice-expansion), [archetypes](workflows/_shared/workflow-archetypes.md), [convergence plan](workflows/vertical-slices/plan.md).

- [ ] `SLICE-007` **[RETIRED][SOL_HIGH] RETIRED 2026-09-02: Resolve every expanded slice to exact entity properties, engines, capabilities and source authority.**
  - **Disposition:** RETIRED 2026-09-02. The vertical-slice program over the 530-slot baseline and 807-name vocabulary has no consumer. Dependencies on this ID resolve as satisfied; its tests are not written.
  - **Depends:** `SLICE-006`, `MODEL-021`, `ENGINE-COVERAGE-001`, `DB-COVERAGE-001`, `CAP-002`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=show the exact data and reusable/domain computation required by every intent and expose ownerless semantics`.
  - **TEST:** `TestVerticalSliceDataEngineCapabilityAuthorityClosureIsExact`.
  - **TEST MATRIX:** `PRIMARY=TestVerticalSliceDataEngineCapabilityAuthorityClosureIsExact`; `PROPERTY=TestTodo_SLICE_007_Property`; `GOLDEN=TestTodo_SLICE_007_Golden`; `SECURITY=TestTodo_SLICE_007_Security`; `CONFORMANCE=TestTodo_SLICE_007_Conformance`; `MUTATION=TestTodo_SLICE_007_Mutation`.
  - **RED:** slice references entity without property-level read/write/effect/freshness/temporal/classification/authority, embeds reusable calculation in workflow, uses unregistered capability, assumes physical system ownership or lacks storage/non-storage disposition.
  - **GREEN:** each reference resolves to one versioned semantic owner and exact property/authority/snapshot/engine/capability contract; missing references emit atomic owner-keyed findings and reverse indexes show every consuming slice.
  - **REFACTOR:** deduplicate shared computation only after equivalent semantics are proven; domain invariants stay domain-owned.
  - **Refs:** [intent entity coverage](data/models/intent-coverage-matrix.md), [model registry](data/models/registry-and-coverage-contracts.md), [gap register](workflows/vertical-slices/gap-register.md).

- [ ] `SLICE-008` **[RETIRED][SOL_HIGH] RETIRED 2026-09-02: Resolve maximal governance, human-decision and continuity requirements per slice.**
  - **Disposition:** RETIRED 2026-09-02. The vertical-slice program over the 530-slot baseline and 807-name vocabulary has no consumer. Dependencies on this ID resolve as satisfied; its tests are not written.
  - **Depends:** `SLICE-006`, `GOVERN-002`, `WORK-001`, `TRUST-010`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=make AuthZ/legal/privacy/risk/obligations and human decision rights explicit for every configuration`.
  - **TEST:** `TestVerticalSliceGovernanceAndHumanDecisionMatrixHasNoImplicitAuthority`.
  - **TEST MATRIX:** `PRIMARY=TestVerticalSliceGovernanceAndHumanDecisionMatrixHasNoImplicitAuthority`; `PROPERTY=TestTodo_SLICE_008_Property`; `GOLDEN=TestTodo_SLICE_008_Golden`; `FAULT=TestTodo_SLICE_008_Fault`; `SECURITY=TestTodo_SLICE_008_Security`; `CONFORMANCE=TestTodo_SLICE_008_Conformance`; `MUTATION=TestTodo_SLICE_008_Mutation`.
  - **RED:** initiator/channel/configuration inherits broader authority, eligibility/legal truth is delegated to manager preference, sensitive evidence crosses compartment, decision omits representation/delegation/recusal/SoD/expiry/appeal or accessibility/manual continuity loses deadline/evidence.
  - **GREEN:** slice resolves specialist decisions into composed allow/deny/restriction/obligation with exact input/version/explanation, assigns permitted human decision rights and continuity routes per configuration and records revalidation/invalidation triggers.
  - **REFACTOR:** shared governance/human-work mechanics remain generic; domain/legal decision semantics stay with their owners.
  - **Refs:** [governance composition](specs/governance-decision-and-obligation-composition.md), [human work](specs/human-work-forms-and-rules.md), [MAX-v1](workflows/vertical-slices/maximal-configuration-profile.md).

- [ ] `SLICE-009` **[RETIRED][SOL_HIGH] RETIRED 2026-09-02: Resolve transaction, external-effect, observation, reconciliation and repair closure per slice.**
  - **Disposition:** RETIRED 2026-09-02. The vertical-slice program over the 530-slot baseline and 807-name vocabulary has no consumer. Dependencies on this ID resolve as satisfied; its tests are not written.
  - **Depends:** `SLICE-006`, `TX-003`, `EFFECT-001`, `RECON-002`, `REPAIR-002`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=show exactly what changes locally or externally and how every configured outcome becomes observable, reconcilable and repairable`.
  - **TEST:** `TestVerticalSliceEffectAndOutcomeClosureHandlesPartialAmbiguousAndIrreversibleStates`.
  - **TEST MATRIX:** `PRIMARY=TestVerticalSliceEffectAndOutcomeClosureHandlesPartialAmbiguousAndIrreversibleStates`; `PROPERTY=TestTodo_SLICE_009_Property`; `GOLDEN=TestTodo_SLICE_009_Golden`; `RACE=TestTodo_SLICE_009_Race`; `FAULT=TestTodo_SLICE_009_Fault`; `SECURITY=TestTodo_SLICE_009_Security`; `CONFORMANCE=TestTodo_SLICE_009_Conformance`; `RECOVERY=TestTodo_SLICE_009_Recovery`; `MUTATION=TestTodo_SLICE_009_Mutation`.
  - **RED:** planned write/effect lacks owner/order/idempotency/irreversible boundary, external acceptance becomes domain fact, partial/ambiguous outcome collapses to success/failure, no fresh observation/reconciliation deadline exists, repair reruns business transaction or cancellation claims reversal.
  - **GREEN:** slice declares read/write/effect/conflict sets, local commit, every operation/observation/comparison/completion dimension and targeted correction/repair route for success, partial, unknown, late and irreversible states with exact evidence.
  - **REFACTOR:** reuse transaction/connectivity/reconciliation mechanics; intent-specific expected state and material mismatch remain semantic inputs.
  - **Refs:** [transaction and repair](specs/transaction-ledger-reconciliation-and-repair.md), [effect graph](#12a-external-effect-reconciliation-repair-and-execution-evidence-convergence), [MAX-v1](workflows/vertical-slices/maximal-configuration-profile.md).

- [ ] `SLICE-010` **[RETIRED][SOL_HIGH] RETIRED 2026-09-02: Assign exact endpoint, initiator, schedule and event exposure to every slice.**
  - **Disposition:** RETIRED 2026-09-02. The vertical-slice program over the 530-slot baseline and 807-name vocabulary has no consumer. Dependencies on this ID resolve as satisfied; its tests are not written.
  - **Depends:** `SLICE-006`, `ENDPOINT-009`, `INTENT-017`, `INTENT-018`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=ensure every intent is reachable only through approved channels and no database entity or trigger creates an accidental public API`.
  - **TEST:** `TestVerticalSliceExposureDispositionIsExactAndAuthoritySafe`.
  - **TEST MATRIX:** `PRIMARY=TestVerticalSliceExposureDispositionIsExactAndAuthoritySafe`; `PROPERTY=TestTodo_SLICE_010_Property`; `GOLDEN=TestTodo_SLICE_010_Golden`; `FUZZ=FuzzTodo_SLICE_010`; `SECURITY=TestTodo_SLICE_010_Security`; `CONFORMANCE=TestTodo_SLICE_010_Conformance`; `MUTATION=TestTodo_SLICE_010_Mutation`.
  - **RED:** slice has no/multiple unexplained endpoint dispositions, system trigger becomes human API, agent/partner gets new authority, gRPC/HTTP semantics diverge, schedule/event bypasses BusinessIntent or internal child capability is public.
  - **GREEN:** every allowed initiator maps to exact typed method/generic lifecycle/event/schedule/internal/no-endpoint disposition with authority attenuation, trusted context, idempotency and parity test; prohibited initiators return non-disclosing denial.
  - **REFACTOR:** generate endpoint/trigger/schedule manifests and reverse coverage from the slice record.
  - **Refs:** [endpoint contract](specs/http-grpc-endpoint-contract.md), [endpoint coverage](#60-canonical-grpc-services-grpcbridge-http-endpoints-and-endpoint-tdd), [trigger model](specs/business-intent-catalog.md#initiator-and-exposure-policy).

- [ ] `SLICE-011` **[RETIRED][SOL_HIGH] RETIRED 2026-09-02: Generate exact maximal-configuration scenarios and tests for every slice.**
  - **Disposition:** RETIRED 2026-09-02. The vertical-slice program over the 530-slot baseline and 807-name vocabulary has no consumer. Dependencies on this ID resolve as satisfied; its tests are not written.
  - **Depends:** `SLICE-007`, `SLICE-008`, `SLICE-009`, `SLICE-010`, `GOV-018`, `GOV-028`, `TOOL-014`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=turn every slice/configuration boundary into exact positive, negative, temporal, concurrency, failure, security, repair and scale evidence`.
  - **TEST:** `TestVerticalSliceScenarioCompilerCoversApplicableMaximalConfiguration`.
  - **TEST MATRIX:** `PRIMARY=TestVerticalSliceScenarioCompilerCoversApplicableMaximalConfiguration`; `PROPERTY=TestTodo_SLICE_011_Property`; `GOLDEN=TestTodo_SLICE_011_Golden`; `FUZZ=FuzzTodo_SLICE_011`; `RACE=TestTodo_SLICE_011_Race`; `FAULT=TestTodo_SLICE_011_Fault`; `SECURITY=TestTodo_SLICE_011_Security`; `CONFORMANCE=TestTodo_SLICE_011_Conformance`; `RECOVERY=TestTodo_SLICE_011_Recovery`; `BENCHMARK=BenchmarkTodo_SLICE_011`; `MUTATION=TestTodo_SLICE_011_Mutation`.
  - **RED:** applicable MAX value lacks positive/boundary/forbidden case, pairwise coverage omits a pair, mandatory high-risk interaction lacks explicit fixture, oracle asserts only status/no-panic, scale claim lacks benchmark or test name/evidence cannot trace to slice/configuration.
  - **GREEN:** deterministic compiler emits minimal covering scenarios plus every mandated interaction, exact returned/persisted/prohibited state/effect/evidence oracle, named test class/command/fixture/timeout and stable coverage digest; unjustified omissions block maturity.
  - **REFACTOR:** reuse mechanics templates while keeping domain semantic outcomes and legally material examples explicit.
  - **Refs:** [MAX-v1](workflows/vertical-slices/maximal-configuration-profile.md), [slice plan testing](workflows/vertical-slices/plan.md#stage-3--generate-tests-and-endpoint-dispositions), [test doctrine](#secondary-test-taxonomy-and-required-return-contracts).

- [ ] `SLICE-012` **[RETIRED][SOL_HIGH] RETIRED 2026-09-02: Compile slice findings into atomic deduplicated TDD todos and reverse coverage.**
  - **Disposition:** RETIRED 2026-09-02. The vertical-slice program over the 530-slot baseline and 807-name vocabulary has no consumer. Dependencies on this ID resolve as satisfied; its tests are not written.
  - **Depends:** `SLICE-011`, `GOV-027`, `GOV-028`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=feed every unresolved slice responsibility into executable production work without creating one implementation per intent when semantics are shared`.
  - **TEST:** `TestVerticalSliceGapCompilerEmitsAtomicOwnerKeyedTodosWithoutDuplication`.
  - **TEST MATRIX:** `PRIMARY=TestVerticalSliceGapCompilerEmitsAtomicOwnerKeyedTodosWithoutDuplication`; `PROPERTY=TestTodo_SLICE_012_Property`; `GOLDEN=TestTodo_SLICE_012_Golden`; `SECURITY=TestTodo_SLICE_012_Security`; `CONFORMANCE=TestTodo_SLICE_012_Conformance`; `MUTATION=TestTodo_SLICE_012_Mutation`.
  - **RED:** missing property/engine/governance/human/effect/repair/endpoint/test produces no todo, shared gap produces hundreds of implementations, domain-specific invariants collapse into generic engine, dependency/phase/intelligence/test oracle is absent or finding loses consuming slice/configuration edges.
  - **GREEN:** compiler resolves current ownership/backlog first, then emits stable atomic candidates keyed by semantic owner+contract with dependencies, phase, intelligence, exact TDD oracle/negative dimensions/refs and complete reverse index; covered gap emits zero candidate.
  - **REFACTOR:** use one finding/todo schema and generate Markdown views; no manual copy of per-slice gap lists.
  - **Refs:** [gap feedback](workflows/vertical-slices/README.md#output-and-gap-feedback), [gap register](workflows/vertical-slices/gap-register.md), [todo generation](#backlog-governance-and-traceability).

- [ ] `SLICE-013` **[RETIRED][SOL_HIGH] RETIRED 2026-09-02: Gate slice maturity and implementation depth by source, contract, phase and current evidence.**
  - **Disposition:** RETIRED 2026-09-02. The vertical-slice program over the 530-slot baseline and 807-name vocabulary has no consumer. Dependencies on this ID resolve as satisfied; its tests are not written.
  - **Depends:** `SLICE-012`, `GOV-003`, `INTENT-CONF-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=prevent candidate mappings and maximal designs from being misreported as contracted, implemented or production-authorized behavior`.
  - **TEST:** `TestVerticalSliceMaturityRequiresSourceContractsPhaseAuthorityAndFreshEvidence`.
  - **TEST MATRIX:** `PRIMARY=TestVerticalSliceMaturityRequiresSourceContractsPhaseAuthorityAndFreshEvidence`; `PROPERTY=TestTodo_SLICE_013_Property`; `GOLDEN=TestTodo_SLICE_013_Golden`; `SECURITY=TestTodo_SLICE_013_Security`; `CONFORMANCE=TestTodo_SLICE_013_Conformance`; `MUTATION=TestTodo_SLICE_013_Mutation`.
  - **RED:** unbound/candidate row reports designed, unresolved contract reports contracted, later-phase slice creates Phase 1 work, passing happy path reports verified despite missing MAX scenarios or stale/failing/waived evidence is hidden in aggregate.
  - **GREEN:** maturity transition checks exact source join, complete graph/references/scenarios/todos, phase authority, implementation and current evidence; coverage reports separate denominators and names every blocker/deferred value/waiver.
  - **REFACTOR:** delivery manifests consume generated slice maturity; dashboards never infer status from checked boxes or prose.
  - **Refs:** [record maturity](workflows/vertical-slices/README.md#record-maturity), [slice plan reporting](workflows/vertical-slices/plan.md#reporting), [phase gates](execution-plan.md).

- [ ] `SLICE-014` **[RETIRED][SOL_HIGH] RETIRED 2026-09-02: Prove vertical-slice gap and todo generation reaches a deterministic fixed point.**
  - **Disposition:** RETIRED 2026-09-02. The vertical-slice program over the 530-slot baseline and 807-name vocabulary has no consumer. Dependencies on this ID resolve as satisfied; its tests are not written.
  - **Depends:** `SLICE-012`, `SLICE-013`, `GOV-002`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=measure convergence and stop endless backlog growth caused by unstable generation or duplicate findings`.
  - **TEST:** `TestVerticalSliceConvergenceSecondPassEmitsNoNewTodoIdentity`.
  - **TEST MATRIX:** `PRIMARY=TestVerticalSliceConvergenceSecondPassEmitsNoNewTodoIdentity`; `PROPERTY=TestTodo_SLICE_014_Property`; `GOLDEN=TestTodo_SLICE_014_Golden`; `CONFORMANCE=TestTodo_SLICE_014_Conformance`; `MUTATION=TestTodo_SLICE_014_Mutation`.
  - **RED:** unchanged inputs reorder slices/scenarios/todos, second pass emits new/renamed duplicate todo, resolved finding persists, missing source is silently dropped or graph digest depends on path/time/map iteration.
  - **GREEN:** two clean passes over identical manifests produce byte-identical registers, graphs, scenarios, findings, todo candidates and digests; adding/removing one contract creates the exact bounded diff and no unrelated churn.
  - **REFACTOR:** canonicalize ordering/IDs from stable semantic keys and remove manual/generated dual ownership.
  - **Refs:** [fixed-point rule](workflows/vertical-slices/gap-register.md#fixed-point-rule), [reproducibility](plan.md#93-reproducibility-contract), [generated drift](#2-go-only-repository-toolchain-and-contract-generation).

- [ ] `SLICE-015` **[RETIRED][TERRA] RETIRED 2026-09-02: Publish truthful baseline, vocabulary and vertical-slice coverage reports.**
  - **Disposition:** RETIRED 2026-09-02. The vertical-slice program over the 530-slot baseline and 807-name vocabulary has no consumer. Dependencies on this ID resolve as satisfied; its tests are not written.
  - **Depends:** `SLICE-002`, `SLICE-003`, `SLICE-013`, `SLICE-014`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=show exactly what is source-bound, mapped, contracted, implemented and verified without denominator or phase deception`.
  - **TEST:** `TestVerticalSliceCoverageReportUsesSeparateExactDenominators`.
  - **TEST MATRIX:** `PRIMARY=TestVerticalSliceCoverageReportUsesSeparateExactDenominators`; `PROPERTY=TestTodo_SLICE_015_Property`; `GOLDEN=TestTodo_SLICE_015_Golden`; `SECURITY=TestTodo_SLICE_015_Security`; `CONFORMANCE=TestTodo_SLICE_015_Conformance`.
  - **RED:** report mixes 530 baseline and 807 vocabulary, counts aliases/overlaps twice, hides 516 unbound slots, treats deferred as implemented, averages failing MAX configuration away or cannot trace a number to slice/source/scenario/todo/evidence.
  - **GREEN:** generated report shows exact separate counts and transitions, domain/profile/phase/configuration blockers, overlaps/extensions, todo/test/evidence links and canonical source/graph digest; current initial report returns `14 source-bound, 516 unbound, 807 vocabulary candidates`.
  - **REFACTOR:** README/dashboard tables consume this artifact rather than hand-maintained counts.
  - **Refs:** [truthful coverage](workflows/vertical-slices/README.md#truthful-current-coverage), [slice plan reporting](workflows/vertical-slices/plan.md#reporting), [baseline register](workflows/vertical-slices/baseline-vertical-slice-register.md).

## 62. Product-decision and implementation convergence

> **Disposition (2026-09-02):** P1A. `PHASE-001` freezes the scope ceiling; `SELECT-*`, `CUSTOMER-*`, `TOPOLOGY-*` and `COMMERCIAL-*` bind the partner selections next-steps.md M1 requires.

These todos close the remaining gap between a broad, internally coherent
architecture and an executable Phase 1 product decision. They do not create new
HCM scope. They select, bind, rehearse and measure the concrete subset already
authorized by the master and execution plans.

- [ ] `CLOSE-001` **[P0][SOL_HIGH] Publish one machine-readable design-closure register.**
  - **Depends:** `SLICE-015`, `SUBSTRATE-COVERAGE-001`, `GOV-026`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=join product, intent, workflow, model, API, security, operations, implementation and commercial readiness without turning document volume into a completeness claim`.
  - **TEST:** `TestDesignClosureRegisterNamesEveryRequiredDecisionOwnerArtifactAndGate`.
  - **TEST MATRIX:** `PRIMARY=TestDesignClosureRegisterNamesEveryRequiredDecisionOwnerArtifactAndGate`; `PROPERTY=TestTodo_CLOSE_001_Property`; `GOLDEN=TestTodo_CLOSE_001_Golden`; `SECURITY=TestTodo_CLOSE_001_Security`; `CONFORMANCE=TestTodo_CLOSE_001_Conformance`; `MUTATION=TestTodo_CLOSE_001_Mutation`.
  - **RED:** an accepted intent or Phase 1 dependency lacks exact source, owner, phase, decision state, artifact, todo, test, evidence, expiry or blocker; prose marked complete satisfies an implementation gate; `UNKNOWN`, `DEFERRED` or waived work disappears from totals.
  - **GREEN:** generated register joins every accepted scope item to stable artifacts and returns `UNSELECTED|UNBOUND|DESIGNED|CONTRACTED|IMPLEMENTED|VERIFIED|DEFERRED|REJECTED` with owner, rationale, evidence digest and expiry; every unresolved item is named and blocks only its declared gate.
  - **REFACTOR:** generate dashboards and executive summaries from this registry; do not create a second hand-maintained status source.
  - **Refs:** [vertical-slice coverage](workflows/vertical-slices/README.md#truthful-current-coverage), [platform coverage](specs/platform-capability-coverage-matrix.md), [phase gates](execution-plan.md).

- [ ] `PHASE-001` **[P0][SOL_HIGH] Freeze the candidate Phase 1 scope ceiling and selection slots.**
  - **Depends:** `GOV-010`, `WEDGE-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=turn the ChangeOps wedge into an exact bounded release without silently importing the future Workforce OS backlog`.
  - **TEST:** `TestPhaseOneManifestContainsOnlyAuthorizedSourceBoundExecutableScope`.
  - **TEST MATRIX:** `PRIMARY=TestPhaseOneManifestContainsOnlyAuthorizedSourceBoundExecutableScope`; `PROPERTY=TestTodo_PHASE_001_Property`; `GOLDEN=TestTodo_PHASE_001_Golden`; `SECURITY=TestTodo_PHASE_001_Security`; `CONFORMANCE=TestTodo_PHASE_001_Conformance`; `MUTATION=TestTodo_PHASE_001_Mutation`.
  - **RED:** manifest includes source-unbound intent, future native payroll/WFM/talent ownership, endpoint without disposition, capability without owner, workflow without vertical slice, hidden provider dependency or scope item lacking explicit include/defer/reject rationale.
  - **GREEN:** signed scope ceiling names the maximum candidate intents, capabilities, workflows, user flows, endpoints, models and effects plus explicit provider, jurisdiction, topology and SLO selection slots; all other catalog items remain visibly deferred and cannot create Phase 1 build work.
  - **REFACTOR:** concrete selection fills slots through `NEXT-002`; selection cannot depend on a supposedly final manifest that already contains the selection.
  - **Refs:** [Phase 1 execution plan](execution-plan.md), [ChangeOps overlay](plan.md#phase-1-changeops-overlay), [endpoint expansion](specs/http-grpc-endpoint-contract.md#expansion-rule).

- [ ] `SELECT-001` **[P0][SOL_HIGH] Select and evidence the first legal jurisdiction and bounded rule-pack scope.**
  - **Depends:** `PHASE-001`, `LEGAL-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.REGULATORY,BI.PEOPLE,BI.WORKFORCE,BI.REWARDS; DIRECT=none; WHY=replace abstract legal composition with one reviewable jurisdiction, effective window and explicit non-coverage boundary for the pilot`.
  - **TEST:** `TestPilotJurisdictionSelectionHasAuthoritativeSourcesReviewScopeAndStopConditions`.
  - **TEST MATRIX:** `PRIMARY=TestPilotJurisdictionSelectionHasAuthoritativeSourcesReviewScopeAndStopConditions`; `PROPERTY=TestTodo_SELECT_001_Property`; `GOLDEN=TestTodo_SELECT_001_Golden`; `SECURITY=TestTodo_SELECT_001_Security`; `CONFORMANCE=TestTodo_SELECT_001_Conformance`; `MUTATION=TestTodo_SELECT_001_Mutation`.
  - **RED:** selection lacks authoritative source citations, qualified legal owner/reviewer, included intent/rule/filing set, effective and known-at interval, collective/company interaction, uncertainty behavior, update SLA, prohibited legal-advice boundary or stop/reselect threshold.
  - **GREEN:** signed jurisdiction profile pins sources and versions, maps every Phase 1 legal input/decision/obligation/evidence path, declares exact exclusions and returns `UNKNOWN|HUMAN_REVIEW_REQUIRED` instead of guessing outside the reviewed scope.
  - **REFACTOR:** selection supplies data to LegalContext and regulatory-pack publication; it never hard-codes law in workflow branches.
  - **Refs:** [regulatory platform](specs/platform-architecture-catalog.md), [legal context](specs/governance-decision-and-obligation-composition.md), [coverage matrix](specs/platform-capability-coverage-matrix.md).

- [ ] `SELECT-002` **[P0][SOL_HIGH] Select the first design-partner system topology and provider contract.**
  - **Depends:** `PHASE-001`, `WEDGE-003`, `WEDGE-005`, `WEDGE-009`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.INTEGRATION,BI.PEOPLE,BI.REWARDS,BI.ACCESS; DIRECT=none; WHY=bind the overlay wedge to real authority systems, editions, APIs, quotas and independently observable downstream outcomes`.
  - **TEST:** `TestPilotProviderSelectionProvesEditionAuthorityOperationsAndExitViability`.
  - **TEST MATRIX:** `PRIMARY=TestPilotProviderSelectionProvesEditionAuthorityOperationsAndExitViability`; `PROPERTY=TestTodo_SELECT_002_Property`; `GOLDEN=TestTodo_SELECT_002_Golden`; `INTEGRATION=TestTodo_SELECT_002_Integration`; `FAULT=TestTodo_SELECT_002_Fault`; `SECURITY=TestTodo_SELECT_002_Security`; `CONFORMANCE=TestTodo_SELECT_002_Conformance`; `MUTATION=TestTodo_SELECT_002_Mutation`.
  - **RED:** generic provider name is accepted without product/edition/region/API entitlement, sandbox fidelity, authority-by-field, webhook/polling behavior, quotas, timeout ambiguity, credential model, data-processing terms, observation path, fallback/exit or commercial limit.
  - **GREEN:** signed provider topology maps each Phase 1 read/write/effect/observation to exact contract/version and owner, includes sandbox evidence and fault matrix, proves one cross-system independently observed outcome and defines stop/reselect conditions.
  - **REFACTOR:** provider-specific facts remain configuration and adapter qualification inputs; semantic workflows and capabilities remain provider-neutral.
  - **Refs:** [integration platform](specs/integration-platform.md), [competitive positioning](specs/competitive-positioning-and-authority-expansion.md), [vendor continuity](#36-operator-surfaces-ownership-and-measurable-production-limits).

- [x] `PROVIDER-001` **[GATE_A][SOL_HIGH] Materialize the selected provider as an executable contract-test fixture.**
  - **Evidence (2026-09-05):** `TestSelectedProviderAdapterPassesPinnedSemanticAndFaultConformanceSuite` in `internal/connectivity/providercontract` (pinned fake and optional sandbox suite return exact normalized results/errors/observations, enforce capability and quota declarations, preserve operation identity and prove redrive/reconciliation/repair without repeating the parent business transaction; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/connectivity/providercontract/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `SELECT-002`, `INTG-012`, `CONN-RT-007`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.INTEGRATION,BI.PEOPLE,BI.REWARDS,BI.ACCESS; DIRECT=none; WHY=prove selected external mechanics preserve HCM Next authority, idempotency, ambiguity, observation and repair semantics`.
  - **TEST:** `TestSelectedProviderAdapterPassesPinnedSemanticAndFaultConformanceSuite`.
  - **TEST MATRIX:** `PRIMARY=TestSelectedProviderAdapterPassesPinnedSemanticAndFaultConformanceSuite`; `GOLDEN=TestTodo_PROVIDER_001_Golden`; `FUZZ=FuzzTodo_PROVIDER_001`; `RACE=TestTodo_PROVIDER_001_Race`; `INTEGRATION=TestTodo_PROVIDER_001_Integration`; `FAULT=TestTodo_PROVIDER_001_Fault`; `SECURITY=TestTodo_PROVIDER_001_Security`; `CONFORMANCE=TestTodo_PROVIDER_001_Conformance`; `RECOVERY=TestTodo_PROVIDER_001_Recovery`; `BENCHMARK=BenchmarkTodo_PROVIDER_001`; `MUTATION=TestTodo_PROVIDER_001_Mutation`.
  - **RED:** fake accepts unsupported fields or ordering, hides provider request IDs, treats timeout-after-send as failure, duplicates effect on replay, promotes stale response to domain truth, leaks credentials/PII or cannot simulate quota/outage/schema drift/partial application.
  - **GREEN:** pinned fake and optional sandbox suite return exact normalized results/errors/observations, enforce capability and quota declarations, preserve operation identity and prove redrive/reconciliation/repair without repeating the parent business transaction.
  - **REFACTOR:** generated fixtures share semantic cases while transport quirks stay inside the selected provider adapter.
  - **Refs:** [provider adapter boundaries](specs/integration-platform.md), [external effects](specs/transaction-ledger-reconciliation-and-repair.md), [tool qualification](specs/adversarial-edge-and-tooling-audit-2026-08-14.md).

- [ ] `THREAT-001` **[P0][SOL_HIGH] Compile trust-boundary threat models for every Phase 1 vertical slice.**
  - **Depends:** `PHASE-001`, `SLICE-008`, `ENDPOINT-005`, `TRUST-019`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=derive threats from real actors, channels, data, decisions, effects and recovery paths rather than maintain one generic security checklist`.
  - **TEST:** `TestPhaseOneThreatModelCoversEveryTrustBoundaryAssetActorAndAbusePath`.
  - **TEST MATRIX:** `PRIMARY=TestPhaseOneThreatModelCoversEveryTrustBoundaryAssetActorAndAbusePath`; `PROPERTY=TestTodo_THREAT_001_Property`; `GOLDEN=TestTodo_THREAT_001_Golden`; `FUZZ=FuzzTodo_THREAT_001`; `FAULT=TestTodo_THREAT_001_Fault`; `SECURITY=TestTodo_THREAT_001_Security`; `CONFORMANCE=TestTodo_THREAT_001_Conformance`; `MUTATION=TestTodo_THREAT_001_Mutation`.
  - **RED:** slice omits actor/asset/trust boundary/data class/entry point/threat/mitigation/detection/recovery/owner/test, ignores confused deputy, tenant crossover, stale authorization, insider abuse, metadata leakage, replay, ambiguous effect, supply-chain or human-social attack, or marks accepted risk without expiry.
  - **GREEN:** generated threat register maps each slice graph edge to reviewed threats and controls, produces exact negative/security/fault tests, retains residual risk with accountable acceptance and expiry and blocks release for unmitigated critical findings.
  - **REFACTOR:** threat identities are stable and deduplicated by asset+boundary+attack; shared mitigations retain every consuming slice edge.
  - **Refs:** [security architecture](specs/platform-responsibility-boundaries.md), [MAX-v1 threats](workflows/vertical-slices/maximal-configuration-profile.md), [risk register](specs/risk-register.md).

- [x] `THREAT-002` **[GATE_A][SOL_HIGH] Execute adversarial privacy, authorization and abuse journeys.**
  - **Evidence (2026-09-05):** `TestAdversarialJourneysDenyWithoutExistenceMetadataOrTelemetryLeakage` in `internal/operations/adversarial`, `internal/trust/adversarial` (every adversarial journey returns the declared non-disclosing denial or safe degraded state, produces bounded redacted evidence and zero unauthorized persistence/effects across all equivalent channels and restart/replay paths; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/operations/adversarial/ ./internal/trust/adversarial/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `THREAT-001`, `UXFLOW-008`, `ABUSE-008`, `OBS-013`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove hostile and accidental user journeys cannot bypass intent governance or leak sensitive content through UI, API, work, messages, logs, traces, analytics or repair`.
  - **TEST:** `TestAdversarialJourneysDenyWithoutExistenceMetadataOrTelemetryLeakage`.
  - **TEST MATRIX:** `PRIMARY=TestAdversarialJourneysDenyWithoutExistenceMetadataOrTelemetryLeakage`; `PROPERTY=TestTodo_THREAT_002_Property`; `GOLDEN=TestTodo_THREAT_002_Golden`; `FUZZ=FuzzTodo_THREAT_002`; `RACE=TestTodo_THREAT_002_Race`; `INTEGRATION=TestTodo_THREAT_002_Integration`; `FAULT=TestTodo_THREAT_002_Fault`; `SECURITY=TestTodo_THREAT_002_Security`; `CONFORMANCE=TestTodo_THREAT_002_Conformance`; `BROWSER=TestTodo_THREAT_002_Browser`; `RECOVERY=TestTodo_THREAT_002_Recovery`; `MUTATION=TestTodo_THREAT_002_Mutation`.
  - **RED:** alternate channel, deep link, delegated/act-as session, bulk action, retry, export, support tool, log/trace query or partial failure reveals existence/field/count/identity or creates an unauthorized work item, message, intent, effect or repair.
  - **GREEN:** every adversarial journey returns the declared non-disclosing denial or safe degraded state, produces bounded redacted evidence and zero unauthorized persistence/effects across all equivalent channels and restart/replay paths.
  - **REFACTOR:** generate journeys from user-flow and threat graphs; keep only high-value hand-authored attack sequences as regression fixtures.
  - **Refs:** [user flows](user-flows/README.md), [structured telemetry](specs/structured-logging-and-opentelemetry.md), [organization scope and AuthZ](specs/organization-scope-and-authz.md).

- [x] `TOPOLOGY-001` **[GATE_A][SOL_HIGH] Freeze and verify the first production-cell deployment topology.**
  - **Evidence (2026-09-05):** `TestPilotCellTopologyMapsEveryProcessDataDependencyBoundaryAndFailureMode` in `internal/platform/topology` (generated topology and deploy manifest have the same digest-backed inventory, exact network/data/trust paths, zones/failure domains, scaling/degradation/recovery behavior and resource budgets; sandbox deployment passes health, load, drain and restore probes; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/platform/topology/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PHASE-001`, `PERF-ENV-001`, `SELECT-002`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=bind abstract planes and process roles to one deployable, capacity-sized and recoverable pilot cell`.
  - **TEST:** `TestPilotCellTopologyMapsEveryProcessDataDependencyBoundaryAndFailureMode`.
  - **TEST MATRIX:** `PRIMARY=TestPilotCellTopologyMapsEveryProcessDataDependencyBoundaryAndFailureMode`; `PROPERTY=TestTodo_TOPOLOGY_001_Property`; `GOLDEN=TestTodo_TOPOLOGY_001_Golden`; `INTEGRATION=TestTodo_TOPOLOGY_001_Integration`; `FAULT=TestTodo_TOPOLOGY_001_Fault`; `SECURITY=TestTodo_TOPOLOGY_001_Security`; `CONFORMANCE=TestTodo_TOPOLOGY_001_Conformance`; `RECOVERY=TestTodo_TOPOLOGY_001_Recovery`; `BENCHMARK=BenchmarkTodo_TOPOLOGY_001`; `MUTATION=TestTodo_TOPOLOGY_001_Mutation`.
  - **RED:** a process, queue/timer, database/object store, control distribution, secret/key, edge/egress path, telemetry pipeline, backup, provider, ownership route or capacity limit is absent; diagram and IaC disagree; single failure silently violates an intent completion invariant.
  - **GREEN:** generated topology and deploy manifest have the same digest-backed inventory, exact network/data/trust paths, zones/failure domains, scaling/degradation/recovery behavior and resource budgets; sandbox deployment passes health, load, drain and restore probes.
  - **REFACTOR:** derive diagrams and operational inventory from deployable manifests rather than maintain topology pictures separately.
  - **Refs:** [platform architecture catalog](specs/platform-architecture-catalog.md), [IaC](#31-infrastructure-as-code-and-physical-data-services), [performance](#36-operator-surfaces-ownership-and-measurable-production-limits).

- [ ] `CUSTOMER-001` **[P0][SOL_HIGH] Define the design-partner implementation blueprint and responsibility matrix.**
  - **Depends:** `WEDGE-001`, `PHASE-001`, `SELECT-001`, `SELECT-002`, `OPS-007`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=make customer discovery, configuration, data, identity, integration, legal review, testing, training, cutover and steady-state ownership explicit`.
  - **TEST:** `TestImplementationBlueprintCoversEveryPilotDependencyDeliverableOwnerAndAcceptanceGate`.
  - **TEST MATRIX:** `PRIMARY=TestImplementationBlueprintCoversEveryPilotDependencyDeliverableOwnerAndAcceptanceGate`; `PROPERTY=TestTodo_CUSTOMER_001_Property`; `GOLDEN=TestTodo_CUSTOMER_001_Golden`; `SECURITY=TestTodo_CUSTOMER_001_Security`; `CONFORMANCE=TestTodo_CUSTOMER_001_Conformance`; `MUTATION=TestTodo_CUSTOMER_001_Mutation`.
  - **RED:** blueprint lacks customer/HCM Next/provider owner, prerequisite, input/output artifact, due/expiry, acceptance oracle, data-processing boundary, escalation or fallback for any pilot workstream; assumed customer capability reports ready.
  - **GREEN:** versioned RACI and implementation graph covers discovery through hypercare, binds every dependency to exact evidence and stop/go gate and returns `UNKNOWN|BLOCKED` for missing or stale customer/provider inputs.
  - **REFACTOR:** instantiate the reusable blueprint per tenant; customer-specific facts never alter the platform's semantic contracts.
  - **Refs:** [responsibility boundaries](specs/platform-responsibility-boundaries.md), [customer onboarding](#35-artifact-bytes-and-customer-data-onboarding), [execution plan](execution-plan.md).

- [x] `CUSTOMER-002` **[GATE_A][SOL_HIGH] Rehearse discovery, data mapping, identity, configuration and migration readiness.**
  - **Evidence (2026-09-05):** `TestPilotReadinessRehearsalClassifiesEverySourceFieldRecordConfigurationAndIdentity` in `internal/connectivity/onboarding/readiness` (rehearsal produces exact ACCEPT|TRANSFORM|QUARANTINE|REJECT|UNKNOWN disposition, simulation and row/field/effective-date reconciliation for every input, with zero production mutation and signed readiness blockers; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/connectivity/onboarding/readiness/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CUSTOMER-001`, `ONBOARD-006`, `DATAOPS-006`, `CP-007`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.PEOPLE,BI.REWARDS,BI.ACCESS,BI.INTEGRATION,BI.DATAOPS; DIRECT=none; WHY=prove design-partner source data and configuration can reach canonical truth without silent loss, unsafe defaulting or identity corruption`.
  - **TEST:** `TestPilotReadinessRehearsalClassifiesEverySourceFieldRecordConfigurationAndIdentity`.
  - **TEST MATRIX:** `PRIMARY=TestPilotReadinessRehearsalClassifiesEverySourceFieldRecordConfigurationAndIdentity`; `PROPERTY=TestTodo_CUSTOMER_002_Property`; `GOLDEN=TestTodo_CUSTOMER_002_Golden`; `FUZZ=FuzzTodo_CUSTOMER_002`; `INTEGRATION=TestTodo_CUSTOMER_002_Integration`; `FAULT=TestTodo_CUSTOMER_002_Fault`; `SECURITY=TestTodo_CUSTOMER_002_Security`; `CONFORMANCE=TestTodo_CUSTOMER_002_Conformance`; `RECOVERY=TestTodo_CUSTOMER_002_Recovery`; `MUTATION=TestTodo_CUSTOMER_002_Mutation`.
  - **RED:** source field/record/config/identity is dropped, defaulted or merged without disposition/provenance; effective/known time, presence, classification, authority or crosswalk is missing; aggregate totals hide item-level drift.
  - **GREEN:** rehearsal produces exact `ACCEPT|TRANSFORM|QUARANTINE|REJECT|UNKNOWN` disposition, simulation and row/field/effective-date reconciliation for every input, with zero production mutation and signed readiness blockers.
  - **REFACTOR:** use DataOps/transform/identity/configuration engines; implementation tooling must not create a second migration semantics stack.
  - **Refs:** [HRIS DataOps](specs/hris-admin-dataops.md), [customer onboarding](#35-artifact-bytes-and-customer-data-onboarding), [model coverage](data/models/intent-coverage-matrix.md).

- [ ] `CUSTOMER-003` **[GATE_B][SOL_HIGH] Execute dry-run, cutover, rollback and hypercare migration drills.**
  - **Depends:** `CUSTOMER-002`, `TOPOLOGY-001`, `RECOVERY-004`, `OPS-008`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove the pilot can enter service, abort safely, resume and reconcile without duplicate intent or external effect`.
  - **TEST:** `TestPilotCutoverDrillMeetsStopGoRollbackRPOAndReconciliationContracts`.
  - **TEST MATRIX:** `PRIMARY=TestPilotCutoverDrillMeetsStopGoRollbackRPOAndReconciliationContracts`; `PROPERTY=TestTodo_CUSTOMER_003_Property`; `GOLDEN=TestTodo_CUSTOMER_003_Golden`; `RACE=TestTodo_CUSTOMER_003_Race`; `INTEGRATION=TestTodo_CUSTOMER_003_Integration`; `FAULT=TestTodo_CUSTOMER_003_Fault`; `SECURITY=TestTodo_CUSTOMER_003_Security`; `CONFORMANCE=TestTodo_CUSTOMER_003_Conformance`; `RECOVERY=TestTodo_CUSTOMER_003_Recovery`; `BENCHMARK=BenchmarkTodo_CUSTOMER_003`; `MUTATION=TestTodo_CUSTOMER_003_Mutation`.
  - **RED:** drill lacks freeze/watermark, delta capture, responsibility handoff, stop/go thresholds, rollback boundary, credential/webhook transition, manual continuity, customer communication or item-level reconciliation; retry duplicates accepted intent/effect.
  - **GREEN:** time-sequenced drill records exact prechecks, cutover decisions, RPO/RTO, rollback/fail-forward result, resumption fences, reconciliation and hypercare ownership; injected failures reach only allowed states and unresolved ambiguity blocks go-live.
  - **REFACTOR:** generate runbook steps/evidence from the implementation graph and deployment manifests; retain human decision points explicitly.
  - **Refs:** [recovery](specs/platform-responsibility-boundaries.md), [incident management](specs/incident-management.md), [integration operations](specs/integration-platform.md).

- [ ] `CUSTOMER-004` **[GATE_B][TERRA] Prove administrator, approver, employee and support adoption readiness.**
  - **Depends:** `CUSTOMER-001`, `UXFLOW-009`, `OPS-010`, `ADMIN-006`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=ensure authorized humans can complete, understand and recover pilot journeys through accessible primary and continuity channels`.
  - **TEST:** `TestPilotRoleReadinessMeetsTaskAccessibilityComprehensionAndSupportThresholds`.
  - **TEST MATRIX:** `PRIMARY=TestPilotRoleReadinessMeetsTaskAccessibilityComprehensionAndSupportThresholds`; `PROPERTY=TestTodo_CUSTOMER_004_Property`; `GOLDEN=TestTodo_CUSTOMER_004_Golden`; `SECURITY=TestTodo_CUSTOMER_004_Security`; `CONFORMANCE=TestTodo_CUSTOMER_004_Conformance`; `BROWSER=TestTodo_CUSTOMER_004_Browser`; `BENCHMARK=BenchmarkTodo_CUSTOMER_004`; `MUTATION=TestTodo_CUSTOMER_004_Mutation`.
  - **RED:** role cannot discover permitted action, distinguish request from committed outcome, understand approval/evidence/deadline, complete accessible flow, recover interrupted work or reach support; training exposes sensitive data or teaches bypass/direct-system mutation.
  - **GREEN:** representative role cohort meets declared task-success, error, time, accessibility and comprehension thresholds on primary/manual routes; failures create owned product/training/support blockers with expiry and rerun evidence.
  - **REFACTOR:** training and help content are generated from versioned intent/user-flow contracts and never become an alternate source of product truth.
  - **Refs:** [user flows](user-flows/README.md), [experience](specs/experience-ui-and-branding.md), [support boundaries](specs/platform-responsibility-boundaries.md).

- [ ] `COMMERCIAL-001` **[P0][SOL_HIGH] Freeze the pilot package, pricing hypothesis and contractual authority boundary.**
  - **Depends:** `PHASE-001`, `SELECT-002`, `WEDGE-007`, `WEDGE-009`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.COMMERCIAL,BI.TENANT; DIRECT=none; WHY=ensure what is sold, metered, supported and warranted matches the exact product authority and operational evidence`.
  - **TEST:** `TestPilotCommercialPackageMatchesReleaseEntitlementsCostsRisksAndExitTerms`.
  - **TEST MATRIX:** `PRIMARY=TestPilotCommercialPackageMatchesReleaseEntitlementsCostsRisksAndExitTerms`; `PROPERTY=TestTodo_COMMERCIAL_001_Property`; `GOLDEN=TestTodo_COMMERCIAL_001_Golden`; `SECURITY=TestTodo_COMMERCIAL_001_Security`; `CONFORMANCE=TestTodo_COMMERCIAL_001_Conformance`; `MUTATION=TestTodo_COMMERCIAL_001_Mutation`.
  - **RED:** package promises unselected intent/provider/jurisdiction/SLO, confuses overlay authority with system-of-record ownership, omits implementation/support/provider cost, bills replay/repair duplicates, lacks data processing/retention/exit terms or hides hypothesis/stop threshold.
  - **GREEN:** signed package maps entitlements and exclusions to the Phase 1 digest, declares price/usage/support/implementation assumptions, legal/authority boundaries, provider pass-throughs, evidence/SLOs, termination/export obligations and quantitative reprice/stop criteria.
  - **REFACTOR:** commercial views consume entitlement, usage, cost and release registries; contract prose cannot independently enable capability.
  - **Refs:** [pricing hypothesis](plan.md#125-commercial-architecture-hypotheses), [commercial proof](execution-plan.md), [competitive position](specs/competitive-positioning-and-authority-expansion.md).

- [ ] `PILOT-001` **[GATE_B][SOL_HIGH] Run an evidence-bound pilot go/no-go and value-realization review.**
  - **Depends:** `WEDGE-015`, `COMMERCIAL-001`, `CUSTOMER-003`, `CUSTOMER-004`, `PERF-008`, `THREAT-002`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=decide whether the ChangeOps wedge is safe, useful and economically credible using current evidence rather than roadmap ambition`.
  - **TEST:** `TestPilotGoNoGoRejectsMissingStaleWaivedOrBelowThresholdEvidence`.
  - **TEST MATRIX:** `PRIMARY=TestPilotGoNoGoRejectsMissingStaleWaivedOrBelowThresholdEvidence`; `PROPERTY=TestTodo_PILOT_001_Property`; `GOLDEN=TestTodo_PILOT_001_Golden`; `SECURITY=TestTodo_PILOT_001_Security`; `CONFORMANCE=TestTodo_PILOT_001_Conformance`; `BENCHMARK=BenchmarkTodo_PILOT_001`; `MUTATION=TestTodo_PILOT_001_Mutation`.
  - **RED:** review averages away critical failure, treats missing/stale/expired/waived evidence as pass, reports activity instead of business outcome, excludes implementation/support cost or authorizes expansion without explicit authority/value/risk thresholds.
  - **GREEN:** decision package reports safety/correctness/SLO/adoption/value/cost/support metrics with denominators and evidence digests, names blockers and waivers, returns `GO|CONDITIONAL_GO|NO_GO|RESELECT`, and permits authority expansion only through a new approved manifest.
  - **REFACTOR:** retain historical decision packages append-only so later claims can be compared with what was known at each gate.
  - **Refs:** [execution gates](execution-plan.md), [competitive success measures](specs/competitive-positioning-and-authority-expansion.md), [SLO evidence](specs/slo-sli-error-budget.md).

- [ ] `CLOSE-002` **[P0][SOL_HIGH] Re-run design, slice and implementation gap compilers to a fixed point after concrete selections.**
  - **Depends:** `SELECT-001`, `SELECT-002`, `THREAT-001`, `TOPOLOGY-001`, `CUSTOMER-001`, `COMMERCIAL-001`, `SLICE-014`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=feed real jurisdiction, provider, topology, customer and commercial facts back into models, workflows, APIs, tests and todos until no ownerless contract remains`.
  - **TEST:** `TestConcreteSelectionConvergenceSecondPassProducesNoNewGapIdentity`.
  - **TEST MATRIX:** `PRIMARY=TestConcreteSelectionConvergenceSecondPassProducesNoNewGapIdentity`; `PROPERTY=TestTodo_CLOSE_002_Property`; `GOLDEN=TestTodo_CLOSE_002_Golden`; `FAULT=TestTodo_CLOSE_002_Fault`; `SECURITY=TestTodo_CLOSE_002_Security`; `CONFORMANCE=TestTodo_CLOSE_002_Conformance`; `MUTATION=TestTodo_CLOSE_002_Mutation`.
  - **RED:** selected fact changes no downstream slice/model/API/threat/test, new gap lacks atomic todo/owner/phase/oracle, unchanged second pass produces new identities, duplicate gap survives under different wording or closure report hides selected-scope unknowns.
  - **GREEN:** compilers regenerate exact bounded diffs, deduplicate by semantic owner+contract, every selected-scope gap resolves to existing/new atomic todo or explicit rejection and two unchanged clean passes produce byte-identical zero-new-gap output.
  - **REFACTOR:** make this the single convergence gate invoked by catalog, selection, architecture and pilot changes.
  - **Refs:** [slice fixed point](workflows/vertical-slices/gap-register.md#fixed-point-rule), [design closure](#product-decision-and-implementation-convergence), [coverage governance](#backlog-governance-and-traceability).

- [ ] `SOURCE-001` **[RETIRED][SOL_HIGH] RETIRED 2026-09-02: Attest the immutable BusinessIntent source artifact and lossless catalog recovery.**
  - **Disposition:** RETIRED 2026-09-02. There is no source artifact to attest; the intake list is non-normative vocabulary and the catalog is the fourteen drafted definitions. Dependencies on this ID resolve as satisfied; its tests are not written.
  - **Depends:** `MODEL-008`, `MODEL-009`, `MSRC-001`, `GOV-008`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=prove the accepted numbered catalog came losslessly from an authentic immutable source rather than a recomputed hand-authored manifest`.
  - **TEST:** `TestIntentSourceAttestationRejectsUntrustedAlteredOrLossyRecovery`.
  - **TEST MATRIX:** `PRIMARY=TestIntentSourceAttestationRejectsUntrustedAlteredOrLossyRecovery`; `PROPERTY=TestTodo_SOURCE_001_Property`; `GOLDEN=TestTodo_SOURCE_001_Golden`; `FUZZ=FuzzTodo_SOURCE_001`; `SECURITY=TestTodo_SOURCE_001_Security`; `CONFORMANCE=TestTodo_SOURCE_001_Conformance`; `MUTATION=TestTodo_SOURCE_001_Mutation`.
  - **RED:** recomputed digest on modified/reordered/Unicode-normalized rows, unverifiable issuer/locator, missing byte offsets/parser version or untrusted key rotation is accepted and promotes an unbound definition.
  - **GREEN:** verified source package pins immutable bytes, trusted issuer/key/release lineage, SHA-256, retrieval provenance, parser version and per-row byte/line spans; exactly 530 rows join losslessly and absent evidence leaves rows `UNBOUND_SOURCE` with zero catalog promotion.
  - **REFACTOR:** generated partitions carry source and parser digests; authenticity verification remains independent of catalog interpretation.
  - **Refs:** [catalog identity](specs/business-intent-catalog.md#catalog-identity), [source manifest](#canonical-values-schemas-registries-and-model-coverage), [baseline register](workflows/vertical-slices/baseline-vertical-slice-register.md).

- [ ] `BIND-001` **[P0][SOL_HIGH] Bind each published capability to exact models, wire descriptors and one typed Go handler.**
  - **Evidence (partial, 2026-09-05):** `TestTypedImplementationBindingRejectsDanglingAmbiguousOrSchemaMismatchedHandler`, `TestTodo_BIND_001_{Property,Golden,Integration,Conformance,Mutation}` and per-file tests in `internal/capability/binding` (BindingTable joins the BOOTSTRAP capability registry, the four generated ServiceDescs read without importing grpc, an AST scan for handler symbols and MSRC-009's model bindings; twelve typed gap kinds; kernel-pure, proven by an import test); `go test -count=1 ./internal/capability/binding/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02. Live finding: of 10 published capabilities and 29 wire methods, none binds cleanly today — 33 gaps allowlisted with owners: the eight P1A capabilities are dispatched generically across six IntentService methods (AMBIGUOUS_WIRE_METHOD), admin GetWorkerState/ExplainTransaction call the domain directly bypassing the gateway (AMBIGUOUS_HANDLER), the registry capabilities have no handlerFor arm, admin and journey are absent from the endpoint manifest's governed services (WIRE_METHOD_UNBOUND ×16, owner PROTO-010/ENDPOINT-009), and every capability still returns map[string]any through the gateway. Remaining: close those gaps so each capability binds to one route and one typed handler; the conformance test fails on any new gap and on any allowlisted gap that closes.
  - **Depends:** `MSRC-007`, `MSRC-009`, `MODEL-010`, `CAP-001`, `PROTO-005`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=ensure every executable semantic capability resolves to one compile-time checked implementation rather than a dangling registry or untyped adapter`.
  - **TEST:** `TestTypedImplementationBindingRejectsDanglingAmbiguousOrSchemaMismatchedHandler`.
  - **TEST MATRIX:** `PRIMARY=TestTypedImplementationBindingRejectsDanglingAmbiguousOrSchemaMismatchedHandler`; `PROPERTY=TestTodo_BIND_001_Property`; `GOLDEN=TestTodo_BIND_001_Golden`; `INTEGRATION=TestTodo_BIND_001_Integration`; `CONFORMANCE=TestTodo_BIND_001_Conformance`; `MUTATION=TestTodo_BIND_001_Mutation`.
  - **RED:** missing/duplicate Go symbol, unregistered capability version, wrong request/result descriptor, SchemaFlux digest mismatch or `map[string]any` handler publishes or starts; no handler may run.
  - **GREEN:** generated binding records intent/version, model release/digest, capability/version, fully qualified typed interface method, build/source digest, effects and Protobuf descriptors; compile/start/publication and in-memory invocation prove descriptor-equivalent typed results.
  - **REFACTOR:** authored code implements generated interfaces while registries own discovery and version binding.
  - **Refs:** [capability lifecycle](specs/capability-registry-and-lifecycle.md), [generated registries](#29-machine-readable-model-sources-and-public-api-contracts), [modular Go architecture](#2a-modular-go-monolith-and-semantic-package-architecture).

- [x] `MODEL-032` **[P0][SOL_HIGH] Prove canonical property semantics and lineage across SchemaFlux, Protobuf, Go, SQL and storage disposition.**
  - **Depends:** `MODEL-011`, `MODEL-016`, `MODEL-017`, `DB-002`, `DB-003`, `PROTO-009`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=prevent property meaning, authority or history from changing between semantic source, wire, runtime and persistence layers`.
  - **TEST:** `TestCanonicalPropertyMappingStorageAndLineageClosureRejectsSemanticDrift`.
  - **TEST MATRIX:** `PRIMARY=TestCanonicalPropertyMappingStorageAndLineageClosureRejectsSemanticDrift`; `PROPERTY=TestTodo_MODEL_032_Property`; `GOLDEN=TestTodo_MODEL_032_Golden`; `FUZZ=FuzzTodo_MODEL_032`; `INTEGRATION=TestTodo_MODEL_032_Integration`; `SECURITY=TestTodo_MODEL_032_Security`; `CONFORMANCE=TestTodo_MODEL_032_Conformance`; `RECOVERY=TestTodo_MODEL_032_Recovery`; `MUTATION=TestTodo_MODEL_032_Mutation`.
  - **RED:** presence/default/enum/decimal/unit/time/classification/authority changes across layers, SQL round-trip changes digest, intent property lacks tenant-scoped ledger/projection/artifact/provenance path, projection accepts write or external observation becomes domain fact.
  - **GREEN:** one generated manifest gives lossless mappings and exact authoritative storage, event, projection, artifact and lineage disposition for every material property/result/effect; round-trip/rebuild preserves canonical bytes and lossy/ambiguous mapping fails by entity/property/layer.
  - **REFACTOR:** generate adapters and coverage from canonical PropertyDefinition; physical stores never redefine semantics.
  - **Refs:** [registry contracts](data/models/registry-and-coverage-contracts.md), [storage disposition](#26-postgresql-database-and-model-materialization), [provenance](specs/provenance-graph-and-lineage.md).
  - **Evidence (2026-09-03):** `TestCanonicalPropertyMappingStorageAndLineageClosureRejectsSemanticDrift`, Declared `MODEL-032` test matrix and acceptance behavior in `tools/gen/storagemanifest`; PASS; `go test -p 1 ./tools/gen/storagemanifest` and `go vet ./tools/gen/storagemanifest` on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.

- [x] `PROTO-010` **[P0][SOL_HIGH] Require total descriptor-level RPC exposure disposition.**
  - **Depends:** `PROTO-005`, `MODEL-010`, `CAP-001`, `ENDPOINT-001`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=prevent declared RPCs or generated handlers from becoming accidental public APIs`.
  - **TEST:** `TestProtoRpcDispositionIsTotalAndNoAccidentalExposure`.
  - **TEST MATRIX:** `PRIMARY=TestProtoRpcDispositionIsTotalAndNoAccidentalExposure`; `PROPERTY=TestTodo_PROTO_010_Property`; `GOLDEN=TestTodo_PROTO_010_Golden`; `SECURITY=TestTodo_PROTO_010_Security`; `CONFORMANCE=TestTodo_PROTO_010_Conformance`; `MUTATION=TestTodo_PROTO_010_Mutation`.
  - **RED:** descriptor method lacks exactly one `PUBLIC_TYPED|PUBLIC_GENERIC|INTERNAL_ONLY|EVENT_OR_SCHEDULE|NO_ENDPOINT` disposition, owner/capability/intent refs, trusted initiator, AuthZ/purpose, retry/idempotency or HTTP/internal rationale; unregistered handler binds.
  - **GREEN:** disposition manifest is set-equal to descriptor methods and shares a digest with endpoint/grpcbridge generation; every method has exact semantic/security/lifecycle policy and startup exposes no method outside the manifest.
  - **REFACTOR:** descriptor inventory and endpoint manifests are generated from one source rather than compared by display name.
  - **Refs:** [endpoint manifest](specs/http-grpc-endpoint-contract.md), [Proto contracts](#29-machine-readable-model-sources-and-public-api-contracts), [endpoint closure](#60-canonical-grpc-services-grpcbridge-http-endpoints-and-endpoint-tdd).
  - **Evidence (2026-09-03):** `TestProtoRpcDispositionIsTotalAndNoAccidentalExposure` in `internal/transport/manifest` (every RPC in every generated service has exactly one disposition); PASS; branch plan-revision-2026-09-02; `go test -count=1 ./internal/transport/manifest/...` on windows/arm64 (Go 1.26.3).

- [ ] `SLICE-016` **[P0][SOL_HIGH] Emit one canonical per-intent closure witness.**
  - **Depends:** `SOURCE-001`, `SLICE-013`, `SLICE-015`, `BIND-001`, `PROTO-010`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=prove each accepted definition is individually traceable from authentic source through behavior, implementation, tests and current evidence`.
  - **TEST:** `TestPerIntentClosureWitnessCrossJoinIsTotalUniqueAndCurrent`.
  - **TEST MATRIX:** `PRIMARY=TestPerIntentClosureWitnessCrossJoinIsTotalUniqueAndCurrent`; `PROPERTY=TestTodo_SLICE_016_Property`; `GOLDEN=TestTodo_SLICE_016_Golden`; `SECURITY=TestTodo_SLICE_016_Security`; `CONFORMANCE=TestTodo_SLICE_016_Conformance`; `MUTATION=TestTodo_SLICE_016_Mutation`.
  - **RED:** any source→slice→model/engine/capability→handler→endpoint→scenario/test/todo/evidence edge is absent, duplicated, stale or aggregate-only while maturity advances.
  - **GREEN:** every source-bound definition has exactly one digest-backed witness containing all graph and reverse edges, phase/gate, endpoint disposition, test/evidence outputs and expiry; orphan/duplicate/stale edge returns `SLICE_CLOSURE_INCOMPLETE`.
  - **REFACTOR:** coverage reports consume witnesses; witnesses are generated from registries and never manually asserted.
  - **Refs:** [vertical slices](workflows/vertical-slices/README.md), [closure register](#product-decision-and-implementation-convergence), [truthful coverage](workflows/vertical-slices/README.md#truthful-current-coverage).

- [x] `UXFLOW-011` **[P0][SOL_HIGH] Version and migrate active user flows without semantic drift.**
  - **Evidence (2026-09-06):** `TestUserFlowDefinitionEvolutionPreservesActiveDraftTaskAndActionSemantics` in `internal/experience/flowmigration`; `go test -count=1 ./internal/experience/flowmigration/` PASS on windows/arm64 (Go 1.26.3); evidence recorded by the orchestrator after the front-end session ticked the entry without one.
  - **Depends:** `UXFLOW-001`, `UXFLOW-006`, `UXFLOW-010`, `INTENT-028`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=preserve drafts, tasks, approvals, deep links and confirmations when experience definitions evolve`.
  - **TEST:** `TestUserFlowDefinitionEvolutionPreservesActiveDraftTaskAndActionSemantics`.
  - **TEST MATRIX:** `PRIMARY=TestUserFlowDefinitionEvolutionPreservesActiveDraftTaskAndActionSemantics`; `PROPERTY=TestTodo_UXFLOW_011_Property`; `GOLDEN=TestTodo_UXFLOW_011_Golden`; `RACE=TestTodo_UXFLOW_011_Race`; `FAULT=TestTodo_UXFLOW_011_Fault`; `SECURITY=TestTodo_UXFLOW_011_Security`; `CONFORMANCE=TestTodo_UXFLOW_011_Conformance`; `BROWSER=TestTodo_UXFLOW_011_Browser`; `RECOVERY=TestTodo_UXFLOW_011_Recovery`; `MODEL_BASED=TestTodo_UXFLOW_011_ModelBased`; `MUTATION=TestTodo_UXFLOW_011_Mutation`.
  - **RED:** incompatible publication silently reinterprets active draft/task/approval/action digest, retired action remains callable, stale deep link performs changed action, rollback rewrites history or migration duplicates effect.
  - **GREEN:** flow versions are immutable; active state stays pinned or follows reviewed successor/replan mapping; stale links return safe read-only/replan outcome and migration receipt proves no lost work or duplicate action.
  - **REFACTOR:** derive flow migrations from versioned action/state contracts, not route-specific code.
  - **Refs:** [user-flow contract](user-flows/README.md), [flow archetypes](user-flows/archetypes.md), [intent evolution](#exact-initial-businessintent-delivery-closure).

- [x] `AUTHN-009` **[GATE_A][SOL_HIGH] Implement subscriber account and digital identity lifecycle revocation fan-out.**
  - **Evidence (2026-09-05):** `TestSubscriberAccountLifecycleRevokesPrincipalSessionsAuthenticatorsAndDelegation` in `internal/authn` (durable account/person/principal/tenant bindings drive immutable lifecycle transitions and revocation epochs; all derived sessions, token families, authenticators and delegation chains are fenced, recovery is assurance-bounded and relink preserves history/evidence; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/authn/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `AUTHN-001`, `AUTHN-002`, `AUTHN-003`, `AUTHN-004`, `MODEL-022`, `TRUST-013`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.SECURITY,BI.ACCESS,BI.TENANT; DIRECT=none; WHY=ensure suspend, disable, terminate, unlink and recovery invalidate every principal/session/delegation authority derived from the account`.
  - **TEST:** `TestSubscriberAccountLifecycleRevokesPrincipalSessionsAuthenticatorsAndDelegation`.
  - **TEST MATRIX:** `PRIMARY=TestSubscriberAccountLifecycleRevokesPrincipalSessionsAuthenticatorsAndDelegation`; `PROPERTY=TestTodo_AUTHN_009_Property`; `GOLDEN=TestTodo_AUTHN_009_Golden`; `RACE=TestTodo_AUTHN_009_Race`; `INTEGRATION=TestTodo_AUTHN_009_Integration`; `FAULT=TestTodo_AUTHN_009_Fault`; `SECURITY=TestTodo_AUTHN_009_Security`; `CONFORMANCE=TestTodo_AUTHN_009_Conformance`; `RECOVERY=TestTodo_AUTHN_009_Recovery`; `MODEL_BASED=TestTodo_AUTHN_009_ModelBased`; `MUTATION=TestTodo_AUTHN_009_Mutation`.
  - **RED:** disabled/unlinked identity authenticates, live or queued authority survives revocation epoch, token/authenticator/federation/delegation remains usable, weak recovery elevates assurance or concurrent authenticate wins after committed revoke.
  - **GREEN:** durable account/person/principal/tenant bindings drive immutable lifecycle transitions and revocation epochs; all derived sessions, token families, authenticators and delegation chains are fenced, recovery is assurance-bounded and relink preserves history/evidence.
  - **REFACTOR:** lifecycle fan-out consumes canonical authority relationships; providers never own HCM Next principal truth.
  - **Refs:** [security models](data/models/security-trust.md), [identity foundation](specs/platform-foundation-gap-closure.md), [production identity](#32-production-identity-key-custody-and-edge-enforcement).

- [ ] `LEGAL-007` **[PHASE_2][SOL_HIGH] Select, compose, review and safely roll back applicable legal RulePacks.**
  - **Depends:** `LEGAL-001`, `LEGAL-002`, `RULE-002`, `MODEL-018`, `SELECT-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.REGULATORY,BI.PEOPLE,BI.WORKFORCE,BI.REWARDS,BI.PAYROLL; DIRECT=none; WHY=turn resolved jurisdiction context into qualified, version-pinned legal composition without ambient specificity guesses`.
  - **TEST:** `TestRulePackSelectionCompositionCounselReviewAndRollbackAreDeterministic`.
  - **TEST MATRIX:** `PRIMARY=TestRulePackSelectionCompositionCounselReviewAndRollbackAreDeterministic`; `PROPERTY=TestTodo_LEGAL_007_Property`; `GOLDEN=TestTodo_LEGAL_007_Golden`; `FAULT=TestTodo_LEGAL_007_Fault`; `SECURITY=TestTodo_LEGAL_007_Security`; `CONFORMANCE=TestTodo_LEGAL_007_Conformance`; `RECOVERY=TestTodo_LEGAL_007_Recovery`; `MODEL_BASED=TestTodo_LEGAL_007_ModelBased`; `MUTATION=TestTodo_LEGAL_007_Mutation`.
  - **RED:** overlapping government/CBA/contract/company packs choose locale/current/most-specific implicitly, material interpretation lacks current qualified counsel approval, stale/quarantined/withdrawn release evaluates, rollback revives withdrawn law or historical replay uses today's pack.
  - **GREEN:** resolver pins JurisdictionContext digest, ordered releases and explicit per-family strategy, records included/excluded/conflicts/counsel scope/effective-known time; uncertainty produces zero-effect `UNKNOWN|CONFLICT|REVIEW_REQUIRED`, and rollback fences in-flight work plus emits impact/recompute/reapproval obligations.
  - **REFACTOR:** selection/composition traces are reusable legal-engine results; workflows consume typed decisions and obligations.
  - **Refs:** [regulatory computation](specs/platform-architecture-catalog.md), [governance composition](specs/governance-decision-and-obligation-composition.md), [legal models](data/models/rewards-payroll-workforce.md).

- [x] `IAC-013` **[GATE_A][SOL_HIGH] Select and qualify the concrete IaC toolchain and state authority.**
  - **Evidence (2026-09-05):** `TestIaCToolchainSelectionRequiresPinnedEngineProvidersStateFencingAndRecovery` in `tools/planning/iac` (signed manifest pins engine/providers/modules/images, isolated encrypted state backend and locks, plan/apply roles, promotion and recovery; clean sandbox plan/apply/destroy/restore is digest reproducible and drift is explicit; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/planning/iac/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `IAC-001`, `IAC-012`, `TOOL-025`, `OPS-007`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=bind provider-neutral deployment contracts to pinned planning/apply/state-lock/recovery mechanics`.
  - **TEST:** `TestIaCToolchainSelectionRequiresPinnedEngineProvidersStateFencingAndRecovery`.
  - **TEST MATRIX:** `PRIMARY=TestIaCToolchainSelectionRequiresPinnedEngineProvidersStateFencingAndRecovery`; `PROPERTY=TestTodo_IAC_013_Property`; `GOLDEN=TestTodo_IAC_013_Golden`; `INTEGRATION=TestTodo_IAC_013_Integration`; `FAULT=TestTodo_IAC_013_Fault`; `SECURITY=TestTodo_IAC_013_Security`; `CONFORMANCE=TestTodo_IAC_013_Conformance`; `RECOVERY=TestTodo_IAC_013_Recovery`; `MUTATION=TestTodo_IAC_013_Mutation`.
  - **RED:** ambient tool/provider version, shared/unlocked state, same reviewer/apply identity, unverified plan, secret output, non-reproducible apply or untested state loss/restore passes admission.
  - **GREEN:** signed manifest pins engine/providers/modules/images, isolated encrypted state backend and locks, plan/apply roles, promotion and recovery; clean sandbox plan/apply/destroy/restore is digest reproducible and drift is explicit.
  - **REFACTOR:** tool selection implements owned deployment contracts and remains replaceable behind generated manifests.
  - **Refs:** [IaC](#31-infrastructure-as-code-and-physical-data-services), [supply chain](#59-adversarial-infrastructure-edges-and-third-party-tool-qualification), [topology](#product-decision-and-implementation-convergence).

- [x] `EDGE-010` **[GATE_A][SOL_HIGH] Select and qualify concrete production edge and egress implementations.**
  - **Evidence (2026-09-05):** `TestEdgeImplementationManifestRejectsAbstractWildcardOrUnverifiedRouteControl` in `internal/connectivity/edge` (concrete manifest proves exact route/method/body/TLS/DNS/east-west/egress allowlist, logging/redaction, rate/abuse budgets, ownership, degraded/outage and migration behavior against endpoint and threat registries; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/connectivity/edge/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `EDGE-001`, `EDGE-008`, `IAC-004`, `IAC-013`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=bind ingress, TLS, DNS, WAF, east-west and egress policy to real versioned controls and failure behavior`.
  - **TEST:** `TestEdgeImplementationManifestRejectsAbstractWildcardOrUnverifiedRouteControl`.
  - **TEST MATRIX:** `PRIMARY=TestEdgeImplementationManifestRejectsAbstractWildcardOrUnverifiedRouteControl`; `PROPERTY=TestTodo_EDGE_010_Property`; `GOLDEN=TestTodo_EDGE_010_Golden`; `FUZZ=FuzzTodo_EDGE_010`; `INTEGRATION=TestTodo_EDGE_010_Integration`; `FAULT=TestTodo_EDGE_010_Fault`; `SECURITY=TestTodo_EDGE_010_Security`; `CONFORMANCE=TestTodo_EDGE_010_Conformance`; `RECOVERY=TestTodo_EDGE_010_Recovery`; `BENCHMARK=BenchmarkTodo_EDGE_010`; `MUTATION=TestTodo_EDGE_010_Mutation`.
  - **RED:** abstract WAF/proxy/DNS, wildcard/default route, unpinned version, ownerless certificate, fail-open egress, unlogged bypass or absent outage/replacement path reports ready.
  - **GREEN:** concrete manifest proves exact route/method/body/TLS/DNS/east-west/egress allowlist, logging/redaction, rate/abuse budgets, ownership, degraded/outage and migration behavior against endpoint and threat registries.
  - **REFACTOR:** edge products remain adapters to HCM Next route/security policy and cannot define semantic authorization.
  - **Refs:** [edge enforcement](#32-production-identity-key-custody-and-edge-enforcement), [endpoint contract](specs/http-grpc-endpoint-contract.md), [threat registry](#product-decision-and-implementation-convergence).

- [x] `PROVIDER-002` **[GATE_A][SOL_HIGH] Detect, quarantine and reconcile selected-provider contract drift.**
  - **Evidence (2026-09-05):** `TestProviderDriftDetectionQuarantinesAffectedOperationsUntilReviewedCompatibility` in `internal/connectivity/providerdrift` (recurring probe returns typed diff and affected capabilities/intents, fences new operations, preserves/observes in-flight work and resumes only after compatible adapter/config rollout or explicit RESELECT; unknown never reports healthy; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/connectivity/providerdrift/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PROVIDER-001`, `INTG-014`, `ROLLOUT-008`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.INTEGRATION,BI.PEOPLE,BI.REWARDS,BI.ACCESS; DIRECT=none; WHY=prevent live provider schema, entitlement, quota or behavior drift from silently changing accepted intent execution`.
  - **TEST:** `TestProviderDriftDetectionQuarantinesAffectedOperationsUntilReviewedCompatibility`.
  - **TEST MATRIX:** `PRIMARY=TestProviderDriftDetectionQuarantinesAffectedOperationsUntilReviewedCompatibility`; `PROPERTY=TestTodo_PROVIDER_002_Property`; `GOLDEN=TestTodo_PROVIDER_002_Golden`; `INTEGRATION=TestTodo_PROVIDER_002_Integration`; `FAULT=TestTodo_PROVIDER_002_Fault`; `SECURITY=TestTodo_PROVIDER_002_Security`; `CONFORMANCE=TestTodo_PROVIDER_002_Conformance`; `RECOVERY=TestTodo_PROVIDER_002_Recovery`; `MUTATION=TestTodo_PROVIDER_002_Mutation`.
  - **RED:** removed/changed field, API version, permission, quota, error or webhook contract differs from signed topology while new dispatch continues or in-flight ambiguity is hidden.
  - **GREEN:** recurring probe returns typed diff and affected capabilities/intents, fences new operations, preserves/observes in-flight work and resumes only after compatible adapter/config rollout or explicit `RESELECT`; unknown never reports healthy.
  - **REFACTOR:** reuse provider schema/support manifests and rollout gates; avoid provider-specific drift logic in workflows.
  - **Refs:** [integration drift](specs/integration-platform.md), [provider selection](#product-decision-and-implementation-convergence), [progressive rollout](#42-confidential-actors-abuse-detection-and-progressive-rollout).

- [x] `PROVIDER-003` **[GATE_B][SOL_HIGH] Reconcile selected-provider exit, export, revocation and retained obligations.**
  - **Evidence (2026-09-05):** `TestProviderExitEnumeratesExportsRevokesAndReconcilesEveryRemoteResource` in `internal/connectivity/providerexit` (provider-specific exit inventory dispositions every remote resource and operation, verifies export digests, revokes credentials/endpoints/grants, preserves required holds/evidence, reconciles zero active authority and returns explicit pending/manual/legal obligations for unverifi; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/connectivity/providerexit/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PROVIDER-002`, `WEDGE-009`, `OPS-009`, `RECORDS-HOLD-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.INTEGRATION,BI.TENANT,BI.PRIVACY; DIRECT=none; WHY=prove provider exit removes active authority without losing required data, evidence, holds or ambiguous in-flight outcomes`.
  - **TEST:** `TestProviderExitEnumeratesExportsRevokesAndReconcilesEveryRemoteResource`.
  - **TEST MATRIX:** `PRIMARY=TestProviderExitEnumeratesExportsRevokesAndReconcilesEveryRemoteResource`; `PROPERTY=TestTodo_PROVIDER_003_Property`; `GOLDEN=TestTodo_PROVIDER_003_Golden`; `INTEGRATION=TestTodo_PROVIDER_003_Integration`; `FAULT=TestTodo_PROVIDER_003_Fault`; `SECURITY=TestTodo_PROVIDER_003_Security`; `CONFORMANCE=TestTodo_PROVIDER_003_Conformance`; `RECOVERY=TestTodo_PROVIDER_003_Recovery`; `MUTATION=TestTodo_PROVIDER_003_Mutation`.
  - **RED:** provider object/request/webhook/credential remains unenumerated or active, export is incomplete/ambiguous, deletion violates hold/retention, provider outage is treated as zero authority or final evidence cannot be independently verified.
  - **GREEN:** provider-specific exit inventory dispositions every remote resource and operation, verifies export digests, revokes credentials/endpoints/grants, preserves required holds/evidence, reconciles zero active authority and returns explicit pending/manual/legal obligations for unverifiable remote state.
  - **REFACTOR:** exit consumes generic tenant/privacy/vendor-continuity contracts plus provider inventory adapter.
  - **Refs:** [tenant exit](specs/platform-responsibility-boundaries.md), [records](specs/records-management-and-disposition.md), [integration lifecycle](specs/integration-platform.md).

- [ ] `RECOVERY-005` **[GATE_B][SOL_HIGH] Restore ledger, projections, outbox, connector journals, search and artifacts as one fenced consistency set.**
  - **Depends:** `RECOVERY-003`, `CONN-RT-003`, `EVENT-004`, `DATA-021`, `DB-022`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=ensure restored internal chronology and derived/external work agree before any provider effect can resume`.
  - **TEST:** `TestCrossPlaneRestoreReconcilesHeadsWatermarksArtifactsAndJournalsWithoutRedrive`.
  - **TEST MATRIX:** `PRIMARY=TestCrossPlaneRestoreReconcilesHeadsWatermarksArtifactsAndJournalsWithoutRedrive`; `PROPERTY=TestTodo_RECOVERY_005_Property`; `GOLDEN=TestTodo_RECOVERY_005_Golden`; `RACE=TestTodo_RECOVERY_005_Race`; `INTEGRATION=TestTodo_RECOVERY_005_Integration`; `FAULT=TestTodo_RECOVERY_005_Fault`; `SECURITY=TestTodo_RECOVERY_005_Security`; `CONFORMANCE=TestTodo_RECOVERY_005_Conformance`; `RECOVERY=TestTodo_RECOVERY_005_Recovery`; `BENCHMARK=BenchmarkTodo_RECOVERY_005`; `MODEL_BASED=TestTodo_RECOVERY_005_ModelBased`; `MUTATION=TestTodo_RECOVERY_005_Mutation`.
  - **RED:** restore loses/reorders outbox or attempts, regresses watermark, mismatches artifact/search/projection digest, contacts production destination, auto-redrives timeout-after-send or reports success while remote state is unknown.
  - **GREEN:** isolated restore verifies exact stream heads, journals, dedupe keys, checkpoints, authority/cutover watermarks, artifact inventory and derived digests within RPO/RTO; dispatch stays fenced and unknown external state becomes `OBSERVATION_PENDING|REPAIR_REQUIRED`.
  - **REFACTOR:** one recovery manifest coordinates dependency order and verification across adapters; it never rewrites authoritative history.
  - **Refs:** [disaster recovery](specs/platform-responsibility-boundaries.md), [integration journals](specs/integration-platform.md), [data plane](specs/platform-plane-model.md).

- [ ] `CONN-RT-009` **[GATE_B][SOL_HIGH] Preserve terminal ambiguity and no-redrive fencing across crash and restore.**
  - **Depends:** `CONN-RT-003`, `CONN-RT-007`, `RECOVERY-005`, `REPAIR-002`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.INTEGRATION,BI.OPERATIONS; DIRECT=none; WHY=prevent restored workers or lease epochs from repeating accepted, terminal or ambiguous external operations`.
  - **TEST:** `TestRestoredConnectorOperationRequiresObservationOrGovernedRepairBeforeRedrive`.
  - **TEST MATRIX:** `PRIMARY=TestRestoredConnectorOperationRequiresObservationOrGovernedRepairBeforeRedrive`; `PROPERTY=TestTodo_CONN_RT_009_Property`; `GOLDEN=TestTodo_CONN_RT_009_Golden`; `RACE=TestTodo_CONN_RT_009_Race`; `INTEGRATION=TestTodo_CONN_RT_009_Integration`; `FAULT=TestTodo_CONN_RT_009_Fault`; `SECURITY=TestTodo_CONN_RT_009_Security`; `CONFORMANCE=TestTodo_CONN_RT_009_Conformance`; `RECOVERY=TestTodo_CONN_RT_009_Recovery`; `MODEL_BASED=TestTodo_CONN_RT_009_ModelBased`; `MUTATION=TestTodo_CONN_RT_009_Mutation`.
  - **RED:** stale worker/lease dispatches after restore, terminal or timeout-after-send attempt automatically retries, missing provider observation is treated as not applied or governed repair creates a second logical operation accidentally.
  - **GREEN:** restored epochs fence stale workers, terminal/ambiguous operation is durable `NO_REDRIVE`, observation evidence resolves state, and only approved RepairPlan may create an explicitly related new attempt with distinct effect identity.
  - **REFACTOR:** connector runtime centralizes restored-state transition rules and provider adapters only supply observation capability.
  - **Refs:** [connector runtime](specs/integration-platform.md), [repair](specs/transaction-ledger-reconciliation-and-repair.md), [recovery](specs/platform-responsibility-boundaries.md).

- [ ] `DATA-022` **[P0][SOL_HIGH] Prove universal per-intent storage, event, effect and correction lineage.**
  - **Depends:** `DATA-015`, `SLICE-016`, `MODEL-032`, `LEDGER-012`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=extend Promotion lineage proof to every intent family, composite child and trigger path`.
  - **TEST:** `TestEveryAcceptedIntentFamilyHasCompleteAuthorizedRebuildableLineage`.
  - **TEST MATRIX:** `PRIMARY=TestEveryAcceptedIntentFamilyHasCompleteAuthorizedRebuildableLineage`; `PROPERTY=TestTodo_DATA_022_Property`; `GOLDEN=TestTodo_DATA_022_Golden`; `INTEGRATION=TestTodo_DATA_022_Integration`; `FAULT=TestTodo_DATA_022_Fault`; `SECURITY=TestTodo_DATA_022_Security`; `CONFORMANCE=TestTodo_DATA_022_Conformance`; `RECOVERY=TestTodo_DATA_022_Recovery`; `MUTATION=TestTodo_DATA_022_Mutation`.
  - **RED:** intent/proposal/workflow/transaction/event/projection/outbox/effect/observation/reconciliation/repair/correction link is missing, cross-tenant, over-disclosing or non-rebuildable while aggregate completion reports true.
  - **GREEN:** family fixtures prove complete authorized redaction-safe lineage with child/trigger causation, exact watermarks/digests and `UNKNOWN|PARTIAL` instead of false completion; correction appends without breaking historical trace.
  - **REFACTOR:** generate cases from closure witnesses and shared lineage assertions; keep domain-specific events in their owners.
  - **Refs:** [provenance](specs/provenance-graph-and-lineage.md), [ledger](specs/transaction-ledger-reconciliation-and-repair.md), [intent coverage](data/models/intent-coverage-matrix.md).

- [ ] `DATA-023` **[GATE_B][SOL_HIGH] Prove object and artifact backup, restore and cryptographic lifecycle.**
  - **Depends:** `ARTIFACT-006`, `DATA-016`, `DB-022`, `RECOVERY-005`, `RECORDS-HOLD-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.DOCUMENTS,BI.CASES,BI.PRIVACY,BI.DATAOPS; DIRECT=none; WHY=restore governed bytes and metadata without resurrecting restricted or cryptographically erased content`.
  - **TEST:** `TestArtifactRestoreReconcilesBytesMetadataKeysHoldsTombstonesAndLineage`.
  - **TEST MATRIX:** `PRIMARY=TestArtifactRestoreReconcilesBytesMetadataKeysHoldsTombstonesAndLineage`; `PROPERTY=TestTodo_DATA_023_Property`; `GOLDEN=TestTodo_DATA_023_Golden`; `INTEGRATION=TestTodo_DATA_023_Integration`; `FAULT=TestTodo_DATA_023_Fault`; `SECURITY=TestTodo_DATA_023_Security`; `CONFORMANCE=TestTodo_DATA_023_Conformance`; `RECOVERY=TestTodo_DATA_023_Recovery`; `BENCHMARK=BenchmarkTodo_DATA_023`; `MUTATION=TestTodo_DATA_023_Mutation`.
  - **RED:** missing/orphan bytes or refs, wrong digest/key, lost hold/tombstone, restored deleted content, absent crypto-erasure proof, stale authorization or unmet object RPO/RTO reports restored.
  - **GREEN:** restore inventories bytes/metadata/refs/digests/key versions/holds/tombstones, re-verifies access and lifecycle, preserves explicit erased/unavailable states and reconciles every artifact to ledger lineage without new domain event/effect.
  - **REFACTOR:** object-store adapter implements the semantic Artifact lifecycle and independent verifier.
  - **Refs:** [artifact storage](specs/platform-responsibility-boundaries.md), [records](specs/records-management-and-disposition.md), [recovery](specs/platform-responsibility-boundaries.md).

- [x] `WF-RUN-026` **[PHASE_2][SOL_HIGH] Migrate pending timers, signals, work and child continuations with workflow state.**
  - **Evidence (2026-09-05):** `TestWorkflowMigrationPreservesPendingTimerSignalWorkAndContinuationIdentity` in `internal/workflow/migrate/artifacts`, `internal/data/runtimestate` (artifact migration inside the WF-RUN-018 transaction with six handlers in fixed order (lease, timer, signal, ready work, approval, child continuation) sharing one versioned Contract and producing a canonical digested Receipt with CARRIED, REKEYED or DEDUPLICATED dispositions; a timer is re-keyed to the new plan's requirement digest and refused WAKE_INSTANT_MOVED unless FireAt is bit-identical, subscriptions keep signal and correlation key, ready work keeps eligibility, priority and attempt, approvals and awaited child links are carried with owner and deadline and refuse relocation NOT_RELOCATABLE because work_item and workflow_child_link nodes are immutable by trigger, a stale, foreign, expired or absent fence refuses the whole run, exactly one continuation ledger row per instance, node, attempt and READY, MarkRepairRequired walking the legal PAUSED to CANCELLING to REPAIR_REQUIRED path; additive runtimestate helpers OpenForInstance, CloseSubscription (CAS), PendingForInstance and LinksForParent; written by an Opus subagent and verified independently); `go test -count=1 ./internal/workflow/migrate/artifacts/ ./internal/data/runtimestate/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WF-RUN-017`, `WF-RUN-018`, `WF-RUN-025`, `UXFLOW-011`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=prevent live workflow migration from losing or duplicating separately persisted pending runtime artifacts`.
  - **TEST:** `TestWorkflowMigrationPreservesPendingTimerSignalWorkAndContinuationIdentity`.
  - **TEST MATRIX:** `PRIMARY=TestWorkflowMigrationPreservesPendingTimerSignalWorkAndContinuationIdentity`; `PROPERTY=TestTodo_WF_RUN_026_Property`; `GOLDEN=TestTodo_WF_RUN_026_Golden`; `RACE=TestTodo_WF_RUN_026_Race`; `INTEGRATION=TestTodo_WF_RUN_026_Integration`; `FAULT=TestTodo_WF_RUN_026_Fault`; `SECURITY=TestTodo_WF_RUN_026_Security`; `CONFORMANCE=TestTodo_WF_RUN_026_Conformance`; `RECOVERY=TestTodo_WF_RUN_026_Recovery`; `MODEL_BASED=TestTodo_WF_RUN_026_ModelBased`; `MUTATION=TestTodo_WF_RUN_026_Mutation`.
  - **RED:** migrated instance loses/duplicates timer wake, signal subscription, ready work/approval, child continuation, ownership/deadline or accepts stale lease; partial failure strands mixed old/new artifacts.
  - **GREEN:** atomic migration transforms and deduplicates every pending artifact under new epoch while preserving semantic identity/owner/deadline; failure leaves old instance runnable or durably `REPAIR_REQUIRED` with zero duplicate continuation.
  - **REFACTOR:** artifact migration handlers share versioned runtime-state migration contracts and canonical receipts.
  - **Refs:** [workflow runtime](specs/workflow-runtime.md), [workflow evolution](#8-durable-workflow-runtime-recovery-and-intervention), [user-flow evolution](#product-decision-and-implementation-convergence).

- [ ] `ENGINE-CONF-001` **[P0][SOL_HIGH] Enforce one immutable deterministic request/result contract across shared HCM engines.**
  - **Depends:** `XFORM-003`, `POP-003`, `ELIG-003`, `CYCLE-003`, `BAL-004`, `QUAL-004`, `DEMAND-003`, `MATCH-004`, `SCENARIO-003`, `ATTEST-004`, `PROGRAM-CONF-001`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=ensure reusable engines propagate context, uncertainty, classification and provenance consistently without hidden effects`.
  - **TEST:** `TestSharedEngineEnvelopePinsContextPropagatesUnknownAndReplaysDeterministically`.
  - **TEST MATRIX:** `PRIMARY=TestSharedEngineEnvelopePinsContextPropagatesUnknownAndReplaysDeterministically`; `PROPERTY=TestTodo_ENGINE_CONF_001_Property`; `GOLDEN=TestTodo_ENGINE_CONF_001_Golden`; `FUZZ=FuzzTodo_ENGINE_CONF_001`; `SECURITY=TestTodo_ENGINE_CONF_001_Security`; `CONFORMANCE=TestTodo_ENGINE_CONF_001_Conformance`; `MODEL_BASED=TestTodo_ENGINE_CONF_001_ModelBased`; `MUTATION=TestTodo_ENGINE_CONF_001_Mutation`.
  - **RED:** engine omits definition/revision, tenant/org/purpose/authority, effective/known time, source watermarks, classification/provenance or digest; converts `UNKNOWN|PARTIAL` to confident result, reads ambient time/config or emits side effect.
  - **GREEN:** all named engines pass one fixture suite with immutable typed envelope, pinned inputs/context, exact uncertainty/taint propagation, canonical result/explanation digest, deterministic replay and zero persistence/effects.
  - **REFACTOR:** common envelope belongs to the kernel/engine boundary while domain-specific request/result semantics remain engine-owned.
  - **Refs:** [engine map](#37-shared-transformation-and-population-engines), [canonical envelope](specs/canonical-envelope-and-digest.md), [epistemic integrity](plan.md#513-preserve-epistemic-integrity).

- [ ] `PERF-009` **[GATE_B][SOL_HIGH] Model and verify long-horizon retention, storage growth and lifecycle cost.**
  - **Depends:** `PERF-005`, `RECORDS-COPY-001`, `RECORDS-HOLD-001`, `RECORDS-DISP-001`, `DATA-023`, `PERF-007`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=prove append-only history, artifacts, holds and telemetry remain operable and affordable beyond short load tests`.
  - **TEST:** `TestLongHorizonRetentionStorageGrowthAndLifecycleStayWithinBudget`.
  - **TEST MATRIX:** `PRIMARY=TestLongHorizonRetentionStorageGrowthAndLifecycleStayWithinBudget`; `PROPERTY=TestTodo_PERF_009_Property`; `GOLDEN=TestTodo_PERF_009_Golden`; `INTEGRATION=TestTodo_PERF_009_Integration`; `FAULT=TestTodo_PERF_009_Fault`; `SECURITY=TestTodo_PERF_009_Security`; `CONFORMANCE=TestTodo_PERF_009_Conformance`; `RECOVERY=TestTodo_PERF_009_Recovery`; `BENCHMARK=BenchmarkTodo_PERF_009`; `MODEL_BASED=TestTodo_PERF_009_ModelBased`; `MUTATION=TestTodo_PERF_009_Mutation`.
  - **RED:** 30/90/365-day modeled growth exceeds partition/WAL/archive/vacuum/index/object/telemetry/legal-hold headroom or cost while benchmark reports pass; lifecycle deletes held data or leaves unbounded bloat.
  - **GREEN:** versioned workload and retention models predict/measure rollover, maintenance, archive/restore, object lifecycle, held-data exceptions and GB-month cost with declared headroom, alarms and safe capacity expansion thresholds.
  - **REFACTOR:** growth model consumes real workload, retention and cost registries; it does not embed tenant-specific constants.
  - **Refs:** [performance](#36-operator-surfaces-ownership-and-measurable-production-limits), [records](specs/records-management-and-disposition.md), [operations models](data/models/operations-production.md).

- [x] `TOOL-025` **[P0][TERRA] Inventory and attest non-module build, test and release tool inputs.**
  - **Depends:** `TOOL-017`, `TOOL-018`, `TOOL-019`, `TOOL-023`, `TOOL-024`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=cover generators, plugins, CI actions and container tools outside the Go module graph without granting them semantic ownership`.
  - **TEST:** `TestToolchainSupplyChainManifestRejectsUntrackedToolInput`.
  - **TEST MATRIX:** `PRIMARY=TestToolchainSupplyChainManifestRejectsUntrackedToolInput`; `PROPERTY=TestTodo_TOOL_025_Property`; `GOLDEN=TestTodo_TOOL_025_Golden`; `SECURITY=TestTodo_TOOL_025_Security`; `CONFORMANCE=TestTodo_TOOL_025_Conformance`; `MUTATION=TestTodo_TOOL_025_Mutation`.
  - **RED:** Buf/protoc/plugin, Cosign, scanner, migration tool, container image, downloaded generator or CI action lacks pinned digest/version, source, license/CVE status, owner, update SLA, provenance or replacement path.
  - **GREEN:** signed manifest set-equals every non-module tool/image/action entering build/test/SBOM/admission, verifies offline, detects drift and records vulnerability/license/update/replacement evidence.
  - **REFACTOR:** generate release attestations from the same inventory and keep mechanics behind owned interfaces.
  - **Refs:** [supply chain](#33-cicd-release-admission-and-observability), [tool qualification](#59-adversarial-infrastructure-edges-and-third-party-tool-qualification), [Go constitution](specs/go-only-technology-constitution.md).
  - **Evidence (2026-09-03):** `TestToolchainSupplyChainManifestRejectsUntrackedToolInput` plus golden/property/security/conformance/mutation tests in `tools/quality/toolinventory` (`definitions/toolchain/tool-inventory.yaml` from gen/TOOLS.lock, go.mod tool directives, package.json/package-lock.json, .husky; drift test); PASS; branch plan-revision-2026-09-02; `go test -count=1 ./tools/quality/toolinventory/...` on windows/arm64 (Go 1.26.3).

- [x] `CONFIG-010` **[GATE_A][SOL_HIGH] Revalidate active workloads when configuration dependencies are revoked or become incompatible.**
  - **Evidence (2026-09-05):** `TestActiveWorkloadDependencyRevocationProducesDeterministicInvalidateReplanMigrateOrBlock` in `internal/platform/config/revalidation` (dependency closure revalidation atomically chooses INVALIDATED|REPLAN_REQUIRED|MIGRATE_REQUIRED|BLOCKED, fences pending work, preserves evidence and requires reviewed mixed-object compatibility/migration/dual-read-write or publication blocking before resume; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/platform/config/revalidation/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `CONFIG-003`, `CP-009`, `WF-RUN-018`, `ROLLOUT-008`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=prevent paused or waiting workflows from resuming under revoked rules, forms, mappings, populations, schemas or capabilities`.
  - **TEST:** `TestActiveWorkloadDependencyRevocationProducesDeterministicInvalidateReplanMigrateOrBlock`.
  - **TEST MATRIX:** `PRIMARY=TestActiveWorkloadDependencyRevocationProducesDeterministicInvalidateReplanMigrateOrBlock`; `PROPERTY=TestTodo_CONFIG_010_Property`; `GOLDEN=TestTodo_CONFIG_010_Golden`; `RACE=TestTodo_CONFIG_010_Race`; `INTEGRATION=TestTodo_CONFIG_010_Integration`; `FAULT=TestTodo_CONFIG_010_Fault`; `SECURITY=TestTodo_CONFIG_010_Security`; `CONFORMANCE=TestTodo_CONFIG_010_Conformance`; `RECOVERY=TestTodo_CONFIG_010_Recovery`; `MODEL_BASED=TestTodo_CONFIG_010_ModelBased`; `MUTATION=TestTodo_CONFIG_010_Mutation`.
  - **RED:** dependency revocation/incompatibility leaves timer/task/approval runnable, mixed rule/form/workflow/mapping/population/schema versions silently compose, pending human work remains actionable or replay duplicates effects.
  - **GREEN:** dependency closure revalidation atomically chooses `INVALIDATED|REPLAN_REQUIRED|MIGRATE_REQUIRED|BLOCKED`, fences pending work, preserves evidence and requires reviewed mixed-object compatibility/migration/dual-read-write or publication blocking before resume.
  - **REFACTOR:** one compatibility graph serves publication, rollout and live runtime revalidation across all configuration object types.
  - **Refs:** [configuration control](#30-control-plane-publication-distribution-and-activation), [workflow migration](specs/workflow-runtime.md), [rollout](#42-confidential-actors-abuse-detection-and-progressive-rollout).

- [ ] `GATEB-EVID-001` **[GATE_B][SOL_HIGH] Compile post-authority Gate B operating and continuation evidence.**
  - **Depends:** `GOV-003`, `GOV-023`, `OPS-008`, `RECOVERY-003`, `RECOVERY-005`, `ASSURANCE-001`, `PILOT-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.OPERATIONS,BI.SECURITY,BI.PRIVACY,BI.COMMERCIAL; DIRECT=none; WHY=prevent an authority-expansion gate from passing on prose, aggregate status or stale evidence`.
  - **TEST:** `TestGateBEvidenceManifestRejectsUnboundStaleFailedOrUnsignedCriterion`.
  - **TEST MATRIX:** `PRIMARY=TestGateBEvidenceManifestRejectsUnboundStaleFailedOrUnsignedCriterion`; `PROPERTY=TestTodo_GATEB_EVID_001_Property`; `GOLDEN=TestTodo_GATEB_EVID_001_Golden`; `FAULT=TestTodo_GATEB_EVID_001_Fault`; `SECURITY=TestTodo_GATEB_EVID_001_Security`; `CONFORMANCE=TestTodo_GATEB_EVID_001_Conformance`; `RECOVERY=TestTodo_GATEB_EVID_001_Recovery`; `MUTATION=TestTodo_GATEB_EVID_001_Mutation`.
  - **RED:** removing/expiring a Gate B test, fixture, owner, command, oracle, artifact, retention or approval still yields pass, or waiver grants authority without scope/control/expiry.
  - **GREEN:** generated manifest maps every acceptance bullet to exact todo/test/fixture/command/result/evidence digest/owner/retention/expiry/sign-off; missing/stale/failed/unsigned returns `GATE_BLOCKED` and grants zero additional authority.
  - **REFACTOR:** gate decision consumes immutable evidence manifests and retains prior decisions append-only.
  - **Refs:** [Gate B](execution-plan.md), [evidence governance](#backlog-governance-and-traceability), [authority expansion](specs/competitive-positioning-and-authority-expansion.md).

- [ ] `CROSS-CONF-001` **[PHASE_2][SOL_HIGH] Prove selected-jurisdiction behavior across Promotion and Medical Leave end to end.**
  - **Depends:** `SELECT-001`, `LEGAL-007`, `PROMO-004`, `LEAVE-006`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.PEOPLE,BI.REWARDS,BI.WORKFORCE,BI.REGULATORY; DIRECT=none; WHY=prove the same jurisdiction and legal-composition contracts work across a transactional promotion and a long-running protected leave process`.
  - **TEST:** `TestSelectedJurisdictionPromotionAndLeaveHandleAmbiguityRuleTimeAndReplanConsistently`.
  - **TEST MATRIX:** `PRIMARY=TestSelectedJurisdictionPromotionAndLeaveHandleAmbiguityRuleTimeAndReplanConsistently`; `PROPERTY=TestTodo_CROSS_CONF_001_Property`; `GOLDEN=TestTodo_CROSS_CONF_001_Golden`; `FAULT=TestTodo_CROSS_CONF_001_Fault`; `SECURITY=TestTodo_CROSS_CONF_001_Security`; `CONFORMANCE=TestTodo_CROSS_CONF_001_Conformance`; `RECOVERY=TestTodo_CROSS_CONF_001_Recovery`; `MODEL_BASED=TestTodo_CROSS_CONF_001_ModelBased`; `MUTATION=TestTodo_CROSS_CONF_001_Mutation`.
  - **RED:** Promotion omits LegalContext, either flow guesses ambiguous/multi-location jurisdiction, known-at/effective-at rule change rewrites prior result, material change resumes old proposal or `UNKNOWN` still creates approval/work/write/effect.
  - **GREEN:** both slices pin the same selected jurisdiction and composition releases in snapshots/proposals, preserve historical evaluation, and deterministically return `BLOCKED|REVIEW_REQUIRED|REPLAN_REQUIRED` with successor proposal and zero stale effects for ambiguity or material rule change.
  - **REFACTOR:** shared jurisdiction/legal fixtures drive domain-specific expected obligations; neither workflow embeds legal rules.
  - **Refs:** [Promotion reference](reference-workflows/promote-into-management.md), [Leave slice](#38a-medical-leave-and-return-to-work-convergence-slice), [legal composition](specs/governance-decision-and-obligation-composition.md).

- [x] `CROSS-CONF-002` **[GATE_A][SOL_HIGH] Enforce tenant commercial entitlement consistently across Promotion and Leave channels.**
  - **Evidence (2026-09-05):** `TestPromotionAndLeaveEntitlementDenialParityAcrossAllInitiationAndResumeChannels` in `internal/workflow/conformance/leave` (action discovery, submit, resume and assisted/manual routes share the pinned entitlement decision and non-disclosing typed denial; zero unauthorized state/effects occur, accepted executions retain their original fingerprint and amendments govern only declared future executions; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/workflow/conformance/leave/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `COMM-001`, `PROMO-007`, `LEAVE-001`, `ENDPOINT-009`, `UXFLOW-007`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.PEOPLE,BI.REWARDS,BI.WORKFORCE,BI.COMMERCIAL; DIRECT=none; WHY=prevent UI, gRPC, grpcbridge, assisted or resumed flows from bypassing suspended, expired or amended pilot entitlement`.
  - **TEST:** `TestPromotionAndLeaveEntitlementDenialParityAcrossAllInitiationAndResumeChannels`.
  - **TEST MATRIX:** `PRIMARY=TestPromotionAndLeaveEntitlementDenialParityAcrossAllInitiationAndResumeChannels`; `PROPERTY=TestTodo_CROSS_CONF_002_Property`; `GOLDEN=TestTodo_CROSS_CONF_002_Golden`; `RACE=TestTodo_CROSS_CONF_002_Race`; `INTEGRATION=TestTodo_CROSS_CONF_002_Integration`; `FAULT=TestTodo_CROSS_CONF_002_Fault`; `SECURITY=TestTodo_CROSS_CONF_002_Security`; `CONFORMANCE=TestTodo_CROSS_CONF_002_Conformance`; `BROWSER=TestTodo_CROSS_CONF_002_Browser`; `RECOVERY=TestTodo_CROSS_CONF_002_Recovery`; `MUTATION=TestTodo_CROSS_CONF_002_Mutation`.
  - **RED:** one channel discovers/submits/resumes an unentitled action, denial omits entitlement fingerprint, suspended/expired contract creates intent/work/message/effect, or amendment retroactively changes already accepted execution.
  - **GREEN:** action discovery, submit, resume and assisted/manual routes share the pinned entitlement decision and non-disclosing typed denial; zero unauthorized state/effects occur, accepted executions retain their original fingerprint and amendments govern only declared future executions.
  - **REFACTOR:** all channel adapters invoke one capability/governance entitlement contract rather than duplicating feature flags.
  - **Refs:** [pilot entitlement](#tenant-lifecycle-placement-sandbox-and-commercial-pilot), [endpoint parity](specs/http-grpc-endpoint-contract.md), [user flows](user-flows/README.md).

## 63. Untraveled operational and delivery surfaces

> **Disposition (2026-09-02):** Gate C, except items the P1A read-only path directly depends on.

These are cross-cutting surfaces discovered by tracing how software, evidence,
communications and trust reach real people and production environments. They do
not add HCM domain scope. Each closes a boundary that could otherwise invalidate
an already-designed BusinessIntent path.

- [x] `CLIENT-001` **[GATE_C][SOL_HIGH] Secure and attest browser-delivered code, policy, storage and cache lifecycle.**
  - **Depends:** `EDGE-004`, `UX-002`, `TOOL-016`, `TOOL-018`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=ensure the browser cannot execute stale, injected or cross-tenant client state that changes governed intent behavior`.
  - **TEST:** `TestBrowserArtifactPolicyCacheAndStorageLifecycleRejectsStaleInjectedOrSensitiveState`.
  - **TEST MATRIX:** `PRIMARY=TestBrowserArtifactPolicyCacheAndStorageLifecycleRejectsStaleInjectedOrSensitiveState`; `PROPERTY=TestTodo_CLIENT_001_Property`; `GOLDEN=TestTodo_CLIENT_001_Golden`; `FUZZ=FuzzTodo_CLIENT_001`; `INTEGRATION=TestTodo_CLIENT_001_Integration`; `FAULT=TestTodo_CLIENT_001_Fault`; `SECURITY=TestTodo_CLIENT_001_Security`; `CONFORMANCE=TestTodo_CLIENT_001_Conformance`; `BROWSER=TestTodo_CLIENT_001_Browser`; `RECOVERY=TestTodo_CLIENT_001_Recovery`; `MUTATION=TestTodo_CLIENT_001_Mutation`.
  - **RED:** unsigned/unpinned asset, CSP bypass, DOM injection, service-worker downgrade, stale schema/action cache, browser back-forward cache, shared-device residue or local/session storage contains sensitive domain data and still permits submission.
  - **GREEN:** release manifest binds asset digests and strict CSP/trusted-types policy; cache/service-worker upgrade is atomic, stale actions revalidate server-side, logout/tenant switch clears protected state and browser storage contains only reviewed bounded references.
  - **REFACTOR:** generated security headers and asset manifests share release provenance; the client remains a non-authoritative intent collector.
  - **Refs:** [experience boundary](specs/experience-ui-and-branding.md), [edge security](specs/platform-foundation-gap-closure.md), [release provenance](#33-cicd-release-admission-and-observability).

- [x] `CLIENT-002` **[PHASE_2][SOL_HIGH] Govern mobile, kiosk and offline client device state and resubmission.**
  - **Depends:** `UX-008`, `UXFLOW-009`, `AUTHN-004`, `EDGE-004`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=preserve identity, privacy and idempotency on shared, intermittently connected and managed or unmanaged devices`.
  - **TEST:** `TestMobileKioskOfflineStateIsDeviceBoundExpiringPrivateAndIdempotentlyResubmitted`.
  - **TEST MATRIX:** `PRIMARY=TestMobileKioskOfflineStateIsDeviceBoundExpiringPrivateAndIdempotentlyResubmitted`; `PROPERTY=TestTodo_CLIENT_002_Property`; `GOLDEN=TestTodo_CLIENT_002_Golden`; `FUZZ=FuzzTodo_CLIENT_002`; `RACE=TestTodo_CLIENT_002_Race`; `INTEGRATION=TestTodo_CLIENT_002_Integration`; `FAULT=TestTodo_CLIENT_002_Fault`; `SECURITY=TestTodo_CLIENT_002_Security`; `CONFORMANCE=TestTodo_CLIENT_002_Conformance`; `BROWSER=TestTodo_CLIENT_002_Browser`; `RECOVERY=TestTodo_CLIENT_002_Recovery`; `MODEL_BASED=TestTodo_CLIENT_002_ModelBased`; `MUTATION=TestTodo_CLIENT_002_Mutation`.
  - **RED:** prior kiosk user data remains, screenshot/clipboard/cache exposes restricted fields, rooted/untrusted device silently raises assurance, offline request uses stale authority or repeated reconnect creates duplicate intent/effect.
  - **GREEN:** device/session posture constrains permitted flows, protected local state is minimized/encrypted/expiring and cleared on handoff, every reconnect revalidates proposal/governance and replays one client request identity to one durable result.
  - **REFACTOR:** channel adapters share the canonical request/idempotency contract; device policy never becomes HCM authorization by itself.
  - **Refs:** [cross-channel flows](user-flows/README.md), [authentication](specs/platform-foundation-gap-closure.md), [experience](specs/experience-ui-and-branding.md).

- [ ] `MAIL-001` **[GATE_B][SOL_HIGH] Operate authenticated transactional-email domains and deliverability controls.**
  - **Depends:** `MSG-006`, `MSG-007`, `TRUST-017`, `EDGE-010`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.EXPERIENCE,BI.WORK,BI.CASES,BI.DOCUMENTS; DIRECT=none; WHY=ensure email delivery mechanics and domain reputation cannot falsify business notice or expose protected content`.
  - **TEST:** `TestEmailDomainAuthenticationBounceComplaintAndSuppressionLifecyclePreservesMessageTruth`.
  - **TEST MATRIX:** `PRIMARY=TestEmailDomainAuthenticationBounceComplaintAndSuppressionLifecyclePreservesMessageTruth`; `PROPERTY=TestTodo_MAIL_001_Property`; `GOLDEN=TestTodo_MAIL_001_Golden`; `INTEGRATION=TestTodo_MAIL_001_Integration`; `FAULT=TestTodo_MAIL_001_Fault`; `SECURITY=TestTodo_MAIL_001_Security`; `CONFORMANCE=TestTodo_MAIL_001_Conformance`; `RECOVERY=TestTodo_MAIL_001_Recovery`; `BENCHMARK=BenchmarkTodo_MAIL_001`; `MUTATION=TestTodo_MAIL_001_Mutation`.
  - **RED:** missing/failed SPF, DKIM or DMARC; unverified sender domain; provider webhook spoof; bounce/complaint misattribution; global suppression crosses tenant/purpose; domain reputation failure counts as delivered or retry floods a recipient.
  - **GREEN:** versioned sending-domain profile verifies DNS/authentication and rotation, authenticates feedback, scopes suppression/preferences, rate limits retries and preserves `ACCEPTED|DELIVERED|BOUNCED|COMPLAINED|SUPPRESSED|UNKNOWN` without satisfying legal acknowledgement by transport alone.
  - **REFACTOR:** provider adapters normalize mechanics into Messaging states; templates and HCM workflow remain independent of email vendor policy.
  - **Refs:** [messaging plane](specs/messaging-and-notification-plane.md), [destination trust](specs/platform-foundation-gap-closure.md), [integration platform](specs/integration-platform.md).

- [ ] `FULFILL-001` **[PHASE_3][SOL_HIGH] Govern print and postal fulfillment for required physical notices.**
  - **Depends:** `MSG-013`, `DOC-TEMPLATE-001`, `LOCATION-002`, `TRUST-018`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.EXPERIENCE,BI.DOCUMENTS,BI.REGULATORY,BI.CASES; DIRECT=none; WHY=make physical notice generation, custody, mailing and returned-mail outcomes observable without treating shipment as acknowledgement`.
  - **TEST:** `TestPhysicalNoticeFulfillmentBindsArtifactAddressCustodyAndDeliveryEvidence`.
  - **TEST MATRIX:** `PRIMARY=TestPhysicalNoticeFulfillmentBindsArtifactAddressCustodyAndDeliveryEvidence`; `PROPERTY=TestTodo_FULFILL_001_Property`; `GOLDEN=TestTodo_FULFILL_001_Golden`; `INTEGRATION=TestTodo_FULFILL_001_Integration`; `FAULT=TestTodo_FULFILL_001_Fault`; `SECURITY=TestTodo_FULFILL_001_Security`; `CONFORMANCE=TestTodo_FULFILL_001_Conformance`; `RECOVERY=TestTodo_FULFILL_001_Recovery`; `MUTATION=TestTodo_FULFILL_001_Mutation`.
  - **RED:** wrong artifact/address/version enters batch, vendor sees excess fields, chain of custody breaks, duplicate print/mailing occurs, returned mail is ignored or carrier acceptance is treated as recipient acknowledgement.
  - **GREEN:** fulfillment instruction pins minimized artifact hash, authorized address revision, envelope/privacy profile, vendor operation and custody receipts; results distinguish printed/mailed/in-transit/delivered/returned/unknown and create governed fallback work without duplicate notice.
  - **REFACTOR:** physical fulfillment is a Messaging/Document effect adapter with independent observations and reconciliation.
  - **Refs:** [legal notice delivery](specs/messaging-and-notification-plane.md), [document controls](specs/platform-responsibility-boundaries.md), [location model](data/models/people-workforce.md).

- [x] `SYNTH-001` **[GATE_A][SOL_HIGH] Run tenant-safe production synthetic journeys with hard non-mutation fences.**
  - **Evidence (2026-09-05):** `TestProductionSyntheticJourneyMeasuresRealPathButCannotMutateOrLeakTenantState` in `internal/operations/synthetic` (dedicated synthetic tenant/principal and execution mode traverse edge/auth/config/read/simulation/telemetry paths, hard fence every effect class, report exact dependency/SLO outcome and are excluded from customer/domain metrics while remaining operationally visible; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/operations/synthetic/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `OPS-001`, `ENDPOINT-009`, `SANDBOX-001`, `OBS-016`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=measure real production reachability and semantic readiness without creating workforce truth, messages or provider effects`.
  - **TEST:** `TestProductionSyntheticJourneyMeasuresRealPathButCannotMutateOrLeakTenantState`.
  - **TEST MATRIX:** `PRIMARY=TestProductionSyntheticJourneyMeasuresRealPathButCannotMutateOrLeakTenantState`; `PROPERTY=TestTodo_SYNTH_001_Property`; `GOLDEN=TestTodo_SYNTH_001_Golden`; `RACE=TestTodo_SYNTH_001_Race`; `INTEGRATION=TestTodo_SYNTH_001_Integration`; `FAULT=TestTodo_SYNTH_001_Fault`; `SECURITY=TestTodo_SYNTH_001_Security`; `CONFORMANCE=TestTodo_SYNTH_001_Conformance`; `RECOVERY=TestTodo_SYNTH_001_Recovery`; `BENCHMARK=BenchmarkTodo_SYNTH_001`; `MUTATION=TestTodo_SYNTH_001_Mutation`.
  - **RED:** synthetic principal accesses customer data, uses write-capable credential, creates intent/work/message/outbox/provider call, hides dependency failure behind cached response or synthetic identifiers pollute tenant analytics.
  - **GREEN:** dedicated synthetic tenant/principal and execution mode traverse edge/auth/config/read/simulation/telemetry paths, hard fence every effect class, report exact dependency/SLO outcome and are excluded from customer/domain metrics while remaining operationally visible.
  - **REFACTOR:** journeys compile from selected vertical slices with generated safe probes rather than bespoke monitoring scripts.
  - **Refs:** [SLOs](specs/slo-sli-error-budget.md), [sandbox](specs/platform-responsibility-boundaries.md), [telemetry](specs/structured-logging-and-opentelemetry.md).

- [ ] `ASSURANCE-001` **[GATE_B][SOL_HIGH] Govern independent penetration, privacy and control-assurance evidence.**
  - **Depends:** `THREAT-002`, `OPS-010`, `SUPPLY-002`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.SECURITY,BI.PRIVACY,BI.OPERATIONS; DIRECT=none; WHY=ensure independent assessments produce scoped, current and remediated evidence rather than a one-time compliance claim`.
  - **TEST:** `TestIndependentAssuranceRegisterRejectsUnscopedExpiredUnremediatedOrMisrepresentedEvidence`.
  - **TEST MATRIX:** `PRIMARY=TestIndependentAssuranceRegisterRejectsUnscopedExpiredUnremediatedOrMisrepresentedEvidence`; `PROPERTY=TestTodo_ASSURANCE_001_Property`; `GOLDEN=TestTodo_ASSURANCE_001_Golden`; `SECURITY=TestTodo_ASSURANCE_001_Security`; `CONFORMANCE=TestTodo_ASSURANCE_001_Conformance`; `MUTATION=TestTodo_ASSURANCE_001_Mutation`.
  - **RED:** penetration/audit/privacy report lacks assessor independence, scope/version/environment/method/date, excluded surfaces, finding severity/owner/deadline, retest evidence or customer-safe claim boundary; open critical finding still permits gate.
  - **GREEN:** assurance register binds each assessment to release/topology/threat/control versions, tracks findings through remediation and independent retest, expires evidence and generates claims that state exact scope and exclusions without implying unattained certification.
  - **REFACTOR:** gate and customer evidence consume the register; raw sensitive reports remain compartmented artifacts.
  - **Refs:** [risk register](specs/risk-register.md), [evidence packaging](specs/platform-responsibility-boundaries.md), [release assurance](#33-cicd-release-admission-and-observability).

- [x] `RESIDENCY-001` **[GATE_A][SOL_HIGH] Enforce tenant residency and transfer policy across every primary, derived and provider copy.**
  - **Evidence (2026-09-05):** `TestResidencyPolicyCoversEveryCopyProcessorRouteBackupAndRecoveryLocation` in `internal/operations/residency` (signed residency policy resolves allowed processing/storage/support/transfer locations by class and purpose, admission checks every copy/route/processor, inventory and restore reconcile actual placement, and UNKNOWN|TRANSFER_REVIEW_REQUIRED blocks affected processing; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/operations/residency/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TENANT-001`, `PRIV-001`, `PRIV-003`, `RECORDS-COPY-001`, `RECOVERY-005`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=prevent placement metadata from claiming residency while backups, telemetry, artifacts, support or providers copy data elsewhere`.
  - **TEST:** `TestResidencyPolicyCoversEveryCopyProcessorRouteBackupAndRecoveryLocation`.
  - **TEST MATRIX:** `PRIMARY=TestResidencyPolicyCoversEveryCopyProcessorRouteBackupAndRecoveryLocation`; `PROPERTY=TestTodo_RESIDENCY_001_Property`; `GOLDEN=TestTodo_RESIDENCY_001_Golden`; `INTEGRATION=TestTodo_RESIDENCY_001_Integration`; `FAULT=TestTodo_RESIDENCY_001_Fault`; `SECURITY=TestTodo_RESIDENCY_001_Security`; `CONFORMANCE=TestTodo_RESIDENCY_001_Conformance`; `RECOVERY=TestTodo_RESIDENCY_001_Recovery`; `MUTATION=TestTodo_RESIDENCY_001_Mutation`.
  - **RED:** data class reaches unapproved cell/object replica/backup/telemetry region/provider/support session/model, recovery restores to prohibited region or unknown location is treated as compliant; or a residency decision is derived from `LocaleContext` rather than the resolved `JurisdictionSet`.
  - **GREEN:** signed residency policy resolves allowed processing/storage/support/transfer locations by class and purpose, admission checks every copy/route/processor, inventory and restore reconcile actual placement, and `UNKNOWN|TRANSFER_REVIEW_REQUIRED` blocks affected processing.
  - **REFACTOR:** placement, privacy and copy inventories share stable location/processor identities; no adapter infers residency from endpoint hostname.
  - **Refs:** [tenant placement](data/models/operations-production.md), [privacy flows](specs/platform-responsibility-boundaries.md), [records inventory](specs/records-management-and-disposition.md), [Jurisdiction model](specs/legal-rule-packs-and-state-configuration.md#2-jurisdiction-model).

- [x] `TIME-002` **[GATE_A][SOL_HIGH] Select and operate a concrete authenticated trusted-time quorum profile.**
  - **Evidence (2026-09-05):** `TestTrustedTimeProfileRequiresAuthenticatedQuorumSkewUncertaintyAndHoldoverPolicy` in `internal/platform/timeauth` (pinned NTS/authenticated sources and quorum policy expose source epoch/offset/skew/uncertainty/holdover health, alarm and failover evidence; sensitive consumers receive trustworthy receipt or exact TIME_UNTRUSTED while sequence ordering remains independent; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/platform/timeauth/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TIME-001`, `IAC-013`, `TOOL-025`, `OPS-003`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=turn abstract time health into an authenticated, observable and recoverable production service profile`.
  - **TEST:** `TestTrustedTimeProfileRequiresAuthenticatedQuorumSkewUncertaintyAndHoldoverPolicy`.
  - **TEST MATRIX:** `PRIMARY=TestTrustedTimeProfileRequiresAuthenticatedQuorumSkewUncertaintyAndHoldoverPolicy`; `PROPERTY=TestTodo_TIME_002_Property`; `GOLDEN=TestTodo_TIME_002_Golden`; `INTEGRATION=TestTodo_TIME_002_Integration`; `FAULT=TestTodo_TIME_002_Fault`; `SECURITY=TestTodo_TIME_002_Security`; `CONFORMANCE=TestTodo_TIME_002_Conformance`; `RECOVERY=TestTodo_TIME_002_Recovery`; `BENCHMARK=BenchmarkTodo_TIME_002`; `MUTATION=TestTodo_TIME_002_Mutation`.
  - **RED:** unauthenticated single source, DNS/spoof/step attack, quorum disagreement, leap/smear mismatch, backward jump, excessive uncertainty or expired holdover still authorizes sensitive timestamp decision.
  - **GREEN:** pinned NTS/authenticated sources and quorum policy expose source epoch/offset/skew/uncertainty/holdover health, alarm and failover evidence; sensitive consumers receive trustworthy receipt or exact `TIME_UNTRUSTED` while sequence ordering remains independent.
  - **REFACTOR:** infrastructure adapter supplies TimeAuthority receipts to the existing temporal kernel without exposing NTP mechanics to domains.
  - **Refs:** [trusted time](specs/platform-architecture-catalog.md), [operations model](data/models/operations-production.md), [temporal primitives](data/models/wire-contract-primitives.md).

- [x] `CONTRACT-ARCHIVE-001` **[GATE_A][SOL_HIGH] Preserve an offline-verifiable archive of every executable historical contract.**
  - **Evidence (2026-09-05):** `TestHistoricalContractArchiveReplaysEvidenceAfterRetirementKeyRotationAndToolUpgrade` in `internal/contractarchive`, `internal/platform/archive` (content-addressed archive packages exact sources, generated descriptors, compiler/tool profiles, signatures/trust history and compatibility metadata; offline verifier reproduces canonical interpretation/digest or returns explicit unsupported/tampered result without executing effe; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/contractarchive/ ./internal/platform/archive/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PROTO-010`, `MODEL-032`, `CONFIG-003`, `EVIDENCE-001`, `TOOL-018`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=keep historical intent, workflow, rule, schema, mapping, endpoint and evidence interpretation possible after retirement or tool change`.
  - **TEST:** `TestHistoricalContractArchiveReplaysEvidenceAfterRetirementKeyRotationAndToolUpgrade`.
  - **TEST MATRIX:** `PRIMARY=TestHistoricalContractArchiveReplaysEvidenceAfterRetirementKeyRotationAndToolUpgrade`; `PROPERTY=TestTodo_CONTRACT_ARCHIVE_001_Property`; `GOLDEN=TestTodo_CONTRACT_ARCHIVE_001_Golden`; `INTEGRATION=TestTodo_CONTRACT_ARCHIVE_001_Integration`; `FAULT=TestTodo_CONTRACT_ARCHIVE_001_Fault`; `SECURITY=TestTodo_CONTRACT_ARCHIVE_001_Security`; `CONFORMANCE=TestTodo_CONTRACT_ARCHIVE_001_Conformance`; `RECOVERY=TestTodo_CONTRACT_ARCHIVE_001_Recovery`; `MUTATION=TestTodo_CONTRACT_ARCHIVE_001_Mutation`.
  - **RED:** retired descriptor/schema/config/rule/workflow/mapping disappears, verification requires mutable live registry/network/tool, signing-key rotation invalidates old package or historical bytes are interpreted with current defaults.
  - **GREEN:** content-addressed archive packages exact sources, generated descriptors, compiler/tool profiles, signatures/trust history and compatibility metadata; offline verifier reproduces canonical interpretation/digest or returns explicit unsupported/tampered result without executing effects.
  - **REFACTOR:** all publication systems deposit one common archival envelope; archive is immutable evidence, not an activation source.
  - **Refs:** [canonical digest](specs/canonical-envelope-and-digest.md), [configuration lifecycle](specs/platform-responsibility-boundaries.md), [evidence](specs/transaction-ledger-reconciliation-and-repair.md).

- [ ] `A11Y-001` **[GATE_A][SOL_HIGH] Qualify assistive-technology, browser, locale and input-mode compatibility.**
  - **Depends:** `UX-003`, `UX-004`, `UXFLOW-009`, `I18N-003`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=prove WCAG assertions survive actual supported assistive technologies, browsers, zoom, languages and non-pointer input`.
  - **TEST:** `TestSupportedAssistiveTechnologyBrowserLocaleMatrixCompletesCriticalFlowsEquivalently`.
  - **TEST MATRIX:** `PRIMARY=TestSupportedAssistiveTechnologyBrowserLocaleMatrixCompletesCriticalFlowsEquivalently`; `PROPERTY=TestTodo_A11Y_001_Property`; `GOLDEN=TestTodo_A11Y_001_Golden`; `SECURITY=TestTodo_A11Y_001_Security`; `CONFORMANCE=TestTodo_A11Y_001_Conformance`; `BROWSER=TestTodo_A11Y_001_Browser`; `MUTATION=TestTodo_A11Y_001_Mutation`.
  - **RED:** supported screen reader/browser/OS/locale/RTL/200-400% zoom/high-contrast/voice/keyboard combination loses label, focus, state, error, timeout warning, evidence upload, approval or semantic result while automated checker passes.
  - **GREEN:** versioned support matrix names tested combinations and critical flows, records exact task/result equivalence and defects/waivers/expiry; unsupported combinations receive an accessible continuity route without weaker identity/privacy/deadline semantics.
  - **REFACTOR:** generate common flow assertions from UserFlowRecord and keep environment-specific drivers separate.
  - **Refs:** [accessibility assurance](specs/platform-foundation-gap-closure.md), [experience](specs/experience-ui-and-branding.md), [user flows](user-flows/README.md).

- [x] `STATUS-001` **[GATE_A][TERRA] Publish accurate tenant-scoped service status and incident advisories.**
  - **Evidence (2026-09-06):** `TestStatusPublicationMatchesIncidentScopeStateSLOAndCustomerDisclosurePolicy` in `internal/experience/status`; `go test -count=1 ./internal/experience/status/` PASS on windows/arm64 (Go 1.26.3); evidence recorded by the orchestrator after the front-end session ticked the entry without one.
  - **Depends:** `OPS-004`, `OPS-005`, `MSG-001`, `OBS-007`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.OPERATIONS; DIRECT=none; WHY=communicate service impact and recovery without leaking tenant existence, security detail or speculative root cause`.
  - **TEST:** `TestStatusPublicationMatchesIncidentScopeStateSLOAndCustomerDisclosurePolicy`.
  - **TEST MATRIX:** `PRIMARY=TestStatusPublicationMatchesIncidentScopeStateSLOAndCustomerDisclosurePolicy`; `PROPERTY=TestTodo_STATUS_001_Property`; `GOLDEN=TestTodo_STATUS_001_Golden`; `RACE=TestTodo_STATUS_001_Race`; `INTEGRATION=TestTodo_STATUS_001_Integration`; `FAULT=TestTodo_STATUS_001_Fault`; `SECURITY=TestTodo_STATUS_001_Security`; `CONFORMANCE=TestTodo_STATUS_001_Conformance`; `RECOVERY=TestTodo_STATUS_001_Recovery`; `MUTATION=TestTodo_STATUS_001_Mutation`.
  - **RED:** unaffected tenant sees advisory, affected tenant is omitted, public page reveals tenant/provider/security details, status says operational despite admission block, stale update remains current or resolved incident lacks correction/post-incident commitment.
  - **GREEN:** incident/SLI scope deterministically drives public and authenticated tenant views with reviewed disclosure, update cadence, timestamps, translations and correction history; publication failure becomes owned incident obligation and never changes technical truth.
  - **REFACTOR:** status channels render one CustomerAdvisory model; operators do not hand-edit independent state.
  - **Refs:** [incident management](specs/incident-management.md), [customer communication](specs/platform-foundation-gap-closure.md), [SLOs](specs/slo-sli-error-budget.md).

- [x] `DIAG-001` **[GATE_A][SOL_HIGH] Harden diagnostic, profiling, metrics and runtime administration surfaces.**
  - **Evidence (2026-09-05):** `TestDiagnosticSurfaceRequiresJITScopeRedactionBoundsAndAutomaticExpiry` in `internal/platform/diagnostics` (diagnostic manifest disables surfaces by default, permits only workload-authenticated JIT purpose/scope/duration/budget, redacts/classifies outputs into protected artifacts, auto-expires and emits complete access/drop evidence with zero business mutation; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/platform/diagnostics/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ADMIN-006`, `EDGE-010`, `OBS-018`, `TRUST-021`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.OPERATIONS,BI.SECURITY; DIRECT=none; WHY=prevent debug and runtime inspection mechanics from bypassing tenant, secret, privacy or availability controls`.
  - **TEST:** `TestDiagnosticSurfaceRequiresJITScopeRedactionBoundsAndAutomaticExpiry`.
  - **TEST MATRIX:** `PRIMARY=TestDiagnosticSurfaceRequiresJITScopeRedactionBoundsAndAutomaticExpiry`; `PROPERTY=TestTodo_DIAG_001_Property`; `GOLDEN=TestTodo_DIAG_001_Golden`; `FUZZ=FuzzTodo_DIAG_001`; `RACE=TestTodo_DIAG_001_Race`; `INTEGRATION=TestTodo_DIAG_001_Integration`; `FAULT=TestTodo_DIAG_001_Fault`; `SECURITY=TestTodo_DIAG_001_Security`; `CONFORMANCE=TestTodo_DIAG_001_Conformance`; `RECOVERY=TestTodo_DIAG_001_Recovery`; `BENCHMARK=BenchmarkTodo_DIAG_001`; `MUTATION=TestTodo_DIAG_001_Mutation`.
  - **RED:** pprof/expvar/metrics/debug/config/goroutine/heap/trace endpoint is public or cross-tenant, contains payload/secret/credential/SQL values, enables unbounded CPU/memory capture, remains enabled after expiry or changes business state.
  - **GREEN:** diagnostic manifest disables surfaces by default, permits only workload-authenticated JIT purpose/scope/duration/budget, redacts/classifies outputs into protected artifacts, auto-expires and emits complete access/drop evidence with zero business mutation.
  - **REFACTOR:** process roles register diagnostics through one hardened adapter and no package exposes a listener independently.
  - **Refs:** [support access](specs/platform-foundation-gap-closure.md), [telemetry diagnostics](specs/structured-logging-and-opentelemetry.md), [edge security](specs/platform-responsibility-boundaries.md).

- [ ] `FORENSIC-001` **[GATE_B][SOL_HIGH] Acquire and preserve incident forensic evidence with bounded chain of custody.**
  - **Depends:** `OPS-004`, `RECORDS-HOLD-001`, `EVIDENCE-001`, `OBS-021`, `DIAG-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.OPERATIONS,BI.SECURITY,BI.PRIVACY; DIRECT=none; WHY=investigate material incidents without unaudited overcollection, evidence mutation or cross-tenant disclosure`.
  - **TEST:** `TestForensicAcquisitionBindsAuthorityScopeCustodyIntegrityRetentionAndDisclosure`.
  - **TEST MATRIX:** `PRIMARY=TestForensicAcquisitionBindsAuthorityScopeCustodyIntegrityRetentionAndDisclosure`; `PROPERTY=TestTodo_FORENSIC_001_Property`; `GOLDEN=TestTodo_FORENSIC_001_Golden`; `INTEGRATION=TestTodo_FORENSIC_001_Integration`; `FAULT=TestTodo_FORENSIC_001_Fault`; `SECURITY=TestTodo_FORENSIC_001_Security`; `CONFORMANCE=TestTodo_FORENSIC_001_Conformance`; `RECOVERY=TestTodo_FORENSIC_001_Recovery`; `MUTATION=TestTodo_FORENSIC_001_Mutation`.
  - **RED:** investigator collects entire tenant/system without necessity, alters source, lacks dual authorization or trusted time, chain of custody breaks, evidence crosses compartment/region, hold/retention is absent or export cannot be independently verified.
  - **GREEN:** acquisition plan pins incident/purpose/scope/data classes/sources/query/tool/actors, captures content-addressed minimized artifacts with source integrity and custody receipts, enforces compartment/residency/hold/disposition and produces a separately redacted disclosure package.
  - **REFACTOR:** forensic tooling consumes evidence/artifact and support-access contracts; raw collection never becomes ordinary telemetry or domain fact.
  - **Refs:** [incident management](specs/incident-management.md), [records](specs/records-management-and-disposition.md), [evidence packaging](specs/platform-responsibility-boundaries.md).

- [x] `SUBPROCESSOR-001` **[GATE_A][SOL_HIGH] Govern subprocessor inventory, material change, objection and emergency replacement.**
  - **Evidence (2026-09-05):** `TestSubprocessorChangeRequiresImpactNoticeObjectionWindowAndActivationFence` in `internal/operations/subprocessor` (immutable change revision identifies affected tenants/flows/intents and material differences, completes security/privacy/residency/contract reviews, issues scoped notice and records objections/disposition; activation is fenced until obligations pass and emergency exception has co; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/operations/subprocessor/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PRIV-001`, `PRIV-003`, `PROVIDER-002`, `OPS-009`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.PRIVACY,BI.INTEGRATION,BI.TENANT; DIRECT=none; WHY=ensure provider supply-chain changes cannot silently alter processing purpose, location, retention or customer commitments`.
  - **TEST:** `TestSubprocessorChangeRequiresImpactNoticeObjectionWindowAndActivationFence`.
  - **TEST MATRIX:** `PRIMARY=TestSubprocessorChangeRequiresImpactNoticeObjectionWindowAndActivationFence`; `PROPERTY=TestTodo_SUBPROCESSOR_001_Property`; `GOLDEN=TestTodo_SUBPROCESSOR_001_Golden`; `INTEGRATION=TestTodo_SUBPROCESSOR_001_Integration`; `FAULT=TestTodo_SUBPROCESSOR_001_Fault`; `SECURITY=TestTodo_SUBPROCESSOR_001_Security`; `CONFORMANCE=TestTodo_SUBPROCESSOR_001_Conformance`; `RECOVERY=TestTodo_SUBPROCESSOR_001_Recovery`; `MODEL_BASED=TestTodo_SUBPROCESSOR_001_ModelBased`; `MUTATION=TestTodo_SUBPROCESSOR_001_Mutation`.
  - **RED:** new processor/region/purpose/data category/retention appears without inventory diff, risk/transfer review, customer notice/objection interval, contractual approval or exit fallback; emergency replacement becomes permanent silently.
  - **GREEN:** immutable change revision identifies affected tenants/flows/intents and material differences, completes security/privacy/residency/contract reviews, issues scoped notice and records objections/disposition; activation is fenced until obligations pass and emergency exception has compensating controls and expiry.
  - **REFACTOR:** provider and processing inventories share one processor identity and change graph; customer communication remains a typed obligation.
  - **Refs:** [privacy flows](specs/platform-responsibility-boundaries.md), [integration lifecycle](specs/integration-platform.md), [risk register](specs/risk-register.md).

- [x] `REFDATA-001` **[GATE_A][SOL_HIGH] Operate global and tenant reference datasets through complete source-to-adoption lifecycle.**
  - **Evidence (2026-09-05):** `TestReferenceDatasetReleasePinsSourceValidityAdoptionImpactAndRollback` in `internal/data/refdata`, `internal/domains/refdata` (immutable dataset release pins source/schema/member digests and applicability, validates completeness/conflicts, compiles permitted overrides, publishes through dependency/rollout gates and records consumer adoption/impact/rollback with historical executions staying pinned; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/data/refdata/ ./internal/domains/refdata/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-005`, `MODEL-018`, `CONFIG-003`, `DATA-021`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=ensure calendars, locations, currencies, codes, classifications and other reference releases cannot change intent behavior through ambient or partially adopted data`.
  - **TEST:** `TestReferenceDatasetReleasePinsSourceValidityAdoptionImpactAndRollback`.
  - **TEST MATRIX:** `PRIMARY=TestReferenceDatasetReleasePinsSourceValidityAdoptionImpactAndRollback`; `PROPERTY=TestTodo_REFDATA_001_Property`; `GOLDEN=TestTodo_REFDATA_001_Golden`; `FUZZ=FuzzTodo_REFDATA_001`; `INTEGRATION=TestTodo_REFDATA_001_Integration`; `FAULT=TestTodo_REFDATA_001_Fault`; `SECURITY=TestTodo_REFDATA_001_Security`; `CONFORMANCE=TestTodo_REFDATA_001_Conformance`; `RECOVERY=TestTodo_REFDATA_001_Recovery`; `MODEL_BASED=TestTodo_REFDATA_001_ModelBased`; `MUTATION=TestTodo_REFDATA_001_Mutation`.
  - **RED:** dataset lacks authoritative source/license/coverage/effective-known interval, malformed or incomplete member publishes, tenant override weakens mandatory value, consumers split across versions silently, rollback revives withdrawn data or affected intents are not identified.
  - **GREEN:** immutable dataset release pins source/schema/member digests and applicability, validates completeness/conflicts, compiles permitted overrides, publishes through dependency/rollout gates and records consumer adoption/impact/rollback with historical executions staying pinned.
  - **REFACTOR:** one reference-data service owns release mechanics while each semantic dataset retains a qualified domain/legal owner.
  - **Refs:** [reference data boundary](specs/platform-responsibility-boundaries.md), [configuration publication](specs/platform-foundation-gap-closure.md), [model registry](data/models/registry-and-coverage-contracts.md).

- [x] `MASK-001` **[GATE_A][SOL_HIGH] Generate privacy-safe masked and synthetic datasets with measured semantic fidelity.**
  - **Evidence (2026-09-05):** `TestMaskedSyntheticDatasetPreventsReidentificationAndPreservesDeclaredSemanticCoverage` in `internal/governance/masking` (versioned profile declares source authority, field/relationship transforms, privacy risk threshold and fidelity requirements; output passes attack tests and expected invariant/distribution/edge-case coverage, is visibly synthetic, side-effect fenced, expiring and independently de; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/governance/masking/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `MODEL-023`, `PRIV-001`, `SANDBOX-001`, `DATA-019`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide realistic development, migration and conformance data without copying identifiable workforce truth or destroying edge-case semantics`.
  - **TEST:** `TestMaskedSyntheticDatasetPreventsReidentificationAndPreservesDeclaredSemanticCoverage`.
  - **TEST MATRIX:** `PRIMARY=TestMaskedSyntheticDatasetPreventsReidentificationAndPreservesDeclaredSemanticCoverage`; `PROPERTY=TestTodo_MASK_001_Property`; `GOLDEN=TestTodo_MASK_001_Golden`; `FUZZ=FuzzTodo_MASK_001`; `INTEGRATION=TestTodo_MASK_001_Integration`; `FAULT=TestTodo_MASK_001_Fault`; `SECURITY=TestTodo_MASK_001_Security`; `CONFORMANCE=TestTodo_MASK_001_Conformance`; `RECOVERY=TestTodo_MASK_001_Recovery`; `BENCHMARK=BenchmarkTodo_MASK_001`; `MUTATION=TestTodo_MASK_001_Mutation`.
  - **RED:** direct/quasi identifier survives, linkage or rare cohort enables reidentification, deterministic token crosses tenant/purpose, referential/temporal/decimal/legal edge cases disappear, synthetic row is mistaken for production truth or regeneration cannot be reproduced/destroyed.
  - **GREEN:** versioned profile declares source authority, field/relationship transforms, privacy risk threshold and fidelity requirements; output passes attack tests and expected invariant/distribution/edge-case coverage, is visibly synthetic, side-effect fenced, expiring and independently destructible.
  - **REFACTOR:** masking uses Transformation/classification/provenance engines but retains separate privacy-risk and fidelity evaluation.
  - **Refs:** [masking boundary](specs/platform-responsibility-boundaries.md), [classification](specs/data-classification-and-dlp.md), [sandbox](specs/platform-responsibility-boundaries.md).

## 64. Dependency-ordered execution convergence

> **Disposition (2026-09-02):** Active. `NEXT-001` is RETIRED; `NEXT-002`–`NEXT-009` are the P1A/P1B compilers and gate decisions.

These todos turn the planning corpus into two bounded releases. They are scope and
evidence compilers, not permission to implement deferred HCM domains.

- [ ] `NEXT-001` **[RETIRED][SOL_HIGH] RETIRED 2026-09-02: Separate local BusinessIntent source recovery mechanics from external authenticity attestation.**
  - **Disposition:** RETIRED 2026-09-02. Source recovery and attestation were withdrawn with the catalog numbering; next-steps.md M0 is a partner conversation only. Dependencies on this ID resolve as satisfied; its tests are not written.
  - **Depends:** `GOV-008`, `TOOL-002`, `TOOL-003`, `TOOL-004`, `MODEL-008`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=make catalog reconciliation useful without falsely authenticating or promoting missing baseline definitions`.
  - **TEST:** `TestSourceRecoveryPackagePreservesBytesRowsAndUnboundStatusWithoutIssuerEvidence`.
  - **TEST MATRIX:** `PRIMARY=TestSourceRecoveryPackagePreservesBytesRowsAndUnboundStatusWithoutIssuerEvidence`; `PROPERTY=TestTodo_NEXT_001_Property`; `GOLDEN=TestTodo_NEXT_001_Golden`; `FUZZ=FuzzTodo_NEXT_001`; `SECURITY=TestTodo_NEXT_001_Security`; `CONFORMANCE=TestTodo_NEXT_001_Conformance`; `MUTATION=TestTodo_NEXT_001_Mutation`.
  - **RED:** parser changes bytes/order/Unicode, loses row spans, maps vocabulary similarity to baseline identity or marks a row source-bound without original bytes plus trusted issuer/release provenance.
  - **GREEN:** deterministic package freezes the 14 draft, 516 unbound and 807 vocabulary records with canonical bytes/digests/dispositions; absent authenticity evidence leaves every affected row `UNBOUND_SOURCE`, while later verified bytes can join exactly once.
  - **REFACTOR:** keep recovery/parser evidence reusable; `SOURCE-001` remains the separate external attestation gate.
  - **Refs:** [catalog identity](specs/business-intent-catalog.md#catalog-identity), [convergence plan](next-steps.md#current-truth).

- [ ] `NEXT-002` **[P0][SOL_HIGH] Publish the selection-bound P1A release and deferred P1B amendment manifests.**
  - **Depends:** `PHASE-001`, `SELECT-001`, `SELECT-002`, `CUSTOMER-001`, `TOPOLOGY-001`, `COMMERCIAL-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.PEOPLE,BI.REWARDS,BI.INTEGRATION,BI.OPERATIONS; DIRECT=none; WHY=bind the scope ceiling to real partner, provider, jurisdiction, topology and commercial facts without granting write authority`.
  - **TEST:** `TestP1AAndP1BManifestsAreDisjointOrderedBoundedAndSelectionComplete`.
  - **TEST MATRIX:** `PRIMARY=TestP1AAndP1BManifestsAreDisjointOrderedBoundedAndSelectionComplete`; `PROPERTY=TestTodo_NEXT_002_Property`; `GOLDEN=TestTodo_NEXT_002_Golden`; `SECURITY=TestTodo_NEXT_002_Security`; `CONFORMANCE=TestTodo_NEXT_002_Conformance`; `MUTATION=TestTodo_NEXT_002_Mutation`.
  - **RED:** one blended manifest grants writes before Gate A, omits a selected external fact, includes unbound/deferred intent or lets P1A create workforce mutation, reservation, WorkItem, timer, message, outbox effect or provider write.
  - **GREEN:** signed P1A manifest contains exactly the approved read/preflight/simulation/observation scope and hard zero-effect ceiling; separately signed P1B template names only the six candidate write/approval contracts and cannot activate without Gate A plus a new authority digest.
  - **REFACTOR:** generate release, endpoint, deployment and gate views from the same manifest identities.
  - **Refs:** [two releases](next-steps.md#two-releases-never-one-blended-phase-1), [execution plan](execution-plan.md).
  - **Evidence (partial, 2026-09-03):** P1A-depth scaffold done: signed manifest `definitions/planning/gates/p1a-manifest.yaml` (eight intents, ten BOOTSTRAP capabilities, four commands, fourteen migrations with checksums, four qualification decisions, per-todo evidence; digest sha256:625e88ce…) plus `p1b-template.yaml`; `TestP1AAndP1BManifestsAreDisjointOrderedBoundedAndSelectionComplete`, `TestTamperedP1AManifestFailsSignatureVerification` in `tools/planning/gateevidence`; `go test -count=1 ./tools/planning/gateevidence/...` PASS on windows/arm64 (Go 1.26.3). Cannot close: its Depends (PHASE-001, SELECT-001/002, CUSTOMER-001, TOPOLOGY-001, COMMERCIAL-001) are open business selections; branch plan-revision-2026-09-02.

- [x] `NEXT-003` **[GATE_A][SOL_HIGH] Compile Gate A evidence from the exact signed P1A manifest.**
  - **Evidence (2026-09-05):** `TestP1AEvidenceCompilerRejectsMissingStaleOutOfManifestOrEffectfulEvidence` in `tools/planning/gateevidence` (manifest closure maps each selected artifact and acceptance claim to an exact oracle, result digest, owner, retention and expiry and returns GATE_BLOCKED for any defect; passing evidence can yield a signed decision but never write authority; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/planning/gateevidence/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `NEXT-002`, `WEDGE-001`–`WEDGE-013`, `NEXT-005`, `NEXT-008`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.PEOPLE,BI.REWARDS,BI.INTEGRATION,BI.OPERATIONS; DIRECT=none; WHY=make the paid-observation decision depend on current release evidence rather than broad todo-number ranges`.
  - **TEST:** `TestP1AEvidenceCompilerRejectsMissingStaleOutOfManifestOrEffectfulEvidence`.
  - **TEST MATRIX:** `PRIMARY=TestP1AEvidenceCompilerRejectsMissingStaleOutOfManifestOrEffectfulEvidence`; `PROPERTY=TestTodo_NEXT_003_Property`; `GOLDEN=TestTodo_NEXT_003_Golden`; `FAULT=TestTodo_NEXT_003_Fault`; `SECURITY=TestTodo_NEXT_003_Security`; `CONFORMANCE=TestTodo_NEXT_003_Conformance`; `RECOVERY=TestTodo_NEXT_003_Recovery`; `MUTATION=TestTodo_NEXT_003_Mutation`.
  - **RED:** omitted/expired/failed test, owner, command, fixture, artifact, restore, SLO, accessibility or privacy evidence still passes; an out-of-manifest system is required; or any workforce/provider effect is observed.
  - **GREEN:** manifest closure maps each selected artifact and acceptance claim to an exact oracle, result digest, owner, retention and expiry and returns `GATE_BLOCKED` for any defect; passing evidence can yield a signed decision but never write authority.
  - **REFACTOR:** `WEDGE-014` consumes only this evidence receipt; phase membership is data, not numeric todo ranges.
  - **Refs:** [Gate A milestone](next-steps.md#m4--gate-a-decision), [Gate A acceptance](execution-plan.md#gate-a-acceptance--paid-observation).
  - **Evidence (partial, 2026-09-03):** Compiler done: `TestP1AEvidenceCompilerRejectsMissingStaleOutOfManifestOrEffectfulEvidence`, `TestP1AEvidenceReportArtifactsAreCurrent` in `tools/planning/gateevidence` (OK/MISSING/STALE/OUT_OF_MANIFEST/EFFECTFUL resolution, `-live` reruns, deterministic `p1a-evidence-report.{md,json}`; the checked-in manifest compiles GATE_CLEAR); `go test -count=1 ./tools/planning/gateevidence/...` PASS on windows/arm64 (Go 1.26.3). Cannot close: NEXT-008 and WEDGE-001..013 are open; branch plan-revision-2026-09-02.

- [x] `NEXT-004` **[GATE_A][SOL_HIGH] Bootstrap the smallest production-shaped Go runtime and PostgreSQL chronology.**
  - **Depends:** `TOOL-001`, `ARCH-GO-001`, `ARCH-GO-018`, `PROTO-001`, `PROTO-002`, `DB-001`, `DB-002`, `DB-006`, `LEDGER-001`, `DATA-007`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.PEOPLE,BI.REWARDS,BI.OPERATIONS; DIRECT=none; WHY=prove one typed intent can traverse generated transport and authoritative persistence before staffing horizontal platform breadth`.
  - **TEST:** `TestBootstrapCellMigratesServesSimulatesAppendsRestartsAndReconciles`.
  - **TEST MATRIX:** `PRIMARY=TestBootstrapCellMigratesServesSimulatesAppendsRestartsAndReconciles`; `GOLDEN=TestTodo_NEXT_004_Golden`; `RACE=TestTodo_NEXT_004_Race`; `INTEGRATION=TestTodo_NEXT_004_Integration`; `FAULT=TestTodo_NEXT_004_Fault`; `SECURITY=TestTodo_NEXT_004_Security`; `RECOVERY=TestTodo_NEXT_004_Recovery`; `MUTATION=TestTodo_NEXT_004_Mutation`.
  - **RED:** release requires Node/npm, lacks generated service bindings, has competing migration roots, loses ledger/projection/outbox chronology on crash, or restart cannot return the same typed intent/simulation result.
  - **GREEN:** root Go module builds `hcmnext`, `worker`, `projector` and bootstrap `migrate`; ephemeral PostgreSQL migrates from zero, typed Create/Get/Simulate calls persist one ACID chronology, worker acknowledgement and restart/reconciliation preserve exact digests.
  - **REFACTOR:** add scheduler/admin and mature migration/deployment qualification only when their first selected runtime behavior requires them.
  - **Refs:** [physical spine](next-steps.md#m2--production-shaped-physical-spine), [Go architecture](#2a-modular-go-monolith-and-semantic-package-architecture).
  - **Evidence (partial, 2026-09-03):** ledger/outbox/projection slice: `TestLedgerOutboxProjectionAppendRestartsAndReconciles`, `TestOutboxConsumerHandlerIsIdempotentByMessageID` in `internal/data/outbox`; `TestApplyAdvancesIdempotentlyAndRejectsGaps`, `TestReconcilerCatchesUpFromLedger` in `internal/data/projection`; `TestKernelDigesterComputesAndVerifies` in `internal/ledger`; `go test -count=1` PASS via embedded-postgres; `cmd/worker` and `cmd/projector` wired with graceful shutdown. Remaining: `TestBootstrapCellMigratesServesSimulatesAppendsRestartsAndReconciles` end-to-end through the served gRPC edge (after Wave 2 wiring); on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Evidence (2026-09-03):** `TestBootstrapCellMigratesServesSimulatesAppendsRestartsAndReconciles` (8 subtests) in `test/bootstrap`: migrates from zero, serves the registry on gRPC and the connect edge, one create writes ledger+projection+outbox in one transaction, failed append leaves no partial chronology, simulate identical on both transports, restart returns the same typed intent, replayed idempotency key yields one intent, worker killed mid-batch loses nothing and never double-applies, reconciler restores the projection. `TestTodo_NEXT_004_Integration` in `test/serve` additionally builds and launches the real `cmd/hcmnext` process against an isolated PostgreSQL schema, waits on its authenticated live Connect edge, and proves idempotent Create/Get/Simulate identity, stable request/proposal digests, zero-effect receipt, and listener cleanup; `go test -count=1 ./cmd/migrate ./internal/data/seed` and `go test -count=1 ./test/serve -run '^TestTodo_NEXT_004_Integration$'` PASS on windows/arm64 (Go 1.26.3); supersedes the partial line above; branch plan-revision-2026-09-02.

- [x] `NEXT-005` **[GATE_A][SOL_HIGH] Prove the complete P1A Promotion path has zero workforce and external effects.**
  - **Depends:** `EP-PROMO-001`, `PROMO-001`–`PROMO-004`, `SNAPSHOT-001`–`SNAPSHOT-003`, `INTG-001`–`INTG-010`, `RECON-001`, `RECON-002`, `REPAIR-001`, `REPAIR-003`, `EVIDENCE-001`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.PEOPLE,BI.REWARDS,BI.INTEGRATION,BI.OPERATIONS; DIRECT=PromoteWorker; WHY=establish the paid observe/preflight/simulation product before requesting authority expansion`.
  - **TEST:** `TestP1APromotionProducesExactEvidenceAndZeroAuthoritativeOrProviderEffect`.
  - **TEST MATRIX:** `PRIMARY=TestP1APromotionProducesExactEvidenceAndZeroAuthoritativeOrProviderEffect`; `PROPERTY=TestTodo_NEXT_005_Property`; `GOLDEN=TestTodo_NEXT_005_Golden`; `RACE=TestTodo_NEXT_005_Race`; `INTEGRATION=TestTodo_NEXT_005_Integration`; `FAULT=TestTodo_NEXT_005_Fault`; `SECURITY=TestTodo_NEXT_005_Security`; `CONFORMANCE=TestTodo_NEXT_005_Conformance`; `RECOVERY=TestTodo_NEXT_005_Recovery`; `MUTATION=TestTodo_NEXT_005_Mutation`.
  - **RED:** request smuggles current truth, stale/partial/denied input becomes complete, replay duplicates intent, simulation omits write/effect/approval/conflict/authority/repair detail, or any domain revision/reservation/task/timer/message/outbox/provider mutation appears.
  - **GREEN:** trusted ingress creates one intent; mixed-source snapshot preserves status/provenance; deterministic simulation and immutable proposal expose complete before/after and uncertainty; handoff/observation/reconciliation/repair recommendation produce one evidence receipt and the prohibited-effect row/request counts remain exactly zero under retry/crash.
  - **REFACTOR:** the Promotion application package orchestrates owner ports; provider adapters and workflow steps contain no promotion semantics.
  - **Refs:** [P1A Promotion slice](next-steps.md#m3--p1a-promotion-vertical-slice), [Promotion workflow](reference-workflows/promote-into-management.md).
  - **Evidence (2026-09-03):** `TestP1APromotionProducesExactEvidenceAndZeroAuthoritativeOrProviderEffect` (8 subtests) in `test/bootstrap`: full-database fingerprint before/after the promotion path excluding chronology tables, seeded stand-in workforce table unchanged, self-checking oracle RED, httptest connector endpoint and http.DefaultTransport recorder both at zero calls, outbox restricted to intent.created, Submit/Cancel/Supersede refused with FAILED_PRECONDITION `p1a.no_governed_write`; `go test -count=1 ./test/...` PASS; branch plan-revision-2026-09-02; on windows/arm64 (Go 1.26.3).

- [ ] `NEXT-006` **[GATE_B][SOL_HIGH] Select and bind exactly one P1B authority topology.**
  - **Depends:** `WEDGE-014`, `NEXT-002`, `WEDGE-004`, `SELECT-002`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.PEOPLE,BI.REWARDS,BI.INTEGRATION; DIRECT=PromoteWorker; WHY=prevent external-authority and local-authority semantics from being implemented simultaneously or confused in ledger evidence`.
  - **TEST:** `TestP1BAuthorityAmendmentSelectsOneTopologyAndEmitsOnlyTruthfulFacts`.
  - **TEST MATRIX:** `PRIMARY=TestP1BAuthorityAmendmentSelectsOneTopologyAndEmitsOnlyTruthfulFacts`; `PROPERTY=TestTodo_NEXT_006_Property`; `GOLDEN=TestTodo_NEXT_006_Golden`; `FAULT=TestTodo_NEXT_006_Fault`; `SECURITY=TestTodo_NEXT_006_Security`; `CONFORMANCE=TestTodo_NEXT_006_Conformance`; `MUTATION=TestTodo_NEXT_006_Mutation`.
  - **RED:** both topologies activate, amendment lacks tenant/field/capability/time/expiry, externally mastered state is emitted as local `DOMAIN_FACT`, or transferred authority writes before explicit partner grant.
  - **GREEN:** immutable amendment selects `EXTERNAL_AUTHORITY` or `TRANSFERRED_AUTHORITY`, binds exact fields and operations, changes transaction/event expectations accordingly and fails closed at expiry, revocation, ambiguity or topology mismatch.
  - **REFACTOR:** conditionally admit local command implementations and event types from the authority manifest; do not maintain two speculative write stacks.
  - **Refs:** [P1B authority](next-steps.md#p1b--one-bounded-authority-amendment), [authority contract](plan.md#91-authority-contract).

- [ ] `NEXT-007` **[CONFORMANCE][SOL_HIGH] Firewall Medical Leave conformance from Gate A/B and Phase 2 implementations.**
  - **Depends:** `CONF-001`, `LEAVE-001`–`LEAVE-008`, `LEAVE-012`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.WORKFORCE,BI.REWARDS,BI.REGULATORY,BI.DOCUMENTS; DIRECT=RequestLeave,ExtendLeave,ReturnFromLeave; WHY=use Leave as a hostile reusable-engine proof without claiming legal coverage or creating production authority`.
  - **TEST:** `TestLeaveConformanceGraphHasNoGateABOrPhase2ImplementationDependency`.
  - **TEST MATRIX:** `PRIMARY=TestLeaveConformanceGraphHasNoGateABOrPhase2ImplementationDependency`; `PROPERTY=TestTodo_NEXT_007_Property`; `GOLDEN=TestTodo_NEXT_007_Golden`; `FAULT=TestTodo_NEXT_007_Fault`; `SECURITY=TestTodo_NEXT_007_Security`; `CONFORMANCE=TestTodo_NEXT_007_Conformance`; `MUTATION=TestTodo_NEXT_007_Mutation`.
  - **RED:** `CONF-004` closure reaches JOIN/SUBWORKFLOW/runtime/provider/legal-delivery implementation, claims real jurisdiction, mutates authoritative state, defaults `UNKNOWN`, leaks medical evidence or extracts a generic engine without cross-domain counterexamples.
  - **GREEN:** fixed hypothetical fixtures run through contract doubles and golden vectors with zero writes/provider calls, exact partial replan and actor-view noninterference; every engine verdict is `EXTRACT|KEEP_DOMAIN_LOCAL|INSUFFICIENT_EVIDENCE`, and implementation remains a separate decision.
  - **REFACTOR:** split `LEAVE-015`, notice, evidence-loop, balance-invalidation and readiness todos into contract/vector and later implementation halves before making `CONF-004` depend on them.
  - **Refs:** [Leave conformance track](next-steps.md#medical-leave-conformance-track), [Leave workflow](workflows/leave/leave-return-to-work.md).

- [ ] `NEXT-008` **[P0][SOL_HIGH] Reject gate dependency inversions and produce gate-scoped assurance closures.**
  - **Depends:** `GOV-017`, `GOV-023`, `GOV-026`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=prevent an earlier release gate from silently requiring later-phase implementation or certifying itself through circular evidence`.
  - **TEST:** `TestGateDependencyGraphRejectsLaterPhaseEdgesCyclesAndEvidenceSelfCertification`.
  - **TEST MATRIX:** `PRIMARY=TestGateDependencyGraphRejectsLaterPhaseEdgesCyclesAndEvidenceSelfCertification`; `PROPERTY=TestTodo_NEXT_008_Property`; `GOLDEN=TestTodo_NEXT_008_Golden`; `SECURITY=TestTodo_NEXT_008_Security`; `CONFORMANCE=TestTodo_NEXT_008_Conformance`; `MUTATION=TestTodo_NEXT_008_Mutation`.
  - **RED:** Gate A reaches Gate B/C/Phase 2 implementation, Gate B evidence omits required recovery/independent assurance, assurance depends on the gate package it must inform, backup readability substitutes for restore, or broad range expansion changes closure silently.
  - **GREEN:** compiler expands exact manifest-selected todo/test/evidence edges, rejects later-phase and cyclic paths with a minimal witness, permits explicit contract/vector dependencies, and places independent assurance plus cross-store restore before the final Gate B evidence node.
  - **REFACTOR:** generate gate graphs and phase-leak reports in CI; scope exceptions require signed exchange and expiry.
  - **Refs:** [required backlog repairs](next-steps.md#required-backlog-repairs), [backlog governance](#backlog-governance-and-traceability).

- [ ] `NEXT-009` **[GATE_B][SOL_HIGH] Compile the pre-write P1B authority evidence amendment.**
  - **Depends:** `WEDGE-014`, `NEXT-002`, `NEXT-006`, `NEXT-008`, `GOV-003`, `GOV-023`, `RECOVERY-005`, `ASSURANCE-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.PEOPLE,BI.REWARDS,BI.INTEGRATION,BI.OPERATIONS; DIRECT=PromoteWorker; WHY=authorize the first bounded write only from complete current evidence, before rather than after the write pilot`.
  - **TEST:** `TestP1BPreWriteAuthorityEvidenceRejectsMissingStaleCircularOrPostPilotProof`.
  - **TEST MATRIX:** `PRIMARY=TestP1BPreWriteAuthorityEvidenceRejectsMissingStaleCircularOrPostPilotProof`; `PROPERTY=TestTodo_NEXT_009_Property`; `GOLDEN=TestTodo_NEXT_009_Golden`; `FAULT=TestTodo_NEXT_009_Fault`; `SECURITY=TestTodo_NEXT_009_Security`; `CONFORMANCE=TestTodo_NEXT_009_Conformance`; `RECOVERY=TestTodo_NEXT_009_Recovery`; `MUTATION=TestTodo_NEXT_009_Mutation`.
  - **RED:** authority relies on future pilot outcomes, self-certifying gate evidence, missing topology/field/provider/expiry, unreadable restore, open critical assurance finding, unowned rollback/repair/incident path or a stale Gate A artifact.
  - **GREEN:** immutable amendment maps every pre-write criterion to current test command, fixture, exact oracle, result digest, owner, retention, expiry and sign-off; success grants only the selected tenant/intent/capability/field/provider/time scope and every defect returns `GATE_BLOCKED` with zero write authority.
  - **REFACTOR:** keep pre-write authorization separate from `GATEB-EVID-001`, which evaluates actual bounded-write operation and the post-pilot continue/narrow/stop decision.
  - **Refs:** [P1B milestone](next-steps.md#m5--p1b-bounded-write-path), [Gate B acceptance](execution-plan.md#gate-b-acceptance--limited-write-authority).

## 65. Additional untraveled operational and delivery surfaces

> **Disposition (2026-09-02):** `IDEMP-001` is P1B.

These continue the cross-cutting inventory from section 63; they remain outside
the bounded convergence tasks unless selected by a signed release manifest.

- [x] `IDEMP-001` **[GATE_A][SOL_HIGH] Govern idempotency identity, retention, expiry and tombstones across every ingress and effect layer.**
  - **Evidence (2026-09-05):** `TestCrossLayerIdempotencyLifecyclePreservesOneLogicalActionAcrossRetentionAndReplayBoundaries` in `internal/platform/idempotency` (registry defines stable logical-action/effect identities, scope and canonical request/result binding per layer; retention derives from maximum replay/legal/provider horizon, expired records leave privacy-minimized tombstones where required, conflicts return typed result and crash; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/platform/idempotency/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `TX-006`, `DATA-009`, `INTG-018`, `JOB-001`, `MSG-006`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=prevent API, workflow, job, webhook, message and connector retry layers from using incompatible dedupe identities or forgetting them before replay risk expires`.
  - **TEST:** `TestCrossLayerIdempotencyLifecyclePreservesOneLogicalActionAcrossRetentionAndReplayBoundaries`.
  - **TEST MATRIX:** `PRIMARY=TestCrossLayerIdempotencyLifecyclePreservesOneLogicalActionAcrossRetentionAndReplayBoundaries`; `PROPERTY=TestTodo_IDEMP_001_Property`; `GOLDEN=TestTodo_IDEMP_001_Golden`; `FUZZ=FuzzTodo_IDEMP_001`; `RACE=TestTodo_IDEMP_001_Race`; `INTEGRATION=TestTodo_IDEMP_001_Integration`; `FAULT=TestTodo_IDEMP_001_Fault`; `SECURITY=TestTodo_IDEMP_001_Security`; `CONFORMANCE=TestTodo_IDEMP_001_Conformance`; `RECOVERY=TestTodo_IDEMP_001_Recovery`; `MODEL_BASED=TestTodo_IDEMP_001_ModelBased`; `MUTATION=TestTodo_IDEMP_001_Mutation`.
  - **RED:** same key with different canonical request is accepted, tenant/principal/channel scopes collide, nested retries multiply action, key expires before downstream replay/provider retention, deletion removes duplicate-effect protection or restore resurrects a reusable key without outcome.
  - **GREEN:** registry defines stable logical-action/effect identities, scope and canonical request/result binding per layer; retention derives from maximum replay/legal/provider horizon, expired records leave privacy-minimized tombstones where required, conflicts return typed result and crash/restore preserves one outcome/effect.
  - **REFACTOR:** layer adapters map to one lifecycle contract but keep business idempotency distinct from transport delivery dedupe.
  - **Refs:** [idempotency lifecycle](specs/platform-foundation-gap-closure.md), [transaction idempotency](specs/transaction-plan-and-commit-coordinator.md), [integration operation identity](specs/integration-platform.md).

## 66. Production frontend and governed page composition

This program implements [the production frontend and governed page-composition plan](specs/production-frontend-and-page-composition.md). It preserves the signed release ceilings: Gate A/B entries serve the bounded Promotion slice; generic pages and customer composition do not gain production authority until their declared later gate or phase.

### Program contracts and governance

- [x] `WEB-001` **[P0][TERRA] Publish the production-frontend delivery manifest.**
  - **Evidence (2026-09-05):** `TestTodo_WEB_001`, `TestTodo_WEB_001_Conformance`, `TestTodo_WEB_001_Golden`, `TestTodo_WEB_001_Browser`, `TestTodo_WEB_001_SizeCeiling` in `tools/policy/webdelivery` over the published manifest `definitions/ux/production-frontend-delivery.yaml` (GWC v5.0.1 wasm bundle + wasm_exec.js embedded by cmd/hcmnext, shell routes, the CSP the shell emits, tunnel transport, forbidden runtimes, 32 MiB ceiling against the built 24.7 MiB bundle); `go test -count=1 ./tools/policy/webdelivery/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `UX-001`, `UXFLOW-001`, `ARCH-GO-001`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=publish the production-frontend delivery manifest without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_001`; `GOLDEN=TestTodo_WEB_001_Golden`; `BROWSER=TestTodo_WEB_001_Browser`; `CONFORMANCE=TestTodo_WEB_001_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—publish the production-frontend delivery manifest—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—publish the production-frontend delivery manifest—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [master plan](plan.md).

- [x] `WEB-002` **[P0][TERRA] Define the versioned PageDefinition contract.**
  - **Evidence (2026-09-05):** `TestTodo_WEB_002`, `TestTodo_WEB_002_{Golden,Security,Conformance}`, `TestTodo_WEB_002_Browser` (documented skip: a Go value contract has no browser surface), `TestRegionKinds`, `TestKnownRPCs`, `TestPromotionPagesProjectOntoJourneyService` in `tools/uxqual/pagedef` (versioned PageDefinition with a closed eight-kind region vocabulary from the frontend plan's page anatomy, widget and RPC references only, actions requiring a role, accessibility landmarks/heading order/live-region politeness, brand token refs; Validate refuses markup, unknown region kinds, unknown RPCs resolved against the real generated ServiceDescs of journey/intents/registry/admin, skipped heading levels; deterministic Canonical/Digest; the Promotion list and detail pages project onto hcmnext.journey.v1.JourneyService with pinned digests); `go test -count=1 ./tools/uxqual/pagedef/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WEB-001`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=define the versioned pagedefinition contract without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_002`; `GOLDEN=TestTodo_WEB_002_Golden`; `BROWSER=TestTodo_WEB_002_Browser`; `CONFORMANCE=TestTodo_WEB_002_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—define the versioned pagedefinition contract—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—define the versioned pagedefinition contract—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [master plan](plan.md).

- [x] `WEB-003` **[P0][TERRA] Define the versioned floorplan registry.**
  - **Evidence (2026-09-05):** `TestTodo_WEB_003` in `tools/uxqual/floorplan` (versioned floorplans with closed breakpoints, named regions from pagedef's closed kind vocabulary, layout constraints and per-breakpoint rules, a registry with Lookup by ref@version, Validate, canonical digest, and a resolver that binds a PageDefinition to its floorplan and refuses undeclared regions; golden for the Promotion pages' floorplan; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/uxqual/floorplan/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WEB-002`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=define the versioned floorplan registry without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_003`; `GOLDEN=TestTodo_WEB_003_Golden`; `BROWSER=TestTodo_WEB_003_Browser`; `CONFORMANCE=TestTodo_WEB_003_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—define the versioned floorplan registry—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—define the versioned floorplan registry—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [master plan](plan.md).

- [x] `WEB-004` **[P0][TERRA] Define the semantic-region vocabulary.**
  - **Evidence (2026-09-05):** `TestTodo_WEB_004` in `tools/uxqual/pagedef` (the semantic-region vocabulary as a first-class registry: each region kind with its landmark role, allowed child kinds, required accessibility attributes and live-region eligibility under a stable digest, a conformance test that the SSR shell's landmark map covers every kind, golden vocabulary digest; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/uxqual/pagedef/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WEB-003`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=define the semantic-region vocabulary without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_004`; `GOLDEN=TestTodo_WEB_004_Golden`; `BROWSER=TestTodo_WEB_004_Browser`; `CONFORMANCE=TestTodo_WEB_004_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—define the semantic-region vocabulary—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—define the semantic-region vocabulary—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [master plan](plan.md).

- [x] `WEB-005` **[P0][TERRA] Define the governed widget-registry lifecycle.**
  - **Evidence (2026-09-05):** `TestTodo_WEB_005` in `tools/uxqual/widgetreg` (governed widget registry with id, version, allowed region kinds, RPC bindings by ref, accessibility contract, brand token refs, owner and a DRAFT/PUBLISHED/DEPRECATED/RETIRED lifecycle as digested transitions, deprecation requiring a successor, retirement refused while a PageDefinition references the widget, Lookup by ref@version, a resolver binding every widget slot to a PUBLISHED widget, golden of the Promotion registry; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/uxqual/widgetreg/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WEB-004`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=define the governed widget-registry lifecycle without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_005`; `GOLDEN=TestTodo_WEB_005_Golden`; `BROWSER=TestTodo_WEB_005_Browser`; `CONFORMANCE=TestTodo_WEB_005_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—define the governed widget-registry lifecycle—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—define the governed widget-registry lifecycle—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [master plan](plan.md).

- [x] `WEB-006` **[P0][TERRA] Define the provenance-bearing BoundValue contract.**
  - **Evidence (2026-09-05):** `TestTodo_WEB_006`, `tools/uxqual/presentation.BoundValue` carries source identity/version, authority class, classification, confidence/effective/recorded time, editability, and the closed field-disposition vocabulary; validation prevents hidden, redacted, or masked raw values reaching a renderer and produces deterministic digests. `TestTodo_WEB_006*`; `go test -count=1 ./tools/uxqual/presentation` PASS.
  - **Depends:** `WEB-005`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=define the provenance-bearing boundvalue contract without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_006`; `GOLDEN=TestTodo_WEB_006_Golden`; `BROWSER=TestTodo_WEB_006_Browser`; `CONFORMANCE=TestTodo_WEB_006_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—define the provenance-bearing boundvalue contract—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—define the provenance-bearing boundvalue contract—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [master plan](plan.md).

- [x] `WEB-007` **[P0][TERRA] Define the semantic-action presentation contract.**
  - **Evidence (2026-09-05):** `TestTodo_WEB_007`, `tools/uxqual/presentation.SemanticAction` binds only registered generated gRPC methods and explicitly carries availability, opaque action token, expected resource version, idempotency key, typed input schema, safe unavailability explanation, and obligations; hidden actions expose neither label nor token. `TestTodo_WEB_007*`; targeted package PASS; `go test -count=1 ./tools/uxqual/presentation/` PASS on windows/arm64 (Go 1.26.3).
  - **Depends:** `WEB-006`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=define the semantic-action presentation contract without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_007`; `GOLDEN=TestTodo_WEB_007_Golden`; `BROWSER=TestTodo_WEB_007_Browser`; `CONFORMANCE=TestTodo_WEB_007_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—define the semantic-action presentation contract—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—define the semantic-action presentation contract—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [master plan](plan.md).

- [x] `WEB-008` **[P0][TERRA] Define the page compatibility dependency graph.**
  - **Evidence (2026-09-05):** `TestTodo_WEB_008`, `tools/uxqual/presentation.CompatibilityGraph` models page, floorplan, widget, token, and RPC dependencies, rejects duplicate/dangling/self/cyclic edges, and returns deterministic transitive impact. `TestTodo_WEB_008*`; targeted package PASS; `go test -count=1 ./tools/uxqual/presentation/` PASS on windows/arm64 (Go 1.26.3).
  - **Depends:** `WEB-007`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=define the page compatibility dependency graph without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_008`; `GOLDEN=TestTodo_WEB_008_Golden`; `BROWSER=TestTodo_WEB_008_Browser`; `CONFORMANCE=TestTodo_WEB_008_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—define the page compatibility dependency graph—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—define the page compatibility dependency graph—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [master plan](plan.md).

- [x] `WEB-009` **[P0][TERRA] Classify frontend scope by release gate.**
  - **Evidence (2026-09-05):** `TestTodo_WEB_009`, `tools/uxqual/presentation.ScopeEntry` uses a closed Gate A/B/C and Phase 2/3/4 vocabulary, names the owning authority for every capability, and distinguishes read-only exposure from action authority. `TestTodo_WEB_009*`; targeted package PASS; `go test -count=1 ./tools/uxqual/presentation/` PASS on windows/arm64 (Go 1.26.3).
  - **Depends:** `WEB-008`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=classify frontend scope by release gate without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_009`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_009`; `GOLDEN=TestTodo_WEB_009_Golden`; `BROWSER=TestTodo_WEB_009_Browser`; `CONFORMANCE=TestTodo_WEB_009_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—classify frontend scope by release gate—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—classify frontend scope by release gate—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [master plan](plan.md).

- [x] `WEB-010` **[P0][TERRA] Define the frontend evidence bundle.**
  - **Evidence (2026-09-05):** `TestTodo_WEB_010`, `tools/uxqual/presentation.EvidenceBundle` requires digest-addressed unit, browser, accessibility, security, localization, performance, and recovery artifacts with reverse requirement coverage. `TestTodo_WEB_010*`; targeted package PASS; `go test -count=1 ./tools/uxqual/presentation/` PASS on windows/arm64 (Go 1.26.3).
  - **Depends:** `WEB-009`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=define the frontend evidence bundle without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_010`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_010`; `GOLDEN=TestTodo_WEB_010_Golden`; `BROWSER=TestTodo_WEB_010_Browser`; `CONFORMANCE=TestTodo_WEB_010_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—define the frontend evidence bundle—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—define the frontend evidence bundle—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [master plan](plan.md).

- [x] `WEB-011` **[P0][TERRA] Publish the website threat model.**
  - **Evidence (2026-09-05):** `TestTodo_WEB_011`, `definitions/ux/website-threat-model.yaml` covers browser/edge, edge/capability, customer-configuration/renderer, and browser-storage/cache trust boundaries with attacks, controls, and evidence; parsed and validated by `TestTodo_WEB_011*`; targeted package PASS; `go test -count=1 ./tools/uxqual/presentation/` PASS on windows/arm64 (Go 1.26.3).
  - **Depends:** `WEB-010`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=publish the website threat model without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_011`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_011`; `GOLDEN=TestTodo_WEB_011_Golden`; `BROWSER=TestTodo_WEB_011_Browser`; `CONFORMANCE=TestTodo_WEB_011_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—publish the website threat model—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—publish the website threat model—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [master plan](plan.md).

- [x] `WEB-012` **[P0][TERRA] Assign production-frontend ownership boundaries.**
  - **Evidence (2026-09-05):** `TestTodo_WEB_012`, `tools/uxqual/presentation.OwnershipBoundary` requires one owner plus explicit positive and negative decision authority for presentation, authorization, and business-state concerns, with deterministic canonical ordering. `TestTodo_WEB_012*`; targeted package PASS; `go test -count=1 ./tools/uxqual/presentation/` PASS on windows/arm64 (Go 1.26.3).
  - **Depends:** `WEB-011`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=assign production-frontend ownership boundaries without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_012`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_012`; `GOLDEN=TestTodo_WEB_012_Golden`; `BROWSER=TestTodo_WEB_012_Browser`; `CONFORMANCE=TestTodo_WEB_012_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—assign production-frontend ownership boundaries—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—assign production-frontend ownership boundaries—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [master plan](plan.md).

### Design-system and accessibility foundations

- [x] `WEB-013` **[GATE_C][TERRA] Define semantic color tokens.**
  - **Evidence (2026-09-05):** `TestTodo_WEB_013`, `TestTodo_WEB_013_{Golden,Browser,Conformance}` in `internal/humanwork/productui` (ordered semantic color registry, deterministic CSP-hashable compilation, allowlisted customer overrides, CSS-injection rejection, WCAG 2.2 AA contrast validation, and platform-owned focus/status tokens); `go test -count=1 ./internal/humanwork/productui ./internal/humanwork/workspace` PASS on windows/arm64 (Go 1.26.3).
  - **Depends:** `WEB-002`, `UX-003`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=define semantic color tokens without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_013`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_013`; `GOLDEN=TestTodo_WEB_013_Golden`; `BROWSER=TestTodo_WEB_013_Browser`; `CONFORMANCE=TestTodo_WEB_013_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—define semantic color tokens—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—define semantic color tokens—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [x] `WEB-014` **[GATE_C][TERRA] Define the production typography scale.**
  - **Evidence (2026-09-05):** `TestTodo_WEB_014`, `TestTodo_WEB_014_{Golden,Browser,Conformance}` in `internal/humanwork/productui` (semantic sans/mono font families, body/supporting/page-heading scale and line height, safe customer font-stack validation, and browser consumption through CSS variables); `go test -count=1 ./internal/humanwork/productui` PASS on windows/arm64 (Go 1.26.3).
  - **Depends:** `WEB-013`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=define the production typography scale without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_014`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_014`; `GOLDEN=TestTodo_WEB_014_Golden`; `BROWSER=TestTodo_WEB_014_Browser`; `CONFORMANCE=TestTodo_WEB_014_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—define the production typography scale—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—define the production typography scale—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [x] `WEB-015` **[GATE_C][TERRA] Define spacing and density tokens.**
  - **Evidence (2026-09-05):** `TestTodo_WEB_015`, `TestTodo_WEB_015_{Golden,Browser,Conformance}` in `internal/humanwork/productui` (ordered spacing scale and bounded comfortable/compact/relaxed density overrides; arbitrary or extreme density rejected); `go test -count=1 ./internal/humanwork/productui` PASS on windows/arm64 (Go 1.26.3).
  - **Depends:** `WEB-014`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=define spacing and density tokens without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_015`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_015`; `GOLDEN=TestTodo_WEB_015_Golden`; `BROWSER=TestTodo_WEB_015_Browser`; `CONFORMANCE=TestTodo_WEB_015_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—define spacing and density tokens—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—define spacing and density tokens—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [x] `WEB-016` **[GATE_C][TERRA] Define surface and elevation semantics.**
  - **Evidence (2026-09-05):** `TestTodo_WEB_016`, `TestTodo_WEB_016_{Golden,Browser,Conformance}` in `internal/humanwork/productui` (semantic canvas/surface/border, bounded control/surface radii, resting/raised elevation, and CSP-safe customer resolution shared by product and embedded Journey components); `go test -count=1 ./internal/humanwork/productui` PASS on windows/arm64 (Go 1.26.3).
  - **Depends:** `WEB-015`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=define surface and elevation semantics without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_016`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_016`; `GOLDEN=TestTodo_WEB_016_Golden`; `BROWSER=TestTodo_WEB_016_Browser`; `CONFORMANCE=TestTodo_WEB_016_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—define surface and elevation semantics—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—define surface and elevation semantics—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [x] `WEB-017` **[GATE_C][TERRA] Define the governed icon vocabulary.**
  - **Evidence (2026-09-05):** `TestTodo_WEB_017`, `TestTodo_WEB_017_{Golden,Browser,Conformance}` in `internal/humanwork/productui` (closed named SVG-path registry, immutable public inventory, decorative accessibility contract, stable Journey glyph, and non-injecting fallback for unknown names); `go test -count=1 ./internal/humanwork/productui` PASS on windows/arm64 (Go 1.26.3).
  - **Depends:** `WEB-016`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=define the governed icon vocabulary without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_017`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_017`; `GOLDEN=TestTodo_WEB_017_Golden`; `BROWSER=TestTodo_WEB_017_Browser`; `CONFORMANCE=TestTodo_WEB_017_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—define the governed icon vocabulary—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—define the governed icon vocabulary—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [x] `WEB-018` **[GATE_C][TERRA] Define motion and reduced-motion semantics.**
  - **Evidence (2026-09-05):** `TestTodo_WEB_018`, `TestTodo_WEB_018_{Golden,Browser,Conformance}` in `internal/humanwork/productui` (bounded duration/easing/distance tokens; page, list, status, popover, button and loading feedback; `prefers-reduced-motion:no-preference` gating; platform-owned `animation:none!important`, near-zero transitions and zero travel under reduced motion); `go test -count=1 ./internal/humanwork/productui ./tools/uxqual/render/journey` PASS on windows/arm64 (Go 1.26.3).
  - **Depends:** `WEB-017`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=define motion and reduced-motion semantics without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_018`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_018`; `GOLDEN=TestTodo_WEB_018_Golden`; `BROWSER=TestTodo_WEB_018_Browser`; `CONFORMANCE=TestTodo_WEB_018_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—define motion and reduced-motion semantics—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—define motion and reduced-motion semantics—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [x] `WEB-019` **[GATE_C][TERRA] Define visible-focus behavior.**
  - **Evidence (2026-09-06):** `TestTodo_WEB_019`, `TestTodo_WEB_019_Golden`, `TestTodo_WEB_019_Browser`, and `TestTodo_WEB_019_Conformance` in `internal/humanwork/productui` (one deterministic platform focus contract uses the protected semantic focus token across native controls and embedded Journey components, excludes programmatic route headings, preserves forced-colors and reduced-motion behavior, and prevents the viewport-edge brand mark from clipping its ring); Codex in-app browser keyboard inspection passed in light and dark modes; `BenchmarkFocusRendering` measured 76.5–79.5 µs/op, 2,888 B/op, 17 allocs/op; `go test -count=1 ./internal/humanwork/productui ./tools/uxqual/render/journey` and the interaction-latency gate PASS on windows/amd64 (Go 1.26.3); implemented by a GPT-5.6 Luna lane and independently refined with GPT-5.6 Sol.
  - **Depends:** `WEB-018`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=define visible-focus behavior without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_019`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_019`; `GOLDEN=TestTodo_WEB_019_Golden`; `BROWSER=TestTodo_WEB_019_Browser`; `CONFORMANCE=TestTodo_WEB_019_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—define visible-focus behavior—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—define visible-focus behavior—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [x] `WEB-020` **[GATE_C][TERRA] Define validation and error presentation.**
  - **Evidence (2026-09-06):** `TestTodo_WEB_020`, `TestTodo_WEB_020_Golden`, `TestTodo_WEB_020_Browser`, and `TestTodo_WEB_020_Conformance` in `internal/humanwork/productui` (reusable GWC validation summary/input contracts localize reviewed findings, associate fields through stable IDs and `aria-invalid`/`aria-errormessage`/`aria-describedby`, expose one predictable assertive announcement, filter warnings and dangling service targets, focus the summary after attempted submission, and use semantic dark/high-contrast/reduced-motion styling); `TestFrontendI18nAccessibilityGateEveryRegisteredPage` passes all 17 registered pages in `en-US`, `de-DE`, and RTL `ar` with parsed ID/reference, accessible-name, keyboard, reduced-motion, and localization checks; Codex in-app browser inspection caught and regression-fixed a two-column composition defect and passed final light/dark renders; validation feedback p95 3.06 ms against a 16 ms gate and `BenchmarkValidationRendering` 0.42–0.64 ms/op; `go test -count=1 ./internal/humanwork/productui ./tools/uxqual/render/journey` PASS on windows/amd64 (Go 1.26.3); implemented by a GPT-5.6 Luna lane and independently refined with GPT-5.6 Sol.
  - **Depends:** `WEB-019`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=define validation and error presentation without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_020`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_020`; `GOLDEN=TestTodo_WEB_020_Golden`; `BROWSER=TestTodo_WEB_020_Browser`; `CONFORMANCE=TestTodo_WEB_020_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—define validation and error presentation—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—define validation and error presentation—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [x] `WEB-021` **[GATE_C][TERRA] Define multidimensional status presentation.**
  - **Depends:** `WEB-020`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=define multidimensional status presentation without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_021`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_021`; `GOLDEN=TestTodo_WEB_021_Golden`; `BROWSER=TestTodo_WEB_021_Browser`; `CONFORMANCE=TestTodo_WEB_021_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—define multidimensional status presentation—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—define multidimensional status presentation—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).
  - **Evidence (2026-09-06):** `TestTodo_WEB_021`, `TestTodo_WEB_021_Golden`, `TestTodo_WEB_021_Browser`, and `TestTodo_WEB_021_Conformance` prove a reusable GoWebComponents presentation of the five canonical lifecycle dimensions with typed protobuf enums, explicit presence/redaction handling, stable hashed DOM identity, independent contradictory states, localized text+glyph cues, responsive/dark/forced-color/reduced-motion styling, and no aggregate status authority; `TestFrontendI18nAccessibilityGateEveryRegisteredPage` passes the complete 17-page × 3-locale registry matrix and is now wired into `go run ./tools/quality` as a release gate; Codex in-app browser review covered light, dark, and RTL Arabic layouts and caught/refined truncated labels plus incomplete accessible link names; focused interaction p95 remained 0.55 ms against the 16 ms budget and `BenchmarkStatusPresentationRendering` measured 61.6–68.5 µs/op, 25,120 B/op, 244 allocs/op; focused `productui`, `localize`, journey transport/client/render/WASM, and quality-gate tests pass on windows/amd64 (Go 1.26.3); implemented by a GPT-5.6 Luna lane, independently corrected by GPT-5.6 Sol, and refined through manual visual QA; `go test -count=1 ./internal/humanwork/productui/` PASS on windows/arm64 (Go 1.26.3).

- [x] `WEB-022` **[GATE_C][TERRA] Define the provenance visual grammar.**
  - **Depends:** `WEB-021`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=define the provenance visual grammar without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_022`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_022`; `GOLDEN=TestTodo_WEB_022_Golden`; `BROWSER=TestTodo_WEB_022_Browser`; `CONFORMANCE=TestTodo_WEB_022_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—define the provenance visual grammar—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—define the provenance visual grammar—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).
  - **Evidence (2026-09-06):** `TestTodo_WEB_022`, its GOLDEN/BROWSER/CONFORMANCE matrix, canonical-adapter and security tests prove a reusable GoWebComponents provenance grammar over canonical `AuthorityKind` and lineage status with seven independently-present facts, explicit partial/redacted/opaque/unavailable states, stable accessible definition-list markup, and structural exclusion of protected policy/evidence references, digests, graph identifiers, confidence, trust, and permission inference; unbound projections render no misleading panel; the registered-page i18n/accessibility release gate passes, as do focused `productui`, `localize`, journey client/render/WASM packages; Codex in-app browser review covered light, dark, and RTL Arabic layouts; provenance interaction p95 remained 0.00-0.52 ms against the 16 ms budget and `BenchmarkProvenancePresentationRendering` measured 48.0-54.6 us/op, 36,703-36,705 B/op, 349 allocs/op; implemented by a GPT-5.6 Luna lane, independently hardened by GPT-5.6 Sol, and refined through manual visual QA; `go test -count=1 ./internal/humanwork/productui/` PASS on windows/arm64 (Go 1.26.3).

- [x] `WEB-023` **[GATE_C][TERRA] Define responsive layout transformation rules.**
  - **Depends:** `WEB-022`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=define responsive layout transformation rules without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_023`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_023`; `GOLDEN=TestTodo_WEB_023_Golden`; `BROWSER=TestTodo_WEB_023_Browser`; `CONFORMANCE=TestTodo_WEB_023_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—define responsive layout transformation rules—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—define responsive layout transformation rules—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).
  - **Evidence (2026-09-06):** `TestTodo_WEB_023`, `TestTodo_WEB_023_Golden`, `TestTodo_WEB_023_Browser`, and `TestTodo_WEB_023_Conformance` prove a deterministic mobile-first transformation of the existing governed floorplan vocabulary into one stable GoWebComponents DOM carrying closed narrow/compact/standard/wide metadata, with renderer-owned 40/60/80rem CSS, semantic breakpoint ordering independent of declaration order, narrow minimum-column fallback, immutable document/action order, 1-12 bounded columns, cumulative-rule validation, logical RTL properties, full-row region headings, and no user-agent, viewport, markup, CSS, policy, or authority input; intrinsically two-dimensional tables retain native semantics and readable values inside labeled keyboard/touch-scrollable regions rather than using hidden page overflow or destructive character wrapping; the registered-page i18n/accessibility gate and focused floorplan/page/workspace/GWC/SSR/token/qualification suites pass; Codex in-app browser QA covered 320px LTR, 320px RTL, keyboard focus plus arrow-key table scrolling, and wide layout, catching and refining both dense-table fragmentation and heading-as-grid-cell defects; responsive latency passed five repetitions under the 16 ms p95 budget, `BenchmarkResponsiveLayoutTransformation` measured 5.26-5.63 us/op at 1,728 B/op and 15 allocs/op, and full construction plus serialization measured 154-164 us/op at about 64.4 KB/op and 432 allocs/op; implemented by GPT-5.6 Luna, independently hardened by GPT-5.6 Sol, and refined through manual visual QA; `go test -count=1 ./tools/uxqual/floorplan/` PASS on windows/arm64 (Go 1.26.3).

- [x] `WEB-024` **[GATE_C][TERRA] Define high-contrast and print modes.**
  - **Depends:** `WEB-023`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=define high-contrast and print modes without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_024`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_024`; `GOLDEN=TestTodo_WEB_024_Golden`; `BROWSER=TestTodo_WEB_024_Browser`; `CONFORMANCE=TestTodo_WEB_024_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—define high-contrast and print modes—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—define high-contrast and print modes—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).
  - **Evidence (2026-09-06):** `TestTodo_WEB_024`, `TestTodo_WEB_024_Golden`, `TestTodo_WEB_024_Browser`, and `TestTodo_WEB_024_Conformance` prove shared renderer-owned `prefers-contrast:more`, forced-colors, and print contracts in both the GoWebComponents and semantic SSR documents; protected system colors and focus outlines remain outside customer-brand token overrides, status and severity retain real non-color DOM labels, print retains machine-readable simulation/provenance timestamps and entered form values while suppressing bounded interactive-only surfaces, logical properties preserve RTL behavior, and no CSS-generated accessibility content or unauthorized fixture value is emitted. The production registry-driven i18n/accessibility release gate passed across every registered page and supported locale, focused renderer/qualification suites passed, and `git diff --check` was clean. Codex in-app browser review compared the GWC and SSR renderings and their accessibility trees. `BenchmarkWorkspaceModeContracts` measured 44.5-72.7 us/op, 50,982-50,983 B/op, and 166 allocs/op; implemented by GPT-5.6 Luna, independently hardened by GPT-5.6 Sol, and refined through manual visual QA.

### Go renderer and browser runtime

- [x] `WEB-025` **[GATE_C][SOL_HIGH] Implement the semantic SSR shell renderer.**
  - **Evidence (2026-09-05):** `TestTodo_WEB_025` in `tools/uxqual/ssrshell` (server-side semantic HTML shell from a validated PageDefinition: landmark elements per region kind, headings at declared levels, live-region attributes, empty widget mount points by ref, a JSON data island with page id/version/digest, html/template autoescaping with no inline script, deterministic bytes and digest, a CSP builder hashing the shell stylesheet, goldens for both Promotion pages and a real DOM parse; integration point is the workspace shell route, reported not wired; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/uxqual/ssrshell/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WEB-002`, `UX-QUAL-001`, `TOOL-009`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=implement the semantic ssr shell renderer without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_025`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_025`; `GOLDEN=TestTodo_WEB_025_Golden`; `BROWSER=TestTodo_WEB_025_Browser`; `CONFORMANCE=TestTodo_WEB_025_Conformance`; `SECURITY=TestTodo_WEB_025_Security`; `INTEGRATION=TestTodo_WEB_025_Integration`; `FAULT=TestTodo_WEB_025_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the semantic ssr shell renderer—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the semantic ssr shell renderer—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Go constitution](specs/go-only-technology-constitution.md).

- [x] `WEB-026` **[GATE_C][SOL_HIGH] Implement deterministic GWC page rendering.**
  - **Evidence (2026-09-05):** `TestTodo_WEB_026` in `tools/uxqual/render/page`, `tools/uxqual/render/journey` (deterministic GWC page renderer mounting registered widgets into the SSR shell's landmark structure in definition order over a widget registry port, unregistered refs refused with a typed error, no free HTML, determinism proven by sha256 over GWC's sorted SSR output and pinned for both Promotion pages; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/uxqual/render/page/ ./tools/uxqual/render/journey/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WEB-025`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=implement deterministic gwc page rendering without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_026`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_026`; `GOLDEN=TestTodo_WEB_026_Golden`; `BROWSER=TestTodo_WEB_026_Browser`; `CONFORMANCE=TestTodo_WEB_026_Conformance`; `SECURITY=TestTodo_WEB_026_Security`; `INTEGRATION=TestTodo_WEB_026_Integration`; `FAULT=TestTodo_WEB_026_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement deterministic gwc page rendering—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement deterministic gwc page rendering—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Go constitution](specs/go-only-technology-constitution.md).

- [x] `WEB-027` **[GATE_C][SOL_HIGH] Implement the Go-WASM mount lifecycle.**
  - **Depends:** `WEB-026`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=implement the go-wasm mount lifecycle without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_027`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_027`; `GOLDEN=TestTodo_WEB_027_Golden`; `BROWSER=TestTodo_WEB_027_Browser`; `CONFORMANCE=TestTodo_WEB_027_Conformance`; `SECURITY=TestTodo_WEB_027_Security`; `INTEGRATION=TestTodo_WEB_027_Integration`; `FAULT=TestTodo_WEB_027_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the go-wasm mount lifecycle—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the go-wasm mount lifecycle—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Go constitution](specs/go-only-technology-constitution.md).
  - **Evidence (2026-09-07):** `TestTodo_WEB_027`, `TestTodo_WEB_027_Golden`, `TestTodo_WEB_027_Browser`, `TestTodo_WEB_027_Conformance`, `TestTodo_WEB_027_Security`, `TestTodo_WEB_027_Integration`, and `TestTodo_WEB_027_Fault` prove one process-wide Go-WASM mount lifecycle with explicit idle/mounting/mounted/failed/stopped states, typed selector/mount/stop/failure errors, duplicate-mount convergence, stop-safe in-flight rendering, panic-contained idempotent cleanup, retry after failure or stop, and real GWC unmounting that releases the Store subscription. A compiled `js/wasm` test binary executed under Go's Node WASM harness mounted the live component, observed its accessible landmarks and live region, propagated a Store update into the DOM, and verified that unmount removed the DOM and subscriber; the native renderer, WASM command, journey client, workspace, and vet suites passed. The production i18n/accessibility release gate passed over every registered page and supported locale, and its quality-runner wiring test passed. Codex in-app browser QA followed the live journey list into a completed promotion detail and found one stable mounted shell with no blank, duplicate, or fallback content. `BenchmarkMountLifecycle` measured 97.31-126.0 ns/op, 208 B/op, and 3 allocs/op; Windows could not run Go's race detector because CGO is unavailable. Implemented by GPT-5.6 Luna and independently hardened to a no-P0/P1 verdict by GPT-5.6 Sol.

- [x] `WEB-028` **[GATE_C][SOL_HIGH] Prove SSR-to-WASM hydration parity.**
  - **Depends:** `WEB-027`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=prove ssr-to-wasm hydration parity without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_028`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_028`; `GOLDEN=TestTodo_WEB_028_Golden`; `BROWSER=TestTodo_WEB_028_Browser`; `CONFORMANCE=TestTodo_WEB_028_Conformance`; `SECURITY=TestTodo_WEB_028_Security`; `INTEGRATION=TestTodo_WEB_028_Integration`; `FAULT=TestTodo_WEB_028_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—prove ssr-to-wasm hydration parity—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—prove ssr-to-wasm hydration parity—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Go constitution](specs/go-only-technology-constitution.md).
  - **Evidence (2026-09-07):** `TestTodo_WEB_028`, `TestTodo_WEB_028_Golden`, `TestTodo_WEB_028_Browser`, `TestTodo_WEB_028_Conformance`, `TestTodo_WEB_028_Security`, `TestTodo_WEB_028_Integration`, and `TestTodo_WEB_028_Fault` prove a bounded deterministic semantic comparison between the validated SSR document and mounted GWC projection: exact governed page id/version/digest; ordered landmarks, headings, and widget slots; one canonical live-region contract; closed authorization dispositions; and preserved focusable target, native form values, accessible field-error relationships, and hidden idempotency inputs. Widget interiors may differ only as passive markup: executable/embedded elements, event handlers, `srcdoc`, executable URLs, duplicate attributes, duplicate JSON keys, detached self-asserted preservation markers, missing evidence, and mismatches are refused with typed sanitized errors. Both real Promotion page definitions pass end-to-end through the SSR and GWC renderers, and a parsed-DOM regression proves the composed workspace owns exactly one canonical live region. The exact matrix passed natively and as a compiled `js/wasm` binary under Go's Node WASM harness; focused renderer/workspace tests, vet, the production registered-page i18n/accessibility gate, its quality-runner wiring test, and `git diff --check` passed. Codex in-app browser QA exercised detail-to-list back navigation through the route-shaped loading proxy and verified a stable shell and settled content without duplicate or blank UI. `BenchmarkHydrationParity` measured 161.9-191.9 us/op, 80,566-80,594 B/op, and 1,485 allocs/op; this snapshot gate binds the focus marker to a real focusable element, while real-browser `document.activeElement` and assistive-technology preservation remain later host qualification. Implemented by GPT-5.6 Luna and adversarially hardened to a no-P0/P1 verdict by GPT-5.6 Sol.

- [x] `WEB-029` **[GATE_C][SOL_HIGH] Implement progressive-enhancement form fallback.**
  - **Depends:** `WEB-028`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=implement progressive-enhancement form fallback without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_029`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_029`; `GOLDEN=TestTodo_WEB_029_Golden`; `BROWSER=TestTodo_WEB_029_Browser`; `CONFORMANCE=TestTodo_WEB_029_Conformance`; `SECURITY=TestTodo_WEB_029_Security`; `INTEGRATION=TestTodo_WEB_029_Integration`; `FAULT=TestTodo_WEB_029_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement progressive-enhancement form fallback—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement progressive-enhancement form fallback—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Go constitution](specs/go-only-technology-constitution.md).
  - **Evidence (2026-09-07):** `TestTodo_WEB_029`, `TestTodo_WEB_029_Golden`, `TestTodo_WEB_029_Browser`, `TestTodo_WEB_029_Conformance`, `TestTodo_WEB_029_Security`, `TestTodo_WEB_029_Integration`, and `TestTodo_WEB_029_Fault` prove one bounded transport-only contract over the existing authorized `contract.RequestField` projection: a complete script-free semantic POST form and the Go/WASM submit collector normalize to identical fresh value maps; server-issued CSRF, idempotency, locale, worker, and transition values are preserved and verified; unknown, duplicate, masked, authority-selecting, oversized, malformed, wrong-route/content-type, noncanonical-date, and forged hidden inputs are refused with static sanitized errors; labels, requiredness, error summary/field relationships, locale, focus target, form values, and native submit semantics remain present. The existing canonical Promotion `IntentInstance` now includes `proposedGrade` across form and capability routes, so changing grade changes the governed digest while localized money presentation canonicalizes to the same locale-independent intent. Refresh, back, and double submission preserve one server-issued idempotency key and one canonical digest without moving durable claim/recovery authority into the UX package. The legacy fixture-only `uxqual.wasm` bundle is explicitly not advertised or served because it would replace the bound form with `action="#"`; the production handler therefore retains the fully functional SSR POST fallback until a future client consumes its binding island. The exact matrix passed natively and as a compiled `js/wasm` test binary; focused forms/workspace tests, vet, the production registered-page i18n/accessibility gate, its quality-runner wiring test, and `git diff --check` passed. Codex in-app browser QA loaded a real Omar promotion, inspected the accessible form tree, submitted `Run simulation` as a native POST, and verified preserved values plus a new zero-effect receipt. `BenchmarkProgressiveFormNormalize` measured 10.4-14.4 us/op, 5,000 B/op, and 76 allocs/op; implemented by GPT-5.6 Luna and adversarially hardened by GPT-5.6 Sol.

- [x] `WEB-030` **[GATE_C][SOL_HIGH] Implement deep-link and resume routing.**
  - **Depends:** `WEB-029`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=implement deep-link and resume routing without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_030`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_030`; `GOLDEN=TestTodo_WEB_030_Golden`; `BROWSER=TestTodo_WEB_030_Browser`; `CONFORMANCE=TestTodo_WEB_030_Conformance`; `SECURITY=TestTodo_WEB_030_Security`; `INTEGRATION=TestTodo_WEB_030_Integration`; `FAULT=TestTodo_WEB_030_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement deep-link and resume routing—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement deep-link and resume routing—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Go constitution](specs/go-only-technology-constitution.md).
  - **Evidence (2026-09-07):** `TestTodo_WEB_030`, `TestTodo_WEB_030_Golden`, `TestTodo_WEB_030_Browser`, `TestTodo_WEB_030_Conformance`, `TestTodo_WEB_030_Security`, `TestTodo_WEB_030_Integration`, and `TestTodo_WEB_030_Fault` prove production deep-link and resume behavior through the real Go/GWC v5 HistoryRouter and product client. Cold, pushState, popstate/back, and forward navigation retain only canonical page-scoped presentation state; unknown, wrong-page, mutation, credential, CSRF, idempotency, and authority-shaped parameters are removed before loader-key construction or service reads; duplicate, malformed, control-character, oversized, and invalid controlled values fail closed. Loaders consume their immutable `RouteContext` query, cancellation and generation guards suppress stale completions, same-session `LoadWithBaseline` preserves the shell while rereading destination-owned data, and tenant/principal/scope/role/permission mismatches forbid baseline reuse. Missing, stale, and unauthorized journey selectors render one static non-disclosing result; history state rejects blank/oversized identifiers and negative, fractional, non-finite, or overflowing indices; cold and history traversal cannot replay preference or workflow-use mutations. Product focus policy exclusively owns heading/collection focus and accessible route announcements. Resolved People and History pagination replaces the current URL with the effective authorized and clamped page without adding a history entry or replaying a write. The exact matrix passed natively and as an actual `GOOS=js GOARCH=wasm` binary under Node; focused product/journey suites, vet, `git diff --check`, the 17-page x 3-locale i18n/accessibility gate, and its quality-runner wiring test passed. Codex in-app browser QA covered a cold employee deep link, software navigation, back/forward restoration, removal of stale and credential-shaped query fields, truthful resolved pagination, stable shell, destination-shaped loading, and focus continuity. `BenchmarkProductDeepLinkResolve` measured 14.1 us/op, 12,960 B/op, 68 allocs/op; `BenchmarkProductRouteResume` measured 112 us/op, 56,847 B/op, 413 allocs/op; `BenchmarkResolvedCanonicalHref` measured 4.97-6.24 us/op, 6,072 B/op, 29 allocs/op; implemented by GPT-5.6 Luna and adversarially hardened by GPT-5.6 Sol.

- [x] `WEB-031` **[GATE_C][SOL_HIGH] Enforce the browser-state storage boundary.**
  - **Depends:** `WEB-030`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=enforce the browser-state storage boundary without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_031`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_031`; `GOLDEN=TestTodo_WEB_031_Golden`; `BROWSER=TestTodo_WEB_031_Browser`; `CONFORMANCE=TestTodo_WEB_031_Conformance`; `SECURITY=TestTodo_WEB_031_Security`; `INTEGRATION=TestTodo_WEB_031_Integration`; `FAULT=TestTodo_WEB_031_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—enforce the browser-state storage boundary—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—enforce the browser-state storage boundary—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Go constitution](specs/go-only-technology-constitution.md).
  - **Evidence (2026-09-07):** `TestTodo_WEB_031`, `TestTodo_WEB_031_Golden`, `TestTodo_WEB_031_Browser`, `TestTodo_WEB_031_Conformance`, `TestTodo_WEB_031_Security`, `TestTodo_WEB_031_Integration`, and `TestTodo_WEB_031_Fault` prove a versioned and bounded browser-state contract: the only browser-persisted record is a random per-tab history high-water mark, `history.state` is closed to its ledger identifier and bounded index, and credentials, workflow authority, business records, configuration truth, and authorization-shaped fields cannot enter browser storage. A recursive AST gate pins `sessionStorage` acquisition/read/write to the reviewed adapter, forbids local storage, IndexedDB, and Cache API authority, and still scans generated/build-named Go. Browser History and storage getters and methods fail soft through reviewed `Reflect.get`/`Reflect.apply` seams, malformed or foreign current state disables navigation, and the real GWC v5 router retains closed-state push/pop compatibility. The exact matrix passed natively and as a compiled `js/wasm` binary under Node, including hostile getter/method faults; focused vet and `git diff --check` passed; the production 17-page x 3-locale i18n/accessibility gate passed. Codex in-app browser QA reloaded the production Go/WASM workspace, traversed People to My Work and back/forward, and verified stable localized destinations without blank or duplicate shells. `TestTodo_WEB_031_Latency` measured p95 0s against 2 ms and `BenchmarkHistoryLedgerIDValidation` measured 143.9 ns/op, 7 B/op, 0 allocs/op; implemented by GPT-5.6 Luna and independently adversarially hardened by GPT-5.6 Sol.

- [x] `WEB-032` **[GATE_C][SOL_HIGH] Generate the frontend asset-integrity manifest.**
  - **Depends:** `WEB-031`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=generate the frontend asset-integrity manifest without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_032`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_032`; `GOLDEN=TestTodo_WEB_032_Golden`; `BROWSER=TestTodo_WEB_032_Browser`; `CONFORMANCE=TestTodo_WEB_032_Conformance`; `SECURITY=TestTodo_WEB_032_Security`; `INTEGRATION=TestTodo_WEB_032_Integration`; `FAULT=TestTodo_WEB_032_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—generate the frontend asset-integrity manifest—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—generate the frontend asset-integrity manifest—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Go constitution](specs/go-only-technology-constitution.md).
  - **Evidence (2026-09-07):** `TestTodo_WEB_032`, `TestTodo_WEB_032_Golden`, `TestTodo_WEB_032_Browser`, `TestTodo_WEB_032_Conformance`, `TestTodo_WEB_032_Security`, `TestTodo_WEB_032_Integration`, and `TestTodo_WEB_032_Fault` prove the generated, versioned `manifest.json` exactly describes the closed routable frontend asset catalogue with SHA-256 hex and SRI for identity and optional gzip representations. Handler construction parses the bounded, canonical manifest once, indexes request metadata, and fails closed on missing, stale, extra, orphaned, reordered, malformed, oversized, empty, or byte-mutated assets; serving uses the precomputed content type, ETag and SRI with authenticated private revalidation and 304 support instead of request-time hashing. The bearer/same-origin loaders authenticate the manifest, shim and WASM requests, verify integrity before execution, and run under the reviewed CSP. Packaging shares the serving allowlist, excludes source-only assets, rejects orphan gzip files, writes deterministic gzip metadata and publishes each file through a synchronized temporary replacement. Windows cannot atomically replace the complete five-file bundle as one filesystem transaction, so startup catalogue validation is the fail-closed interruption boundary rather than a claim of bundle-wide filesystem atomicity. The exact matrix, loader, focused workspace, packager, vet, `git diff --check`, full repository hook, and the 17-page x 3-locale i18n/accessibility gate passed. A freshly restarted production Go/WASM workspace mounted in the Codex browser after real login in `de-DE`; the interaction gate measured p95 1.0004 ms against 2 ms, while the build/startup manifest benchmark measured 1.43-1.51 ms/op, 820,153 B/op, 1,033 allocs/op outside the request path. Implemented by GPT-5.6 Luna, adversarially reviewed and hardened by GPT-5.6 Sol, with the final test-aliasing correction independently verified by the orchestrator.

- [x] `WEB-033` **[GATE_C][SOL_HIGH] Enforce the production Content Security Policy.**
  - **Depends:** `WEB-032`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=enforce the production content security policy without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_033`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_033`; `GOLDEN=TestTodo_WEB_033_Golden`; `BROWSER=TestTodo_WEB_033_Browser`; `CONFORMANCE=TestTodo_WEB_033_Conformance`; `SECURITY=TestTodo_WEB_033_Security`; `INTEGRATION=TestTodo_WEB_033_Integration`; `FAULT=TestTodo_WEB_033_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—enforce the production content security policy—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—enforce the production content security policy—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Go constitution](specs/go-only-technology-constitution.md).
  - **Evidence (2026-09-07):** `TestTodo_WEB_033`, `TestTodo_WEB_033_Golden`, `TestTodo_WEB_033_Browser`, `TestTodo_WEB_033_Conformance`, `TestTodo_WEB_033_Security`, `TestTodo_WEB_033_Integration`, and `TestTodo_WEB_033_Fault` prove one typed, deterministic, fail-closed CSP builder governs product, journey, legacy workspace, login, refusal, redirect and SSR responses. The policy explicitly denies default, base, object, frame, child, attribute-script, attribute-style, font, media, worker and manifest capabilities; pins the exact loader and stylesheet hashes; retains only the SRI-verified blob shim and `wasm-unsafe-eval`; removes ambient `script-src 'self'`; and restricts `connect-src` to the canonical host's authenticated asset prefix and exact `/workspace/grpc` WS/WSS path. Malformed hosts, ports, IP spellings, hashes, stylesheet terminators, duplicate directives, oversized headers, inline handlers/styles and cached/error/redirect downgrade paths fail closed. The reusable DataTable replaced arbitrary inline widths with a closed stylesheet-backed class contract. The exact matrix, full workspace, product UI, SSR/GWC/journey, web-delivery, vet, `git diff --check`, regenerated asset catalogue, and full repository hook passed; the 17-page x 3-locale i18n/accessibility gate passed. The exact rebuilt Go/WASM bundle was served through the local gateway in the Codex browser: Rafael login, WASM mount, People software navigation and in-app back all succeeded with no browser diagnostic log entries. CSP construction measured p95 513.5 us, max 1.0278 ms against 2 ms; three-policy benchmarks measured 11.7-12.1 us/op, 10,012 B/op and 109 allocs/op. Trusted Types is not falsely claimed: GWC v5 private serialized-subtree fast paths still assign `template.innerHTML`, so enforcement remains blocked on an upstream named `TrustedHTML` policy or sink removal. Implemented by GPT-5.6 Luna, adversarially broken and hardened by GPT-5.6 Sol, and independently browser-qualified by the orchestrator.

- [x] `WEB-034` **[GATE_C][SOL_HIGH] Implement the qualified browser RPC adapter.**
  - **Depends:** `WEB-033`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=implement the qualified browser rpc adapter without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_034`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_034`; `GOLDEN=TestTodo_WEB_034_Golden`; `BROWSER=TestTodo_WEB_034_Browser`; `CONFORMANCE=TestTodo_WEB_034_Conformance`; `SECURITY=TestTodo_WEB_034_Security`; `INTEGRATION=TestTodo_WEB_034_Integration`; `FAULT=TestTodo_WEB_034_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the qualified browser rpc adapter—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the qualified browser rpc adapter—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Go constitution](specs/go-only-technology-constitution.md).
  - **Evidence (2026-09-07):** `TestTodo_WEB_034`, `TestTodo_WEB_034_Golden`, `TestTodo_WEB_034_Browser`, `TestTodo_WEB_034_Conformance`, `TestTodo_WEB_034_Security`, `TestTodo_WEB_034_Integration`, and `TestTodo_WEB_034_Fault` prove the browser RPC boundary is one bounded `grpc.ClientConnInterface` adapter over generated Journey service descriptors rather than a second API or business-authority layer. It cross-checks the complete generated and protobuf descriptor sets, enforces each method's exact generated request and response types, bounds protobuf messages and metadata, rejects authority-shaped or malformed metadata and unapproved call options, injects only a validated server-issued bearer, propagates short deadlines and cancellation, caps long calls, preserves typed gRPC status details, and releases owned stream contexts on every terminal path. Native and full client tests, focused vet, the registered 17-page x 3-locale i18n/accessibility gate, five latency-gate repetitions, and the full repository pre-commit gate passed. The actual js/wasm test binary ran through Go's Node harness, and `TestWEB034QualifiedAdapterRunsThroughRealTunnel` exercised unary and cancellable streaming calls through the real GoGRPCBridge WebSocket tunnel against embedded PostgreSQL. The exact rebuilt asset was served on the local development gateway and manually qualified in the Codex browser: authenticated Organization, Work and People routes loaded authorized live data, including 64 people, with no browser warning or error log entries. Adapter construction measured p95 526.8 us against 2 ms; invocation benchmarks measured 1.58-3.94 us/op, 970-973 B/op and 13 allocs/op. The code is present in `a42586a`; a concurrent shared-index commit grouped it with an unrelated pgxadapter optimization after the complete hook had passed, and history was deliberately not rewritten. Implemented by GPT-5.6 Luna, adversarially broken and hardened by GPT-5.6 Sol, then tunnel-, browser-, accessibility-, internationalization- and performance-qualified by the orchestrator.

- [x] `WEB-035` **[GATE_C][SOL_HIGH] Implement the filtered WebSocket invalidation client.**
  - **Depends:** `WEB-034`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=implement the filtered websocket invalidation client without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_035`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_035`; `GOLDEN=TestTodo_WEB_035_Golden`; `BROWSER=TestTodo_WEB_035_Browser`; `CONFORMANCE=TestTodo_WEB_035_Conformance`; `SECURITY=TestTodo_WEB_035_Security`; `INTEGRATION=TestTodo_WEB_035_Integration`; `FAULT=TestTodo_WEB_035_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the filtered websocket invalidation client—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the filtered websocket invalidation client—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Go constitution](specs/go-only-technology-constitution.md).
  - **Evidence (2026-09-07):** `TestTodo_WEB_035`, `TestTodo_WEB_035_Golden`, `TestTodo_WEB_035_Browser`, `TestTodo_WEB_035_Conformance`, `TestTodo_WEB_035_Security`, `TestTodo_WEB_035_Integration`, and `TestTodo_WEB_035_Fault` prove one bounded invalidation client decodes only the canonical `productquery.InvalidationMessage`, filters by tenant, projection and authorized subject, rejects malformed, foreign, stale and unknown hints without identifier leakage, and refetches authoritative data before committing sequence, watermark or revision cursors. Queue bounds, cancellation, typed-nil and non-closeable streams, exactly-once close, failed/cancelled retryability, panic-contained serialized observability and actual `syscall/js` WebSocket cleanup are covered. Native packages, focused vet, five latency repetitions, the compiled js/wasm suite under Go's Node harness, the 17-page x 3-locale i18n/accessibility gate, scoped diff checks and the full repository hook passed. Native p95 was 509.6 us and WASM p95 847.1 us against 2 ms; the benchmark measured 20.8-27.8 us/op, about 6.7 KB/op and 70 allocations. The local product shell was manually inspected in the Codex browser on Journeys and People with no warning or error log entries. Commit `f29b156` contains the componentized client, contract, processing, observability, browser adapter and qualified-RPC integration test. This closes the injectable client contract only: no canonical invalidation server endpoint, authentication/subprotocol contract or HTTP shadow currently exists, so product composition deliberately does not invent a live subscription; WEB-036 owns reconnect and cursor catch-up. Implemented by GPT-5.6 Luna, adversarially broken and hardened by GPT-5.6 Sol, and independently WASM-, accessibility-, internationalization-, performance- and browser-qualified by the orchestrator.

- [x] `WEB-036` **[GATE_C][SOL_HIGH] Implement sequence-based reconnect catch-up.**
  - **Depends:** `WEB-035`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=implement sequence-based reconnect catch-up without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_036`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_036`; `GOLDEN=TestTodo_WEB_036_Golden`; `BROWSER=TestTodo_WEB_036_Browser`; `CONFORMANCE=TestTodo_WEB_036_Conformance`; `SECURITY=TestTodo_WEB_036_Security`; `INTEGRATION=TestTodo_WEB_036_Integration`; `FAULT=TestTodo_WEB_036_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement sequence-based reconnect catch-up—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement sequence-based reconnect catch-up—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Go constitution](specs/go-only-technology-constitution.md).
  - **Evidence (2026-09-07):** `TestTodo_WEB_036`, `TestTodo_WEB_036_Golden`, `TestTodo_WEB_036_Browser`, `TestTodo_WEB_036_Conformance`, `TestTodo_WEB_036_Security`, `TestTodo_WEB_036_Integration`, and `TestTodo_WEB_036_Fault` prove bounded reconnect generations resume from the last locally committed checkpoint, detect exact sequence gaps, perform an authorized authoritative catch-up before admitting the later hint, and advance sequence, watermark, subject revisions or replacement scope only after successful catch-up/refetch. The checkpoint has private fields and read-only accessors, no serialization, and no authority semantics; it cannot be forged into a server cursor. Consecutive failed generations have a bounded exponential-backoff budget which resets only on committed progress, while clean useful generations continue without a hot EOF loop. Tests cover duplicate, out-of-order, overflow, regression, overshoot, contradictory scope, foreign and malformed messages, failed open/catch-up/refetch, panic containment, cancellation during open/backoff/receive/callback, concurrent lifecycle exclusion, exactly-once close and generation replacement. Commit `d225e01` contains the componentized reconnect policy and lifecycle integration. The exact native matrix, WEB-035 regressions, full invalidation/productclient packages, focused vet, 86.2% coverage, five repeated latency runs, scoped formatting/diff checks and full repository hook passed. A freshly compiled js/wasm binary ran both WEB-035 and WEB-036 browser tests through Go's Node runtime; the WEB-036 browser case passed in 0.00-0.04 s. Native p95 was 0-512.5 us against 2 ms; the root benchmark measured 35.2-35.6 us/op, 10,186-10,188 B/op and 112 allocations. The unchanged running product was visually regression-checked in the Codex browser and emitted no warnings or errors; i18n/accessibility output did not change. Race execution was unavailable because the Windows Go toolchain has CGO disabled, while the repository race-policy gate passed. No canonical invalidation endpoint, authentication/subprotocol contract or server-issued opaque cursor exists, so this closes the injectable reconnect/catch-up contract without inventing a live subscription or importing server signing authority into WASM. Implemented by GPT-5.6 Luna, adversarially broken and hardened by GPT-5.6 Sol, and independently native-, WASM-, performance- and browser-qualified by the orchestrator.

### Application shell and navigation

- [x] `WEB-037` **[GATE_C][TERRA] Implement the stable application shell.**
  - **Depends:** `WEB-025`, `WEB-013`, `TRUST-009`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=implement the stable application shell without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_037`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_037`; `GOLDEN=TestTodo_WEB_037_Golden`; `BROWSER=TestTodo_WEB_037_Browser`; `CONFORMANCE=TestTodo_WEB_037_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the stable application shell—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the stable application shell—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).
  - **Evidence (2026-09-07):** `TestTodo_WEB_037`, `TestTodo_WEB_037_Golden`, `TestTodo_WEB_037_Browser`, and `TestTodo_WEB_037_Conformance` prove one hydrated GoWebComponents application shell retains its banner, authorization-resolved navigation, main region, live region, DOM identities and component-local hook state while HistoryRouter replaces only the route outlet across Home, People and Settings. Startup now hydrates the server-rendered shell instead of clearing it, and both the layout and page route factories establish explicit component boundaries before hook-using renderers execute. The actual compiled js/wasm browser test exercises production route factories, preserved shell nodes and state, title and heading updates, exactly one live announcement, zero reloads and generation-safe cancellation of a stale People loader. Commit `7570f57` contains the production hydration and component-context repairs plus the exact matrix. `go test -count=1 ./internal/humanwork/productui` passed, as did actual Node/WASM execution, native and js/wasm vet, compilation, five repeated latency runs, the 17-page x 3-locale i18n/accessibility, RTL and theme gates, scoped formatting/diff checks and the full repository hook. Stable-shell navigation p95 was 2.036-9.556 ms against 16 ms; the benchmark measured 0.884-1.266 ms/op, about 744 KB/op and 3,752 allocations. Home, People and Settings were manually traversed with software navigation in the Codex browser; the shell remained visually stable and browser diagnostics contained no warnings or errors. Implemented by GPT-5.6 Luna, adversarially broken and hardened by GPT-5.6 Sol, and independently native-, WASM-, performance-, accessibility-, internationalization- and browser-qualified by the orchestrator.

- [x] `WEB-038` **[GATE_C][TERRA] Implement the tenant and acting-context switcher.**
  - **Depends:** `WEB-037`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=implement the tenant and acting-context switcher without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_038`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_038`; `GOLDEN=TestTodo_WEB_038_Golden`; `BROWSER=TestTodo_WEB_038_Browser`; `CONFORMANCE=TestTodo_WEB_038_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the tenant and acting-context switcher—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the tenant and acting-context switcher—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).
  - **Evidence (2026-09-07):** `TestTodo_WEB_038`, `TestTodo_WEB_038_Golden`, `TestTodo_WEB_038_Browser`, and `TestTodo_WEB_038_Conformance` prove that the GoWebComponents shell renders only a bounded, server-resolved catalogue of complete tenant plus acting-authority pairs, never independently composable tenant and authority choices. The switcher exposes delegated principal, delegator, expiry and elevated-state presentation without treating URL, browser storage, labels or selection values as authority. Its component props and exchange result are reflection-guarded narrow records with no reachable `View`, `*View`, page-wide record or credential; an opaque non-authoritative projection reference lets the injected adapter privately stage the replacement view and transactionally clear scoped history, storage and projection state before adoption, with rollback on commit error or panic. Same-context selection is a no-op; malformed, duplicate, oversized, foreign, partially resolved and stale responses fail closed; overlapping exchanges cancel and fence older generations. Commit `1346552` contains the implementation and exact native/WASM matrix. `go test -count=1 ./internal/humanwork/productui` passed, as did native and js/wasm vet, the architecture guard, actual compiled Node/WASM execution of WEB-037/038, the 17-page x 3-locale i18n/accessibility, RTL, theme, token, qualification and WCAG gates, scoped formatting/diff checks and the full repository hook. Five final latency runs measured p95 508.9-538.8 us against 2 ms; the benchmark measured 90.3-100.5 us/op, 41,958 B/op and 321 allocations. Desktop, 390 px and 320 px layouts were manually inspected in light and dark modes with the Codex browser; a mobile panel/search overlap found during that review was fixed and browser diagnostics were clean. The repository still has no authoritative backend context-exchange RPC, so production must inject the documented authenticated adapter; this closes the honest component and transactional browser boundary without fabricating an endpoint or client-side authority. Implemented by GPT-5.6 Luna, adversarially broken and hardened by GPT-5.6 Sol, and independently native-, WASM-, performance-, accessibility-, internationalization- and browser-qualified by the orchestrator.

- [x] `WEB-039` **[GATE_C][TERRA] Implement authorization-resolved primary navigation.**
  - **Depends:** `WEB-038`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=implement authorization-resolved primary navigation without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_039`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_039`; `GOLDEN=TestTodo_WEB_039_Golden`; `BROWSER=TestTodo_WEB_039_Browser`; `CONFORMANCE=TestTodo_WEB_039_Conformance`; `SECURITY=TestTodo_WEB_039_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement authorization-resolved primary navigation—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement authorization-resolved primary navigation—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).
  - **Evidence (2026-09-07):** `TestTodo_WEB_039`, `TestTodo_WEB_039_Golden`, `TestTodo_WEB_039_Browser`, `TestTodo_WEB_039_Conformance`, and `TestTodo_WEB_039_Security` prove that the stable GoWebComponents shell consumes a bounded, versioned authorization-resolved navigation projection and never treats route, label, browser state, roles, favorites, fuzzy search or the page registry as authority once a projection is supplied. Validation fails closed before copying hostile deep or wide graphs; exact canonical page, route, label-key and icon relationships reject foreign, traversal, query, fragment, control, bidi, duplicate and cross-page inputs. Invalid, denied and intentionally empty projections remain authoritative across locale changes, support links, global search and shell shortcuts; unauthorized favorites are removed from rendered URLs while destination RPC and page authorization remain independent enforcement boundaries. Commits `04e8ffe` and `e431b76` contain the componentized projection, navigation rendering, shell integration, English/German/Arabic copy, narrow-props architecture guard and exact native/WASM/security matrix. `go test -count=1 ./internal/humanwork/productui` PASS on windows/amd64 (Go 1.26.3); branch plan-revision-2026-09-02. Native and js/wasm vet, token/qualification/WCAG gates, a freshly compiled Node/WASM run of WEB-037/038/039, scoped formatting/diff checks and the full repository hook also passed. Five final latency runs measured p95 511.3 us-1.7641 ms against 5 ms; the benchmark measured 195.5-223.7 us/op, about 104,881 B/op and 1,262 allocations. Desktop, 390 px and 320 px layouts were manually inspected in light and dark modes with the Codex browser; the review separated authorization-empty copy from fuzzy-search misses and replaced a truncated 320 px fallback wordmark with its compact brand mark. Browser diagnostics were clean. Implemented by GPT-5.6 Luna, adversarially broken and hardened by GPT-5.6 Sol, and independently native-, WASM-, performance-, accessibility-, internationalization- and browser-qualified by the orchestrator.

- [x] `WEB-040` **[GATE_C][TERRA] Implement the global action launcher.**
  - **Evidence (2026-09-07):** `TestTodo_WEB_040`, `TestTodo_WEB_040_Golden`, `TestTodo_WEB_040_Browser`, `TestTodo_WEB_040_Conformance` in `internal/humanwork/productui` (shell `action-launcher` control offering only authorized starts, denied create hides the promotion start, navigation-only hrefs, deterministic derivation, dialog/trigger accessible names, no unresolved keys in en-US/de-DE/RTL ar; goldens `2fabcf2f…`, `a9374abc…` shell, `e2762626…` navigation); `go test -count=1 ./internal/humanwork/productui/` PASS, `go vet` clean, coverage 86.6%, `tools/uxqual/i18n` + `pagedef` PASS, `./internal/humanwork/...` PASS on windows/arm64 (Go 1.26.3); `test/workspace` has two failures (`TestTodo_UX_004_Browser`, `TestPromotionWorkspaceRendersFromTheLiveCellWithZeroEffects`) proven unrelated — the harness does not build at clean HEAD without the other session's uncommitted journey-edge work; static SSR screenshots at 1280px and 390px verified in headless Chromium (a missing trigger stylesheet rendering the control dark was found and fixed, `.artifacts/web040-desktop.png`, `.artifacts/web040-mobile.png`); live browser interaction pending (no running cell in this environment).
  - **Depends:** `WEB-039`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=implement the global action launcher without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_040`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_040`; `GOLDEN=TestTodo_WEB_040_Golden`; `BROWSER=TestTodo_WEB_040_Browser`; `CONFORMANCE=TestTodo_WEB_040_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the global action launcher—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the global action launcher—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [x] `WEB-041` **[GATE_C][TERRA] Implement authorization-safe global search.**
  - **Depends:** `WEB-040`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=implement authorization-safe global search without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_041`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_041`; `GOLDEN=TestTodo_WEB_041_Golden`; `BROWSER=TestTodo_WEB_041_Browser`; `CONFORMANCE=TestTodo_WEB_041_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement authorization-safe global search—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement authorization-safe global search—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [x] `WEB-042` **[GATE_C][TERRA] Implement the attention notification center.**
  - **Depends:** `WEB-041`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=implement the attention notification center without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_042`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_042`; `GOLDEN=TestTodo_WEB_042_Golden`; `BROWSER=TestTodo_WEB_042_Browser`; `CONFORMANCE=TestTodo_WEB_042_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the attention notification center—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the attention notification center—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [x] `WEB-043` **[GATE_C][TERRA] Implement account locale and accessibility controls.**
  - **Depends:** `WEB-042`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=implement account locale and accessibility controls without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_043`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_043`; `GOLDEN=TestTodo_WEB_043_Golden`; `BROWSER=TestTodo_WEB_043_Browser`; `CONFORMANCE=TestTodo_WEB_043_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement account locale and accessibility controls—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement account locale and accessibility controls—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [x] `WEB-044` **[GATE_C][TERRA] Implement meaningful breadcrumb resolution.**
  - **Evidence (2026-09-07):** `TestTodo_WEB_044`, `TestTodo_WEB_044_Golden`, `TestTodo_WEB_044_Browser`, `TestTodo_WEB_044_Conformance` in `internal/humanwork/productui` (registry ParentNav trail with projection-filtered ancestors, worker-naming profile crumb, nil for unknown pages, silence for single generic crumbs; nav landmark with ordered list, software-routed links, aria-current page marker, aria-hidden separators, en-US/de-DE/RTL ar labels; golden `a2817ac7…`); `go test -count=1 ./internal/humanwork/productui/` PASS, `go vet` clean, `gofmt` clean, `tools/uxqual/...` 26/26 PASS, playwright browser gate 18/20 with the 2 failures proven pre-existing on clean-HEAD fixtures; two older document-wide aria-current assertions re-scoped to the primary-nav landmark (Studio, person) with breadcrumb-marker assertions added.
  - **Depends:** `WEB-043`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=implement meaningful breadcrumb resolution without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_044`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_044`; `GOLDEN=TestTodo_WEB_044_Golden`; `BROWSER=TestTodo_WEB_044_Browser`; `CONFORMANCE=TestTodo_WEB_044_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement meaningful breadcrumb resolution—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement meaningful breadcrumb resolution—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [x] `WEB-045` **[GATE_C][TERRA] Implement the canonical page-identity header.**
  - **Evidence (2026-09-07):** `TestTodo_WEB_045`, `TestTodo_WEB_045_Golden`, `TestTodo_WEB_045_Browser`, `TestTodo_WEB_045_Conformance` in `internal/humanwork/productui` (governed identity resolution per page/role/locale, withheld settings destination under a denying permissions projection, Home greeting, unknown-page Home fallback, profile keeps registry H1; `data-hcm-page` stamp, labelledby wiring, golden `0016c935…`); `go test -count=1 ./internal/humanwork/productui/` PASS, `go vet`/`gofmt` clean, browser gate 18/20 with the same 2 pre-existing failures; WEB-037/WEB-039 goldens re-pinned after strip-proof of attribute-only delta; route-adapter markup rule kept green via `header_identity.go` naming.
  - **Depends:** `WEB-044`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=implement the canonical page-identity header without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_045`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_045`; `GOLDEN=TestTodo_WEB_045_Golden`; `BROWSER=TestTodo_WEB_045_Browser`; `CONFORMANCE=TestTodo_WEB_045_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the canonical page-identity header—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the canonical page-identity header—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [x] `WEB-046` **[GATE_C][TERRA] Implement the contextual utility drawer.**
  - **Evidence (2026-09-07):** `TestTodo_WEB_046`, `TestTodo_WEB_046_Golden`, `TestTodo_WEB_046_Browser`, `TestTodo_WEB_046_Conformance` in `internal/humanwork/productui` (registry-derived Related plus grant-gated Actions sections, denied/empty/unknown contexts yield nothing, deterministic derivation, dialog semantics with hidden-until-open dialog, registry-coupled in-shell links, de-DE/RTL ar labels, typed stylesheet selectors, golden `0f4db16a…`); `go test -count=1 ./internal/humanwork/productui/` PASS with no golden fallout, `go vet`/`gofmt` clean, browser gate 18/20 with the same 2 pre-existing failures.
  - **Depends:** `WEB-045`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=implement the contextual utility drawer without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_046`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_046`; `GOLDEN=TestTodo_WEB_046_Golden`; `BROWSER=TestTodo_WEB_046_Browser`; `CONFORMANCE=TestTodo_WEB_046_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the contextual utility drawer—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the contextual utility drawer—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [x] `WEB-047` **[GATE_C][TERRA] Implement the responsive mobile shell.**
  - **Evidence (2026-09-07):** `TestTodo_WEB_047`, `TestTodo_WEB_047_Golden`, `TestTodo_WEB_047_Browser`, `TestTodo_WEB_047_Conformance` in `internal/humanwork/productui` (collapsible trigger-label spans with intact accessible names, canonical 430px collapse rules, exactly 2 logical-only narrow blocks, viewport-bound dialogs, golden `96b75252…`); `go test -count=1 ./internal/humanwork/productui/` PASS, `go vet`/`gofmt` clean, browser gate 18/20 with the same 2 pre-existing failures; WEB-037/WEB-039/WEB-040/WEB-046 goldens re-pinned after strip-proofs of span-wrap-only deltas.
  - **Depends:** `WEB-046`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=implement the responsive mobile shell without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_047`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_047`; `GOLDEN=TestTodo_WEB_047_Golden`; `BROWSER=TestTodo_WEB_047_Browser`; `CONFORMANCE=TestTodo_WEB_047_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the responsive mobile shell—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the responsive mobile shell—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [x] `WEB-048` **[GATE_C][TERRA] Prove shell keyboard and landmark navigation.**
  - **Evidence (2026-09-07):** `TestTodo_WEB_048`, `TestTodo_WEB_048_Golden`, `TestTodo_WEB_048_Browser`, `TestTodo_WEB_048_Conformance` in `internal/humanwork/productui` (named-landmark inventory per page/projection/locale, document-existence proof per landmark, skip-link wiring, shared Escape predicate, golden `1d58b14d…`) plus new `tools/uxqual/browser/web048_keyboard_landmarks.spec.mjs` 6/6 in real Chromium (DOM-order tabs, landmark roles, 390px reflow); `go test -count=1 ./internal/humanwork/productui/` PASS, `go vet`/`gofmt` clean, full browser gate 24 passed with the same 2 pre-existing ux003 failures.
  - **Depends:** `WEB-047`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE; DIRECT=none; WHY=prove shell keyboard and landmark navigation without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_048`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_048`; `GOLDEN=TestTodo_WEB_048_Golden`; `BROWSER=TestTodo_WEB_048_Browser`; `CONFORMANCE=TestTodo_WEB_048_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—prove shell keyboard and landmark navigation—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—prove shell keyboard and landmark navigation—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

### Authentication session and delegated context

- [x] `WEB-049` **[GATE_C][SOL_HIGH] Implement the tenant-federation entry experience.**
  - **Evidence (2026-09-07):** `TestTodo_WEB_049`, `TestTodo_WEB_049_Golden`, `TestTodo_WEB_049_Browser`, `TestTodo_WEB_049_Conformance` in `internal/humanwork/productui` (tenantless gate with grouped issuer links and in-workspace hrefs, tenant chrome suppressed, honest empty state, tenanted views unaffected, deterministic grouping, de-DE/RTL ar titles, typed stylesheet selectors, golden `b5f9cf65…`); `go test -count=1 ./internal/humanwork/productui/` PASS with no fallout, `go vet`/`gofmt` clean, browser gate 24 passed with the same 2 pre-existing ux003 failures.
  - **Depends:** `WEB-037`, `AUTHN-001`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.SECURITY,BI.EXPERIENCE; DIRECT=none; WHY=implement the tenant-federation entry experience without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_049`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_049`; `GOLDEN=TestTodo_WEB_049_Golden`; `BROWSER=TestTodo_WEB_049_Browser`; `CONFORMANCE=TestTodo_WEB_049_Conformance`; `SECURITY=TestTodo_WEB_049_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the tenant-federation entry experience—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the tenant-federation entry experience—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [authorization model](specs/organization-scope-and-authz.md).

- [x] `WEB-050` **[GATE_C][SOL_HIGH] Implement accessible sign-in recovery.**
  - **Evidence (2026-09-07):** `TestTodo_WEB_050`, `TestTodo_WEB_050_Golden`, `TestTodo_WEB_050_Browser`, `TestTodo_WEB_050_Conformance` in `internal/humanwork/productui` (labelled recovery section with discernible link text, javascript:/data:/network-relative/plain-http/empty dropped with no section when nothing safe, gate placement after title, de-DE/RTL ar titles, 9-scheme policy table, typed stylesheet selectors, golden `8e43b2bc…`); `go test -count=1 ./internal/humanwork/productui/` PASS with no fallout, `go vet`/`gofmt` clean, browser gate 24 passed with the same 2 pre-existing ux003 failures.
  - **Depends:** `WEB-049`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.SECURITY,BI.EXPERIENCE; DIRECT=none; WHY=implement accessible sign-in recovery without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_050`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_050`; `GOLDEN=TestTodo_WEB_050_Golden`; `BROWSER=TestTodo_WEB_050_Browser`; `CONFORMANCE=TestTodo_WEB_050_Conformance`; `SECURITY=TestTodo_WEB_050_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement accessible sign-in recovery—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement accessible sign-in recovery—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [authorization model](specs/organization-scope-and-authz.md).

- [x] `WEB-051` **[GATE_C][SOL_HIGH] Prove accessible-authentication conformance.**
  - **Evidence (2026-09-07):** `TestTodo_WEB_051`, `TestTodo_WEB_051_Golden`, `TestTodo_WEB_051_Browser`, `TestTodo_WEB_051_Conformance` in `internal/humanwork/productui` (3-state × 3-locale sweep: 8 conformance properties green with zero findings, gate-document shell honesty, shared scheme policy, matrix digest `6380d315…`); `go test -count=1 ./internal/humanwork/productui/` PASS with no fallout, `go vet`/`gofmt` clean, browser gate 24 passed with the same 2 pre-existing ux003 failures. Proof-only: no production change.
  - **Depends:** `WEB-050`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.SECURITY,BI.EXPERIENCE; DIRECT=none; WHY=prove accessible-authentication conformance without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_051`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_051`; `GOLDEN=TestTodo_WEB_051_Golden`; `BROWSER=TestTodo_WEB_051_Browser`; `CONFORMANCE=TestTodo_WEB_051_Conformance`; `SECURITY=TestTodo_WEB_051_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—prove accessible-authentication conformance—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—prove accessible-authentication conformance—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [authorization model](specs/organization-scope-and-authz.md).

- [x] `WEB-052` **[GATE_C][SOL_HIGH] Implement the session-expiry warning.**
  - **Evidence (2026-09-07):** `TestTodo_WEB_052`, `TestTodo_WEB_052_Golden`, `TestTodo_WEB_052_Browser`, `TestTodo_WEB_052_Conformance` in `internal/humanwork/productui` (labelled banner with server detail, re-auth link, labelled dismiss, fragment without projection, topbar-to-content placement, de-DE/RTL ar titles, typed stylesheet selectors, golden `93253689…`); `go test -count=1 ./internal/humanwork/productui/` PASS with no fallout, `go vet`/`gofmt` clean, browser gate 24 passed with the same 2 pre-existing ux003 failures.
  - **Depends:** `WEB-051`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.SECURITY,BI.EXPERIENCE; DIRECT=none; WHY=implement the session-expiry warning without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_052`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_052`; `GOLDEN=TestTodo_WEB_052_Golden`; `BROWSER=TestTodo_WEB_052_Browser`; `CONFORMANCE=TestTodo_WEB_052_Conformance`; `SECURITY=TestTodo_WEB_052_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the session-expiry warning—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the session-expiry warning—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [authorization model](specs/organization-scope-and-authz.md).

- [x] `WEB-053` **[GATE_C][SOL_HIGH] Implement reauthentication work restoration.**
  - **Evidence (2026-09-07):** `TestTodo_WEB_053`, `TestTodo_WEB_053_Golden`, `TestTodo_WEB_053_Browser`, `TestTodo_WEB_053_Conformance` in `internal/humanwork/productui` (resume target on the re-auth link from the shell address primitive, destination query preserved, unsafe/unparsable bases pass through, registry-coupled resume paths for every page); `go test -count=1 ./internal/humanwork/productui/` PASS, `go vet`/`gofmt` clean, browser gate 24 passed with the same 2 pre-existing ux003 failures. WEB-052's exact-href assertion evolved to parse the destination path (intended behavior change).
  - **Depends:** `WEB-052`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.SECURITY,BI.EXPERIENCE; DIRECT=none; WHY=implement reauthentication work restoration without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_053`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_053`; `GOLDEN=TestTodo_WEB_053_Golden`; `BROWSER=TestTodo_WEB_053_Browser`; `CONFORMANCE=TestTodo_WEB_053_Conformance`; `SECURITY=TestTodo_WEB_053_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement reauthentication work restoration—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement reauthentication work restoration—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [authorization model](specs/organization-scope-and-authz.md).

- [x] `WEB-054` **[GATE_C][SOL_HIGH] Implement risk-bound step-up authentication.**
  - **Evidence (2026-09-07):** `TestTodo_WEB_054`, `TestTodo_WEB_054_Golden`, `TestTodo_WEB_054_Browser`, `TestTodo_WEB_054_Conformance` in `internal/humanwork/productui` (action-bound prompt with server reason and policy-gated challenge link, labelled dismiss, fragment without projection, warning-then-prompt-then-content ordering, de-DE/RTL ar titles, typed stylesheet selectors, golden `24c5c9f1…`); `go test -count=1 ./internal/humanwork/productui/` PASS with no fallout, `go vet`/`gofmt` clean, browser gate 24 passed with the same 2 pre-existing ux003 failures.
  - **Depends:** `WEB-053`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.SECURITY,BI.EXPERIENCE; DIRECT=none; WHY=implement risk-bound step-up authentication without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_054`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_054`; `GOLDEN=TestTodo_WEB_054_Golden`; `BROWSER=TestTodo_WEB_054_Browser`; `CONFORMANCE=TestTodo_WEB_054_Conformance`; `SECURITY=TestTodo_WEB_054_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement risk-bound step-up authentication—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement risk-bound step-up authentication—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [authorization model](specs/organization-scope-and-authz.md).

- [x] `WEB-055` **[GATE_C][SOL_HIGH] Implement the delegation-context selector.**
  - **Evidence (2026-09-07):** `TestTodo_WEB_055`, `TestTodo_WEB_055_Golden`, `TestTodo_WEB_055_Browser`, `TestTodo_WEB_055_Conformance` in `internal/humanwork/productui` (selectable delegated grants with delegator/expiry/elevation, current and malformed grants excluded, shared exchange contract, no-opaque-ids DOM, topbar placement, de-DE/RTL ar labels, typed stylesheet selectors, golden `fec579a1…`); `go test -count=1 ./internal/humanwork/productui/` PASS (native Linux Go 1.26.3) with no fallout, `go vet`/`gofmt` clean, fixtures regenerated byte-identical. Browser gate deferred (no launchable Chromium on either side) — recorded in devlog §17.
  - **Depends:** `WEB-054`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.SECURITY,BI.EXPERIENCE; DIRECT=none; WHY=implement the delegation-context selector without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_055`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_055`; `GOLDEN=TestTodo_WEB_055_Golden`; `BROWSER=TestTodo_WEB_055_Browser`; `CONFORMANCE=TestTodo_WEB_055_Conformance`; `SECURITY=TestTodo_WEB_055_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the delegation-context selector—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the delegation-context selector—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [authorization model](specs/organization-scope-and-authz.md).

- [x] `WEB-056` **[GATE_C][SOL_HIGH] Implement the persistent acting-authority banner.**
  - **Depends:** `WEB-055`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.SECURITY,BI.EXPERIENCE; DIRECT=none; WHY=implement the persistent acting-authority banner without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_056`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_056`; `GOLDEN=TestTodo_WEB_056_Golden`; `BROWSER=TestTodo_WEB_056_Browser`; `CONFORMANCE=TestTodo_WEB_056_Conformance`; `SECURITY=TestTodo_WEB_056_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the persistent acting-authority banner—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the persistent acting-authority banner—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [authorization model](specs/organization-scope-and-authz.md).

- [x] `WEB-057` **[GATE_C][SOL_HIGH] Implement the break-glass activation experience.**
  - **Depends:** `WEB-056`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.SECURITY,BI.EXPERIENCE; DIRECT=none; WHY=implement the break-glass activation experience without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_057`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_057`; `GOLDEN=TestTodo_WEB_057_Golden`; `BROWSER=TestTodo_WEB_057_Browser`; `CONFORMANCE=TestTodo_WEB_057_Conformance`; `SECURITY=TestTodo_WEB_057_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the break-glass activation experience—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the break-glass activation experience—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [authorization model](specs/organization-scope-and-authz.md).

- [x] `WEB-058` **[GATE_C][SOL_HIGH] Implement safe view-as policy simulation.**
  - **Depends:** `WEB-057`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.SECURITY,BI.EXPERIENCE; DIRECT=none; WHY=implement safe view-as policy simulation without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_058`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_058`; `GOLDEN=TestTodo_WEB_058_Golden`; `BROWSER=TestTodo_WEB_058_Browser`; `CONFORMANCE=TestTodo_WEB_058_Conformance`; `SECURITY=TestTodo_WEB_058_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement safe view-as policy simulation—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement safe view-as policy simulation—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [authorization model](specs/organization-scope-and-authz.md).

- [x] `WEB-059` **[GATE_C][SOL_HIGH] Implement logout and revocation convergence.**
  - **Depends:** `WEB-058`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.SECURITY,BI.EXPERIENCE; DIRECT=none; WHY=implement logout and revocation convergence without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_059`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_059`; `GOLDEN=TestTodo_WEB_059_Golden`; `BROWSER=TestTodo_WEB_059_Browser`; `CONFORMANCE=TestTodo_WEB_059_Conformance`; `SECURITY=TestTodo_WEB_059_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement logout and revocation convergence—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement logout and revocation convergence—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [authorization model](specs/organization-scope-and-authz.md).

- [x] `WEB-060` **[GATE_C][SOL_HIGH] Enforce authentication telemetry privacy.**
  - **Depends:** `WEB-059`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.SECURITY,BI.EXPERIENCE; DIRECT=none; WHY=enforce authentication telemetry privacy without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_060`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_060`; `GOLDEN=TestTodo_WEB_060_Golden`; `BROWSER=TestTodo_WEB_060_Browser`; `CONFORMANCE=TestTodo_WEB_060_Conformance`; `SECURITY=TestTodo_WEB_060_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—enforce authentication telemetry privacy—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—enforce authentication telemetry privacy—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [authorization model](specs/organization-scope-and-authz.md).

### Authorization and visibility projection

- [x] `WEB-061` **[GATE_C][SOL_HIGH] Implement the authorized presentation projection.**
  - **Depends:** `WEB-049`, `TRUST-009`, `TRUST-010`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.SECURITY,BI.PRIVACY,BI.EXPERIENCE; DIRECT=none; WHY=implement the authorized presentation projection without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_061`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_061`; `GOLDEN=TestTodo_WEB_061_Golden`; `BROWSER=TestTodo_WEB_061_Browser`; `CONFORMANCE=TestTodo_WEB_061_Conformance`; `SECURITY=TestTodo_WEB_061_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the authorized presentation projection—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the authorized presentation projection—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [authorization model](specs/organization-scope-and-authz.md).

- [x] `WEB-062` **[GATE_C][SOL_HIGH] Enforce resource discoverability decisions.**
  - **Depends:** `WEB-061`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.SECURITY,BI.PRIVACY,BI.EXPERIENCE; DIRECT=none; WHY=enforce resource discoverability decisions without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_062`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_062`; `GOLDEN=TestTodo_WEB_062_Golden`; `BROWSER=TestTodo_WEB_062_Browser`; `CONFORMANCE=TestTodo_WEB_062_Conformance`; `SECURITY=TestTodo_WEB_062_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—enforce resource discoverability decisions—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—enforce resource discoverability decisions—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [authorization model](specs/organization-scope-and-authz.md).

- [x] `WEB-063` **[GATE_C][SOL_HIGH] Enforce record-level page authorization.**
  - **Depends:** `WEB-062`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.SECURITY,BI.PRIVACY,BI.EXPERIENCE; DIRECT=none; WHY=enforce record-level page authorization without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_063`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_063`; `GOLDEN=TestTodo_WEB_063_Golden`; `BROWSER=TestTodo_WEB_063_Browser`; `CONFORMANCE=TestTodo_WEB_063_Conformance`; `SECURITY=TestTodo_WEB_063_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—enforce record-level page authorization—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—enforce record-level page authorization—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [authorization model](specs/organization-scope-and-authz.md).

- [x] `WEB-064` **[GATE_C][SOL_HIGH] Resolve current and proposed organization scope.**
  - **Depends:** `WEB-063`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.SECURITY,BI.PRIVACY,BI.EXPERIENCE; DIRECT=none; WHY=resolve current and proposed organization scope without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_064`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_064`; `GOLDEN=TestTodo_WEB_064_Golden`; `BROWSER=TestTodo_WEB_064_Browser`; `CONFORMANCE=TestTodo_WEB_064_Conformance`; `SECURITY=TestTodo_WEB_064_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—resolve current and proposed organization scope—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—resolve current and proposed organization scope—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [authorization model](specs/organization-scope-and-authz.md).

- [x] `WEB-065` **[GATE_C][SOL_HIGH] Enforce data-domain presentation scope.**
  - **Depends:** `WEB-064`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.SECURITY,BI.PRIVACY,BI.EXPERIENCE; DIRECT=none; WHY=enforce data-domain presentation scope without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_065`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_065`; `GOLDEN=TestTodo_WEB_065_Golden`; `BROWSER=TestTodo_WEB_065_Browser`; `CONFORMANCE=TestTodo_WEB_065_Conformance`; `SECURITY=TestTodo_WEB_065_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—enforce data-domain presentation scope—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—enforce data-domain presentation scope—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [authorization model](specs/organization-scope-and-authz.md).

- [x] `WEB-066` **[GATE_C][SOL_HIGH] Implement all field-disposition renderings.**
  - **Depends:** `WEB-065`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.SECURITY,BI.PRIVACY,BI.EXPERIENCE; DIRECT=none; WHY=implement all field-disposition renderings without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_066`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_066`; `GOLDEN=TestTodo_WEB_066_Golden`; `BROWSER=TestTodo_WEB_066_Browser`; `CONFORMANCE=TestTodo_WEB_066_Conformance`; `SECURITY=TestTodo_WEB_066_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement all field-disposition renderings—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement all field-disposition renderings—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [authorization model](specs/organization-scope-and-authz.md).

- [x] `WEB-067` **[GATE_C][SOL_HIGH] Enforce population-query presentation limits.**
  - **Depends:** `WEB-066`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.SECURITY,BI.PRIVACY,BI.EXPERIENCE; DIRECT=none; WHY=enforce population-query presentation limits without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_067`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_067`; `GOLDEN=TestTodo_WEB_067_Golden`; `BROWSER=TestTodo_WEB_067_Browser`; `CONFORMANCE=TestTodo_WEB_067_Conformance`; `SECURITY=TestTodo_WEB_067_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—enforce population-query presentation limits—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—enforce population-query presentation limits—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [authorization model](specs/organization-scope-and-authz.md).

- [x] `WEB-068` **[GATE_C][SOL_HIGH] Implement semantic action-availability states.**
  - **Depends:** `WEB-067`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.SECURITY,BI.PRIVACY,BI.EXPERIENCE; DIRECT=none; WHY=implement semantic action-availability states without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_068`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_068`; `GOLDEN=TestTodo_WEB_068_Golden`; `BROWSER=TestTodo_WEB_068_Browser`; `CONFORMANCE=TestTodo_WEB_068_Conformance`; `SECURITY=TestTodo_WEB_068_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement semantic action-availability states—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement semantic action-availability states—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [authorization model](specs/organization-scope-and-authz.md).

- [x] `WEB-069` **[GATE_C][SOL_HIGH] Enforce export and artifact visibility.**
  - **Depends:** `WEB-068`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.SECURITY,BI.PRIVACY,BI.EXPERIENCE; DIRECT=none; WHY=enforce export and artifact visibility without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_069`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_069`; `GOLDEN=TestTodo_WEB_069_Golden`; `BROWSER=TestTodo_WEB_069_Browser`; `CONFORMANCE=TestTodo_WEB_069_Conformance`; `SECURITY=TestTodo_WEB_069_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—enforce export and artifact visibility—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—enforce export and artifact visibility—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [authorization model](specs/organization-scope-and-authz.md).

- [x] `WEB-070` **[GATE_C][SOL_HIGH] Filter real-time events by current authority.**
  - **Depends:** `WEB-069`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.SECURITY,BI.PRIVACY,BI.EXPERIENCE; DIRECT=none; WHY=filter real-time events by current authority without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_070`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_070`; `GOLDEN=TestTodo_WEB_070_Golden`; `BROWSER=TestTodo_WEB_070_Browser`; `CONFORMANCE=TestTodo_WEB_070_Conformance`; `SECURITY=TestTodo_WEB_070_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—filter real-time events by current authority—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—filter real-time events by current authority—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [authorization model](specs/organization-scope-and-authz.md).

- [x] `WEB-071` **[GATE_C][SOL_HIGH] Invalidate presentation caches on authority drift.**
  - **Depends:** `WEB-070`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.SECURITY,BI.PRIVACY,BI.EXPERIENCE; DIRECT=none; WHY=invalidate presentation caches on authority drift without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_071`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_071`; `GOLDEN=TestTodo_WEB_071_Golden`; `BROWSER=TestTodo_WEB_071_Browser`; `CONFORMANCE=TestTodo_WEB_071_Conformance`; `SECURITY=TestTodo_WEB_071_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—invalidate presentation caches on authority drift—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—invalidate presentation caches on authority drift—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [authorization model](specs/organization-scope-and-authz.md).

- [x] `WEB-072` **[GATE_C][SOL_HIGH] Prove cross-surface authorization noninterference.**
  - **Depends:** `WEB-071`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.SECURITY,BI.PRIVACY,BI.EXPERIENCE; DIRECT=none; WHY=prove cross-surface authorization noninterference without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_072`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_072`; `GOLDEN=TestTodo_WEB_072_Golden`; `BROWSER=TestTodo_WEB_072_Browser`; `CONFORMANCE=TestTodo_WEB_072_Conformance`; `SECURITY=TestTodo_WEB_072_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—prove cross-surface authorization noninterference—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—prove cross-surface authorization noninterference—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [authorization model](specs/organization-scope-and-authz.md).

### Governed customer-page platform

- [x] `WEB-073` **[GATE_C][SOL_HIGH] Persist immutable PageDefinition revisions.**
  - **Depends:** `WEB-002`, `CONFIG-001`, `WEB-061`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.TENANT; DIRECT=none; WHY=persist immutable pagedefinition revisions without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_073`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_073`; `GOLDEN=TestTodo_WEB_073_Golden`; `BROWSER=TestTodo_WEB_073_Browser`; `CONFORMANCE=TestTodo_WEB_073_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—persist immutable pagedefinition revisions—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—persist immutable pagedefinition revisions—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [x] `WEB-074` **[GATE_C][SOL_HIGH] Implement the page draft lifecycle.**
  - **Depends:** `WEB-073`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.TENANT; DIRECT=none; WHY=implement the page draft lifecycle without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_074`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_074`; `GOLDEN=TestTodo_WEB_074_Golden`; `BROWSER=TestTodo_WEB_074_Browser`; `CONFORMANCE=TestTodo_WEB_074_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the page draft lifecycle—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the page draft lifecycle—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [x] `WEB-075` **[GATE_C][SOL_HIGH] Validate floorplan compatibility.**
  - **Depends:** `WEB-074`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.TENANT; DIRECT=none; WHY=validate floorplan compatibility without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_075`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_075`; `GOLDEN=TestTodo_WEB_075_Golden`; `BROWSER=TestTodo_WEB_075_Browser`; `CONFORMANCE=TestTodo_WEB_075_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—validate floorplan compatibility—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—validate floorplan compatibility—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [ ] `WEB-076` **[GATE_C][SOL_HIGH] Validate semantic-region composition.**
  - **Depends:** `WEB-075`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.TENANT; DIRECT=none; WHY=validate semantic-region composition without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_076`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_076`; `GOLDEN=TestTodo_WEB_076_Golden`; `BROWSER=TestTodo_WEB_076_Browser`; `CONFORMANCE=TestTodo_WEB_076_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—validate semantic-region composition—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—validate semantic-region composition—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [ ] `WEB-077` **[GATE_C][SOL_HIGH] Validate typed widget bindings.**
  - **Depends:** `WEB-076`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.TENANT; DIRECT=none; WHY=validate typed widget bindings without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_077`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_077`; `GOLDEN=TestTodo_WEB_077_Golden`; `BROWSER=TestTodo_WEB_077_Browser`; `CONFORMANCE=TestTodo_WEB_077_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—validate typed widget bindings—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—validate typed widget bindings—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [ ] `WEB-078` **[GATE_C][SOL_HIGH] Validate semantic action bindings.**
  - **Depends:** `WEB-077`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.TENANT; DIRECT=none; WHY=validate semantic action bindings without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_078`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_078`; `GOLDEN=TestTodo_WEB_078_Golden`; `BROWSER=TestTodo_WEB_078_Browser`; `CONFORMANCE=TestTodo_WEB_078_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—validate semantic action bindings—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—validate semantic action bindings—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [ ] `WEB-079` **[GATE_C][SOL_HIGH] Enforce page classification ceilings.**
  - **Depends:** `WEB-078`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.TENANT; DIRECT=none; WHY=enforce page classification ceilings without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_079`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_079`; `GOLDEN=TestTodo_WEB_079_Golden`; `BROWSER=TestTodo_WEB_079_Browser`; `CONFORMANCE=TestTodo_WEB_079_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—enforce page classification ceilings—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—enforce page classification ceilings—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [ ] `WEB-080` **[GATE_C][SOL_HIGH] Resolve page configuration precedence.**
  - **Depends:** `WEB-079`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.TENANT; DIRECT=none; WHY=resolve page configuration precedence without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_080`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_080`; `GOLDEN=TestTodo_WEB_080_Golden`; `BROWSER=TestTodo_WEB_080_Browser`; `CONFORMANCE=TestTodo_WEB_080_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—resolve page configuration precedence—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—resolve page configuration precedence—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [ ] `WEB-081` **[GATE_C][SOL_HIGH] Migrate page definitions across widget versions.**
  - **Depends:** `WEB-080`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.TENANT; DIRECT=none; WHY=migrate page definitions across widget versions without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_081`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_081`; `GOLDEN=TestTodo_WEB_081_Golden`; `BROWSER=TestTodo_WEB_081_Browser`; `CONFORMANCE=TestTodo_WEB_081_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—migrate page definitions across widget versions—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—migrate page definitions across widget versions—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [ ] `WEB-082` **[GATE_C][SOL_HIGH] Implement scoped page publication rollout.**
  - **Depends:** `WEB-081`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.TENANT; DIRECT=none; WHY=implement scoped page publication rollout without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_082`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_082`; `GOLDEN=TestTodo_WEB_082_Golden`; `BROWSER=TestTodo_WEB_082_Browser`; `CONFORMANCE=TestTodo_WEB_082_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement scoped page publication rollout—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement scoped page publication rollout—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [ ] `WEB-083` **[GATE_C][SOL_HIGH] Implement page rollback and retirement.**
  - **Depends:** `WEB-082`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.TENANT; DIRECT=none; WHY=implement page rollback and retirement without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_083`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_083`; `GOLDEN=TestTodo_WEB_083_Golden`; `BROWSER=TestTodo_WEB_083_Browser`; `CONFORMANCE=TestTodo_WEB_083_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement page rollback and retirement—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement page rollback and retirement—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [ ] `WEB-084` **[GATE_C][SOL_HIGH] Report page dependency impact.**
  - **Depends:** `WEB-083`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.TENANT; DIRECT=none; WHY=report page dependency impact without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_084`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_084`; `GOLDEN=TestTodo_WEB_084_Golden`; `BROWSER=TestTodo_WEB_084_Browser`; `CONFORMANCE=TestTodo_WEB_084_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—report page dependency impact—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—report page dependency impact—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

### Experience Studio

- [ ] `WEB-085` **[PHASE_2][TERRA] Implement the page-definition inventory.**
  - **Depends:** `WEB-073`, `WEB-061`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.TENANT; DIRECT=none; WHY=implement the page-definition inventory without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_085`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_085`; `GOLDEN=TestTodo_WEB_085_Golden`; `BROWSER=TestTodo_WEB_085_Browser`; `CONFORMANCE=TestTodo_WEB_085_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the page-definition inventory—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the page-definition inventory—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [ ] `WEB-086` **[PHASE_2][TERRA] Implement new-page purpose and audience setup.**
  - **Depends:** `WEB-085`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.TENANT; DIRECT=none; WHY=implement new-page purpose and audience setup without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_086`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_086`; `GOLDEN=TestTodo_WEB_086_Golden`; `BROWSER=TestTodo_WEB_086_Browser`; `CONFORMANCE=TestTodo_WEB_086_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement new-page purpose and audience setup—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement new-page purpose and audience setup—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [ ] `WEB-087` **[PHASE_2][TERRA] Implement the governed floorplan chooser.**
  - **Depends:** `WEB-086`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.TENANT; DIRECT=none; WHY=implement the governed floorplan chooser without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_087`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_087`; `GOLDEN=TestTodo_WEB_087_Golden`; `BROWSER=TestTodo_WEB_087_Browser`; `CONFORMANCE=TestTodo_WEB_087_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the governed floorplan chooser—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the governed floorplan chooser—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [ ] `WEB-088` **[PHASE_2][TERRA] Implement the semantic outline editor.**
  - **Depends:** `WEB-087`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.TENANT; DIRECT=none; WHY=implement the semantic outline editor without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_088`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_088`; `GOLDEN=TestTodo_WEB_088_Golden`; `BROWSER=TestTodo_WEB_088_Browser`; `CONFORMANCE=TestTodo_WEB_088_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the semantic outline editor—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the semantic outline editor—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [ ] `WEB-089` **[PHASE_2][TERRA] Implement keyboard page-region reordering.**
  - **Depends:** `WEB-088`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.TENANT; DIRECT=none; WHY=implement keyboard page-region reordering without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_089`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_089`; `GOLDEN=TestTodo_WEB_089_Golden`; `BROWSER=TestTodo_WEB_089_Browser`; `CONFORMANCE=TestTodo_WEB_089_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement keyboard page-region reordering—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement keyboard page-region reordering—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [ ] `WEB-090` **[PHASE_2][TERRA] Implement the permitted widget catalog.**
  - **Depends:** `WEB-089`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.TENANT; DIRECT=none; WHY=implement the permitted widget catalog without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_090`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_090`; `GOLDEN=TestTodo_WEB_090_Golden`; `BROWSER=TestTodo_WEB_090_Browser`; `CONFORMANCE=TestTodo_WEB_090_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the permitted widget catalog—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the permitted widget catalog—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [ ] `WEB-091` **[PHASE_2][TERRA] Implement the authorized binding inspector.**
  - **Depends:** `WEB-090`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.TENANT; DIRECT=none; WHY=implement the authorized binding inspector without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_091`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_091`; `GOLDEN=TestTodo_WEB_091_Golden`; `BROWSER=TestTodo_WEB_091_Browser`; `CONFORMANCE=TestTodo_WEB_091_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the authorized binding inspector—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the authorized binding inspector—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [ ] `WEB-092` **[PHASE_2][TERRA] Implement the sanitized content editor.**
  - **Depends:** `WEB-091`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.TENANT; DIRECT=none; WHY=implement the sanitized content editor without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_092`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_092`; `GOLDEN=TestTodo_WEB_092_Golden`; `BROWSER=TestTodo_WEB_092_Browser`; `CONFORMANCE=TestTodo_WEB_092_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the sanitized content editor—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the sanitized content editor—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [ ] `WEB-093` **[PHASE_2][TERRA] Implement multidimensional page preview.**
  - **Depends:** `WEB-092`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.TENANT; DIRECT=none; WHY=implement multidimensional page preview without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_093`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_093`; `GOLDEN=TestTodo_WEB_093_Golden`; `BROWSER=TestTodo_WEB_093_Browser`; `CONFORMANCE=TestTodo_WEB_093_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement multidimensional page preview—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement multidimensional page preview—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [ ] `WEB-094` **[PHASE_2][TERRA] Implement the page-validation findings panel.**
  - **Depends:** `WEB-093`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.TENANT; DIRECT=none; WHY=implement the page-validation findings panel without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_094`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_094`; `GOLDEN=TestTodo_WEB_094_Golden`; `BROWSER=TestTodo_WEB_094_Browser`; `CONFORMANCE=TestTodo_WEB_094_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the page-validation findings panel—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the page-validation findings panel—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [ ] `WEB-095` **[PHASE_2][TERRA] Implement semantic page diff review.**
  - **Depends:** `WEB-094`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.TENANT; DIRECT=none; WHY=implement semantic page diff review without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_095`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_095`; `GOLDEN=TestTodo_WEB_095_Golden`; `BROWSER=TestTodo_WEB_095_Browser`; `CONFORMANCE=TestTodo_WEB_095_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement semantic page diff review—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement semantic page diff review—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

- [ ] `WEB-096` **[PHASE_2][TERRA] Implement governed page publication controls.**
  - **Depends:** `WEB-095`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.TENANT; DIRECT=none; WHY=implement governed page publication controls without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_096`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_096`; `GOLDEN=TestTodo_WEB_096_Golden`; `BROWSER=TestTodo_WEB_096_Browser`; `CONFORMANCE=TestTodo_WEB_096_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement governed page publication controls—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement governed page publication controls—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [experience contract](specs/experience-ui-and-branding.md).

### Home and My Work

- [ ] `WEB-097` **[GATE_C][TERRA] Implement the authorization-resolved Home floorplan.**
  - **Depends:** `WEB-037`, `WEB-061`, `WORK-001`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.WORK; DIRECT=none; WHY=implement the authorization-resolved home floorplan without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_097`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_097`; `GOLDEN=TestTodo_WEB_097_Golden`; `BROWSER=TestTodo_WEB_097_Browser`; `CONFORMANCE=TestTodo_WEB_097_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the authorization-resolved home floorplan—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the authorization-resolved home floorplan—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-098` **[GATE_C][TERRA] Implement the unified attention list.**
  - **Depends:** `WEB-097`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.WORK; DIRECT=none; WHY=implement the unified attention list without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_098`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_098`; `GOLDEN=TestTodo_WEB_098_Golden`; `BROWSER=TestTodo_WEB_098_Browser`; `CONFORMANCE=TestTodo_WEB_098_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the unified attention list—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the unified attention list—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-099` **[GATE_C][TERRA] Implement recent-work continuity.**
  - **Depends:** `WEB-098`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.WORK; DIRECT=none; WHY=implement recent-work continuity without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_099`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_099`; `GOLDEN=TestTodo_WEB_099_Golden`; `BROWSER=TestTodo_WEB_099_Browser`; `CONFORMANCE=TestTodo_WEB_099_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement recent-work continuity—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement recent-work continuity—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-100` **[GATE_C][TERRA] Implement personal-essential summaries.**
  - **Depends:** `WEB-099`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.WORK; DIRECT=none; WHY=implement personal-essential summaries without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_100`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_100`; `GOLDEN=TestTodo_WEB_100_Golden`; `BROWSER=TestTodo_WEB_100_Browser`; `CONFORMANCE=TestTodo_WEB_100_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement personal-essential summaries—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement personal-essential summaries—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-101` **[GATE_C][TERRA] Implement configurable quick actions.**
  - **Depends:** `WEB-100`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.WORK; DIRECT=none; WHY=implement configurable quick actions without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_101`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_101`; `GOLDEN=TestTodo_WEB_101_Golden`; `BROWSER=TestTodo_WEB_101_Browser`; `CONFORMANCE=TestTodo_WEB_101_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement configurable quick actions—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement configurable quick actions—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-102` **[GATE_C][TERRA] Implement governed announcement regions.**
  - **Depends:** `WEB-101`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.WORK; DIRECT=none; WHY=implement governed announcement regions without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_102`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_102`; `GOLDEN=TestTodo_WEB_102_Golden`; `BROWSER=TestTodo_WEB_102_Browser`; `CONFORMANCE=TestTodo_WEB_102_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement governed announcement regions—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement governed announcement regions—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-103` **[GATE_C][TERRA] Implement the My Work collection.**
  - **Depends:** `WEB-102`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.WORK; DIRECT=none; WHY=implement the my work collection without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_103`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_103`; `GOLDEN=TestTodo_WEB_103_Golden`; `BROWSER=TestTodo_WEB_103_Browser`; `CONFORMANCE=TestTodo_WEB_103_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the my work collection—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the my work collection—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-104` **[GATE_C][TERRA] Implement task and approval filtering.**
  - **Depends:** `WEB-103`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.WORK; DIRECT=none; WHY=implement task and approval filtering without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_104`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_104`; `GOLDEN=TestTodo_WEB_104_Golden`; `BROWSER=TestTodo_WEB_104_Browser`; `CONFORMANCE=TestTodo_WEB_104_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement task and approval filtering—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement task and approval filtering—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-105` **[GATE_C][TERRA] Implement the Draft Center view.**
  - **Depends:** `WEB-104`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.WORK; DIRECT=none; WHY=implement the draft center view without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_105`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_105`; `GOLDEN=TestTodo_WEB_105_Golden`; `BROWSER=TestTodo_WEB_105_Browser`; `CONFORMANCE=TestTodo_WEB_105_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the draft center view—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the draft center view—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-106` **[GATE_C][TERRA] Implement tracked-request summaries.**
  - **Depends:** `WEB-105`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.WORK; DIRECT=none; WHY=implement tracked-request summaries without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_106`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_106`; `GOLDEN=TestTodo_WEB_106_Golden`; `BROWSER=TestTodo_WEB_106_Browser`; `CONFORMANCE=TestTodo_WEB_106_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement tracked-request summaries—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement tracked-request summaries—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-107` **[GATE_C][TERRA] Implement completed-work history.**
  - **Depends:** `WEB-106`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.WORK; DIRECT=none; WHY=implement completed-work history without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_107`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_107`; `GOLDEN=TestTodo_WEB_107_Golden`; `BROWSER=TestTodo_WEB_107_Browser`; `CONFORMANCE=TestTodo_WEB_107_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement completed-work history—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement completed-work history—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-108` **[GATE_C][TERRA] Implement responsive My Work list-detail behavior.**
  - **Depends:** `WEB-107`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.EXPERIENCE,BI.WORK; DIRECT=none; WHY=implement responsive my work list-detail behavior without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_108`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_108`; `GOLDEN=TestTodo_WEB_108_Golden`; `BROWSER=TestTodo_WEB_108_Browser`; `CONFORMANCE=TestTodo_WEB_108_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement responsive my work list-detail behavior—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement responsive my work list-detail behavior—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

### People directory and worker object

- [ ] `WEB-109` **[GATE_C][TERRA] Implement the authorized People directory.**
  - **Depends:** `WEB-037`, `PEOPLE-001`, `TRUST-009`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.PEOPLE,BI.EXPERIENCE; DIRECT=none; WHY=implement the authorized people directory without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_109`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_109`; `GOLDEN=TestTodo_WEB_109_Golden`; `BROWSER=TestTodo_WEB_109_Browser`; `CONFORMANCE=TestTodo_WEB_109_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the authorized people directory—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the authorized people directory—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [People domain](specs/people-employment-assignment-domain.md).

- [ ] `WEB-110` **[GATE_C][TERRA] Implement the bounded people-query builder.**
  - **Depends:** `WEB-109`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.PEOPLE,BI.EXPERIENCE; DIRECT=none; WHY=implement the bounded people-query builder without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_110`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_110`; `GOLDEN=TestTodo_WEB_110_Golden`; `BROWSER=TestTodo_WEB_110_Browser`; `CONFORMANCE=TestTodo_WEB_110_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the bounded people-query builder—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the bounded people-query builder—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [People domain](specs/people-employment-assignment-domain.md).

- [ ] `WEB-111` **[GATE_C][TERRA] Prevent people-search enumeration leakage.**
  - **Depends:** `WEB-110`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.PEOPLE,BI.EXPERIENCE; DIRECT=none; WHY=prevent people-search enumeration leakage without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_111`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_111`; `GOLDEN=TestTodo_WEB_111_Golden`; `BROWSER=TestTodo_WEB_111_Browser`; `CONFORMANCE=TestTodo_WEB_111_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—prevent people-search enumeration leakage—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—prevent people-search enumeration leakage—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [People domain](specs/people-employment-assignment-domain.md).

- [ ] `WEB-112` **[GATE_C][TERRA] Implement the worker identity header.**
  - **Depends:** `WEB-111`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.PEOPLE,BI.EXPERIENCE; DIRECT=none; WHY=implement the worker identity header without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_112`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_112`; `GOLDEN=TestTodo_WEB_112_Golden`; `BROWSER=TestTodo_WEB_112_Browser`; `CONFORMANCE=TestTodo_WEB_112_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the worker identity header—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the worker identity header—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [People domain](specs/people-employment-assignment-domain.md).

- [ ] `WEB-113` **[GATE_C][TERRA] Implement the worker overview section.**
  - **Depends:** `WEB-112`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.PEOPLE,BI.EXPERIENCE; DIRECT=none; WHY=implement the worker overview section without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_113`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_113`; `GOLDEN=TestTodo_WEB_113_Golden`; `BROWSER=TestTodo_WEB_113_Browser`; `CONFORMANCE=TestTodo_WEB_113_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the worker overview section—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the worker overview section—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [People domain](specs/people-employment-assignment-domain.md).

- [ ] `WEB-114` **[GATE_C][TERRA] Implement the worker employment section.**
  - **Depends:** `WEB-113`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.PEOPLE,BI.EXPERIENCE; DIRECT=none; WHY=implement the worker employment section without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_114`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_114`; `GOLDEN=TestTodo_WEB_114_Golden`; `BROWSER=TestTodo_WEB_114_Browser`; `CONFORMANCE=TestTodo_WEB_114_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the worker employment section—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the worker employment section—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [People domain](specs/people-employment-assignment-domain.md).

- [ ] `WEB-115` **[GATE_C][TERRA] Implement the worker time and leave section.**
  - **Depends:** `WEB-114`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.PEOPLE,BI.EXPERIENCE; DIRECT=none; WHY=implement the worker time and leave section without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_115`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_115`; `GOLDEN=TestTodo_WEB_115_Golden`; `BROWSER=TestTodo_WEB_115_Browser`; `CONFORMANCE=TestTodo_WEB_115_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the worker time and leave section—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the worker time and leave section—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [People domain](specs/people-employment-assignment-domain.md).

- [ ] `WEB-116` **[GATE_C][TERRA] Implement the worker pay and benefits section.**
  - **Depends:** `WEB-115`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.PEOPLE,BI.EXPERIENCE; DIRECT=none; WHY=implement the worker pay and benefits section without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_116`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_116`; `GOLDEN=TestTodo_WEB_116_Golden`; `BROWSER=TestTodo_WEB_116_Browser`; `CONFORMANCE=TestTodo_WEB_116_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the worker pay and benefits section—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the worker pay and benefits section—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [People domain](specs/people-employment-assignment-domain.md).

- [ ] `WEB-117` **[GATE_C][TERRA] Implement the worker growth section.**
  - **Depends:** `WEB-116`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.PEOPLE,BI.EXPERIENCE; DIRECT=none; WHY=implement the worker growth section without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_117`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_117`; `GOLDEN=TestTodo_WEB_117_Golden`; `BROWSER=TestTodo_WEB_117_Browser`; `CONFORMANCE=TestTodo_WEB_117_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the worker growth section—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the worker growth section—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [People domain](specs/people-employment-assignment-domain.md).

- [ ] `WEB-118` **[GATE_C][TERRA] Implement worker documents and activity sections.**
  - **Depends:** `WEB-117`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.PEOPLE,BI.EXPERIENCE; DIRECT=none; WHY=implement worker documents and activity sections without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_118`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_118`; `GOLDEN=TestTodo_WEB_118_Golden`; `BROWSER=TestTodo_WEB_118_Browser`; `CONFORMANCE=TestTodo_WEB_118_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement worker documents and activity sections—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement worker documents and activity sections—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [People domain](specs/people-employment-assignment-domain.md).

- [ ] `WEB-119` **[GATE_C][TERRA] Implement contextual worker action discovery.**
  - **Depends:** `WEB-118`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.PEOPLE,BI.EXPERIENCE; DIRECT=none; WHY=implement contextual worker action discovery without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_119`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_119`; `GOLDEN=TestTodo_WEB_119_Golden`; `BROWSER=TestTodo_WEB_119_Browser`; `CONFORMANCE=TestTodo_WEB_119_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement contextual worker action discovery—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement contextual worker action discovery—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [People domain](specs/people-employment-assignment-domain.md).

- [ ] `WEB-120` **[GATE_C][TERRA] Implement the responsive worker object page.**
  - **Depends:** `WEB-119`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.PEOPLE,BI.EXPERIENCE; DIRECT=none; WHY=implement the responsive worker object page without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_120`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_120`; `GOLDEN=TestTodo_WEB_120_Golden`; `BROWSER=TestTodo_WEB_120_Browser`; `CONFORMANCE=TestTodo_WEB_120_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the responsive worker object page—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the responsive worker object page—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [People domain](specs/people-employment-assignment-domain.md).

### Intent Workspace

- [x] `WEB-121` **[GATE_C][SOL_HIGH] Implement the canonical Intent Workspace shell.**
  - **Evidence (2026-09-05):** `TestTodo_WEB_121` in `tools/uxqual/render/workspace` (canonical Intent Workspace shell composed of skip link, hash-route navigation from journeyclient, a live status region, a session strip read from the existing journey-config island (never a credential) and the page renderer, golden digests for both Promotion pages; the wasm swap steps are reported and reactive re-mounting is left to WEB-027; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/uxqual/render/workspace/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WEB-025`, `UX-001`, `WORK-001`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.PEOPLE,BI.REWARDS,BI.WORK,BI.EXPERIENCE; DIRECT=none; WHY=implement the canonical intent workspace shell without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_121`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_121`; `GOLDEN=TestTodo_WEB_121_Golden`; `BROWSER=TestTodo_WEB_121_Browser`; `CONFORMANCE=TestTodo_WEB_121_Conformance`; `INTEGRATION=TestTodo_WEB_121_Integration`; `FAULT=TestTodo_WEB_121_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the canonical intent workspace shell—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the canonical intent workspace shell—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Promotion flow](user-flows/reference/promote-into-management.md).

- [ ] `WEB-122` **[GATE_C][SOL_HIGH] Implement durable draft autosave presentation.**
  - **Depends:** `WEB-121`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.PEOPLE,BI.REWARDS,BI.WORK,BI.EXPERIENCE; DIRECT=none; WHY=implement durable draft autosave presentation without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_122`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_122`; `GOLDEN=TestTodo_WEB_122_Golden`; `BROWSER=TestTodo_WEB_122_Browser`; `CONFORMANCE=TestTodo_WEB_122_Conformance`; `INTEGRATION=TestTodo_WEB_122_Integration`; `FAULT=TestTodo_WEB_122_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement durable draft autosave presentation—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement durable draft autosave presentation—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Promotion flow](user-flows/reference/promote-into-management.md).

- [ ] `WEB-123` **[GATE_C][SOL_HIGH] Implement current-versus-proposed field presentation.**
  - **Depends:** `WEB-122`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.PEOPLE,BI.REWARDS,BI.WORK,BI.EXPERIENCE; DIRECT=none; WHY=implement current-versus-proposed field presentation without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_123`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_123`; `GOLDEN=TestTodo_WEB_123_Golden`; `BROWSER=TestTodo_WEB_123_Browser`; `CONFORMANCE=TestTodo_WEB_123_Conformance`; `INTEGRATION=TestTodo_WEB_123_Integration`; `FAULT=TestTodo_WEB_123_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement current-versus-proposed field presentation—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement current-versus-proposed field presentation—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Promotion flow](user-flows/reference/promote-into-management.md).

- [ ] `WEB-124` **[GATE_C][SOL_HIGH] Implement guided proposal collection.**
  - **Depends:** `WEB-123`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.PEOPLE,BI.REWARDS,BI.WORK,BI.EXPERIENCE; DIRECT=none; WHY=implement guided proposal collection without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_124`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_124`; `GOLDEN=TestTodo_WEB_124_Golden`; `BROWSER=TestTodo_WEB_124_Browser`; `CONFORMANCE=TestTodo_WEB_124_Conformance`; `INTEGRATION=TestTodo_WEB_124_Integration`; `FAULT=TestTodo_WEB_124_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement guided proposal collection—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement guided proposal collection—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Promotion flow](user-flows/reference/promote-into-management.md).

- [ ] `WEB-125` **[GATE_C][SOL_HIGH] Implement the accessible validation summary.**
  - **Depends:** `WEB-124`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.PEOPLE,BI.REWARDS,BI.WORK,BI.EXPERIENCE; DIRECT=none; WHY=implement the accessible validation summary without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_125`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_125`; `GOLDEN=TestTodo_WEB_125_Golden`; `BROWSER=TestTodo_WEB_125_Browser`; `CONFORMANCE=TestTodo_WEB_125_Conformance`; `INTEGRATION=TestTodo_WEB_125_Integration`; `FAULT=TestTodo_WEB_125_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the accessible validation summary—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the accessible validation summary—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Promotion flow](user-flows/reference/promote-into-management.md).

- [ ] `WEB-126` **[GATE_C][SOL_HIGH] Implement simulation comparison presentation.**
  - **Depends:** `WEB-125`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.PEOPLE,BI.REWARDS,BI.WORK,BI.EXPERIENCE; DIRECT=none; WHY=implement simulation comparison presentation without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_126`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_126`; `GOLDEN=TestTodo_WEB_126_Golden`; `BROWSER=TestTodo_WEB_126_Browser`; `CONFORMANCE=TestTodo_WEB_126_Conformance`; `INTEGRATION=TestTodo_WEB_126_Integration`; `FAULT=TestTodo_WEB_126_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement simulation comparison presentation—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement simulation comparison presentation—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Promotion flow](user-flows/reference/promote-into-management.md).

- [ ] `WEB-127` **[GATE_C][SOL_HIGH] Implement policy and obligation explanations.**
  - **Depends:** `WEB-126`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.PEOPLE,BI.REWARDS,BI.WORK,BI.EXPERIENCE; DIRECT=none; WHY=implement policy and obligation explanations without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_127`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_127`; `GOLDEN=TestTodo_WEB_127_Golden`; `BROWSER=TestTodo_WEB_127_Browser`; `CONFORMANCE=TestTodo_WEB_127_Conformance`; `INTEGRATION=TestTodo_WEB_127_Integration`; `FAULT=TestTodo_WEB_127_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement policy and obligation explanations—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement policy and obligation explanations—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Promotion flow](user-flows/reference/promote-into-management.md).

- [ ] `WEB-128` **[GATE_C][SOL_HIGH] Implement proposal confirmation review.**
  - **Depends:** `WEB-127`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.PEOPLE,BI.REWARDS,BI.WORK,BI.EXPERIENCE; DIRECT=none; WHY=implement proposal confirmation review without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_128`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_128`; `GOLDEN=TestTodo_WEB_128_Golden`; `BROWSER=TestTodo_WEB_128_Browser`; `CONFORMANCE=TestTodo_WEB_128_Conformance`; `INTEGRATION=TestTodo_WEB_128_Integration`; `FAULT=TestTodo_WEB_128_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement proposal confirmation review—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement proposal confirmation review—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Promotion flow](user-flows/reference/promote-into-management.md).

- [ ] `WEB-129` **[GATE_C][SOL_HIGH] Implement approval progress presentation.**
  - **Depends:** `WEB-128`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.PEOPLE,BI.REWARDS,BI.WORK,BI.EXPERIENCE; DIRECT=none; WHY=implement approval progress presentation without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_129`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_129`; `GOLDEN=TestTodo_WEB_129_Golden`; `BROWSER=TestTodo_WEB_129_Browser`; `CONFORMANCE=TestTodo_WEB_129_Conformance`; `INTEGRATION=TestTodo_WEB_129_Integration`; `FAULT=TestTodo_WEB_129_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement approval progress presentation—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement approval progress presentation—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Promotion flow](user-flows/reference/promote-into-management.md).

- [ ] `WEB-130` **[GATE_C][SOL_HIGH] Implement execution-status dimensions.**
  - **Depends:** `WEB-129`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.PEOPLE,BI.REWARDS,BI.WORK,BI.EXPERIENCE; DIRECT=none; WHY=implement execution-status dimensions without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_130`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_130`; `GOLDEN=TestTodo_WEB_130_Golden`; `BROWSER=TestTodo_WEB_130_Browser`; `CONFORMANCE=TestTodo_WEB_130_Conformance`; `INTEGRATION=TestTodo_WEB_130_Integration`; `FAULT=TestTodo_WEB_130_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement execution-status dimensions—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement execution-status dimensions—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Promotion flow](user-flows/reference/promote-into-management.md).

- [ ] `WEB-131` **[GATE_C][SOL_HIGH] Implement reconciliation and repair presentation.**
  - **Depends:** `WEB-130`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.PEOPLE,BI.REWARDS,BI.WORK,BI.EXPERIENCE; DIRECT=none; WHY=implement reconciliation and repair presentation without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_131`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_131`; `GOLDEN=TestTodo_WEB_131_Golden`; `BROWSER=TestTodo_WEB_131_Browser`; `CONFORMANCE=TestTodo_WEB_131_Conformance`; `INTEGRATION=TestTodo_WEB_131_Integration`; `FAULT=TestTodo_WEB_131_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement reconciliation and repair presentation—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement reconciliation and repair presentation—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Promotion flow](user-flows/reference/promote-into-management.md).

- [ ] `WEB-132` **[GATE_C][SOL_HIGH] Implement interruption and recovery continuity.**
  - **Depends:** `WEB-131`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.PEOPLE,BI.REWARDS,BI.WORK,BI.EXPERIENCE; DIRECT=none; WHY=implement interruption and recovery continuity without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_132`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_132`; `GOLDEN=TestTodo_WEB_132_Golden`; `BROWSER=TestTodo_WEB_132_Browser`; `CONFORMANCE=TestTodo_WEB_132_Conformance`; `INTEGRATION=TestTodo_WEB_132_Integration`; `FAULT=TestTodo_WEB_132_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement interruption and recovery continuity—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement interruption and recovery continuity—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Promotion flow](user-flows/reference/promote-into-management.md).

### Hire and onboard

- [ ] `WEB-133` **[PHASE_3][SOL_HIGH] Implement the headcount-request page.**
  - **Depends:** `WEB-121`, `RECRUIT-001`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.RECRUITING,BI.LIFECYCLE,BI.EXPERIENCE; DIRECT=none; WHY=implement the headcount-request page without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_133`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_133`; `GOLDEN=TestTodo_WEB_133_Golden`; `BROWSER=TestTodo_WEB_133_Browser`; `CONFORMANCE=TestTodo_WEB_133_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the headcount-request page—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the headcount-request page—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-134` **[PHASE_3][SOL_HIGH] Implement the position-request page.**
  - **Depends:** `WEB-133`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.RECRUITING,BI.LIFECYCLE,BI.EXPERIENCE; DIRECT=none; WHY=implement the position-request page without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_134`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_134`; `GOLDEN=TestTodo_WEB_134_Golden`; `BROWSER=TestTodo_WEB_134_Browser`; `CONFORMANCE=TestTodo_WEB_134_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the position-request page—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the position-request page—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-135` **[PHASE_3][SOL_HIGH] Implement the requisition workspace.**
  - **Depends:** `WEB-134`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.RECRUITING,BI.LIFECYCLE,BI.EXPERIENCE; DIRECT=none; WHY=implement the requisition workspace without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_135`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_135`; `GOLDEN=TestTodo_WEB_135_Golden`; `BROWSER=TestTodo_WEB_135_Browser`; `CONFORMANCE=TestTodo_WEB_135_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the requisition workspace—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the requisition workspace—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-136` **[PHASE_3][SOL_HIGH] Implement the candidate pipeline.**
  - **Depends:** `WEB-135`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.RECRUITING,BI.LIFECYCLE,BI.EXPERIENCE; DIRECT=none; WHY=implement the candidate pipeline without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_136`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_136`; `GOLDEN=TestTodo_WEB_136_Golden`; `BROWSER=TestTodo_WEB_136_Browser`; `CONFORMANCE=TestTodo_WEB_136_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the candidate pipeline—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the candidate pipeline—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-137` **[PHASE_3][SOL_HIGH] Implement the candidate object page.**
  - **Depends:** `WEB-136`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.RECRUITING,BI.LIFECYCLE,BI.EXPERIENCE; DIRECT=none; WHY=implement the candidate object page without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_137`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_137`; `GOLDEN=TestTodo_WEB_137_Golden`; `BROWSER=TestTodo_WEB_137_Browser`; `CONFORMANCE=TestTodo_WEB_137_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the candidate object page—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the candidate object page—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-138` **[PHASE_3][SOL_HIGH] Implement interview scheduling.**
  - **Depends:** `WEB-137`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.RECRUITING,BI.LIFECYCLE,BI.EXPERIENCE; DIRECT=none; WHY=implement interview scheduling without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_138`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_138`; `GOLDEN=TestTodo_WEB_138_Golden`; `BROWSER=TestTodo_WEB_138_Browser`; `CONFORMANCE=TestTodo_WEB_138_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement interview scheduling—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement interview scheduling—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-139` **[PHASE_3][SOL_HIGH] Implement structured candidate evaluation.**
  - **Depends:** `WEB-138`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.RECRUITING,BI.LIFECYCLE,BI.EXPERIENCE; DIRECT=none; WHY=implement structured candidate evaluation without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_139`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_139`; `GOLDEN=TestTodo_WEB_139_Golden`; `BROWSER=TestTodo_WEB_139_Browser`; `CONFORMANCE=TestTodo_WEB_139_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement structured candidate evaluation—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement structured candidate evaluation—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-140` **[PHASE_3][SOL_HIGH] Implement offer review and acceptance.**
  - **Depends:** `WEB-139`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.RECRUITING,BI.LIFECYCLE,BI.EXPERIENCE; DIRECT=none; WHY=implement offer review and acceptance without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_140`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_140`; `GOLDEN=TestTodo_WEB_140_Golden`; `BROWSER=TestTodo_WEB_140_Browser`; `CONFORMANCE=TestTodo_WEB_140_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement offer review and acceptance—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement offer review and acceptance—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-141` **[PHASE_3][SOL_HIGH] Implement the external candidate portal.**
  - **Depends:** `WEB-140`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.RECRUITING,BI.LIFECYCLE,BI.EXPERIENCE; DIRECT=none; WHY=implement the external candidate portal without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_141`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_141`; `GOLDEN=TestTodo_WEB_141_Golden`; `BROWSER=TestTodo_WEB_141_Browser`; `CONFORMANCE=TestTodo_WEB_141_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the external candidate portal—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the external candidate portal—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-142` **[PHASE_3][SOL_HIGH] Implement the onboarding-plan page.**
  - **Depends:** `WEB-141`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.RECRUITING,BI.LIFECYCLE,BI.EXPERIENCE; DIRECT=none; WHY=implement the onboarding-plan page without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_142`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_142`; `GOLDEN=TestTodo_WEB_142_Golden`; `BROWSER=TestTodo_WEB_142_Browser`; `CONFORMANCE=TestTodo_WEB_142_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the onboarding-plan page—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the onboarding-plan page—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-143` **[PHASE_3][SOL_HIGH] Implement onboarding task completion.**
  - **Depends:** `WEB-142`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.RECRUITING,BI.LIFECYCLE,BI.EXPERIENCE; DIRECT=none; WHY=implement onboarding task completion without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_143`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_143`; `GOLDEN=TestTodo_WEB_143_Golden`; `BROWSER=TestTodo_WEB_143_Browser`; `CONFORMANCE=TestTodo_WEB_143_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement onboarding task completion—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement onboarding task completion—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-144` **[PHASE_3][SOL_HIGH] Implement worker-activation readiness.**
  - **Depends:** `WEB-143`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.RECRUITING,BI.LIFECYCLE,BI.EXPERIENCE; DIRECT=none; WHY=implement worker-activation readiness without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_144`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_144`; `GOLDEN=TestTodo_WEB_144_Golden`; `BROWSER=TestTodo_WEB_144_Browser`; `CONFORMANCE=TestTodo_WEB_144_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement worker-activation readiness—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement worker-activation readiness—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

### Time and leave

- [ ] `WEB-145` **[PHASE_3][SOL_HIGH] Implement the employee time hub.**
  - **Depends:** `WEB-121`, `TIME-001`, `CONF-011`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.WORKFORCE,BI.REGULATORY,BI.EXPERIENCE; DIRECT=none; WHY=implement the employee time hub without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_145`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_145`; `GOLDEN=TestTodo_WEB_145_Golden`; `BROWSER=TestTodo_WEB_145_Browser`; `CONFORMANCE=TestTodo_WEB_145_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the employee time hub—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the employee time hub—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Leave flow](user-flows/reference/medical-leave-and-return.md).

- [ ] `WEB-146` **[PHASE_3][SOL_HIGH] Implement accessible time entry.**
  - **Depends:** `WEB-145`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.WORKFORCE,BI.REGULATORY,BI.EXPERIENCE; DIRECT=none; WHY=implement accessible time entry without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_146`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_146`; `GOLDEN=TestTodo_WEB_146_Golden`; `BROWSER=TestTodo_WEB_146_Browser`; `CONFORMANCE=TestTodo_WEB_146_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement accessible time entry—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement accessible time entry—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Leave flow](user-flows/reference/medical-leave-and-return.md).

- [ ] `WEB-147` **[PHASE_3][SOL_HIGH] Implement time correction.**
  - **Depends:** `WEB-146`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.WORKFORCE,BI.REGULATORY,BI.EXPERIENCE; DIRECT=none; WHY=implement time correction without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_147`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_147`; `GOLDEN=TestTodo_WEB_147_Golden`; `BROWSER=TestTodo_WEB_147_Browser`; `CONFORMANCE=TestTodo_WEB_147_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement time correction—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement time correction—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Leave flow](user-flows/reference/medical-leave-and-return.md).

- [ ] `WEB-148` **[PHASE_3][SOL_HIGH] Implement manager time approval.**
  - **Depends:** `WEB-147`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.WORKFORCE,BI.REGULATORY,BI.EXPERIENCE; DIRECT=none; WHY=implement manager time approval without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_148`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_148`; `GOLDEN=TestTodo_WEB_148_Golden`; `BROWSER=TestTodo_WEB_148_Browser`; `CONFORMANCE=TestTodo_WEB_148_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement manager time approval—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement manager time approval—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Leave flow](user-flows/reference/medical-leave-and-return.md).

- [ ] `WEB-149` **[PHASE_3][SOL_HIGH] Implement the time-exception workbench.**
  - **Depends:** `WEB-148`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.WORKFORCE,BI.REGULATORY,BI.EXPERIENCE; DIRECT=none; WHY=implement the time-exception workbench without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_149`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_149`; `GOLDEN=TestTodo_WEB_149_Golden`; `BROWSER=TestTodo_WEB_149_Browser`; `CONFORMANCE=TestTodo_WEB_149_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the time-exception workbench—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the time-exception workbench—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Leave flow](user-flows/reference/medical-leave-and-return.md).

- [ ] `WEB-150` **[PHASE_3][SOL_HIGH] Implement time-off balance and calendar.**
  - **Depends:** `WEB-149`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.WORKFORCE,BI.REGULATORY,BI.EXPERIENCE; DIRECT=none; WHY=implement time-off balance and calendar without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_150`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_150`; `GOLDEN=TestTodo_WEB_150_Golden`; `BROWSER=TestTodo_WEB_150_Browser`; `CONFORMANCE=TestTodo_WEB_150_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement time-off balance and calendar—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement time-off balance and calendar—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Leave flow](user-flows/reference/medical-leave-and-return.md).

- [ ] `WEB-151` **[PHASE_3][SOL_HIGH] Implement the time-off request journey.**
  - **Depends:** `WEB-150`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.WORKFORCE,BI.REGULATORY,BI.EXPERIENCE; DIRECT=none; WHY=implement the time-off request journey without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_151`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_151`; `GOLDEN=TestTodo_WEB_151_Golden`; `BROWSER=TestTodo_WEB_151_Browser`; `CONFORMANCE=TestTodo_WEB_151_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the time-off request journey—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the time-off request journey—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Leave flow](user-flows/reference/medical-leave-and-return.md).

- [ ] `WEB-152` **[PHASE_3][SOL_HIGH] Implement team-coverage review.**
  - **Depends:** `WEB-151`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.WORKFORCE,BI.REGULATORY,BI.EXPERIENCE; DIRECT=none; WHY=implement team-coverage review without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_152`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_152`; `GOLDEN=TestTodo_WEB_152_Golden`; `BROWSER=TestTodo_WEB_152_Browser`; `CONFORMANCE=TestTodo_WEB_152_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement team-coverage review—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement team-coverage review—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Leave flow](user-flows/reference/medical-leave-and-return.md).

- [ ] `WEB-153` **[PHASE_3][SOL_HIGH] Implement protected-leave intake.**
  - **Depends:** `WEB-152`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.WORKFORCE,BI.REGULATORY,BI.EXPERIENCE; DIRECT=none; WHY=implement protected-leave intake without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_153`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_153`; `GOLDEN=TestTodo_WEB_153_Golden`; `BROWSER=TestTodo_WEB_153_Browser`; `CONFORMANCE=TestTodo_WEB_153_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement protected-leave intake—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement protected-leave intake—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Leave flow](user-flows/reference/medical-leave-and-return.md).

- [ ] `WEB-154` **[PHASE_3][SOL_HIGH] Implement restricted leave-evidence tasks.**
  - **Depends:** `WEB-153`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.WORKFORCE,BI.REGULATORY,BI.EXPERIENCE; DIRECT=none; WHY=implement restricted leave-evidence tasks without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_154`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_154`; `GOLDEN=TestTodo_WEB_154_Golden`; `BROWSER=TestTodo_WEB_154_Browser`; `CONFORMANCE=TestTodo_WEB_154_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement restricted leave-evidence tasks—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement restricted leave-evidence tasks—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Leave flow](user-flows/reference/medical-leave-and-return.md).

- [ ] `WEB-155` **[PHASE_3][SOL_HIGH] Implement the leave-status timeline.**
  - **Depends:** `WEB-154`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.WORKFORCE,BI.REGULATORY,BI.EXPERIENCE; DIRECT=none; WHY=implement the leave-status timeline without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_155`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_155`; `GOLDEN=TestTodo_WEB_155_Golden`; `BROWSER=TestTodo_WEB_155_Browser`; `CONFORMANCE=TestTodo_WEB_155_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the leave-status timeline—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the leave-status timeline—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Leave flow](user-flows/reference/medical-leave-and-return.md).

- [ ] `WEB-156` **[PHASE_3][SOL_HIGH] Implement return-to-work planning.**
  - **Depends:** `WEB-155`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.WORKFORCE,BI.REGULATORY,BI.EXPERIENCE; DIRECT=none; WHY=implement return-to-work planning without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_156`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_156`; `GOLDEN=TestTodo_WEB_156_Golden`; `BROWSER=TestTodo_WEB_156_Browser`; `CONFORMANCE=TestTodo_WEB_156_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement return-to-work planning—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement return-to-work planning—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [Leave flow](user-flows/reference/medical-leave-and-return.md).

### Pay rewards and benefits

- [ ] `WEB-157` **[PHASE_4][SOL_HIGH] Implement the employee pay summary.**
  - **Depends:** `WEB-121`, `COMP-003`, `CONF-010`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.REWARDS,BI.PAYROLL,BI.EXPERIENCE; DIRECT=none; WHY=implement the employee pay summary without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_157`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_157`; `GOLDEN=TestTodo_WEB_157_Golden`; `BROWSER=TestTodo_WEB_157_Browser`; `CONFORMANCE=TestTodo_WEB_157_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the employee pay summary—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the employee pay summary—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-158` **[PHASE_4][SOL_HIGH] Implement accessible pay statements.**
  - **Depends:** `WEB-157`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.REWARDS,BI.PAYROLL,BI.EXPERIENCE; DIRECT=none; WHY=implement accessible pay statements without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_158`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_158`; `GOLDEN=TestTodo_WEB_158_Golden`; `BROWSER=TestTodo_WEB_158_Browser`; `CONFORMANCE=TestTodo_WEB_158_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement accessible pay statements—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement accessible pay statements—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-159` **[PHASE_4][SOL_HIGH] Implement pay-discrepancy intake.**
  - **Depends:** `WEB-158`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.REWARDS,BI.PAYROLL,BI.EXPERIENCE; DIRECT=none; WHY=implement pay-discrepancy intake without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_159`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_159`; `GOLDEN=TestTodo_WEB_159_Golden`; `BROWSER=TestTodo_WEB_159_Browser`; `CONFORMANCE=TestTodo_WEB_159_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement pay-discrepancy intake—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement pay-discrepancy intake—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-160` **[PHASE_4][SOL_HIGH] Implement manager compensation proposals.**
  - **Depends:** `WEB-159`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.REWARDS,BI.PAYROLL,BI.EXPERIENCE; DIRECT=none; WHY=implement manager compensation proposals without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_160`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_160`; `GOLDEN=TestTodo_WEB_160_Golden`; `BROWSER=TestTodo_WEB_160_Browser`; `CONFORMANCE=TestTodo_WEB_160_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement manager compensation proposals—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement manager compensation proposals—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-161` **[PHASE_4][SOL_HIGH] Implement salary-range and budget comparison.**
  - **Depends:** `WEB-160`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.REWARDS,BI.PAYROLL,BI.EXPERIENCE; DIRECT=none; WHY=implement salary-range and budget comparison without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_161`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_161`; `GOLDEN=TestTodo_WEB_161_Golden`; `BROWSER=TestTodo_WEB_161_Browser`; `CONFORMANCE=TestTodo_WEB_161_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement salary-range and budget comparison—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement salary-range and budget comparison—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-162` **[PHASE_4][SOL_HIGH] Implement compensation-cycle populations.**
  - **Depends:** `WEB-161`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.REWARDS,BI.PAYROLL,BI.EXPERIENCE; DIRECT=none; WHY=implement compensation-cycle populations without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_162`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_162`; `GOLDEN=TestTodo_WEB_162_Golden`; `BROWSER=TestTodo_WEB_162_Browser`; `CONFORMANCE=TestTodo_WEB_162_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement compensation-cycle populations—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement compensation-cycle populations—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-163` **[PHASE_4][SOL_HIGH] Implement the compensation worksheet.**
  - **Depends:** `WEB-162`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.REWARDS,BI.PAYROLL,BI.EXPERIENCE; DIRECT=none; WHY=implement the compensation worksheet without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_163`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_163`; `GOLDEN=TestTodo_WEB_163_Golden`; `BROWSER=TestTodo_WEB_163_Browser`; `CONFORMANCE=TestTodo_WEB_163_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the compensation worksheet—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the compensation worksheet—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-164` **[PHASE_4][SOL_HIGH] Implement compensation calibration.**
  - **Depends:** `WEB-163`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.REWARDS,BI.PAYROLL,BI.EXPERIENCE; DIRECT=none; WHY=implement compensation calibration without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_164`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_164`; `GOLDEN=TestTodo_WEB_164_Golden`; `BROWSER=TestTodo_WEB_164_Browser`; `CONFORMANCE=TestTodo_WEB_164_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement compensation calibration—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement compensation calibration—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-165` **[PHASE_4][SOL_HIGH] Implement the benefit-program overview.**
  - **Depends:** `WEB-164`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.REWARDS,BI.PAYROLL,BI.EXPERIENCE; DIRECT=none; WHY=implement the benefit-program overview without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_165`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_165`; `GOLDEN=TestTodo_WEB_165_Golden`; `BROWSER=TestTodo_WEB_165_Browser`; `CONFORMANCE=TestTodo_WEB_165_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the benefit-program overview—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the benefit-program overview—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-166` **[PHASE_4][SOL_HIGH] Implement benefit-plan comparison.**
  - **Depends:** `WEB-165`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.REWARDS,BI.PAYROLL,BI.EXPERIENCE; DIRECT=none; WHY=implement benefit-plan comparison without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_166`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_166`; `GOLDEN=TestTodo_WEB_166_Golden`; `BROWSER=TestTodo_WEB_166_Browser`; `CONFORMANCE=TestTodo_WEB_166_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement benefit-plan comparison—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement benefit-plan comparison—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-167` **[PHASE_4][SOL_HIGH] Implement benefit enrollment.**
  - **Depends:** `WEB-166`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.REWARDS,BI.PAYROLL,BI.EXPERIENCE; DIRECT=none; WHY=implement benefit enrollment without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_167`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_167`; `GOLDEN=TestTodo_WEB_167_Golden`; `BROWSER=TestTodo_WEB_167_Browser`; `CONFORMANCE=TestTodo_WEB_167_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement benefit enrollment—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement benefit enrollment—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-168` **[PHASE_4][SOL_HIGH] Implement payroll and benefit reconciliation status.**
  - **Depends:** `WEB-167`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.REWARDS,BI.PAYROLL,BI.EXPERIENCE; DIRECT=none; WHY=implement payroll and benefit reconciliation status without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_168`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_168`; `GOLDEN=TestTodo_WEB_168_Golden`; `BROWSER=TestTodo_WEB_168_Browser`; `CONFORMANCE=TestTodo_WEB_168_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement payroll and benefit reconciliation status—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement payroll and benefit reconciliation status—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

### Performance and growth

- [ ] `WEB-169` **[PHASE_3][TERRA] Implement the employee Growth home.**
  - **Depends:** `WEB-121`, `CONF-012`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.TALENT,BI.EXPERIENCE; DIRECT=none; WHY=implement the employee growth home without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_169`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_169`; `GOLDEN=TestTodo_WEB_169_Golden`; `BROWSER=TestTodo_WEB_169_Browser`; `CONFORMANCE=TestTodo_WEB_169_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the employee growth home—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the employee growth home—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-170` **[PHASE_3][TERRA] Implement goal planning.**
  - **Depends:** `WEB-169`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.TALENT,BI.EXPERIENCE; DIRECT=none; WHY=implement goal planning without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_170`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_170`; `GOLDEN=TestTodo_WEB_170_Golden`; `BROWSER=TestTodo_WEB_170_Browser`; `CONFORMANCE=TestTodo_WEB_170_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement goal planning—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement goal planning—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-171` **[PHASE_3][TERRA] Implement governed feedback.**
  - **Depends:** `WEB-170`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.TALENT,BI.EXPERIENCE; DIRECT=none; WHY=implement governed feedback without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_171`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_171`; `GOLDEN=TestTodo_WEB_171_Golden`; `BROWSER=TestTodo_WEB_171_Browser`; `CONFORMANCE=TestTodo_WEB_171_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement governed feedback—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement governed feedback—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-172` **[PHASE_3][TERRA] Implement manager check-ins.**
  - **Depends:** `WEB-171`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.TALENT,BI.EXPERIENCE; DIRECT=none; WHY=implement manager check-ins without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_172`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_172`; `GOLDEN=TestTodo_WEB_172_Golden`; `BROWSER=TestTodo_WEB_172_Browser`; `CONFORMANCE=TestTodo_WEB_172_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement manager check-ins—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement manager check-ins—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-173` **[PHASE_3][TERRA] Implement the performance-review workspace.**
  - **Depends:** `WEB-172`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.TALENT,BI.EXPERIENCE; DIRECT=none; WHY=implement the performance-review workspace without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_173`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_173`; `GOLDEN=TestTodo_WEB_173_Golden`; `BROWSER=TestTodo_WEB_173_Browser`; `CONFORMANCE=TestTodo_WEB_173_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the performance-review workspace—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the performance-review workspace—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-174` **[PHASE_3][TERRA] Enforce review-participant visibility.**
  - **Depends:** `WEB-173`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.TALENT,BI.EXPERIENCE; DIRECT=none; WHY=enforce review-participant visibility without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_174`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_174`; `GOLDEN=TestTodo_WEB_174_Golden`; `BROWSER=TestTodo_WEB_174_Browser`; `CONFORMANCE=TestTodo_WEB_174_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—enforce review-participant visibility—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—enforce review-participant visibility—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-175` **[PHASE_3][TERRA] Implement the governed skills profile.**
  - **Depends:** `WEB-174`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.TALENT,BI.EXPERIENCE; DIRECT=none; WHY=implement the governed skills profile without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_175`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_175`; `GOLDEN=TestTodo_WEB_175_Golden`; `BROWSER=TestTodo_WEB_175_Browser`; `CONFORMANCE=TestTodo_WEB_175_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the governed skills profile—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the governed skills profile—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-176` **[PHASE_3][TERRA] Implement assigned learning.**
  - **Depends:** `WEB-175`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.TALENT,BI.EXPERIENCE; DIRECT=none; WHY=implement assigned learning without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_176`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_176`; `GOLDEN=TestTodo_WEB_176_Golden`; `BROWSER=TestTodo_WEB_176_Browser`; `CONFORMANCE=TestTodo_WEB_176_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement assigned learning—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement assigned learning—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-177` **[PHASE_3][TERRA] Implement career-opportunity discovery.**
  - **Depends:** `WEB-176`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.TALENT,BI.EXPERIENCE; DIRECT=none; WHY=implement career-opportunity discovery without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_177`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_177`; `GOLDEN=TestTodo_WEB_177_Golden`; `BROWSER=TestTodo_WEB_177_Browser`; `CONFORMANCE=TestTodo_WEB_177_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement career-opportunity discovery—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement career-opportunity discovery—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-178` **[PHASE_3][TERRA] Implement the manager talent workbench.**
  - **Depends:** `WEB-177`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.TALENT,BI.EXPERIENCE; DIRECT=none; WHY=implement the manager talent workbench without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_178`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_178`; `GOLDEN=TestTodo_WEB_178_Golden`; `BROWSER=TestTodo_WEB_178_Browser`; `CONFORMANCE=TestTodo_WEB_178_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the manager talent workbench—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the manager talent workbench—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-179` **[PHASE_3][TERRA] Implement talent calibration.**
  - **Depends:** `WEB-178`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.TALENT,BI.EXPERIENCE; DIRECT=none; WHY=implement talent calibration without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_179`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_179`; `GOLDEN=TestTodo_WEB_179_Golden`; `BROWSER=TestTodo_WEB_179_Browser`; `CONFORMANCE=TestTodo_WEB_179_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement talent calibration—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement talent calibration—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-180` **[PHASE_3][TERRA] Implement succession planning.**
  - **Depends:** `WEB-179`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.TALENT,BI.EXPERIENCE; DIRECT=none; WHY=implement succession planning without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_180`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_180`; `GOLDEN=TestTodo_WEB_180_Golden`; `BROWSER=TestTodo_WEB_180_Browser`; `CONFORMANCE=TestTodo_WEB_180_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement succession planning—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement succession planning—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

### Organization and workforce planning

- [ ] `WEB-181` **[PHASE_3][SOL_HIGH] Implement the organization explorer.**
  - **Depends:** `WEB-121`, `ORG-001`, `POSITION-001`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.WORKFORCE,BI.PEOPLE,BI.EXPERIENCE; DIRECT=none; WHY=implement the organization explorer without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_181`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_181`; `GOLDEN=TestTodo_WEB_181_Golden`; `BROWSER=TestTodo_WEB_181_Browser`; `CONFORMANCE=TestTodo_WEB_181_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the organization explorer—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the organization explorer—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [position domain](specs/position-and-headcount-domain.md).

- [ ] `WEB-182` **[PHASE_3][SOL_HIGH] Implement the accessible organization outline.**
  - **Depends:** `WEB-181`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.WORKFORCE,BI.PEOPLE,BI.EXPERIENCE; DIRECT=none; WHY=implement the accessible organization outline without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_182`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_182`; `GOLDEN=TestTodo_WEB_182_Golden`; `BROWSER=TestTodo_WEB_182_Browser`; `CONFORMANCE=TestTodo_WEB_182_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the accessible organization outline—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the accessible organization outline—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [position domain](specs/position-and-headcount-domain.md).

- [ ] `WEB-183` **[PHASE_3][SOL_HIGH] Implement effective-date organization navigation.**
  - **Depends:** `WEB-182`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.WORKFORCE,BI.PEOPLE,BI.EXPERIENCE; DIRECT=none; WHY=implement effective-date organization navigation without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_183`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_183`; `GOLDEN=TestTodo_WEB_183_Golden`; `BROWSER=TestTodo_WEB_183_Browser`; `CONFORMANCE=TestTodo_WEB_183_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement effective-date organization navigation—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement effective-date organization navigation—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [position domain](specs/position-and-headcount-domain.md).

- [ ] `WEB-184` **[PHASE_3][SOL_HIGH] Implement the position object page.**
  - **Depends:** `WEB-183`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.WORKFORCE,BI.PEOPLE,BI.EXPERIENCE; DIRECT=none; WHY=implement the position object page without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_184`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_184`; `GOLDEN=TestTodo_WEB_184_Golden`; `BROWSER=TestTodo_WEB_184_Browser`; `CONFORMANCE=TestTodo_WEB_184_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the position object page—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the position object page—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [position domain](specs/position-and-headcount-domain.md).

- [ ] `WEB-185` **[PHASE_3][SOL_HIGH] Implement position occupancy presentation.**
  - **Depends:** `WEB-184`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.WORKFORCE,BI.PEOPLE,BI.EXPERIENCE; DIRECT=none; WHY=implement position occupancy presentation without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_185`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_185`; `GOLDEN=TestTodo_WEB_185_Golden`; `BROWSER=TestTodo_WEB_185_Browser`; `CONFORMANCE=TestTodo_WEB_185_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement position occupancy presentation—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement position occupancy presentation—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [position domain](specs/position-and-headcount-domain.md).

- [ ] `WEB-186` **[PHASE_3][SOL_HIGH] Implement the headcount-plan workspace.**
  - **Depends:** `WEB-185`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.WORKFORCE,BI.PEOPLE,BI.EXPERIENCE; DIRECT=none; WHY=implement the headcount-plan workspace without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_186`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_186`; `GOLDEN=TestTodo_WEB_186_Golden`; `BROWSER=TestTodo_WEB_186_Browser`; `CONFORMANCE=TestTodo_WEB_186_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the headcount-plan workspace—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the headcount-plan workspace—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [position domain](specs/position-and-headcount-domain.md).

- [ ] `WEB-187` **[PHASE_3][SOL_HIGH] Implement workforce scenario authoring.**
  - **Depends:** `WEB-186`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.WORKFORCE,BI.PEOPLE,BI.EXPERIENCE; DIRECT=none; WHY=implement workforce scenario authoring without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_187`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_187`; `GOLDEN=TestTodo_WEB_187_Golden`; `BROWSER=TestTodo_WEB_187_Browser`; `CONFORMANCE=TestTodo_WEB_187_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement workforce scenario authoring—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement workforce scenario authoring—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [position domain](specs/position-and-headcount-domain.md).

- [ ] `WEB-188` **[PHASE_3][SOL_HIGH] Implement governed population building.**
  - **Depends:** `WEB-187`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.WORKFORCE,BI.PEOPLE,BI.EXPERIENCE; DIRECT=none; WHY=implement governed population building without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_188`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_188`; `GOLDEN=TestTodo_WEB_188_Golden`; `BROWSER=TestTodo_WEB_188_Browser`; `CONFORMANCE=TestTodo_WEB_188_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement governed population building—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement governed population building—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [position domain](specs/position-and-headcount-domain.md).

- [ ] `WEB-189` **[PHASE_3][SOL_HIGH] Implement cost and capacity simulation.**
  - **Depends:** `WEB-188`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.WORKFORCE,BI.PEOPLE,BI.EXPERIENCE; DIRECT=none; WHY=implement cost and capacity simulation without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_189`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_189`; `GOLDEN=TestTodo_WEB_189_Golden`; `BROWSER=TestTodo_WEB_189_Browser`; `CONFORMANCE=TestTodo_WEB_189_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement cost and capacity simulation—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement cost and capacity simulation—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [position domain](specs/position-and-headcount-domain.md).

- [ ] `WEB-190` **[PHASE_3][SOL_HIGH] Implement reorganization proposals.**
  - **Depends:** `WEB-189`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.WORKFORCE,BI.PEOPLE,BI.EXPERIENCE; DIRECT=none; WHY=implement reorganization proposals without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_190`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_190`; `GOLDEN=TestTodo_WEB_190_Golden`; `BROWSER=TestTodo_WEB_190_Browser`; `CONFORMANCE=TestTodo_WEB_190_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement reorganization proposals—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement reorganization proposals—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [position domain](specs/position-and-headcount-domain.md).

- [ ] `WEB-191` **[PHASE_3][SOL_HIGH] Distinguish planned state from committed truth.**
  - **Depends:** `WEB-190`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.WORKFORCE,BI.PEOPLE,BI.EXPERIENCE; DIRECT=none; WHY=distinguish planned state from committed truth without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_191`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_191`; `GOLDEN=TestTodo_WEB_191_Golden`; `BROWSER=TestTodo_WEB_191_Browser`; `CONFORMANCE=TestTodo_WEB_191_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—distinguish planned state from committed truth—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—distinguish planned state from committed truth—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [position domain](specs/position-and-headcount-domain.md).

- [ ] `WEB-192` **[PHASE_3][SOL_HIGH] Implement responsive organization exploration.**
  - **Depends:** `WEB-191`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.WORKFORCE,BI.PEOPLE,BI.EXPERIENCE; DIRECT=none; WHY=implement responsive organization exploration without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_192`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_192`; `GOLDEN=TestTodo_WEB_192_Golden`; `BROWSER=TestTodo_WEB_192_Browser`; `CONFORMANCE=TestTodo_WEB_192_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement responsive organization exploration—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement responsive organization exploration—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [position domain](specs/position-and-headcount-domain.md).

### HR help and cases

- [ ] `WEB-193` **[PHASE_3][SOL_HIGH] Implement the employee Help hub.**
  - **Depends:** `WEB-121`, `CASE-001`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.CASES,BI.PRIVACY,BI.EXPERIENCE; DIRECT=none; WHY=implement the employee help hub without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_193`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_193`; `GOLDEN=TestTodo_WEB_193_Golden`; `BROWSER=TestTodo_WEB_193_Browser`; `CONFORMANCE=TestTodo_WEB_193_Conformance`; `SECURITY=TestTodo_WEB_193_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the employee help hub—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the employee help hub—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-194` **[PHASE_3][SOL_HIGH] Implement authorized knowledge search.**
  - **Depends:** `WEB-193`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.CASES,BI.PRIVACY,BI.EXPERIENCE; DIRECT=none; WHY=implement authorized knowledge search without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_194`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_194`; `GOLDEN=TestTodo_WEB_194_Golden`; `BROWSER=TestTodo_WEB_194_Browser`; `CONFORMANCE=TestTodo_WEB_194_Conformance`; `SECURITY=TestTodo_WEB_194_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement authorized knowledge search—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement authorized knowledge search—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-195` **[PHASE_3][SOL_HIGH] Implement HR service-request intake.**
  - **Depends:** `WEB-194`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.CASES,BI.PRIVACY,BI.EXPERIENCE; DIRECT=none; WHY=implement hr service-request intake without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_195`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_195`; `GOLDEN=TestTodo_WEB_195_Golden`; `BROWSER=TestTodo_WEB_195_Browser`; `CONFORMANCE=TestTodo_WEB_195_Conformance`; `SECURITY=TestTodo_WEB_195_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement hr service-request intake—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement hr service-request intake—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-196` **[PHASE_3][SOL_HIGH] Implement confidential case intake.**
  - **Depends:** `WEB-195`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.CASES,BI.PRIVACY,BI.EXPERIENCE; DIRECT=none; WHY=implement confidential case intake without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_196`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_196`; `GOLDEN=TestTodo_WEB_196_Golden`; `BROWSER=TestTodo_WEB_196_Browser`; `CONFORMANCE=TestTodo_WEB_196_Conformance`; `SECURITY=TestTodo_WEB_196_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement confidential case intake—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement confidential case intake—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-197` **[PHASE_3][SOL_HIGH] Implement safe participant case status.**
  - **Depends:** `WEB-196`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.CASES,BI.PRIVACY,BI.EXPERIENCE; DIRECT=none; WHY=implement safe participant case status without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_197`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_197`; `GOLDEN=TestTodo_WEB_197_Golden`; `BROWSER=TestTodo_WEB_197_Browser`; `CONFORMANCE=TestTodo_WEB_197_Conformance`; `SECURITY=TestTodo_WEB_197_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement safe participant case status—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement safe participant case status—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-198` **[PHASE_3][SOL_HIGH] Implement restricted case messaging.**
  - **Depends:** `WEB-197`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.CASES,BI.PRIVACY,BI.EXPERIENCE; DIRECT=none; WHY=implement restricted case messaging without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_198`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_198`; `GOLDEN=TestTodo_WEB_198_Golden`; `BROWSER=TestTodo_WEB_198_Browser`; `CONFORMANCE=TestTodo_WEB_198_Conformance`; `SECURITY=TestTodo_WEB_198_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement restricted case messaging—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement restricted case messaging—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-199` **[PHASE_3][SOL_HIGH] Implement the specialist Case Center.**
  - **Depends:** `WEB-198`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.CASES,BI.PRIVACY,BI.EXPERIENCE; DIRECT=none; WHY=implement the specialist case center without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_199`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_199`; `GOLDEN=TestTodo_WEB_199_Golden`; `BROWSER=TestTodo_WEB_199_Browser`; `CONFORMANCE=TestTodo_WEB_199_Conformance`; `SECURITY=TestTodo_WEB_199_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the specialist case center—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the specialist case center—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-200` **[PHASE_3][SOL_HIGH] Implement case assignment and recusal.**
  - **Depends:** `WEB-199`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.CASES,BI.PRIVACY,BI.EXPERIENCE; DIRECT=none; WHY=implement case assignment and recusal without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_200`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_200`; `GOLDEN=TestTodo_WEB_200_Golden`; `BROWSER=TestTodo_WEB_200_Browser`; `CONFORMANCE=TestTodo_WEB_200_Conformance`; `SECURITY=TestTodo_WEB_200_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement case assignment and recusal—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement case assignment and recusal—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-201` **[PHASE_3][SOL_HIGH] Implement restricted case evidence review.**
  - **Depends:** `WEB-200`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.CASES,BI.PRIVACY,BI.EXPERIENCE; DIRECT=none; WHY=implement restricted case evidence review without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_201`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_201`; `GOLDEN=TestTodo_WEB_201_Golden`; `BROWSER=TestTodo_WEB_201_Browser`; `CONFORMANCE=TestTodo_WEB_201_Conformance`; `SECURITY=TestTodo_WEB_201_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement restricted case evidence review—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement restricted case evidence review—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-202` **[PHASE_3][SOL_HIGH] Implement case finding and disposition.**
  - **Depends:** `WEB-201`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.CASES,BI.PRIVACY,BI.EXPERIENCE; DIRECT=none; WHY=implement case finding and disposition without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_202`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_202`; `GOLDEN=TestTodo_WEB_202_Golden`; `BROWSER=TestTodo_WEB_202_Browser`; `CONFORMANCE=TestTodo_WEB_202_Conformance`; `SECURITY=TestTodo_WEB_202_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement case finding and disposition—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement case finding and disposition—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-203` **[PHASE_3][SOL_HIGH] Implement case appeal.**
  - **Depends:** `WEB-202`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.CASES,BI.PRIVACY,BI.EXPERIENCE; DIRECT=none; WHY=implement case appeal without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_203`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_203`; `GOLDEN=TestTodo_WEB_203_Golden`; `BROWSER=TestTodo_WEB_203_Browser`; `CONFORMANCE=TestTodo_WEB_203_Conformance`; `SECURITY=TestTodo_WEB_203_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement case appeal—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement case appeal—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-204` **[PHASE_3][SOL_HIGH] Prove case-view redaction and audit.**
  - **Depends:** `WEB-203`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.CASES,BI.PRIVACY,BI.EXPERIENCE; DIRECT=none; WHY=prove case-view redaction and audit without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_204`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_204`; `GOLDEN=TestTodo_WEB_204_Golden`; `BROWSER=TestTodo_WEB_204_Browser`; `CONFORMANCE=TestTodo_WEB_204_Conformance`; `SECURITY=TestTodo_WEB_204_Security`.
  - **RED:** the named test returns an accepted result when the declared behavior—prove case-view redaction and audit—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—prove case-view redaction and audit—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

### Exit and offboarding

- [ ] `WEB-205` **[PHASE_3][SOL_HIGH] Implement exit initiation.**
  - **Depends:** `WEB-121`, `WORKER-LIFE-004`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.LIFECYCLE,BI.ACCESS,BI.PAYROLL,BI.EXPERIENCE; DIRECT=none; WHY=implement exit initiation without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_205`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_205`; `GOLDEN=TestTodo_WEB_205_Golden`; `BROWSER=TestTodo_WEB_205_Browser`; `CONFORMANCE=TestTodo_WEB_205_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement exit initiation—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement exit initiation—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-206` **[PHASE_3][SOL_HIGH] Implement exit reason and effective-date collection.**
  - **Depends:** `WEB-205`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.LIFECYCLE,BI.ACCESS,BI.PAYROLL,BI.EXPERIENCE; DIRECT=none; WHY=implement exit reason and effective-date collection without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_206`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_206`; `GOLDEN=TestTodo_WEB_206_Golden`; `BROWSER=TestTodo_WEB_206_Browser`; `CONFORMANCE=TestTodo_WEB_206_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement exit reason and effective-date collection—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement exit reason and effective-date collection—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-207` **[PHASE_3][SOL_HIGH] Implement offboarding impact simulation.**
  - **Depends:** `WEB-206`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.LIFECYCLE,BI.ACCESS,BI.PAYROLL,BI.EXPERIENCE; DIRECT=none; WHY=implement offboarding impact simulation without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_207`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_207`; `GOLDEN=TestTodo_WEB_207_Golden`; `BROWSER=TestTodo_WEB_207_Browser`; `CONFORMANCE=TestTodo_WEB_207_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement offboarding impact simulation—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement offboarding impact simulation—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-208` **[PHASE_3][SOL_HIGH] Implement exit review and approval.**
  - **Depends:** `WEB-207`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.LIFECYCLE,BI.ACCESS,BI.PAYROLL,BI.EXPERIENCE; DIRECT=none; WHY=implement exit review and approval without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_208`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_208`; `GOLDEN=TestTodo_WEB_208_Golden`; `BROWSER=TestTodo_WEB_208_Browser`; `CONFORMANCE=TestTodo_WEB_208_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement exit review and approval—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement exit review and approval—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-209` **[PHASE_3][SOL_HIGH] Implement the offboarding plan.**
  - **Depends:** `WEB-208`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.LIFECYCLE,BI.ACCESS,BI.PAYROLL,BI.EXPERIENCE; DIRECT=none; WHY=implement the offboarding plan without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_209`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_209`; `GOLDEN=TestTodo_WEB_209_Golden`; `BROWSER=TestTodo_WEB_209_Browser`; `CONFORMANCE=TestTodo_WEB_209_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the offboarding plan—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the offboarding plan—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-210` **[PHASE_3][SOL_HIGH] Implement manager and work reassignment review.**
  - **Depends:** `WEB-209`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.LIFECYCLE,BI.ACCESS,BI.PAYROLL,BI.EXPERIENCE; DIRECT=none; WHY=implement manager and work reassignment review without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_210`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_210`; `GOLDEN=TestTodo_WEB_210_Golden`; `BROWSER=TestTodo_WEB_210_Browser`; `CONFORMANCE=TestTodo_WEB_210_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement manager and work reassignment review—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement manager and work reassignment review—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-211` **[PHASE_3][SOL_HIGH] Implement final-pay and benefit status.**
  - **Depends:** `WEB-210`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.LIFECYCLE,BI.ACCESS,BI.PAYROLL,BI.EXPERIENCE; DIRECT=none; WHY=implement final-pay and benefit status without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_211`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_211`; `GOLDEN=TestTodo_WEB_211_Golden`; `BROWSER=TestTodo_WEB_211_Browser`; `CONFORMANCE=TestTodo_WEB_211_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement final-pay and benefit status—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement final-pay and benefit status—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-212` **[PHASE_3][SOL_HIGH] Implement access and equipment reconciliation.**
  - **Depends:** `WEB-211`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.LIFECYCLE,BI.ACCESS,BI.PAYROLL,BI.EXPERIENCE; DIRECT=none; WHY=implement access and equipment reconciliation without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_212`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_212`; `GOLDEN=TestTodo_WEB_212_Golden`; `BROWSER=TestTodo_WEB_212_Browser`; `CONFORMANCE=TestTodo_WEB_212_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement access and equipment reconciliation—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement access and equipment reconciliation—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-213` **[PHASE_3][SOL_HIGH] Implement final-document delivery.**
  - **Depends:** `WEB-212`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.LIFECYCLE,BI.ACCESS,BI.PAYROLL,BI.EXPERIENCE; DIRECT=none; WHY=implement final-document delivery without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_213`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_213`; `GOLDEN=TestTodo_WEB_213_Golden`; `BROWSER=TestTodo_WEB_213_Browser`; `CONFORMANCE=TestTodo_WEB_213_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement final-document delivery—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement final-document delivery—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-214` **[PHASE_3][SOL_HIGH] Implement offboarding external-effect status.**
  - **Depends:** `WEB-213`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.LIFECYCLE,BI.ACCESS,BI.PAYROLL,BI.EXPERIENCE; DIRECT=none; WHY=implement offboarding external-effect status without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_214`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_214`; `GOLDEN=TestTodo_WEB_214_Golden`; `BROWSER=TestTodo_WEB_214_Browser`; `CONFORMANCE=TestTodo_WEB_214_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement offboarding external-effect status—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement offboarding external-effect status—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-215` **[PHASE_3][SOL_HIGH] Implement retained-obligation presentation.**
  - **Depends:** `WEB-214`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.LIFECYCLE,BI.ACCESS,BI.PAYROLL,BI.EXPERIENCE; DIRECT=none; WHY=implement retained-obligation presentation without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_215`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_215`; `GOLDEN=TestTodo_WEB_215_Golden`; `BROWSER=TestTodo_WEB_215_Browser`; `CONFORMANCE=TestTodo_WEB_215_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement retained-obligation presentation—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement retained-obligation presentation—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-216` **[PHASE_3][SOL_HIGH] Implement exit completion and correction.**
  - **Depends:** `WEB-215`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.LIFECYCLE,BI.ACCESS,BI.PAYROLL,BI.EXPERIENCE; DIRECT=none; WHY=implement exit completion and correction without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_216`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_216`; `GOLDEN=TestTodo_WEB_216_Golden`; `BROWSER=TestTodo_WEB_216_Browser`; `CONFORMANCE=TestTodo_WEB_216_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement exit completion and correction—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement exit completion and correction—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

### Insights and governed reporting

- [ ] `WEB-217` **[PHASE_2][TERRA] Implement the report catalog.**
  - **Depends:** `WEB-037`, `REPORT-001`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ANALYTICS,BI.INTELLIGENCE,BI.EXPERIENCE; DIRECT=none; WHY=implement the report catalog without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_217`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_217`; `GOLDEN=TestTodo_WEB_217_Golden`; `BROWSER=TestTodo_WEB_217_Browser`; `CONFORMANCE=TestTodo_WEB_217_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the report catalog—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the report catalog—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-218` **[PHASE_2][TERRA] Distinguish certified and customer reports.**
  - **Depends:** `WEB-217`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ANALYTICS,BI.INTELLIGENCE,BI.EXPERIENCE; DIRECT=none; WHY=distinguish certified and customer reports without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_218`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_218`; `GOLDEN=TestTodo_WEB_218_Golden`; `BROWSER=TestTodo_WEB_218_Browser`; `CONFORMANCE=TestTodo_WEB_218_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—distinguish certified and customer reports—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—distinguish certified and customer reports—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-219` **[PHASE_2][TERRA] Implement the analysis floorplan.**
  - **Depends:** `WEB-218`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ANALYTICS,BI.INTELLIGENCE,BI.EXPERIENCE; DIRECT=none; WHY=implement the analysis floorplan without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_219`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_219`; `GOLDEN=TestTodo_WEB_219_Golden`; `BROWSER=TestTodo_WEB_219_Browser`; `CONFORMANCE=TestTodo_WEB_219_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the analysis floorplan—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the analysis floorplan—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-220` **[PHASE_2][TERRA] Enforce authorized analysis filters.**
  - **Depends:** `WEB-219`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ANALYTICS,BI.INTELLIGENCE,BI.EXPERIENCE; DIRECT=none; WHY=enforce authorized analysis filters without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_220`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_220`; `GOLDEN=TestTodo_WEB_220_Golden`; `BROWSER=TestTodo_WEB_220_Browser`; `CONFORMANCE=TestTodo_WEB_220_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—enforce authorized analysis filters—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—enforce authorized analysis filters—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-221` **[PHASE_2][TERRA] Implement result lineage presentation.**
  - **Depends:** `WEB-220`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ANALYTICS,BI.INTELLIGENCE,BI.EXPERIENCE; DIRECT=none; WHY=implement result lineage presentation without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_221`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_221`; `GOLDEN=TestTodo_WEB_221_Golden`; `BROWSER=TestTodo_WEB_221_Browser`; `CONFORMANCE=TestTodo_WEB_221_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement result lineage presentation—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement result lineage presentation—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-222` **[PHASE_2][TERRA] Implement data-freshness presentation.**
  - **Depends:** `WEB-221`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ANALYTICS,BI.INTELLIGENCE,BI.EXPERIENCE; DIRECT=none; WHY=implement data-freshness presentation without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_222`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_222`; `GOLDEN=TestTodo_WEB_222_Golden`; `BROWSER=TestTodo_WEB_222_Browser`; `CONFORMANCE=TestTodo_WEB_222_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement data-freshness presentation—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement data-freshness presentation—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-223` **[PHASE_2][TERRA] Implement aggregate suppression states.**
  - **Depends:** `WEB-222`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ANALYTICS,BI.INTELLIGENCE,BI.EXPERIENCE; DIRECT=none; WHY=implement aggregate suppression states without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_223`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_223`; `GOLDEN=TestTodo_WEB_223_Golden`; `BROWSER=TestTodo_WEB_223_Browser`; `CONFORMANCE=TestTodo_WEB_223_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement aggregate suppression states—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement aggregate suppression states—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-224` **[PHASE_2][TERRA] Implement governed report export.**
  - **Depends:** `WEB-223`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ANALYTICS,BI.INTELLIGENCE,BI.EXPERIENCE; DIRECT=none; WHY=implement governed report export without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_224`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_224`; `GOLDEN=TestTodo_WEB_224_Golden`; `BROWSER=TestTodo_WEB_224_Browser`; `CONFORMANCE=TestTodo_WEB_224_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement governed report export—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement governed report export—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-225` **[PHASE_2][TERRA] Implement authorized report sharing.**
  - **Depends:** `WEB-224`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ANALYTICS,BI.INTELLIGENCE,BI.EXPERIENCE; DIRECT=none; WHY=implement authorized report sharing without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_225`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_225`; `GOLDEN=TestTodo_WEB_225_Golden`; `BROWSER=TestTodo_WEB_225_Browser`; `CONFORMANCE=TestTodo_WEB_225_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement authorized report sharing—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement authorized report sharing—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-226` **[PHASE_2][TERRA] Implement safe natural-language analysis.**
  - **Depends:** `WEB-225`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ANALYTICS,BI.INTELLIGENCE,BI.EXPERIENCE; DIRECT=none; WHY=implement safe natural-language analysis without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_226`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_226`; `GOLDEN=TestTodo_WEB_226_Golden`; `BROWSER=TestTodo_WEB_226_Browser`; `CONFORMANCE=TestTodo_WEB_226_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement safe natural-language analysis—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement safe natural-language analysis—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-227` **[PHASE_2][TERRA] Implement analysis-to-proposal handoff.**
  - **Depends:** `WEB-226`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ANALYTICS,BI.INTELLIGENCE,BI.EXPERIENCE; DIRECT=none; WHY=implement analysis-to-proposal handoff without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_227`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_227`; `GOLDEN=TestTodo_WEB_227_Golden`; `BROWSER=TestTodo_WEB_227_Browser`; `CONFORMANCE=TestTodo_WEB_227_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement analysis-to-proposal handoff—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement analysis-to-proposal handoff—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

- [ ] `WEB-228` **[PHASE_2][TERRA] Implement accessible data visualization.**
  - **Depends:** `WEB-227`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ANALYTICS,BI.INTELLIGENCE,BI.EXPERIENCE; DIRECT=none; WHY=implement accessible data visualization without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_228`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_228`; `GOLDEN=TestTodo_WEB_228_Golden`; `BROWSER=TestTodo_WEB_228_Browser`; `CONFORMANCE=TestTodo_WEB_228_Conformance`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement accessible data visualization—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement accessible data visualization—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [user-flow catalog](user-flows/catalog.md).

### Administration operations and release

- [ ] `WEB-229` **[GATE_C][SOL_HIGH] Implement the authorization-resolved Admin home.**
  - **Depends:** `WEB-037`, `DATAOPS-001`, `OBS-001`, `A11Y-001`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.OPERATIONS,BI.SECURITY,BI.EXPERIENCE; DIRECT=none; WHY=implement the authorization-resolved admin home without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_229`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_229`; `GOLDEN=TestTodo_WEB_229_Golden`; `BROWSER=TestTodo_WEB_229_Browser`; `CONFORMANCE=TestTodo_WEB_229_Conformance`; `SECURITY=TestTodo_WEB_229_Security`; `INTEGRATION=TestTodo_WEB_229_Integration`; `FAULT=TestTodo_WEB_229_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the authorization-resolved admin home—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the authorization-resolved admin home—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [execution plan](execution-plan.md).

- [ ] `WEB-230` **[GATE_C][SOL_HIGH] Implement the Policy Studio.**
  - **Depends:** `WEB-229`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.OPERATIONS,BI.SECURITY,BI.EXPERIENCE; DIRECT=none; WHY=implement the policy studio without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_230`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_230`; `GOLDEN=TestTodo_WEB_230_Golden`; `BROWSER=TestTodo_WEB_230_Browser`; `CONFORMANCE=TestTodo_WEB_230_Conformance`; `SECURITY=TestTodo_WEB_230_Security`; `INTEGRATION=TestTodo_WEB_230_Integration`; `FAULT=TestTodo_WEB_230_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the policy studio—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the policy studio—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [execution plan](execution-plan.md).

- [ ] `WEB-231` **[GATE_C][SOL_HIGH] Implement authorization-policy simulation.**
  - **Depends:** `WEB-230`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.OPERATIONS,BI.SECURITY,BI.EXPERIENCE; DIRECT=none; WHY=implement authorization-policy simulation without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_231`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_231`; `GOLDEN=TestTodo_WEB_231_Golden`; `BROWSER=TestTodo_WEB_231_Browser`; `CONFORMANCE=TestTodo_WEB_231_Conformance`; `SECURITY=TestTodo_WEB_231_Security`; `INTEGRATION=TestTodo_WEB_231_Integration`; `FAULT=TestTodo_WEB_231_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement authorization-policy simulation—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement authorization-policy simulation—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [execution plan](execution-plan.md).

- [ ] `WEB-232` **[GATE_C][SOL_HIGH] Implement the configuration center.**
  - **Depends:** `WEB-231`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.OPERATIONS,BI.SECURITY,BI.EXPERIENCE; DIRECT=none; WHY=implement the configuration center without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_232`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_232`; `GOLDEN=TestTodo_WEB_232_Golden`; `BROWSER=TestTodo_WEB_232_Browser`; `CONFORMANCE=TestTodo_WEB_232_Conformance`; `SECURITY=TestTodo_WEB_232_Security`; `INTEGRATION=TestTodo_WEB_232_Integration`; `FAULT=TestTodo_WEB_232_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the configuration center—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the configuration center—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [execution plan](execution-plan.md).

- [ ] `WEB-233` **[GATE_C][SOL_HIGH] Implement integration operations.**
  - **Depends:** `WEB-232`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.OPERATIONS,BI.SECURITY,BI.EXPERIENCE; DIRECT=none; WHY=implement integration operations without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_233`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_233`; `GOLDEN=TestTodo_WEB_233_Golden`; `BROWSER=TestTodo_WEB_233_Browser`; `CONFORMANCE=TestTodo_WEB_233_Conformance`; `SECURITY=TestTodo_WEB_233_Security`; `INTEGRATION=TestTodo_WEB_233_Integration`; `FAULT=TestTodo_WEB_233_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement integration operations—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement integration operations—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [execution plan](execution-plan.md).

- [ ] `WEB-234` **[GATE_C][SOL_HIGH] Implement the reconciliation and repair workbench.**
  - **Depends:** `WEB-233`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.OPERATIONS,BI.SECURITY,BI.EXPERIENCE; DIRECT=none; WHY=implement the reconciliation and repair workbench without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_234`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_234`; `GOLDEN=TestTodo_WEB_234_Golden`; `BROWSER=TestTodo_WEB_234_Browser`; `CONFORMANCE=TestTodo_WEB_234_Conformance`; `SECURITY=TestTodo_WEB_234_Security`; `INTEGRATION=TestTodo_WEB_234_Integration`; `FAULT=TestTodo_WEB_234_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—implement the reconciliation and repair workbench—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—implement the reconciliation and repair workbench—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [execution plan](execution-plan.md).

- [ ] `WEB-235` **[GATE_C][SOL_HIGH] Instrument privacy-safe frontend telemetry.**
  - **Depends:** `WEB-234`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.OPERATIONS,BI.SECURITY,BI.EXPERIENCE; DIRECT=none; WHY=instrument privacy-safe frontend telemetry without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_235`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_235`; `GOLDEN=TestTodo_WEB_235_Golden`; `BROWSER=TestTodo_WEB_235_Browser`; `CONFORMANCE=TestTodo_WEB_235_Conformance`; `SECURITY=TestTodo_WEB_235_Security`; `INTEGRATION=TestTodo_WEB_235_Integration`; `FAULT=TestTodo_WEB_235_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—instrument privacy-safe frontend telemetry—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—instrument privacy-safe frontend telemetry—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [execution plan](execution-plan.md).

- [ ] `WEB-236` **[GATE_C][SOL_HIGH] Enforce frontend performance budgets.**
  - **Depends:** `WEB-235`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.OPERATIONS,BI.SECURITY,BI.EXPERIENCE; DIRECT=none; WHY=enforce frontend performance budgets without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_236`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_236`; `GOLDEN=TestTodo_WEB_236_Golden`; `BROWSER=TestTodo_WEB_236_Browser`; `CONFORMANCE=TestTodo_WEB_236_Conformance`; `SECURITY=TestTodo_WEB_236_Security`; `INTEGRATION=TestTodo_WEB_236_Integration`; `FAULT=TestTodo_WEB_236_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—enforce frontend performance budgets—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—enforce frontend performance budgets—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [execution plan](execution-plan.md).

- [ ] `WEB-237` **[GATE_C][SOL_HIGH] Qualify the production browser matrix.**
  - **Depends:** `WEB-236`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.OPERATIONS,BI.SECURITY,BI.EXPERIENCE; DIRECT=none; WHY=qualify the production browser matrix without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_237`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_237`; `GOLDEN=TestTodo_WEB_237_Golden`; `BROWSER=TestTodo_WEB_237_Browser`; `CONFORMANCE=TestTodo_WEB_237_Conformance`; `SECURITY=TestTodo_WEB_237_Security`; `INTEGRATION=TestTodo_WEB_237_Integration`; `FAULT=TestTodo_WEB_237_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—qualify the production browser matrix—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—qualify the production browser matrix—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [execution plan](execution-plan.md).

- [ ] `WEB-238` **[GATE_C][SOL_HIGH] Qualify assistive-technology compatibility.**
  - **Depends:** `WEB-237`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.OPERATIONS,BI.SECURITY,BI.EXPERIENCE; DIRECT=none; WHY=qualify assistive-technology compatibility without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_238`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_238`; `GOLDEN=TestTodo_WEB_238_Golden`; `BROWSER=TestTodo_WEB_238_Browser`; `CONFORMANCE=TestTodo_WEB_238_Conformance`; `SECURITY=TestTodo_WEB_238_Security`; `INTEGRATION=TestTodo_WEB_238_Integration`; `FAULT=TestTodo_WEB_238_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—qualify assistive-technology compatibility—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—qualify assistive-technology compatibility—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [execution plan](execution-plan.md).

- [ ] `WEB-239` **[GATE_C][SOL_HIGH] Prove frontend disaster recovery.**
  - **Depends:** `WEB-238`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.OPERATIONS,BI.SECURITY,BI.EXPERIENCE; DIRECT=none; WHY=prove frontend disaster recovery without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_239`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_239`; `GOLDEN=TestTodo_WEB_239_Golden`; `BROWSER=TestTodo_WEB_239_Browser`; `CONFORMANCE=TestTodo_WEB_239_Conformance`; `SECURITY=TestTodo_WEB_239_Security`; `INTEGRATION=TestTodo_WEB_239_Integration`; `FAULT=TestTodo_WEB_239_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—prove frontend disaster recovery—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—prove frontend disaster recovery—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [execution plan](execution-plan.md).

- [ ] `WEB-240` **[GATE_C][SOL_HIGH] Gate the production frontend release.**
  - **Depends:** `WEB-239`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.OPERATIONS,BI.SECURITY,BI.EXPERIENCE; DIRECT=none; WHY=gate the production frontend release without creating a second source of business authority`.
  - **TEST:** `TestTodo_WEB_240`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_WEB_240`; `GOLDEN=TestTodo_WEB_240_Golden`; `BROWSER=TestTodo_WEB_240_Browser`; `CONFORMANCE=TestTodo_WEB_240_Conformance`; `SECURITY=TestTodo_WEB_240_Security`; `INTEGRATION=TestTodo_WEB_240_Integration`; `FAULT=TestTodo_WEB_240_Fault`.
  - **RED:** the named test returns an accepted result when the declared behavior—gate the production frontend release—is missing, stale, unauthorized, inaccessible, nondeterministic, or inconsistent with a controlling contract.
  - **GREEN:** the named test returns an exact deterministic result proving the declared behavior—gate the production frontend release—from versioned inputs with authorization-filtered outputs, accessible states, and retained evidence.
  - **REFACTOR:** keep presentation mechanics behind registered floorplan/widget contracts and keep domain, workflow, authorization, and transaction truth in their owning packages.
  - **Refs:** [frontend plan](specs/production-frontend-and-page-composition.md), [execution plan](execution-plan.md).

---

## 67. Default product slice alignment

> **Disposition:** Gate C. These tasks join experience, business semantics and PostgreSQL durability for the default-shipped product. They do not expand Gate A/B runtime authority.

A capability is default-ready only when its human job, semantic owner, authorized query/action, durable disposition, recovery route and version-bound evidence are admitted together. The governing contract is [Default Product Slice Alignment](specs/default-product-slice-alignment.md).

### Contract and registry

- [x] `ALIGN-001` **[GATE_C][TERRA] Define the machine-readable ProductSliceDefinition schema.**
  - **Evidence (2026-09-05):** `TestTodo_ALIGN_001` in `tools/planning/productslice`, `tools/planning/cmd/productslice` (the machine-readable ProductSliceDefinition schema (business intents, coverage features, pages, widgets, capabilities, Phase 1 packages, proving todos, jurisdictions, personas, exit criteria) validated against the live registries by name, canonical digest, a generator writing definitions/planning/product-slices.yaml for the Promotion slice from the real capability registry, coverage registry, page and widget registries, the live Phase 1 import graph and the todo registry, with a byte-for-byte drift test; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/planning/productslice/ ./tools/planning/cmd/productslice/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `WEB-001`, `DB-003`, `MODEL-009`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for define the machine readable productslicedefinition schema without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_001`; `PROPERTY=TestTodo_ALIGN_001_Property`; `GOLDEN=TestTodo_ALIGN_001_Golden`; `SECURITY=TestTodo_ALIGN_001_Security`; `CONFORMANCE=TestTodo_ALIGN_001_Conformance`.
  - **RED:** the named test accepts the `Define the machine-readable ProductSliceDefinition schema` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Define the machine-readable ProductSliceDefinition schema` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [x] `ALIGN-002` **[GATE_C][TERRA] Define the default product disposition vocabulary.**
  - **Evidence (2026-09-05):** `TestTodo_ALIGN_002` in `tools/planning/productslice` (default product disposition vocabulary over definitions/planning/product-slices.yaml; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/planning/productslice/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ALIGN-001`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for define the default product disposition vocabulary without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_002`; `PROPERTY=TestTodo_ALIGN_002_Property`; `GOLDEN=TestTodo_ALIGN_002_Golden`; `SECURITY=TestTodo_ALIGN_002_Security`; `CONFORMANCE=TestTodo_ALIGN_002_Conformance`.
  - **RED:** the named test accepts the `Define the default product disposition vocabulary` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Define the default product disposition vocabulary` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [x] `ALIGN-003` **[GATE_C][TERRA] Define the cross-layer identifier vocabulary.**
  - **Evidence (2026-09-07):** `TestTodo_ALIGN_003`, `TestTodo_ALIGN_003_Conformance`, `TestTodo_ALIGN_003_Golden`, `TestTodo_ALIGN_003_Property`, `TestTodo_ALIGN_003_Security` in `tools/planning/productslice` (written in 0a56524 on 2026-09-06 and never ticked; the named tests prove the GREEN contract: the named test deterministically proves the `Define the cross-layer identifier vocabulary` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence); `go test -count=1 ./tools/planning/productslice/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ALIGN-002`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for define the cross layer identifier vocabulary without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_003`; `PROPERTY=TestTodo_ALIGN_003_Property`; `GOLDEN=TestTodo_ALIGN_003_Golden`; `SECURITY=TestTodo_ALIGN_003_Security`; `CONFORMANCE=TestTodo_ALIGN_003_Conformance`.
  - **RED:** the named test accepts the `Define the cross-layer identifier vocabulary` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Define the cross-layer identifier vocabulary` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-004` **[GATE_C][TERRA] Define the cross-layer temporal vocabulary.**
  - **Depends:** `ALIGN-003`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for define the cross layer temporal vocabulary without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_004`; `PROPERTY=TestTodo_ALIGN_004_Property`; `GOLDEN=TestTodo_ALIGN_004_Golden`; `SECURITY=TestTodo_ALIGN_004_Security`; `CONFORMANCE=TestTodo_ALIGN_004_Conformance`.
  - **RED:** the named test accepts the `Define the cross-layer temporal vocabulary` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Define the cross-layer temporal vocabulary` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-005` **[GATE_C][TERRA] Define the multidimensional lifecycle projection vocabulary.**
  - **Depends:** `ALIGN-004`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for define the multidimensional lifecycle projection vocabulary without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_005`; `PROPERTY=TestTodo_ALIGN_005_Property`; `GOLDEN=TestTodo_ALIGN_005_Golden`; `SECURITY=TestTodo_ALIGN_005_Security`; `CONFORMANCE=TestTodo_ALIGN_005_Conformance`.
  - **RED:** the named test accepts the `Define the multidimensional lifecycle projection vocabulary` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Define the multidimensional lifecycle projection vocabulary` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-006` **[GATE_C][TERRA] Define the absence and disclosure-state vocabulary.**
  - **Depends:** `ALIGN-005`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for define the absence and disclosure state vocabulary without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_006`; `PROPERTY=TestTodo_ALIGN_006_Property`; `GOLDEN=TestTodo_ALIGN_006_Golden`; `SECURITY=TestTodo_ALIGN_006_Security`; `CONFORMANCE=TestTodo_ALIGN_006_Conformance`.
  - **RED:** the named test accepts the `Define the absence and disclosure-state vocabulary` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Define the absence and disclosure-state vocabulary` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-007` **[GATE_C][TERRA] Enforce product-slice ownership and consumer declarations.**
  - **Depends:** `ALIGN-006`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for enforce product slice ownership and consumer declarations without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_007`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_007`; `PROPERTY=TestTodo_ALIGN_007_Property`; `GOLDEN=TestTodo_ALIGN_007_Golden`; `SECURITY=TestTodo_ALIGN_007_Security`; `CONFORMANCE=TestTodo_ALIGN_007_Conformance`.
  - **RED:** the named test accepts the `Enforce product-slice ownership and consumer declarations` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Enforce product-slice ownership and consumer declarations` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [x] `ALIGN-008` **[GATE_C][TERRA] Generate the product-slice alignment registry.**
  - **Evidence (2026-09-05):** `TestTodo_ALIGN_008` in `tools/policy/tableinventory` (the named test deterministically proves the Generate the product-slice alignment registry contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/tableinventory/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ALIGN-007`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for generate the product slice alignment registry without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_008`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_008`; `PROPERTY=TestTodo_ALIGN_008_Property`; `GOLDEN=TestTodo_ALIGN_008_Golden`; `SECURITY=TestTodo_ALIGN_008_Security`; `CONFORMANCE=TestTodo_ALIGN_008_Conformance`.
  - **RED:** the named test accepts the `Generate the product-slice alignment registry` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Generate the product-slice alignment registry` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

### PostgreSQL disposition and invariants

- [x] `ALIGN-009` **[GATE_C][TERRA] Inventory default PostgreSQL tables by semantic role.**
  - **Evidence (2026-09-05):** `TestTodo_ALIGN_009` in `tools/policy/tableinventory` (the named test deterministically proves the Inventory default PostgreSQL tables by semantic role contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/tableinventory/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ALIGN-008`.
  - **INTENT CONTEXT:** `ROLE=DATA; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for inventory default postgresql tables by semantic role without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_009`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_009`; `PROPERTY=TestTodo_ALIGN_009_Property`; `GOLDEN=TestTodo_ALIGN_009_Golden`; `SECURITY=TestTodo_ALIGN_009_Security`; `CONFORMANCE=TestTodo_ALIGN_009_Conformance`.
  - **RED:** the named test accepts the `Inventory default PostgreSQL tables by semantic role` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Inventory default PostgreSQL tables by semantic role` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [x] `ALIGN-010` **[GATE_C][TERRA] Reject ownerless default PostgreSQL tables.**
  - **Evidence (2026-09-05):** `TestTodo_ALIGN_010` in `tools/policy/tableownership` (the named test deterministically proves the Reject ownerless default PostgreSQL tables contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/tableownership/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ALIGN-009`.
  - **INTENT CONTEXT:** `ROLE=DATA; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for reject ownerless default postgresql tables without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_010`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_010`; `PROPERTY=TestTodo_ALIGN_010_Property`; `GOLDEN=TestTodo_ALIGN_010_Golden`; `SECURITY=TestTodo_ALIGN_010_Security`; `CONFORMANCE=TestTodo_ALIGN_010_Conformance`.
  - **RED:** the named test accepts the `Reject ownerless default PostgreSQL tables` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Reject ownerless default PostgreSQL tables` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [x] `ALIGN-011` **[GATE_C][TERRA] Reject consumerless default PostgreSQL tables.**
  - **Evidence (2026-09-05):** `TestTodo_ALIGN_011` in `tools/policy/tableownership` (the named test deterministically proves the Reject consumerless default PostgreSQL tables contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/tableownership/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ALIGN-010`.
  - **INTENT CONTEXT:** `ROLE=DATA; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for reject consumerless default postgresql tables without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_011`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_011`; `PROPERTY=TestTodo_ALIGN_011_Property`; `GOLDEN=TestTodo_ALIGN_011_Golden`; `SECURITY=TestTodo_ALIGN_011_Security`; `CONFORMANCE=TestTodo_ALIGN_011_Conformance`.
  - **RED:** the named test accepts the `Reject consumerless default PostgreSQL tables` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Reject consumerless default PostgreSQL tables` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [x] `ALIGN-012` **[GATE_C][TERRA] Verify tenant cell and placement bindings.**
  - **Evidence (2026-09-05):** `TestTodo_ALIGN_012` in `tools/policy/placementbindings` (the named test deterministically proves the Verify tenant cell and placement bindings contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/placementbindings/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ALIGN-011`.
  - **INTENT CONTEXT:** `ROLE=DATA; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for verify tenant cell and placement bindings without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_012`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_012`; `PROPERTY=TestTodo_ALIGN_012_Property`; `GOLDEN=TestTodo_ALIGN_012_Golden`; `SECURITY=TestTodo_ALIGN_012_Security`; `CONFORMANCE=TestTodo_ALIGN_012_Conformance`.
  - **RED:** the named test accepts the `Verify tenant cell and placement bindings` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Verify tenant cell and placement bindings` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [x] `ALIGN-013` **[GATE_C][SOL_HIGH] Prove row-security and repository-scope parity.**
  - **Evidence (2026-09-05):** `TestTodo_ALIGN_013` in `tools/policy/rlsparity` (the named test deterministically proves the Prove row-security and repository-scope parity contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/rlsparity/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ALIGN-012`.
  - **INTENT CONTEXT:** `ROLE=DATA; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for prove row security and repository scope parity without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_013`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_013`; `PROPERTY=TestTodo_ALIGN_013_Property`; `GOLDEN=TestTodo_ALIGN_013_Golden`; `SECURITY=TestTodo_ALIGN_013_Security`; `INTEGRATION=TestTodo_ALIGN_013_Integration`; `FAULT=TestTodo_ALIGN_013_Fault`; `CONFORMANCE=TestTodo_ALIGN_013_Conformance`.
  - **RED:** the named test accepts the `Prove row-security and repository-scope parity` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Prove row-security and repository-scope parity` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [x] `ALIGN-014` **[GATE_C][TERRA] Enforce explicit SQL default semantics.**
  - **Evidence (2026-09-05):** `TestTodo_ALIGN_014` in `tools/policy/placementbindings` (the named test deterministically proves the Enforce explicit SQL default semantics contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/placementbindings/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ALIGN-013`.
  - **INTENT CONTEXT:** `ROLE=DATA; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for enforce explicit sql default semantics without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_014`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_014`; `PROPERTY=TestTodo_ALIGN_014_Property`; `GOLDEN=TestTodo_ALIGN_014_Golden`; `SECURITY=TestTodo_ALIGN_014_Security`; `CONFORMANCE=TestTodo_ALIGN_014_Conformance`.
  - **RED:** the named test accepts the `Enforce explicit SQL default semantics` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Enforce explicit SQL default semantics` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [x] `ALIGN-015` **[GATE_C][TERRA] Verify storage disposition and rebuild sources.**
  - **Evidence (2026-09-05):** `TestTodo_ALIGN_015` in `tools/policy/dispositionrebuild` (the named test deterministically proves the Verify storage disposition and rebuild sources contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/dispositionrebuild/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ALIGN-014`.
  - **INTENT CONTEXT:** `ROLE=DATA; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for verify storage disposition and rebuild sources without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_015`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_015`; `PROPERTY=TestTodo_ALIGN_015_Property`; `GOLDEN=TestTodo_ALIGN_015_Golden`; `SECURITY=TestTodo_ALIGN_015_Security`; `CONFORMANCE=TestTodo_ALIGN_015_Conformance`.
  - **RED:** the named test accepts the `Verify storage disposition and rebuild sources` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Verify storage disposition and rebuild sources` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [x] `ALIGN-016` **[GATE_C][TERRA] Generate the storage-to-product-slice reverse index.**
  - **Evidence (2026-09-05):** `TestTodo_ALIGN_016` in `tools/policy/storageproduct` (the named test deterministically proves the Generate the storage-to-product-slice reverse index contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/storageproduct/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ALIGN-015`.
  - **INTENT CONTEXT:** `ROLE=DATA; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for generate the storage to product slice reverse index without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_016`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_016`; `PROPERTY=TestTodo_ALIGN_016_Property`; `GOLDEN=TestTodo_ALIGN_016_Golden`; `SECURITY=TestTodo_ALIGN_016_Security`; `CONFORMANCE=TestTodo_ALIGN_016_Conformance`.
  - **RED:** the named test accepts the `Generate the storage-to-product-slice reverse index` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Generate the storage-to-product-slice reverse index` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

### Authorized query and projection path

- [x] `ALIGN-017` **[GATE_C][TERRA] Define the authorized product-query envelope.**
  - **Evidence (2026-09-05):** `TestTodo_ALIGN_017` in `internal/transport/queryenvelope` (the named test deterministically proves the Define the authorized product-query envelope contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/transport/queryenvelope/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ALIGN-016`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for define the authorized product query envelope without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_017`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_017`; `PROPERTY=TestTodo_ALIGN_017_Property`; `GOLDEN=TestTodo_ALIGN_017_Golden`; `SECURITY=TestTodo_ALIGN_017_Security`; `CONFORMANCE=TestTodo_ALIGN_017_Conformance`.
  - **RED:** the named test accepts the `Define the authorized product-query envelope` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Define the authorized product-query envelope` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-018` **[GATE_C][SOL_HIGH] Compile field dispositions into product queries.**
  - **Depends:** `ALIGN-017`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for compile field dispositions into product queries without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_018`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_018`; `PROPERTY=TestTodo_ALIGN_018_Property`; `GOLDEN=TestTodo_ALIGN_018_Golden`; `SECURITY=TestTodo_ALIGN_018_Security`; `INTEGRATION=TestTodo_ALIGN_018_Integration`; `FAULT=TestTodo_ALIGN_018_Fault`; `CONFORMANCE=TestTodo_ALIGN_018_Conformance`.
  - **RED:** the named test accepts the `Compile field dispositions into product queries` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Compile field dispositions into product queries` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-019` **[GATE_C][TERRA] Attach projection freshness to product responses.**
  - **Depends:** `ALIGN-018`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for attach projection freshness to product responses without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_019`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_019`; `PROPERTY=TestTodo_ALIGN_019_Property`; `GOLDEN=TestTodo_ALIGN_019_Golden`; `SECURITY=TestTodo_ALIGN_019_Security`; `CONFORMANCE=TestTodo_ALIGN_019_Conformance`.
  - **RED:** the named test accepts the `Attach projection freshness to product responses` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Attach projection freshness to product responses` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-020` **[GATE_C][TERRA] Map semantic absence states into product responses.**
  - **Depends:** `ALIGN-019`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for map semantic absence states into product responses without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_020`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_020`; `PROPERTY=TestTodo_ALIGN_020_Property`; `GOLDEN=TestTodo_ALIGN_020_Golden`; `SECURITY=TestTodo_ALIGN_020_Security`; `CONFORMANCE=TestTodo_ALIGN_020_Conformance`.
  - **RED:** the named test accepts the `Map semantic absence states into product responses` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Map semantic absence states into product responses` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-021` **[GATE_C][TERRA] Project currently available semantic actions.**
  - **Depends:** `ALIGN-020`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for project currently available semantic actions without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_021`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_021`; `PROPERTY=TestTodo_ALIGN_021_Property`; `GOLDEN=TestTodo_ALIGN_021_Golden`; `SECURITY=TestTodo_ALIGN_021_Security`; `CONFORMANCE=TestTodo_ALIGN_021_Conformance`.
  - **RED:** the named test accepts the `Project currently available semantic actions` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Project currently available semantic actions` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [x] `ALIGN-022` **[GATE_C][SOL_HIGH] Unify product-query transport semantics.**
  - **Evidence (2026-09-05):** `TestTodo_ALIGN_022` in `internal/transport/productquery` (the named test deterministically proves the Unify product-query transport semantics contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/transport/productquery/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ALIGN-021`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for unified product-query transport semantics without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_022`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_022`; `PROPERTY=TestTodo_ALIGN_022_Property`; `GOLDEN=TestTodo_ALIGN_022_Golden`; `SECURITY=TestTodo_ALIGN_022_Security`; `INTEGRATION=TestTodo_ALIGN_022_Integration`; `FAULT=TestTodo_ALIGN_022_Fault`; `CONFORMANCE=TestTodo_ALIGN_022_Conformance`.
  - **RED:** the named test accepts the `Unify product-query transport semantics` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Unify product-query transport semantics` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [x] `ALIGN-023` **[GATE_C][SOL_HIGH] Emit bounded authorized invalidation messages.**
  - **Evidence (2026-09-05):** `TestTodo_ALIGN_023` in `internal/transport/productquery` (the named test deterministically proves the Emit bounded authorized invalidation messages contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/transport/productquery/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ALIGN-022`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for emit bounded authorized invalidation messages without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_023`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_023`; `PROPERTY=TestTodo_ALIGN_023_Property`; `GOLDEN=TestTodo_ALIGN_023_Golden`; `SECURITY=TestTodo_ALIGN_023_Security`; `INTEGRATION=TestTodo_ALIGN_023_Integration`; `FAULT=TestTodo_ALIGN_023_Fault`; `CONFORMANCE=TestTodo_ALIGN_023_Conformance`.
  - **RED:** the named test accepts the `Emit bounded authorized invalidation messages` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Emit bounded authorized invalidation messages` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-024` **[GATE_C][SOL_HIGH] Require authoritative refetch after invalidation.**
  - **Depends:** `ALIGN-023`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for require authoritative refetch after invalidation without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_024`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_024`; `PROPERTY=TestTodo_ALIGN_024_Property`; `GOLDEN=TestTodo_ALIGN_024_Golden`; `SECURITY=TestTodo_ALIGN_024_Security`; `INTEGRATION=TestTodo_ALIGN_024_Integration`; `FAULT=TestTodo_ALIGN_024_Fault`; `CONFORMANCE=TestTodo_ALIGN_024_Conformance`.
  - **RED:** the named test accepts the `Require authoritative refetch after invalidation` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Require authoritative refetch after invalidation` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

### Governed work and mutation loop

- [ ] `ALIGN-025` **[GATE_C][TERRA] Define durable draft persistence semantics.**
  - **Depends:** `ALIGN-024`.
  - **INTENT CONTEXT:** `ROLE=ORCHESTRATION; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for define durable draft persistence semantics without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_025`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_025`; `PROPERTY=TestTodo_ALIGN_025_Property`; `GOLDEN=TestTodo_ALIGN_025_Golden`; `SECURITY=TestTodo_ALIGN_025_Security`; `CONFORMANCE=TestTodo_ALIGN_025_Conformance`.
  - **RED:** the named test accepts the `Define durable draft persistence semantics` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Define durable draft persistence semantics` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-026` **[GATE_C][TERRA] Bind product drafts to immutable proposal revisions.**
  - **Depends:** `ALIGN-025`.
  - **INTENT CONTEXT:** `ROLE=ORCHESTRATION; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for bind product drafts to immutable proposal revisions without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_026`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_026`; `PROPERTY=TestTodo_ALIGN_026_Property`; `GOLDEN=TestTodo_ALIGN_026_Golden`; `SECURITY=TestTodo_ALIGN_026_Security`; `CONFORMANCE=TestTodo_ALIGN_026_Conformance`.
  - **RED:** the named test accepts the `Bind product drafts to immutable proposal revisions` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Bind product drafts to immutable proposal revisions` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-027` **[GATE_C][TERRA] Prove simulation remains write-free.**
  - **Depends:** `ALIGN-026`.
  - **INTENT CONTEXT:** `ROLE=ORCHESTRATION; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for prove simulation remains write free without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_027`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_027`; `PROPERTY=TestTodo_ALIGN_027_Property`; `GOLDEN=TestTodo_ALIGN_027_Golden`; `SECURITY=TestTodo_ALIGN_027_Security`; `CONFORMANCE=TestTodo_ALIGN_027_Conformance`.
  - **RED:** the named test accepts the `Prove simulation remains write-free` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Prove simulation remains write-free` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-028` **[GATE_C][SOL_HIGH] Bind human decisions to proposal digests.**
  - **Depends:** `ALIGN-027`.
  - **INTENT CONTEXT:** `ROLE=ORCHESTRATION; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for bind human decisions to proposal digests without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_028`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_028`; `PROPERTY=TestTodo_ALIGN_028_Property`; `GOLDEN=TestTodo_ALIGN_028_Golden`; `SECURITY=TestTodo_ALIGN_028_Security`; `INTEGRATION=TestTodo_ALIGN_028_Integration`; `FAULT=TestTodo_ALIGN_028_Fault`; `CONFORMANCE=TestTodo_ALIGN_028_Conformance`.
  - **RED:** the named test accepts the `Bind human decisions to proposal digests` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Bind human decisions to proposal digests` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-029` **[GATE_C][SOL_HIGH] Project workflow lifecycle into product states.**
  - **Depends:** `ALIGN-028`.
  - **INTENT CONTEXT:** `ROLE=ORCHESTRATION; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for project workflow lifecycle into product states without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_029`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_029`; `PROPERTY=TestTodo_ALIGN_029_Property`; `GOLDEN=TestTodo_ALIGN_029_Golden`; `SECURITY=TestTodo_ALIGN_029_Security`; `INTEGRATION=TestTodo_ALIGN_029_Integration`; `FAULT=TestTodo_ALIGN_029_Fault`; `CONFORMANCE=TestTodo_ALIGN_029_Conformance`.
  - **RED:** the named test accepts the `Project workflow lifecycle into product states` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Project workflow lifecycle into product states` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-030` **[GATE_C][SOL_HIGH] Bind accepted actions to transaction plans.**
  - **Depends:** `ALIGN-029`.
  - **INTENT CONTEXT:** `ROLE=ORCHESTRATION; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for bind accepted actions to transaction plans without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_030`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_030`; `PROPERTY=TestTodo_ALIGN_030_Property`; `GOLDEN=TestTodo_ALIGN_030_Golden`; `SECURITY=TestTodo_ALIGN_030_Security`; `INTEGRATION=TestTodo_ALIGN_030_Integration`; `FAULT=TestTodo_ALIGN_030_Fault`; `CONFORMANCE=TestTodo_ALIGN_030_Conformance`.
  - **RED:** the named test accepts the `Bind accepted actions to transaction plans` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Bind accepted actions to transaction plans` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-031` **[GATE_C][SOL_HIGH] Enforce semantic idempotency for product submissions.**
  - **Depends:** `ALIGN-030`.
  - **INTENT CONTEXT:** `ROLE=ORCHESTRATION; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for enforce semantic idempotency for product submissions without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_031`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_031`; `PROPERTY=TestTodo_ALIGN_031_Property`; `GOLDEN=TestTodo_ALIGN_031_Golden`; `SECURITY=TestTodo_ALIGN_031_Security`; `INTEGRATION=TestTodo_ALIGN_031_Integration`; `FAULT=TestTodo_ALIGN_031_Fault`; `CONFORMANCE=TestTodo_ALIGN_031_Conformance`.
  - **RED:** the named test accepts the `Enforce semantic idempotency for product submissions` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Enforce semantic idempotency for product submissions` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-032` **[GATE_C][SOL_HIGH] Expose governed correction and repair routes.**
  - **Depends:** `ALIGN-031`.
  - **INTENT CONTEXT:** `ROLE=ORCHESTRATION; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for expose governed correction and repair routes without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_032`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_032`; `PROPERTY=TestTodo_ALIGN_032_Property`; `GOLDEN=TestTodo_ALIGN_032_Golden`; `SECURITY=TestTodo_ALIGN_032_Security`; `INTEGRATION=TestTodo_ALIGN_032_Integration`; `FAULT=TestTodo_ALIGN_032_Fault`; `CONFORMANCE=TestTodo_ALIGN_032_Conformance`.
  - **RED:** the named test accepts the `Expose governed correction and repair routes` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Expose governed correction and repair routes` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

### Durability and recovery

- [ ] `ALIGN-033` **[GATE_C][TERRA] Enforce append-oriented material history.**
  - **Depends:** `ALIGN-032`.
  - **INTENT CONTEXT:** `ROLE=DATA; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for enforce append oriented material history without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_033`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_033`; `PROPERTY=TestTodo_ALIGN_033_Property`; `GOLDEN=TestTodo_ALIGN_033_Golden`; `SECURITY=TestTodo_ALIGN_033_Security`; `CONFORMANCE=TestTodo_ALIGN_033_Conformance`.
  - **RED:** the named test accepts the `Enforce append-oriented material history` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Enforce append-oriented material history` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-034` **[GATE_C][TERRA] Enforce half-open effective intervals.**
  - **Depends:** `ALIGN-033`.
  - **INTENT CONTEXT:** `ROLE=DATA; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for enforce half open effective intervals without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_034`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_034`; `PROPERTY=TestTodo_ALIGN_034_Property`; `GOLDEN=TestTodo_ALIGN_034_Golden`; `SECURITY=TestTodo_ALIGN_034_Security`; `CONFORMANCE=TestTodo_ALIGN_034_Conformance`.
  - **RED:** the named test accepts the `Enforce half-open effective intervals` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Enforce half-open effective intervals` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-035` **[GATE_C][TERRA] Enforce exact monetary storage.**
  - **Depends:** `ALIGN-034`.
  - **INTENT CONTEXT:** `ROLE=DATA; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for enforce exact monetary storage without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_035`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_035`; `PROPERTY=TestTodo_ALIGN_035_Property`; `GOLDEN=TestTodo_ALIGN_035_Golden`; `SECURITY=TestTodo_ALIGN_035_Security`; `CONFORMANCE=TestTodo_ALIGN_035_Conformance`.
  - **RED:** the named test accepts the `Enforce exact monetary storage` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Enforce exact monetary storage` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-036` **[GATE_C][SOL_HIGH] Enforce optimistic concurrency on mutable coordination rows.**
  - **Depends:** `ALIGN-035`.
  - **INTENT CONTEXT:** `ROLE=DATA; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for enforce optimistic concurrency on mutable coordination rows without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_036`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_036`; `PROPERTY=TestTodo_ALIGN_036_Property`; `GOLDEN=TestTodo_ALIGN_036_Golden`; `SECURITY=TestTodo_ALIGN_036_Security`; `INTEGRATION=TestTodo_ALIGN_036_Integration`; `FAULT=TestTodo_ALIGN_036_Fault`; `CONFORMANCE=TestTodo_ALIGN_036_Conformance`.
  - **RED:** the named test accepts the `Enforce optimistic concurrency on mutable coordination rows` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Enforce optimistic concurrency on mutable coordination rows` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-037` **[GATE_C][SOL_HIGH] Prove atomic ledger projection and outbox persistence.**
  - **Depends:** `ALIGN-036`.
  - **INTENT CONTEXT:** `ROLE=DATA; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for prove atomic ledger projection and outbox persistence without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_037`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_037`; `PROPERTY=TestTodo_ALIGN_037_Property`; `GOLDEN=TestTodo_ALIGN_037_Golden`; `SECURITY=TestTodo_ALIGN_037_Security`; `INTEGRATION=TestTodo_ALIGN_037_Integration`; `FAULT=TestTodo_ALIGN_037_Fault`; `CONFORMANCE=TestTodo_ALIGN_037_Conformance`.
  - **RED:** the named test accepts the `Prove atomic ledger projection and outbox persistence` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Prove atomic ledger projection and outbox persistence` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-038` **[GATE_C][SOL_HIGH] Enforce storage classification and encryption policy.**
  - **Depends:** `ALIGN-037`.
  - **INTENT CONTEXT:** `ROLE=DATA; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for enforce storage classification and encryption policy without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_038`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_038`; `PROPERTY=TestTodo_ALIGN_038_Property`; `GOLDEN=TestTodo_ALIGN_038_Golden`; `SECURITY=TestTodo_ALIGN_038_Security`; `INTEGRATION=TestTodo_ALIGN_038_Integration`; `FAULT=TestTodo_ALIGN_038_Fault`; `CONFORMANCE=TestTodo_ALIGN_038_Conformance`.
  - **RED:** the named test accepts the `Enforce storage classification and encryption policy` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Enforce storage classification and encryption policy` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-039` **[GATE_C][SOL_HIGH] Enforce retention and disposition on product records.**
  - **Depends:** `ALIGN-038`.
  - **INTENT CONTEXT:** `ROLE=DATA; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for enforce retention and disposition on product records without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_039`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_039`; `PROPERTY=TestTodo_ALIGN_039_Property`; `GOLDEN=TestTodo_ALIGN_039_Golden`; `SECURITY=TestTodo_ALIGN_039_Security`; `INTEGRATION=TestTodo_ALIGN_039_Integration`; `FAULT=TestTodo_ALIGN_039_Fault`; `CONFORMANCE=TestTodo_ALIGN_039_Conformance`.
  - **RED:** the named test accepts the `Enforce retention and disposition on product records` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Enforce retention and disposition on product records` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-040` **[GATE_C][SOL_HIGH] Prove projection rebuild from authoritative chronology.**
  - **Depends:** `ALIGN-039`.
  - **INTENT CONTEXT:** `ROLE=DATA; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for prove projection rebuild from authoritative chronology without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_040`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_040`; `PROPERTY=TestTodo_ALIGN_040_Property`; `GOLDEN=TestTodo_ALIGN_040_Golden`; `SECURITY=TestTodo_ALIGN_040_Security`; `INTEGRATION=TestTodo_ALIGN_040_Integration`; `FAULT=TestTodo_ALIGN_040_Fault`; `CONFORMANCE=TestTodo_ALIGN_040_Conformance`.
  - **RED:** the named test accepts the `Prove projection rebuild from authoritative chronology` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Prove projection rebuild from authoritative chronology` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

### Default product definitions

- [ ] `ALIGN-041` **[GATE_C][TERRA] Seed platform semantic tokens and floorplans.**
  - **Depends:** `ALIGN-040`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for seed platform semantic tokens and floorplans without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_041`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_041`; `PROPERTY=TestTodo_ALIGN_041_Property`; `GOLDEN=TestTodo_ALIGN_041_Golden`; `SECURITY=TestTodo_ALIGN_041_Security`; `CONFORMANCE=TestTodo_ALIGN_041_Conformance`.
  - **RED:** the named test accepts the `Seed platform semantic tokens and floorplans` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Seed platform semantic tokens and floorplans` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-042` **[GATE_C][TERRA] Seed the authorization-resolved application shell.**
  - **Depends:** `ALIGN-041`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for seed the authorization resolved application shell without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_042`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_042`; `PROPERTY=TestTodo_ALIGN_042_Property`; `GOLDEN=TestTodo_ALIGN_042_Golden`; `SECURITY=TestTodo_ALIGN_042_Security`; `CONFORMANCE=TestTodo_ALIGN_042_Conformance`.
  - **RED:** the named test accepts the `Seed the authorization-resolved application shell` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Seed the authorization-resolved application shell` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-043` **[GATE_C][TERRA] Seed the governed work-loop pages.**
  - **Depends:** `ALIGN-042`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for seed the governed work loop pages without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_043`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_043`; `PROPERTY=TestTodo_ALIGN_043_Property`; `GOLDEN=TestTodo_ALIGN_043_Golden`; `SECURITY=TestTodo_ALIGN_043_Security`; `CONFORMANCE=TestTodo_ALIGN_043_Conformance`.
  - **RED:** the named test accepts the `Seed the governed work-loop pages` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Seed the governed work-loop pages` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-044` **[GATE_C][TERRA] Seed accessible failure and recovery pages.**
  - **Depends:** `ALIGN-043`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for seed accessible failure and recovery pages without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_044`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_044`; `PROPERTY=TestTodo_ALIGN_044_Property`; `GOLDEN=TestTodo_ALIGN_044_Golden`; `SECURITY=TestTodo_ALIGN_044_Security`; `CONFORMANCE=TestTodo_ALIGN_044_Conformance`.
  - **RED:** the named test accepts the `Seed accessible failure and recovery pages` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Seed accessible failure and recovery pages` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-045` **[GATE_C][TERRA] Register default routes against admitted capabilities.**
  - **Depends:** `ALIGN-044`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for register default routes against admitted capabilities without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_045`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_045`; `PROPERTY=TestTodo_ALIGN_045_Property`; `GOLDEN=TestTodo_ALIGN_045_Golden`; `SECURITY=TestTodo_ALIGN_045_Security`; `CONFORMANCE=TestTodo_ALIGN_045_Conformance`.
  - **RED:** the named test accepts the `Register default routes against admitted capabilities` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Register default routes against admitted capabilities` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-046` **[GATE_C][TERRA] Prove zero-override product usability.**
  - **Depends:** `ALIGN-045`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for prove zero override product usability without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_046`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_046`; `PROPERTY=TestTodo_ALIGN_046_Property`; `GOLDEN=TestTodo_ALIGN_046_Golden`; `SECURITY=TestTodo_ALIGN_046_Security`; `CONFORMANCE=TestTodo_ALIGN_046_Conformance`.
  - **RED:** the named test accepts the `Prove zero-override product usability` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Prove zero-override product usability` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-047` **[GATE_C][TERRA] Compile domain-pack activation manifests.**
  - **Depends:** `ALIGN-046`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for compile domain pack activation manifests without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_047`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_047`; `PROPERTY=TestTodo_ALIGN_047_Property`; `GOLDEN=TestTodo_ALIGN_047_Golden`; `SECURITY=TestTodo_ALIGN_047_Security`; `CONFORMANCE=TestTodo_ALIGN_047_Conformance`.
  - **RED:** the named test accepts the `Compile domain-pack activation manifests` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Compile domain-pack activation manifests` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [x] `ALIGN-048` **[GATE_C][SOL_HIGH] Reject unadmitted default feature activation.**
  - **Evidence (2026-09-05):** `TestTodo_ALIGN_048` in `tools/policy/defaultactivation` (the named test deterministically proves the Reject unadmitted default feature activation contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./tools/policy/defaultactivation/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ALIGN-047`.
  - **INTENT CONTEXT:** `ROLE=GOVERNANCE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for reject unadmitted default feature activation without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_048`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_048`; `PROPERTY=TestTodo_ALIGN_048_Property`; `GOLDEN=TestTodo_ALIGN_048_Golden`; `SECURITY=TestTodo_ALIGN_048_Security`; `INTEGRATION=TestTodo_ALIGN_048_Integration`; `FAULT=TestTodo_ALIGN_048_Fault`; `CONFORMANCE=TestTodo_ALIGN_048_Conformance`.
  - **RED:** the named test accepts the `Reject unadmitted default feature activation` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Reject unadmitted default feature activation` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

### Operations and evidence

- [x] `ALIGN-049` **[GATE_C][SOL_HIGH] Expose projection freshness to authorized operators.**
  - **Evidence (2026-09-05):** `TestTodo_ALIGN_049` in `internal/operations/authorizedhealth` (the named test deterministically proves the Expose projection freshness to authorized operators contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/operations/authorizedhealth/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ALIGN-048`.
  - **INTENT CONTEXT:** `ROLE=OPERATIONS; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for expose projection freshness to authorized operators without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_049`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_049`; `PROPERTY=TestTodo_ALIGN_049_Property`; `GOLDEN=TestTodo_ALIGN_049_Golden`; `SECURITY=TestTodo_ALIGN_049_Security`; `INTEGRATION=TestTodo_ALIGN_049_Integration`; `FAULT=TestTodo_ALIGN_049_Fault`; `CONFORMANCE=TestTodo_ALIGN_049_Conformance`.
  - **RED:** the named test accepts the `Expose projection freshness to authorized operators` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Expose projection freshness to authorized operators` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [x] `ALIGN-050` **[GATE_C][SOL_HIGH] Expose workflow and work-item health to authorized operators.**
  - **Evidence (2026-09-05):** `TestTodo_ALIGN_050` in `internal/operations/authorizedhealth` (the named test deterministically proves the Expose workflow and work-item health to authorized operators contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/operations/authorizedhealth/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ALIGN-049`.
  - **INTENT CONTEXT:** `ROLE=OPERATIONS; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for expose workflow and work item health to authorized operators without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_050`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_050`; `PROPERTY=TestTodo_ALIGN_050_Property`; `GOLDEN=TestTodo_ALIGN_050_Golden`; `SECURITY=TestTodo_ALIGN_050_Security`; `INTEGRATION=TestTodo_ALIGN_050_Integration`; `FAULT=TestTodo_ALIGN_050_Fault`; `CONFORMANCE=TestTodo_ALIGN_050_Conformance`.
  - **RED:** the named test accepts the `Expose workflow and work-item health to authorized operators` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Expose workflow and work-item health to authorized operators` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [x] `ALIGN-051` **[GATE_C][SOL_HIGH] Expose outbox and reconciliation health to authorized operators.**
  - **Evidence (2026-09-05):** `TestTodo_ALIGN_051` in `internal/operations/authorizedhealth` (the named test deterministically proves the Expose outbox and reconciliation health to authorized operators contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/operations/authorizedhealth/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ALIGN-050`.
  - **INTENT CONTEXT:** `ROLE=OPERATIONS; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for expose outbox and reconciliation health to authorized operators without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_051`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_051`; `PROPERTY=TestTodo_ALIGN_051_Property`; `GOLDEN=TestTodo_ALIGN_051_Golden`; `SECURITY=TestTodo_ALIGN_051_Security`; `INTEGRATION=TestTodo_ALIGN_051_Integration`; `FAULT=TestTodo_ALIGN_051_Fault`; `CONFORMANCE=TestTodo_ALIGN_051_Conformance`.
  - **RED:** the named test accepts the `Expose outbox and reconciliation health to authorized operators` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Expose outbox and reconciliation health to authorized operators` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-052` **[GATE_C][SOL_HIGH] Implement the bounded product-slice repair workbench.**
  - **Depends:** `ALIGN-051`.
  - **INTENT CONTEXT:** `ROLE=OPERATIONS; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for implement the bounded product slice repair workbench without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_052`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_052`; `PROPERTY=TestTodo_ALIGN_052_Property`; `GOLDEN=TestTodo_ALIGN_052_Golden`; `SECURITY=TestTodo_ALIGN_052_Security`; `INTEGRATION=TestTodo_ALIGN_052_Integration`; `FAULT=TestTodo_ALIGN_052_Fault`; `CONFORMANCE=TestTodo_ALIGN_052_Conformance`.
  - **RED:** the named test accepts the `Implement the bounded product-slice repair workbench` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Implement the bounded product-slice repair workbench` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-053` **[GATE_C][SOL_HIGH] Correlate product telemetry without protected payloads.**
  - **Depends:** `ALIGN-052`.
  - **INTENT CONTEXT:** `ROLE=OPERATIONS; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for correlate product telemetry without protected payloads without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_053`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_053`; `PROPERTY=TestTodo_ALIGN_053_Property`; `GOLDEN=TestTodo_ALIGN_053_Golden`; `SECURITY=TestTodo_ALIGN_053_Security`; `INTEGRATION=TestTodo_ALIGN_053_Integration`; `FAULT=TestTodo_ALIGN_053_Fault`; `CONFORMANCE=TestTodo_ALIGN_053_Conformance`.
  - **RED:** the named test accepts the `Correlate product telemetry without protected payloads` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Correlate product telemetry without protected payloads` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-054` **[GATE_C][SOL_HIGH] Record version-bound product-slice release evidence.**
  - **Depends:** `ALIGN-053`.
  - **INTENT CONTEXT:** `ROLE=OPERATIONS; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for record version bound product slice release evidence without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_054`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_054`; `PROPERTY=TestTodo_ALIGN_054_Property`; `GOLDEN=TestTodo_ALIGN_054_Golden`; `SECURITY=TestTodo_ALIGN_054_Security`; `INTEGRATION=TestTodo_ALIGN_054_Integration`; `FAULT=TestTodo_ALIGN_054_Fault`; `CONFORMANCE=TestTodo_ALIGN_054_Conformance`.
  - **RED:** the named test accepts the `Record version-bound product-slice release evidence` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Record version-bound product-slice release evidence` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-055` **[GATE_C][SOL_HIGH] Detect binary migration and definition skew.**
  - **Depends:** `ALIGN-054`.
  - **INTENT CONTEXT:** `ROLE=OPERATIONS; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for detect binary migration and definition skew without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_055`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_055`; `PROPERTY=TestTodo_ALIGN_055_Property`; `GOLDEN=TestTodo_ALIGN_055_Golden`; `SECURITY=TestTodo_ALIGN_055_Security`; `INTEGRATION=TestTodo_ALIGN_055_Integration`; `FAULT=TestTodo_ALIGN_055_Fault`; `CONFORMANCE=TestTodo_ALIGN_055_Conformance`.
  - **RED:** the named test accepts the `Detect binary migration and definition skew` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Detect binary migration and definition skew` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-056` **[GATE_C][SOL_HIGH] Prove product-slice rollback support.**
  - **Depends:** `ALIGN-055`.
  - **INTENT CONTEXT:** `ROLE=OPERATIONS; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for prove product slice rollback support without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_056`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_056`; `PROPERTY=TestTodo_ALIGN_056_Property`; `GOLDEN=TestTodo_ALIGN_056_Golden`; `SECURITY=TestTodo_ALIGN_056_Security`; `INTEGRATION=TestTodo_ALIGN_056_Integration`; `FAULT=TestTodo_ALIGN_056_Fault`; `CONFORMANCE=TestTodo_ALIGN_056_Conformance`.
  - **RED:** the named test accepts the `Prove product-slice rollback support` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Prove product-slice rollback support` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

### Cross-layer conformance and release

- [ ] `ALIGN-057` **[GATE_C][SOL_HIGH] Prove SSR and enhanced-browser semantic parity.**
  - **Depends:** `ALIGN-056`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for prove ssr and enhanced browser semantic parity without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_057`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_057`; `PROPERTY=TestTodo_ALIGN_057_Property`; `GOLDEN=TestTodo_ALIGN_057_Golden`; `SECURITY=TestTodo_ALIGN_057_Security`; `INTEGRATION=TestTodo_ALIGN_057_Integration`; `FAULT=TestTodo_ALIGN_057_Fault`; `CONFORMANCE=TestTodo_ALIGN_057_Conformance`.
  - **RED:** the named test accepts the `Prove SSR and enhanced-browser semantic parity` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Prove SSR and enhanced-browser semantic parity` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [x] `ALIGN-058` **[GATE_C][SOL_HIGH] Prove product-query transport parity.**
  - **Evidence (2026-09-05):** `TestTodo_ALIGN_058` in `internal/transport/conformance` (the named test deterministically proves the Prove product-query transport parity contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/transport/conformance/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ALIGN-057`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for product-query transport parity without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_058`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_058`; `PROPERTY=TestTodo_ALIGN_058_Property`; `GOLDEN=TestTodo_ALIGN_058_Golden`; `SECURITY=TestTodo_ALIGN_058_Security`; `INTEGRATION=TestTodo_ALIGN_058_Integration`; `FAULT=TestTodo_ALIGN_058_Fault`; `CONFORMANCE=TestTodo_ALIGN_058_Conformance`.
  - **RED:** the named test accepts the `Prove product-query transport parity` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Prove product-query transport parity` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [x] `ALIGN-059` **[GATE_C][SOL_HIGH] Prove cross-surface authorization noninterference.**
  - **Evidence (2026-09-05):** `TestTodo_ALIGN_059` in `internal/transport/conformance` (the named test deterministically proves the Prove cross-surface authorization noninterference contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence; written by a codex GPT-5.6 Luna lane and verified independently); `go test -count=1 ./internal/transport/conformance/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `ALIGN-058`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for prove cross surface authorization noninterference without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_059`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_059`; `PROPERTY=TestTodo_ALIGN_059_Property`; `GOLDEN=TestTodo_ALIGN_059_Golden`; `SECURITY=TestTodo_ALIGN_059_Security`; `INTEGRATION=TestTodo_ALIGN_059_Integration`; `FAULT=TestTodo_ALIGN_059_Fault`; `CONFORMANCE=TestTodo_ALIGN_059_Conformance`.
  - **RED:** the named test accepts the `Prove cross-surface authorization noninterference` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Prove cross-surface authorization noninterference` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-060` **[GATE_C][SOL_HIGH] Run the product-slice PostgreSQL chronology harness.**
  - **Depends:** `ALIGN-059`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for run the product slice postgresql chronology harness without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_060`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_060`; `PROPERTY=TestTodo_ALIGN_060_Property`; `GOLDEN=TestTodo_ALIGN_060_Golden`; `SECURITY=TestTodo_ALIGN_060_Security`; `INTEGRATION=TestTodo_ALIGN_060_Integration`; `FAULT=TestTodo_ALIGN_060_Fault`; `CONFORMANCE=TestTodo_ALIGN_060_Conformance`.
  - **RED:** the named test accepts the `Run the product-slice PostgreSQL chronology harness` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Run the product-slice PostgreSQL chronology harness` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-061` **[GATE_C][SOL_HIGH] Prove product state after restart and restore.**
  - **Depends:** `ALIGN-060`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for prove product state after restart and restore without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_061`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_061`; `PROPERTY=TestTodo_ALIGN_061_Property`; `GOLDEN=TestTodo_ALIGN_061_Golden`; `SECURITY=TestTodo_ALIGN_061_Security`; `INTEGRATION=TestTodo_ALIGN_061_Integration`; `FAULT=TestTodo_ALIGN_061_Fault`; `CONFORMANCE=TestTodo_ALIGN_061_Conformance`.
  - **RED:** the named test accepts the `Prove product state after restart and restore` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Prove product state after restart and restore` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-062` **[GATE_C][SOL_HIGH] Prove duplicate stale and concurrent action safety.**
  - **Depends:** `ALIGN-061`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for prove duplicate stale and concurrent action safety without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_062`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_062`; `PROPERTY=TestTodo_ALIGN_062_Property`; `GOLDEN=TestTodo_ALIGN_062_Golden`; `SECURITY=TestTodo_ALIGN_062_Security`; `INTEGRATION=TestTodo_ALIGN_062_Integration`; `FAULT=TestTodo_ALIGN_062_Fault`; `CONFORMANCE=TestTodo_ALIGN_062_Conformance`.
  - **RED:** the named test accepts the `Prove duplicate stale and concurrent action safety` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Prove duplicate stale and concurrent action safety` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-063` **[GATE_C][SOL_HIGH] Qualify accessibility and localization across the slice.**
  - **Depends:** `ALIGN-062`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for qualify accessibility and localization across the slice without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_063`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_063`; `PROPERTY=TestTodo_ALIGN_063_Property`; `GOLDEN=TestTodo_ALIGN_063_Golden`; `SECURITY=TestTodo_ALIGN_063_Security`; `INTEGRATION=TestTodo_ALIGN_063_Integration`; `FAULT=TestTodo_ALIGN_063_Fault`; `CONFORMANCE=TestTodo_ALIGN_063_Conformance`.
  - **RED:** the named test accepts the `Qualify accessibility and localization across the slice` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Qualify accessibility and localization across the slice` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

- [ ] `ALIGN-064` **[GATE_C][SOL_HIGH] Gate default product-slice release.**
  - **Depends:** `ALIGN-063`.
  - **INTENT CONTEXT:** `ROLE=CONFORMANCE; SETS=BI.ALL; DIRECT=none; WHY=provide cross-layer closure for gate default product slice release without transferring authority between presentation, business and persistence layers`.
  - **TEST:** `TestTodo_ALIGN_064`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_ALIGN_064`; `PROPERTY=TestTodo_ALIGN_064_Property`; `GOLDEN=TestTodo_ALIGN_064_Golden`; `SECURITY=TestTodo_ALIGN_064_Security`; `INTEGRATION=TestTodo_ALIGN_064_Integration`; `FAULT=TestTodo_ALIGN_064_Fault`; `CONFORMANCE=TestTodo_ALIGN_064_Conformance`.
  - **RED:** the named test accepts the `Gate default product-slice release` contract when its cross-layer behavior is missing, ownerless, unauthorized, stale, non-durable, unrecoverable, or inconsistent across UI, business contracts and PostgreSQL.
  - **GREEN:** the named test deterministically proves the `Gate default product-slice release` contract from versioned inputs with tenant isolation, explicit authority, exact persistence effects, safe presentation and retained evidence.
  - **REFACTOR:** keep canonical semantics with their domain or workflow owner, physical durability with the owning repository, and presentation mechanics behind registered page and widget contracts.
  - **Refs:** [default product alignment](specs/default-product-slice-alignment.md), [frontend plan](specs/production-frontend-and-page-composition.md), [modeling conventions](data/models/modeling-conventions.md).

## 68. Backend performance optimization

Measured hot spots from the 2026-09-07 baseline (`.artifacts/coverage/bench_baseline.txt` and the store, migration and regular-expression scans): every pool acquire pays three to four extra round trips of session hygiene, every HTTP edge request authenticates twice, twenty-two stores issue row-at-a-time statements inside loops, twenty-seven regular expressions compile per call, and 218 foreign keys have no supporting index. Each item below is atomic: it names the code, the benchmark that proves the change, and the behaviour that must not move.

- [x] `PERFOPT-001` **[GATE_B][SOL_HIGH] Collapse pool session hygiene into one round trip.**
  - **Evidence (2026-09-07):** `TestTodo_PERFOPT_001`, `TestTodo_PERFOPT_001_Security`, `TestTodo_PERFOPT_001_Integration`, `TestTodo_PERFOPT_001_StartupParameterEvidence`, `TestHygieneSQLSpellsOutEveryDiscardAllComponent`, `BenchmarkTodo_PERFOPT_001` in `internal/data/pgxadapter` (the BeforeAcquire hook now sends one simple-protocol message per acquire; PostgreSQL refuses DISCARD ALL inside the implicit transaction of a multi-statement message, so the message spells out every DISCARD ALL component plus RESET ROLE, pinned by the spec test; startup runtime parameters such as search_path are proven to survive the reset on PostgreSQL 17.5, so the per-parameter set_config round trips are gone; `Stats` exposes the hygiene round-trip counter and the existing leak tests still prove role, advisory locks, temp tables, prepared statements and settings are reset; acquire and release 139,966 to 40,367 ns/op, QueryRow 170,290 to 91,901 ns/op on the embedded server); `go test -count=1 ./internal/data/pgxadapter/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DB-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PERFOPT_001`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERFOPT_001`; `INTEGRATION=TestTodo_PERFOPT_001_Integration`; `SECURITY=TestTodo_PERFOPT_001_Security`; `BENCHMARK=BenchmarkTodo_PERFOPT_001`.
  - **RED:** the BeforeAcquire hook in `pgxadapter.NewPool` issues RESET ROLE, pg_advisory_unlock_all, DISCARD ALL and one set_config per runtime parameter as separate round trips on every acquire; the benchmark records the acquire cost and the round-trip count before the change.
  - **GREEN:** hygiene runs as a single simple-protocol statement per acquire, the benchmark shows the round-trip count fell to one (plus zero per runtime parameter when startup parameters are proven to survive DISCARD ALL, else one), and `TestPooledConnectionCannotLeakTenantRoleLocksOrSessionState` still proves role, advisory locks, temp tables, prepared statements and session settings are reset.
  - **REFACTOR:** keep the reset list in one place with the reason each item is there; no statement text is built from untrusted input.
  - **Refs:** [SLOs](specs/slo-sli-error-budget.md), [Master plan quantitative envelope](plan.md).

- [x] `PERFOPT-002` **[GATE_B][SOL_HIGH] Authenticate each edge request once.**
  - **Evidence (2026-09-07):** `TestTodo_PERFOPT_002`, `TestTodo_PERFOPT_002_Security`, `TestTodo_PERFOPT_002_Integration`, `BenchmarkTodo_PERFOPT_002` in `internal/transport/edge` and `TestPreAdmissionFingerprintMismatch`, `TestPreAdmissionMethodMismatch`, `TestPreAdmissionAbsentCarrier` in `internal/transport` (the strict JSON middleware stores its admission result in an unexported request-context carrier fingerprinted over the authorization, request-id and reserved metadata; `PreAdmit` reuses it only when method and fingerprint match, so a counting verifier sees one Verify per request, a tampered Authorization header is re-verified and refused, and non-JSON or unknown-path requests still authenticate in the interceptor; end-to-end request 50,456 ns/op, 279 allocs/op before, 34,751 ns/op, 245 allocs/op after); `go test -count=1 ./internal/transport/ ./internal/transport/edge/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `EDGE-002`.
  - **INTENT CONTEXT:** `ROLE=EXPOSURE; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PERFOPT_002`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERFOPT_002`; `SECURITY=TestTodo_PERFOPT_002_Security`; `INTEGRATION=TestTodo_PERFOPT_002_Integration`; `BENCHMARK=BenchmarkTodo_PERFOPT_002`.
  - **RED:** the strict JSON middleware in `internal/transport/edge` runs `transport.PreAdmit` and the connect admission interceptor runs `transport.Admit`, so the credential verifier and request-id resolution execute twice per HTTP request; a counting verifier in the test observes two Verify calls for one request.
  - **GREEN:** the middleware's admission result is carried to the interceptor in the request context and reused only when the metadata it was derived from is unchanged; the counting verifier observes one Verify call, a request that bypasses the middleware (non-JSON, unknown path) still authenticates in the interceptor, and a tampered Authorization header between the two stages is refused.
  - **REFACTOR:** the carrier type is unexported, immutable, and set only by the middleware; no handler can read or forge it.
  - **Refs:** [SLOs](specs/slo-sli-error-budget.md), [trusted request boundary](specs/trusted-request-boundary.md).

- [x] `PERFOPT-003` **[GATE_B][SOL_LOW] Compile regular expressions once and repair the reservation benchmark.**
  - **Evidence (2026-09-07):** `TestTodo_PERFOPT_003`, `TestScan_FixtureCatchesIndentedCompile`, `TestScan_MarkerExemptsOnlyItsOwnLine` in `tools/policy/regexhoist`, `TestTodo_PERFOPT_003_Golden`, `BenchmarkTodo_PERFOPT_003` in `internal/governance/legal/extract`, `TestCompileQualityPatternReusesCompiledPatterns`, `TestCompileQualityPatternCacheIsBounded` in `internal/intent/model` (the AST checker reports regexp compiles inside function bodies and package-level compiler aliases, exempting only lines marked `regexhoist:dynamic`; the real per-call sites were the DLP bank-detail pattern, two evidence annotation patterns and the quality FORMAT rule, now package-level or served from a bounded 512-entry cache; the remaining marked sites compile runtime schema or configuration input once per construction; the extractor golden digest is unchanged; `BenchmarkTodo_RESERVE_001` keys are now `strconv.Itoa` and it passes at any benchtime, 3,232 ns/op); `go test -count=1 ./tools/policy/regexhoist/ ./internal/governance/legal/extract/ ./internal/intent/model/ ./internal/trust/dlp/ ./internal/data/partition/ ./internal/domains/paymethod/ ./internal/customobject/ ./internal/domains/dataops/importing/ ./internal/resource/reservation/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PERF-ENV-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PERFOPT_003`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERFOPT_003`; `GOLDEN=TestTodo_PERFOPT_003_Golden`; `BENCHMARK=BenchmarkTodo_PERFOPT_003`.
  - **RED:** twenty-seven `regexp.MustCompile` calls sit inside function bodies in `internal/governance/legal/extract`, `internal/domains/dataops/importing`, `internal/intent/model`, `internal/customobject`, `internal/trust/dlp`, `internal/data/partition` and `internal/domains/paymethod`, so each call recompiles its pattern; `BenchmarkTodo_RESERVE_001` fails because a rune-cast loop counter yields duplicate idempotency keys.
  - **GREEN:** every fixed pattern is a package-level compiled value (patterns built from input keep a bounded cache or are documented as per-call), the golden test proves the extracted output is byte-identical before and after, benchmarks show the allocation count per call dropped, and the reservation benchmark passes at any benchtime.
  - **REFACTOR:** no behaviour change; a static check in the primary test scans the named packages for indented `regexp.MustCompile` so the pattern cannot return.
  - **Refs:** [Master plan quantitative envelope](plan.md).

- [x] `PERFOPT-004` **[GATE_B][SOL_HIGH] Batch row-at-a-time writes on the ledger and intent hot paths.**
  - **Evidence (2026-09-07):** `TestTodo_PERFOPT_004`, `TestTodo_PERFOPT_004_Integration`, `TestTodo_PERFOPT_004_Race`, `BenchmarkTodo_PERFOPT_004` in `internal/data/ledger`, `TestTodo_PERFOPT_004_ExecAllFallback`, `TestTodo_PERFOPT_004_ExecAllBatch`, `TestTodo_PERFOPT_004_ExecAllBatchError`, `TestFailedStatementNamesTheStatementThatDidNotComplete` in `internal/data/dbport`, `TestTodo_PERFOPT_004_AdapterBatchIntegration` in `internal/data/pgxadapter` (`dbport.Batcher` and `dbport.ExecAll` give stores one batch primitive with a loop fallback and `FailedStatement` names the statement that failed; `pgxadapter` Conn and Tx implement it over pgx SendBatch; the multi-stream append locks every head in one ordered `FOR UPDATE` query, validates every event before the first insert, and writes all event rows and head advances in one batch built by the same `planEvent` the single append uses, so the ledger_event row shape exists once; the checkpoint store, intentcontrol proposal and transaction writes and the outbox consumer batch their per-row loops; receipts pinned by a golden, stale second stream rolls everything back, overlapping appends on two connections resolve one winner with no deadlock; statements per three-stream ten-event append 51 to 33); `go test -count=1 ./internal/data/dbport/ ./internal/data/ledger/ ./internal/data/ledger/checkpoint/ ./internal/data/intentcontrol/ ./internal/data/outbox/ ./internal/data/pgxadapter/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DB-001`, `LEDGER-001`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PERFOPT_004`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERFOPT_004`; `INTEGRATION=TestTodo_PERFOPT_004_Integration`; `RACE=TestTodo_PERFOPT_004_Race`; `BENCHMARK=BenchmarkTodo_PERFOPT_004`.
  - **RED:** `dbport` exposes no batch or multi-row primitive, so the ledger multi-stream append, the ledger checkpoint store, the intentcontrol proposal and transaction writes and the outbox consumer issue one statement per row inside loops; the benchmark records statements per append for a ten-event, three-stream request.
  - **GREEN:** `dbport` gains an optional batch capability implemented by `pgxadapter` (pgx SendBatch in one round trip, results checked per statement), the named hot paths use it or a multi-row insert from unnest, the benchmark shows statements per append fell, and every existing ledger, checkpoint, intentcontrol and outbox test passes unchanged, including stale-head, duplicate-key and rollback behaviour.
  - **REFACTOR:** callers keep the same receipts and errors; a store without batch support falls back to the loop through one helper, not per-site branches.
  - **Refs:** [SLOs](specs/slo-sli-error-budget.md), [ledger contract](plan.md).

- [x] `PERFOPT-005` **[GATE_B][SOL_HIGH] Audit foreign keys against indexes from the live catalog and add the missing ones that queries use.**
  - **Evidence (2026-09-07):** `TestTodo_PERFOPT_005`, `TestTodo_PERFOPT_005_Integration`, `TestTodo_PERFOPT_005_Golden`, `TestAuditMatchesLeadingIndexColumns`, `TestReferencesAndPlanAreDeterministic` in `tools/policy/fkindex` (the audit reads pg_constraint and pg_index on embedded PostgreSQL with every migration applied, treating an index as covering only when its leading key columns equal the constraint's columns in order and resolving partition children to their parent; store SQL under `internal/data` is scanned for the constrained columns in WHERE, JOIN, ON or USING clauses; the golden pins 87 unindexed foreign keys before, 65 referenced ones now indexed by `migrations/00262_fk_indexes.sql` with the citing store line beside each index, and 22 unreferenced ones left unindexed by decision; the integration test proves the indexes exist and that EXPLAIN on the referencing queries uses them; `tools/policy/rlsparity` and `tools/policy/tableinventory` pass with the migration applied); `go test -count=1 ./tools/policy/fkindex/ ./tools/policy/rlsparity/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `DB-020`.
  - **INTENT CONTEXT:** `ROLE=SUBSTRATE; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PERFOPT_005`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERFOPT_005`; `INTEGRATION=TestTodo_PERFOPT_005_Integration`; `GOLDEN=TestTodo_PERFOPT_005_Golden`.
  - **RED:** a textual scan of `migrations/` finds 218 foreign-key constraints with no index whose leading columns cover the constraint; nothing in the repository proves which of them a store query actually filters or joins on.
  - **GREEN:** a policy package boots embedded PostgreSQL with every migration applied, reads the constraint and index catalogs, lists unindexed foreign keys, cross-references the SQL text under `internal/data` for those columns in WHERE, JOIN or ON clauses, pins the list as a golden, and a new migration adds `CREATE INDEX IF NOT EXISTS` for exactly the referenced ones with the reason per index; the integration test proves each new index exists and that EXPLAIN on the referencing query no longer sequential-scans.
  - **REFACTOR:** unreferenced foreign keys stay unindexed and appear in the golden marked unreferenced so the decision is visible; the audit runs in the policy suite, not per commit.
  - **Refs:** [PostgreSQL model](plan.md), [SLOs](specs/slo-sli-error-budget.md).

- [x] `PERFOPT-006` **[GATE_B][SOL_LOW] Cut allocations on the envelope, ledger evidence and cycle engine paths.**
  - **Evidence (2026-09-07):** `TestTodo_PERFOPT_006`, `TestTodo_PERFOPT_006_Golden`, `BenchmarkTodo_PERFOPT_006` in `internal/transport/envelope`, `internal/data/ledger/evidence` and `internal/engines/cycle` (allocation profiles taken before any change with `go tool pprof -top -sample_index=alloc_objects`; violations and proto detail slices are sized once, evidence caches UUID text, hashes strings through a reused scratch buffer and formats paths without fmt, the cycle explanation renders through a pre-sized builder; the goldens were generated from the pre-change code and pass unchanged against it in a HEAD worktree, so outputs are byte-identical; allocs/op 12 to 8 for envelope, 209 to 152 for evidence, 104 to 71 for cycle, each pinned by `testing.AllocsPerRun` ceilings); `go test -count=1 ./internal/transport/envelope/ ./internal/data/ledger/evidence/ ./internal/engines/cycle/` PASS on windows/arm64 (Go 1.26.3); branch plan-revision-2026-09-02.
  - **Depends:** `PERF-ENV-001`.
  - **INTENT CONTEXT:** `ROLE=DOMAIN_SUPPORT; SETS=BI.ALL; DIRECT=none; WHY=provide owned semantics, computation or effects consumed by the declared intent set`.
  - **TEST:** `TestTodo_PERFOPT_006`.
  - **TEST MATRIX:** `PRIMARY=TestTodo_PERFOPT_006`; `GOLDEN=TestTodo_PERFOPT_006_Golden`; `BENCHMARK=BenchmarkTodo_PERFOPT_006`.
  - **RED:** benchmarks with allocation reporting and a CPU and allocation profile over `internal/transport/envelope`, `internal/data/ledger/evidence` and `internal/engines/cycle` record the top allocation sites (string formatting in loops, unsized slices and maps, repeated marshalling) before any change.
  - **GREEN:** each top site is fixed (preallocation, a string builder, strconv over fmt.Sprintf, marshal once), the benchmark shows allocations per operation fell by at least a quarter on each package, and golden tests prove the produced envelopes, evidence text and cycle outputs are byte-identical.
  - **REFACTOR:** no exported signature changes; the profile command and the before and after numbers are recorded in the evidence line.
  - **Refs:** [Master plan quantitative envelope](plan.md).
