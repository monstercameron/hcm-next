# operations entity sources (MSRC-005).
#
# `covered: true` entities/properties/relationships here must match, field
# for field, internal/intent/model/catalog.go's compiled
# internal/intent/model.Catalog() registry (MSRC-002's Go anchor).
# tools/gen/schemaflux/sources.CrossCheckModel enforces this; a mismatch is
# reported, never silently accepted (per this repo's cross-check mandate).
#
# `covered: false` entities are drawn from operations-production.md's reconciliation/incident/recovery sections with no
# funded Go registration yet (see planning/data/models/README.md's coverage
# rule: "Coverage is claimed only for the fourteen drafted definitions...
# Everything else... carries no coverage claim"). They are encoded here so a
# future funded domain starts from a reviewed source rather than
# re-transcribing prose, and so the loader's cross-reference resolution has
# real, non-trivial structure to validate against. A `covered: false` entity
# or property must not declare status ACTIVE.

family: operations
family_version: 1
source_docs:
  - planning/data/models/operations-production.md#reconciliation-drift-and-repair
  - planning/data/models/operations-production.md#incidents-quarantine-and-availability
  - planning/data/models/operations-production.md#recovery-and-backup
entities:
  - name: ReconciliationRun
    version: 1
    key: reconciliation_run
    aliases: []
    owner_domain: OPERATIONS_ASSURANCE
    class: AGGREGATE_ROOT
    status: DRAFT
    covered: false
    tenant_scoped: true
    lifecycle_assignment: RevisionedFactLifecycle
    command_boundary: LOCAL_ACID
    source_ref: planning/data/models/operations-production.md#reconciliation-drift-and-repair
    summary: >-
      Policy/version, subject/effect/resource scope, expected/observed snapshot refs and started/completed/status with difference refs.
    properties:
      - name: status
        go_type: string
        schema_path: hcmnext.dataops.v1.ReconciliationRun.status
        presence: REQUIRED
        classification: INTERNAL
        temporal: POINT_IN_TIME
        authority_ref: authority.operations_extension/v1
        correction: APPENDS_CORRECTION
        retention_class_ref: OPERATIONAL_EVIDENCE
        status: DRAFT

  - name: DriftFinding
    version: 1
    key: drift_finding
    aliases: []
    owner_domain: OPERATIONS_ASSURANCE
    class: EVIDENCE
    status: DRAFT
    covered: false
    tenant_scoped: true
    lifecycle_assignment: ImmutableEvidenceLifecycle
    source_ref: planning/data/models/operations-production.md#reconciliation-drift-and-repair
    summary: >-
      Reconciliation/difference refs, mismatch type/suspected causes/confidence/impact/risk, affected intents/workflows/effects.
    properties:
      - name: mismatch_type
        go_type: string
        schema_path: hcmnext.dataops.v1.DriftFinding.mismatch_type
        presence: REQUIRED
        classification: INTERNAL
        temporal: IMMUTABLE
        authority_ref: authority.operations_extension/v1
        correction: IMMUTABLE_NO_CORRECTION
        retention_class_ref: OPERATIONAL_EVIDENCE
        status: DRAFT

  - name: RepairPlanRevision
    version: 1
    key: repair_plan_revision
    aliases: []
    owner_domain: OPERATIONS_ASSURANCE
    class: CHILD
    status: DRAFT
    covered: false
    tenant_scoped: true
    lifecycle_assignment: RevisionedFactLifecycle
    source_ref: planning/data/models/operations-production.md#reconciliation-drift-and-repair
    summary: >-
      Repair plan/discrepancy/expected-state refs, precondition/CAS snapshot, action DAG/capabilities/idempotency/reversibility.
    properties:
      - name: action_dag_digest
        go_type: string
        schema_path: hcmnext.dataops.v1.RepairPlanRevision.action_dag_digest
        presence: REQUIRED
        classification: INTERNAL
        temporal: IMMUTABLE
        authority_ref: authority.repair_plan/v1
        correction: IMMUTABLE_NO_CORRECTION
        retention_class_ref: OPERATIONAL_EVIDENCE
        status: DRAFT

  - name: RepairExecution
    version: 1
    key: repair_execution
    aliases: []
    owner_domain: OPERATIONS_ASSURANCE
    class: EVIDENCE
    status: DRAFT
    covered: false
    tenant_scoped: true
    lifecycle_assignment: ImmutableEvidenceLifecycle
    source_ref: planning/data/models/operations-production.md#reconciliation-drift-and-repair
    summary: >-
      Plan revision, lease/fence, action execution/attempt/provider receipt refs and ambiguity/cancellation/compensation/result.
    properties:
      - name: result_status
        go_type: string
        schema_path: hcmnext.dataops.v1.RepairExecution.result_status
        presence: REQUIRED
        classification: INTERNAL
        temporal: IMMUTABLE
        authority_ref: authority.repair_plan/v1
        correction: IMMUTABLE_NO_CORRECTION
        retention_class_ref: OPERATIONAL_EVIDENCE
        status: DRAFT

  - name: RepairVerification
    version: 1
    key: repair_verification
    aliases: []
    owner_domain: OPERATIONS_ASSURANCE
    class: EVIDENCE
    status: DRAFT
    covered: false
    tenant_scoped: true
    lifecycle_assignment: ImmutableEvidenceLifecycle
    source_ref: planning/data/models/operations-production.md#reconciliation-drift-and-repair
    summary: >-
      Execution/post-state observations, authority/watermark/residual differences and PASS|FAIL|PARTIAL|UNKNOWN result.
    properties:
      - name: result
        go_type: string
        schema_path: hcmnext.dataops.v1.RepairVerification.result
        presence: REQUIRED
        classification: INTERNAL
        temporal: IMMUTABLE
        authority_ref: authority.repair_plan/v1
        correction: IMMUTABLE_NO_CORRECTION
        retention_class_ref: OPERATIONAL_EVIDENCE
        status: DRAFT

  - name: IncidentSignal
    version: 1
    key: incident_signal
    aliases: []
    owner_domain: OPERATIONS_ASSURANCE
    class: EVENT
    status: DRAFT
    covered: false
    tenant_scoped: true
    lifecycle_assignment: ImmutableEvidenceLifecycle
    source_ref: planning/data/models/operations-production.md#incidents-quarantine-and-availability
    summary: >-
      Incident/source/type/severity/confidence, affected target/time/evidence and dedupe/correlation.
    properties:
      - name: severity
        go_type: string
        schema_path: hcmnext.dataops.v1.IncidentSignal.severity
        presence: REQUIRED
        classification: INTERNAL
        temporal: IMMUTABLE
        authority_ref: authority.operations_extension/v1
        correction: IMMUTABLE_NO_CORRECTION
        retention_class_ref: OPERATIONAL_EVIDENCE
        status: DRAFT

  - name: RecoveryContract
    version: 1
    key: recovery_contract
    aliases: []
    owner_domain: OPERATIONS_ASSURANCE
    class: VALUE
    status: DRAFT
    covered: false
    tenant_scoped: true
    lifecycle_assignment: RevisionedReferenceLifecycle
    source_ref: planning/data/models/operations-production.md#recovery-and-backup
    summary: >-
      Plane/store/service/authority RPO/RTO objectives and RESTORE|REBUILD|REPLAY mode with ordering/key/config/schema dependencies.
    properties:
      - name: mode
        go_type: string
        schema_path: hcmnext.dataops.v1.RecoveryContract.mode
        presence: REQUIRED
        classification: INTERNAL
        temporal: POINT_IN_TIME
        authority_ref: authority.operations_extension/v1
        correction: SUPERSEDES
        retention_class_ref: OPERATIONAL_EVIDENCE
        status: DRAFT

  - name: BackupPolicy
    version: 1
    key: backup_policy
    aliases: []
    owner_domain: OPERATIONS_ASSURANCE
    class: VALUE
    status: DRAFT
    covered: false
    tenant_scoped: true
    lifecycle_assignment: RevisionedReferenceLifecycle
    source_ref: planning/data/models/operations-production.md#recovery-and-backup
    summary: >-
      Plane/data class/tenant scope, full/incremental schedule, isolation/immutability/encryption and retention/RPO/verification rules.
    properties:
      - name: rpo_seconds
        go_type: uint32
        schema_path: hcmnext.dataops.v1.BackupPolicy.rpo_seconds
        presence: REQUIRED
        classification: INTERNAL
        temporal: POINT_IN_TIME
        authority_ref: authority.operations_extension/v1
        correction: SUPERSEDES
        retention_class_ref: OPERATIONAL_EVIDENCE
        status: DRAFT

relationships: []
